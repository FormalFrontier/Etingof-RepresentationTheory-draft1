import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter5.Lemma5183

#doc (Manual) "S^n U is spanned by u\u2297...\u2297u; S^n A is generated by Delta\\_n(a)" =>

# S^n U is spanned by u⊗...⊗u; S^n A is generated by Delta\_n(a)
%%%
tag := "Chapter5/Lemma5.18.3"
number := false
%%%

*Lemma 5.18.3.* _Let $`k` be a field of characteristic zero._

_(i) For any finite dimensional vector space $`U` over $`k`, the space $`S^n U` is spanned by elements of the form $`u \otimes \cdots \otimes u`, $`u \in U`._

_(ii) For any algebra $`A` over $`k`, the algebra $`S^n A` is generated by elements $`\Delta_n(a)`, $`a \in A`._

*Proof.* (i) The space $`S^n U` is an irreducible representation of $`GL(U)` (Problem 4.12.3). The subspace spanned by $`u \otimes \cdots \otimes u` is a nonzero subrepresentation, so it must be everything.

(ii) By the fundamental theorem on symmetric functions, there exists a polynomial $`P` with rational coefficients such that

$$`P(H_1(x), \ldots, H_n(x)) = x_1 \ldots x_n`

(where $`x = (x_1, \ldots, x_n)`). Then

$$`P(\Delta_n(a), \Delta_n(a^2), \ldots, \Delta_n(a^n)) = a \otimes \cdots \otimes a.`

The rest follows from (i). $`\square`
