import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter8.DiscussionAfterProblem828

#doc (Manual) "Tor and Ext in abelian categories with enough projectives" =>

# Tor and Ext in abelian categories with enough projectives
%%%
tag := "Chapter8/Discussion_after_Problem8.2.8"
number := false
%%%

In a similar way one can define Tor and Ext groups in any abelian category which has *enough projectives*; i.e., any object is a quotient of a projective object (this condition insures that every object has a projective resolution).
