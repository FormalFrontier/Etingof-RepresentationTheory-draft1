import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter3.Definition357

#doc (Manual) "Semisimple algebra" =>
# Semisimple algebra
%%%
tag := "Chapter3/Definition3.5.7"
number := false
%%%
**Definition 3.5.7.** A finite dimensional algebra $`A` is said to be **semisimple**[^Chapter3/Definition3.5.7/footnote-4] if $`\operatorname{Rad}(A) = 0`.

[^Chapter3/Definition3.5.7/footnote-4]: In particular, the algebra $`A = 0` is semisimple, although it is not simple. Every representation of this algebra is zero, so it does not have any irreducible or indecomposable representations. It is, nevertheless, a direct sum of an (empty) collection of matrix algebras.
