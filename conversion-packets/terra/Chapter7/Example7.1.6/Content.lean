import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter7.Example716

#doc (Manual) "Rep(A) enriched over k-vector spaces" =>

# Rep(A) enriched over k-vector spaces
%%%
tag := "Chapter7/Example7.1.6"
number := false
%%%

*Example 7.1.6.* The category $`\operatorname{Rep}(A)` of representations of a $`k`-algebra $`A` is enriched over the category of $`k`-vector spaces.
