import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter2.Definition222

#doc (Manual) "Unit in an associative algebra" =>
# Unit in an associative algebra
%%%
tag := "Chapter2/Definition2.2.2"
number := false
%%%
*Definition 2.2.2.* A *unit* in an associative algebra $`A` is an element $`1 \in A` such that $`1a = a1 = a`.
