import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter2.Proposition223

#doc (Manual) "Uniqueness of unit" =>
# Uniqueness of unit
%%%
tag := "Chapter2/Proposition2.2.3"
number := false
%%%
*Proposition 2.2.3.* _If a unit exists, it is unique._

*Proof.* Let $`1, 1'` be two units. Then $`1 = 11' = 1'`. $`\square`
