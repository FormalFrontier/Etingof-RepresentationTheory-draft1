import VersoManual

open Verso.Genre Manual

namespace IntroductionToRepresentationTheoryVerso.Content.Chapter2.Exercise295

#doc (Manual) "R^3 cross product as special case of so(3)" =>
# R^3 cross product as special case of so(3)
%%%
tag := "Chapter2/Exercise2.9.5"
number := false
%%%
**Exercise 2.9.5.** Show that example (1) is a special case of example (5) (for $`n = 3`).
