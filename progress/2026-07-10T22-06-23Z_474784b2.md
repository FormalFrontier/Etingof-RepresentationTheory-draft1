## Accomplished

Worked issue #6310 (Ch2 Problem 2.7.5(c): every finite-dim irreducible of the `q`-Weyl algebra
has `ℂ`-dimension `orderOf q`).

- Proved `finrank_irreducible_eq_orderOf` sorry-free in
  `EtingofRepresentationTheory/Chapter2/Problem2_7_5.lean`, mirroring the ordinary-Weyl proof
  `finrank_irreducible_charP` from `Problem2_7_4.lean`.
- Added private helpers in the same file:
  - `gMono_one`, `gMono_mul'` — subtype-level (`A = qWeylAlgebra ℂ q`) monomial arithmetic
    lifting `qMono_mul`.
  - `mem_center_of_comm_qMono` — an element commuting with every monomial `qMono p` is central
    (via the `kappa` monomial expansion).
  - `mem_of_invariant`, `central_smul_scalar` — the `q`-Weyl analogues of the Schur/invariant
    submodule helpers.
- Proof structure: obtain a `y`-eigenvector `v` (eigenvalue `lam ≠ 0`); the orbit
  `w m = xᵐ·v` gives distinct `y`-eigenvalues `qᵐ·lam`; `xⁿ` (`n = orderOf q`) is central hence
  a scalar `μ ≠ 0` by Schur, closing the orbit `n`-periodically; the `n` vectors `w 0 … w (n-1)`
  are independent (distinct eigenvalues) and their span is a nonzero `A`-submodule, so equals `V`
  by simplicity, giving `finrank = n`.
- `lake build EtingofRepresentationTheory.Chapter2.Problem2_7_5` succeeds.

## Current frontier

The file now has exactly one remaining `sorry`: `center_of_isOfFinOrder` (line ~448), the
part-(a) root-of-unity center computation, which #6310 explicitly scoped out.

## Overall project progress

Stage 3 formalization ongoing. Problem 2.7.5: parts (b), the not-root-of-unity parts of (a),
and now all of part (c) are proved sorry-free. Only the part-(a) center-of-a-root-of-unity case
remains.

## Next step

A future issue can tackle `center_of_isOfFinOrder` (part a, root-of-unity case): show the center
is the subalgebra generated by `xⁿ, x⁻ⁿ, yⁿ, y⁻ⁿ`. The `mem_center_of_comm_qMono` helper and the
monomial-support machinery already in the file should help.

## Blockers

None.
