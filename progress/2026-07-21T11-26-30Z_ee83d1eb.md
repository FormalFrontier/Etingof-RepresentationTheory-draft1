## Accomplished

Partially formalized the full dihedral-irrep classification (Problem 4.12.1(a), issue #7222),
adding the explicit construction and half the classification to
`EtingofRepresentationTheory/Chapter4/Problem4_12_1.lean` (sorry-free, axiom-clean —
propext/Classical.choice/Quot.sound only):

- `eigen`/`zeta_pow_card`/`isPrimitiveRoot_zeta` — eigenvalue-scalar infrastructure
  (`ζ^{jk}`, homomorphism in the argument, primitive-root facts).
- `repMat` + `repMat_mul`/`repMat_one` — the 2×2 matrix homomorphism (rotations diagonal
  `diag(ζ^{jk},ζ^{-jk})`, reflections antidiagonal).
- `Vrep N j : Representation ℂ (DihedralGroup N) (Fin 2 → ℂ)` — the explicit 2-dim rep
  (built via `Matrix.toLin'`; a genuine `def`, not sorried).
- `Vrep_irreducible (hj : 2*j ≠ 0)` — each `V_j` with `2j ≠ 0` is irreducible (`r 1` has
  distinct eigenvalues `ζ^{±j}`; the only eigenlines are the coordinate axes and `sr 0`
  swaps them; via `IsSimpleOrder (Subrepresentation _)`).
- `Vrep_trace_r` — character `χ_{V_j}(r k) = ζ^{jk} + ζ^{-jk}`.
- `Vrep_not_iso` — trace-based pairwise non-isomorphism criterion (an intertwining linear
  equiv forces equal trace at `r 1`, via `LinearMap.trace_conj'`).

This fixes the central non-vacuity complaint of the fidelity audit (#7223/#7219): the irrep
construction, irreducibility, and non-isomorphism content that was "entirely absent" now
exists. Part (b) `tensor_square_character` and `irreducible_dim` untouched.

## Current frontier

The 2-dim family is fully constructed, irreducible, and pairwise-distinguished. What remains
is the enumeration half: constructing the 1-dim characters, exhaustiveness, and the odd/even
totals.

## Overall project progress

Stage 3.7 fidelity remediation. Problem 4.12.1(a) upgraded from "gap (nothing constructed)" to
"partially_formalized" — construction + irreducibility + non-isomorphism done sorry-free;
exhaustiveness + counts remain.

## Next step

Issue #7225 (filed this turn) covers the remainder: (1) construct the 1-dim characters and
prove their count (2 for odd N, 4 for even N, via the abelianization, mirroring
`Problem4_12_2.one_dim_reps_card`); (2) exhaustiveness via
`exists_simples_sum_finrank_sq_eq_card` + the sum-of-squares pigeonhole (`Σ dᵢ²=2N`),
feeding `Vrep_not_iso` with the distinct-trace fact for `1 ≤ j < j' ≤ ⌊N/2⌋`; (3) the
odd/even totals as headline theorems.

## Blockers

None. Landed as a partial PR; #7225 tracks the remaining exhaustiveness/counts work.
