## Accomplished

Claimed #4598 (Ch5 sub-A1: GL-equivariant embedding of a weight-homogeneous
algebraic rep into `(V^⊗n)^m`). Landed the **interface** for it in
`Chapter5/PolynomialRepEmbedding.lean`:

- `polynomial_homog_rep_equivariant_embedding` — the FDRep-facing deliverable
  exactly as the issue specifies — is **zero-sorry**, reducing to the helper
  below via the existing `polynomialRep_embeds_in_tensorPower'`.
- `polynomialRep_homogeneous_hpoly'` — the det⁻¹-elimination helper that
  produces the bare-entry homogeneous `hpoly'` witness — is stated with a
  single documented `sorry` (the remaining mathematical core).

Added `import …Chapter5.Theorem5_22_1` (no cycle; `glWeightSpace` needed for
`h_homog`). Build of `PolynomialRepEmbedding` passes (one expected sorry).

## Current frontier

The hard core — proving weight spaces span an arbitrary algebraic rep (giving
`M.ρ (t•1) = t^n • id`) and the det⁻¹ elimination — is sorried and routed to a
focused successor.

## Overall project progress

Stage 3 (formalization) ongoing. #4598 interface complete; #2482 (Schur-Weyl #5)
can now consume `polynomial_homog_rep_equivariant_embedding`. Decomposed the
core into #4643.

## Next step

Pick up #4643 (`polynomialRep_homogeneous_hpoly'` body). Strategy is fully
written in that issue: Piece 1 (scalar-matrix scaling via weight-span — the
genuinely missing general lemma `⨆ μ, glWeightSpace = ⊤` for any algebraic rep)
and Piece 2 (det⁻¹ elimination via `eq_of_eval_eq_on_gl`).

## Blockers

None. #4643 depends-on #4598 (this PR); unblocks on merge.
