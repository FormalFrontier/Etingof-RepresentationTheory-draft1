## Accomplished
Worked issue #5602 (realization core `exists_dominantWeight_equivariant_realization`,
`Chapter5/Theorem5_23_2_PeterWeyl.lean`) — the genuinely-missing highest-weight-classification
step of the Cauchy/Peter-Weyl spanning argument, and the hardest remaining sorry in the chain.

Decomposed the monolithic `sorry` into **two cleanly-isolated sub-lemmas** and proved the
**assembly sorry-free** (steps 6-composition + step 7):

- `exists_detTwistNeg_schurModule_realization_of_simple` (steps 1–5, sorried, → #5606): a simple
  finite-dim `localRightRep`-subrep `S` is, after a `det^{-r}`-twist, equivariantly iso to a Schur
  module `SchurModuleSubmodule k n ν`. This is the analytic content (det-clearing → algebraic →
  weight-spanning → single-degree-homogeneous → character = `schurPoly n ν` → `M ≅ SchurModule k n ν`),
  mirroring `rightHull_isSemisimple` (`RightTranslationHullDecomp.lean:221-314`).
- `exists_dominantWeight_algIrrepGL_iso_detTwistNeg_schurModule` (step 6, sorried, → #5608): a
  `det^{-r}`-twisted Schur module is equivariantly iso to `algIrrepGLRepρ n lam k` for a concrete
  dominant weight `lam`. This isolates the subtle det-shift `DominantWeight` bookkeeping
  (`lam.shift = (r − ν(last)).toNat`, so `lam.toNatWeight = ν − ν(last)·1`), to be discharged via
  `schurModule_shift_iso_detTwist` (Prop 5.22.2) iterated `ν(last)` times — purely about Schur
  modules, does not touch `S`.

The main theorem now composes `f ∘ e : AlgIrrepGL n lam k ≃ₗ[k] S.toSubmodule` (intertwining
`algIrrepGLRepρ` with `S.toRepresentation`) and postcomposes the inclusion `S.toSubmodule ↪ R` to
build `ι`; equivariance via `LinearMap.coe_restrict_apply` and range via
`LinearMap.range_comp` / `LinearEquiv.range` / `Submodule.range_subtype`. All sorry-free.

`lake build EtingofRepresentationTheory.Chapter5.Theorem5_23_2_PeterWeyl` is green
(`Build completed successfully (8686 jobs)`).

## Current frontier
The realization core is reduced to exactly the two sorried sub-lemmas above (#5606, #5608). Both
are independently attackable and each has a complete, type-checked signature in the file plus a
step-by-step proof route in its docstring and issue body.

## Overall project progress
Stage 3 (formalization). The Peter-Weyl / Cauchy capstone chain
(`Theorem5_23_2_ii_equivariant`) reduces to `peterWeylMap_bijective`, which consumes the
realization core; the descent from the core to the matrix-coefficient supremum is already
sorry-free (#5603). With this PR, the core itself is reduced from one monolithic research-level
sorry to two well-scoped sub-lemmas with a proven assembly.

## Next step
Claim #5606 (analytic half — the larger of the two; mirror `rightHull_isSemisimple` closely and
additionally extract the formal character via `simple_constituent_formalCharacter_eq_schurPoly_mem`)
and #5608 (det-shift packaging — iterate `schurModule_shift_iso_detTwist`, compare
`ContragredientIdentity.lean`).

## Blockers
None. Both sub-lemmas are self-contained with all required infrastructure already in the project
(verified: `exists_invSelf_normalForm`, `boundedRightRep_isAlgebraic`,
`IsAlgebraicRepresentation.{restrict,of_linearEquiv,detTwist}`, `isSemisimpleModule_charTwistRep`,
`polyRightHomogeneousSubrep`, `simple_constituent_formalCharacter_eq_schurPoly_mem`,
`simpleRep_iso_schurModule_of_formalCharacter_eq`, `schurModule_shift_iso_detTwist`).
