## Accomplished

Feature session for issue #7257 — the "Wedderburn half" of the character-theory
counting bridge `#(irreducible ℂ-reps of G) = #(ConjClasses G)` (parent #7254).

New file `EtingofRepresentationTheory/Chapter5/WedderburnCenterFinrank.lean`,
imported into `Chapter5.lean`. Sorry-free, axiom-clean (only `propext`,
`Classical.choice`, `Quot.sound`). Headline declarations:

- `Etingof.finrank_center_eq_card_of_algEquiv_pi_matrix` — for a `k`-algebra `R`
  (field `k`) with `e : R ≃ₐ[k] Π i : ι, Matrix (Fin (d i)) (Fin (d i)) k`
  (`ι` a `Fintype`, each `d i ≠ 0`),
  `Module.finrank k ↥(Subalgebra.center k R) = Fintype.card ι`.
- `Etingof.exists_wedderburn_factors_eq_finrank_center` — Artin–Wedderburn over an
  algebraically closed field: a finite-dimensional semisimple algebra splits as a
  product of exactly `finrank_k Z(R)` matrix factors.
- `Etingof.finrank_center_monoidAlgebra_eq_wedderburn_factors` — the `MonoidAlgebra ℂ G`
  (char-0, alg-closed, finite `G`) specialization.

Supporting lemmas: `centerPiLinearEquiv` (`Z(Π S i) ≃ₗ[k] Π i, Z(S i)`),
`map_center_algEquiv` (center transports along an `AlgEquiv`), `finrank_center_congr`,
`finrank_center_matrix` (`finrank_k Z(Matrix n n k) = 1` for nonempty `n`).

## Current frontier

Issue #7257 complete. PR opened for `agent/2285c2f6`.

## Overall project progress

Stage 3.7 formalization ongoing. This adds the Wedderburn side of the counting
bridge; the group-algebra side (`finrank_k Z(k[G]) = #ConjClasses`) is in flight
as PR #7260 / issue #7258. Once both land, #7258 assembles the count and #7259
discharges the GL₂(𝔽_q) completeness hypothesis.

## Next step

Land PR #7260 (group-algebra center), then pick up #7258 to combine the two
dimension bridges into `#(irreducible ℂ-reps of G) = #(ConjClasses G)`.

## Blockers

None.
