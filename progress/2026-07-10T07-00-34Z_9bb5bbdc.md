## Accomplished

Substantial progress on issue #6032 — `Etingof.homTransportPlusEquivReduced` (kernel/transport
side of the Exercise 7.9.8 adjunction). In `Chapter7/Exercise7_9_8.lean`:

- Added reusable transport-as-`LinearEquiv` infrastructure that avoids the `HEq` plumbing the
  issue anticipated: `objTransportEquiv` / `objTransportEquiv_mapLinear` (general, by `subst`)
  and their `transportReversedTwice` specializations `transportReversedTwiceEquiv` /
  `transportReversedTwiceEquiv_mapLinear`. Packaging the vertex-space transport as an honest
  `LinearEquiv` turns the accessor `HEq`s into plain equations.
- Added arrow/reindex helpers: `reversedArrow_ne_ne_twice'`, `revOut_eq_reversedArrow_eq_ne`,
  `arrowsIntoReversed_source_ne`, `sourceArrowReindexEquiv` (`ArrowsOutOf Q i ≃ ArrowsInto Q̄ᵢ i`),
  `sinkMap_apply_eq_sum`, `sinkMap_lof`.
- Built the `Equiv` scaffold and **fully proved `toFun`** (all three `AdjReducedData` fields):
  - `h` (data), `compat` (A) via `g.naturality` + `reflFunctorPlus_mapLinear_ne_ne` + the
    twice lemma, and `constraint` (C) via `g.naturality` at `i` +
    `reflFunctorPlus_mapLinear_eq_ne`, reindexed to `ArrowsInto Q̄ᵢ i` and recognized as
    `sinkMap` of a kernel element (= 0).
- **Constructed `invFun.app`**: at `v ≠ i` via `(τ v hv).symm ∘ r.h v hv`; at `i` via
  `κ.symm ∘ LinearMap.codRestrict` of the source element `x ↦ ∑ a lof (reindex a) (r.h a.fst …)`
  into `ker(W.sinkMap i)`, with kernel membership proved from `r.constraint`.

The file builds (`lake build EtingofRepresentationTheory.Chapter7.Exercise7_9_8` succeeds).

## Current frontier

Three fields of `homTransportPlusEquivReduced` remain `sorry` (see `case nat`, `case li`,
`case ri`):
- `invFun`'s `naturality`,
- the `Equiv`'s `left_inv` and `right_inv`.

## Overall project progress

Stage 3 formalization ongoing. `Exercise7_9_8` (main) is assembled from `homFMinusEquivReduced`
(sorry, the F⁻ cokernel side — separate issue) and `homTransportPlusEquivReduced` (this issue,
now toFun+invFun.app complete). Both must be sorry-free for `Exercise7.9.8 → sorry_free`.

## Next step

Filed follow-up issue for the three remaining fields. To finish:
- **naturality**: case on `v,w` vs `i`. `w = i` is vacuous (`i` is a source: `(hi v).false e`).
  `v ≠ i, w ≠ i`: mirror the `toFun` `compat` proof (transport equiv +
  `reflFunctorPlus_mapLinear_ne_ne` + `r.compat` + `reversedArrow_ne_ne_twice'`). `v = i, w ≠ i`:
  the substantive source-map case (`reflFunctorPlus_mapLinear_eq_ne` + the `sourceEl`/`κ`
  round-trip). Note the built `app` is a `dite`; reduce with `dif_pos`/`dif_neg`.
- **left_inv/right_inv**: `AdjReducedData`/`QuiverRepresentationHom` extensionality; at `v ≠ i`
  the `τ`/`τ.symm` cancel; at `i` use injectivity of `κ` and `(W.sinkMap i).ker.subtype`.

## Blockers

None. The instance diamond (`W` lives over `reversedAtVertex Q i`, so `instAddCommMonoid`
doesn't auto-fire and `DirectSum`/`sinkMap` dot-notation resolves the ambient `Quiver`) is
handled by explicit `@`-applications and `letI acmW/modW`; the next worker should follow the
same pattern.
