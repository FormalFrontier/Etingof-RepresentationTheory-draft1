## Accomplished

Session type: **feature** (`/work` → `/feature`), issue **#6359**
(`not_finiteDimensional_g_four`, Ch2 Problem 2.16.3(b)).

Landed the explicit twisted `A₂⁽²⁾` loop realization in
`EtingofRepresentationTheory/Chapter2/Problem2_16_3.lean` (sorry-free, builds):

- `NX = E₂₀·t`, `NY = E₀₁ - E₁₂` in `Matrix (Fin 3) (Fin 3) (Polynomial k)`.
- `matHom₄ : FreeLieAlgebra k (Fin 2) →ₗ⁅k⁆ gl₃(k[t])` (`x↦NX`, `y↦NY`).
- `matHom₄_relator1` (`ad(x)²(y) ↦ 0`, from `NX² = 0`) and `matHom₄_relator2`
  (`ad(y)⁵(x) ↦ 0`, from `NY³ = 0 ⇒ ad(NY)⁵ = 0`) — both hold over any comm ring.
- `relIdeal_le_ker_matHom₄`: `matHom₄` factors through `𝔤₄`.

Added import `Mathlib.Algebra.Polynomial.AlgebraMap` (needed for
`Algebra k (Matrix _ _ k[X])` → `LieAlgebra k`).

Created follow-up **#6370** with the full crux strategy and the characteristic
obstruction I verified by hand (see below).

## Current frontier

`not_finiteDimensional_g_four` is still `sorry`. The `matHom₄` image is
infinite dimensional and proves the result **only in char 0 (or > 3)**.
Verified obstruction:
- **𝔽₃**: `ad(y)²x = E₀₀-2E₁₁+E₂₂` becomes the identity (central);
  `ad(y)³x, ad(y)⁴x` vanish (factors 3, 6). Image collapses to **4 dimensions**.
- **𝔽₂**: the `ℤ/2`-grading `𝔰𝔩₃ = 𝔰𝔬₃ ⊕ (module)` degenerates.
So `LinearMap.range matHom₄` is **finite** over `𝔽₂, 𝔽₃`; a characteristic-uniform
witness needs more (char-specific reps, or a ℤ-form/Chevalley-basis argument).

## Overall project progress

Stage 3 formalization, broad proof-fill wave. Ch2 Problem 2.16.3: parts (a)
`finrank_g_one/two = 3/4` sorry-free; `finrank_g_three = 6` in flight (#6340);
part (b) now has the verified explicit realization + factorization, main
theorem deferred to #6370.

## Next step

Claim **#6370**. Recommended: prove the `char ≠ 2,3` case via `matHom₄` (build
`ḡ` with `Submodule.liftQ … relIdeal_le_ker_matHom₄`, reduce
`Module.Finite (g k 4)` to finiteness of `LinearMap.range`, contradict with a
`t`-graded infinite family), then handle `char ∈ {2,3}` separately, OR invest
in the ℤ-form lower bound. Full details and the `ad(NY)^k(NX)` chain are in #6370.

## Blockers

None for the landed infra. The main theorem's remaining difficulty is the
characteristic-uniform infinite-dimensionality argument (tracked in #6370),
which is genuinely research-formalization-level.
