## Accomplished

Audited PRs #2945 (`embed_etilde6_in_tree_per_kQ` body) and #2947
(`embed_etilde7_in_tree_per_kQ` body) per issue #2949. Combined
report at
`progress/reviews/2026-05-19-embed-etilde6-and-etilde7-in-tree-per-kQ.md`
covers D1-D5 with explicit per-PR verdicts (all PASS).

D1 (signature fidelity): both landed bodies preserve the PR #2941
stub signature exactly (vertex tuple, edge hypotheses,
distinctness hypotheses, `F`/`Q`/`hOrient` block, conclusion).

D2 (distinctness lattice): Ẽ₆ derives all 21 pairs (6 edges + 6
triangle + 6 distance-3 + 3 distance-4); Ẽ₇ derives all 28 pairs
(7 + 7 + 6 + 5 + 2 + 1). Spot-checked apex/witness threading and
`acyclic_no_triangle` / `acyclic_path_nonadj` argument order on
representative entries; the 21-argument `path_nodup7` for the
distance-6 `e₂-e₃` non-edge verified exhaustively.

D3 (embedding map): Ẽ₆ map `0→v₀, 1→c₁, 2→d₁, 3→c₂, 4→d₂, 5→c₃,
6→d₃` and Ẽ₇ map `0→v₀, 1→u₁, 2→c₂, …, 7→e₃` confirmed against
`etilde6Adj` / `etilde7Adj` definitions
(`InfiniteTypeConstructions.lean:1246`, `:3435`). Fact lists in
both `hembed` proofs match the D2 inventory.

D4 (dispatch): both bodies match the `embed_t125_in_tree_per_kQ`
universal dispatch shape (`FieldGenericT125.lean:437-439`), only
the leaf swaps to `etilde{6,7}_not_finite_type_per_kQ`.

D5 (build): `lake build` of both targets green from
`7e4a8ab` (descendant of `aaaaaad`/`23e7a62`); only pre-existing
`sorry`s remain (`etilde{6,7}Rep_kQ_isIndecomposable`, wave-54
framework wall, both within the docstring bands flagged by the
issue body).

No follow-up issues required. Note left in the report flagging
the local-helper duplication (`path_nodup{4..7}` /
`path_edges{4..7}` now in three files); not action-worthy yet.

## Current frontier

Issue #2949 closed by this audit. The siblings to watch:

- #2955 (Case E + uncovered all-leaves of
  `non_adjacent_branches_leaf_case_per_kQ`) — flagged by its
  body as the trickiest remaining sub-issue; strong skip→replan
  candidate.
- #2967 (`d7tildeRep_kQ_isIndecomposable` body) — sizeable proof,
  precedent #2834 still in flight.
- #2564 — externally blocked on Mathlib PR #38583.

## Overall project progress

Chapter 6 field-generic infinite-type chain progressing steadily:
the leaf-case (`non_adjacent_branches_leaf_case_per_kQ`) is now
covered through Cases A/B/C+D + C.short subcases + the residual
C.short all-leaves chain.length=4 D̃₇ dispatch. Both ETilde6 /
ETilde7 embedders have audited bodies. Outstanding gates:
indecomposability proofs (d5tilde, d7tilde, etilde6, etilde7 —
all transitively gated on the wave-54 framework wall) and the
non-trivial Case E + uncovered-all-leaves logic of #2955.

## Next step

Next worker: pick from the remaining unclaimed queue. Likely
candidates are #2955 (probable skip→replan) or #2967 (defer or
decompose if scope exceeds session).

## Blockers

None for this audit. (External blocker on #2564 unchanged.)
