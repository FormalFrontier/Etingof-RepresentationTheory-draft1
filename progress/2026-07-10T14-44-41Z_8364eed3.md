## Accomplished

Assembled **both** general-`L` main theorems of Problem 3.8.4 (issue #6058), via the book's
Zariski-specialization route, in a new top-level file
`EtingofRepresentationTheory/Chapter3/Problem3_8_4_Main.lean`:

- part (i) `iso_of_baseChange_iso`;
- part (ii) `directSummand_of_baseChange_directSummand` (Noether-Deuring).

The two theorems were **moved out** of `Problem3_8_4.lean` (the base file of the import DAG):
proving them there would create an import cycle, since `Problem3_8_4_Finite.lean` and
`Problem3_8_4_Descent.lean` both import the base file. `Problem3_8_4.lean` now carries a pointer
comment; `Chapter3.lean` imports the new `Problem3_8_4_Main`.

Supporting sorry-free API added along the way:
- `Problem3_8_4_Finite.lean`: `bcRestrictScalarsMap` (map analog of `bcRestrictScalars`) and
  `directSummand_of_baseChange_directSummand_finite` (finite-extension direct-summand case, via
  `directSummand_pow_cancel`).
- `Problem3_8_4_Functoriality.lean`: `pushMap` (functorial pushforward of an `S⊗A`-linear map
  along `f : S →ₐ[K] T`), `pushMap_id`, `pushMap_comp`, and `exists_baseChange_directSummand`
  (pushforward of a split injection).

Added the split-injection descent scaffold
`Descent.exists_fg_subalgebra_baseChange_directSummand` (sorried, mirroring the iso descent).

## Current frontier

Both main theorems compile and are proved **modulo** the two descent lemmas in
`Problem3_8_4_Descent.lean`, which remain `sorry` scaffolds:
`exists_fg_subalgebra_baseChange_iso` (from #6057) and the new
`exists_fg_subalgebra_baseChange_directSummand`. `#print axioms` on both main theorems shows
`sorryAx` only through these two lemmas. `Problem3_8_4_Main.lean` itself introduces **no** sorry.
`lake build EtingofRepresentationTheory.Chapter3` succeeds.

## Overall project progress

Stage 3 formalization ongoing. `Chapter3/Problem3.8.4` moved from `statement_formalized` to
`proof_partial` in `progress/items.json` (assembly done; descent proofs pending, so not yet
`sorry_free`).

## Next step

Issue #6193 (created) covers proving the two descent lemmas following the turnkey route in the
`Problem3_8_4_Descent.lean` module docstring; landing both makes both main theorems `sorry_free`
and completes #6058's deliverable 3.

## Blockers

None. The descent proofs are a self-contained, tracked follow-up (#6193); they were always
scaffold-only (#6057's deliverable was "scaffold + turnkey proof route"), so the assembly here
rests on a pre-existing, intentional gap rather than a new one.
