## Accomplished

Completed issue #5004 (Ch5 #4946 sub-C1): general-`k` analogues of the two
Young-symmetrizer special-block endomorphism lemmas, sorry-free.

New file `Chapter5/SchurWeylSpecialBlockGeneral.lean` (imported by `Chapter5.lean`):

- `youngSym_action_vanishes_off_block_general` — `c_λ` vanishes on a simple
  `symGroupImage`-stable submodule whose Specht character is that of
  `la' ≠ weightToPartition N lam`.
- `youngSym_action_on_special_block_rank_one_scaled_proj_general` — on the
  special block `c_λ|_S = α • π` with `α ≠ 0`, `π² = π`, `finrank (range π) = 1`.
- Supporting general-`k` machinery: `spechtBlockActionK` / `spechtBlockCharacterK`
  (left-mult-by-`of σ` on `SpechtModuleK` and its trace — definitionally the
  bridge's `spechtModuleCharacterK`), `youngSym_trace_kronecker_K` (character
  orthogonality `∑_σ c_λ(σ)·χ_{la'}(σ) = α·δ`), plus the ported diagonal /
  off-diagonal / proportionality / trace-zero-idempotent helpers.

## Current frontier

Architectural decision worth recording for downstream work: the planner's
"generalize in place in `Theorem5_22_1.lean`" framing hits a **circular-import
wall**. The off-diagonal orthogonality genuinely needs general-`k` Specht
simplicity (`SpechtModuleK_isSimpleModule_general`) and distinctness
(`Theorem5_12_2_distinct_general`), both in files that *import*
`Theorem5_22_1.lean`. So the general-`k` versions had to land in a new
**downstream** file, not in `Theorem5_22_1.lean`. The ℂ originals are untouched.

The `h_label` hypothesis uses `spechtBlockCharacterK` (this file's local Specht
character) rather than #4991's `spechtModuleCharacterK`, to stay independent of
#4991/#4992 as the issue required. The two are **definitionally equal** (both are
`LinearMap.trace k _` of left-mult-by-`of σ` on `SpechtModuleK k n la`), so the
bridge-based consumer (#5005, blocked on #4992) can reconcile them by `rfl`.

## Overall project progress

Stage 3 (formalization) ongoing. This advances the general-`k` Schur-Weyl
simplicity chain (#4946): Sub-C special-block analysis now has its
parallelizable half (the two endomorphism lemmas) done. The sibling assembly
half (#5005: `exists_unique_special_block` + `SchurModuleSpecialBlock` helpers)
remains blocked on the general-`k` bridge #4992.

## Next step

- #5005 (assembly half) once #4992 lands: it will consume the two
  `*_general` lemmas here. When wiring it, note the `h_label` reconciliation
  (`spechtBlockCharacterK = spechtModuleCharacterK` by `rfl`).
- Eventually Sub-D (#4976) assembles `schurModule_isSimple_general` using the
  general-`k` special-block analysis.

## Blockers

None for this issue (delivered complete). Downstream #5005 stays blocked on
#4992 (general-`k` Specht bridge).
