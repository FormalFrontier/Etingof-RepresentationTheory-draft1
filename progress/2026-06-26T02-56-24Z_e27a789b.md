# Progress — 2026-06-26T02:56:24Z (session e27a789b)

Session type: one-shot `work` dispatch (`pod once 4847`), issue #4847 (label `feature`).

## Accomplished
- Claimed #4847 and triaged it. The issue is a **bug-record**: it documents that the
  pre-existing `kernelLemmaK'` statement (as landed by #4826) was mathematically false
  and records the corrective restatement over `GL_N`-invariant subrepresentations.
- Verified the corrective fix described in the issue is **already merged into `main`**
  via PR #4848 ("fix(Ch5 #4832): correct kernelLemmaK' — was false, restate over
  GL-invariant subreps", merged 2026-06-21):
  - `kernelLemmaK'` (in `Chapter5/KernelLemmaKPrimeAssembly.lean:168`) now takes
    `(W : Subrepresentation (quotDetTwistRep k N r))` and concludes `W = ⊥`.
  - `kernelLemmaK'_submodule` (`:183`) takes the explicit `GL_N`-invariance hypothesis
    `hW_inv` and is sorry-free (derived from the headline).
- Confirmed the corrected statement is in **active downstream use**, not dead:
  `Chapter5/KernelLemmaK.lean:167` consumes `kernelLemmaK'_submodule k N (r+1) … hinv hnn`,
  supplying the GL-invariance hypothesis as designed (the comment at `:106` cites #4847).
- Closed #4847 — every deliverable it describes (statement correction + bug record) has
  landed and is consumed correctly. No new code was required.

## Current frontier
The genuine research-level core of (K′) — every constituent of `k[Xᵢⱼ]/(det)` has
`ν_N = 0`, via the `GL×GL`-equivariant Cauchy decomposition — remains the single residual
`sorry` underneath the (now true) `kernelLemmaK'`. That is explicitly out of scope for
#4847 and is tracked separately (see `progress/kernel-lemma-K-route.md`).

## Overall project progress
Stage 3 formalization ongoing across chapters. Ch5 kernel-lemma-K statement now correct
and consumed; deep Cauchy-decomposition infrastructure still outstanding.

## Next step
A planner should ensure the remaining equivariant-Cauchy / highest-weight infrastructure
work (the residual `sorry` on `kernelLemmaK'`) has a live tracking issue, if not already.

## Blockers
None for #4847 (resolved). The deep Cauchy core is research-level work tracked elsewhere.
