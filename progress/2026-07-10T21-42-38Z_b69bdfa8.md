## Accomplished

Worked issue #6285 — Problem 5.16.3(a), `sumTranspositionsWith1_diagonalizable_integer_eigenvalues`.
Proved the algebraic core (steps 1–3 of the book hint `E = Cₙ − Cₙ₋₁`) sorry-free and
axiom-clean (`propext, Classical.choice, Quot.sound` only) in
`EtingofRepresentationTheory/Chapter5/Problem5_16_3.lean`:

- `sumTranspositionsWith1_eq_sub`: `E = sumTranspositions n − sumTranspositionsStab n`
  (split the transposition filter `{p.1<p.2}` by whether `p.1 = 0`).
- `permEmbZero` / `restrictRep` / `permEmbZero_swap`: the point-`0` stabilizer embedding
  `Sₘ ↪ Sₘ₊₁` (via `Fin.succ`) and its action on transpositions.
- `asAlgebraHom_sumTranspositionsStab`: `Cₙ₋₁` acts as `sumTranspositions m` under the
  restricted `Sₘ`-representation (reindex the stabilizer filter as a `Finset.map` of the
  `Fin m` filter).
- **`sumTranspositionsWith1_hasEigenvalue_integer`**: every eigenvalue of `T = A − B` is an
  integer. `A` commutes with `B` (`Cₙ` central), so `A` preserves each `T`-eigenspace `E_μ`;
  an `A`-eigenvector `w ∈ E_μ` has `A w = α w` (α a content ∈ ℤ, via #6284) and
  `B w = (α − μ) w` (α − μ a content ∈ ℤ), so `μ = α − (α − μ) ∈ ℤ`. Reuses #6284
  (`sumTranspositions_isSemisimple_and_integer_eigenvalues`) on both `ρ` and `restrictRep m ρ`.

The file now imports `Problem5_16_2` and `SumTranspositionsEigenvalues` (bringing the latter,
previously absent from the Chapter5 aggregator, into the CI build graph).

## Current frontier

The headline theorem `sumTranspositionsWith1_diagonalizable_integer_eigenvalues` still has 3
sorries: the eigenbasis (first conjunct) and the two range bounds `1 − n ≤ m`, `m ≤ n − 1`
(second conjunct). All three need the **unitarizability / self-adjoint** argument (step 4), which
is filed as sub-issue **#6311**. Integrality of each eigenvalue is already available from
`sumTranspositionsWith1_hasEigenvalue_integer`; only diagonalizability and the `|μ| ≤ n−1` bound
remain. Part (b) (`..._acts_scalar_iff_rectangular`) remains sorried (out of scope of #6285;
its own tracking is #6286).

## Overall project progress

Stage 3 (formalization). This lands the reusable integer-eigenvalue core of Problem 5.16.3(a).

## Next step

Take sub-issue #6311: build the `ρ`-invariant inner product (`Theorem4_6_2_existence`), show
`T = ∑_{0<j} ρ(swap 0 j)` is `LinearMap.IsSymmetric`, extract the eigenbasis
(`IsSymmetric.eigenvectorBasis`) and the bound (real eigenvalues + Cauchy-Schwarz over `n−1`
self-adjoint unitary summands), and combine with the proved integrality.

## Blockers

None. #6311 is unblocked once this partial PR merges (it depends-on #6285).
