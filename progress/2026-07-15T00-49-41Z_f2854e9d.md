## Accomplished

Review session for #6630 — independent review of the root-of-unity De Concini–Kac
dimension bound chain in `EtingofRepresentationTheory/Chapter2/Problem2_16_5.lean`
(four declarations from PRs #6621/#6627/#6620).

**Verdict: the chain is faithful, non-vacuous, and sorry-free. No correctness defects.**

Verified:
- **Build**: `lake build EtingofRepresentationTheory.Chapter2.Problem2_16_5` exits 0.
- **Axioms**: `#print axioms` on all four declarations
  (`finrank_irreducible_le_of_isOfFinOrder`, `finrank_le_of_epow_orderOf_eq_zero`,
  `finrank_le_of_fpow_orderOf_eq_zero`, `finrank_le_of_cyclic`) shows only
  `[propext, Classical.choice, Quot.sound]` — no `sorryAx`.
- **Statement faithfulness**: hypotheses (`IsOfFinOrder q`, `(q:ℂ)^2 ≠ 1`,
  `IsSimpleModule`, `FiniteDimensional`) match the book; the bound `dim V ≤ orderOf q`
  is a quantitative sharpening of Etingof 2.16.5's qualitative "dimensions are bounded".
  Assembly case split (`by_cases a=0`, then `b=0`) is exhaustive by construction; both
  scalars `a`, `b` are extracted from the real module action via
  `epow_orderOf_isScalar`/`fpow_orderOf_isScalar` (Schur), never assumed.
- **No vacuous content**: `Uqsl2` and generators are genuine (`RingQuot`/`FreeAlgebra`),
  `smul_mem_of_generators` is a real `FreeAlgebra.induction`, the spanning submodule `W`
  is a genuine `Submodule.span`. No `sorry`'d `def`/`instance`.
- **Soundness spot-check**: `e_pow_mul_f` (commutator induction) and `epow_orderOf_mul_f`
  (geometric sums vanish since `(q²)^ℓ = 1`, `q² ≠ 1`) are the genuine centrality
  computation; `central_isScalar` is a correct Schur's-lemma proof.
- **Flagged concern resolved**: `finrank_le_of_cyclic` genuinely never uses the
  `e^ℓ`-scalar hypotheses `ha`/`ha0`; the proof reaches the bound via f-bijectivity
  (`hb`, `hb0`) alone. This is a genuinely-stronger-than-needed true lemma, NOT a
  vacuity/fidelity problem.

**Cleanup applied**: prefixed the two unused binders `ha`→`_ha`, `ha0`→`_ha0` in
`finrank_le_of_cyclic` (line 1474) to document them as intentionally-kept (preserving
the faithful "both scalars nonzero" cyclic-case statement) while silencing the
`unusedVariables` linter. Positional call site in the assembly is unaffected; build
still exits 0 with those two warnings gone.

## Current frontier

Review complete. Remaining file warnings are pre-existing style-linter noise
(`linter.style.show` on defeq `show` tactics, a couple of >100-char lines) — out of
scope for this correctness review.

## Overall project progress

Chapter 2 Problem 2.16.5 root-of-unity dimension bound: reviewed and confirmed sound.
Both root-of-unity (this review) and non-root-of-unity (#6569) sides now have an
independent review pass.

## Next step

Pick up the remaining unclaimed feature issues (#6629 free-algebra homological
dimension, #6631 McKay Cartan positive semidefinite).

## Blockers

None.
