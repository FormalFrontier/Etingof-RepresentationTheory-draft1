## Accomplished

Feature session for **#6706** (`charval_A4_involution` — χ = −1 on involutions of the
3-dimensional A₄ irrep, the hard crux of #6704).

Proved `charval_A4_involution` **sorry-free** (`#print axioms` = `[propext,
Classical.choice, Quot.sound]`) in `Chapter5/Problem5_11_1.lean`:

```
lemma charval_A4_involution (H : Subgroup A5) (hH : Nat.card H = 12)
    (σ : FDRep ℂ ↥H) [Simple σ] (hdim : Module.finrank ℂ σ = 3)
    (h : ↥H) (hh2 : h ^ 2 = 1) (hh1 : h ≠ 1) : σ.character h = -1
```

Route (two invariant-space projections + Schur):
- **Integrality**: the idempotent `p = (1 + ρ(h))/2` has `trace p = (3+χ(h))/2 = finrank(range p) ∈ ℕ`
  (`LinearMap.IsProj.trace`), forcing `χ(h) ∈ {-3,-1,1,3}`.
- **/4 constraint**: averaging over the Klein four-group `V ⊴ H`
  (`FDRep.average_char_eq_finrank_invariants`) gives `(3+3χ(h))/4 = finrank(V-invariants) ∈ ℕ`,
  leaving `χ(h) ∈ {-1,3}`.
- **Rule out 3**: `χ(h)=3` forces `finrank(V-invariants)=3`, so `V ⊆ ker σ`; commutators land in
  `V` (`A4std_commutator_sq`), making the image abelian, so by Schur (`endo_scalar'`) every linear
  endomorphism is scalar — but `finrank(End σ)=9`. Contradiction. So `χ(h) = -1`.

New supporting infrastructure (all sorry-free):
- `A4std_invol_mul`, `A4std_commutator_sq`, `A4std_invol_conj` — decide-backed A₄ facts.
- `kleinV`, `kleinV_card` (= 4), `kleinV_char_const` (χ constant on involutions).
- `avg_char_subgroup` — character-average / invariants-dimension bridge for `K ≤ ↥H`.
- Local Schur `endo_scalar'` (+ finrank helpers), copied from the private version in
  `Chapter4/Problem4_5_2.lean`.

`lake build EtingofRepresentationTheory.Chapter5.Problem5_11_1` exits 0.

## Current frontier

`charval_A4_involution` landed. The only remaining `sorry` in the file is the pre-existing
`charval_A4_threeDim` (the assembly issue that consumes this lemma plus the 3-cycle value).

## Overall project progress

Chapter 5 Problem 5.11.1 part (d): the involution character value (the "hard crux") is now
sorry-free. `charval_A4_threeDim` (and hence `indA4_threeDim`) still has one `sorry`, awaiting the
assembly issue that combines `charval_A4_involution` with the 3-cycle value `χ = 0`.

## Next step

Assemble `charval_A4_threeDim` from `charval_A4_involution` (involutions → -1), `FDRep.char_one`
(identity → 3), and the 3-cycle value (χ = 0, via ⟨χ,χ⟩ = 1 over H). That is the natural follow-up
issue.

## Blockers

None.
