## Accomplished

Feature session for issue #6625 (Ch6 Problem 6.1.6 part (e) + second half of (c)).
Proved both target theorems in `EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean`
sorry-free:

- `mckay_dims_are_marks` — the irreducible-dimension vector spans the kernel of the
  McKay Cartan matrix: `∑ⱼ (2δᵢⱼ − rᵢⱼ) dⱼ = 0`.
- `mckayCartan_not_posDef` — the dimension vector is a nonzero null vector, so the
  Cartan form is not positive definite.

Supporting reusable infrastructure added to the same file:

- `char_eq_sum_mult` — character decomposition (complete reducibility over ℂ):
  `χ_S = ∑ⱼ finrank(Wⱼ ⟶ S) • χ_{Wⱼ}`, via `Etingof.classFunction_eq_zero_of_orthogonal_simples`
  (required a new import of `Chapter4.Theorem4_2_1`), `scalar_product_char_eq_finrank_equivariant`,
  and `char_orthonormal`.
- `finrank_eq_sum_mult` — the dimension count `dim S = ∑ⱼ finrank(Wⱼ ⟶ S)·dim Wⱼ`,
  obtained by evaluating `char_eq_sum_mult` at `1 ∈ G` with `FDRep.char_one`.
- `mckay_marks_aux` — the marks identity, used by both public theorems (needed since
  `mckayCartan_not_posDef` precedes `mckay_dims_are_marks` in the file).

`#print axioms` on both public theorems shows only `propext, Classical.choice, Quot.sound`
(no `sorryAx`). `lake build EtingofRepresentationTheory.Chapter6.Problem6_1_6` succeeds.
No `def`/`abbrev`/`instance` was sorried.

Also recorded the reusable dimension-count recipe and the `⊗`-parses-as-TensorProduct
trap in the `lean-formalization` skill.

## Current frontier

Problem 6.1.6 in Ch6 now has parts (a) and (e) landed, plus the "not positive definite"
half of (c). Three sorries remain in the file, all explicitly out of scope for #6625:
`mckay_connected` (b), `mckay_isAffineDynkin` (c), `mckayCartan_posSemidef` (c, the
positive-semidefinite half).

## Overall project progress

Stage 3 (formalization) ongoing across chapters. This turn closed one Ch6 feature issue.

## Next step

Pick up the remaining Problem 6.1.6 sorries as separate issues if planned:
`mckayCartan_posSemidef` (the `((2 − χ_V)f, f) ≥ 0` argument from the book's hint),
`mckay_connected` (uses Problem 4.12.10), and `mckay_isAffineDynkin` (assembles (b)+(c)
into the affine-Dynkin classification). Other unclaimed features: #6607 (Clifford ≅
End(S), flagged substantial) and #6624 (A₅ induced-from-ℤ₃ character decomposition).

## Blockers

None.
