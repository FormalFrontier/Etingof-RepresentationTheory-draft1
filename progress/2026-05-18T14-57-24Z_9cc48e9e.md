# Progress — feature session 9cc48e9e (#2910)

## Accomplished

Claimed and executed #2910 (sub-D of #2905): the `(b₂ deg 1, b₃ deg 1)`
sub-case of the "both arms extend" branch in `single_branch_leaf_case`.

In `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`:

- **New helper `single_branch_leaf_both_extend_t122_per_kQ`** (~440
  lines). Direct port of `InfiniteTypeConstructions.lean:7964-8352`: the
  T(1, 2, 2) = D₅ posdef contradiction. Inputs mirror the dispatch site
  inside `single_branch_leaf_case_both_extend_per_kQ` — top-level graph
  hypotheses, `v₀ leaf a₂ a₃ b₂ b₃` with their adjacency facts, the
  singleton/two-element equalities (`hS₀_eq`, `hb₂_eq`, `hb₃_eq`), and
  `hb₂_deg1 = hb₃_deg1 = 1`. `F`, `Q`, `hOrient` are carried through
  unused (per audit recipe
  `progress/reviews/2026-05-18-degree4-per-kQ-placement.md`).

- Inlined `QF` (currently `private abbrev` in
  `InfiniteTypeConstructions.lean`) as `dotProduct x ((2 • 1 - adj).mulVec x)`
  inside the helper. Avoids touching `InfiniteTypeConstructions.lean`;
  the de-`private` of `QF` is left for a future sibling sub-issue if any
  needs it.

- `set_option maxHeartbeats 6400000 in` carries the same justification
  as the source's setting on `single_branch_leaf_case`
  (`InfiniteTypeConstructions.lean:6896`).

Helper is sorry-free. The only `sorry` remaining in
`FieldGenericTpqr.lean` is on the parent
`single_branch_leaf_case_both_extend_per_kQ` stub (line 512 post-edit),
which will be discharged once all four sub-case helpers exist (D2 of
each of #2907 / #2908 / #2909 / #2910).

`lake build EtingofRepresentationTheory.Chapter6` passes. Sorry count
unchanged: this PR is sorry-neutral.

## Current frontier

PR for sub-D (#2910) ready to push.

`single_branch_leaf_case_both_extend_per_kQ` dispatch chain remaining
sub-issues:

- sub-A (#2907) Ẽ₇ embed (q, r ≥ 3) — has-pr (#2911).
- sub-B (#2908) T(1, q, 2), q ≥ 3 — unclaimed.
- sub-C (#2909) T(1, 2, r), r ≥ 3 — unclaimed.
- sub-D (#2910) T(1, 2, 2) = D₅ — this PR.
- Final D2 (wire all four into `single_branch_leaf_case_both_extend_per_kQ`):
  belongs to whichever sub-issue lands last, per #2907 / #2908 / #2909 /
  #2910 deliverable D2.

Chapter 6 per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard (#2877):

- D2.degree4 on `main` (PR #2891).
- D2.cycle on `main` (PR #2897).
- D2.adjacent on `main` (PR #2900).
- D2.single on `main` (PR #2903).
- D2.single follow-up #2904 partial on `main` (PR #2906).
- D2.single follow-up #2905 sub-A: has-pr (#2911).
- D2.single follow-up #2905 sub-D: this PR.
- **Pending**: D2.single follow-up #2905 sub-B / sub-C, then final
  wiring; plus D2.nonadj, D2.outer + D3 (still under #2877).

## Overall project progress

Stage 3 (Lean formalisation). Sorry count on `main` post-#2906: 19.
This PR is sorry-neutral. Post-merge sorry count: 19.

## Next step

- Next workers should claim #2908 (T(1, q, 2)) or #2909 (T(1, 2, r))
  — these are the two remaining unclaimed sub-issues of #2905.
- Whichever PR lands last among #2907 / #2908 / #2909 / #2910 should
  do the final D2 wiring step that replaces the
  `single_branch_leaf_case_both_extend_per_kQ` sorry body with the
  four-way dispatch.
- Parent #2877 still has D2.nonadj (~900 lines, decomposition candidate)
  and D2.outer + D3 (~150 lines) outstanding.

## Blockers

None for this worker. Standing constraints unchanged: framework wall
#2436 gates the inherited sorries in the per-(F, Q) indecomposability
proofs; the static pr-repair PRs (#2849 / #2694 / #2550) remain in
`/repair` territory.
