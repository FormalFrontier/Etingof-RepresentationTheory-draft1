## Accomplished

Formalized **Problem 6.1.1 (field embeddings)** sorry-free, closing issue #4784
(directive #4777, step 2(c) — "the piece feared hardest").

New file `EtingofRepresentationTheory/Chapter6/Problem6_1_1.lean` with:
- `trdeg_fractionRing_mvPolynomial m` : `trdeg k k(y₁,…,y_m) = m` (via
  `IsLocalization.isAlgebraic` ⟹ `trdeg_eq_zero` over the polynomial ring, then
  the tower formula `trdeg_add_eq` + `MvPolynomial.trdeg_of_isDomain`).
- `le_trdeg_of_injective` : injective `k[x₁,…,x_n] → L` (L a domain) ⟹
  `n ≤ trdeg k L` (the `trdeg_le_of_injective` Mathlib API, cleaner than the
  hand-rolled `algebraicIndependent_X.map'` route the issue sketched — both work).
- `n_le_m_of_injective_of_trdeg` : the main statement for a field `L` of trdeg `m`.
- `n_le_m_of_injective_to_rationalFunctions` : deduction to
  `k[x₁,…,x_n] → k(y₁,…,y_m)`.
- `n_le_m_of_field_embedding` : deduction to a `k`-linear field embedding
  `k(x₁,…,x_n) → k(y₁,…,y_m)` (field hom is auto-injective; compose with the
  fraction-field inclusion).

Wired the module into `Chapter6.lean`. `lake build` of the module is green
(8026 jobs). `items.json`/`progress/items.json` already marked
`Chapter6/Problem6.1.1` as `sorry_free` (planner pre-mark, now backed by code).

## Current frontier

Problem 6.1.1 complete. Chapter 6 infinite-type direction (directive #4777)
continues elsewhere.

## Overall project progress

Stage 3 (formalization), endgame. This adds a real sorry-free file for a
previously-unimplemented exercise. `k`/`L` carriers are pinned to `Type` so the
index `Fin n` and codomain share a universe (required by `cardinalMk_le_trdeg`);
that matches the orbit-counting application (`V.obj v ≃ Fin (d v) → k`).

## Next step

Continue the #4777 orbit-counting redirect (other open steps of Problem 6.1.5).

## Blockers

None.
