## Accomplished

Worked the Chapter 8 Problem 8.2.8 `Tor` quasiIso obligation (issue #6729,
`extTensorProjectiveResolution.quasiIso`).

**Decomposition.** Assessed #6729 as three substantial constructions and decomposed it:
- Step 1 (restriction of scalars *reflects* QuasiIso) is a direct Mathlib citation —
  `HomologicalComplex.quasiIso_map_iff_of_preservesHomology` with
  `F := ModuleCat.restrictScalars`; all instances (`Additive`, `PreservesHomology` via
  `PreservesFiniteLimits`+`Colimits`, `ReflectsIsomorphisms`) already exist. No separate issue.
- #6734 — the restriction-of-scalars **commutation iso** (geometric core).
- #6735 — the map-level **quasiIso assembly** (consumes #6734 + Ch7 Künneth). Depends on #6734, #6738.
Skipped #6729 with a `Decomposed into #6734, #6735` breadcrumb.

**Landed (partial PR on #6734).** New file
`EtingofRepresentationTheory/Chapter8/ExternalTensorRestriction.lean`, sorry-free:
- `extModule_algebraMap_smul`: the external action of `algebraMap k (A₁⊗A₂)ᵐᵒᵖ c` on `X ⊗[k] Y`
  is scalar mult by `c` (since `extTensorRep` is a `k`-algebra map → `AlgHom.commutes`).
- `extRestrictObjEquiv` / `extRestrictObjIso`: the **pointwise** iso
  `resExt (extTensorFunctorObj X Y) ≅ res₁ X ⊗ res₂ Y` in `ModuleCat k` (identity on `X ⊗[k] Y`).
- `extRestrictObjIso_naturality` (+ `extRestrictObjIso_hom_tmul`): naturality in `(X, Y)`.
- Helpers `resExt`/`res₁`/`res₂` (the three restriction-of-scalars functors).
Added to `Chapter8.lean`. `lake build …Chapter8` succeeds.

**Follow-up filed.** #6738 — the **complex-level** commutation iso `extTensorComplex_restrictIso`
(the remaining half of #6734): transport the pointwise iso through the `mapBifunctor` total complex
using `restrictScalars`'s coproduct preservation (`preservesColimit_restrictScalars`) +
`total`/`mapBifunctor` summand API + Koszul-sign differential matching. Detailed route + exact API in
the issue body.

## Current frontier

Pointwise commutation iso is done and reusable. The complex-level assembly (#6738) is the open hard
kernel: no Mathlib "functor commutes with `total`" lemma exists, so it must be built by hand from the
colimit-preservation machinery (`GradedObject.mapObj` coproduct), which is where the documented
`isDefEq` blow-ups on the bifunctor summands live.

## Overall project progress

Stage 3, Ch8 Problem 8.2.8 (Tor) tensor-of-resolutions sub-tree. `extTensorProjectiveResolution`
has `complex`/`π`/`projective` real; `quasiIso` still `sorry`, now decomposed into #6734 (pointwise
half landed), #6738 (complex iso), #6735 (quasiIso glue). Downstream Künneth Tor assembler #6657
still blocked on #6735 + #6727.

## Next step

Pick up #6738 (complex-level commutation iso) once the partial PR merges — it builds directly on the
pointwise `extRestrictObjIso`/`extRestrictObjIso_naturality`. Budget it as a full session: the
`resExt`-commutes-with-`total`-coproduct step is the crux. Then #6735 (quasiIso glue) unblocks.

## Blockers

None new. #6738 is unblocked (needs only the merged pointwise iso). #6735 blocked on #6738.
