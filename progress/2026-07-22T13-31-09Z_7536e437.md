## Accomplished

Stage 3.7 **coverage-arm** audit of **Problem 4.12.2** (Heisenberg group `R_z`
reps & full irrep classification), issue #7317. Report-only: recorded per-sub-part
`coverage` verdicts as durable state in `progress/items.json`; no Lean proof edits.

Per-sub-part verdicts (each with `lean_decl` pointers, recorded as `derived`
items under `Chapter4/Problem4.12.2`):

- **(a) covered_full** — `exists_unique_rep` is a genuine `∃!` whose defining
  conditions are exactly the book's two generator actions (existence AND
  uniqueness, via `closure_gens_eq_top` + `eq_gen_prod`). "Compute ρ(g) for all
  g" is `rhoLin`/`rhoLin_apply` (ρ⟨a,b,c⟩f t = z^(b·t−c).val·f(t−a)); `|G|=p³`
  is `card_eq`.
- **(b) covered_full** — `irreducible_iff` is a genuine two-directional
  `IsSimpleModule … ↔ z ≠ 1` over the group algebra, non-vacuous.
- **(c) covered_partial** — the multiplicity-free decomposition clause is full
  (`R1_decomposes`: genuine `DirectSum.IsInternal` of p lines, finrank 1 each,
  `Function.Injective χ` = "each occurs exactly once"). The 1-dim classification
  clause is partial: count `p²` (`one_dim_reps_card`) + factoring through the
  abelianization (`abHom_ker_le_ker`) are exposed, but the explicit parametrizing
  bijection is only a proof-local `let e`, not a headline (contrast GL₂'s
  exposed `characterCompDetEquiv`, which is why the GL₂ sibling is covered_full).
- **(d) covered_partial** — exhaustion trichotomy IS exposed
  (`simple_iso_charRep_or_rhoHom` + `irreducible_dim` + the three iso-iff
  lemmas), but the assembled grand totals (`p²` and `p−1` counts, sum-of-squares
  `p²·1²+(p−1)·p²=p³=|G|`) live only as internal `have`s inside the classification
  proof — no exposed total-count/exhaustion headline. Mirrors the 4.12.6/4.12.9
  iso-vs-character partials.

Roll-up `coverage = covered_partial` (min across sub-parts). `coverage_arm:
audited`, `lean_file` and `fidelity_decl` pointers added. `fidelity: faithful`
and `status: sorry_free` left untouched (report-only).

Opened two follow-up `feature` issues:
- **#7321** — expose the explicit 1-dim rep parametrization by the abelianization
  `(ZMod p)²` for part (c).
- **#7322** — expose the explicit total-count/exhaustion headline
  (`p² + (p−1)` irreducibles) for part (d).

This completes the coverage-arm sweep of §4.12 alongside the already-audited
siblings (4.12.1 #7314, 4.12.3/4/5/8/10/11) and in-flight 4.12.7 (#7306).

## Current frontier

4.12.2 coverage audit complete and recorded. Remaining unclaimed review issues:
#7306 (4.12.7 coverage audit), #7320 (Exercise 5.27.2 coverage audit).

## Overall project progress

Stage 3.7 (coverage-arm audits) in progress across Chapter 4 §4.12. This turn
closed the 4.12.2 gap (`coverage: None → covered_partial` at sub-part
granularity). Two follow-up feature issues (#7321, #7322) queued to lift (c)/(d)
to covered_full.

## Next step

A worker can pick up #7306 or #7320 (remaining §4.12 / Ch5 coverage audits), or
a feature worker can take #7321/#7322 to upgrade 4.12.2 (c)/(d) to covered_full.

## Blockers

None.
