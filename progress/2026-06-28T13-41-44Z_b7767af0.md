## Accomplished
Worked issue #5579 (Cauchy assembly of `peterWeylSummandMap_iSup_range_eq_top`). Found that the
issue's plan assumed step 2 (#5577) would decompose `rightHull φ` into concrete `L_λ`-summands, but
#5577 delivered only abstract semisimplicity (`rightHull_isSemisimple`). The genuinely missing
infrastructure — identifying each simple constituent of the hull with a concrete `AlgIrrepGL n λ k`
via a `GL_n`-equivariant inclusion into `R` (the highest-weight classification wired to submodules
of `R`) — is not present in the codebase.

Did the coherent top-down subset: reduced `peterWeylSummandMap_iSup_range_eq_top` (and hence
`peterWeylMap_surjective` / `peterWeylMap_bijective` / `Theorem5_23_2_ii_equivariant`) to a single
isolated, fully-documented bridge lemma `rightHull_le_iSup_range_peterWeylSummandMap`, via
`RightTranslationHull.self_mem_rightHull`. The bridge lemma's docstring records the complete
intended route (semisimplicity #5577 -> decompose into simples -> realize each simple as an
equivariant `AlgIrrepGL n λ k` copy in `R` -> step-4 correspondence #5578).

Created sub-issue #5587 for the bridge lemma. The module builds green with the sole remaining
`sorry` now isolated in the bridge.

## Current frontier
`Chapter5/Theorem5_23_2_PeterWeyl.lean`: single `sorry` in
`rightHull_le_iSup_range_peterWeylSummandMap` (line ~490). Everything downstream is a real proof.

## Overall project progress
Chapter 5 Peter-Weyl (#5572) Cauchy route: step 1 (hull) and step 2 (semisimplicity, #5577) and
step 4 (matrix-coefficient correspondence, #5578) all landed. The final assembly is now wired; the
only remaining mathematical content is the realization/classification bridge (#5587).

## Next step
Work #5587: discharge `rightHull_le_iSup_range_peterWeylSummandMap`. The hard part is the
realization step — turning a simple algebraic constituent `S ≤ R` of the hull into a concrete
dominant weight `λ` plus an equivariant iso `AlgIrrepGL n λ k ≃ S`, using
`iso_of_formalCharacter_eq_schurPoly`. Consider splitting that realization step into its own helper.

## Blockers
None blocking #5587. #5579 is reduced to #5587 (partial PR landing the assembly skeleton).
