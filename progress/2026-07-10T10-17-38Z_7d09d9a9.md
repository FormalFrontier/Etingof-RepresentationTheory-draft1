## Accomplished

Discharged the single char-`p` deliverable of issue #6132: proved
`Etingof.Problem2_16_2.lie_theorem_fails_charP` (Lie's theorem fails in positive
characteristic) in `EtingofRepresentationTheory/Chapter2/Problem2_16_2.lean`. The theorem is
sorry-free and axiom-clean (`#print axioms` → `[propext, Classical.choice, Quot.sound]`).

Construction added (all real defs, no sorried data):
- `lam` : the prime-field embedding `ZMod p ↪ k` giving the `p` distinct eigenvalues.
- `diagOp`, `shiftOp` : the diagonal and cyclic-shift operators on `k^{ℤ/p}`, with
  `bracket_diag_shift : ⁅diagOp, shiftOp⁆ = shiftOp` (clean because `lam` is a ring hom, so
  consecutive eigenvalues differ by `lam 1 = 1`).
- `rowZero` / `mem_g_row` : every element of `g k` has vanishing second row (used to compute the
  matrix-commutator entries in the representation).
- `ρ : g k →ₗ⁅k⁆ End k (ZMod p → k)` : the representation `X ↦ diagOp`, `Y ↦ shiftOp`.
- `repModule` / `repLieModule` : the induced `𝔤`-module structure via `LieRingModule.compLieHom`.
- `repModule_irreducible` : irreducibility, proved by a **minimal-support** argument (strong
  induction on `Finset` support size): a nonzero submodule contains a minimal-support vector;
  distinct eigenvalues force the support to a single point (else `diagOp v - λⱼ v` has strictly
  smaller support), giving a standard basis vector, and the shift `p`-cycle sweeps out all of them.
- `lie_theorem_fails_charP` : instantiate the universal statement at `k^{ℤ/p}`, whose `finrank`
  is `p ≠ 1`.

## Current frontier

Char-`p` deliverable complete. One deliberate statement adjustment: the theorem now binds
`k : Type` explicitly (was inherited `k : Type*`). This is **required** for provability — the
statement quantifies `∀ (M : Type)` (universe 0), and the witness `k^{ℤ/p}` lives in `k`'s
universe, so a universe-0 witness only exists when `k : Type`. The change is localized to
`lie_theorem_fails_charP` (via a shadowing `(k : Type)` binder); the char-0 theorems keep
`k : Type*`. See the PR body.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 2.16.2 char-`p` half now proven. The file still holds
the 3 char-`0` sorries (`bracket_X_Y`, `charZero_irreducible_finrank_one`,
`charZero_Y_acts_zero`) — these are issue #6122's deliverables, currently in open PR #6133, not
touched here. `items.json` left at `statement_formalized`; flip to `sorry_free` once #6122 also
lands.

## Next step

Land #6122 / PR #6133 (char-0 cluster). Once both are on `main`, the file is fully sorry-free and
`Chapter2/Problem2.16.2` can be set to `sorry_free`.

## Blockers

None. This item was independent of other open work (the char-0 sorries do not block the char-`p`
proof; my proof does not reference them).
