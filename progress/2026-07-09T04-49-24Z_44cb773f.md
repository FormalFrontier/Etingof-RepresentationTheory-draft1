## Accomplished
Proved **Problem 4.12.2(a)** (issue #6017) in
`EtingofRepresentationTheory/Chapter4/Problem4_12_2.lean`, sorry-free:

- `Heisenberg.card_eq`: `|Heisenberg p| = p³` (transport `Fintype.card` across
  `equivProd` to `(ZMod p)³`, then `Fintype.card_prod` + `ZMod.card`).
- `exists_unique_rep`: existence and uniqueness of `R_z`. Built the representation
  explicitly as `ρ⟨a,b,c⟩ f t = z^((b·t − c).val) · f (t − a)` via `rhoLin`/`rhoHom`;
  the key identity `z^((m+n).val) = z^m.val · z^n.val` (lemma `zpow_val_add`, using
  `z^p = 1`) drives `map_mul`. Uniqueness: `xGen, yGen` generate `G`, formalized by
  `eq_gen_prod` (`⟨a,b,c⟩ = xGen^a·yGen^b·⟨0,0,1⟩^(c−ab)`) and `central_word`
  (`⟨0,0,1⟩ = xGen·yGen·xGen^(p−1)·yGen^(p−1)`, positive powers only so only `map_mul`/
  `map_pow` are needed — no `map_inv`, which does not hold into a plain monoid `End`).

Support lemmas added (all sorry-free): `xGen_pow`, `yGen_pow`, `centralGen_pow`,
`central_word`, `eq_gen_prod`, `zpow_mod`, `zpow_val_add`, `rhoLin`(+`_apply`),
`rhoHom`(+`_apply`, `_xGen`, `_yGen`).

`#print axioms` for both `card_eq` and `exists_unique_rep` shows only
`propext, Classical.choice, Quot.sound` — no `sorryAx`.

## Current frontier
Parts (b) `irreducible_iff`, (c) `one_dim_reps_card`/`R1_decomposes`, (d)
`irreducible_dim` remain `sorry` (they were explicitly out of scope for #6017 and take
a `ρ` satisfying the generator equations as hypotheses).

## Overall project progress
Phase 3 formalization ongoing. Problem 4.12.2 part (a) now complete; item remains
`statement_formalized` overall until (b)–(d) land.

## Next step
File/claim follow-up issues for 4.12.2(b), (c), (d). They can reuse `rhoHom` and the
generator lemmas here. Part (b) needs the shift/multiplication operators' irreducibility;
part (c) the abelianization `(ZMod p)²`; part (d) the sum-of-squares formula.

## Blockers
None.
