## Accomplished
Statement pass for Chapter 3 exercises (issue #5893). Formalized faithful Lean 4
statements with `sorry` proofs (no proving) for 4 of the 10 listed exercises:

- **Exercise 3.10.1** (`Exercise3_10_1.lean`): `Matₘ(k) ⊗ Matₙ(k) ≅ Mat_{mn}(k)` as
  `k`-algebras, stated as `Nonempty (_ ≃ₐ[k] Matrix (Fin (m*n)) …)`.
- **Exercise 3.6.1** (`Exercise3_6_1.lean`): character additivity
  `χ_V = χ_W + χ_{V/W}` for a subrepresentation `W : Submodule A V` of a
  finite-dimensional representation. Registered the `Module.Finite k` instances for
  `↥W` and `V ⧸ W` as local instances.
- **Problem 3.3.3** (`Problem3_3_3.lean`): alternative proof of Theorem 3.3.1.
  (a) irreducibility of a representation of a product algebra `∀ i, 𝒜 i` via the
  constructed `A`-linear idempotent projection `idemProj`; (b) uniqueness of `k^d`
  as the irreducible of `Mat_d(k)` and decomposition of finite-dim reps into copies
  of `k^d`; (c) pointer to the existing `Etingof.irreducible_reps_of_matrix_algebra`.
- **Problem 3.9.1** (`Problem3_9_1.lean`): extensions and `Ext¹`. Constructed the
  reusable infrastructure: `IsCocycle`, `blockOp`, `cocycles` (Z¹), `coboundaryOf`
  (built from linear combinators), `coboundaries` (B¹), `Ext1 = Z¹/B¹`, and stated
  parts (a)–(d) (block-multiplicativity ⇔ cocycle; coboundary is a cocycle and
  vanishes iff `A`-linear; isomorphism of extensions via block-operator
  intertwiners).

All four files build (sorry warnings only on the theorem bodies; every `def`/
`instance` is fully constructed). `progress/items.json` updated to
`statement_formalized` for these four ids, and its formatting normalized back to the
repo's `indent=2` style.

## Current frontier
Four Chapter 3 exercise statements landed. The remaining six exercises need new
infrastructure and were scoped into a follow-up issue.

## Overall project progress
Phase 3 statement pass ongoing. Chapter 3 exercise backlog: 4 of the 10 in #5893
now `statement_formalized`; the other 6 tracked in #5896.

## Next step
Claim #5896 to formalize the remaining Chapter 3 exercise statements: Problem 3.8.4
(Noether-Deuring), Problem 3.8.5 (periodic-function algebra), Problem 3.9.2
(`ℂ[x]` Ext / algebra with `xᵢxⱼ=0`), Problem 3.9.3 (quiver path algebra), Problem
3.9.4 (formal deformations), Problem 3.9.5 (Clifford algebra). The Ext¹ machinery in
`Problem3_9_1.lean` is reusable for 3.9.2 and 3.9.4.

## Blockers
None. #5893 is a partial completion (4/10); a follow-up sub-issue (#5896) covers the
remainder, so the parent is routed to `replan` via the `--partial` PR.
