# Progress — planner session 94a08ec3

## Accomplished

Planner cycle. Queue state on entry: `POD_QUEUE_DEPTH=1`,
`POD_MIN_QUEUE=3`, `POD_QUEUE_DEFICIT=2`.

Pre-plan checks:

- **Directives**: none open.
- **Merge sweep**: PR **#2867** (doc(Ch5 #2841): forward to Mathlib
  PR 39523 — `LinearMap.IsIdempotentElem.eq_zero_of_trace_eq_zero`)
  satisfied the mergeable + non-failing predicate; merged
  (squash + delete-branch).
- **Remaining open PRs**: #2849 (CI FAILURE), #2694 (CONFLICTING),
  #2550 (CONFLICTING) — all pr-repair territory, no fix issues filed
  here per the standard planner/repair split.
- **Replan triage**: handled by `/replan`. Sizeable replan queue
  unchanged from cycle 12f509fe; out of scope here.
- **`check-blocked` / `check-has-pr` housekeeping**: no changes.

Step 4 overlap audit: re-read the open `agent-plan` issue titles
plus the bodies of #2853 (the parent the new sub-issues
pre-decompose) and the wave-60 cleanup hoist audit
(`progress/reviews/2026-05-18-d5tilde-subB-cleanup-hoist.md`). The
audit explicitly notes (`§4`) that the sibling-direction analogues
"are already part of #2853's scope" but the previous worker's
progress note (`20260518T034747Z_6fc85452.md`) also says "those
siblings can be filed as part of #2853 **or as a separate scoping
sub-issue if #2853 is too large for one session**." Since #2853 is
self-sized as 300–700 lines and the parent #2853 is **`blocked` on
#2850 `replan`**, pre-staging the siblings in unblocked sub-issues
is planner-led decomposition that lets workers make forward progress
while #2850 awaits replan triage.

Created two new feature issues — both **pre-step decompositions of
#2853**, both **unblocked**, both **mechanically independent of
`d5tildeRep_kQ_leaf_equalities` itself**:

- **#2868** — `feat(Ch6 #2853 pre-step): projection-based
  reversed-leaf-edge sibling lemmas (d5tilde_core_F_proj* /
  d5tilde_core3_F_proj*)`. Adds up to 4 top-level sibling theorems
  to `FieldGenericD5Tilde.lean` for the reversed leaf-edge sub-cases
  at lines 798 (e53), 800 (e43), 804 (e12), 806 (e02). Consumes
  `starFirst_F` / `starSecond_F` projections + `center_decomp_F` +
  `starFirst_F_starEmbed*_F` left-inverse identities — all in `main`.
  Audit's "Table of applicability" §3 quantifies 24 of 31 sub-cases
  need these siblings.

- **#2869** — `feat(Ch6 #2853 pre-step): γ⁻¹-coupled sibling lemma
  d5tilde_gamma_containment_F_inv for reversed central edge`. Adds
  the γ⁻¹ analogue of `d5tilde_gamma_containment_F` for the 4
  sub-cases at line 802 (e23 reversed). Consumes
  `gammaInv_embed1_plus_embed2_F` and
  `gammaInv_embed1_plus_embedNshift_F` (PR #2843 closed-form
  γ⁻¹ identities), both in `main`. The companion to #2868 — pair
  covers all 5 reversed-edge categories.

Both issues explicitly note in their "Notes for the worker" sections
that consolidation back into #2853 is acceptable if the worker
discovers the split was over-decomposition; the split is a
pre-staging convenience, not a hard requirement.

Queue depth post-planning: **3** actionable issues (= `POD_MIN_QUEUE`).
Composition:

- **#2868** — projection-based siblings (just filed).
- **#2869** — γ⁻¹ analogue (just filed).
- **#2564** — Mathlib upstream tracker
  (`MvPolynomial.eq_of_eval_eq_on_gl`, external-blocked).

(#2841 is now removed from the queue post-merge of #2867's
forwarding doc; its replan label can be cleared by the next
`/replan` cycle.)

`coordination set-target` not called — active development phase,
operator-configured pool size stands.

## Current frontier

Unclaimed actionable issues:

- **#2868** / **#2869** — D̃₅ Sub B sibling-lemma pre-steps for
  #2853 (just filed, both unblocked).
- **#2564** — Mathlib upstream tracker, external-blocked.

Active in-flight PRs (all in pr-repair territory): #2849 (#2821
dedupe, CI FAILURE), #2694 (Schur-Weyl γ.A, CONFLICTING — ~15 days
static), #2550 (Wall 3 C.1.a.ii, CONFLICTING — ~24 days static).

Per-(F, Q) Ch6 refactor scoreboard (unchanged from cycle 12f509fe
except for the new pre-steps under D̃₅ Sub B):
- Cycle / K_{1,4} canonical / D̃₅ Sub A / Ẽ₆ Sub A+B / Ẽ₇: done.
- K_{1,4} per-(F, Q) indecomposability: #2801 — `replan`.
- K_{1,4} bridge `starRep_kQ ↔ starRepGen` canonical: #2823 —
  `replan`.
- D̃₅ Sub B: helpers + γ⁻¹ identities + canonical leaf-equality case
  + wave-60 cleanup hoists landed. Pre-steps for #2853 filed:
  **#2868** (projection siblings), **#2869** (γ⁻¹ analogue).
  #2839 / #2850 / #2853 / #2851 still in the chain (all
  `replan`/`blocked`).
- T(1,2,5): #2793 still `replan`. Subgraph transfer: deliverables
  2–3 in #2774 still `replan`.

Schur-Weyl L_i C-tier chain: γ.A (#2694) still CONFLICTING, all
downstream items (γ.B, C-4a aggregation, C-4c, Schur-Weyl #5/#6)
still blocked.

## Overall project progress

Stage 3 (Lean formalisation). The wave-60 D̃₅ Sub B helper
infrastructure is fully audited and clean. The remaining bulk work
(31 reversed-orientation sub-cases of `d5tildeRep_kQ_leaf_equalities`)
is concentrated in #2853 but currently parent-blocked.

This cycle pre-decomposes #2853's natural sub-scaffold (the
reversed-direction sibling lemmas) into two unblocked pre-step
issues — workers can land them in parallel while #2850's replan
proceeds, halving the eventual size of #2853 once it unblocks.

Sorry-count delta this turn: **0** (planner cycle, no code changes).
Expected sorry delta once #2868 + #2869 land: **0** (both add new
top-level lemmas with proofs — no `sorry` placeholders).

## Next step

- Dispatcher will run `/replan` first (sizeable queue), then pr-repair
  on #2849 / #2694 / #2550, then claim a worker on the unclaimed
  queue. **#2868** and **#2869** are the natural feature picks; both
  are mechanically scoped at ~30–50 lines each.
- After both pre-steps land, the next planner cycle should re-scope
  the remaining #2853 work — likely smaller and cleaner once the
  sibling lemmas are in place.
- Wave-61 summarize threshold will be re-evaluated after the next
  batch of D̃₅ Sub B / Schur-Weyl / Wall-3 movement.

## Blockers

None for the planner work itself. Standing constraints unchanged:
framework-wall decision (#2436) gates the inherited sorries in the
per-(F, Q) and ℂ-side indecomposability proofs; the three static
pr-repair PRs (#2849 / #2694 / #2550) remain in `/repair` territory.
