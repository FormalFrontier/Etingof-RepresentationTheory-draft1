## Accomplished

Feature session, issue #6100 (Ch9 Problem 9.4.2 — projective dimension via Ext + dimension shifting). Proved all three sorried theorems in `EtingofRepresentationTheory/Chapter9/Problem9_4_2.lean`, sorry-free:

- `hasProjectiveDimensionLE_iff_ext_vanishing` (i): forward via `HasProjectiveDimensionLT.subsingleton`, backward via `.mk`.
- `hasProjectiveDimensionLE_syzygy` (iii, dimension shifting): via `ShortComplex.ShortExact.hasProjectiveDimensionLT_X₁` with `n = d`, using `Projective S.X₂ ⟹ HasProjectiveDimensionLT S.X₂ 1` bumped to `d`.
- `projectiveDimension_succ_of_nonsplit` (ii): modelled on Mathlib `RingTheory/Regular/ProjectiveDimension.lean:87` (`= succ` template). `aux (n) : pd X₃ ≤ n ↔ pd X₁ + 1 ≤ n` from `hasProjectiveDimensionLT_X₃_iff`; `eq_of_forall_ge_iff` + `WithBot ℕ∞` induction. Nonsplit rules out the degenerate cases: `splittingOfProjective` gives `¬Projective X₃`, `splittingOfInjective` + `IsZero.injective` gives `¬IsZero X₁`.

`#print axioms` on all three: only `propext, Classical.choice, Quot.sound`. Updated `progress/items.json` (`Chapter9/Problem9.4.2` → `sorry_free`).

## Current frontier

Issue #6100 complete. Ready to PR.

## Overall project progress

Statement-pass Ch9 Problem 9.4.2 now fully proved. No new sorries introduced.

## Next step

Create PR closing #6100; enable auto-merge. Other unclaimed feature issues remain: #6057 (Ch3 descent), #6098 (Ch4 field-general count bound).

## Blockers

None.
