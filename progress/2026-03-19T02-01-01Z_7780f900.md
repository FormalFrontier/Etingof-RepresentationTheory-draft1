## Accomplished

- Proved the zero case of Proposition6_6_7_sink: when V is simple at i (IsSimpleAt), F⁺ᵢ(V) is zero at every vertex
- Proved nontriviality for the indecomposable case: when sinkMap is surjective, F⁺(V) has a nontrivial vertex
- Constructed U₁, U₂ submodule families on V from complementary subreps W₁, W₂ of F⁺(V)
- Proved the transport lemma at v ≠ i: if all U_k(v) = ⊥ for v ≠ i, then W_k(v) = ⊥ (via equiv injectivity)
- Added helper `reversedArrow_ne_ne` to convert reversed-quiver arrows back to original

## Current frontier

The `reflFunctorPlus_mapLinear_ne_ne` helper lemma is sorry'd due to a **fundamental Decidable instance mismatch** in `reflectionFunctorPlus` (Definition6_6_3.lean):
- `obj` field uses `let dp := fun v => inst v i` (the DecidableEq argument)
- `mapLinear` field uses `by_cases ha : a = i` (Classical.propDecidable via `by classical`)

These produce different `Decidable.rec` terms that can't be reconciled via `rw`, `cases`, `match`, `simp`, or `convert` due to dependent types. Multiple approaches were tried over 3+ attempts.

## Overall project progress

Proposition6_6_7.lean has 5 `sorry` occurrences across 3 declarations:
1. `reflFunctorPlus_mapLinear_ne_ne` — blocked on Decidable mismatch
2. `Proposition6_6_7_sink` — 4 sorries (subrep U₁, subrep U₂, IsCompl, transport at i), all downstream of the map lemma
3. `Proposition6_6_7_source` — blocked on Definition6_6_4 (reflectionFunctorMinus)

## Next step

**Fix the root cause**: Refactor `reflectionFunctorPlus` in `Definition6_6_3.lean` so that the `mapLinear` field uses the same `dp` Decidable instance as `obj`, instead of `by_cases` (which introduces Classical.propDecidable). Specifically, replace `by_cases ha : a = i` / `by_cases hb : b = i` with explicit `dite` using `dp a` / `dp b`. This would make `reflFunctorPlus_mapLinear_ne_ne` provable by `rfl` after unfolding + matching on the shared instance.

After that fix, the remaining sorries in the sink theorem should follow from the map identification lemma.

## Blockers

- `reflectionFunctorPlus` Decidable instance mismatch (needs refactor of Definition6_6_3.lean)
