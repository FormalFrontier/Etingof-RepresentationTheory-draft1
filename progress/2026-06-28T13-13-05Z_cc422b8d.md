## Accomplished

One-shot dispatch on **#5577** (Cauchy step 2: complete reducibility of the right-translation
hull, sub-issue of #5572). Landed a new sorry-free file
`Chapter5/RightTranslationHullDecomp.lean` carrying out step 2 end-to-end:

- **`numEmbed_intertwines`** — the genuine new bridge: the numerator embedding `numEmbed r`
  (`p ↦ algebraMap p · det⁻ʳ`) intertwines the polynomial-side right translation `polyRightRep`
  with the `det^r`-twisted localization action `charTwistRep (detChar^r) (localRightRep)`. This is
  the precise sense in which the `det^r`-twist clears the `det⁻ʳ` denominator.

- **`boundedSubrep` / `boundedRightRep_isAlgebraic`** — right translation on the
  finite-dimensional space of polynomials of total degree `≤ d` is an algebraic representation,
  via the monomial basis and the matrix-coefficient polynomials `rightTransPoly`
  (`Etingof.evalAtGL_rightTransPoly`), mirroring the homogeneous `polyRightDegreeFDRep_isAlgebraic`.

- **`numEmbed_injective`** — `numEmbed r` is injective (`algebraMap_away_injective` + the unit
  `det⁻ʳ`).

- **`rightHull_isSemisimple`** — the headline result: the hull `rightHull φ`, as a
  `localRightRep`-representation, is a semisimple `k[GL_N]`-module ("complete reducibility of the
  right-translation hull"). The `det^r`-twist of the hull is equivalent (via `numEmbed`) to a
  `polyRightRep`-invariant subspace `U = comap ι (rightHull φ)` of the bounded-degree polynomials,
  hence algebraic (`IsAlgebraicRepresentation.restrict` + `.of_linearEquiv`); an algebraic
  representation is completely reducible (`Theorem5_23_2_i`); untwisting by `det⁻ʳ`
  (`isSemisimpleModule_charTwistRep`) recovers the hull.

All sorry-free; `lake build EtingofRepresentationTheory.Chapter5` green. Wired into `Chapter5.lean`.

## Current frontier

`Theorem5_23_2_PeterWeyl.peterWeylSummandMap_iSup_range_eq_top` still has its single sorry. The
Cauchy route now has steps 1 (`RightTranslationHull.lean`) and 2 (`RightTranslationHullDecomp.lean`)
landed. Remaining: identify the hull constituents as the specific `L_λ = algIrrepGLRepρ` with
weights shifted by `-r·(1,…,1)` (the finer statement, via
`quotDetRep_irreducible_constituent_lastWeight_zero`); the matrix-coefficient correspondence
(#5578, step 4); final assembly (#5579).

## Overall project progress

Stage 3 ongoing. Peter-Weyl injectivity half sorry-free; surjectivity (Cauchy spanning) half is
the lone capstone sorry, being built bottom-up. Steps 1+2 of the Cauchy route now exist sorry-free.

## Next step

Pick up **#5578** (matrix-coefficient correspondence) and the constituent identification
(decompose the semisimple hull into `algIrrepGLRepρ n λ k` summands using
`decompose_polynomial_gl_rep` + `quotDetRep_irreducible_constituent_lastWeight_zero`, untwisting
the weights). Then #5579 assembles the spanning statement.

## Blockers

None.
