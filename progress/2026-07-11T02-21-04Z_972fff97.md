## Accomplished
Formalized **Problem 9.5.3(ii) step 3** (issue #6404): `Ext¹`-vanishing forces a short exact
sequence to split. New sorry-free file
`EtingofRepresentationTheory/Chapter9/Problem9_5_3_Splitting.lean` with

```lean
theorem Etingof.shortExact_splits_of_ext_subsingleton
    {A B C : ModuleCat.{v} R} {f : A ⟶ B} {g : B ⟶ C} (w : f ≫ g = 0)
    (hSES : (ShortComplex.mk f g w).ShortExact)
    (hExt : Subsingleton (Abelian.Ext C A 1)) :
    Nonempty (B ≅ A ⊞ C)
```

`#print axioms` = `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

## Current frontier
Problem 9.5.3(ii) block decomposition. Steps 1 (base case, PR #6407) and 2 (finite-length
dévissage, issue #6403) are the covariant/inductive half; this is the independent step 3.

## Overall project progress
Chapter 9 blocks material progressing. Step 3 of 9.5.3(ii) now landed independently of steps 1/2.

## Next step
Assemble 9.5.3(ii): once step 2 (#6403) lands, combine the finite-length `Ext¹`-vanishing
result with this splitting lemma to conclude `Hom(M,N)=0` across distinct blocks / the direct-sum
decomposition of the category.

## Key patterns
- The extension class lives in `hSES.extClass : Ext C A 1`; the contravariant Ext long exact
  sequence `Abelian.Ext.contravariant_sequence_exact₁` at target `A` gives a preimage of
  `mk₀ (𝟙 A)` when the connecting map into `Ext¹(C,A)` is zero (forced by `Subsingleton`).
- `Abelian.Ext.homEquiv₀ : Ext B A 0 ≃ (B ⟶ A)` plus `mk₀_bijective` / `mk₀_comp_mk₀`
  convert the degree-0 Ext preimage into an honest retraction `f ≫ r = 𝟙 A`.
- `ShortComplex.Splitting.ofExactOfRetraction` (balanced category) + `isoBinaryBiproduct`
  turn the retraction into `B ≅ A ⊞ C`.

## Blockers
None.
