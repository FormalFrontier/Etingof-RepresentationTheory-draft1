## Accomplished

One-shot dispatch on **#5572** (the Cauchy-spanning crux
`peterWeylSummandMap_iSup_range_eq_top`, sub-issue of #5550). This is the research-level core
that the issue itself flagged as a multi-PR effort ("none of this bridge exists yet"). After
mapping the available infrastructure, I landed **step 1 of the Cauchy route** sorry-free as a
self-contained building-block file, and decomposed the rest into successor issues.

New file `Chapter5/RightTranslationHull.lean` (sorry-free, axioms only
`propext`/`Classical.choice`/`Quot.sound`):

- `rightHull φ` — the `k`-span of the right-translation orbit `{localRightRep g φ}` of `φ ∈ R`.
- `self_mem_rightHull` and `localRightRep_mem_rightHull` (invariance), packaged as
  `rightHullSubrep φ : Subrepresentation (localRightRep k N)`.
- **`rightHull_finiteDimensional φ`** (the meaty result): writing `φ = Q · det⁻ʳ`
  (`exists_invSelf_normalForm`), the orbit lands in the image under `numEmbed r`
  (`p ↦ algebraMap p · det⁻ʳ`) of the finite-dimensional space of polynomials of total degree
  `≤ Q.totalDegree`, so the span is finite-dimensional.
- supporting, reusable: `rTransAlgHom_totalDegree_le` (right translation does not raise total
  degree, via the homogeneous decomposition + `rTransAlgHom_isHomogeneous`),
  `localRightRep_normalForm` (`R_g (Q·det⁻ʳ) = (det g)⁻ʳ · (R_g Q)·det⁻ʳ`), `numEmbed`.
- Wired into `Chapter5.lean`; `lake build EtingofRepresentationTheory.Chapter5` green.

Step 3 (matrix-coefficient realization `evalGLAway (R_g φ) 1 = φ(g)`) is documented but **not**
repackaged as a standalone lemma: `evalGLAway` is an `IsLocalization.Away.lift`, and re-deriving
the identity outside `DetInvElim`'s rewrite context triggers an `IsLocalization` `isDefEq`
blow-up (both `rw` "pattern not found" and a >2M-heartbeat `isDefEq` timeout). Downstream should
call `Etingof.DetInvElim.evalGLAway_localRightRep` directly in-context.

## Current frontier

`Chapter5/Theorem5_23_2_PeterWeyl.lean` still has its single sorry
(`peterWeylSummandMap_iSup_range_eq_top`); step 1 of its Cauchy proof now exists as
infrastructure.

## Overall project progress

Stage 3 ongoing. Peter-Weyl injectivity half is sorry-free; the surjectivity (Cauchy spanning)
half is the lone remaining sorry of the capstone, now being built bottom-up. #5572 decomposed
into: #5577 (step 2, complete reducibility of the hull), #5578 (step 4, matrix-coefficient
correspondence), #5579 (final assembly, depends on #5577 + #5578).

## Next step

Pick up **#5577** (det-twist the hull to a polynomial rep, apply
`polynomialRep_isSemisimple` / `decompose_polynomial_gl_rep`, identify constituents as `L_λ`)
and **#5578** (the equivariant-map ↦ `peterWeylSummandMap` correspondence). Then #5579 assembles
the spanning statement. All build on `Chapter5/RightTranslationHull.lean`.

## Blockers

None new. #5572 itself is the research-level crux and is intentionally landed partially (step 1)
with the remainder tracked as #5577/#5578/#5579. The `evalGLAway` `IsLocalization` `isDefEq`
fragility is documented in `RightTranslationHull.lean` for successors.
