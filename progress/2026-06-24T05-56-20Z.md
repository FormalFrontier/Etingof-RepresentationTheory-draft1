## Accomplished

Replaced the vacuous `True` stub in `Chapter5/Example5_12_3.lean` (issue #5125)
with six genuine, fully-proved Specht-module dimension theorems, following
Etingof Example 5.12.3:

- `Example5_12_3_trivial (n) (hn : 0 < n)`: `dim_ℂ V_{(n)} = 1` (one-row
  partition, the trivial representation).
- `Example5_12_3_sign (n) (hn : 0 < n)`: `dim_ℂ V_{(1ⁿ)} = 1` (one-column
  partition, the sign representation).
- `Example5_12_3_dim_21`: `dim V_{(2,1)} = 2` (standard rep of `S₃`).
- `Example5_12_3_dim_22`: `dim V_{(2,2)} = 2`.
- `Example5_12_3_dim_31`: `dim V_{(3,1)} = 3`.
- `Example5_12_3_dim_211`: `dim V_{(2,1,1)} = 3`.

All proofs go through the Frame–Robinson–Thrall hook-length route already in the
project: `finrank_spechtModule_eq_card_syt_general` (`dim V_λ = |SYT(λ)|`) and
`card_standardYoungTableau_eq` (`|SYT| = n! / ∏ h(i,j)`).

Supporting reusable machinery added in the file (private `Example5_12_3`
namespace):
- `toYoungDiagram_colLen_eq`: a `Nat.find`-free formula for `colLen`.
- `hookLengthProduct_eq_compute`: rewrites `hookLengthProduct` into a fully
  computable (kernel-reducible) product, since `rowLen`/`colLen` use `Nat.find`
  and block `decide`.
- `sortedParts_eq_of`: pins down `μ.sortedParts` from a descending list via
  `List.mergeSort_eq_self`.
- `hookLengthProduct_eq_of`: bridge that reduces a concrete shape's hook product
  to a `decide`-closable product over `cellsOfRowLens L`.
- `prod_range_sub`: `∏_{k<n} (n − k) = n!` (the single-row / single-column hook
  product), used for the general trivial/sign cases.

`lake build EtingofRepresentationTheory.Chapter5.Example5_12_3` succeeds with no
errors, no warnings, no `sorry`, no `True`.

## Current frontier

Issue #5125 complete. The file is build-clean.

## Overall project progress

Stage 3 formalization. One more vacuity-audit `True` stub eliminated; the four
explicit small Specht-module dimensions and the trivial/sign extremes are now
formalized on top of the existing §5.12/§5.17 hook-length theory. No new Mathlib
API required.

## Next step

Continue the claim-coverage / vacuity audit: pick the next `vacuous-statement`
issue. The `hookLengthProduct_eq_of` + `sortedParts_eq_of` + `prod_range_sub`
helpers here are a reusable template for any future "dimension of `V_λ` for an
explicit `λ`" obligation.

## Blockers

None.
