## Accomplished

Worked on issue #1263: prove `hker` (range(source_map) = ker(Φ)) in Proposition 6.6.6.

1. **Created `reversedArrow_eq_ne` API function** (Definition6_6_3.lean): Extracts the original `b ⟶ i` arrow from a reversed-quiver arrow `i →_{Q̄ᵢ} b` (case a = i, b ≠ i). Follows the same pattern as the existing `reversedArrow_ne_ne`.

2. **Created `reflFunctorPlus_mapLinear_eq_ne` API theorem** (Definition6_6_3.lean): At the sink vertex going to a non-sink vertex (a = i, b ≠ i), the F⁺ᵢ map sends ker(sinkMap) elements to their direct sum components via component projection composed with kernel subtype inclusion. This is the missing API lemma identified as the blocker.

3. **Created `Φ_comp_source_eq_zero` helper** (Proposition6_6_6.lean): Applies the API lemma via `simp_rw` to reduce each term in the Φ ∘ source_map composition. Successfully reduces the goal to a sum-reindexing problem.

4. **Closed PR #1294** as redundant (superseded by #1279 which already proved hΦsurj).

5. **Closed issue #1300** as stale (PR #1292 already merged).

## Current frontier

The `hker` proof is reduced to three remaining sub-problems:
- **Arrow equality**: `arrowsOutReversed_origArrow hi x = reversedArrow_eq_ne (ne x) x.snd` — both extract `x.fst ⟶ i` but through different Decidable.casesOn paths, causing `rfl` to fail and `rw` to hit "motive is not type correct".
- **Sum reindexing**: Need `∑_{x : ArrowsOutOf instR i} f(reindex x) = ∑_{b : ArrowsInto inst i} f(b)` via a proven bijection.
- **Reverse direction**: `ker(Φ) ≤ range(source_map)` — constructing preimages.

## Overall project progress

- ~193/583 items sorry-free (~33%)
- Proposition 6.6.6 has 4 sorries: Φ_comp_source_eq_zero (1), hker (1), naturality (1), source case (1)
- The `reflFunctorPlus_mapLinear_eq_ne` API lemma unblocks the proof approach

## Next step

- Complete `Φ_comp_source_eq_zero` by resolving the arrow equality + sum reindexing
- Consider alternative: unfold ALL definitions (sinkMap, reflectionFunctorPlus, equivAt_ne, etc.) and rewrite Decidable instances globally, similar to the `_ne_ne` proof pattern
- The Decidable.casesOn dependent type issue is the fundamental blocker across all remaining sorries

## Blockers

- Decidable.casesOn dependent type transport: `rw` and `rfl` fail across different Decidable reduction paths. This affects arrow equality, sum reindexing, and naturality proofs.
