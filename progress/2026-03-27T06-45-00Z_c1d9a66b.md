## Accomplished

- Created `EtingofRepresentationTheory/Chapter9/ShapiroLemma.lean` with infrastructure for Shapiro's lemma (Ext adjunction for extension-restriction of scalars)
- Proved `restrictScalars_preservesEpimorphisms`: restriction of scalars preserves epimorphisms (surjectivity unchanged)
- Proved `extendScalars_preservesProjectiveObjects`: extension of scalars preserves projectives (left adjoint to epi-preserving functor)
- Proved `restrictScalars_preservesProjectiveDimensionLT`: exact functor preserving projectives preserves `HasProjectiveDimensionLT` (induction on n with dimension shifting)
- Proved `hasProjectiveDimensionLT_of_extendScalars`: transfers pd bound from F(M) to M when M retracts G(F(M))
- Left `ext_subsingleton_of_extendScalars` as sorry with detailed proof approach documentation

## Current frontier

The remaining sorry (`ext_subsingleton_of_extendScalars`) requires building a cochain complex isomorphism from the adjunction, following the pattern of Mathlib's `groupCohomology.coindIso`. This involves:
1. Taking a projective resolution P of M
2. Mapping it through extendScalars to get a projective resolution of F(M)
3. Using the adjunction to build degree-wise isos `Hom_S(F(P_n), N) ≅ Hom_R(P_n, G(N))`
4. Showing these commute with coboundary maps (naturality)
5. Lifting to cohomology iso → Ext iso → Subsingleton transfer

## Overall project progress

- Hilbert syzygy theorem structure is in place (Example9_4_4.lean)
- Three components needed for the lower bound: (1) SES construction, (2) X-action vanishing (#1868), (3) Shapiro's lemma (#1867)
- Shapiro infrastructure (preserving projectives, dimension transfer) is complete
- Individual Ext transfer (the core Shapiro statement) still needs the cochain complex argument

## Next step

Complete the `ext_subsingleton_of_extendScalars` sorry using the cochain complex isomorphism approach. The key Mathlib APIs are `Cochain`, `Cocycle`, `CohomologyClass`, `HomComplex`, and `extEquivCohomologyClass`. Reference `Mathlib/RepresentationTheory/Homological/GroupCohomology/Shapiro.lean` for the pattern.

## Blockers

None — the approach is clear, just requires significant work with the HomComplex API.
