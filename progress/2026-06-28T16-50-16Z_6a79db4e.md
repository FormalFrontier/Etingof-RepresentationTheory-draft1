## Accomplished

Discharged issue #5606 — the **analytic half** of the realization core (steps 1–5 of
#5602), sorry-free. New file `Chapter5/RealizationCoreAnalytic.lean` proves
`Etingof.exists_detTwistNeg_schurModule_realization_of_simple`: a simple finite-dimensional
`localRightRep`-subrep `S` of `R = k[gᵢⱼ][1/det]` is, after a `det^{-r}`-twist, equivariantly
isomorphic to a Schur module. Axioms: `propext, Classical.choice, Quot.sound` (no `sorryAx`).

Pieces (all sorry-free):
- `polyRightDegree_char_as_antitone_sum` — character of the degree-`d` Cauchy component as a
  nonnegative `ℕ`-combination of distinct antitone Schur polynomials (mirror of the
  `quotDetDegree` version, `d < N` branch logic).
- `detTwist_clearing` (step 1) — parameterised det-clearing across a basis: for any exponent
  `r` clearing the basis denominators, the `det^r`-twist of `S` is algebraic and embeds
  `GL_n`-equivariantly into `k[Xᵢⱼ]` (mirror of `rightHull_isSemisimple`'s det-clearing).
- `exists_detTwist_polyEmbedding_of_simple_subrep` (step 1 + weight-spanning) — clears at
  `r = r₀ + s`, where `s` from `IsAlgebraicRepresentation.exists_detPow_twist_isPolynomial`
  makes the twist genuinely **polynomial**, giving weight-spanning via
  `polynomial_rep_iSup_glWeightSpace_eq_top`.
- `exists_polyRightDegree_embedding_of_simple` (steps 2–3) — single-degree reduction: a simple
  rep with an equivariant injection into `k[Xᵢⱼ]` embeds into a single homogeneous degree-`d`
  Cauchy component (Schur on the homogeneous-component projections; mirror of
  `exists_degree_embedding_of_simple` over `A` rather than `A/det`).
- Assembly — constituent extraction (`simple_constituent_formalCharacter_eq_schurPoly_mem`) +
  highest-weight uniqueness (`simpleRep_iso_schurModule_of_formalCharacter_eq`) + det-twist
  untwisting (`Intertwines.symm`/`.charTwist`).

Wired into `Theorem5_23_2_PeterWeyl.lean`: deleted the local sorried theorem, imported the new
file; `exists_dominantWeight_equivariant_realization` now consumes the sorry-free analytic half.

## Current frontier

`Chapter5/Theorem5_23_2_PeterWeyl.lean` builds green. The one remaining sorry there is the
**other** half of #5602, `exists_dominantWeight_algIrrepGL_iso_detTwistNeg_schurModule` (the
det-shift packaging, step 6) — a separate issue, out of scope for #5606.

## Overall project progress

Phase 3 formalization, Peter-Weyl (§5.23) capstone. Issue #5606 complete. The Peter-Weyl
capstone `Theorem5_23_2_ii_equivariant` still depends on the det-shift packaging half and
`peterWeylMap_bijective`.

## Next step

Pick up the det-shift packaging half `exists_dominantWeight_algIrrepGL_iso_detTwistNeg_schurModule`
(step 6 of #5602): identify `charTwistRep (det^{-r}) (schurModuleRep n ν)` with `algIrrepGLRepρ n lam k`
by iterating `schurModule_shift_iso_detTwist` (Prop 5.22.2), as `ContragredientIdentity.lean` does.

## Blockers

None.
