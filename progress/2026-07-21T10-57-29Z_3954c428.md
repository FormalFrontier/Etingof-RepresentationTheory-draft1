## Accomplished

Stage 3.7 fidelity audit of **Chapter 4 Problem 4.12.1** (dihedral-group irreps +
V⊗V decomposition), issue #7219. Verdict: **gap**, driven by part (a).

- **Part (a):** `irreducible_dim` proves only the dimension dichotomy
  `finrank ℂ W ∈ {1, 2}` — a necessary condition. The book's part (a) ("describe
  *all* irreps, consider odd/even `N`": odd → 2 dim-1 + `(N−1)/2` dim-2; even → 4
  dim-1 + `(N−2)/2` dim-2) is **entirely absent** — no construction, exhaustiveness,
  non-isomorphism, or counts, and (unlike 4.12.2(d)) nothing proved internally to
  expose. The counts live only in a docstring, which is not a checked assertion.
  Silent weakening → **gap**.
- **Part (b):** `tensor_square_character` (`χ_V² = 1 + χ_ε + χ_{V₂}`) is a faithful,
  non-vacuous character-level rendering of `V⊗V ≅ 𝟙 ⊕ ε ⊕ V₂`. Character values
  verified correct; identity is genuine for all `N`. Noted (not counted against (b)):
  the characters are hand-defined functions, and `V₂`'s irreducibility is not
  established (character-level modelling is the accepted convention here).

Actions taken:
- `progress/items.json` `Chapter4/Problem4.12.1`: `status sorry_free →
  partially_formalized`, `fidelity → gap`, `fidelity_issue → 7222`, `fidelity_note`
  added.
- Filed follow-up **feature #7222** to formalize the full part (a) classification.
- Wrote report `progress/reviews/2026-07-21-ch4-problem4_12_1-dihedral-fidelity.md`.

## Current frontier

Audit complete; PR being opened for #7219. No Lean files changed (verdict recorded
in `items.json` + report only).

## Overall project progress

Stage 3.7 fidelity sweep ongoing. This item joins `Chapter4/Problem4.12.2` as a
recorded `gap` (both dihedral/Heisenberg dimension-dichotomy-vs-classification
patterns). Remaining unclaimed review items at session start: #7220 (5.16.2 sign
convention), #7221 (7.8.5 homological). Feature #7210 (weight-space spanning) and new
#7222 (dihedral classification) available for feature agents.

## Next step

A feature agent should pick up **#7222** (formalize the dihedral irrep
classification, reusing the 4.12.2 sum-of-squares/pigeonhole machinery). Review
agents can take #7220 / #7221.

## Blockers

None.
