# Review session — `embed_t125_in_tree_per_kQ` audit (issue #2928)

## Accomplished

Filed audit report
`progress/reviews/2026-05-19-embed-t125-in-tree-per-kQ.md`. Verdict
**PASS** on all five deliverables of #2928 — no code changes, no
follow-up issues.

Highlights:

- **D1 Signature fidelity** ✓ — parameter list of
  `embed_t125_in_tree_per_kQ` (`Chapter6/FieldGenericT125.lean:71-91`)
  matches the universal `embed_t125_in_tree`
  (`InfiniteTypeConstructions.lean:4918-4935`) parameter-for-parameter,
  with the per-(F, Q) carriage `(F, [Field F], [IsAlgClosed F], Q,
  [Subsingleton …], hOrient)` appended exactly as #2928 specified.
- **D2 Conclusion fidelity** ✓ — per-(F, Q) conclusion matches the
  canonical `Etingof.QuiverRepresentation` predicate shape used by
  sibling per-(F, Q) leaves (`t125_not_finite_type_per_kQ`,
  `etilde7_not_finite_type_per_kQ`,
  `subgraph_infinite_type_transfer_per_kQ`).
- **D3 Embedding construction fidelity** ✓ — the helper body (lines
  96-436) is **bit-identical** to the universal body (lines 4937-5277),
  verified by `diff` on the 341-line range. Same `φ_fun` vertex
  assignment `0→v₀, 1→u₁, 2→p₁, 3→p₂, 4→q₁, 5→q₂, 6→q₃, 7→q₄, 8→q₅`;
  same 50-fact `linarith` argument list in the `hembed` proof.
- **D4 Dispatch fidelity** ✓ — the final `exact …` swaps universal
  `subgraph_infinite_type_transfer + t125_not_finite_type` for
  per-(F, Q) `subgraph_infinite_type_transfer_per_kQ +
  t125_not_finite_type_per_kQ +
  restrictOrientationViaEmb_isOrientationOf` in the expected order,
  threading `F`, `Q`, `hOrient` from the outer binder.
- **D5 Call-site fidelity (both uses)** ✓ —
  `FieldGenericTpqr.lean:607-612` (d₂-extends in
  `single_branch_leaf_both_extend_b3leaf_per_kQ`, PR #2917) and
  `FieldGenericTpqr.lean:971-976` (d₃-extends in
  `single_branch_leaf_both_extend_b2leaf_per_kQ`, PR #2918) both
  verified by position-by-position table; the symmetric `ha₂₃ /
  ha₂₃.symm` usage between the two sites is consistent (`ha₂₃` means
  `a₂ ≠ a₃` in both files).

Build of `EtingofRepresentationTheory.Chapter6.FieldGenericT125` +
`FieldGenericTpqr` passes from `main` = `c86c23a` (8045 jobs); only
warning-only output, and one pre-existing `sorry` at
`FieldGenericTpqr.lean:1233` unrelated to the helper or its call
sites.

## Current frontier

The shared helper `embed_t125_in_tree_per_kQ` is structurally clean
and ready to support the two further universal call patterns the
per-(F, Q) outer assembly will eventually need (the
`b3leaf_per_kQ` and `b2leaf_per_kQ` siblings whose universal
counterparts are invoked at
`InfiniteTypeConstructions.lean:7679, 7686`). No structural drift
that would block those future call sites was found.

## Overall project progress

Stage 3 (Lean formalization) in flight. Per-(F, Q) outer assembly
sub-tree (#2877) continues to be filled in by parallel pods — wave 62
landed the shared T(1, 2, 5) helper plus four of the five
single-branch sub-cases (sub-B/C with the helper, plus sub-D); sub-A
(PR #2911) remains in `/repair`. This audit confirms the shared helper
is sound, removing one risk vector from the remaining sub-A repair
and any future fan-out.

## Next step

Next planner cycle: continue with #2922 (sub-A1 leaf-case helper for
non_adjacent_branches) and #2564 (Mathlib upstream tracker, blocked
externally). #2911 repair remains the highest-leverage open salvage.
Once #2911 lands, this helper is ready for the third call site without
re-audit.

## Blockers

None for this review session. The two source PRs (#2917, #2918) are
already merged into `main`.
