## Accomplished

Closed issue #4536 (feat Ch6 #2978 sub-B): the chain-length-general
per-(F, Q) D̃ embedding and the `chain.length ≥ 6` all-leaves dispatch.

* Added `embed_dtilde_in_tree_per_kQ` to `FieldGenericDTilde.lean`
  (Section 6), the chain-length-general analogue of
  `embed_d8tilde_in_tree_per_kQ`. Rather than re-deriving the full
  non-edge lattice with `fin_cases` (as the fixed-shape D̃₆/₇/₈ helpers
  do), it reuses the universal proof's clean approach: build `φ` over
  `Fin (k + 6)` with `k = chain.length - 2`, prove one-directional edge
  preservation `hedges`, then obtain the full adjacency equality
  `hembed` uniformly from `tree_embed_adj_eq` + `dTilde_nodup_path_between`,
  and dispatch via `subgraph_infinite_type_transfer_per_kQ` +
  `dTilde_not_finite_type_per_kQ`. The φ-construction / `hedges` block
  ports verbatim from `non_adjacent_branches_infinite_type`
  (`InfiniteTypeConstructions.lean:9770-9961`); only the final two-line
  dispatch changes (universal → per-kQ).
* Un-privatised four reusable infrastructure declarations in
  `InfiniteTypeConstructions.lean`: `dTildeEdgePred`,
  `dTildeAdj_eq_one_iff`, `tree_embed_adj_eq`, `dTilde_nodup_path_between`.
* Replaced the residual `sorry` in
  `non_adjacent_branches_leaf_case_per_kQ`
  (`FieldGenericNonAdjacentBranches.lean`) with a call to the new helper;
  added the `FieldGenericDTilde` import; refreshed the now-stale file and
  theorem docstrings (the leaf-case helper is now fully sorry-free).

`set_option maxHeartbeats 1600000` is needed on the helper (matching the
D̃₈ precedent) for the `k`-parametric injectivity / edge case analysis.

## Current frontier

`non_adjacent_branches_leaf_case_per_kQ` is sorry-free. The full project
builds (`lake build`, 8326 jobs, success).

## Overall project progress

Stage 3 formalization. The non-adjacent-branches per-(F, Q) leaf case is
complete across all chain lengths. Pre-existing deferred sorries remain
(unchanged by this work): the `d{5,7,8}tildeRep_kQ_isIndecomposable` and
`dTildeRep_kQ_isIndecomposable` decomposition cores, and the universal
`InfiniteTypeConstructions` sporadic cases.

## Next step

The outer assembly `non_adjacent_branches_infinite_type_per_kQ` (#2923)
consumes this leaf case. The deferred `dTildeRep_kQ_isIndecomposable`
sorry (`FieldGenericDTilde.lean:240`) is the parametric indecomposability
core — the per-kQ analogue of the D̃₅/₇/₈ deferred cores; closing it would
make `dTilde_not_finite_type_per_kQ` (and hence this embed) sorry-free
transitively.

## Blockers

None.
