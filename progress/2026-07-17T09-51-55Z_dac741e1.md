## Accomplished

- Merged ready PR #6905 (already merged by the time I checked).
- Released #6898 (final Ext assembly): its `depends-on: #6897` was stale — #6897 was closed by
  decomposition into #6900 (closed) + #6901 (OPEN, blocked). The `extAbelianIsoExtₖ` bridge
  (deliverable 3, the substantial missing piece) is produced by the still-open #6901, so #6898 is
  not doable yet. Added the correct `depends-on: #6901` (→ blocked) and skipped it back for replan.
- Claimed #6882 (one-branch tree case ⟹ Ẽ₆/Ẽ₇/Ẽ₈). This is flagged as the largest tree-case piece;
  decomposed it after landing a coherent subset:
  - **Landed (this PR):** `affine_arm_length_solutions` in
    `Chapter6/Problem6_1_3_continued_tildeE.lean` — the equality analogue of the finite
    `arm_length_solutions`. Only solutions of `1/(p+1)+1/(q+1)+1/(r+1)=1` with `1≤p≤q≤r` are
    `(2,2,2)`, `(1,3,3)`, `(1,2,5)`. Sorry-free; `interval_cases` + `omega` with `gcongr`/`nlinarith`
    upper bounds. Module builds clean (no new warnings).
  - **Sub-issue #6906 (piece b):** `affine_tree_one_arm_reciprocal` — extract three arms from the
    unique degree-3 vertex + derive the reciprocal equality from the kernel vector (tent-function
    analogue, the ~350-line hard part).
  - **Sub-issue #6907 (piece c):** close `affine_tree_one_branch_iso` by reindexing the three arms
    onto Ẽ₆/Ẽ₇/Ẽ₈; `depends-on: #6906` (correctly blocked).

## Current frontier

`affine_tree_one_branch_iso` still `sorry`, now with piece (a) available as a clean input.
`affine_tree_two_branch_iso` (#6893) also still `sorry` (separate issue).

## Overall project progress

Stage 3 formalization ongoing. Ch6 #Problem6.1.3-g (affine Dynkin classification, ⟹ direction) is
being assembled bottom-up: tree case dispatches on branch count; one-branch arm (#6882) now split
into (a) landed / (b) #6906 / (c) #6907. Ch8 #Problem8.2.8-Ext final assembly (#6898) parked behind
the Ext≃Extₖ bridge #6901.

## Next step

Pick up #6906 (structural three-arm extraction + reciprocal equality) — it unblocks #6907 which
closes `affine_tree_one_branch_iso`. Alternatively #6893 (two-branch D̃ₙ) is independent and ready.

## Blockers

None for the landed work. #6898 is genuinely blocked on #6901 (the `extAbelianIsoExtₖ` bridge);
#6907 blocked on #6906 by construction.
