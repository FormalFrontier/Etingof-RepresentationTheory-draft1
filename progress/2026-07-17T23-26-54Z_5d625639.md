## Accomplished

Worked issue #6935 (`feat(Ch8 #Problem8.2.8-Ext): discharge key123`) as a **partial**.
`Chapter8/ExtAbelianComparison.lean`:

- Landed `HomologicalComplex.homologyMap_smul` sorry-free (the general lemma Mathlib records as a
  TODO in `Algebra/Homology/Linear`): `homologyMap (r • φ) i = r • homologyMap φ i`, via
  `ShortComplex.homologyMap_smul`.
- Landed `Etingof.smul_homology_eq` sorry-free ("fact 1", the clean half): on
  `(P.complex.linearYonedaObj k N).homology n` the `ModuleCat k` scalar `r • v` equals
  `(homologyMap (r • 𝟙 (…)) n).hom v` (via `homologyMap_smul`, `homologyMap_id`,
  `ModuleCat.lsmul_eq_smul_id`).
- Reduced the `key123` sorry: `key123` is now proved **sorry-free** from a single new, cleaner sorry
  `hnat` — the purely categorical naturality of the steps-1–3 composite `e123` in `N` under
  `θ = r • 𝟙 N` (source scalar `·.comp (mk₀ θ)` intertwines `homologyMap (r • 𝟙)` on the target). The
  module-scalar bookkeeping is gone; only the categorical tracing remains.
- Filed sub-issue **#6943** with the full `hnat` route (reduce to `extMk` generators via
  `extMk_surjective`/`extMk_comp_mk₀`, then the cocycle-class target formula
  `e123 (extMk f) = linYonClass f` traced through the three chain steps).

File builds; the only `sorry` is `hnat`. Two commits on `agent/5d625639`.

Also merged (start-of-cycle) PRs #6937, #6936, #6934.

## Current frontier

`Chapter8/ExtAbelianComparison.lean` has one `sorry` (`hnat`, line ~111). Everything else in the
`Ext ≃ₗ[k] Extₖ` bridge is sorry-free.

## Overall project progress

Stage 3 formalization, Chapter 8 Problem 8.2.8 (`Ext` half). The `Ext ≃ₗ[k] Extₖ` comparison is one
`sorry` (`hnat`) from complete; that unblocks the final assembly #6898. Fact 1 and the general
`homologyMap_smul` are now reusable.

## Next step

Claim **#6943** and discharge `hnat`. The de-risked path: reduce to `extMk` generators, prove
`e123 (extMk f) = linYonClass f` (cocycle homology class in `linearYonedaObj`), whose `k`-linearity
plus fact 1 closes it. Step 3 (the `homIso` tower tracing in `HomComplexHomologyK.lean`) is the hard
part — consider splitting it off.

## Blockers

None. `hnat` is hard (no Mathlib naturality API for `homologyAddEquiv`/`CohomologyClass` under a
target morphism, nor for the repo `homIso` tower — must be built), but well-scoped in #6943.
