## Accomplished

One-shot dispatch on **#5516** (Peter-Weyl brick (2)+(3): the per-weight equivariant
matrix-coefficient map `L*_λ ⊗ L_λ → R`). Landed sorry-free (in its own body) in the
new leaf file `Chapter5/PeterWeylMatrixCoeff.lean` (namespace `Etingof`), now wired
into the `Chapter5.lean` aggregator. Builds green (`lake build
EtingofRepresentationTheory.Chapter5.PeterWeylMatrixCoeff`, 8661 jobs).

Deliverables (exactly as #5516 requested):
- `peterWeylSummandMap (n) (lam) (k) : AlgIrrepGLDual n lam k ⊗[k] AlgIrrepGL n lam k
  →ₗ[k] Localization.Away (detPoly k n)` — genuine data, built via `TensorProduct.lift`
  from the algebraic witness of `L_λ`:
  `u ⊗ v ↦ ∑_{a,c} (⟨u, b a⟩ · b.repr v c) • coordToAway (P a c)`.
- `peterWeylSummandMap_equivariant : peterWeylSummandMap (ρ*(g) u ⊗ ρ(h) v) =
  localBiRep k n (g,h) (peterWeylSummandMap (u ⊗ v))` — the `GL_n × GL_n`-equivariance,
  intertwining the `peterWeylRHS` summand action `(algIrrepGLRepDualρ ∘ fst) ⊗
  (algIrrepGLRepρ ∘ snd)` with `localBiRep`.

Supporting lemmas (all sorry-free):
- `evalGLAway_peterWeylSummandMap` — the load-bearing matrix-coefficient identity
  `evalGLAway (psm (u ⊗ v)) g = ⟨u, ρ(g) v⟩`, read off through the faithful
  functions-on-`GL` model.
- `algIrrepGLRepρ_isAlgebraic` — `L_λ = algIrrepGLRepρ` is an algebraic representation.
- `IsAlgebraicRepresentation.detInvTwist` — reusable: the `det^{-s}` twist of an
  algebraic rep is algebraic (multiply each coefficient by `Dˢ`). Complements the
  existing `.detTwist` (positive det character) in `GLRepAlgebraic`.
- `evalAtGL_X_inr`, `evalAtGL_pow`, `detChar_zpow_neg_apply'` (local copy).

Construction followed the recipe in the #5516 comment by session ba97562a exactly:
algebraic witness `(d, b, P)` → `coordToAway` of the matrix-coefficient polynomials →
matrix-coefficient identity via `evalAtGL_eq_evalGLAway_coordToAway` + `evalGLAway_smul`
→ equivariance by `evalGLAway_injective` (needs `k` infinite, from `CharZero`): right
factor via `evalGLAway_localRightRep` + `ρ(y)ρ(h)=ρ(yh)`, left factor via the
post-#5536 `evalGLAway_localLeftRep` (left translation by `g⁻¹`) + the pairing
invariance `⟨ρ*(g₀)u, w⟩ = ⟨u, ρ(g₀⁻¹)w⟩`.

## Current frontier

The per-summand map and its equivariance are done. The remaining step for
Theorem 5.23.2(ii) (`Theorem5_23_2_PeterWeyl.lean:107` `Theorem5_23_2_ii_equivariant`,
still `sorry`) is to sum `peterWeylSummandMap` over all dominant weights and prove the
assembled `⊕_λ L*_λ ⊗ L_λ → R` is a `GL_n × GL_n`-equivariant *isomorphism* (injective +
surjective), i.e. that the matrix coefficients of the pairwise-distinct `L_λ` are
linearly independent and span `R` (the Cauchy decomposition input).

## Overall project progress

Stage 3 ongoing. Bricks (2)+(3) of Peter-Weyl are now in place; the per-summand
equivariant map exists as genuine data. The headline `Theorem5_23_2_ii_equivariant`
awaits the linear-independence + spanning assembly across all `λ`.

## Next step

Assemble `peterWeylSummandMap` over `DominantWeight n` into a single map `peterWeylRHS →
R` (via `DirectSum`/`Representation.directSum`), prove `IsEquivariantEquiv` from the
per-summand `peterWeylSummandMap_equivariant`, then discharge bijectivity from the
Cauchy decomposition (`PolynomialGLDecomposition` / `CauchyDetQuotient*`) to close
`Theorem5_23_2_ii_equivariant`.

## Blockers

None for this item. Note `peterWeylSummandMap`'s axiom list transitively includes
`sorryAx` only via the still-`sorry` contragredient identity #5522
(`ContragredientIdentity.lean:123`, `algIrrepGLDual_iso_linearDual`), a pre-existing
proof-level dependency sorry — this file adds no new sorry.
