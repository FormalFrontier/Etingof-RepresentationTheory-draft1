## Accomplished

- Claimed milestone (c) of the Problem 8.2.8 `Tor` rearrangement (#6687). On orientation found
  the one-line "package as an iso of complexes" plan had an unanticipated infrastructure gap: the
  RHS `⊗_k` of complexes must be `HomologicalComplex.tensorObj` over `ModuleCat.{u} k` (the shape
  `Chapter7/KunnethChainComplexNat.lean` consumes), but the existing `tensorRightFunctor` lands in
  `AddCommGrpCat` and there was no k-linear `tensorRight`. So neither side of the milestone-(c)
  iso could even be stated over `ModuleCat k`.
- Decomposed #6687 into #6726 (k-linear tensorRight functor, prerequisite) and #6727 (the complex
  assembly proper, depends-on #6726), left the `Decomposed into …` breadcrumb, and `skip`ped
  #6687 to `replan`.
- Claimed and **completed #6726**: new file
  `EtingofRepresentationTheory/Chapter8/TensorRightFunctorK.lean` providing
  `tensorRightMapₖ`, `tensorRightFunctorₖ : ModuleCat Aᵐᵒᵖ ⥤ ModuleCat k` (M ↦ M ⊗_A N), its
  `Additive` instance, and `tensorRightFunctorₖ_forget₂ : tensorRightFunctorₖ ⋙ forget₂ … ≅
  tensorRightFunctor` (so a ModuleCat-k complex iso descends to the AddCommGrpCat homology defining
  `Tor`). Builds clean, no warnings; `#print axioms` = {propext, Classical.choice, Quot.sound}.
  Added the file to `Chapter8.lean`.

## Current frontier

#6726 done and ready for PR. #6727 (milestone (c) complex assembly) is unblocked once #6726 merges.

## Overall project progress

Stage 3 (formalization). Problem 8.2.8 `Tor` (#6657) still needs: tensor-of-resolutions assembler
(#6683), the milestone-(c) complex iso (#6727, now unblocked-pending-#6726), and the final Künneth
`Tor` wiring. Milestones (a)/(b) of the rearrangement are merged; (c) now split into #6726 (this
PR) + #6727.

## Next step

Merge #6726, then a worker claims #6727 to build the complex iso: commute `tensorRightFunctorₖ`
past the `mapBifunctor` total complex, package `rearrangeBidegree` (a) + `rearrangeBidegree_naturality`
(b) into a bifunctor `NatIso`, transport through `mapBifunctor` with Koszul-sign differential
commutation. Full route in the #6727 body.

## Blockers

None. #6727 is `blocked` on #6726 by design; clears automatically when #6726 merges.
