## Accomplished

Session type: `/work` → `/feature`. Branch `agent/cd407dc6`.

**Queue triage (3 stale/conflicted issues routed to replan):**

- **#4632** (close d6tilde outer reversed leaf branches e02/e12/e23) — *skipped,
  superseded.* Its premise (reuse the #4631 mixed-direction forcing lemma) is
  dead: #4631's closing PR #4646 **machine-refuted combos C/C′ as false** (the
  under-coupled `d6tildeRep_kQ` is decomposable at m=1; explicit complementary
  pairs break the leaf equality). The outer reversed branches nest those false
  configs, so they cannot be closed on the current rep. Fix is the
  homogeneous-tube redesign #4649, which explicitly absorbs "the outer reversed
  e02/e12/e23 branches of #4632" and instructs "do NOT re-file the #4631/#4632
  forcing-lemma sorries against the refuted rep."
- **#2793** (T(1,2,5) `t125_not_finite_type_per_kQ`) — *skipped, superseded.* All
  three deliverables already exist in `Chapter6/FieldGenericT125.lean` via the
  newer decomposed effort (#4559/#4612/#4620); the residual indecomposability
  `sorry` is owned by #4613/#4621/#4633.
- **#4630** (R2.b crux `twistedResidual_maximal_support_colStd`) — *skipped,
  cross-effort conflict.* It sits on the Garnir column-straightening route that
  #4595 (Frobenius route, #4609 closed, #4608 in progress) is actively retiring.
  Twice-refuted crux; flagged for a planner to decide whether #4593 R2.b is still
  needed before a long proof.

**Real work — decomposed #4608 (Frobenius/Vandermonde determinant form, the
single remaining blocker for #4595's Wall-3 retirement):**

- Full infrastructure audit + validated proof route written to
  `progress/frobeniusDetForm-route-design.md`. Key finding: the two steps feared
  hardest are near-direct Mathlib applications — the multinomial coefficient of
  `(∑X)^n` via `Finset.sum_pow` + `Nat.multinomial_spec`, and the
  falling-factorial→Vandermonde column reduction via
  `Matrix.det_eval_matrixOfPolynomials_eq_det_vandermonde` (Vandermonde.lean:278)
  with `descPochhammer` (monic, degree `j`).
- Decomposed into three turnkey sub-issues with exact Lean signatures:
  - **#4669** sub-A1 (analytic): `charValue → n!/∏β! · det(descPochhammer)` (steps 1–6).
  - **#4670** sub-A2 (algebraic): `det(descPochhammer) = ∏_{i<j}(β_i−β_j)` (step 7).
  - **#4671** sub-A3 (capstone): combine + cast (step 8); `depends-on #4669, #4670`.

## Current frontier

`charValue_trivialCycleType_eq_frobeniusDetForm` (`CharValueHookFormula.lean:268`)
decomposed into #4669/#4670/#4671. No Lean code changed (the proof is multi-step
determinant/coefficient algebra; decomposition is the Step-4b success path). This
partial PR lands the design note only.

## Overall project progress

Stage 3 (formalization) ongoing. Two active Ch5 crux fronts:
the Frobenius route (#4595 + #4608→#4669/#4670/#4671) which **retires** Wall 3, and
the older Garnir-straightening route (#4593/#4630) it supersedes. Ch6 D̃-family is
mid-redesign (homogeneous tubes, #4597 → #4647–#4652); the old under-coupled reps
(#4632 etc.) are refuted and being replaced.

## Next step

- Work **#4669** and **#4670** (independent, parallelizable) — both are
  well-scoped with exact Mathlib lemmas in the design note. Then **#4671** closes
  #4608, which unblocks #4595 (re-point `Theorem5_17_1`, retire Wall 3).
- Planner: close #4608 (fully covered by #4669/#4670/#4671), and triage the
  #4630/#4595 conflict — likely retire the #4593 R2.b sub-tree once the Frobenius
  route lands.

## Blockers

None new. #4595 remains blocked on #4608 (now #4669/#4670/#4671). #4642 blocked on
#4569 (open); #4595 downstream rewiring blocked until the Frobenius crux lands.
</content>
