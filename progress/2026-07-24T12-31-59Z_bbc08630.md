## Accomplished

Closed issue #7678 (existence half of Problem 9.4.2(iii)): constructed a finitely generated
projective resolution over a Noetherian ring.

New file `EtingofRepresentationTheory/Chapter9/FiniteProjectiveResolution.lean` provides:

- `freeCover` / `freeCoverπ` — a finite free cover `Rⁿ ↠ M` of a finite module, from
  `Module.Finite.exists_fin'`; with instances for finiteness, projectivity, and epi-ness.
- `finite_kernel` — the kernel of a map out of a finite module is finite (over a Noetherian ring),
  via `Module.Finite.of_injective` + `ModuleCat.mono_iff_injective`.
- `syzygyMod M n` — the `n`-th syzygy, `syzygyMod M 0 = M`, `syzygyMod M (n+1) = ker (pₙ)`, packaged
  with its finiteness (`syzygyData` recursion).
- `cx M` — the finite free chain complex, `Xobj M n = freeCover (syzygyMod M n)`.
- `pStep_exact` / `exact_d_d` — the finite-cover analogue of `CategoryTheory.exact_d_f`, proved with
  `ShortComplex.exact_iff_of_epi_of_isIso_of_mono` against the kernel sequence.
- `resolution M : ProjectiveResolution M` — assembled exactly like Mathlib's
  `ProjectiveResolution.of`, with `π`, `hasHomology`, and `quasiIso`.
- `exists_finite_projectiveResolution` — the deliverable theorem: for `M : ModuleCat.{u} R` finite
  over a left-Noetherian ring, `∃ P : ProjectiveResolution M, ∀ n, Module.Finite R (P.complex.X n)`.

`lake build` clean; `#print axioms` shows only `propext, Classical.choice, Quot.sound` (no `sorry`).
Full `EtingofRepresentationTheory.Chapter9` builds successfully (8657 jobs).

## Current frontier

Combining this existence result with `Etingof.Problem942.finite_truncated_projective_resolution`
(landing in PR #7679 for #7673) closes the book's finite-dimensional conclusion of 9.4.2(iii).
That composition is a small follow-up once #7679 merges.

## Overall project progress

Stage 3 (formalization) ongoing. Chapter 9 projective-dimension / syzygy thread advanced: the
finite-free-resolution existence input is now in place alongside the finiteness-propagation results.

## Next step

After #7679 merges, add a one-line corollary composing `exists_finite_projectiveResolution` with
`finite_truncated_projective_resolution` and `finite_of_finiteDimensional_algebra` to state the
finite-dimensional-algebra conclusion directly.

## Blockers

None.
