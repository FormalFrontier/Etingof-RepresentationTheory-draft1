## Accomplished

Worked issue #4608 (`charValue_trivialCycleType_eq_frobeniusDetForm`, Part A of the
Frobenius route to `dim V_λ = #SYT`). The single monolithic `sorry` is now reduced
to **one precisely-stated residual** (the determinant column-reduction), with two
reusable building blocks landed sorry-free in
`EtingofRepresentationTheory/Chapter5/CharValueHookFormula.lean`:

1. **`coeff_alternant_mul`** — ℚ analogue of `coeff_vandermonde_mul`: the coefficient
   of `x^α` in `(alternantMatrix N e).det · P` as a signed sum over `σ : Perm (Fin N)`
   of shifted coefficients of `P`. Works directly with the ℚ-valued `alternantMatrix`
   that defines `charValue`, so no ℚ/ℂ transfer is needed. Proof mirrors the existing
   ℂ lemma via `Matrix.det_apply` + `prod_X_pow_eq_monomial'` + `coeff_monomial_mul'`.

2. **`coeff_sumXpow_multinomial`** — the multinomial coefficient `coeff β ((∑ᵢ Xᵢ)^n)
   = n!/∏ᵢ(βᵢ)!` when `∑β = n`, via Mathlib `Finset.sum_pow_eq_sum_piAntidiag` +
   `Nat.multinomial_spec`.

The main theorem now does `unfold charValue; rw [psumPart_trivialCycleType,
coeff_alternant_mul]`, leaving the residual signed-sum → Vandermonde-determinant
identity as a documented `sorry`.

## Current frontier

`charValue_trivialCycleType_eq_frobeniusDetForm` residual (CharValueHookFormula.lean,
the `sorry` after the two rewrites). Goal shape:

  ∑ σ, sign σ • (if vExpσ ≤ lF then coeff (lF - vExpσ) ((∑X)^n) else 0)
    = n! · ∏_{i<j}(l_i − l_j) / ∏_j l_j!

with `lF = equivFunOnFinite.symm (shiftedExps N lam.parts)` and
`vExpσ = equivFunOnFinite.symm (vandermondeExps N ∘ σ⁻¹)`.

## Overall project progress

Stage 3 formalization, Chapter 5 hook-length / Frobenius route. The chain
`dim V_λ = charValue(λ,1) = n!/∏hooks = #SYT` is assembled
(`charValue_trivialCycleType_eq_hookFormula` etc. already merged); it depended on
this Part A sorry plus Part B (#4609). After this turn, Part A is reduced to the
determinant assembly only.

## Next step

Close the residual `sorry` (a #4608 follow-up sub-issue):
1. Apply `coeff_sumXpow_multinomial` to each nonzero term (sum-constraint
   `∑ (lF − vExpσ) = n` holds whenever `vExpσ ≤ lF`, since both `∑ lF` and `∑ vExpσ`
   telescope), giving `n!/∏_k (l_k − (N−1−σ⁻¹k))!`.
2. Reorganize the signed sum as `n!/∏l_j! · det A`, `A_{j,k} = l_j!/(l_j−(N−1−k))!`
   (the falling factorial `l_j (l_j−1)…`), `= 0` when `l_j < N−1−k`.
3. Column-reduce `det A` to `det(l_j^{N−i})` (falling factorials are monic of the
   same degrees → unitriangular change of basis), then `Matrix.det_vandermonde`
   gives `∏_{i<j}(l_i − l_j)`. Mind the ℕ-subtraction (`shiftedExps_strictAnti`
   makes `l_i − l_j` the genuine positive difference for `i<j`).

## Blockers

GitHub GraphQL API rate limit was exhausted mid-turn (issue/PR creation deferred
until reset). No technical blockers; the two landed lemmas compile sorry-free.
