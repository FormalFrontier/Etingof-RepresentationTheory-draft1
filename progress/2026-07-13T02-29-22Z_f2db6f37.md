## Accomplished

Feature session on issue **#6452** (dihedral classification via Theorem 5.27.1).
Landed the first two deliverables of the assembly sorry-free in
`EtingofRepresentationTheory/Chapter5/Exercise5_27_2_Dihedral.lean`, and decomposed
the remaining orbit-method content into sub-issue **#6471**.

- **Deliverable 1 — group isomorphism.** Built `invAut` (inversion automorphism of
  `Multiplicative (ZMod N)`), `dihedralφ` (the `ℤ/2` action by inversion, via
  `MonoidHom.mk'`), and `dihedralEquiv : DihedralGroup N ≃* DihedralSemidirect N`.
  Discovered the issue's suggested reflection map `sr i ↦ ⟨ofAdd i, ofAdd 1⟩` is **not**
  a homomorphism under Mathlib's `r i * sr j = sr (j - i)` convention; the correct map is
  `sr i ↦ ⟨ofAdd (-i), ofAdd 1⟩` (verified all four multiplication cases by hand).
- **Deliverable 2 — transport.** `dihedralRepEquiv := Action.resEquiv (FGModuleCat ℂ) dihedralEquiv`
  (category equivalence `FDRep ℂ (⟨r⟩⋊⟨s⟩) ≌ FDRep ℂ D_N`), plus `simple_equivalence_functor`
  (equivalences preserve `Simple`, reproved from the fully-faithful + preserves-mono reflection
  lemma applied to the inverse) and `finrank_dihedralRepEquiv_functor` (dimension preserved by
  `rfl`, since `Action.res` keeps the underlying object). **`dihedral_classification` is now
  fully derived (no sorry)** from a new `semidirect_classification` theorem via family transport,
  `fullyFaithfulFunctor.preimageIso` (iso reflection), the counit (essential surjectivity), and
  `Finset.filter_congr` (count transfer).

The file builds with exactly **one** `sorry`: `semidirect_classification`.

## Current frontier

`semidirect_classification` (the 6-conjunct classification stated on the `A ⋊[φ] G` model).
This is the pure orbit-method assembly of `Etingof.Theorem5_27_1` for the `ℤ/2`-inversion
action on `Â = Multiplicative (ZMod N) →* ℂˣ`.

## Overall project progress

Stage 3 formalization ongoing. One genuine `sorry` removed from the dihedral landscape by
reducing `dihedral_classification` to the model-level `semidirect_classification`, plus two
reusable pieces of infrastructure (a `DihedralGroup`-as-semidirect-product iso that Mathlib
lacks, and a `Simple`/`finrank`-preserving `FDRep` transport along a group iso). The analogous
affine (#6458) and Heisenberg assemblies can reuse the same transport pattern.

## Next step

Claim **#6471** and prove `semidirect_classification`. Roadmap (in the issue): apply the engine
to get `dualSmul/stab/V/(i)-(v)`; show `dualSmul` is inversion and `stab χ = ⊤` (self-inverse)
or `⊥` (free); dims 1 vs 2 from (v) + `AbelianFDRep.charFDRep_finrank`; build the transversal
family (set up `MulAction` from `dualSmul`, use `Quotient.out` orbit reps); discharge
simplicity/non-iso/completeness from engine (i)/(ii)/(iii) + `AbelianFDRep`; counts from
`DihedralCharacterCombinatorics.card_selfInverse` / `card_freeOrbitPairs`.

## Blockers

None. #6471 is unblocked (all its dependencies — the engine, `AbelianFDRep`,
`DihedralCharacterCombinatorics`, and the landed transport infra — are in place).
