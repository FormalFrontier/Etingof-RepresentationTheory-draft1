## Accomplished

- **Proved `inducedRepV_orbit_injectivity`**: If V(χ₁,U₁) ≅ V(χ₂,U₂) as FDRep, then χ₁ and χ₂ are in the same G-orbit. This is part (ii) of Theorem 5.27.1 (Mackey machine classification).
  - Uses eigenvalue argument: the intertwining map T preserves A-eigenspaces, so the A-eigencharacters must match at every coset where Tf is nonzero.
  - Key helper `A_action_scalar`: shows A acts by scalar multiplication by the dual-translated character at each coset.
  - Fixed the `hT_comm` equivariance proof: `FDRep.Iso.conj_ρ` unfolds `conj` to `arrowCongr`, requiring `simp only [h, LinearEquiv.conj_apply, LinearMap.comp_apply, LinearEquiv.coe_coe]` followed by a `change` to fold back `FDRep.isoToLinearEquiv e` into `T`.

## Current frontier

- **2 sorrys remain in Theorem5_27_1.lean**:
  1. `simple_fdRep_isIrreducible` (line 467): Bridge lemma `Simple U → IsIrreducible (FDRep.ρ U)`. Requires constructing an FDRep mono from a Subrepresentation — non-trivial category-theoretic plumbing between Subobject (categorical) and Subrepresentation/Submodule (algebraic).
  2. `inducedRepV_completeness` (line 1100): Every irrep of G ⋉ A arises as some V(χ,U). Book proof uses dimension counting: Σ dim V(O,U)² = |G||Â| = |G ⋉ A|. Needs sum-of-squares formula for finite groups, orbit-stabilizer theorem, etc.

## Overall project progress

- Theorem 5.27.1 orbit method classification: 2 of 4 parts proven (irreducibility via `inducedRepV_simple`, orbit injectivity via `inducedRepV_orbit_injectivity`). Completeness and bridge lemma remain.
- The main theorem statement is fully formalized with all infrastructure in place.

## Next step

- **Bridge lemma** (`simple_fdRep_isIrreducible`): Most promising approach is constructing the FDRep mono from a Subrepresentation directly (σ.toSubmodule is finite-dimensional, inclusion is equivariant). Alternatively, prove `Simple U ↔ Simple (E.functor.obj (forget₂.obj U))` using `Simple.of_iso` with the unit iso + `simple_of_full_faithful_preservesMono'' E.inverse`.
- **Completeness**: Requires significant algebra infrastructure (sum of squares of irrep dimensions = group order, orbit-stabilizer). May benefit from decomposition into sub-issues.

## Blockers

- None blocking. The two remaining sorrys are independent proof obligations.
