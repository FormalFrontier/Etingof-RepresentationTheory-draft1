## Accomplished

Session type: **feature** (`/work` → `/feature`), issue **#6814**
(fix `Problem_8_2_8_ext` spec, then prove it).

Corrected the ill-posed `Problem_8_2_8_ext` signature in
`EtingofRepresentationTheory/Chapter8/Problem8_2_8.lean` (the file builds; only the
expected `sorry` warning remains):

- **Added finiteness hypotheses** `[FiniteDimensional k A₁] [FiniteDimensional k A₂]`
  and `[FiniteDimensional k M₁] [FiniteDimensional k M₂]`. The original signature only
  asked `Nᵢ` finite-dimensional, which is insufficient: the crux degreewise map
  `Hom_{A₁}(P₁,N₁) ⊗ₖ Hom_{A₂}(P₂,N₂) → Hom_{A₁⊗A₂}(P₁⊗P₂, N₁⊗N₂)` is an iso only when the
  `Pᵢ` are finitely generated projective, which requires the `Mᵢ` finite-dimensional.
- **Strengthened the conclusion** from `Nonempty (… ≃+ …)` to `Nonempty (… ≃ₗ[k] …)`,
  matching the strength of the `Tor` half `Problem_8_2_8_tor`. Confirmed the k-module
  instances on both sides resolve automatically (LHS via `Linear k (ModuleCat (A₁⊗ₖA₂))`
  from `ModuleCat.Algebra`; RHS as a direct sum of `TensorProduct k`s).
- Updated the module + theorem docstrings and `progress/items.json` coverage note.

Per the issue's own Scope guard (the full proof is a cohomological twin of the entire
`Tor` stack — too large for one session), **decomposed the proof into four sub-issues**:

- **#6815** left-module external-tensor stack (cohomological twin of `ExternalTensorResolution`)
- **#6816** f.g.-projective Hom-tensor iso (the crux; where `Mᵢ`-finiteness is consumed)
- **#6817** cohomological Künneth over `k` (cochain analogue of `kunnethChainComplexNat` +
  `ProjectiveResolution.isoExt` hookup)
- **#6818** assembly of `Problem_8_2_8_ext` (blocked on #6815/#6816/#6817)

## Current frontier

Statement fix landed against the corrected signature with proof `sorry`. The three
building-block sub-issues (#6815, #6816, #6817) are independent and unclaimed; the assembly
#6818 is blocked until they close.

## Overall project progress

Stage 3 (formalization). `Chapter8/Problem8.2.8`: `Tor` half sorry-free (#6796); `Ext` half now
has a **correct, faithful, provable** signature (this PR) with the proof decomposed for
follow-up. No new sorries introduced anywhere else.

## Next step

A worker should claim #6815 or #6816 (both independent, no blockers). #6816 (the f.g.-projective
Hom-tensor iso) is the mathematical heart and the smallest self-contained piece — good first
target.

## Blockers

None. #6818 is intentionally blocked on its three building-block sub-issues.
