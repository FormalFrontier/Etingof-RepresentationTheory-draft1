## Accomplished

Completed the Chapter 9 fidelity audit (issue #5346, wave 9):

- Re-judged the 5 items previously marked `gap` (Theorem9.2.1 #5669,
  Definition9.2.2 #5664, Definition9.3.1 #5631, Example9.5.2 #5648,
  Theorem9.6.4 #5665) against the current post-repair Lean: all five repairs
  are on `main` and faithful → flipped to `verified`, stale
  `fidelity_issue`/`fidelity_note` reconciled.
- Normalized Corollary9.7.3's non-standard `fidelity: "faithful"` →
  `verified` (#5738 gap 2 closed by `Corollary_9_7_3_i_categorical_fgModule`).
- Re-audited the 12 previously-`verified` items with three parallel
  sub-auditors (wave-7 precedent). 11 confirmed; **Definition9.5.1 refuted to
  `gap`** (new repair issue #5850): `AreLinked` is defined on all modules
  (chains through arbitrary modules — strictly coarser on simples than the
  book's simple-module chains), and `Etingof.Block` drops the book's
  JH-factor block subcategory entirely.
- Removed Definition9.7.2's stale `fidelity_issue` #5386 (closed, repair on
  main).
- Wrote the wave certificate `progress/coverage-audit/fidelity-wave-9.md`.

Final Chapter 9 state: 18 claim-bearing items = 17 `verified` + 1 `gap`
(#5850). No Lean changes; only `progress/items.json` and the wave file.

## Current frontier

Stage 3.7 fidelity sweep: Chapters 5, 6, 7, 9 now have wave certificates.
Chapter 9's single open gap is #5850 (Definition9.5.1 linking relation).

## Overall project progress

Chapter 9: 18/18 claim-bearing items audited (17 verified, 1 gap with open
repair issue). Earlier chapters per their own wave certificates.

## Next step

A worker should claim #5850 (restore the simple-module chain condition in
`Definition9_5_1.lean` and define the block subcategory via JH factors;
update `Example9_5_2.lean` consumers — their content survives, see the issue
body). A future fidelity wave reconciles #5850 once merged.

## Blockers

None.
