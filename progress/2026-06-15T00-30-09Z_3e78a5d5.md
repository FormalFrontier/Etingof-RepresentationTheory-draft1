## Accomplished

Worked issue #4694 (det⁻¹-elimination **kernel lemma (K)** — the genuine
mathematics of Theorem 5.23.2(i)'s polynomial case). After deep investigation I
established that the theorem's strategy relies on infrastructure that is **absent
from both the repo and Mathlib**, then did a worker-led decomposition (flow skill
Step 4b), which the issue explicitly sanctions ("decomposed with a clearly-scoped
residual sorry + sub-issues").

Concrete deliverables:

- **`progress/kernel-lemma-K-route.md`** — a sharp route analysis that advances
  the problem:
  - **Det-power filtration reduction (K) ⟸ (K′):** filter `O = k[Xᵢⱼ][det⁻¹]` by
    `A_r = det^{-r}·A`; the GL-quotient `A_r/A_{r-1} ≅ (A/det) ⊗ χ^{-r}`; a
    nonneg-weight subrep escaping `A` would give a nonzero nonneg-weight subrep of
    `(A/det)⊗χ^{-r}` (`r ≥ 1`), contradicting (K′).
  - **Explicit proof the torus-only route is insufficient:** full-torus
    right-multiplication yields only "every monomial of the cleared numerator has
    all column sums ≥ r", which is NOT `det^r ∣ Q` (`α X₁₁X₂₂ + β X₁₂X₂₁`,
    `N=2`). Confirms the route doc's claim with the actual computation.
  - **(K′) pinned as the deep core:** ⟺ every constituent of `k[Xᵢⱼ]/(det)` has
    highest weight `ν_N = 0` — i.e. the GL×GL-equivariant **Cauchy** decomposition
    + `det·A ≅ A⊗χ`.
  - **Missing-infra list:** (1) `det` irreducibility (absent in repo AND Mathlib —
    verified by grep); (2) equivariant Peter–Weyl (`Theorem5_23_2_ii` is only a
    **rank** iso via `nonempty_linearEquiv_of_rank_eq`, no weight data); (3) GL_N
    highest/lowest-weight theory (classification entry `iso_of_formalCharacter_eq_schurPoly`
    is itself an open `sorry`, #4699).

- **Sub-issues filed:**
  - **#4712** (`sub-K-det`, not blocked): `det` irreducibility/prime in
    `MvPolynomial (Fin N × Fin N) k` + det-power normal form on `A[det⁻¹]` +
    faithful functions-on-GL model. The elementary, session-sized prerequisite.
  - **#4713** (`sub-K-cauchy`, blocked on #4712): (K′), the deep
    representation-theoretic core; flagged for further decomposition into the
    equivariant Cauchy decomposition + highest-weight theory.

## Current frontier

#4694 reduced to (K′) (#4713) over the det-localization prerequisite (#4712).
The interface #4695 (assembly) consumes is documented (functions-on-GL form via
`evalAtGL`/`eq_of_eval_eq_on_gl`).

## Overall project progress

Stage 3 formalization. The polynomial-rep → V^⊗n embedding chain is sorry-free
above the single `detInv_elim_of_polynomial` sorry; that sorry is now backed by a
concrete reduction and a two-step prerequisite chain (#4712 → #4713 → narrowed
#4694 assembly → #4695). No Lean code changed this turn (decomposition only).

## Next step

Claim **#4712** (det irreducibility + normal form) — self-contained, no rep
theory, the natural unblocking move. Then #4713 (deep, likely needs the
equivariant Cauchy decomposition, related to #4699 / making `Theorem5_23_2_ii`
equivariant).

## Blockers

#4713 (K′) is genuinely research-level: it needs the GL×GL-equivariant Cauchy /
Peter–Weyl decomposition and GL_N highest-weight theory, neither of which exists
in the repo (only a rank-level Peter–Weyl) or Mathlib (no det irreducibility).
This is the real reason #4694 cannot be a single-session no-sorry proof.
</content>
