## Accomplished

One-shot feature session `5e93201a` (`/once 5274`): completed **all three deliverables of #5274**
in `EtingofRepresentationTheory/Chapter9/KrullSchmidt/Length.lean`, sorry-free modulo the
pre-existing `clength_*` sorries (`clength_additive`, `clength_pos_of_not_isZero`,
`clength_eq_zero_iff`, `clength_biprod`).

1. **Monotonicity (deliverable 1).** `clength_ofLE_add_cokernel` (additivity across the canonical
   SES `0 → A → B → cokernel(ofLE A B h) → 0`, built from `ShortComplex.cokernelSequence` +
   `ShortExact.mk'`), `clength_mono` (`A ≤ B → clength A ≤ clength B`), and `clength_strictMono`
   (`A < B → clength A < clength B`; strictness from `clength_pos_of_not_isZero` on the nonzero
   quotient, via cokernel-zero ⟹ epi ⟹ iso ⟹ `B ≤ A` contradiction).

2. **Chain conditions (deliverable 2).** `wellFoundedLT_subobject` (a `WellFoundedLT (Subobject X)`
   instance: `clength` strictly monotone ⟹ `Subrelation.wf` over `InvImage (<) clength`),
   `exists_eventually_constant_of_antitone` (descending chains, via Mathlib's
   `WellFoundedLT.antitone_chain_condition`), and `exists_eventually_constant_of_monotone`
   (ascending chains, via a bounded-monotone-ℕ helper using `Nat.sSup_mem`, since `clength` is
   bounded by the length of the underlying object of `⊤`).

3. **Endomorphism corollary (deliverable 3).** The image chain `imageSubobject (fⁿ)` is antitone and
   the kernel chain `kernelSubobject (fⁿ)` is monotone (via `imageSubobject_comp_le` /
   `kernelSubobject_comp_le` and `pow_succ`/`pow_succ'` with `End.mul_def`); both stabilise
   (`exists_image_kernel_pow_stabilizes` gives a single positive `n` with `im(fⁿ)=im(f^{2n})`,
   `ker(fⁿ)=ker(f^{2n})`). Then `exists_pow_stabilizes` proves the exact signature the issue
   requested: `IsIso (Abelian.image.ι (fⁿ) ≫ Abelian.factorThruImage (fⁿ))`. The proof is a
   pseudoelement chase (`Abelian.Pseudoelement` API): `mono_of_zero_of_map_zero` + the kernel
   stabilisation for mono, `epi_of_pseudo_surjective` + the image stabilisation for epi, then
   `isIso_of_mono_of_epi` (abelian is balanced).

## Verification

- `lake build EtingofRepresentationTheory.Chapter9.KrullSchmidt.Length` succeeds (2155 jobs).
- Downstream `Fitting`, `Existence`, `Exchange` still build.
- Sorry count in the file: still 4 (the pre-existing `clength_*` sorries); all new declarations are
  sorry-free. Length is CI-reachable via `Existence → Introduction_9_7 → Chapter9`.

## Current frontier

`#5274` is complete. The only consumer of `exists_pow_stabilizes` is `fitting_decomposition`
(`KrullSchmidt/Fitting.lean`, #5230), which is still a documented `sorry` — it can now be discharged
using `exists_pow_stabilizes` plus the block-bicone construction.

## Overall project progress

Etingof draft 1, Stage 3. Chapter 9 Krull–Schmidt chain: link 1/5 (`Length.lean`) now has its full
monotonicity + chain-condition + im/ker-stabilisation API (sorry-free), feeding link 3/5
(`Fitting.lean`). The remaining `Length.lean` sorries (`clength_additive` etc.) are the categorical
Jordan–Hölder finiteness content, tracked separately.

## Next step

Discharge `fitting_decomposition` (#5230) in `KrullSchmidt/Fitting.lean` by consuming
`Etingof.exists_pow_stabilizes`: the iso `g'` on `Abelian.image (fⁿ)` packages into the
`X ≅ ker(fⁿ) ⊞ im(fⁿ)` block splitting. Separately, the `clength_*` sorries remain the genuine
Jordan–Hölder finiteness work.

## Blockers

None.
