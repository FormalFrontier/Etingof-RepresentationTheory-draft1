## Accomplished

Claimed #6471 (feat Ch5 Exercise 5.27.2: prove `semidirect_classification` orbit assembly). On
orientation the plan was found **materially stale**: it described Deliverables 1 & 2
(`DihedralSemidirect`, `dihedralEquiv`, `dihedralRepEquiv`, `semidirect_classification`) as
"landed and sorry-free," but **none existed** — the file was only the 76-line statement pass with
a single sorry in `dihedral_classification` (not `semidirect_classification`, which did not exist).
The sibling `Exercise5_27_2_Affine.lean` is likewise unassembled (2 sorries), so there was no
template.

Rather than skip, salvaged by building the missing foundational prerequisite (Deliverable 1),
sorry-free, in `Exercise5_27_2_Dihedral.lean` (commit 60fa4bb1):

- `invAut N : MulAut (Multiplicative (ZMod N))` (`MulEquiv.inv`), `invAut_apply`, `invAut_mul_self`.
- `dihedralφ N : Multiplicative (ZMod 2) →* MulAut (Multiplicative (ZMod N))` (generator `ofAdd 1`
  acts by inversion), `dihedralφ_one`, `dihedralφ_ofAdd_one`.
- `DihedralSemidirect N := Multiplicative (ZMod N) ⋊[dihedralφ N] Multiplicative (ZMod 2)`.
- `dihedralEquiv N : DihedralGroup N ≃* DihedralSemidirect N` (r i ↦ ⟨ofAdd i, 1⟩,
  sr i ↦ ⟨ofAdd (-i), ofAdd 1⟩). `#print axioms dihedralEquiv` = clean (no `sorryAx`).

Key Lean patterns for the ℤ/2 casing: `private lemma zmod2_cases : ∀ x : ZMod 2, x = 0 ∨ x = 1 :=
by decide`, then `rcases … <;> simp only [… , show (1:ZMod 2)+1 = 0 by decide, reduceIte]`. The
`toAdd_ofAdd`/`ofAdd_toAdd`/`ofAdd_neg`/`toAdd_eq_zero`/`toAdd_mul` lemmas are **root namespace**,
not `Multiplicative.`. `rw [if_pos/if_neg h]` fails on ZMod 2 conditions (Decidable-instance
mismatch); use `simp only [hcond, reduceIte]` instead.

Created follow-up **#6475** with an accurate scope: prove `dihedral_classification` via the orbit
method on the now-landed scaffolding (state `semidirect_classification`, build the `dihedralRepEquiv`
representation transport, apply `Theorem5_27_1`, derive counts). It carries `depends-on: #6471`
(blocked until the scaffolding merges to main).

## Current frontier

`Exercise5_27_2_Dihedral.lean` builds; single remaining sorry in `dihedral_classification`
(line ~132). Deliverable 1 scaffolding complete and axiom-clean.

## Overall project progress

Stage 3 formalization. Chapter 5 orbit-method (Theorem 5.27.1, 0 sorry) exercises: Exercise 5.27.2
now has its dihedral semidirect scaffolding landed; the orbit assembly + affine + Heisenberg
assemblies remain (issues #6475, #6448, and the affine one).

## Next step

Land the partial PR for #6471 (Deliverable 1), then a worker picks up #6475 once unblocked to
prove `dihedral_classification` via the orbit method.

## Blockers

None. #6475 is intentionally blocked on #6471 until the scaffolding merges.
