## Accomplished

Fixed the regression in `EtingofRepresentationTheory/Chapter4/Problem4_12_7.lean`
(issue #7539). The file `lake env lean` direct-check previously failed at line 427
with four unsolved goals in `quaternionBasis_zero`:

```
QuaternionAlgebra.re 1 = 1
QuaternionAlgebra.imI 1 = 0
QuaternionAlgebra.imJ 1 = 0
QuaternionAlgebra.imK 1 = 0
```

After `Module.Basis.apply_eq_iff.mpr; ext i; fin_cases i <;> simp [...]`, each branch
reduces to one component equality for `(1 : ℍ[ℝ])`. The `@[simps]`/`scoped simp`
component lemmas for `1` on the general 4-parameter `QuaternionAlgebra` no longer close
these under the file's simp set (the file works in `QuaternionAlgebra ℝ (-1) 0 (-1)` after
the `change`, not the `Quaternion ℝ` abbreviation whose `re_one` etc. are keyed on `ℍ[R]`).
The goals hold by `rfl` since `One ℍ = ⟨⟨1,0,0,0⟩⟩`, so the fix appends `<;> rfl`:

```
fin_cases i <;> simp [QuaternionAlgebra.coe_basisOneIJK_repr] <;> rfl
```

## Current frontier

- `lake env lean EtingofRepresentationTheory/Chapter4/Problem4_12_7.lean` now elaborates
  with no errors (only pre-existing `linter.unusedSimpArgs` warnings remain).
- No `sorry`/`admit`/`axiom` in the file.
- `#print axioms` on the six audited endpoints (`real_irreducible`, `finrank_quaternion`,
  `star_mul_rev`, `normSq_mul`, `unit_quaternions_mulEquiv_SU2`, `finrank_commutant`,
  `exists_surjective_hom_to_SO3`) reports only `[propext, Classical.choice, Quot.sound]`
  — no `sorryAx`. The explicit `{±1}` kernel of the SO(3) surjection is preserved.

## Overall project progress

Stage 3 (formalization). This was a single-file regression repair; no structural change.

## Next step

Other `fix(Ch4/Ch5)` restore issues remain (#7537, #7540, #7544, #7545, #7547, #7548);
several depend on rebuilding shared broken sources (#7535, #7554, #7546). Pick a leaf.

## Blockers

None.
