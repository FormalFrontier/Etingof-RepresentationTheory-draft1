## Accomplished

One-shot session on issue #5898 (statement pass: Chapter 4 exercises). Landed faithful
Lean statements (sorry proofs) for **3 of the 14** listed exercises, each a single-fact
statement fully captured:

- **Problem 4.1.4** (`Chapter4/Problem4_1_4.lean`): irreducible representations of a
  `p`-group over a characteristic-`p` field are trivial (every `g` acts as identity).
- **Problem 4.12.4** (`Chapter4/Problem4_12_4.lean`): a finite graph with nonabelian
  automorphism group has a repeated adjacency eigenvalue (charpoly over ℝ not squarefree).
- **Problem 4.12.10** (`Chapter4/Problem4_12_10.lean`): every irreducible complex rep of a
  finite group occurs inside a tensor power `V^{⊗n}` of a faithful rep, formalized as a
  nonzero equivariant map into the diagonal tensor-power representation. Includes a genuine
  construction of `diagTensorPow` (real def, not a sorry'd body) via `PiTensorProduct.map`.

All three build with only expected `sorry` warnings; added to `Chapter4.lean` aggregator;
`progress/items.json` updated to `statement_formalized`.

## Current frontier

The other 11 Chapter 4 exercises are multi-part classification/construction problems
(dihedral/Heisenberg/ax+b/SU(2)/SO(3)/elasticity reps, icosahedron decomposition) or have
a count-of-irreducibles statement that resists a clean non-vacuous single-session
formalization. These were left for follow-up rather than shipping fragile/partial/vacuous
statements.

## Overall project progress

Chapter 4 exercise statement pass: 3 more exercises now `statement_formalized`. The bulk of
Chapter 4's proved theory (Sections 4.1–4.10) is already sorry-free; this work is on the
exercise backlog (statement phase).

## Next step

Follow-up issue **#5904** covers 9 of the remaining exercises (Exercise 4.3.1, Problems
4.12.1/2/5/6/7/8/9/11). I commented on #5904 correcting its preamble: **Exercise 4.2.3**
and **Problem 4.5.2** were assumed done by #5898 but were NOT formalized — they remain
`not_formalized` and need adding to #5904 (or a small companion issue). A worker should pick
up #5904 and decompose per-exercise as needed.

## Blockers

None. Remaining exercises are hard-to-state (multi-part / non-vacuous-count) but not blocked.
