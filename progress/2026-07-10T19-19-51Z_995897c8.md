## Accomplished

Feature session for **#6247** — the A₅ permRep isotypic/multiplicity engine shared by the three
icosahedral decomposition theorems (#6229/#6230/#6231). All four deliverables landed in
`EtingofRepresentationTheory/Chapter4/Problem4_12_5.lean`, sorry-free:

- **D1 — character additivity.** `fixCard_eq_sum_subChar`: the fixed-point character of
  `permRep act g` equals `∑ k, subChar (S k)` over any internal direct sum, by trace additivity
  (`LinearMap.trace_eq_sum_trace_restrict`).
- **D2 — type classification.** `subFDRep` (the `FDRep ℂ A₅` carried by an invariant submodule),
  `subFDRep_simple` (each `IsIrredSub` summand is a simple `FDRep`), and
  `exists_subFDRep_iso_irrepA5` (it is `≅ irrepA5 t`, completeness #6244). The
  `asSubmodule ≃ₗ toRepresentation.asModule` transport was inlined as `toRepAsModuleEquiv` to
  avoid a Chapter4 → Chapter5 import (`Etingof.isSimpleModule_toRepresentation_asModule` lives in
  Chapter5).
- **D3 — multiplicity = inner product.** Bundled into master theorem
  `exists_typed_isotypic_decomposition`: produces `S`, `type`, `IsInternal`, per-summand
  `subChar = (irrepA5 (type k)).character` and `finrank = ![1,3,3,4,5] (type k)` (#6246), and
  `#{k | type k = i} = ⅟|A₅| • ∑_g χ_perm(g) · χ_i(g⁻¹)` via `FDRep.char_orthonormal`.
- **D4 — packaging consumer.** `exists_sorted_isotypic_decomposition`: the same decomposition
  reindexed by `Tuple.sort` so `type` is monotone, with `IsInternal`/`IsIrredSub` transported
  through the permutation and the multiplicity clause shown permutation-invariant. This is the
  shape the per-part theorems reindex to; the 3≇3' hook is available via `hchar` at `classRepA5 3`.

New import: `Example4_8_1.A5Complete` (brings `irrepA5`, `simple_iso_irrepA5`, `irrepA5_finrank`,
`irrepA5_pairwise`, `classRepA5`) and `Exercise4_2_3` (`simple_fdRepOf_of_isSimpleModule`).

## Current frontier

`lake build EtingofRepresentationTheory.Chapter4.Problem4_12_5` succeeds. The only remaining
sorries in the file are the three per-part decomposition theorems `vertices_decomposition`
(:432), `faces_decomposition` (:450), `edges_decomposition` (:469) — issues #6229/#6230/#6231,
explicitly out of scope for #6247.

## Overall project progress

Phase 3 formalization ongoing. This lands the shared isotypic layer that #6229/#6230/#6231 were
blocked on; they can now consume `exists_sorted_isotypic_decomposition` plus the fixed-point
character (#6245 for vertices) to finish.

## Next step

Work #6229 (vertices): use `exists_sorted_isotypic_decomposition`, evaluate the multiplicity
clause with the vertex fixed-point character `(12,0,0,2,2)` (from #6245) to get
`mult = (1,1,1,0,1)`, deduce `m = 4` and (by monotone `type`) `type = ![0,1,2,4]`, then reindex to
the `Fin 4` dimensions `(1,3,3,5)`. #6230/#6231 are analogous with characters `(20,0,2,0,0)` /
`(30,2,0,0,0)`.

## Blockers

None. #6247 complete.
