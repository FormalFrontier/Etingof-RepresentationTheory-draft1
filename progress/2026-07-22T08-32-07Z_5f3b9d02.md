## Accomplished

`/work` → `/feature` session, claimed **#7287** (feat, Ch4: prove
`Problem4_12_10_symmetric` via orbit-interpolation + Maschke). Assessed scope:
the book's hint needs two hard infrastructure pieces beyond one session, so
**decomposed** and landed the mechanical pillars as a partial PR.

Landed in `EtingofRepresentationTheory/Chapter4/Problem4_12_10_Symmetric.lean`
(all sorry-free, axiom-clean `[propext, Classical.choice, Quot.sound]`):

- `exists_component_intertwiner_of_ne_zero` + `Representation.directSum_component_equivariant`
  — a nonzero G-equivariant map into a direct sum of representations yields a nonzero
  G-equivariant map into a single summand (turns `Hom_G(W, ⊕ₙ SⁿV) ≠ 0` into
  `Hom_G(W, SⁿV) ≠ 0` for one `n`).
- `Representation.intertwinerOfAsModuleHom` (+ `_apply`, `_equivariant`, `_injective`)
  — the reverse of `asModuleHomOfIntertwiner`: repackage a `MonoidAlgebra k G`-linear map
  of `asModule`s as a G-equivariant k-linear intertwiner. `asModuleEquiv = LinearEquiv.refl`,
  so equivariance is `map_smul h (single g 1) · + single_smul`, closed by `exact key`.
- `exists_injective_intertwiner_ofMulAction` — a representation whose `asModule` is a simple
  `k[G]`-module embeds G-equivariantly in the regular rep `ofMulAction k G G`. Proof:
  `isSimpleModule_iff_quot_maximal` gives `σ.asModule ≃ k[G]⧸I`; the regular module is
  semisimple (Maschke instance `IsSemisimpleModule k[G] V`), so the surjection
  `k[G] ↠ k[G]⧸I ≃ σ.asModule` splits via `IsSemisimpleModule.lifting_property`; the section
  is injective and converted with `intertwinerOfAsModuleHom`.

## Current frontier

`Etingof.Problem4_12_10_symmetric` remains the single `sorry` in the file. The two
supporting pillars above + the pre-existing `exists_trivialStabilizer_covector` are done.
Decomposed the remaining work into:

- **#7289** (crux, no deps): construct the G-equivariant **surjection**
  `E : ⊕ₙ Sym[ℂ]^n V ↠ ofMulAction ℂ G G` — the symmetric-tensor orbit-evaluation
  `t ↦ (g ↦ ∑ₙ evalAt ((dual ρ) g u) (tₙ))`, with surjectivity via polynomial
  point-separation on the distinct orbit `{(dual ρ) g u}`.
- **#7290** (assembly, depends on #7289): discharge `Problem4_12_10_symmetric` by splitting
  `E` (Maschke `lifting_property`), composing with `exists_injective_intertwiner_ofMulAction`
  and `exists_component_intertwiner_of_ne_zero`, then flip `items.json` to `covered_full`.

Parent #7287 has a `Decomposed into #7289, #7290` breadcrumb for planner triage.

## Overall project progress

Etingof draft 1, Stage 3, Chapter 4. Problem 4.12.10 (symmetric-power form): statement
infrastructure (#7285/#7288) + the two assembly pillars (this PR) now in; the crux
(evaluation surjection #7289) and final assembly (#7290) remain. Tensor-power form
(`Problem4_12_10`) already fully proved.

## Next step

- **Worker:** #7289 — the orbit-evaluation surjection. This is the only genuinely hard
  remaining piece (evaluation map construction + point-separation surjectivity). Consider
  building `evalAt p n` via `symPowEmbedding` + `PiTensorProduct.map (fun _ => p)`, and the
  finite-domain algebraic Stone-Weierstrass fact for surjectivity.
- **Worker:** #7290 once #7289 merges (auto-unblocks via `check-blocked`).

## Blockers

None. #7290 is `blocked` on #7289 by design.
