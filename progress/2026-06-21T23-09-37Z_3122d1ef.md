## Accomplished

Decomposed #4979 (general-`k` classification `Theorem5_12_2_classification_general`)
into two ordered sub-issues — the full task spans general-`k` ports of the
Young-projector vanishing chain, distinctness, the ~370-line Wedderburn block
machinery, and counting, which is too large for one session:

- #4981 (sub-A): general-`k` Young-projector vanishing + distinctness.
- #4982 (sub-B, blocked on #4981): block machinery + counting + classification
  assembly.

Executed #4981. Added `Chapter5/Theorem5_12_2_DistinctGeneral.lean` (sorry-free):
- `RowSymmetrizerK`, `ColumnAntisymmetrizerK` — the symmetrizer factors of
  `YoungSymmetrizerK` over a general commutative ring, with
  `youngSymmetrizerK_eq_factored` (`c_λ = b_λ · a_λ`, by `rfl`).
- `RowSymmetrizerK_mul_of_row`, `of_col_mul_ColumnAntisymmetrizerK` — Lemma 5.13.1
  absorption over `k`.
- `basis_vanishing_K`, `Lemma5_13_2_K` — Lemma 5.13.2 vanishing
  (`a_λ · x · b_μ = 0` for `¬ μ.Dominates λ`) over `[Field k] [CharZero k]`,
  reusing the field-free `pigeonhole_transposition_general`. Only field-specific
  input is `(2 : k) ≠ 0`.
- `youngSymmetrizerK_sq_ne_zero` (`c_λ² ≠ 0`) via `YoungSymmetrizerK_sq_scalar` +
  `_ne_zero_general` + `c_λ(1) = 1`.
- `youngSymmetrizerK_annihilates_specht` and the deliverable
  `Theorem5_12_2_distinct_general`: for `la ≠ mu`,
  `IsEmpty (SpechtModuleK k n la ≃ₗ SpechtModuleK k n mu)`.

Registered the file in `EtingofRepresentationTheory/Chapter5.lean`. ℂ theorems
left intact.

## Current frontier

#4981 complete and building. #4982 (block machinery + counting + assembly)
remains, blocked on #4981 until it merges.

## Overall project progress

Stage 3 (formalization) ongoing. General-`k` classification half of Theorem 5.12.2:
Specht simplicity (#4973/#4980) and now distinctness (#4981) are done; the
Wedderburn assembly (#4982) is the last piece.

## Next step

After #4981 merges, claim #4982: port the Wedderburn block machinery
(`centralIdem'` … `same_block_iso`) and `irrepDecomp_n_le_card_partition` over
`k` (mechanical, no Specht dependency), then
`exists_young_symmetrizer_nontrivial_general` + `Theorem5_12_2_classification_general`,
using `Theorem5_12_2_distinct_general` (this PR) and the existing general-`k`
Specht simplicity.

## Blockers

None.
