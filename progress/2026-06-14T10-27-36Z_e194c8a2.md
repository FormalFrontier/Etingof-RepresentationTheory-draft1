## Accomplished

Review session — soundness audit #4542 of the sporadic affine base cases
`etilde6/etilde7/t125_not_finite_type_per_kQ`. **Verdict: UNSOUND /
NEEDS-REWORK** for all three.

- Confirmed against the **current** `_kQ` defs (not the removed ℂ reps)
  that `etilde6Rep_kQ_isIndecomposable` (`FieldGenericETilde6.lean:291`)
  and `etilde7Rep_kQ_isIndecomposable` (`FieldGenericETilde7.lean:273`) are
  **mathematically false** for every `m ≥ 1`: the reps carry the identical
  single-nilpotent-twist maps (`etilde6Gamma_F` `(a,b,c)↦(a+b,a+Nc)` `:97`;
  `etilde7Arm1Embed_F` `(p,q)↦(p+q,p,q,Nq)` `:79`) that
  `indecomposability-framework-investigation.md` refuted with explicit
  decompositions. The `e_m` direction is free → peels off a 1-dim summand.
- Showed the hole is **live and load-bearing**: consumed across
  `FieldGenericTpqr.lean`, `FieldGenericNonAdjacentBranches.lean`,
  `FieldGenericAssembly.lean` (the not-finite-type half of the
  forbidden-subgraph classification).
- Refuted directive #4516's "dispatch to sound D̃₅/D̃₇" claim for the three
  **minimal** shapes: `subgraph_infinite_type_transfer_per_kQ`
  (`FieldGenericInfiniteType.lean:374`) only transfers subgraph→whole, and
  Ẽ₆=T(2,2,2)/Ẽ₇=T(1,3,3)/T(1,2,5) each have a single degree-3 vertex while
  D̃ₙ needs two → no D̃ subgraph exists. The D̃ dispatch is sound only for the
  non-minimal shapes that genuinely contain a D̃.
- Posted the verdict comment on #4542, filed follow-up feature issue **#4548**
  (corrected explicit indecomposable family — Jordan-tube / multi-twist), and
  warned **#2793** that "mirror etilde6" targets a refuted statement.

No Lean source changes (audit only).

## Current frontier

Audit complete. The sound replacement is specified in #4548 but not yet
built. #2793 needs replanning (close in favour of #4548 or re-scope to
depend on it).

## Overall project progress

Phase 3 formalization, Chapter 6 affine-Dynkin infinite-type direction. The
D̃ family is sound (#4530). The three minimal sporadic base cases are the
remaining genuine soundness hole, now precisely scoped in #4548.

## Next step

A planner should triage #2793 (the refuted-target issue) against #4548, and
prioritise #4548 as definition-level work. The owner should be aware that
directive #4516's "~80% landed / sound path exists" assessment overstates
completeness — the three minimal sporadic shapes still rest on a false lemma
(see #4542 verdict §3).

## Blockers

#4548 is definition-level (changes the `Rep_kQ` objects) and may need the
homogeneous-tube indecomposable construction, which is non-trivial. Not a
blocker for the audit itself, which is done.
