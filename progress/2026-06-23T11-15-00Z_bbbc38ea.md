## Accomplished
- Claimed #5082 (DetInvElim-clean constituent-character extractor).
- Unblocked the critical path: #5082's dependency #5081 (`CleanQuotientHelpers.lean`) was NOT
  on main (stuck in stale PR #5093, predates the v4.30.0-rc1 toolchain bump). Verified it builds
  on current toolchain and vendored it onto current main, registered in `Chapter5.lean`.
- Wrote `Chapter5/CleanConstituentExtraction.lean` with the sorry-free composition-series
  infrastructure for the clean extractor:
  - `subFDRep_iSup_glWeightSpace_eq_top` (sub-of-spanning glue over #5086's crux),
  - `subFDRep_isAlgebraic`,
  - `formalCharacter_eq_sum_simple_factors` — composition-series character additivity
    (`char M = ∑ char (W j)` over simple algebraic spanning factors; finrank induction +
    `formalCharacter_eq_sub_add_quotient`).
- Stated the headline theorem `clean_simple_constituent_formalCharacter_eq_schurPoly_mem` with a
  detailed multi-step spec at the remaining `sorry`.
- Verified the whole file's transitive import closure (57 modules) contains NO `DetInvElim` /
  `PolynomialGLDecomposition` / `SchurWeylFormalCharacterIso` — the core no-cycle requirement.
- Created sub-issue #5100 for the extractor tail (depends on #5082); left a `Decomposed into
  #5100` breadcrumb on #5082.

## Current frontier
- Extractor tail (the lone `sorry`): steps 1-5 in the sorry-site comment / #5100. The
  composition-series infrastructure reduces it to a finite character-bookkeeping step:
  peel `L ↪ M`, expand `char(M/L)` via the new additivity lemma, then dedup the reps by
  character value and kill the vanishing relation with the torus-trace engine. A clean
  `SchurModule` weight-spanning lemma is also needed (restrict spanning `glTensorRep`).

## Overall project progress
- Stage 3 (Ch5). Clean re-scope #4905/#5072: #5074/#5075/#5078/#5081/#5086 closed; #5082
  infrastructure now landed (this PR, partial); extractor tail tracked in #5100; downstream
  assembly #5076.

## Next step
- Work #5100: fill the extractor tail per the documented plan; mirror the polluted reference
  `ConstituentCharacterExtraction.lean:95-196` for the coefficient-matching shape, but use
  dedup-by-character + the torus-trace engine (not `decompose_polynomial_gl_rep`).

## Blockers
- None hard. #5093 (#5081's PR) is now redundant (its `CleanQuotientHelpers.lean` is vendored
  here onto current main) and can be closed by repair/planner.
