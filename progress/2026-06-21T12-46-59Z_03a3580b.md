## Accomplished

Completed **S4** (#4786) of directive #4777 (orbit-counting redirect for the
Chapter 6 infinite-type direction), in two commits:

**Part 1 — wire the forward direction.** Filled the single remaining
forward-direction `sorry` of `Etingof.Theorem_6_1_5` (finite type ⟹ Dynkin)
using the orbit-counting chain. Added `Etingof.standardOrientation` (orient each
edge `i ⟶ j` for `i < j`) + `standardOrientation_isOrientationOf` to
`OrientationDefs.lean` as the orientation witness for the abstract adjacency
matrix. The proof picks that orientation and `k = ℂ`, applies
`orbitRel_quotient_finite_of_isFiniteType` (finite type ⟹ finitely many
`G(m)`-orbits) and `repSpace_finrank_lt_repGroup_ambient_finrank` (strict
`dim W(m) < dim G(m)`), and packages via `isDynkinDiagram_of_strict_finrank`.
`Theorem_6_1_5` and both corollaries (`non_Dynkin_not_finite_type`,
`non_ADE_not_finite_type`) are now sorry-free on the orbit route.

**Part 2 — retire the construction track + reroute Chapter 2.** Deleted
`InfiniteTypeConstructions.lean` and all 15 `FieldGeneric*.lean` files
(~579 KB, ~83 sorries) and dropped their imports from `Chapter6.lean`. Found a
cross-chapter entanglement the directive did not anticipate:
`Chapter2/Theorem2_1_2.lean`'s `not_posdef_not_HasFiniteRepresentationType`
consumed `not_posdef_infinite_type_per_kQ` from `FieldGenericAssembly`. Rerouted
it onto the orbit route (finite rep type → finite `AreIsomorphic`-cover →
`orbitRel_quotient_finite_of_finite_reps` → strict dim bound →
`isDynkinDiagram_of_strict_finrank` → contradicts ¬ pos def). Dropped the now-dead
`IsFiniteTypeQuiverDimVec` notion from `FiniteTypeDefs.lean`.

Full project build is green (8336 jobs, exit 0, no `sorry` introduced).

## Current frontier

Directive #4777's closing condition is met: `Theorem_6_1_5` is sorry-free on the
orbit route and the construction-track files are deleted. PR opened to close
#4786 (and the directive #4777).

## Overall project progress

Chapter 6 Gabriel's theorem (Problem 6.1.5 / Theorem 2.1.2) now proved entirely
on the book's orbit-counting route; the off-book explicit-construction track is
gone. The orbit chain S-def→S0→S1→S2(a,b,c)→S3→S4 is fully landed.

## Next step

After this PR merges, a planner should close any remaining bookkeeping on the
S-* sub-issue chain and confirm directive #4777 is closed. `finite_dimVectors`
in `Theorem2_1_2.lean` is now an unused (but still valid) public lemma; a future
cleanup could remove it, but it is harmless.

## Blockers

None.
