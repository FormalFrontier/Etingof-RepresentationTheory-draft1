## Accomplished

Summarize session (issue #5018): refreshed the stale sorry-landscape +
design-walls snapshot. The previous snapshot (`progress/sorry-landscape.md`) was
from wave 63 (2026-05-20), ~200 merged PRs stale, and still pointed the planning
loop at Chapter 6 infinite-type work that is no longer the frontier.

Key finding — **the real sorry count is 4, not 76**. A comment-stripping awk pass
(`/- … -/` depth counter + `--` truncation, then whole-word `sorry` over the
surviving code) shows the entire `EtingofRepresentationTheory/` tree has exactly
**4 genuine proof-gap `sorry` tactics, all in Chapter 5**, plus **0 axioms and 0
admits**. A bare `grep -rc sorry` reports 76 because the project documents *where
sorries are and are not* in docstrings ("sorry-free", "sorry'd dependency
(#4832)", etc.) — a ~18× inflation. The new snapshot records both numbers so the
discrepancy is auditable, and pins each of the 4 real sorries to its declaration
and tracking issue:

1. `CauchyDetQuotient.lean:124` `quotDetRep_irreducible_constituent_lastWeight_zero` — #4905 ← #4961 ← #5003 ← PR #4997
2. `SchurWeylFormalCharacterIso.lean:200` `schurModule_isSimple_general` — #4946 → #4976 ← #4992/#5005
3. `SchurWeylSimplesClassification.lean:132` `schurWeyl_simples_formalCharacter_classification_core` — #4721; relocate/retire in #5023/#5024
4. `SpechtModuleBasis.lean:2345` `twistedPolytabloid_residual_invariant` — #5010 (the James/Fulton nut)

The snapshot also maps the Ch5 critical path (Threads A–D), flags that **main is
currently broken** (repair PR #5022 in CI; until it merges all feature CI fails),
and lists the four design walls.

## Current frontier

`progress/sorry-landscape.md` rewritten. No code touched. The pre-existing
uncommitted revert of `.claude/skills/agent-worker-flow/SKILL.md` in the worktree
was left untouched (not my work, unrelated to this issue) and excluded from the
commit.

## Overall project progress

Stage 3 formalization, Chapter 5 general-`k` Schur-Weyl / Specht push. 4 real
sorries remain. Bottlenecks: broken main (#5021/#5022), then PR #4997 CI
(unblocks Thread A's #5003→#4961→#4905), then #4992 (Thread B gate for #4976).
Threads C (#5010 nut) and D (#5023/#5024 retire) are the genuinely hard design
work.

## Next step

Planner: use the refreshed landscape to stop routing workers at `grep`-inflated
files. The actionable queue is effectively: repair #5022 (main), repair #4997 CI,
land #4992 — everything else is `blocked` on those. #5010 is worker-actionable
but needs a committed R1/R2 route, not a casual claim.

## Blockers

None for this summarize task — completed. (Project-level: main broken #5022, PR
#4997 CI failed, #4992 in flight.)
</content>
