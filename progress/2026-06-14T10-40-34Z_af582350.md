## Accomplished

Claimed #4548 (replace the refuted single-twist family for the sporadic
affine base cases Ẽ₆/Ẽ₇/T(1,2,5)). Investigated the indecomposability
framework and the three target files, determined the fix is a research-level
multi-session redesign, and decomposed it:

- Wrote `progress/sporadic-tube-redesign-design.md`: the shared mathematical
  design. Establishes that (a) the project proves indecomposability *only*
  via the submodule-splitting route (workhorse
  `nilpotent_invariant_compl_trivial_gen`; no `End`-local infra exists),
  (b) the three shapes are simple trees with a single trivalent vertex, so
  neither the cycle's identity-collapse nor D̃₅'s iso-bridge applies, (c) the
  forced fix is the homogeneous-tube construction with an eigenvalue `λ` and a
  Jordan block `λI+J` at the eigenvalue site, and (d) the Kronecker template
  showing the tube reduces to the *existing* workhorse via identity-arrow
  collapse. Decides against building `End`-local infrastructure.
- Created sub-issues: #4555 (foundational tube mechanism, validated on D̃₄),
  #4557 (Ẽ₆=T(2,2,2)), #4558 (Ẽ₇=T(1,3,3)), #4559 (construct t125Rep_kQ for
  T(1,2,5)). The latter three `depends-on: #4555` (auto-blocked).
- Left the `Decomposed into #4555, #4557, #4558, #4559` breadcrumb on #4548
  and routed it to `replan`.

## Current frontier

Design landed; implementation not started. The genuinely hard open part
(deferred to the sub-issues) is deriving the explicit rectangular matrices of
the regular simple `R_λ` for each non-Kronecker shape — these are the
homogeneous points of the canonical-algebra `P¹` family and are not worked
out in the design doc.

## Overall project progress

Stage 3 (formalization), Chapter 6 affine-infinite-type cluster. The D̃-family
(D̃₅/₆/₇/₈) is being built bottom-up and is largely sound; the sporadic
E/T base cases were found UNSOUND (#4542) and are now routed for a correct
redesign via #4555/#4557/#4558/#4559. No code change to the (false)
`*_isIndecomposable` sorries this turn — they remain as-is pending the
sub-issues; downstream consumers are unchanged.

## Next step

A worker should claim #4555 first (it unblocks the other three): factor out
the identity-arrow collapse helper, state the §3 reduction lemma, and validate
the eigenvalue/`J` tube pattern on D̃₄. Then #4557 → #4558 → #4559.

## Blockers

None procedurally. The mathematical blocker (explicit regular-simple matrices
for T(2,2,2)/T(1,3,3)/T(1,2,5)) is scoped into the per-shape sub-issues.
