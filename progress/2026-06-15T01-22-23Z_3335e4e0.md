## Accomplished

Feature session (UUID `3335e4e0`), issue #4700 (`etilde6Rep_kQ_leaf_equalities`,
Ẽ₆ canonical leaf-equality). **Partial** — deliverables 1 and 3 done,
deliverable 2 (the canonical center-collapse) reduced to a single sharply-stated
obligation and found to be a genuine wall.

Landed in `Chapter6/FieldGenericETilde6.lean`:
- Stated `etilde6Rep_kQ_leaf_equalities` (orientation-generic signature
  mirroring `d6tildeRep_kQ_leaf_equalities`, conclusion `W₁⟨2⟩ = W₁⟨4⟩ ∧
  W₁⟨2⟩ = W₁⟨6⟩`).
- Full orientation case tree over the six edges of `etilde6Adj`; the six
  non-canonical divergence points are `sorry` (deliverable 3, → sub-B #4639).
- All-canonical branch: extracted the six canonical push facts on `W₁`/`W₂`
  and wired the three arm criteria (`etilde6_armA/B/C_criterion`), reducing the
  goal to a single center-collapse obligation `hcenter` (the three
  plane-diagonal composites `compA=(0,x,x)`, `compB=(x,0,x)`, `compC=(x,Jx,0)`
  agree on membership in `W₁⟨0⟩`).
- `hcenter` is `sorry`, tracked by new sub-issue **#4750**.

Lean-plumbing lesson (recorded for successors): `etilde6Rep_kQ` is defined with
`by … exact {…}`, so `.obj v` is not `@[reducible]`. Instance synthesis for
`a ∈ W ⟨n, by omega⟩` fails because the `by omega` Fin proof is an unsolved
metavar during synthesis, blocking the `obj` reduction. Fix: write vertex
indices as OfNat literals `(n : Fin 7)` (which `obj` *does* reduce against),
not `⟨n, by omega⟩`. (d6tilde's analogous code happens to elaborate; the
robust idiom is the OfNat form.)

## Current frontier

`hcenter` (the canonical Ẽ₆ center collapse) is the genuine wall. Verified, with
a sharp obstruction, that the D̃₄/D̃₆ `core` mechanism does **not** port to the
Ẽ₆ three-arm star:
- the three mid→center maps embed three *overlapping* coordinate planes
  (pairwise sharing single blocks), so the naive pairwise leaf containments are
  **false** (`(0,x,x)∈W₁⟨0⟩ ⇏ (x,0,x)∈W₁⟨0⟩`; the difference `(-x,x,0)` is not
  a priori in `W₁⟨0⟩`), and
- the `core` residual is not single-block-separable, so `center3_sum_zero_F`
  does not apply.
Open question (in #4750): whether `hcenter` is provable for an *arbitrary*
complementary pair, or only via full indecomposability (circular with sub-C
#4577). This sharpens the prior session's family-wide "wall (A)" diagnosis to a
specific, new finding: even the **canonical** Ẽ₆ branch is blocked (unlike
d6tilde's canonical, which works via chain routing).

## Overall project progress

Endgame depth phase. Two walls remain (per prior session): (A) orientation-
generic affine-tube indecomposability — now sharpened: the Ẽ₆ star's *canonical*
leaf collapse is itself blocked on overlapping-plane geometry (#4750);
(B) Schur-Weyl highest-weight classification (Ch5). #4700 lands the Ẽ₆
leaf-equality interface (needed by sub-C #4577) but its canonical proof is open.

## Next step

Planner: triage #4750 — decide route (A) prove the overlapping-plane 3-block
collapse, or (B) re-scope the Ẽ₆ indecomposability route to fold the collapse
and the eigenvalue-site argument into one pass (avoiding a standalone
arbitrary-pair leaf-equality). The same wall likely covers the sporadic-tube
siblings (T(1,2,5) #4613, etc.); a single resolution may unblock several.

## Blockers

- #4750: the Ẽ₆ canonical center-collapse `hcenter` is not provable by the
  D̃₄/D̃₆ `core` mechanism (overlapping-plane star geometry); may need re-scoping.
- Non-canonical Ẽ₆ branches (#4639 sub-B) remain `sorry` (out of scope here).
