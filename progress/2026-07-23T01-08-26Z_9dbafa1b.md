## Accomplished

Worked issue #7519 (fix(Ch4): restore fresh-buildable character-basis theorem 4.2.1).

The defect: `Chapter4/Theorem4_2_1.lean` was marked `sorry_free`/`verified` in the tracker
but failed a direct `lake env lean` source check. The private helper
`toGroupAlgebra_injective` reduced its goal to `(0 : MonoidAlgebra k G) g⁻¹ = 0` via
`simp [h]`, which no longer closes: the zero there has type `MonoidAlgebra k G`, so
`Finsupp.coe_zero` (stated for `G →₀ k`) does not fire. Cached oleans hid this from the
aggregate build.

Changes (both in Chapter 4, sorry-free, full `lake build` green — 9229 jobs, exit 0):

- `Chapter4/Theorem4_2_1.lean`: replaced `by simp [h]` with `by rw [h]; rfl`. The zero
  Finsupp applied to any argument is definitionally `0`, so `rfl` closes it robustly after
  rewriting with `h : toGroupAlgebra f = 0`. Direct `lake env lean` check now passes with 0
  errors (only pre-existing unused-section-variable and deprecation warnings remain).

- `Chapter4/Theorem4_2_1_Test.lean` (new) + `Chapter4.lean`: downstream import/`#check` test
  pinning the three public endpoints — `Etingof.Theorem4_2_1` (spanning),
  `Etingof.Theorem4_2_1_linearIndependent` (independence), and
  `Etingof.classFunction_eq_zero_of_orthogonal_simples` (completeness) — via `#check` plus
  `example` signature-lock witnesses. Wired into `Chapter4.lean` so a future regression in
  the source is caught by a fresh downstream importer rather than hidden behind cached oleans.

No `sorry`, `admit`, or replacement axiom introduced.

## Current frontier

Issue #7519 fully addressed. All deliverables met: full source direct-checks, spanning and
linear-independence endpoints preserved, downstream `#check` test added.

## Overall project progress

Phase 3 (formalization) ongoing. This closes one of the "restore fresh-buildable"
completeness-audit regressions (files that fail a direct `lake env lean` check despite the
cached aggregate build passing). Many sibling issues remain unclaimed (#7507–#7522 and
others) — same class of cached-olean-masked source regressions.

## Next step

Pick another "restore fresh-buildable" completeness-audit issue. Several are crisp
single-failure fixes (e.g. #7511 §9.6 `exact_leftComp` elaboration mismatch); #7507
(Theorem 5.27.1) is large with many failures and better decomposed.

## Blockers

None.
