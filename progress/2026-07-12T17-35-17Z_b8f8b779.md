## Accomplished

Completed issue #6451 (feature): the standalone dihedral character combinatorics helper file
`EtingofRepresentationTheory/Chapter5/DihedralCharacterCombinatorics.lean`, providing the
reusable orbit/cardinality counts for the ℤ/N character group
`Â = (Multiplicative (ZMod N) →* ℂˣ)` under the inversion involution `χ ↦ χ⁻¹` (needed by the
dihedral assembly #6435). All lemmas build sorry-free and axiom-clean (only propext,
Classical.choice, Quot.sound).

Deliverables (all three from the issue + extras):

- `card_charGroup`: `Nat.card Â = N`. Proof via `charSelfEquiv`, the non-canonical self-duality
  `Â ≃* Multiplicative (ZMod N)` from `CommGroup.monoidHom_mulEquiv_of_hasEnoughRootsOfUnity`
  (ℂ has enough roots of unity).
- `card_selfInverse`: `#{χ = χ⁻¹} = Nat.gcd 2 N`. Proof identifies `{χ // χ = χ⁻¹}` with
  `(powMonoidHom 2).ker` (`χ = χ⁻¹ ↔ χ² = 1`) and applies `IsCyclic.card_powMonoidHom_ker`
  (`Nat.card ker = (Nat.card Â).gcd 2 = N.gcd 2`), using an exported `IsCyclic Â` instance.
- `card_notSelfInverse`: `#{χ ≠ χ⁻¹} = N - Nat.gcd 2 N` (fixed/non-fixed split via
  `Fintype.card_subtype_compl`).
- `two_dvd_card_ne_of_involutive` (reusable, general): for any involution `f` on a finite type,
  `2 ∣ #{a // f a ≠ a}`. Proof: the moved points are `e.support` for the induced permutation
  `e`, and every cycle of an involution has length 2 (`cycleType` entries divide `orderOf e | 2`
  and are `≥ 2`), so `sum_cycleType` is divisible by 2 (`Multiset.dvd_sum`).
- `two_dvd_card_notSelfInverse`: `2 ∣ #{χ ≠ χ⁻¹}`, derived from the general lemma applied to
  `χ ↦ χ⁻¹` (`inv_involutive`), justifying the halving as a real orbit count.
- `card_freeOrbitPairs`: `#{χ ≠ χ⁻¹} / 2 = (N - Nat.gcd 2 N) / 2`.

No dependence on `Theorem5_27_1.lean`. Added the module to the `Chapter5.lean` aggregator.

## Current frontier

Issue #6451 complete; PR opened with auto-merge. The dihedral assembly #6435 can now consume
these counts (fixed-char count `gcd(2,N)`, free-orbit count `(N-gcd(2,N))/2`). The
stabilizer identification (`stab` = ⊤/⊥) is deferred to #6435 per the issue.

## Overall project progress

Stage 3 formalization ongoing. This turn added one standalone infrastructure file (6 lemmas,
all sorry-free) unblocking the dihedral part of Exercise 5.27.2. Remaining unclaimed feature
issues include #6437 (q-Weyl center at root of unity), #6447/#6448 (affine/Heisenberg
classifications via Theorem 5.27.1), #6449 (FFT/Schur–Weyl).

## Next step

Pick up another unclaimed feature issue. The affine/Heisenberg classifications (#6447/#6448)
are the natural companions using the same Theorem 5.27.1 engine; #6437 is a self-contained
q-Weyl computation.

## Blockers

None.
