## Accomplished

Executed review issue #7003 (report-only): statement-fidelity + axiom-cleanliness audit of
`Chapter9/Problem9_4_6.lean` (path-algebra homological dimension + Cartan matrix + the new
`finite_path` acyclic-quiver infrastructure). Produced
`progress/reviews/2026-07-20-ch9-path-algebra-cartan-fidelity.md`.

- **Axiom cleanliness:** ran `#print axioms` on all 8 required headline declarations plus 4
  spot-checked definitions (`freeToPath`, `pathToFree`, `pathCountMatrix`, `not_hasHom…zero…`).
  Every one reports exactly `[propext, Classical.choice, Quot.sound]` — no `sorryAx`, no custom
  axiom.
- **`hacyclic` load-bearing:** confirmed concretely. `finite_path hacyclic`
  (`Problem9_4_6.lean:531`) is the sole path-finiteness source; a scratch `example` proves
  `Finite (Quiver.Path i j)` does NOT synthesize from `[Fintype Q] [∀ i j, Finite (i ⟶ j)]`
  alone, so `hacyclic` is not decorative.
- **Fidelity:** part (i) (`= 1`, both bounds), the free-algebra corollary (`freePathEquiv` genuine
  `AlgEquiv`), and part (ii) (`cᵢⱼ = Nat.card (Quiver.Path i j)`) all faithfully match
  `blobs/Chapter9/Problem9.4.6.md`. No `sorry`/`admit`/`True`-placeholder; all def/abbrev/instance
  bodies are genuine constructions.
- **Verdict: SOUND.** No fidelity or soundness defect; no follow-up issue filed.

## Current frontier

Review complete. Two non-blocking observations recorded in the report (part (i) scoped to
finite-vertex quivers; `A·eᵢ`-is-projective-cover identification asserted in upstream prose, not
certified as a theorem) and two cosmetic style-lint warnings (`Problem9_4_6.lean:274` unused binder
`n`, line 303 >100 chars). None warrant a source change under a report-only review.

## Overall project progress

Deep convergence. Codebase has a single genuine `sorry` remaining (`Chapter2/Problem2_16_3.lean`,
`finrank_g_three`, owned by #6340). This review adds tail-cleanup assurance for the Ch9 path-algebra
cluster. Remaining unclaimed queue is docstring-fidelity fixes (#7005, #7007).

## Next step

A worker can claim #7005 or #7007 (stale-docstring corrections in Ch4 §4.12 files). Optionally a
future style pass can address the two cosmetic lint warnings noted above.

## Blockers

None.
