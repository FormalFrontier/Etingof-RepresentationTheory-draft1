## Accomplished

Stage 3.7 statement-fidelity & non-vacuity audit of **Problem 4.12.4** (nonabelian
graph automorphism group ⇒ repeated adjacency eigenvalue), issue #7265. Verdict:
**VERIFIED**.

- Built `EtingofRepresentationTheory.Chapter4.Problem4_12_4` (exit 0, 8580 jobs).
- `#print axioms Etingof.Problem4_12_4` → `[propext, Classical.choice, Quot.sound]`,
  no `sorryAx`.
- Confirmed hypothesis fidelity: `Γ ≃g Γ` (= `RelIso Γ.Adj Γ.Adj`,
  `Maps.lean:306`) is the graph automorphism group, carries a `Group` instance,
  `(σ*τ) x = σ (τ x)`; `∃ σ τ, σ*τ ≠ τ*σ` = exact negation of commutativity =
  "nonabelian". Not vacuous, not over-strong.
- Confirmed conclusion fidelity: `Γ.adjMatrix ℝ` is real symmetric (`IsHermitian`),
  so `Matrix.IsHermitian.charpoly_eq` splits `charpoly = ∏(X - C λ_i)` with `λ_i`
  **real**; hence `¬ Squarefree charpoly ⟺` eigenvalues not distinct `⟺` repeated real
  eigenvalue — the symmetric case dissolves the "squarefree-over-ℝ vs distinct-roots"
  gap. `injective_of_squarefree_prod` supplies the (⇒) direction internally.
- Confirmed non-vacuity: `K₃` has `Aut = S₃` (nonabelian) so hypothesis is
  satisfiable; empty/singleton `W` forces trivial `Aut`, so `W` is genuinely populated
  wherever `hAut` holds.
- Wrote `progress/reviews/2026-07-22-ch4-problem4_12_4-graph-eigenvalue-fidelity.md`.
- Updated `progress/items.json` `Chapter4/Problem4.12.4`: `fidelity: verified`,
  `fidelity_decl`, `lean_file`, `coverage: covered_full`, `fidelity_note`,
  `last_updated`.

## Current frontier

Problem 4.12.4 audit complete, no gap, no `.lean` edits (report-only). No follow-up
issue needed.

## Overall project progress

Stage 3.7 (statement-fidelity sweep) ongoing; many Chapter 4 items still `fidelity:
(none)`. Sibling audits recently in flight: #7270 (Problem 4.1.4), #7272 (Problem
4.12.9).

## Next step

Claim the next oldest Stage 3.7 review issue (#7270 or #7272).

## Blockers

None.
