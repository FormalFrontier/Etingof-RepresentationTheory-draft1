# Feature session 1a918f61 — #2875 D1 (partial)

## Accomplished

Claimed #2875 (feat Ch2 #2774): close `not_posdef_not_HasFiniteRepresentationType`
— per-(k,Q) assembly + Theorem 2.1.2 bridge. The issue grouped three
deliverables; landed **deliverable 1 only** (the two per-(F, Q) API stubs)
as a partial PR and decomposed deliverables 2 + 3 to sub-issue #2877.

- **D1a.** Added `star_not_finite_type_per_kQ` (K_{1,4} / D̃₄ per-(F, Q)
  API stub) at the end of
  `EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean`. Body is
  `by sorry` per spec; the actual proof is tracked by #2789 / #2801.
- **D1b.** Created
  `EtingofRepresentationTheory/Chapter6/FieldGenericT125.lean` with
  `t125_not_finite_type_per_kQ` (T(1,2,5) per-(F, Q) API stub). Body is
  `by sorry`; actual proof tracked by #2793. Added the new file to the
  Chapter 6 manifest (`EtingofRepresentationTheory/Chapter6.lean`).

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericStar
EtingofRepresentationTheory.Chapter6.FieldGenericT125` passes with the
expected `sorry` warnings (and no others).

Sorry-count delta: +2 (the two new stubs). 16 → 18 raw sorries, 8 → 10
declarations with sorry. These two new stubs are tracked by the existing
proof-level issues (#2789 / #2801 / #2793).

## Current frontier

After D1: the base-case readiness table is complete. All 6 per-(F, Q)
forbidden-subgraph theorems exist on `main` (4 fully proven with
sorry-propagating bodies, 2 fresh API stubs from this PR). The
`not_posdef_infinite_type_per_kQ` assembly in
`FieldGenericInfiniteType.lean` and the Theorem 2.1.2 bridge in
`Chapter2/Theorem2_1_2.lean:173` are unblocked at the name level — both
remain unfiled-as-code.

## Overall project progress

Stage 3 (Lean formalization) ongoing. 18 sorries / 10 files (was 16 / 8).
582/583 items sorry-free; 0 definition-level sorries.

Active per-(F, Q) chain coverage:
- 6 of 6 forbidden-subgraph per-(F, Q) theorems live on `main`
  (4 sorry-propagating, 2 fresh API stubs).
- The per-(F, Q) assembly `not_posdef_infinite_type_per_kQ` and the
  Theorem 2.1.2 forward bridge `not_posdef_not_HasFiniteRepresentationType`
  remain to be implemented — tracked by sub-issue #2877.

## Decomposition rationale

The parent issue #2875 sized D2 + D3 at ~200-250 lines. Measuring the
existing case helpers on `main`:

| Helper | Lines |
|---|---|
| `degree_ge_4_infinite_type` | 47 |
| `graph_with_list_cycle_infinite_type` | 154 |
| `adjacent_branches_infinite_type` | 153 |
| `acyclic_branch_not_posdef_infinite_type` | 51 |
| `single_branch_not_posdef_infinite_type` | 305 |
| `non_adjacent_branches_infinite_type` | 927 |

Total of helper code that needs to be mirrored at the per-(F, Q) level
because `¬ IsFiniteTypeQuiver` is strictly weaker than per-(F, Q)
`¬ Set.Finite`: ~1637 lines. Plus the outer assembly (~50 lines) and
the D3 bridge (~50 lines). Far exceeds the parent's estimate.

Filed sub-issue #2877 with explicit decomposition guidance suggesting
splitting into D2.cycle / D2.degree4 / D2.adjacent / D2.single /
D2.nonadj / D2.outer + D3. The nonadj case (927 lines) is itself a
candidate for further decomposition by branch geometry.

## Next step

- A `/feature` worker can claim #2877. The body of #2877 includes
  explicit decomposition guidance — the worker should follow the
  agent-worker-flow "Assess Scope" path and split into 5-6 sub-tasks
  before writing code.
- Alternatively a planner can pre-emptively split #2877 into the 5-6
  child sub-issues during the next `/replan` cycle, freeing workers to
  claim them in parallel.

## Blockers

None for D1. For D2 + D3 (in #2877), the volume of mirror code is the
practical blocker — solved by decomposition.
