## Accomplished
Finished `Etingof.homTransportPlusEquivReduced` (issue #6085): filled the three remaining
`sorry`s (naturality of the reconstructed morphism, `left_inv`, `right_inv`) in
`Chapter7/Exercise7_9_8.lean`. The kernel/transport side of the Exercise 7.9.8 adjunction
bijection `Hom(V, transportReversedTwice(F⁺ᵢW)) ≃ AdjReducedData hi V W` is now sorry-free,
and since `homFMinusEquivReduced` already landed (#6087), `Etingof.Exercise7_9_8` is fully
sorry-free. `#print axioms` on both shows only `propext, Classical.choice, Quot.sound`.

Key restructure: introduced named helpers before the `Equiv` assembly — `sumMap` (the
direct-sum-valued map from the reduced family), `hker` (constraint C = lands in
`ker(sinkMap_W)`), `appAtI` (the map at `i` via `κ.symm ∘ codRestrict`), plus reusable
`hκ_appAtI` (the `κ`/codRestrict round-trip) and `hsumComp` (component read-off of `sumMap`
at a reindexed source arrow). Naturality splits into: vacuous `w = i` (no arrow into a
source), the ne/ne case (mirror of `compat` inverted through `(τ _ _).symm`, using
`reflFunctorPlus_mapLinear_ne_ne` + `reversedArrow_ne_ne_twice`), and the eq/ne source case
(`reflFunctorPlus_mapLinear_eq_ne` + `hsumComp`). `left_inv` at `i` uses `DirectSum.ext_component`
+ the reindex bijection + a per-arrow identity `hval`.

## Current frontier
`Chapter7/Exercise7_9_8.lean` builds cleanly (8588 jobs). `progress/items.json`
`Chapter7/Exercise7.9.8` set to `sorry_free`.

## Overall project progress
Stage 3 formalization ongoing. Exercise 7.9.8 (reflection functors as an adjoint pair) is
now sorry-free end to end.

## Next step
Pick up the next unclaimed feature issue. Remaining Ch7 adjunction cleanup, if any, plus the
Ch4 field-general counting bound (#6098) are open.

## Blockers
None.

## Key patterns discovered
- Rewriting a `DirectSum.component ⟨w, β⟩ z` index fails with "motive is not type correct"
  (the value type `W.obj idx.fst` is dependent). Fix: rewrite only the *arrow* second
  component, keeping `fst = w` fixed, so the motive stays well-typed.
- `DirectSum.ext_component R (fun b => …)` + `Equiv.surjective` to reduce a direct-sum
  equality to a per-index check over a reindexing bijection.
