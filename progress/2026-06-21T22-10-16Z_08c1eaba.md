## Accomplished

Worked #4962 (Ch5 #4905 part-C: generic constituent-character extraction). Added a
new sorry-free file `Chapter5/ConstituentCharacterExtraction.lean` with:

1. **`ConstituentCharacterExtraction.isSimpleModule_iso_of_inj_into_directSum_simple`**
   (generic module theory, sorry-free) — a simple `R`-module admitting an injective
   `R`-linear map into a finite `⨁ i, L i` of simples is isomorphic to one summand
   `L i`. The single-summand specialisation of
   `SemisimpleIsotypic.submodule_of_directSum_simple_iso_directSum`; proved by realising
   the image as a simple submodule and applying `Submodule.linearEquiv_of_le_sSup` against
   the coordinate lines (mirrors the `hmatch` step of the isotypic-extraction engine).

2. **`Etingof.simple_constituent_formalCharacter_eq_schurPoly_mem`** (the deliverable,
   sorry-free) — for a polynomial `GL_N(k)`-rep `M` (algebraic, weight-spanning,
   homogeneous degree `n`) with `formalCharacter M = ∑_{ν ∈ S} c_ν • schurPoly N ν.val`
   (distinct antitone `ν`, `c_ν : ℕ`), any simple `L` with a `GL_N`-equivariant injection
   `φ : L →ₗ[k] M` has `formalCharacter L = schurPoly N ν.val` for some `ν ∈ S`, `c_ν > 0`.

   Proof follows the `iso_of_formalCharacter_eq_schurPoly` template: `decompose_polynomial_gl_rep`
   → `char M = ∑_j schurPoly N (lam_cl (f j))` (via `formalCharacter_directSum` +
   `kEquivOfAsModuleEquiv`) → locate `L ≅ Lf (f j₀)` via the new module lemma →
   `schurPoly_linearIndependent` matches the two coefficient `Finsupp`s → evaluate at
   `lam_cl (f j₀)`: LHS coefficient `≥ 1`, forcing `lam_cl (f j₀) ∈ S` with `c > 0`.

Statement design: `S` ranges over `Finset {l : Fin N → ℕ // Antitone l}` (the antitone
subtype used by `schurPoly_linearIndependent` / `decompose_polynomial_gl_rep`), so a
consumer indexing by `BoundedPartition` reads `ν.parts := ν.val`. Added the import to the
`Chapter5.lean` aggregator (before `CauchyDetQuotient`).

Build green; the new file has no `sorry` and no warnings.

## Current frontier

The generic extraction lemma is done and ready for the #4905 final assembly. The #4905
assembly (`quotDetRep_irreducible_constituent_lastWeight_zero`,
`CauchyDetQuotient.lean`, single `sorry`) still needs wiring: instantiate this lemma with
`M = quotDetDegreeFDRep k N d` (#4960), whose character is supported on `{ν : ν_N = 0}`,
to conclude `0 ∈ Set.range ν`. That assembly remains #4905's job.

## Overall project progress

Chapter 5 Schur-Weyl. This lands the generic Schur-Weyl character-bookkeeping ingredient
of the kernel-lemma-K assembly (#4905 / #4896). The classification dependencies
(`simpleRep_iso_schurModule_of_formalCharacter_eq` ingredients #4946/#4947/#4948) are NOT
relied on by this lemma — it only uses the already-sorry-free `decompose_polynomial_gl_rep`,
`formalCharacter_directSum`, `formalCharacter_eq_of_rep_iso`, and `schurPoly_linearIndependent`.
So this ingredient is fully unconditional.

## Next step

Wire `simple_constituent_formalCharacter_eq_schurPoly_mem` into the #4905 assembly.

## Blockers

None.
