## Accomplished

Worked issue #6598 (block ↔ indecomposable-central-idempotent bijection, Problem 9.5.3(i) main
theorem). Assessed it as under-scoped (the planner's "final assembly" framing missed that one
direction is a genuine sub-theorem), decomposed it, and landed the tractable majority as a partial
PR in `EtingofRepresentationTheory/Chapter9/Problem9_5_3.lean`:

- **`existsUnique_actsAsOne_of_completeOrthogonal`** (axiom-clean): given a complete orthogonal
  central family `e : ι → R`, exactly one `e i` acts as the identity on a given simple module.
- **`actsAsOne_of_isCompositionFactor`** (axiom-clean): a central element fixing a module fixes
  each of its composition factors.
- **`exists_simple_actsAsOne`** (axiom-clean): a nonzero central idempotent acts as the identity on
  some simple module — a composition factor of the cyclic module `R·f`, realised inside
  `Shrink.{v} R`.
- **`blocks_equiv_indecomposableCentralIdempotents`** fully assembled: `ι ≃ {indecomposable central
  idempotents}` (orthogonality+completeness of the family) composed with `Block R ≃ ι` (the index
  `idxOf` descends via linkage invariance; inverse via `exists_simple_actsAsOne`). Its `right_inv`
  round-trip closes by index-uniqueness. The `left_inv` round-trip is the injectivity crux, isolated
  as the named stub `areLinked_of_actsAsOne_common` (the only remaining `sorry`).

`#print axioms blocks_equiv_indecomposableCentralIdempotents` → `sorryAx` enters **only** through
that one stub; the three helpers are `sorryAx`-free.

## Current frontier

`areLinked_of_actsAsOne_common` (block connectivity from indecomposability) — the single remaining
`sorry`. Filling it makes the main theorem sorry-free with zero further edits.

## Overall project progress

Phase 3 formalization ongoing. Problem 9.5.3(i) main bijection now assembled end-to-end modulo the
connectivity crux. The crux is split out as **#6618** (self-contained, with a full proof route:
decompose the regular module `f R` along the linkage-class partition of its composition factors and
read off orthogonal central idempotents, contradicting indecomposability, using the landed
Ext-vanishing dévissage `ext_subsingleton_of_compositionFactors_unlinked` and the splitting lemmas).

## Next step

Claim **#6618** and prove `areLinked_of_actsAsOne_common`. It is the block-projection / regular-
module-decomposition argument (the mathematically substantial half). Everything else for
Problem 9.5.3(i) is landed.

## Blockers

None. #6598's residual is exactly #6618; #6598 is being marked `replan` (partial PR) with a
`Decomposed into #6618` breadcrumb for the planner to narrow/close.
