## Accomplished

Completed issue #7143 — the explicit isotypic decomposition of the regular representation,
the book's final display for Maschke 4.1.1(ii): `k[G] ≅ ⊕ᵢ (dim Vᵢ) · Vᵢ`.

Added to `EtingofRepresentationTheory/Chapter4/Theorem4_1_1.lean` (in `namespace IrrepDecomp`):

- `columnMultRep` — the isotypic multiplicity representation `∏ᵢ Vᵢ^(dᵢ)` on
  `Π i, Fin (D.d i) → (D.columnFDRep i)`, with diagonal action `(g · w) i j = ρᵢ(g) (w i j)`,
  plus `columnMultRep_apply` certifying the action (each `(i,j)` copy is genuinely `Vᵢ`).
- `columnMultFDRep` — that representation packaged as an `FDRep k G`.
- `endColumnEquiv` — the underlying `k`-linear equivalence `∏ᵢ End(Vᵢ) ≃ₗ ∏ᵢ Vᵢ^(dᵢ)` sending
  `f ↦ (j ↦ f(eⱼ))` in the standard basis (via `Basis.constr ... |>.symm`), with
  `endColumnEquiv_apply` and `endColumnEquiv_comm` (the intertwining lemma).
- `columnMultIso` — the equivariant iso `endRegFDRep ≅ columnMultFDRep` built with `Action.mkIso`.
- `regularColumnMultIso` — composition `regularIso ≪≫ columnMultIso`:
  `MonoidAlgebra.regularFDRep k G ≅ columnMultFDRep`.

Flagship theorem: `Etingof.Theorem4_1_1_regularRep_isotypic` — existential in the house style of
`Theorem4_1_1_regularRep_iso`, surfacing the complete irreducible family, the multiplicities
`d i = finrank k (V i)`, the certified diagonal action, and the iso to `FDRep.of ρ_dec`.

Math route (as in the issue): decompose each `End(Vᵢ)` under post-composition `f ↦ ρᵢ(g) ∘ f` into
`dᵢ` columns via the standard basis (`Basis.constr` is equivariant since post-composition commutes
with linear combinations), assemble over `i` as a product, compose with the equivariant Wedderburn
iso `regularIso` from #7134/#7144.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter4.Theorem4_1_1` exits 0. All three new headline
declarations are axiom-clean `[propext, Classical.choice, Quot.sound]` (no `sorryAx`). Non-vacuity
confirmed by instantiating over `ℂ` and `S₃` (`Equiv.Perm (Fin 3)`) and destructuring the result.

## Overall project progress

Project is proof-complete and in the confidence/audit phase; this issue was one of the few
remaining `feature` items (filling the last explicit form of a flagship theorem). Chapter 4
Theorem 4.1.1 (Maschke) now carries: semisimplicity (i), sum-of-squares, algebra iso, equivariant
Wedderburn iso, and the isotypic decomposition (this turn) — the full content of the book's part (ii).

## Next step

Remaining unclaimed work is the three Ch4/Ch9 review audits (#7156, #7157, #7158). A `review`
audit of the new `Theorem4_1_1_regularRep_isotypic` could be planned once it lands, but is
optional given it was just built axiom-clean and non-vacuous.

## Blockers

None.
