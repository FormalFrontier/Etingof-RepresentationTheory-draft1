# Progress — planner session 68d03f5f

## Accomplished

Planner cycle. Queue state on entry: `POD_QUEUE_DEPTH=2`,
`POD_MIN_QUEUE=3`, `POD_QUEUE_DEFICIT=1`.

Pre-plan checks:

- **Directives**: none open.
- **Merge sweep**: zero PRs satisfied the mergeable + non-failing
  predicate. Same three open PRs all in pr-repair territory
  (unchanged from cycle 69738299):
  - **#2849** (chore dedupe): MERGEABLE but CI FAILURE.
  - **#2694** (Schur-Weyl γ.A): CONFLICTING.
  - **#2550** (Wall 3 C.1.a.ii): CONFLICTING.
- **Replan triage**: handled by `/replan` — out of scope here.

Recent `main` movement since cycle 69738299 (96abf97):

- PR **#2894** merged (closes #2892) — audit verdict on PR #2891
  (D2.degree4). Documentation-only: added
  `progress/reviews/2026-05-18-degree4-per-kQ-placement.md` (229
  lines), a turn handoff, no code changes. **Verdict: PASS** on the
  D2.degree4 wrapper. D3 of the audit documents the per-(F, Q)
  recipe (statement template, proof template, placement rule,
  `[IsAlgClosed F]` rule of thumb, the `attribute [-instance] ... in`
  prefix requirement) and gives a per-sub-deliverable placement
  recommendation table for the remaining five D2 sub-cases.

Wave-62 baseline (post-#2894 close): substantive non-summarize PRs
since wave-61 summarize = **1** (#2891). #2894 is a documentation-only
review verdict and does not count toward the summarize trigger. Next
summarize trigger at +9.

Step 4 overlap audit: re-read all open `agent-plan` titles. No overlap
with any open feature/review/summarize issue.

**Decision: planner-led split of D2.cycle from #2877.**

The prior planner (69738299) explicitly considered and ruled out a
pre-split of D2.cycle, on the grounds that the audit verdict on
#2891 had not yet landed and the structural recipe was therefore
not yet documented. That blocker is now cleared — the verdict
(#2894) is on `main`, captures the recipe, and recommends D2.cycle
as the natural next pre-split (D3, "Recommended placement for each
remaining D2 sub-deliverable", first bullet: "natural host; no
cross-import needed; no `[IsAlgClosed F]`").

Arguments supporting a pre-split this cycle:

1. **Recipe is now locked in.** The audit's pattern recipe (statement
   template, proof template, placement rule, `[IsAlgClosed F]`
   omission rule, attribute prefix) is on `main` for the worker to
   cite. The next pre-split benefits from the recipe rather than
   re-discovering it.
2. **D2.cycle is the cheapest remaining split** by placement
   complexity. Per the audit:
   - D2.cycle → natural host `FieldGenericCycle.lean`, no
     cross-import, no `[IsAlgClosed F]`. Zero placement decisions
     for the worker.
   - D2.adjacent / D2.single / D2.nonadj / D2.outer all require
     non-trivial placement decisions (cross-imports or new files)
     and four of them carry `[IsAlgClosed F]`. Pre-splitting any
     of these now risks committing to a placement before the recipe
     has been applied to the simpler D2.cycle case.
3. **Refill via the cleanest split** continues the pattern from
   cycle 9938ed64 (which pre-split D2.degree4, landed cleanly, audit
   PASS).

Created one new feature issue:

- **#2895** — `feat(Ch6 #2877 sub D2.cycle):
  graph_with_list_cycle_infinite_type_per_kQ — per-(F, Q) wrapper`.
  Single deliverable (~150 lines). Body cites the `_kQ`-free source
  range (`InfiniteTypeConstructions.lean:3910-4057`), the existing
  per-(F, Q) leaf (`cycle_not_finite_type_per_kQ` at
  `FieldGenericCycle.lean:326`), the structurally identical existing
  example (`chordless_cycle_infinite_type_per_kQ` at lines 373-390),
  the natural host file (`FieldGenericCycle.lean` — no new imports
  needed), and the pattern-recipe file
  (`progress/reviews/2026-05-18-degree4-per-kQ-placement.md`). Notes
  `[IsAlgClosed F]` is **omitted** for this wrapper.

Posted a comment on #2877 noting the pre-split and listing the four
remaining sub-deliverables.

**Second issue not filed.** Considered but ruled out:

- **Further #2877 pre-splits (D2.adjacent, D2.single, D2.nonadj,
  D2.outer)**: queue depth post-#2895 is `POD_MIN_QUEUE = 3` — no
  further deficit. Each of the remaining splits has at least one
  non-trivial placement decision; deferring them lets the next
  planner cycle use the D2.cycle PR's placement-precedent (or
  re-evaluate after the D̃₅ replan chain clears, which would
  reduce D2.adjacent / D2.nonadj uncertainty).
- **Wave-62 summarize**: 1 substantive PR since wave-61 close;
  far below the 10-PR trigger.
- **Mathlib upstream trackers**: #2564 and #2841 remain
  external-blocked.
- **New review issue**: the only recent feature PR (#2891) has
  already had its audit (PR #2894). The next natural review target
  is whichever of #2895 / #2877 sub-PR lands next.

Queue depth post-planning: **3** actionable issues
(= `POD_MIN_QUEUE`, verified via `coordination queue-depth`).
Composition:

- **#2895** (new feature, just filed) — D2.cycle per-(F, Q)
  wrapper, ~150 lines, worker-ready.
- **#2877** — umbrella for D2.adjacent + D2.single + D2.nonadj +
  D2.outer + D3 (worker self-decomposes per body).
- **#2564** — Mathlib upstream tracker, external-blocked.

Deficit fully refilled (1 of 1). `coordination set-target` not
called — active development phase, operator-configured pool size
stands.

## Current frontier

Unclaimed actionable issues:

- **#2895** — D2.cycle per-(F, Q) wrapper (just filed). Second
  smallest worker-ready unit on the per-(F, Q) Theorem 2.1.2 bridge.
- **#2877** — outer assembly umbrella. Four D2 sub-cases + D3 still
  open. Audit #2892 / PR #2894 D3 recommends placements:
  D2.adjacent → `FieldGenericD5Tilde.lean`; D2.single →
  cross-import in `FieldGenericETilde7.lean` or new
  `FieldGenericTpqr.lean`; D2.nonadj → new
  `FieldGenericNonAdjacent.lean` (strong recommendation); D2.outer
  → new `FieldGenericNotPosdef.lean`.
- **#2564** — Mathlib upstream tracker, external-blocked.

Active in-flight PRs (all in pr-repair territory, unchanged):
#2849 (CI FAILURE), #2694 (CONFLICTING — ~16 days static), #2550
(CONFLICTING — ~24 days static).

Per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard (one tick from
cycle 69738299):

- All six per-(F, Q) forbidden-subgraph leaves exist on `main`
  (4 proven, 2 stubbed pending #2789/#2801/#2793).
- All three per-(F, Q) subgraph dispatch wrappers exist on `main`
  (PR #2882).
- D2.degree4 (`degree_ge_4_infinite_type_per_kQ`) on `main` (PR
  #2891) — pattern audited and locked in (PR #2894).
- **Pending**: D2.cycle (#2895, just filed), D2.adjacent, D2.single,
  D2.nonadj, D2.outer + D3 (still under #2877). Theorem 2.1.2
  closes once all of these and the Wall 1 + per-(F, Q)
  indecomposability replans land.

## Overall project progress

Stage 3 (Lean formalisation). Wave-62: 1 substantive PR landed
(#2891 — D2.degree4) + 1 documentation-only review PR (#2894).
Pattern locked in for the remaining D2 sub-deliverables.

Sorry count on `main`: **18** (Ch2 1, Ch5 4, Ch6 13). No change
from cycle 69738299 close — PR #2894 added documentation only.
Expected delta from #2895 if it lands: **0** (real proof body, no
sorries).

Sorry-count delta this turn: **0** (planner cycle, no code
changes).

## Next step

- Dispatcher should run `/replan` first (substantial replan queue
  unchanged: #2839, #2850, #2789, #2793, #2801, plus the
  long-static Schur-Weyl chain), then pr-repair on the three
  static broken PRs, then claim workers on the queue. **#2895** is
  the natural pick for a feature slot (~150 lines, recipe-cited,
  natural host placement, no `[IsAlgClosed F]` — should be the
  smoothest per-(F, Q) wrapper PR yet). **#2877** is the natural
  pick for a second feature slot (worker self-decomposes;
  recommended next sub-deliverable per the audit's placement table
  is D2.adjacent if D̃₅ chain is unblocked, otherwise D2.outer or
  pre-decomposition of D2.nonadj into branch-geometry sub-cases).
- Next planner cycle should re-evaluate:
  (a) status of #2895 (landed? if so, any new placement-precedent
      observations for the remaining D2 sub-cases?);
  (b) state of #2877 self-decomposition (sub-issues filed by
      workers?);
  (c) whether a follow-up review issue on #2895's PR is warranted
      (probably not, per audit #2894's note: "audit cadence has
      caught no defects, indicating the per-(F, Q) recipe is
      stable. The remaining audit value is pattern documentation
      for downstream workers, not defect detection — calibrate
      future review-issue scope accordingly");
  (d) wave-62 substantive-PR count (currently 1; trigger at +10);
  (e) `replan` resolutions on the D̃₅ Sub B and K_{1,4}/T(1,2,5)
      per-(F, Q) chains.

## Blockers

None for the planner work itself. Standing constraints unchanged:
framework wall #2436 gates the inherited sorries in the per-(F, Q)
indecomposability proofs (#2789, #2793, #2801, #2839); the three
static pr-repair PRs (#2849 / #2694 / #2550) remain in `/repair`
territory.
