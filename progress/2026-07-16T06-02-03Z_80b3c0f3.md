## Accomplished

Closed the verification residual of issue #6674 (Ch6 Problem 6.1.6 (c),
`mckayAdj_no_selfLoop` / `mckay_isAffineDynkin`).

The issue's residual scope was purely a **sorry-free verification gate**: confirm
that once the odd-order crux #6715 (`isCyclic_of_odd_card`) and the Frobenius
`dim ∣ |G|` chain #6714 landed on `main`, the assembled McKay affine-Dynkin
classification `mckay_isAffineDynkin` became axiom-clean, with no further proof
work in `Problem6_1_6.lean`. Both dependencies are now on `main` (commits
`e0da5625` #6715, `a38649c7`/`6b52f1b2` #6714 chain).

**Verification performed (all three deliverables met):**

- `lake build EtingofRepresentationTheory.Chapter6.Problem6_1_6` → exit 0
  (8600 jobs; only `push_neg` deprecation warnings, no `declaration uses 'sorry'`).
- `#print axioms Etingof.Problem6_1_6.mckay_isAffineDynkin` →
  `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).
- No already-proven lemma weakened — this turn makes **no code changes** to any
  Lean file. `Problem6_1_6.lean` contains zero `sorry`.

The `mckayAdj_no_selfLoop` self-loop-free property, the cyclic/central-`−Id`
dichotomy (`su2_finite_cyclic_or_contains_negId`), and the full six-conjunct
`IsAffineDynkinDiagram` assembly are all sorry-free and axiom-clean on `main`.

## Current frontier

Ch6 Problem 6.1.6 parts (a)–(c) are complete and axiom-clean. Part (d)
(`McKayCorrespondence`) remains a recorded `Prop` pinning the correspondence to
the affine-type enumeration (the group classification of Problem 4.12.8 is a
separate downstream item), and part (e) (`mckay_dims_are_marks`) is proven.

## Overall project progress

Stage 3. This turn is a verification-only closure; it lands no new mathematics,
only confirms that the McKay affine-Dynkin classification `mckay_isAffineDynkin`
is now sorry-free following the merge of its dependency chain (#6714, #6715).

## Next step

Available unclaimed feature work: #6732 (construct the p-dimensional simple
`sl(2)`-module `L(p-1)`, Ch2 Problem 2.16.4 sharpness — a definition-construction
task, self-contained), #6742 (assemble `rearrangeBifunctorNatIso`, Ch8; builds
on PR #6747 once it merges), and the #6733 summarize refresh. Recommend #6732
next as a self-contained construction not gated on any pending PR.

## Blockers

None.
