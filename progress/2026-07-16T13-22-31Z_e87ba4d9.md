## Accomplished

Completed issue #6822 (feat Ch8 #Problem8.2.8-Ext): the **left-module external-tensor projective
resolution**, twin of `extTensorProjectiveResolution`, via **route (a)** (genuine left-module
stack) rather than the transport-heavy route (b).

Five new files under `EtingofRepresentationTheory/Chapter8/`, mirroring the right-module stack but
simpler (no `Algebra.TensorProduct.opAlgEquiv` / `op` transport — the action lands directly in
`A₁ ⊗[k] A₂`):

- `ExternalTensorFunctorLeft.lean` — the bifunctor `ModuleCat A₁ ⥤ ModuleCat A₂ ⥤ ModuleCat (A₁⊗A₂)`.
- `ExternalTensorComplexLeft.lean` — total complex `extTensorComplexLeft` + augmentation `extTensorπL`.
- `ExternalTensorProjectiveLeft.lean` — degreewise projectivity `extTensor_projectiveLeft`
  (free case via `finsuppTensorFinsupp k k A₁ A₂` directly, then retract).
- `ExternalTensorRestrictionLeft.lean` — restriction-of-scalars commutation
  `extTensorComplexLeft_restrictIso` and the degree-0 π-square `ι_extRestrictComplexXIsoL_aug₀`.
- `ExternalTensorResolutionLeft.lean` — the deliverable
  `extTensorProjectiveResolutionLeft : ProjectiveResolution (ModuleCat.of (A₁⊗A₂) (M₁⊗M₂))`, with
  the `quasiIso` field proved in full.

Key efficiency: the four generic homological lemmas in the right-module `ExternalTensorResolution.lean`
(`restrictScalars_preservesHomology`, `homology_mapHomologicalComplex_projective_isZero_succ`,
`quasiIsoAt_zero_of_isColimitCokernelCofork`, `isColimitCokernelCofork_tensorObj_augmentation`) are
polymorphic, so the assembly imports that file and reuses them directly instead of re-proving.

Added the five files to the `EtingofRepresentationTheory/Chapter8.lean` aggregate.

## Current frontier

`extTensorProjectiveResolutionLeft` is **sorry-free**; `#print axioms` gives exactly
`[propext, Classical.choice, Quot.sound]`. `lake build EtingofRepresentationTheory.Chapter8`
completes successfully. The `[Field k]` hypothesis is required for the `quasiIso` obligation (higher
`Tor` over `k` vanishes only over a field), matching the right-module case.

## Overall project progress

Phase 3 formalization. This delivers deliverable 2 of #6815 (the left-module external-tensor stack
for the `Ext` Künneth proof). Combined with the landed deliverable 1 (`ExternalTensorModuleLeft`),
the left-module resolution infrastructure for `Problem_8_2_8_ext` is now in place.

## Next step

#6818 (the `Ext` assembly of `Problem_8_2_8_ext`) can now consume
`Etingof.extTensorProjectiveResolutionLeft`. It still additionally depends on #6816 (degreewise
f.g.-projective Hom-tensor iso) and #6817 (cohomological Künneth / Ext identification). Verify those
before claiming #6818.

## Blockers

None.
