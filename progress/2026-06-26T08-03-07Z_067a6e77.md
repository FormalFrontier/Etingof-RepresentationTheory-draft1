## Accomplished

Closed the fidelity gap on `Chapter3/Proposition3.5.8` (issue #5371). The
declaration `Etingof.semisimple_algebra_equiv` previously stated only the
definitional `(1) ⇔ (5)` (`IsSemisimpleRing A ↔ IsSemisimpleModule A A`, by
`Iff.rfl`). It now asserts the full book claim: a `List.TFAE` over all five
conditions, relative to a complete set `V : ι → Type u` of pairwise
nonisomorphic irreducibles (same data as Theorem 3.5.4):

1. `IsSemisimpleRing A`
2. `∑ᵢ (finrank k (V i))² = finrank k A`
3. `∃ n (d : Fin n → ℕ), (∀ j, NeZero (d j)) ∧ Nonempty (A ≃ₐ[k] Π j, Matrix (Fin (d j)) (Fin (d j)) k)`
4. every f.d. `A`-module is `IsSemisimpleModule`
5. `IsSemisimpleModule A A`

Proof structure (no sorries; axioms = propext/Classical.choice/Quot.sound):
- `(1) ⇔ (2)`: `key` lemma `finrank k (A ⧸ Rad A) = ∑ᵢ (dim Vᵢ)²` from
  `Etingof.structure_mod_radical` (Thm 3.5.4) + `finrank_pi_fintype` +
  `finrank_linearMap`; `bridge` lemma `finrank(A/Rad) + finrank Rad = finrank A`
  via `Submodule.finrank_quotient_add_finrank` and
  `Submodule.Quotient.restrictScalarsEquiv`; `Rad = ⊥ ↔ IsSemisimpleRing` via
  `IsArtinianRing.isSemisimpleRing_iff_jacobson` + `Ideal.jacobson_bot`.
- `(1) ⇒ (3)`: `IsSemisimpleRing.exists_algEquiv_pi_matrix_of_isAlgClosed` (Mathlib
  Wedderburn–Artin over alg-closed field).
- `(3) ⇒ (1)`: `Π Matrix` is semisimple (Mathlib instances), transport via
  `RingEquiv.isSemisimpleRing`.
- `(1) ⇒ (4)`: `IsSemisimpleRing.isSemisimpleModule`; `(4) ⇒ (5)`: specialize to `A`;
  `(5) ⇒ (1)`: `id` (`IsSemisimpleRing` is an `abbrev` for `IsSemisimpleModule A A`).

Added `[IsAlgClosed k]` to the hypotheses (the book's conditions (2),(3) require it,
matching Theorem 3.5.4 / Corollary 3.5.5). Set `fidelity: verified` in
`progress/items.json`.

## Current frontier

Proposition 3.5.8 is now sorry-free and faithful. `lake build
EtingofRepresentationTheory.Chapter3` succeeds.

## Overall project progress

Phase 3 formalization ongoing; this turn resolved one wave-1 fidelity-sweep finding
(epic #5338).

## Next step

Continue with other open fidelity-gap / feature issues.

## Blockers

None.
