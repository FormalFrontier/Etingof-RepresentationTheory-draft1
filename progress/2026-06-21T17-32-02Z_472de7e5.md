## Accomplished

Worked issue #4908 (the character-independence *seam*). Discharged the algebraic
(Dedekind/Artin) half sorry-free, isolating the one genuinely geometric step.

- `Etingof.formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero`
  (`Chapter5/SchurWeylSimplesClassificationComplex.lean`) is now **sorry-free**. The
  proof:
  1. converts `hLdist` (an `FDRep` non-iso `¬ (L i ≅ L j)`) into an `asModule`
     `A`-module non-iso via `Representation.kEquivOfAsModuleEquiv` +
     `Action.mkIso` (the same construction used in `SchurWeylFormalCharacterIso`);
  2. applies `Etingof.traceCharacter_linearIndependent` to the
     `MonoidAlgebra ℂ (GL_N(ℂ))`-modules `Representation.asModule (L i).ρ`;
  3. bridges `traceChar (asModule (L i).ρ) (of g) = trace_ℂ ((L i).ρ g)` (via the
     `asModule` smul = `asAlgebraHom` action, `Representation.asAlgebraHom_of`);
  4. shows the functional combination vanishes on the `of g`-spanning set
     (`MonoidAlgebra.induction_on`), hence is the zero functional;
  5. independence then forces every `c i = 0`.
- Added the isolated geometric core as a precisely-typed lemma
  `trace_combination_vanishes_of_torus_vanishes` (one `sorry`): a regular,
  conjugation-invariant trace combination vanishing on the diagonal torus vanishes on
  all of `GL_N(ℂ)`.
- New import: `EtingofRepresentationTheory.Chapter5.SchurWeylFormalCharacterIso`
  (no cycle; it imports neither this file nor its dependents).
- Filed sub-issue **#4925** for the geometric density lemma.

Full `lake build` is green (8075 jobs).

## Current frontier

The character-independence seam is reduced to a single self-contained geometric
statement: diagonalizable matrices are Zariski-dense in `GL_N(ℂ)`, so a regular class
function vanishing on the torus vanishes everywhere. This is #4925.

## Overall project progress

Stage 3 (formalization) ongoing in Chapter 5. The seam #4908 algebra is landed; the
remaining sorries in `SchurWeylSimplesClassificationComplex.lean` are
`trace_combination_vanishes_of_torus_vanishes` (#4925, this turn's isolation) and the
separately-tracked `schurWeyl_simple_summand_glWeightSpace_top` (#4909, claimed).

## Next step

Claim **#4925**. It is research-level (diagonalizable-density / regular-semisimple
density / Chevalley restriction) and will likely decompose further: (i) distinct
eigenvalues ⟹ diagonalizable over ℂ, (ii) distinct-eigenvalue matrices Zariski-dense
in `GL_N`, (iii) regular class function vanishing on torus ⟹ vanishing on `GL_N` via
`MvPolynomial.funext` / `eq_of_eval_eq_on_gl`. Scaffolding: `EvalEqOnGL.lean`,
`DetLocalization.lean`, `PolynomialRepEmbedding.lean`, `FormalCharacterTorusTrace.lean`.

## Blockers

None. #4908 is delivered as a partial (algebra done, geometry isolated as #4925).
