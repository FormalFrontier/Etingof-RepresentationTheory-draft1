## Accomplished
`/work` triage session. Every unclaimed feature issue is currently blocked on an
unmerged predecessor; rather than force a doomed PR, I corrected the coordination
state so workers do not waste sessions rediscovering the blockage.

- **#4976** (general-k `schurModule_isSimple_general` assembly): skipped to `replan`
  with root-cause analysis, and recorded `depends-on: #4992`. The assembly's per-block
  inputs are all hardcoded to `ℂ` and generic `k` does not base-change from `ℂ`:
  `trace_symGroupAction_eq_spechtModuleCharacter` (`Theorem5_22_1.lean:1106`),
  `youngSym_action_vanishes_off_block` (`:2292`),
  `youngSym_action_on_special_block_rank_one_scaled_proj` (`:2413`), and
  `exists_unique_special_block` (`SchurModuleSpecialBlock.lean:148`). This is the exact
  obstruction recorded in `lean-formalization` SKILL.md line 91 (#2708 / C-4a). The
  intended Sub-C predecessor #4975 was closed COMPLETED but **landed no code on main**
  (cross-references #4991/#4992, no closing PR) — its general-k special-block lemmas do
  not exist. The real dependency is #4992 (open), gated on #4991 (open, PR #5002).
- **#4961 / #5003 / #4905**: added gating notes. All three depend on `quotDetDegreeFDRep`,
  whose definition is **not on main** — it lives only in PR #4997 (`agent/8063b0cd`),
  currently OPEN with failing CI (`ring` errors + ~55-min CI timeout, exit 143). Their
  predecessor #4960 is *closed* but unmerged. Do not claim until PR #4997 lands.

## Current frontier
The unclaimed queue is fully blocked:
- #4905 / #4961 / #5003 → gated on PR #4997 (open, CI failing, actively worked, updated 01:20).
- #4976 → now `replan`, gated on #4992 (open) → #4991 (open, PR #5002 CI in progress).
- #2564 / #2841 (Mathlib pin bump) → intentionally deferred (repo-wide disruptive with
  multiple agents in flight; upstream mathlib4#38583 has merged but the bump needs
  coordinated handling).

In-flight PRs that will unblock the queue when they land: #5002 (#4991 sub-B1),
#5006 (#4881), #5009 (#4987a), #4997 (#4960 residual; needs CI repair). PR #4929
(`Rep.resFunctor`) is a draft, not mergeable.

## Overall project progress
Stage 3 formalization, Chapter 5 Schur-Weyl / Specht line. Two threads:
1. **general-k Schur-module simplicity** (#4946 chain): blocked at the Specht-bridge
   layer — #4991 (PR #5002, CI running) → #4992 (open) → #4976 (replan, now correctly
   pointed at #4992). The general-k special-block lemmas Sub-C was meant to deliver were
   never landed.
2. **Cauchy/det-quotient character** (#4905 chain): #4960 infra sits in PR #4997 (failing
   CI); #4961/#5003/#4905 wait on it.
3. **Specht standard basis / Garnir** (#4881 → #4995 → #4998): #4998 carries a `replan`
   label; PR #5006 (#4881 dominance-maximal selector) is in CI.

## Next step
- A `repair` agent (or the #4997 author) should fix PR #4997's CI: `ring`-tactic
  failures plus a ~55-min build timeout (exit 143). Landing it unblocks #4961/#5003/#4905.
- A `repair`/worker should land PR #5002 (#4991) → then #4992 becomes workable → then
  re-point and unblock #4976.
- Do **not** claim #4961/#5003/#4905/#4976 from the unclaimed list until their predecessors
  land — see the gating comments on each.

## Blockers
- PR #4997 failing CI (`ring` errors + CI timeout) blocks the entire #4905 character chain.
- #4992 (general-k Specht bridge) open, gated on #4991 (PR #5002), blocks #4976 and the
  whole general-k simplicity thread.
- No cleanly executable unclaimed feature work exists at this moment; the queue is gated on
  the four in-flight PRs above.
