## Accomplished

Worked issue #4763 (feat(Ch6 #4722 residual): combo-D branch of
`d6tildeRep_kQ_leaf_equalities`). **Complete.**

- Expanded the former single `e54`-reversed `sorry` (one branch covering both
  `e64` sub-cases) in `d6tildeRep_kQ_leaf_equalities`
  (`Chapter6/FieldGenericD6Tilde.lean`) into a split on `e64`:
  - **e64 canonical (6→4)** — combo C, mixed v=4 leaves → `sorry`, folds into
    #4724.
  - **e64 reversed (4→6)** — **COMBO D**, both v=4 leaf edges reversed → now
    **proven**.
- The combo-D proof mirrors the D̃₅ combo-D precedent
  (`d5tildeRep_kQ_leaf_equalities`, `FieldGenericD5Tilde.lean:1241`): the v=2
  leaves push canonically into center 2, the central `γ_λ` deposits the image
  into vertex 3, the chain-collapse equalities `hcol1`/`hcol2` (`⟨3⟩ = ⟨4⟩`)
  move it to center 4, and the reversed v=4 leaves are read off by projecting
  out of center 4 via the landed siblings `d6tilde_core4_F_proj1`/`_proj2`.
  Three `compl_le_forces_eq` applications assemble
  `W₁⟨0⟩ = W₁⟨1⟩ = W₁⟨5⟩ = W₁⟨6⟩` (assembly identical to the all-canonical
  branch: `⟨heq05.trans heq15.symm, heq05, heq06⟩`).

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericD6Tilde` passes
(green, 8044 jobs).

### Scope note (issue header vs. proved branch)

The #4763 header labelled combo-D "all-leaves-reversed" with edge list
`e02=2→0, e12=2→1, e54=4→5, e64=4→6`. The all-four-sinks orientation is **not
leaf-derivable**: every leaf would be a sink with no source vertex to transport
from, so no `W₁⟨a⟩ ≤ W₁⟨b⟩` containment is available. The genuine, provable
combo-D — the one the issue's own Strategy section describes ("mirror the D̃₅
combo-D precedent", "core4 proj siblings", "transports across γ_λ like
canonical") — is **both v=4 leaf edges reversed with v=2 + central canonical**,
exactly the orientation D̃₅ calls combo-D. That is what landed. The all-reversed
orientation, the mixed v=4 sub-cases, the central-reversed (3→2) branch, and the
v=2-reversed branches all fold into #4724.

## Current frontier

`d6tildeRep_kQ_leaf_equalities` carries `sorry` in 5 orientation classes
(`FieldGenericD6Tilde.lean`): e64-reversed-under-canonical-e54 (mixed v=4),
e64-canonical-under-reversed-e54 (combo C, mixed v=4), e23 reversed (central
`3→2`), e12 reversed (`2→1`), e02 reversed (`2→0`). All five are the same
re-scoping class as the D̃₅ precedent and fold into the indecomposability
assembly #4724.

## Overall project progress

Stage 3 (Chapter 6, affine-tube indecomposability family). D̃₆ leaf-equality now
has 2 of 7 orientation classes proven (all-canonical via #4760, combo-D here),
matching the D̃₅ and D̃₇ achieved state. Downstream assembly #4724
(`d6tildeRep_kQ_isIndecomposable`) stays blocked on the eigenvalue-site / brick
argument for the residual branches.

## Next step

Mirror the D̃₅ re-scoping decision (#4647 → #4743) for D̃₆: restrict
`d6tildeRep_kQ_leaf_equalities` to eigenvalue-free orientations and fold the
mixed-v4 / central-reversed / v2-reversed branches directly into
`d6tildeRep_kQ_isIndecomposable` (#4724) via the eigenvalue-site argument, rather
than proving the standalone leaf collapse in those branches.

## Blockers

None for this PR. The residual 5 branches are blocked on the same open
mathematical content as the rest of the family (reversed-arm leaf-collapse
foundation), tracked by #4724 / #4743 / #4613 — a re-scoping decision, not a
hard blocker.
