## Accomplished

Worked issue #4922 (the extraction keystone for the kernel-lemma-K′ assembly,
parent #4897). Landed a new sorry-free file
`EtingofRepresentationTheory/Chapter5/SimpleSubrepExtraction.lean` and wired it
into the `Chapter5` aggregator.

The file delivers (all sorry-free):

- **`Etingof.exists_isSimpleModule_le`** (reusable, generic `ρ : Representation k G V`):
  a nonzero `k[G]`-submodule of `ρ.asModule` that is finite-dimensional over `k`
  contains a *simple* `k[G]`-submodule. Proof: finite over `k` ⟹ Artinian over
  `k[G]` (`isArtinian_of_tower`) ⟹ atomic submodule lattice
  (`isAtomic_of_orderBot_wellFounded_lt`) ⟹ an atom, pushed forward along the
  subtype, is simple (`isSimpleModule_iff_isAtom`, `IsSimpleModule.congr`).
- **`Etingof.toRepAsModuleEquiv`** + **`isSimpleModule_toRepresentation_asModule`**:
  the simplicity bridge `(σ.toRepresentation).asModule ≃ₗ[k[G]] ↥σ.asSubmodule`
  (the carriers coincide on `σ.toSubmodule`; on `single g t` both actions are
  `y ↦ t • ρ g y`, closed by `rfl` after `Representation.single_smul`).
- **`polyRightRep_mem_restrictTotalDegree`**: right translation preserves bounded
  total degree (homogeneous-component decomposition + `polyRightRep_isHomogeneous`).
- **`exists_simple_subrep_of_quotDetRep`** (the issue deliverable): from a nonzero
  `GL_N`-invariant submodule `W` of `A/det` produce `(L : FDRep, φ)` with `L` simple,
  `φ` an injective equivariant embedding, `Set.range φ ⊆ W`. Finite-dim reduction:
  lift `w ≠ 0` to a degree-`D` polynomial, push `restrictTotalDegree D` down to
  `A/det`, intersect with `W` for a nonzero finite-dim invariant `M₀ ≤ W`; then
  apply the two helpers above.

`lake build EtingofRepresentationTheory.Chapter5.SimpleSubrepExtraction` passes
with no new `sorry`.

## Current frontier

Issue #4923 (the #4897 assembly) is now unblocked: it can consume
`exists_simple_subrep_of_quotDetRep` to manufacture the `(L, hLsimp, φ, hφ_inj,
hφ_equiv)` data for `quotDetRep_irreducible_constituent_lastWeight_zero`
(`CauchyDetQuotient.lean`), combine with part-(a) + the twist-shift
(`glWeightSpaceℤ_quotDetTwist`) + highest-weight realization, and discharge
`quotDetTwist_nonzero_subrep_has_neg_weight` — the lone remaining `sorry` in
`KernelLemmaKPrime.lean`.

## Overall project progress

Stage 3 (formalization) on the Chapter 5 Schur–Weyl / kernel-lemma frontier.
`KernelLemmaKPrime.lean` still has one assembly `sorry`
(`quotDetTwist_nonzero_subrep_has_neg_weight`), now reduced to #4923 (the extraction
keystone it needed is landed here). `CauchyDetQuotient.lean` part-(a) remains the
Cauchy-decomposition research `sorry` (#4896 residual), independent of this work.

## Next step

A worker should claim **#4923** and assemble
`quotDetTwist_nonzero_subrep_has_neg_weight` using
`exists_simple_subrep_of_quotDetRep` (this file).

## Blockers

None for this issue. #4923 depends only on this keystone (now landed) plus the
already-stated part-(a) lemma (whose `sorry` is fine to consume).
