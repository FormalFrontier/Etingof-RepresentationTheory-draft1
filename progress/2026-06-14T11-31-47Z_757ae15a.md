## Accomplished

One-shot session on #2853 (`d5tildeRep_kQ_leaf_equalities` — 31
non-canonical orientation branches). **Refuted and skipped** — no code.

Determined the issue is **not completable as specified** ("close all 5
sorries with zero new sorries"): the theorem is *false* for the
mixed-direction orientations, the same single-twist / under-coupling
failure mode #4566 found in D̃₄ (`starRep_kQ`) and #4548 found in the
sporadic families. #4566 recommendation #1 explicitly names auditing the
dTilde `_leaf_equalities` sub-issues; this is one.

Worked the obstruction directly on D̃₅ (combo-C orientation, the L935
sorry): the available edges force only the M-twisted leaf relation
`M(W₁⟨5⟩) = W₁⟨0⟩` with `M = (I−N)⁻¹`, derived via the existing
γ-equiv untwisting lemma (`linearEquiv_invariant_isCompl_symm_mem`) +
`gammaInv_embed_general_F` + v=2 `d5tilde_core_F`. Concluding the claimed
`W₁⟨5⟩ = W₁⟨0⟩` would need N-invariance of the leaf plane, which no edge
of the mixed orientation supplies — so a non-N-invariant `W₁⟨0⟩` gives an
explicit counterexample (m=1: `W₁⟨0⟩ = span{(1,1)}`,
`W₁⟨5⟩ = (I−N)W₁⟨0⟩ = span{(0,1)}`). Full analysis posted as a comment on
#2853.

The γ-equiv + untwisting tooling (the prior strategy memo's proposed fix)
works correctly; it just produces the M-twisted relation, which is all
that is true. The missing step is exactly the false statement.

## Current frontier

#2853 marked `replan`. Sorries in `d5tildeRep_kQ_leaf_equalities`
unchanged (intentionally — they cannot all be closed; the theorem is
false). All-canonical and combo-D (both-v3-leaves-reversed) branches
remain genuinely proven; only the mixed orientations are
counterexamples.

## Overall project progress

Reinforces the #4566/#4548 finding for the D̃₅ rep specifically. The
orientation-generic D̃ `_kQ` indecomposability program (#2839/#2850/#2851
/#2853 and the d6/d7/d8 analogs) is built on an under-coupled
construction and needs the homogeneous-tube redesign
(`progress/sporadic-tube-redesign-design.md`). Downstream
`d5tilde_not_finite_type_per_kQ` stays true (D̃₅ affine = infinite type)
and should be re-routed off the false explicit-rep lemma.

## Next step

Planner: triage #2853's `replan` together with parent #2850, grandparent
#2839, and sibling #2851 (`d5tildeRep_kQ_isIndecomposable`, inherits the
same refutation). Either close them as refuted with a forward link to the
tube-redesign track, or narrow to the redesigned construction. Do not
re-dispatch a worker to "prove" any dTilde `_kQ_leaf_equalities` /
`_isIndecomposable` over arbitrary orientations — verify against #4566
first (per the skill note landed in #4573).

## Blockers

#2853 is refuted (false theorem); cannot be completed without the
construction redesign tracked off #4566 / #4548.
