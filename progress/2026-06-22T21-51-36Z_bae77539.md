## Accomplished

Review session — soundness audit of issue #5044: the right-torus eigenbasis
lemmas `rTransAlgHom_diagonal_X` (`:157`) and `rTransAlgHom_diagonal_monomial`
(`:172`) in `Chapter5/PolynomialGLRightAction.lean`.

**Verdict: PASS.** Audit comment posted on #5044. Findings:

- **Column-vs-row convention is correct.** The foundation `rTransAlgHom`
  (`:47`) sends `X_{(i,j)} ↦ ∑_l M_{l,j} • X_{(i,l)}`, faithful to
  `(R_h f)(g)=f(gh)` (right multiplication mixes the column index, fixes the
  row). For `M = diagonal v` only `l=j` survives, so the eigenvalue of `X_{ij}`
  is the **column** entry `v_j = v p.2`. No row/column transpose error.
- **Monomial eigenvalue collects correctly.** `s.prod (p e ↦ v p.2 ^ e)
  = ∏_{(i,j)} v_j^{s(i,j)} = ∏_j v_j^{μ_j}` with `μ_j = ∑_i s(i,j)` the column
  degree — matches docstring and the book's symmetric-function convention. No
  off-by-one, no mis-collected product, no `True`-placeholder, no weakened
  hypothesis.
- **Mechanical:** `lake build …PolynomialGLRightAction` green;
  `#print axioms rTransAlgHom_diagonal_monomial` →
  `[propext, Classical.choice, Quot.sound]` (no `sorryAx`, no custom axioms).
  Same for `rTransAlgHom_diagonal_X`.

No code changes (review item). No follow-up issues opened — the eigenbasis is
sound.

## Current frontier

Audit closed. The eigenbasis underpinning the weight-space dimension count
(#4957) and `polyRightDegreeFDRep_formalCharacter` is verified sound.

## Overall project progress

Stage 3 (formalization) ongoing. Ch5 Cauchy/right-`GL_N` character machinery
is being assembled (`#4905` epic glue-1..3). This audit clears one of the
remaining soundness checks on the foundational weight lemmas.

## Next step

Open unclaimed work: #5037 (James leading-term fact — hard combinatorial epic,
4+ prior failed sessions) and #2841 (stale Mathlib pin bump from May). The
`#4905` glue chain (#5063/#5064) is in progress by other sessions.

## Blockers

None.
