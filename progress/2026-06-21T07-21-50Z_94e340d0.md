## Accomplished

- **Closed #4808** (Ch6 #4803 sub-asm, Problem 6.1.5 step 2(b)/(c)): assembled the
  dimension bound `dim W(m) ≤ dim G(m)` by feeding the injective orbit-map
  comorphism (#4807) to the field-embedding bridge (#4783). New file
  `Chapter6/Problem6_1_5_DimBound.lean`, sorry-free, builds clean.
  - `dim_le_of_injective_comorphism_isLocalization_index`: the index-agnostic
    field-embedding payoff. An injective comorphism `k[xσ] → B` into a domain
    localization `B` of `k[xτ]` forces `card σ ≤ card τ`. Generalises
    `dim_le_of_injective_comorphism_isLocalization` (Fin-indexed) to arbitrary
    finite index types. Source is reindexed to `Fin (card σ)` by precomposition
    with `MvPolynomial.renameEquiv`; the base ring `k[xτ]` is reindexed to
    `Fin (card τ)` by carrying the `IsLocalization` structure across the rename
    ring equiv with `IsLocalization.isLocalization_of_base_ringEquiv` (the new
    base algebra instance + its `k`-scalar-tower compatibility are built by hand
    via `AlgEquiv.commutes`).
  - `card_wIdx_le_card_gIdx`: instantiates the index bridge at the concrete
    principal-open localization `B = k[gₚᵩ, detProd⁻¹] = Localization (powers
    (detProd m))` (a domain, via `IsLocalization.isDomain_localization` +
    `powers_le_nonZeroDivisors_of_noZeroDivisors (detProd_ne_zero …)`), fed the
    dense-orbit injective comorphism `exists_injective_orbitComorphism`.
  - `repSpace_finrank_le_repGroup_ambient_finrank`: the S2/S3 interface,
    `Module.finrank k (W(m)) ≤ Module.finrank k (∏ᵢ Matrix (Fin mᵢ) (Fin mᵢ) k)`,
    obtained by rewriting `card_wIdx_le_card_gIdx` through `wIdx_card`/`gIdx_card`
    and `repSpace_finrank`/`repGroup_ambient_finrank`.
  - Added the import to `Chapter6.lean`.

## Current frontier

`Chapter6/Problem6_1_5_DimBound.lean` builds clean and sorry-free
(`lake build EtingofRepresentationTheory.Chapter6.Problem6_1_5_DimBound`). All
three theorems `#print axioms` to `[propext, Classical.choice, Quot.sound]` only.

## Overall project progress

- Chapter 6 orbit-counting directive (#4777): S0/S1/S2a/S2b/S2c/S3 merged; the
  step-2(b)/(c) dimension bound `N ≤ M` is now assembled (this turn). The chain
  is: finitely-many-orbits ⟹ dense orbit (`exists_isAlgDense_orbit`) ⟹ injective
  comorphism (`exists_injective_orbitComorphism`, #4807) ⟹ `card (WIdx) ≤ card
  (GIdx)` (`card_wIdx_le_card_gIdx`, this turn) ⟹ `dim W ≤ dim G`.
- Remaining on the directive: the `−1` strict refinement and
  `titsForm_posDef_of_cone` (`Problem6_1_5_PosDef`), and S4 (#4786, the
  construction-track deletion, still blocked on the Theorem_2_1_2 migration).

## Next step

- A planner should scope: thread `repSpace_finrank_le_repGroup_ambient_finrank`
  into `Problem6_1_5_PosDef` for the `−1` strict refinement (the dense-orbit base
  point gives a *strict* dimension drop after removing the global stabiliser
  scalar), then assemble `finite type ⟹ q(m) > 0`.

## Blockers

- Pre-existing, unrelated: `Chapter6/FieldGenericD5Tilde.lean` fails to compile on
  `origin/main` (undefined `heq01`/`heq05`/… around lines 1627-1713). This file is
  in the explicit-construction track slated for deletion by directive #4777 and is
  independent of this PR. CI builds modules individually (`find … -mindepth 2`,
  skipping aggregators), so `Problem6_1_5_DimBound` is built and green; the
  `FieldGenericD5Tilde` failure pre-dates and is orthogonal to this change.
