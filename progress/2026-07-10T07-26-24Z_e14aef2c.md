## Accomplished
Worked issue #6073 (Ch4 Problem 4.12.10: every irreducible occurs in a tensor power of a
faithful representation). Landed the reusable infrastructure and the full logical assembly:

- `trace_piTensorProduct_map_const`: `trace (⨂ⁿ f) = (trace f)^n` on `⨂[k]^n V`, via the
  `Basis.piTensorProduct` tensor basis + `Finset.sum_pow'` (sorry-free, general field).
- `character_diagTensorPow`: `χ_{V^⊗n}(g) = χ_V(g)^n` (sorry-free).
- `Etingof.Problem4_12_10` fully assembled: uses `Representation.card_inv_mul_sum_char_mul_char_eq_finrank`
  (multiplicity = character inner product) + `IntertwiningMap`/Schur to extract the map, and a
  polynomial `∏_{μ≠dim V}(X-μ)` to isolate `g=1` from the vanishing power sums. Compiles.

The proof is complete **modulo one analytic lemma**, `ρ_eq_one_of_character_eq_finrank`
(`trace(ρg)=dim V ⟹ ρg=1`, i.e. faithfulness pins the top character value to g=1), which is
the sole remaining `sorry`.

## Current frontier
`ρ_eq_one_of_character_eq_finrank` in `Chapter4/Problem4_12_10.lean` is `sorry`. This is a
self-contained unitarization/Cauchy-Schwarz fact: `ρ g` has finite order → unitary for an
averaged Hermitian form (`Discussion_4_4.char_inv_eq_conj` builds exactly this `H`) → trace =
dim only when all eigenvalues are 1. Decomposed into sub-issue **#6096** with a full route.

## Overall project progress
Phase 3 formalization ongoing. This item moved statement_formalized → proof_partial. Two
new reusable lemmas about tensor-power traces/characters added to Chapter 4.

## Next step
Claim #6096 and prove `ρ_eq_one_of_character_eq_finrank` (matrix unitarization route: reuse
`H = Σ (M h)ᴴ M h` from Discussion_4_4, conjugate `M g` to a standard-unitary `U` via a
Hermitian sqrt of `H`, then "unitary + trace = dim ⟹ U = 1" via column norms). That closes
Problem 4.12.10 fully.

## Blockers
None. #6096 is a proof-only sub-task with a clear route; no dependency blockers.
