## Accomplished

Landed **ingredient (2)** of the `kernelLemmaK'` deep-core decomposition
(issue #4865): the highest-weight shift isomorphism `det · A ≅ A ⊗ χ`, in a new
self-contained file `EtingofRepresentationTheory/Chapter5/DetShiftIso.lean`
(~160 lines, **no `sorry`**).

Contents (`namespace Etingof.DetShiftIso`, over `[Field k]`):
- `rTransAlgHom_detPoly` — restatement of `rTransAlgHom_det` in the `detPoly`
  spelling: `R_M detPoly = det(M) · detPoly`.
- `mulDet` — the `k`-linear multiplication-by-`detPoly` map (`LinearMap.mulLeft`),
  with `mulDet_apply`.
- `mulDet_intertwine` — **the entire mathematical content**: `mulDet` intertwines
  the χ-twisted source rep `charTwistRep (detChar) (polyRightRep)` with the
  untwisted `polyRightRep`. Immediate from `rTransAlgHom_det`; the `detChar`
  factor cancels the `det(g)` produced on the right.
- `mulDet_injective` (`detPoly` nonzerodivisor) and `range_mulDet` (range =
  `detSubmodule = (det)`).
- `detShiftSubrep` — `(det)` packaged as a `Subrepresentation (polyRightRep k N)`.
- `detShiftLinearEquiv : A ≃ₗ[k] ↥(detSubmodule)` with `detShiftLinearEquiv_coe`.
- `detShiftLinearEquiv_intertwine` — the iso packaged at representation level
  (`A ⊗ χ ≅ detShiftSubrep`).

`lake build EtingofRepresentationTheory.Chapter5.DetShiftIso` passes clean (no
new `sorry`, no linter warnings on the file). The file is auto-discovered by CI
(`find … -mindepth 2`), so no aggregator edit was needed.

## Current frontier

The standalone shift building block is in place. It is **not** wired into
`kernelLemmaK'` — that wiring also needs ingredient (1), the `GL×GL`-equivariant
Cauchy decomposition of `k[Xᵢⱼ]`, which remains the sole research-level `sorry`
on this path (`KernelLemmaKPrime.lean:185`, untouched).

## Overall project progress

Stage 3 formalization. The det⁻¹-elimination kernel lemma (K) is reduced to the
single core `kernelLemmaK'`, now decomposed into (1) Cauchy decomposition [open,
deep] and (2) `det·A ≅ A⊗χ` shift [**this turn, done**].

## Next step

A planner should open the follow-up for ingredient (1) (the `GL×GL`-equivariant
Cauchy / highest-weight decomposition) and a wiring issue that combines (1)+(2)
to discharge the `kernelLemmaK'` sorry. The shift API to consume is
`DetShiftIso.detShiftLinearEquiv` + `detShiftLinearEquiv_intertwine` (and the
short exact sequence `0 → A⊗χ → A → A/det → 0` noted in the module docstring).

## Blockers

None for this item. The downstream `kernelLemmaK'` remains blocked on the
equivariant Cauchy decomposition (a separate, deep work item).
