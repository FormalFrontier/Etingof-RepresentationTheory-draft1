## Accomplished

Claimed #4558 (Ẽ₇ = T(1,3,3) homogeneous-tube indecomposable, sub-3 of #4548).
Assessed scope and **decomposed** it — the full task is genuinely multi-session
pioneering work, matching the D̃ₙ orientation-generic programs (each 5+
sub-issues).

- Wrote `progress/etilde7-tube-design.md`: a per-shape design note pinning down
  δ = (4;2;3,2,1;3,2,1), the construction target (tensor-model tube
  `S_λ ⊗ I + ∂_λM ⊗ J` at one site), the orientation-generic leaf-equality
  requirement, and the indecomposability reduction target.
- **Key finding** recorded in the note (§2): a one-arm `N ↦ λI+J` swap in
  `etilde7Arm1Embed_F` is **insufficient** — arm 3's `embed3to4_ACD_F` covers
  center block D independently, so the §1 `m=1` peeling pair survives. The
  eigenvalue must be woven through the rectangular arm maps, confirming the
  parent design's §3 obstruction. This is the trap sub-A must avoid.
- Created sub-issues #4568 (sub-A: tube `def` + dimVec — the soundness win),
  #4569 (sub-B: orientation-generic leaf-equality case tree), #4570 (sub-C:
  indecomposability assembly + consumer re-point). Linked #4570 `depends-on`
  #4568 and #4569. Left the `Decomposed into …` breadcrumb on #4558.

## Current frontier

Sporadic-tube redesign (#4548). D̃₄ mechanism landed (#4555,
`FieldGenericTube.lean`). Ẽ₆ (#4557) is claimed by another session. Ẽ₇ now
scoped into #4568/#4569/#4570; T(1,2,5) (#4559) still open and unstarted.

## Overall project progress

Stage 3 (formalization), Chapter 6 affine-Dynkin infinite-type frontier. The
sporadic base cases Ẽ₆/Ẽ₇/T(1,2,5) are being rebuilt as homogeneous tubes after
the #4542 audit found the single-twist family decomposable (false
`_isIndecomposable` sorries). Ẽ₇ design + decomposition done this session.

## Next step

A worker can claim #4568 (sub-A): derive the T(1,3,3) regular-simple rectangular
matrices and build the tube `def etilde7Rep_kQ` + `_dimVec`. This is the core
unworked math; landing it flips the `_isIndecomposable` sorry from a false to a
true statement (the soundness win) even before sub-C closes it. sub-B (#4569)
can proceed in parallel (independent up to the `leaf_equalities` interface).

## Blockers

#4570 (sub-C) is blocked on #4568 + #4569 (linked). No other blockers.
The deep work in #4568 — explicit rectangular regular-simple matrices for
T(1,3,3) — is unworked in any design doc and is the genuine difficulty.
