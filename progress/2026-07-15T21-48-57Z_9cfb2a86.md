## Accomplished

Session type: **feature** (`/work` → `/feature`), claimed #6710
(`isCyclic_of_odd_card`, the odd-order crux of Problem 6.1.6(c)).

- **Discovered #6710 was mis-scoped for `main`.** Its stated base — the
  `isCyclic_of_odd_card` `sorry` plus the tautological-rep / faithfulness / `-Id`
  infrastructure — lives only on the still-open PR #6711 (issue #6691),
  MERGEABLE/CLEAN but not yet merged. None of those lemmas exist on `main`.
- Its genuine hard content, "**dim of a complex irrep divides |G|**" (Frobenius),
  is a large, general, reusable theorem absent from Mathlib (no central-character
  / class-sum machinery). Confirmed present building blocks: `IsIntegral`,
  `IsIntegrallyClosed`, `Algebra.IsIntegral.of_finite`,
  `Matrix.trace_eq_sum_roots_charpoly`, `Matrix.eval_charpoly`,
  `Matrix.exists_mulVec_eq_zero_iff`, Niven's `exists_int_iff_exists_rat`,
  `FDRep.scalar_product_char_eq_finrank_equivariant`.
- **Decomposed #6710** into:
  - **#6714** — `dim ∣ |G|` (general, `main`-mergeable, unblocked).
  - **#6715** — assemble `isCyclic_of_odd_card` from #6714 + the faithful-character
    route (blocked: `depends-on #6714`, and needs #6691/PR #6711 on `main`).
  Skipped #6710 (replan) with a `Decomposed into #6714, #6715` breadcrumb.
- **Started #6714** (new file `EtingofRepresentationTheory/Chapter6/DimDvdCard.lean`,
  registered in `Chapter6.lean`), top-down:
  - `isIntegral_int_of_pow_eq_one` — a complex root of unity is integral over `ℤ`
    (monic witness `X^n - C 1`). **Sorry-free.**
  - `FDRep.isIntegral_character` — **character values are algebraic integers.**
    Full proof via the matrix of `ρ g` (`A^n = 1`), `trace = sum of charpoly
    roots`, each root is an eigenvalue (`eval_charpoly` → `det = 0` →
    `exists_mulVec_eq_zero_iff` → `A *ᵥ v = r • v`), `r^n = 1` by iterating the
    eigenvalue relation, hence integral; sum of integrals is integral.
    **Sorry-free** (`#print axioms` = `propext, Classical.choice, Quot.sound`).
  - `FDRep.isIntegral_classSum_scalar` (central-character integrality) and
    `finrank_dvd_card_of_irreducible` (headline): stated with documented `sorry`
    proofs (the remaining, harder steps A2/A3 of #6714).

## Current frontier

`DimDvdCard.lean` builds sorry-free except the two intentional top-down stubs.
The next hard piece is `isIntegral_classSum_scalar` (integral group ring center /
class sums acting as scalars), then the Schur-orthogonality assembly of
`finrank_dvd_card_of_irreducible`.

## Overall project progress

Stage 3 formalization ongoing. This session added general representation-theory
infrastructure (character values integral) plus a clean decomposition of the
Ch6 McKay no-self-loop crux chain (#6710 → #6714/#6715).

## Next step

- Land #6714's remaining steps: `isIntegral_classSum_scalar` (build the class-sum
  central character over `ℤ[G]`, use `Algebra.IsIntegral.of_finite`) then assemble
  `finrank_dvd_card_of_irreducible` via `⟨χ,χ⟩ = 1` and `ℤ` integrally closed in `ℚ`.
- Once #6714 and PR #6711 are on `main`, do #6715 (`isCyclic_of_odd_card`).

## Blockers

- #6715 blocked on #6714 and on PR #6711 (#6691) landing on `main`.
- #6711 is MERGEABLE/CLEAN with green CI; expected to auto-merge without action.
