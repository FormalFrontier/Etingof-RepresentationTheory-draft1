## Accomplished

One-shot feature session `90ff9e80` (`/once 5108`). Broke the `DetInvElim`
import cycle blocking #5076, **without** the re-routing of the SchurWeyl
character machinery that the issue feared (its proposed part (b)).

Root-caused the cycle to two heavy *transitive* imports that each carried only a
single, mathematically clean symbol:

1. `CauchyCharDiff.lean` imported `Proposition5_22_2` (which reaches `DetInvElim`
   via `SchurWeylFormalCharacterIso`) only to use `schurPoly_shift` — a pure
   Schur-polynomial alternant identity needing nothing beyond `Theorem5_22_1`.
2. `CauchyDetQuotientGrading.lean` imported `PolynomialGLDecomposition` (also
   reaching `DetInvElim`, via `PolynomialRepEmbedding`) only to use
   `Representation.asModuleHomOfIntertwiner` — a Mathlib-only `asModule` transfer
   utility.

Both crossing symbols were clean code colocated in polluted files. Extracted them:

- new `SchurPolyShift.lean` (`import Theorem5_22_1`): `schurPoly_shift` + its 3
  private alternant helpers, moved out of `Proposition5_22_2.lean`.
- new `RepresentationAsModuleHom.lean` (`import Mathlib` only): the entire
  `namespace Representation` utility block (glue-A/B + intertwiner lifts), moved
  out of `PolynomialGLDecomposition.lean`.

Rewired: `Proposition5_22_2` and `PolynomialGLDecomposition` now import the new
files (re-export, same namespaces — no qualified-name breakage); `CauchyCharDiff`
imports `SchurPolyShift` instead of `Proposition5_22_2`; `CauchyDetQuotientGrading`
imports `RepresentationAsModuleHom` instead of `PolynomialGLDecomposition`. Added
both new files to the `Chapter5.lean` aggregator.

## Current frontier

Deliverable met. Transitive-import check (verified by script on the edited
files): `DetInvElim` is now **absent** from the closure of every delivering file —
`CauchyDetQuotientDegree`, `CauchyDetQuotientGrading`, `QuotDetDegreeAlgebraic`,
`CauchyCharDiff`, `SchurPolyShift`, `RepresentationAsModuleHom`. The whole project
import graph is cycle-free. Simulating the #5076 assembly (importing the four
ingredient files into `CauchyDetQuotient.lean`) also yields **0 cycles**, so #5076
can proceed as the pure assembly it was scoped to be.

All four #5076 ingredients now live in a DetInvElim-free closure:
`exists_degree_embedding_of_simple` (Grading), `quotDetDegree_char_as_antitone_sum`
+ `quotDetDegree_iSup_glWeightSpace_eq_top` (Degree), `quotDetDegreeFDRep_isAlgebraic`
(QuotDetDegreeAlgebraic); plus the degree-component defs `quotDetDegreeFDRep` /
`quotDetDegreeSubrep` / `quotDetDegreeProj` / `quotDetProj`.

## Overall project progress

Etingof draft 1, Stage 3, Chapter 5. The #4905/#5076 determinant-quotient chain
is the active frontier; this session removes the structural import-cycle blocker
discovered during the #5076 one-shot attempt. No new sorries; the lone
`CauchyDetQuotient.lean:124` sorry (the #5076 assembly target) is untouched and
now unblockable.

## Next step

- **#5076** (`quotDetRep_irreducible_constituent_lastWeight_zero` assembly) is now
  structurally unblocked: import the four ingredients into `CauchyDetQuotient.lean`
  and replace the `sorry` at line 124. No import cycle will result.

## Blockers

None.
