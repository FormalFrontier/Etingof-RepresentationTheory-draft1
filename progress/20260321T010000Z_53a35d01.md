## Accomplished

Decomposed the single monolithic sorry in `Theorem_9_2_1_ii` (complete orthogonal idempotents for WA decomposition) into 3 targeted, smaller sorry's with clear proof obligations:

1. **`completeOrthogonalIdempotents_matrix_single`** (line ~1519): Diagonal matrix units `E_{jj}` form COI in `Mat_n(R)`.
   - Orthogonality fully proved via `Matrix.single_mul_single_of_ne`
   - Completeness sorry'd: `∑ j, E_{jj} = I` (standard identity, needs `Finset.sum_apply` for matrix function application)

2. **`completeOrthogonalIdempotents_pi_matrix`** (line ~1538): `Pi.single l (E_{jj})` form COI in `∏ Mat_{d_l}(R)`.
   - Sorry'd; follows from (1) + `Pi.single` orthogonality + block-diagonal completeness

3. **`exists_complete_orthogonal_idempotents_for_simples`** (line ~1560): Main construction.
   - Proved: WA decomposition setup, COI in quotient, lifting via Corollary 9.1.3, reindexing via σ-bijection + dimension matching, COI for reindexed system, rank property transfer
   - Sorry'd: Block assignment σ (bijectivity, dimension matching, rank property)

### Technical notes
- `Matrix.single_mul_single_of_ne` has confusing implicit `{l : n}` that shadows type parameter; use `(h := hjk)` named arg
- `Finset.sum_apply` doesn't directly distribute `(∑ f) r s` for matrix-valued functions; need explicit `show` or `rfl` coercion
- `finCongr` surjectivity proof requires `conv_lhs` or `rwa` to avoid ▸ rewriting both sides; variable shadowing (`k : Fin (d l)` shadows field `k`) caused `Module.finrank k` parse errors

## Current frontier

`Theorem9_2_1.lean`: 3 sorry warnings (down from 1 sorry in the original, but now decomposed into smaller, targeted goals).

## Overall project progress

- ~207/583 items sorry_free in items.json (~35.5%)
- Theorem_9_2_1_ii: original sorry decomposed into 3 sub-sorries with clear proof paths

## Next step

1. Prove `completeOrthogonalIdempotents_matrix_single` completeness (need to figure out `Finset.sum_apply` for matrices)
2. Prove `completeOrthogonalIdempotents_pi_matrix` using (1) + `Pi.single` properties
3. Prove `h_block` (block assignment σ) — this parallels the existing `exists_orthogonal_idempotents_for_simples` infrastructure

## Blockers

- Matrix `Finset.sum_apply` for completeness proof: `(∑ j, f j) r s ≠ ∑ j, f j r s` syntactically; need explicit conversion
