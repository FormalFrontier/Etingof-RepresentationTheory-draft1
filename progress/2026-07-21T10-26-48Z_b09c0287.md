## Accomplished

Review session (issue #7203): statement-fidelity & non-vacuity audit of
**Problem 5.16.1** (branching rule for `Sₙ ⊆ Sₙ₊₁`),
`EtingofRepresentationTheory/Chapter5/Problem5_16_1.lean`.

**Verdict: FAITHFUL — axiom-clean, no defect. Nothing filed.**

- Build: `lake build EtingofRepresentationTheory.Chapter5.Problem5_16_1` succeeds
  (8609 jobs; one `show`-vs-`change` style warning only).
- `#print axioms` on both `res_spechtModule_character` and
  `ind_spechtModule_multiplicity`: `[propext, Classical.choice, Quot.sound]` — no
  `sorryAx`.
- (a) restriction is rendered as a **full-character identity** over all `σ`; (b)
  induction as the **Frobenius-reciprocity multiplicity** over all `λ ⊢ n+1`. Both
  are faithful equivalents of the book's module direct-sum decompositions in char 0,
  because the `V_λ` are irreducible (`Theorem5_12_2_irreducible`), pairwise
  non-isomorphic (`spechtModuleCharacter_injective`), and complete. The issue
  explicitly accepts the character/multiplicity form as equivalent.
- Constituent objects all genuine, not surrogates: `spechtModuleCharacter` (trace of
  action), `permEmb` (`viaEmbeddingHom Fin.castSuccEmb`, adds one fixed point),
  `removeSquare`/`addSquare` (containment filters = `R(μ)`/`A(μ)`), `branchingPairing`
  (`|Sₙ|⁻¹ ∑ χ(σ)ψ(σ⁻¹)`).
- Non-vacuity confirmed: the `if`-guard discriminates (worked `μ=(2)`, `λ=(1,1,1)` → 0
  vs `μ⊆λ` → 1), and `R(μ)` can have >1 element (`R((2,1))={(1,1),(2)}`) while the
  filter still excludes non-contained diagrams (`R((3))={(2)}`).

Report: `progress/reviews/2026-07-21-ch5-problem5_16_1-branching-rule-fidelity.md`.
`progress/items.json`: set `Chapter5/Problem5.16.1` `fidelity: verified` with note.

## Current frontier

Stage 3.7 fidelity audits ongoing. This item now has a fidelity review report and
`fidelity: verified`.

## Overall project progress

Formalization largely `sorry_free`; Stage 3.7 statement-fidelity audits are sweeping
items lacking a fidelity review. Two other review issues remain unclaimed at session
start: #7198 (Problem 2.16.1, Lie's theorem) and #7204 (Problem 4.12.2, Heisenberg
group).

## Next step

A worker can claim #7198 or #7204 (both `review`), or a planner can generate more
fidelity-audit issues for items whose `fidelity` field is still unset.

## Blockers

None.
