## Accomplished

Worked issue #6920 (feature): discharge `map_smul'` of `Etingof.extAbelianIsoExtₖ`
(`Chapter8/ExtAbelianComparison.lean`). After a thorough scope assessment the full obligation
proved to be a multi-session, multi-tower naturality proof, so I landed a **coherent structural
reduction** as a partial and split the residual into a precise successor issue.

- **Reduction (landed):** factored out step 4 of the four-step chain
  (`extIsoCohomologyHomₖ = P.isoExt`, already a `ModuleCat k` iso hence `k`-linear). `map_smul'`
  now follows from a single crux `have key123 : ∀ y, e123 (r • y) = r • e123 y`, where
  `e123 : Ext M N n ≃+ (P.complex.linearYonedaObj k N).homology n` is the steps 1-3 composite. The
  file builds; net `sorry` count unchanged (still exactly 1, now `key123`).
- **Why this matters:** never leaving the `linearYonedaObj`-homology description eliminates 3 of the
  original plan's 5 obligations — functor-`k`-linearity of `(Ext k (ModuleCat A) n).obj (op M)` and
  naturality of `P.isoExt` in `N`, *neither in Mathlib*.

## Current frontier

`key123` (the sole remaining `sorry`) = naturality-in-`N` of `e123` under `θ := r • 𝟙 N`. Blocked on
naturality-under-target-morphism infrastructure that Mathlib does **not** package for
`extAddEquivCohomologyClass` (partial: `extMk_comp_mk₀` exists) and `homologyAddEquiv`
(`CohomologyClass` is `AddCommGroup`-only), plus threading `θ` through the repo's `homIso` tower in
`homComplexHomologyAddEquivₖ` (clean in principle: `homDegEquiv` only touches the source `P.complex.X i`).

## Overall project progress

Stage 3 formalization ongoing. Ch8 Problem 8.2.8 (`Ext` half): the `Ext ≃ₗ[k] Extₖ` bridge additive
chain is complete; `map_smul'` reduced to `key123` (#6935). This bridge feeds the final assembly
`Problem_8_2_8_ext` (#6898). Active parallel chains: Ch6 6.1.3-g affine classification, Ch4 4.12.8-a-iv
SO(3) classification.

## Next step

Claim #6935 and discharge `key123`. Recommended decomposition if still too large: (a)
`homComplexHomologyAddEquivₖ` naturality under `θ` (repo-local, self-contained), (b) steps 1-2 Mathlib
naturality, sharing a "`θ`-induced map on `homCochainComplex` homology" definition. Also prove the
clean half first: `homologyMap (linearYonedaObj.map (r • 𝟙 N)) = r • 𝟙`.

## Blockers

None hard. `key123` is a genuinely deep proof requiring new naturality infrastructure; tracked in
#6935 with a full route and honesty note. Partial PR for #6920 lands the reduction.
