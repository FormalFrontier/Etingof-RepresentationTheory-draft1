## Accomplished

Worker (`/work` → `/feature`) session on **#4862** (sub-b of #4821/#4732: the
ℂ-side highest-weight classification of the Schur-Weyl simples). Landed a new file
`Chapter5/SchurWeylSimplesClassificationComplex.lean` that reduces the entire
ℂ-side crux to a **single isolated `sorry`** (the counting step), with everything
else sorry-free:

1. `schurWeyl_decomposition_numerical_identity` (sorry-free) — for any equivariant
   decomposition `V^{⊗n} ≃ ⨁ᵢ Sᵢ⊗Lᵢ`, `∑ᵢ dim(Sᵢ)·char(Lᵢ) = ∑_λ
   dim(Specht_λ)·schurPoly λ`, chaining `formalCharacter_eq_of_rep_iso` /
   `_directSum` / `_trivialTensor`, `formalCharacter_glTensorRep_eq_pow`,
   `sum_X_pow_eq_sum_finrank_smul_schurPoly`.
2. `schurWeyl_decomposition_asModule_flatten` (sorry-free `def`) — extracts the
   `Einner` construction of `polynomial_homog_rep_asModule_embeds_directSum_simple`
   for arbitrary decomposition data: `asModule(glTensorRep) ≃_R ⨁_{ν:Σ i, Fin dim Sᵢ}
   asModule(L ν.1).ρ`.
3. `formalCharacter_eq_of_asModule_linearEquiv` (sorry-free) — a `MonoidAlgebra`-linear
   `asModule` equivalence gives equal formal characters (upgrades to an equivariant
   `k`-equiv via `asModuleEquiv_symm_map_rho`/`_map_smul`/`asAlgebraHom_of`).
4. `simpleModule_iso_component_of_embeds` (sorry-free) — a simple `R`-module
   embedding into `W ≃ ⨁_κ (simple Lsum)` is iso to one summand
   (`Submodule.linearEquiv_of_le_sSup` on the coordinate lines).
5. `schurWeyl_simples_isotypic_matching_complex` (sorry-free) — **the isotypic
   matching half** (route 1, steps 1–2,4): an injective `φ : BoundedPartition N n ↪ ι`
   with `char(L (φ λ)) = schurPoly N λ.parts`. Each `SchurModule ℂ N λ.parts`
   (simple via `schurModule_isSimple`) includes into `V^{⊗n}`, flattens, matches a
   single `Lᵢ`; char via #3 + `formalCharacter_schurModule_eq_schurPoly`; injectivity
   via `schurPoly_injective`.
6. `schurWeyl_simples_formalCharacter_classification_core_complex` — the headline
   ℂ-side theorem. Reduced to the **sole `sorry`**: `Function.Surjective φ` (the
   counting `|ι| = |P|`). Given surjectivity, `φ` is bijective and `lam := φ⁻¹`
   discharges the full conclusion (total + injective + char = schurPoly).

Registered the file in `Chapter5.lean`. Rebased onto latest `origin/main` (was 4
commits behind, incl. #4867 touching `decompose_polynomial_gl_rep`); module rebuilds
clean (`lake build …Complex` green, 8064 jobs).

## Current frontier

The single remaining `sorry` is `hφsurj : Function.Surjective φ` in
`schurWeyl_simples_formalCharacter_classification_core_complex` — the
double-centralizer counting `|ι| = |P|`. Tracked by the new sub-issue (see below).

## Overall project progress

Stage 3 ongoing. The Tier-4 highest-weight crux #4821/#4732 is now reduced, on the
ℂ side, to exactly one counting lemma. The matching half (long believed the hard
part) is fully formalized and sorry-free.

## Next step

Discharge `Function.Surjective φ` via Theorem5_18_4 (double-centralizer): the number
of simple GL-types in `V^{⊗n}` equals the number of antitone partitions of `n` with
length `≤ N` (= `|BoundedPartition N n|`). With `φ` injective `P ↪ ι`, card equality
gives surjectivity. The new sub-issue carries the details.

## Blockers

None. (An earlier full build off the stale base hit a broken
`Chapter6/FieldGenericD5Tilde.lean`; that file was retired on `main` by #4868, and
the breakage is gone after rebasing onto latest `origin/main`.)
