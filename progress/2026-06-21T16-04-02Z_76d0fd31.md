# Progress — session 76d0fd31 (feature, issue #4896 part a)

UTC: 2026-06-21T16:04:02Z · type: feature (decompose + top-down spec) · branch: agent/76d0fd31

## Accomplished

- Claimed #4896 (part (a) of the kernel-lemma-K′ core: "constituents of
  `A/det = k[Xᵢⱼ]/(det)` have last highest-weight coordinate `ν_N = 0`").
- Mapped the actual infrastructure state and confirmed the deliverable is
  research-level and not landable sorry-free in one session:
  - The genuine content is the **GL_N × GL_N-equivariant Cauchy decomposition**
    of `k[Xᵢⱼ]` (`= ⊕_{ν dom ∈ ℕ^N} V_ν^* ⊠ V_ν`), absent from repo and Mathlib;
    the route doc itself calls it a multi-issue effort. `Theorem5_23_2_ii` is
    rank-only (no weight/equivariance data); torus shortcuts are defeated by the
    `N=2,r=1` example `αX₁₁X₂₂+βX₁₂X₂₁`.
  - `AlgIrrepGL` (`Theorem5_23_2.lean:86`) is a **bare k-module** (GL-action
    ignored by design), so "L ≅ AlgIrrepGL ν as representations" can't even be
    stated against it; the real classification interface is
    `iso_of_formalCharacter_eq_schurPoly` (still `sorry`, #4882/#4699).
- Landed the **consumable statement** (top-down): new file
  `Chapter5/CauchyDetQuotient.lean` stating
  `quotDetRep_irreducible_constituent_lastWeight_zero` — an irreducible
  finite-dim GL_N-rep `L` embedding GL_N-equivariantly into `A/det` has
  `formalCharacter k N L = schurPoly N ν` with `ν` antitone and
  `(0:ℕ) ∈ Set.range ν` (= last/smallest coordinate `ν_N=0`). Phrased over the
  project's established idiom (`IsSimpleModule` over `MonoidAlgebra k (GL …)` +
  `formalCharacter = schurPoly`), directly consumable by sibling #4897. Single
  documented `sorry`. Registered in `Chapter5.lean`. Builds clean.
- Decomposed #4896 into successor issues:
  - #4902 — give `AlgIrrepGL` a genuine GL_N-representation/FDRep structure
    (independent, session-sized, sorry-free).
  - #4903 — right-GL homogeneous grading of `k[Xᵢⱼ]` + per-degree det SES
    `0 → A_{d-N}⊗χ → A_d → (A/det)_d → 0` (independent, session-sized,
    sorry-free).
  - #4904 — the GL×GL-equivariant Cauchy multiplicity-one decomposition
    (research-level epic; expect further decomposition).
  - #4905 — assemble #4902+#4903+#4904+det-shift to discharge the `sorry` in
    `CauchyDetQuotient.lean` (`depends-on` #4902,#4903,#4904).

## Current frontier

`quotDetRep_irreducible_constituent_lastWeight_zero` stated and building; its
proof and the three infrastructure pieces are the open successor issues.

## Overall project progress

Chapter 5 frontier (Schur–Weyl / Peter–Weyl / kernel lemma K). The det⁻¹-
elimination kernel lemma (K) reduces to (K′); (K′) reduces to part (a) (this
issue, now a concrete consumable statement) + part (b) (#4897). The deep gap is
the equivariant Cauchy decomposition (#4904), gated like the classification
capstone (#4882/#4699).

## Next step

Pick up #4902 or #4903 — both independent, session-sized, sorry-free infra wins
that the assembly #4905 consumes. #4904 is the research epic (decompose on
claim). #4905 is blocked until #4902/#4903/#4904 land.

## Blockers

#4905 blocked on #4902/#4903/#4904 (wired). The eventual sorry-free proof also
leans on the highest-weight classification interface `iso_of_formalCharacter_eq_schurPoly`
(#4882/#4699). No blockers on the landed statement file itself.
