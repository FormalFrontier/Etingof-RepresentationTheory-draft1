## Accomplished

Proved both `sorry`s in `EtingofRepresentationTheory/Chapter2/Problem2_5_2.lean`
(issue #5970), making it sorry-free:

- **(a)** `irreducible_iff_forall_cyclic`: `IsSimpleModule A V ↔ ∀ v ≠ 0, span A {v} = ⊤`.
  Forward via `IsSimpleModule.eq_bot_or_eq_top` on `span A {v}` (nonzero ⇒ not `⊥` ⇒ `⊤`).
  Reverse by building `IsSimpleOrder` from `eq_bot_or_eq_top`: a nonzero submodule contains a
  nonzero `v` (`Submodule.exists_mem_ne_zero_of_ne_bot`), whose span is `⊤` and `≤ p`.
- **(b)** `cyclic_iff_isoQuotient`: cyclic ↔ `V ≃ₗ[A] A ⧸ I`. Forward: `LinearMap.toSpanSingleton`
  is surjective (range = span = `⊤` via `LinearMap.span_singleton_eq_range`), then
  `LinearMap.quotKerEquivOfSurjective ... |>.symm`. Reverse: `A ⧸ I` is generated by `I.mkQ 1`;
  transport the span along `e.symm` with `Submodule.map_span` + `Submodule.map_top`.

`#print axioms` on both shows only `propext, Classical.choice, Quot.sound` — no `sorryAx`.
`items.json`: `Chapter2/Problem2.5.2` → `sorry_free`.

## Current frontier

Issue #5970 complete. Part (c) (coregular `ℂ[x,y]/I₂` counterexample) is explicitly out of scope
and remains a separate deferred item.

## Overall project progress

Phase 3 formalization ongoing; one more Chapter 2 problem now sorry-free.

## Next step

Open PR for #5970. Remaining unclaimed feature issues at session start: #5971 (Problem 4.1.4,
p-group irreducibles), #5972 (Exercise 2.11.2, tensor product as quotient).

## Blockers

None.
