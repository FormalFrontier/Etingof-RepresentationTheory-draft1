## Accomplished
Worked the **irreducibility** conjunct of `Chapter5/Example5.19.3` (issue #5638; the
GL(V)-equivariance conjunct already landed in PR #5716). Re-confirmed the fidelity finding
and assessed Problem 4.12.3 (the cited irreducibility proof). Concrete additions to
`EtingofRepresentationTheory/Chapter5/Example5_19_3.lean`:

- `Example5_19_3_exterior_subsingleton_of_dim_lt` — genuine, complete proof of the book's
  parenthetical "`∧ⁿV = 0` when `n > dim V`" via `exteriorPower.finrank_eq`
  (`finrank = C(dim V, n) = 0`) + `Module.finrank_zero_iff`. Previously uncaptured.
- `Example5_19_3_symmetric_irreducible` / `Example5_19_3_exterior_irreducible` — the precise
  irreducibility propositions recorded as `Prop`-valued defs referencing the actual
  GL(V)-actions already in the file (`symmetricPowerMap`, `exteriorPower.map n`). A
  subrepresentation is a submodule stable under every `g : V ≃ₗ[k] V`; irreducibility says
  the only such submodules are `⊥`/`⊤`. No sorry, no vacuous content — these name the
  claims faithfully so #5715 can prove them against a fixed formulation.
- A section docstring documenting the obstruction (the explicit scope note the directive
  asked for): the proof is the distinct-eigenvalue / transvection argument, which needs the
  symmetric-power monomial basis (absent from Mathlib — only the exterior side has
  `Module.Basis.exteriorPower`), a generic distinct-weight diagonal, the spectral-spanning
  lemma, and the transvection orbit-connectivity argument. Multi-session; tracked in #5715.

Module builds cleanly (8585 jobs), no sorries. All warnings are pre-existing (lines
190/238/274/284 in the original code), none in the new declarations.

## Current frontier
The full irreducibility proof (Problem 4.12.3) is genuinely out of reach for a single
session and remains open under #5715. The exact statements to prove are now pinned in the
source as `Example5_19_3_symmetric_irreducible` / `Example5_19_3_exterior_irreducible`.

## Overall project progress
Stage 3 formalization; Wave-2/3 fidelity adjudication (epic #5338) ongoing. #5638 fully
adjudicated: equivariance (PR #5716) + vanishing conjunct (this PR) landed; the irreducibility
proposition is recorded faithfully and its proof is tracked in #5715.

## Next step
Pick up #5715: formalize Problem 4.12.3. Suggested order — (1) abstract spectral-spanning
lemma (invariant subspace of a distinct-eigenvalue diagonalizable operator is spanned by
eigenvectors), (2) symmetric-power monomial basis + diagonal weight computation, (3) generic
distinct-weight diagonal existence, (4) transvection orbit connectivity, then discharge
`Example5_19_3_symmetric_irreducible` / `_exterior_irreducible`.

## Blockers
None for the landed work. Full irreducibility is blocked on the multi-step Problem 4.12.3
development (no Mathlib symmetric-power basis), tracked in #5715.
