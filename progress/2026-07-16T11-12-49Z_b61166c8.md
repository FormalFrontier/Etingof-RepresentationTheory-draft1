## Accomplished

Feature session for #6789 (Ch5 #Problem5.24.2): the FFT range-identification capstone. Landed the
**assembly** of `weightedHomogeneous_invariant_mem_range_endTensorEval` in
`Problem5_24_2_Bridge.lean`, reducing the deep `sorry` to a single, precisely-scoped crux lemma.

- Added `import ...SchurWeylGLTransfer` to Bridge (acyclic: SchurWeylGLTransfer → Theorem5_18_4;
  Bridge already imported Theorem5_18_4). This makes `Theorem5_18_4_centralizers` and
  `adjoin_unitsTensorPow_eq_diagonalActionImage` available without the circular dependency that
  blocked reusing Supporting's `mem_symGroupImage_iff_commute_diag` (Supporting imports Bridge).
- Stated the crux `exists_endTensorEval_equivariant_section` (sorried): existence of a
  `GL`-equivariant section of `endTensorEval slot` on the multidegree-`d` part (section property +
  equivariance in the `PiTensorProduct.map (mulVecLin g)` form matching `endTensorEval_conj`).
- Proved `weightedHomogeneous_invariant_mem_range_endTensorEval` **sorry-free modulo the crux**:
  * take `M := σ p`; section property gives `endTensorEval slot M = p`;
  * for each `End`-unit `g'`, build the corresponding `Matrix`-unit `g` via
    `LinearMap.toMatrix'` (mutually-inverse `A, A'`), with `mulVecLin ↑g = ↑g'` by
    `toLin'_toMatrix'`;
  * invariance `conjAlgHom g p = p` + equivariance ⟹ `σ p = Q · σ p · P` with `P = g'^{⊗n}`,
    `Q = (g'⁻¹)^{⊗n}`; `P·Q = 1` via `PiTensorProduct.map_mul`/`map_one` ⟹ `Commute P (σ p)`;
  * `Theorem5_18_4_centralizers` + `adjoin_unitsTensorPow_eq_diagonalActionImage` +
    `Algebra.commute_of_mem_adjoin_of_forall_mem_commute` ⟹ `M ∈ symGroupImage`.

`lake build EtingofRepresentationTheory.Chapter5.Problem5_24_2` succeeds; the only remaining `sorry`
is the crux at `Problem5_24_2_Bridge.lean:277`. All lint clean (fixed line-length + `show`→`change`).

## File-placement decision

Kept the theorem in `Problem5_24_2_Bridge.lean` with statement unchanged (as #6789 requires). Used
the Schur–Weyl centralizer identity inline rather than Supporting's `mem_symGroupImage_iff_commute_diag`,
because Supporting imports Bridge (circular). Adding the `SchurWeylGLTransfer` import to Bridge is
acyclic and gives the same lemmas Supporting used.

## Current frontier

`exists_endTensorEval_equivariant_section` (`Bridge:277`) is the last `sorry` — the block-symmetrization
section construction (FFT reductivity heart). Filed as child issue #6807 with the full route.

## Overall project progress

Phase 3 formalization ongoing. Problem 5.24.2 FFT range identification: B1 surjectivity (#6797) done;
B2 symGroupImage=GL-invariants (#6798) merged; equivariance `endTensorEval_conj` (#6787) merged;
invariant-range assembly (this) landed modulo the equivariant section (#6807). Once #6807 lands, the
whole range identification is sorry-free and the FFT capstone `weightedHomogeneous_invariant_mem_adjoin`
in `Problem5_24_2.lean` can be discharged.

## Next step

Claim #6807: construct the GL-equivariant section `exists_endTensorEval_equivariant_section` by
block-symmetrization, mirroring `PolynomialTensorBridge` (`homogeneousPolyToTensor` +
`tensorToPoly_polyToTensor_eq_self` + `homogeneousPolyToTensor_equivariant`), generalized to the
`k`-letter `End(V^{⊗n})` setting. Reuse the in-file combinatorics (`exists_fg_realizes`,
`prod_monomial_single`) and `endTensorEval_conj`.

## Blockers

None. #6789 is a partial completion: assembly landed, crux deferred to #6807.
