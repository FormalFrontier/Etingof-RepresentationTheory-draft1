## Accomplished

Claimed and completed #7478, "Formalize the A₄ character table in the introduction to Section 4.8".

New file `EtingofRepresentationTheory/Chapter4/Introduction_4_8.lean` (sorry-free, `native_decide`-free,
axiom-clean on every public endpoint), added to the `Chapter4.lean` aggregator. It realizes the book's

| `A₄` | `Id` | `(123)` | `(132)` | `(12)(34)` |
|---|---|---|---|---|
| `#` | 1 | 4 | 4 | 3 |
| `ℂ` | 1 | 1 | 1 | 1 |
| `ℂ_ε` | 1 | `ε` | `ε²` | 1 |
| `ℂ_{ε²}` | 1 | `ε²` | `ε` | 1 |
| `ℂ³` | 3 | 0 | 0 | −1 |

by four genuine representations, not by an orthonormality certificate.

* `qHom : A₄ →* Multiplicative (ZMod 3)` is surjective (`qHom_surjective`) with kernel exactly
  `alternatingGroup.kleinFour (Fin 4)` (`ker_qHom`), which is the book's `A₄/(ℤ/2 ⊕ ℤ/2) = ℤ/3`.
  Normality of `V₄` is Mathlib's `alternatingGroup.normal_kleinFour`.
* `repEps k` (`k : ZMod 3`) is the one-dimensional representation attached to
  `cubeChar k ∘ qHom`, with `ε = exp(2πi/3)` (`eps_eq`) a primitive cube root (`eps_primitive`).
* `repStd` is the deleted natural permutation representation, character `#fix(g) − 1`.
* `classRepA4 = (Id, (012), (021), (01)(23))` with recogniser `classIdxA4`; class sizes `1,4,4,3`,
  and the two 3-cycle classes are proved distinct (`classRepA4_one_two_not_conj`).
* `irrepA4_character` gives every table entry; `irrepA4_simple`, `irrepA4_pairwise`,
  `simple_iso_irrepA4` (via Corollary 4.2.2 and `Fintype.card (ConjClasses A₄) = 4`),
  and `sum_sq_dim` (`1²+1²+1²+3² = 12`).

Public endpoints `Etingof.Introduction_4_8_A4_{card, conj_classes, class_sizes, class_reps,
quotient, eps, sum_sq_dim, irrep, simple, character, pairwise, complete}`.

`progress/items.json`: `Chapter4/Introduction_4.8` moved from `covered_partial`/`partial` to
`covered`/`verified`, `fidelity_issue` 7478 dropped, `fidelity_decl` recorded.

Also closed stale issue #7423 (`fix(Ch9 #Corollary9.7.3)`): all five acceptance criteria were
already delivered by PR #7727, which lacked a `Closes #7423` line. Verified the declarations on
`main` before closing.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter4.Introduction_4_8` is green (6 s for the module,
no warnings); `#print axioms` on all twelve public endpoints reports exactly
`[propext, Classical.choice, Quot.sound]`. The Chapter 4 aggregator build was running at hand-off.

## Overall project progress

Stage 3 formalization. Chapter 4's §4.8 introduction now has a complete public `A₄` catalogue,
matching the existing `Q₈`/`S₄`/`A₅` catalogues of Example 4.8.1, whose generic machinery
(`charRep`, `stdRepM`, `fixCardM`, `conjIdxS4`) it reuses without duplication.

## Next step

The largest remaining unclaimed items are the Chapter 5 Schur–Weyl fidelity fixes (#7379, #7399,
#7400) and the Chapter 2/6 classification upgrades (#7364, #7402, #7420). #7759 (Gabber–Kac for
`A₂⁽²⁾`) is now unblocked by the merged `ℕ²`-grading but remains research-level; its issue records
four ruled-out families of attempts.

## Blockers

None.

The optional tetrahedron-rotation realization of `ℂ³` was deliberately not formalized: the issue
marks it optional, and the deleted permutation representation is the same representation.
