## Accomplished
- Bumped toolchain + Mathlib to **v4.30.0** (final). Full build green (0 errors, 8824 jobs).
- v4.30.0 final Mathlib API changes migrated:
  - `Submodule.linearProjOfIsCompl` → `projectionOnto` (deprecated alias is a distinct constant, breaking `rw`/`simp` matches). Renamed across Example6_3_1, Problem6_9_1, MoritaStructural, Theorem5_15_1, GLRepAlgebraic, Theorem3_8_1, including `_apply_left` → `projectionOnto_apply_left`, `_ker` → `ker_projectionOnto`, and `_apply_right'` → `projectionOnto_apply_of_mem_right` (which also changed argument shape).
  - `Submodule.mk_quotientEquivOfIsCompl_apply` made its submodule args implicit (CleanQuotientHelpers).
  - `zero_le` made its argument implicit (PowerSumCauchyBilinear).
  - `group` tactic now reports "made no progress" on already-normal goals: replaced with explicit cancellation rewrites (Theorem4_5_4, Lemma5_13_1).
  - simp normal-form change: restructured the `case pos` proof (Proposition5_21_1).
  - whnf timeout in GL2NormalizerInfra root-caused to a `g.inv` vs `(↑g)⁻¹` unification loop — fixed by matching `charpoly_units_conj'` syntactically (no heartbeat bump).

## Current frontier
v4.30.0 PR being opened with auto-merge. Next: v4.31.0-rc1.

## Overall project progress
Sweep: v4.28.1 → v4.30.0-rc2 merged (12 versions). v4.30.0 green and merging. Remaining: v4.31.0-rc1, v4.31.0-rc2, v4.31.0, v4.32.0-rc1.

## Next step
After v4.30.0 merges, run bumpstep for v4.31.0-rc1.

## Blockers
None.
