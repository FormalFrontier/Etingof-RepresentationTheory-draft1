## Accomplished

Claimed #4948 (route-B ingredient (c): `glWeightSpace_top_of_simple_formalCharacter_eq_schurPoly`).
While analyzing the crux, **discovered the lemma is false as stated** and escalated rather
than attempting an unprovable `sorry`.

- **Counterexample** (`N=2`, `M = det⁻¹ ⊗ Sym³(std)`): `M` is simple (irreducible ⊗ 1-dim
  character), 4-dimensional, with `formalCharacter k 2 M = x₁ + x₂ = schurPoly 2 (1,0)`
  (the `det⁻¹` shift pushes weights `(3,0),(2,1),(1,2),(0,3)` to `(2,-1),(1,0),(0,1),(-1,2)`;
  only `(1,0),(0,1) ∈ ℕ²` survive). Its ℕ-weight spaces span only a 2-dim subspace, so the
  conclusion `⨆ μ, glWeightSpace = ⊤` fails. `formalCharacter` is blind to non-ℕ weights, so
  it is not a complete invariant for non-polynomial simples. Same construction works for every
  `lam` (e.g. `det⁻¹ ⊗ Sym²` for `lam = (0,0)`).
- **Confirmed independently** via second opinion (Codex): counterexample correct, lemma false,
  fix = assume polynomiality.
- **Downstream impact:** the sole consumer `simpleRep_iso_schurModule_of_formalCharacter_eq`
  (`SchurWeylFormalCharacterIso.lean:188`, landed under #4901) is also false on the same
  example (`L` 4-dim ≇ `SchurModule 2 (1,0)` = std, 2-dim). Route B (#4901, PR #4956)
  introduced the gap by manufacturing `L`'s polynomiality from this false ingredient.
- Posted detailed analysis + recommended fix on #4948 and a cross-reference on #4901;
  `coordination skip 4948` (now `replan`).

## Current frontier

Route B (#4901 assembly) needs restructuring. The recommended fix (in the #4948 comment):
1. Add `hLtop : ⨆ μ, glWeightSpace k N L (fun i => μ i) = ⊤` as a hypothesis to
   `simpleRep_iso_schurModule_of_formalCharacter_eq`.
2. In its body use `hLtop` for the `L` side and the proven
   `Proposition5_22_2.glWeightSpace_schurModule_iSup_eq_top` for the `S = SchurModule` side;
   delete the two calls to the false ingredient.
3. At the real caller `iso_of_formalCharacter_eq_schurPoly:349`, discharge `hLtop` for the
   simple summand `L (f 0)` by transporting `M`'s `h_span` across the existing equivariant
   iso `hφ'` (line 336). The transport machinery exists: `glWeightSpace_map_eq_of_rep_iso`
   (`FormalCharacterIso.lean:582`, currently `private`) + `Submodule.map_iSup`; package as a
   public `glWeightSpace_iSup_eq_top_of_rep_iso`. Watch the `M` vs `FDRep.of M.ρ` defeq
   (`formalCharacter_FDRep_of_ρ` is not `rfl`).
4. Retire the false lemma `glWeightSpace_top_of_simple_formalCharacter_eq_schurPoly`.

Ingredients #4946 (simplicity) and #4947 (character independence) are unaffected and remain
valid.

## Overall project progress

Stage 3 (formalization) ongoing in Chapter 5. Route B highest-weight uniqueness (#4901)
landed sorry-free *modulo* three isolated ingredient sorries (#4946/#4947/#4948); this turn
established that ingredient (c) #4948 is unprovable as stated and the assembly itself needs a
polynomiality hypothesis.

## Next step

A planner should pick up the #4948 replan and create a route-B correction issue implementing
the 4-step fix above (self-contained in `SchurWeylFormalCharacterIso.lean` +
`FormalCharacterIso.lean`). It does not conflict with the in-flight #4946/#4947 ingredient
signatures.

## Blockers

None for the project; #4948 is unblocked-but-unprovable-as-stated and routed to replan.
