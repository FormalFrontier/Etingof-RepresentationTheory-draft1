## Accomplished

Completed issue #6743 (Ch8 Problem 8.2.8 `Tor`, route step 1 of milestone (c), parent #6727):
`mapBifunctorPostcompIso` — an additive functor commutes with the total complex
`HomologicalComplex.mapBifunctor … (down ℕ)`.

New file `EtingofRepresentationTheory/Chapter8/MapBifunctorPostcomp.lean`, sorry-free, axioms
`[propext, Classical.choice, Quot.sound]`:

- `postcompBifunctor F G := F ⋙ (whiskeringRight _ _ _).obj G` plus its `PreservesZeroMorphisms`
  instances (via `Functor.preservesZeroMorphisms_comp`).
- `postcompX j` — the degreewise iso
  `G.obj ((mapBifunctor F c).X j) ≅ (mapBifunctor (F ⋙ …G) c).X j`. This is *exactly*
  `Limits.PreservesCoproduct.iso G (postcompFam …)`: both degrees are the finite coproduct
  `∐_{π (i₁,i₂)=j}` over the fiber, and an additive functor preserves finite coproducts
  (`preservesFiniteCoproductsOfAdditive` + the `Finite` fiber). All three object types
  (`mapHomologicalComplex.obj.X`, `total.X`, the postcomposed `total.X`) are *definitionally*
  `G.obj (∐ f)` / `∐ (G ∘ f)`, so the iso lands with the right type with no transport.
- Summand compatibility `ιMapBifunctor_comp_postcompX_hom` (+ inv/orZero forms) from
  `ι_comp_sigmaComparison` / `PreservesCoproduct.inv_hom`.
- `d₁_postcompX` / `d₂_postcompX` — `G` intertwines the partial differentials, using
  `Functor.map_units_smul` (additive ⇒ `Linear ℤ`, so `G` commutes with the Koszul sign
  `ε₁/ε₂ : ℤˣ`) and the fact that `d₁ = F.map d`, `d₂` pass through `G` definitionally.
- `postcompX_hom_ext` — extensionality for maps out of `G (∐ summands)` via
  `isColimitOfHasCoproductOfPreservesColimit`.
- `postcompX_comm` (differential square) and the assembled `mapBifunctorPostcompIso` via
  `HomologicalComplex.Hom.isoOfComponents`.
- Smoke `example` instantiating `G = tensorRightFunctorₖ k (A₁⊗A₂) N`,
  `F = extTensorFunctor k A₁ A₂` over `down ℕ` — the shape route step 3 consumes.

Wired into `Chapter8.lean`. `lake build EtingofRepresentationTheory.Chapter8` exits 0.

## Current frontier

Route step 1 of milestone (c) of #6727 is done and reusable (fully general in `F`, `G`, the
shape, with finite-fiber + additive hypotheses). The degreewise-coproduct crux that #6738/#6727
flagged as having no Mathlib lemma is dispatched by recognizing it as `PreservesCoproduct.iso`
against defeq object types.

## Overall project progress

Stage 3, Ch8 Problem 8.2.8 (`Tor`) four-fold rearrangement. Milestone (c) now has step 1
(`mapBifunctorPostcompIso`) landed. Step 2 (the `rearrangeBifunctorNatIso`, #6742) and step 3
(#6727 final assembly, consuming this iso) remain. Independent sibling #6738 (restriction-of-scalars
complex iso) is still open.

## Next step

Pick up #6727 route step 3 (final milestone-(c) assembly): compose `mapBifunctorPostcompIso`
(this issue) with the NatIso from step 2 (#6742) and the bidegree rearrangement. Also #6738 is
still open and is the analogous complex-level iso for restriction of scalars.

## Blockers

None. #6743 complete; PR opened.
