## Accomplished

Closed issue #6139 — assembled the strict modular counting drop **Exercise 4.2.3**
(`Etingof.Exercise4_2_3`): if `|G| = 0` in `k` then
`Nat.card (IrrepClasses k G) < Nat.card (ConjClasses G)`. Now sorry-free;
`#print axioms Etingof.Exercise4_2_3` reports only `[propext, Classical.choice, Quot.sound]`.

Changes:
- **New `Chapter4/Exercise4_2_3_Assembly.lean`** (imports `Exercise4_2_3_FieldGeneral` and
  `Exercise4_2_3_StrictBound`, resolving the import cycle):
  - `natCard_irrepClasses_lt_conjClasses_of_isAlgClosed` — the strict split-field lemma over an
    algebraically closed `K` with `|G| = 0`. Reuses the split-field enumeration (`SplitData`,
    `exists_splitSimples_count`) and the trace-form independence, feeding the strict cocenter
    bound `CocenterMonoidAlgebra.card_lt_conjClasses_of_traceForm_linearIndependent` (each block
    trace form annihilates the nonzero central nilpotent `P̄ = ∑_g g`).
  - `Exercise4_2_3` (relocated) — proved by `lt_of_le_of_lt` through `K = AlgebraicClosure k`:
    base-change monotonicity `natCard_irrepClasses_le_of_ringHom_field` (#6127) for the `≤`, and
    the strict lemma for the `<`, transporting `|G| = 0` to `K` via `map_natCast (algebraMap …)`.
- **`Exercise4_2_3_FieldGeneral.lean`**: extracted the standard-module trace-form independence
  from the `≤`-proof into the reusable `traceFormQ_Std_linearIndependent`, and rewired
  `natCard_irrepClasses_le_conjClasses_of_isAlgClosed` to call it. Shared by both the `≤` and the
  new strict `<` bound.
- **`Exercise4_2_3.lean`**: removed the placeholder `sorry` theorem (statement moved downstream;
  the two dependency files import this file, so it cannot state `Exercise4_2_3` itself). Left a
  pointer comment.
- **`Chapter4.lean`**: added the import; **`progress/items.json`**: `Chapter4/Exercise4.2.3` →
  `sorry_free`.

Full `lake build EtingofRepresentationTheory.Chapter4` passes (8646/8646, 0 errors).

## Current frontier

Exercise 4.2.3 is fully assembled and sorry-free. Its whole dependency chain (SplitSimples count,
split-field bound, base-change monotonicity, strict cocenter drop) is now closed — the earlier
`coverage_note` caveat about the "still-deferred SplitSimples count lemma #6150" is stale; the
axiom check confirms no `sorryAx`.

Note: `Exercise4_2_3` requires `k` and `G` in the **same universe** (`Type u`), matching the
non-strict companion `natCard_irrepClasses_le_conjClasses`. This is forced by the base-change
lemma `natCard_irrepClasses_le_of_ringHom_field` (same-universe, from #6127). The original
placeholder used `Type*` (independent universes), which the base-change route cannot support.

## Overall project progress

Phase 3 formalization ongoing. This turn moved one Chapter 4 exercise to `sorry_free`.

## Next step

One unclaimed feature issue remains: #6162 (Ch8 Problem 8.2.6 — left-derived homology LES /
six-term exact sequence). That is deep homological-algebra work; a fresh session should claim it.

## Blockers

None.
