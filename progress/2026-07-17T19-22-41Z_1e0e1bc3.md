## Accomplished
Closed the `sorry` in `affine_tree_one_branch_iso`
(`Chapter6/Problem6_1_3_continued_tildeE.lean`, #6907), the final piece of #6882: a connected
acyclic affine Dynkin diagram with all degrees ≤ 3 and a unique degree-3 vertex is graph-isomorphic
to `AffineType.E6tilde`, `E7tilde`, or `E8tilde`.

Proof structure:
- Feed `affine_tree_one_arm_reciprocal` to obtain the three arms `1 ≤ p ≤ q ≤ r`, the `armAdjIdx`
  re-indexing `σ : Fin n ≃ Fin n`, and the cleared-denominator reciprocal equality.
- `affine_arm_length_solutions` pins `(p,q,r)` to `(2,2,2)`, `(1,3,3)`, or `(1,2,5)`.
- For each triple, `subst`, derive `n = 7/8/9`, and build the graph iso as
  `(Equiv.ofBijective <explicit vertex permutation>).trans ((finCongr h).trans σ)`. The explicit
  permutation vectors map each affine type's standard vertex numbering onto the `armAdjIdx` layout:
  E₆ `![2,1,0,3,4,5,6]`, E₇ `![4,3,2,1,5,6,7,0]`, E₈ `![8,7,6,5,4,1,2,3,0]`.
- Adjacency identity `adj (σ ..) (σ ..) = t.adj i j` closed by a `decide` over `Fin t.rank²` after
  reducing `adj (σ x)(σ y)` to `if armAdjIdx .. then 1 else 0` (via `hadj_iff` + the 0/1 property
  `hD.2.2.1`) and stripping `finCongr` with a `rfl` cast lemma.

## Current frontier
`affine_tree_one_branch_iso` compiles sorry-free. `lake build` of the file succeeds (8586 jobs).

## Overall project progress
Stage 3 formalization, Chapter 6 affine Dynkin classification (#6779). The ⟹ direction assembly
(`affine_dynkin_classification`) is in place; the one-branch tree case is now fully reduced to two
remaining structural sorries, both in earlier lemmas:
- `affine_one_branch_arm_layout` (line ~2844): the three-arm walk / `armAdjIdx` layout extraction
  (transitive dependency of this lemma via `affine_tree_one_arm_reciprocal`).
- `affine_tree_two_branch_iso` (line ~2719): the two-branch `D̃ₙ` case (independent).

## Next step
Discharge `affine_one_branch_arm_layout` — the structural graph-combinatorial walk that lays out the
three arms onto the `armAdjIdx` pattern. Once it lands, `affine_tree_one_branch_iso` and
`affine_tree_one_arm_reciprocal` become fully sorry-free (axioms limited to
`propext, Classical.choice, Quot.sound`).

## Blockers
None. The residual `sorryAx` in `#print axioms affine_tree_one_branch_iso` is purely transitive
through the still-sorried `affine_one_branch_arm_layout`; the lemma's own proof is sorry-free.
