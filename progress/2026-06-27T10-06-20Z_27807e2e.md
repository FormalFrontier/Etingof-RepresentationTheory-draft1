## Accomplished
Landed the contragredient identity API for `GL_n` (issue #5522). New file
`EtingofRepresentationTheory/Chapter5/ContragredientIdentity.lean` with:

- `algIrrepGLDual_iso_linearDual` — the deliverable: a `GL_n`-equivariant `k`-linear
  isomorphism `AlgIrrepGLDual n lam k ≃ₗ[k] Module.Dual k (AlgIrrepGL n lam k)`
  intertwining `algIrrepGLRepDualρ` with `(algIrrepGLRepρ n lam k).dual`. **Its body
  is sorry-free.**
- `isEquivariant_of_charTwist` — **fully proven, reusable.** Cancelling a common
  character twist (`charTwistRep c`) from an equivariant equivalence preserves
  equivariance (the scalar `c g` is a unit and `e` is `k`-linear). This is the
  "untwist" glue that strips the `det^s`-twist.
- `exists_detTwist_equivariantEquiv_dual` — the analytic heart, **sorried**, tracked
  in #5526. States that after a large `det^s`-twist the two contragredient models
  become equivariantly isomorphic (via `iso_of_formalCharacter_eq_schurPoly` on both
  polynomial models).

Added the file to the `Chapter5.lean` aggregator. `lake build
EtingofRepresentationTheory.Chapter5` is green (8741 jobs); the only `sorry` warning
is on the tracked sub-lemma.

## Current frontier
The contragredient identity reduces to one isolated character-theoretic fact
(#5526): `formalCharacter (ρ.dual)` at inverse weights plus the inverted-variable
Schur identity `s_λ(x⁻¹)·(x₁⋯x_n)^s = s_{w₀λ+s}(x)`, feeding
`iso_of_formalCharacter_eq_schurPoly`.

## Overall project progress
Chapter 5 (§5.23 contragredient / Peter-Weyl) infrastructure. The
`algIrrepGLDual_iso_linearDual` name is now available to retype the canonical
evaluation pairing `contractLeft` (`ContragredientPairing.lean`, #5515) onto
`AlgIrrepGLDual ⊗ AlgIrrepGL` — the downstream assembly tracked in #5523.

## Next step
Discharge #5526 (the `det^s`-twisted character match). Likely sub-decomposes into:
(a) formal-character-of-dual at inverse weights; (b) the inverted-variable Schur
identity; plus algebraicity / `ℕ`-weight spanning / `finrank` match of the twisted
dual for the `iso_of_formalCharacter_eq_schurPoly` hypotheses.

## Blockers
None. #5526 is a self-contained follow-up; the main API and the untwist glue are in
and sorry-free.
