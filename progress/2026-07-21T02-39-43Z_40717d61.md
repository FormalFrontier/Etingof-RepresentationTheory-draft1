## Accomplished

Proved `finrank_g_three` sorry-free (issue #7084), with the corrected signature

```lean
theorem finrank_g_three (k : Type*) [Field k] (hk : (2 : k) ≠ 0) :
    Module.finrank k (g k 3) = 6
```

in `EtingofRepresentationTheory/Chapter2/Problem2_16_3.lean`. `#print axioms
finrank_g_three` shows `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

Structure (mirrors the `n=2`/`B₂` template):

- New defs `vb n := ⁅yb n, wb n⁆` (root `α+3β`) and `ub n := ⁅xb n, vb n⁆`
  (root `2α+3β`), extending the existing `xb, yb, zb, wb`.
- All 15 pairwise bracket relations among the six basis monomials, derived by
  Jacobi (`leibniz_lie` / `lie_lie`) from the two defining relators. The crux
  `lie_zb_vb_three_zero : ⁅zb,vb⁆ = 0` comes from the two Jacobi routes
  `[z̄,v̄] = [ȳ,ū] = -[z̄,v̄]`, giving `2·[z̄,v̄] = 0`; `hk : (2:k) ≠ 0` then forces
  it to vanish. The six hk-dependent lemmas plus `span_eq_top_three` take
  `[Field k]` (inverting `2` needs more than `CommRing`).
- Lower bound: `matHom₃ : FreeLieAlgebra → gl₇(k)`, `x ↦ GX`, `y ↦ GY`
  (explicit single-entry 7×7 matrices). Key discovery: a graded module with
  weights `{0} ∪ (positive roots)` is faithful on `n⁺(G₂)` in **every**
  characteristic (rank 6 mod 2,3,5,7 — verified numerically before coding),
  unlike the standard 7-dim G₂ rep which degenerates in char 3. So `indep_three`
  is char-free; only the upper bound needs `hk`.
- Upper bound: `span_eq_top_three` via `Submodule.span_induction₂` (36-case
  bracket table), assembled with `Module.Basis.mk` over `Fin 6`.

No downstream callers of `finrank_g_three` (grep confirms), so the added
hypothesis breaks nothing.

## Current frontier

Chapter 2 Problem 2.16.3(a) is now fully formalized: `finrank_g_one = 3`,
`finrank_g_two = 4`, `finrank_g_three = 6`. Part (b) (`¬ Module.Finite (g 4)`)
was already done.

## Overall project progress

Stage 3 formalization ongoing. This item completes the `𝔤₃` dimension, the last
open piece of Problem 2.16.3(a).

## Next step

Pick the next unclaimed `feature` issue. The `finrank_g_*` file
(`Problem2_16_3.lean`) is now free of `sorry` in the part-(a) results.

## Blockers

None.
