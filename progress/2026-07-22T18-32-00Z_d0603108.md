## Accomplished

Stage 3.7 coverage-arm audit of the two §3.9 problems (issue #7361):

- **Problem 3.9.1** (extensions & `Ext¹`, parts a–d) and **Problem 3.9.4**
  (formal deformations, parts a–b): added `coverage` fields at sub-part
  granularity via `derived` arrays in `progress/items.json`.
- Verified both modules build sorry-free (`lake build`, 1959 jobs) and are
  axiom-clean (`#print axioms` on all 8 headline decls → only propext /
  Classical.choice / Quot.sound, no `sorryAx`).
- Applied the deliverable-2 spot check to the flagged weaker-looking parts:
  - **3.9.1(c)** is **forward-only** (`iso_of_sub_mem_coboundaries`); the book's
    converse (unitriangular iso ⇒ f−f'∈B¹) is not formalized and is not
    subsumed by the part-(d) theorem → marked `covered_partial`, follow-up
    `feature` issue **#7362** opened.
  - **3.9.1(d)** `[IsAlgClosed k]` is the mathematically correct hypothesis
    (Schur needs End_A=k for the ℙ Ext¹ parametrization), documented in-file →
    `covered_full`.
  - **3.9.4(b)** is an open-ended book question, faithfully *stated* as
    `ConverseHolds`/`Problem3_9_4b` but unresolved → `covered_partial`, no
    follow-up (the book asserts no claim).
- Both items now `covered_partial` at item level (3.9.1 because of (c); 3.9.4
  because of (b)). `items.json` parses; diff localized to the two items only.
- Wrote coverage report
  `progress/reviews/2026-07-22T18-30-00Z-ch3-problem3_9_1-3_9_4-extensions-deformations-coverage.md`.

## Current frontier

§3.9 extensions/deformations coverage arm complete. Stage 3.7 exercise-coverage
ratchet sweep continues elsewhere (siblings §5.8 #7342, §5.3.3 #7346 still
in-flight/unclaimed).

## Overall project progress

Stage 3.7 (exercise-coverage ratchet) ongoing across chapters. This turn moved
`Chapter3/Problem3.9.1` and `Chapter3/Problem3.9.4` from blanket `sorry_free`
to honest per-sub-part `coverage` records.

## Next step

Land follow-up **#7362** (3.9.1(c) converse) to lift 3.9.1 to `covered_full`.
The elementary converse is a short read-off of the same (1,2)-block identity
already proved in `iso_of_sub_mem_coboundaries`. Continue the ratchet on the
remaining unclaimed review issues (#7342, #7346).

## Blockers

None.
