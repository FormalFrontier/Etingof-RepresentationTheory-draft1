## Accomplished

Worked issue #6636 (feat Ch9 #Problem9.4.6-i-free). Built, sorry-free and compiling, the genuine
algebra isomorphism realizing the free algebra as a one-vertex path algebra, in
`EtingofRepresentationTheory/Chapter9/Problem9_4_6.lean`:

- `LoopVertex n` (one-point vertex type) with `Fintype`/`DecidableEq`/`Unique` instances and
  `loopQuiver : Quiver.{u+1} (LoopVertex n)` (hom-type `ULift.{u+1} (Fin n)`).
- `freeToPath : FreeAlgebra k (Fin n) →ₐ[k] PathAlgebra k (LoopVertex n)` (via `FreeAlgebra.lift`,
  sending `xᵢ` to the `i`-th loop).
- `pathToFree : PathAlgebra k (LoopVertex n) →ₐ[k] FreeAlgebra k (Fin n)` (via the path-algebra
  universal property `Etingof.Problem2_8_6.defining_relations_universal`).
- Both round-trip identities (`freeToPath_comp_pathToFree`, `pathToFree_comp_freeToPath`) and the
  assembled `freePathEquiv : FreeAlgebra k (Fin n) ≃ₐ[k] PathAlgebra k (LoopVertex n)`.

This is deliverable 1 of #6636, complete and correct.

## Current frontier

`homologicalDimension_freeAlgebra_eq_one` remains `sorry`. It cannot be finished with the stated
dependency #6635 (`homologicalDimension_congr`, same-universe only): `FreeAlgebra k (Fin n) : Type u`
but `PathAlgebra k (LoopVertex n) : Type (u+1)` (the quiver path type bumps the universe, and the
standard-resolution machinery hard-requires `Quiver.{u+1}`). The issue's "both Type u" premise is
mathematically wrong. Filling the theorem needs universe-lift invariance of `homologicalDimension`
(`homologicalDimension R = homologicalDimension (ULift.{u+1} R)`), which is genuinely non-trivial
(`ModuleCat.{u} R` and `ModuleCat.{u+1} R` are not equivalent). Created sub-issue #6644 for it.

## Overall project progress

Phase 3 formalization ongoing. Problem 9.4.6 part (i) path-algebra case is sorry-free on `main`;
the free-algebra corollary is now reduced to a single clean universe-invariance lemma (#6644) plus
a two-line assembly (spelled out in #6644's deliverable 2), with the algebra iso already built.

## Next step

Land #6644 (`homologicalDimension_ulift`), then the two-line assembly fills
`homologicalDimension_freeAlgebra_eq_one`. All the supporting infrastructure is already on the
partial PR branch.

## Blockers

`homologicalDimension_freeAlgebra_eq_one` blocked on #6644 (universe-lift invariance of
`homologicalDimension`). Not a proof-level sorry that can be papered over — it is a real missing
lemma.
