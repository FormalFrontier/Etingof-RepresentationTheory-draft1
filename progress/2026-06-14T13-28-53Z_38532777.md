## Accomplished
- Claimed #4576 (T(2,2,2) tube two-level collapse + orientation-generic leaf_equalities).
- Read the full mathematical structure: corrected Ẽ₆ homogeneous tube (sub-A #4574),
  canonical D̃₄ model `starTubeRepGen_isIndecomposable`, D̃₆ collapse precedent,
  reduction tooling (`forward_leaf_subspace_eq` / `reversed_leaf_subspace_eq`),
  and `progress/etilde6-tube-matrices.md` §3-4 (brick proof + reduction sketch).
- Built and committed (sorry-free) the **three-block center decomposition primitives**
  in `FieldGenericETilde6.lean`: `centerEmbed{0,1,2}_F`, `centerProj{0,1,2}_F`,
  `center3_decomp_F`, `center3_sum_zero_F`. These are the 3-block analogues of
  `center_decomp_F` / `embed_sum_zero_F` and are the foundation of the two-level
  collapse (pushing each leaf subspace up through its mid to the center).
- Scope assessment: the remaining collapse is too large for one session
  (brick argument forcing the three leaf spaces equal + 6 reversible edges).
  Decomposed into #4638 (canonical two-level collapse + brick + N-invariance) and
  #4639 (orientation-generic non-canonical edge tree, depends on #4638).

## Current frontier
`etilde6Rep_kQ_isIndecomposable` remains `sorry`. The center primitives are
landed; next is the canonical brick-collapse (#4638).

## Overall project progress
Stage 3 (formalization). Ẽ₆ tube redesign program (#4548/#4557): sub-A #4574
(construction) merged; sub-B #4576 now partially landed (center primitives) and
decomposed into #4638 / #4639; sub-C #4577 (final assembly) still blocked.

## Next step
Claim #4638: prove the canonical two-level collapse using `forward_leaf_subspace_eq`
(twice, leaf→mid→center) + the §3 brick argument + the eigenvalue-site N-invariance
step, modeled on `starTubeRepGen_isIndecomposable`.

## Blockers
None. #4638 unblocks once this partial PR's foundation is on main.
