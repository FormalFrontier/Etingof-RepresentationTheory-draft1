## Accomplished

One-shot session (`pod once 5100`) on issue **#5100**: filled the final `sorry` (the
extractor tail) in `clean_simple_constituent_formalCharacter_eq_schurPoly_mem`
(`Chapter5/CleanConstituentExtraction.lean`). The file is now **sorry-free**, axioms
`[propext, Classical.choice, Quot.sound]`, and **DetInvElim-clean** (57-module closure;
no `DetInvElim` / `PolynomialGLDecomposition` / `SchurWeylFormalCharacterIso`). Builds at
the default heartbeat budget.

Added four reusable helper lemmas (all clean, all in the same file):

- `schurModule_iSup_glWeightSpace_eq_top_clean` — clean replacement for the banned
  `glWeightSpace_schurModule_iSup_eq_top` (which lives in polluted
  `SchurWeylFormalCharacterIso`): transfers `glTensorRep`'s weight-spanning through the
  GL-equivariant `LinearMap.rangeRestrict (youngSymEndomorphism …)`. Proof body is a
  verbatim port of the polluted lemma's (itself clean) proof.
- `formalCharacter_eq_of_FDRep_iso` — isomorphic `FDRep`s have equal formal characters
  (via `FDRep.isoToLinearEquiv` + `FDRep.Iso.conj_ρ` + `formalCharacter_eq_of_rep_iso`).
  Supplies the pairwise-distinctness hypothesis of the torus-trace engine by
  contraposition (distinct characters ⟹ non-isomorphic).
- `coeff_zero_of_char_combination_zero` — combined torus-trace engine wrapper: chains
  `trace_combination_eq_zero_of_formalCharacter_combination_eq_zero` into
  `formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero_general`.
- `net_coeff_zero_of_char_combination_zero` — dedup-by-character: for a finite family of
  simple/algebraic/spanning reps with a vanishing character combination, the net
  coefficient at every character value is 0 (deduplicate into pairwise non-isomorphic
  representatives, regroup the sum fiberwise, apply the engine; the empty-fiber case is
  trivial).

Main-theorem proof route: image subrep `σL ≅ L` (algebraic + weight-spanning via the
`subFDRep_*` glue and `IsAlgebraicRepresentation.of_linearEquiv` /
`glWeightSpace_iSup_eq_top_of_equivariant_surjective`); SES split
`char M = char L + ∑_j char (W j)` via `formalCharacter_eq_sub_add_quotient` +
`formalCharacter_eq_sum_simple_factors`; assemble the raw family
`{L} ∪ {W_j} ∪ {SchurModule ν}_{ν∈S}` with coefficients `(+1, +1, -c_ν)` whose character
combination is `char M − char M = 0`; apply `net_coeff_zero_…` at `w = char L`; the net
coefficient there is `1 + (matching W's) − ∑_{ν : S_ν = char L} c_ν = 0`, and a
nonnegativity + `Finset.single_le_sum` argument (under the contradiction hypothesis the
negative terms all vanish) forces `∃ ν ∈ S, 0 < c_ν ∧ char L = schurPoly N ν.val`.

### Key implementation note (for future agents)

The dedup machinery hit a hard `(deterministic) timeout at whnf` (did **not** clear even
at `maxHeartbeats 6400000`) when written inline against the concrete `Sum.elim` family —
a genuine defeq loop, not a budget shortfall. Fix: extract the dedup into
`net_coeff_zero_of_char_combination_zero` over an **abstract** `R : ι → FDRep`, so its body
type-checks without ever reducing the caller's concrete `Sum.elim` carriers. After that the
main theorem compiles at the **default** budget. Generic-`R` extraction is the lever for
`whnf`-loop blowups caused by concrete representation families.

## Current frontier

#5100 deliverable complete. The clean extractor
`clean_simple_constituent_formalCharacter_eq_schurPoly_mem` is sorry-free, unblocking the
downstream final-assembly consumer **#5076**.

## Overall project progress

Etingof draft 1, Stage 3, Chapter 5. The #5082 DetInvElim-clean constituent-character
extraction chain is now fully closed (infrastructure landed in #5101/#5095/#5096; this
session closes the extractor tail). `CleanConstituentExtraction.lean` is sorry-free and
already wired into the `Chapter5.lean` aggregator.

## Next step

- Worker: pick up **#5076** (final assembly), which consumes the now-complete clean
  extractor.

## Blockers

None.
