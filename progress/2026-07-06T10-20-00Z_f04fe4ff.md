## Accomplished

One-shot dispatch on issue #5936 (statement pass for the two Chapter 2 exercises
deferred from #5927). Both blobs were verified against `source/original.pdf`:

- **Exercise 2.9.5** (`Chapter2/Exercise2_9_5.lean`) — the exercise's "example (1)
  is a special case of example (5) (for n = 3)" refers to the **concrete**
  Lie-algebra list (`Discussion_concrete_Lie_examples{,_continued}.md`), not the
  abstract Example 2.9.2. Concrete example (1) = ℝ³ with cross product, example (5)
  = so(n) skew-symmetric matrices; at n = 3 this is so(3) ≅ (ℝ³, ×). Re-enabled
  `Cross.lieRing` as a local instance, constructed the matching `LieAlgebra ℝ`
  (via bilinearity of `crossProduct`), proved the concrete facts (`bracket_eq_cross`,
  `mem_so3`), and stated the isomorphism as
  `Nonempty (Orthogonal.so (Fin 3) ℝ ≃ₗ⁅ℝ⁆ (Fin 3 → ℝ))`. The explicit hat-map
  `LieEquiv` construction is `sorry` (statement pass). → `statement_formalized`.

- **Exercise 2.11.7** (`Chapter2/Exercise2_11_7.lean`) — the blob was mis-extracted
  (it held the trailing clause of Problem 2.11.6(b), the tensor-hom adjunction).
  The **real** exercise, transcribed from p.35, is the elementary "M ⊗_A N is an
  A-module over a commutative ring A". This is a direct Mathlib match
  (`TensorProduct.instModule`), so the file is **sorry-free** (action lemmas
  `a•(m⊗ₜn) = (a•m)⊗ₜn = m⊗ₜ(a•n)` proved too). Corrected the corrupted blob and
  set items.json → `sorry_free`. This closes the "either build the bimodule
  tensor-hom API or record as audit conclusion" dilemma raised in #5936: neither is
  needed — the real 2.11.7 has no relation to Problem 2.11.6.

Both files build standalone with only the one expected sorry (2.9.5); the full
`EtingofRepresentationTheory.Chapter2` aggregator builds successfully (8713 jobs).

## Current frontier

Chapter 2 exercise backlog. #5936's two deferred exercises are now handled.

## Overall project progress

Phase 3 statement-pass / cleanup for the Chapter 2 exercise backlog. This closes
the last two items deferred from the #5927 batch.

## Next step

Optional follow-up: discharge the single `sorry` in `Exercise2_9_5.lean` by
constructing the explicit hat-map `LieEquiv` `(a,b,c) ↦ ![![0,-c,b],[c,0,-a],[-b,a,0]]`
and proving it is bracket-preserving (its matrix action on v is `(a,b,c) ⨯₃ v`).
Not blocking; could be a small dedicated proof issue.

## Blockers

None.
