## Accomplished

Session type: `/work` → claimed `feature` issue #6581 (Ch9 Problem 9.5.3: blocks ≃
indecomposable central idempotents + Hom-vanishing).

- Merged two ready PRs at the start of the cycle: #6586 (Ch3 Clifford semisimple) and #6584
  (Ch3 Clifford radical quotient), both green.
- Investigated #6581. Found **part (ii)** (`hom_subsingleton_of_not_linked`) is already
  **proven sorry-free** on `main` (a prior PR landed it). The file builds with the only
  remaining `sorry` at line 56.
- Found **part (i)** (`blocks_equiv_indecomposableCentralIdempotents`) is **false as stated**:
  it is quantified over a general `[Ring R]` with only `[Small.{v} R]`, but for `R = ℤ` there
  are infinitely many blocks (ℤ/p pairwise unlinked, `Ext¹_ℤ(ℤ/p,ℤ/q)=0` for p≠q) versus a
  single indecomposable central idempotent (`1`). So `Nonempty (Block ℤ ≃ {e // …})` is
  `Nonempty (infinite ≃ singleton) = False`. It needs a finiteness hypothesis (Artinian /
  finite-dimensional algebra).
- Posted the counterexample analysis as a comment on #6581.
- Filed corrected follow-up **#6590** with the finite-dimensional-algebra statement and an API
  breakdown (central character of a simple → linked simples share character → primitive central
  idempotent decomposition).
- Left `Decomposed into #6590` breadcrumb and `coordination skip`ped #6581 (→ `replan`).

No Lean code changed this session (part ii already landed; part i unprovable as written).

## Current frontier

Part (i) of Problem 9.5.3 is re-scoped in #6590 and unclaimed. The block/idempotent
correspondence needs new API: central character of a simple module w.r.t. a central idempotent,
invariance under linking, and (with finiteness) the primitive central idempotent decomposition.

## Overall project progress

Stage 3 (formalization) ongoing across chapters. Ch9 Problem 9.5.3 part (ii) done; part (i)
awaiting a corrected-statement attempt (#6590). Two Ch3 Clifford PRs merged to `main` this
cycle.

## Next step

A worker should claim #6590. Start with the general-ring, true content: the central-character
map `Block A → (central idempotents → Bool)` and "linked simples share central character" (uses
`Etingof.ext_subsingleton_of_not_areLinked` and nonsplit-extension reasoning), then add the
finiteness half. Decompose further if the full bijection overflows a session.

## Blockers

None. #6581 routed to `replan` (planner will close it pointing at #6590); part (ii) already on
`main`.
