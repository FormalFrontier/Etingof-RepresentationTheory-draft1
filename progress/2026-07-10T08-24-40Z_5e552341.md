## Accomplished

Review session for issue #6102 — vacuousness + statement-faithfulness audit of the
two Chapter 4 counting/character files that feed the Exercise 4.2.3 assembly (#6048)
and Problem 4.12.2(d) (#6094):

- `EtingofRepresentationTheory/Chapter4/Exercise4_2_3.lean` (`Bridge` section, D1/D2)
- `EtingofRepresentationTheory/Chapter4/Problem4_12_2.lean` (part (c))

**Verdict: CLEAN — no defects found.** No follow-up fix issue opened.

### 1. Vacuousness / definition audit — PASS

- Every `def`/`noncomputable def`/`instance`/`abbrev` in both files has a genuine
  (non-`sorry`) body: `IrrepClasses`, `groupSum`, `simpleProp`, `subInclusion`,
  `repSimpleClassesEquivModuleSimpleClasses`, `fdRepSimpleToRepSimple`,
  `SimpleModuleClasses`, `irrepClassesEquivRepSimpleClasses`, and the
  `IsClosedUnderIsomorphisms`/`EssSurj`/`IsEquivalence` instances (Exercise4_2_3);
  `Heisenberg` + its group instances, `rhoLin`, `rhoHom`, `abHom`, `equivProd`
  (Problem4_12_2).
- `grep` confirms the only two `sorry` proofs in the two files are the **out-of-scope**
  top-level assembly theorems: `Exercise4_2_3` (line 424 → #6048) and
  `irreducible_dim` part (d) (line 707 → #6094). Both are theorem-level, not `def`
  bodies.
- `#print axioms` on all 13 reviewed theorems (captured in `/tmp/axcheck-6102.log`)
  reports only `propext, Classical.choice, Quot.sound` — **no `sorryAx`**. Because
  `#print axioms` is transitive, this proves every transitive dependency of the
  reviewed items (including `abHom`, the DFT-character helpers, `SimpleModuleClasses`,
  `groupSum`, and the `isoClassesEquivOfEquivalence`/`congrFullSubcategory` infra) is
  sorry-free. `abHom_surjective` needs only `propext, Quot.sound`.

### 2. Universe-pinning check (D2) — PASS

`card_irrepClasses_eq_card_simpleModuleClasses` states
`Nat.card (IrrepClasses k G) = Nat.card (SimpleModuleClasses.{u} (MonoidAlgebra k G))`
with `k : Type u`, `G : Type v`. It is proved by `Nat.card_congr` of a genuine `Equiv`
starting from `IrrepClasses k G` and composing `irrepClassesEquivRepSimpleClasses` with
`isoClassesEquivOfEquivalence (…Rep.equivalenceModuleMonoidAlgebra.{u}…)`. The
`SimpleModuleClasses.{u}` carrier universe and the `Rep`/`FDRep k G` carrier universe
are both `u` — no mismatch. Since the two `Nat.card`s are provably equinumerous via a
real `Equiv` from the genuine object `IrrepClasses`, D2 counts the correct set; any
downstream consumer (#6048) that further relates `SimpleModuleClasses.{u}` to the
centre dimension is relating the right count.

### 3. Statement-faithfulness audit — PASS

Cross-checked against `blobs/Chapter4/Exercise4.2.3.md` and
`blobs/Chapter4/Problem4.12.2.md`:

- `Exercise4_2_3`: `(Fintype.card G : k) = 0 → Nat.card (IrrepClasses k G) < Nat.card
  (ConjClasses G)` — faithful to "if |G| = 0 in k, #irreducibles < #conjugacy classes".
- `one_dim_reps_card`: `Nat.card (Heisenberg p →* ℂˣ) = p^2` — one-dim reps are exactly
  characters `G → ℂˣ`; count `p²` via `abHom` (abelianization `(ZMod p)²`). `abHom` is a
  genuine `MonoidHom` with `abHom_surjective` and `abHom_ker_le_ker` establishing the
  character correspondence. Uses `Nat.card` (genuine finite count). Faithful.
- `R1_decomposes`: for any `ρ` with the `z = 1` generator actions (`hx`: shift,
  `hy`: identity — satisfiable by `rhoHom 1`, so not vacuous), produces `S : Fin p →
  Submodule` that is `G`-invariant, each `finrank = 1`, `DirectSum.IsInternal S` (so
  `R_1 = ⊕ S i`, `p` summands), plus characters `χ : Fin p → (Heisenberg p →* ℂˣ)` with
  `Function.Injective χ` and each `S i` the `χ i`-isotypic line. The `Injective χ`
  clause faithfully encodes "each 1-dim rep occurs **exactly once**" (multiplicity-free);
  strengthened in commit 55c5a223.
- `card_eq`: `Fintype.card (Heisenberg p) = p^3` — matches `|G| = p³`.

### Minor (non-blocking, not defects)

Pre-existing `lake build` style-linter warnings in `Problem4_12_2.lean`, left untouched
to keep the review non-mutating and conflict-free with the pending part-(d) work (#6094):
- lines 466:11 / 466:13 — unused binder names `x`, `y` in `abHom`'s `map_mul' x y := rfl`
  (could be `_ _`).
- line 560:4 — `show` used to change the goal; linter suggests `change`.

## Current frontier

Review #6102 complete. Both files build sorry-free (modulo the two expected
out-of-scope assembly sorries) with clean axiom profiles.

## Overall project progress

Stage 3 (formalization) ongoing. This turn was a review-only audit — no mathematical
statements changed. The audited Exercise 4.2.3 `Bridge` (D1/D2) and Problem 4.12.2(c)
results are confirmed genuine (non-vacuous) and faithful to the book, clearing them as
solid foundations for the downstream assemblies #6048 and #6094.

## Next step

Pick up the next unclaimed feature issue (e.g. #6098 Ch4 field-general count bound —
flagged as possibly needing decomposition — #6090 finiteness of IrrepClasses, or #6057
Ch3 descent).

## Blockers

None.
