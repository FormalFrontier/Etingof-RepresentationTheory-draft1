## Accomplished

Issue #2991: refreshed the TODO comment block at lines 1267-1279 of
`Chapter6/FieldGenericTpqr.lean` (body of
`single_branch_leaf_case_both_extend_per_kQ`, residual `sorry`).

Concrete changes:
* Replaced the generic "TODO (follow-up issue)" header with
  "TODO (parent assembly issue #2905)" so the four-way decomposition
  surfaces from one pointer.
* Split the previous 3-bullet enumeration into the planned 4-bullet
  structure, citing each sub-issue inline:
  * Bullet 1 — both arms ≥ 3 (Ẽ₇ embed) → sub-issue #2907.
  * Bullet 2 — `b₃` leaf, q ≥ 3 (T(1, q, 2)) → sub-issue #2908.
  * Bullet 3 — `b₂` leaf, r ≥ 3 (T(1, 2, r), symmetric) → sub-issue
    #2909.
  * Bullet 4 — ADE shapes T(1, 2, 2/3/4) (posdef contradiction) →
    sub-issue #2910, noting the case landed via PR #2912 covering
    T(1, 2, 2), T(1, 2, 3), and T(1, 2, 4).
* Left lines 1219-1232 (per-theorem docstring), the discard block
  (now 1285-1290), and the `sorry` (now 1291) untouched, per the
  issue's scope guard.

Build verification: `lake build
EtingofRepresentationTheory.Chapter6.FieldGenericTpqr` passes with the
expected single residual-`sorry` warning at the
`single_branch_leaf_case_both_extend_per_kQ` declaration. No other
diagnostics. Comment-only edit; line numbers downstream of the block
shift by +5.

## Current frontier

Wave-63 cleanup chores: the parallel issue #2990 (residual-sorry
comment blocks in `FieldGenericNonAdjacentBranches.lean`) is claimed by
another agent (race lost on my first claim attempt). The two
sub-issues for `single_branch_leaf_case_both_extend_per_kQ` open work
(#2907, #2908, #2909) remain claimed/has-pr / replan; assembly issue
#2905 still `replan`.

## Overall project progress

Stage 3 formalization; Chapter 6 finite-representation classification.
This is a navigational chore — does not change sorry count or unblock
downstream work, but makes the residual-`sorry` site at line 1291
self-navigable from one pointer (#2905) to four sub-issues. Mirrors
the precedent set by PR #2983 / issue #2982 (docstring refresh on the
sister file).

## Next step

Once #2907/#2908/#2909 land and the parent #2905 is dispatched, the
worker assembling the per-(F, Q) "both arms extend" body can read the
refreshed comment block to see exactly which sub-issue covered each
case-split branch.

## Blockers

None. PR opened with auto-merge; CI runs.
