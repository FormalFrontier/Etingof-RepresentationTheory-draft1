## Accomplished
Corrected stale docstrings in `Chapter5/PolynomialRepEmbedding.lean` (issue #7062):
- Rewrote the module `## Status` block: the file is sorry-free and proves both
  the injection and GL_N-equivariance of the embedding. Named
  `polynomialRep_embeds_in_tensorPower(_inj/')` and the FDRep-facing
  `polynomial_homog_rep_equivariant_embedding` (which needs only `h_span`,
  `h_homog` — no leaked equivariance/multiplicativity hypothesis). Removed the
  false claim that `PolynomialTensorBridge` "explicitly defers" its
  equivariance (proved in #7050 as `homogeneousPolyToTensor_equivariant`).
- Extended `## Main results` to name the equivariant theorems.
- Fixed the parallel stale sentence (~line 199) on
  `polynomialRep_embeds_in_tensorPower_inj`: equivariance is not deferred to a
  sibling issue; it is proved separately in this file.

## Current frontier
Docstring-fidelity edit complete and building. Comment/docstring-only diff.

## Overall project progress
Stage 3 formalization; ongoing docstring-fidelity cleanups across chapters.
This item is one of the #705x/#706x sibling fidelity fixes.

## Next step
No follow-up needed for this item. Optionally, the docstring on
`polynomialRep_embeds_in_tensorPower` (~line 472) still says the `hP_mul`
derivation is "deferred to a follow-up"; `hP_mul_of_hP` now derives it, so a
future fidelity pass could correct that clause too (left untouched here to keep
this edit within the issue's stated scope).

## Blockers
None.
