## Accomplished

One-shot dispatch (`pod once 5320`, session `0420dc49`) on **#5320 — discharge the 4
`clength` API sorries in `Chapter9/KrullSchmidt/Length.lean`**. Claimed #5320 with the
`feature` label. Re-verified state independently (4th confirmation) and re-checked the
Mathlib gap against the *current* pin (`v4.32.0-rc1`), which prior assessments predate.

Findings:

- **2 of the original 4 sorries are already discharged on `main`** (via merged PR #5353):
  `clength_pos_of_not_isZero` (reduces to `clength_eq_zero_iff`) and `clength_biprod`
  (reduces to `clength_additive` via the split SES). `Length.lean` is byte-identical to
  merged `main`; exactly **2 code sorries** remain — line 177 `clength_eq_zero_iff`,
  line 226 `clength_additive`.

- **Both remaining statements are *false as stated*, not merely unproven.** They carry
  only `[IsFiniteAbelianCategory C]`, which (`Definition9_6_1.lean`) is
  `Abelian + EnoughProjectives + Fintype-many simples` and does **not** imply finite
  length. Counterexample: all modules over `k[x]/(x²)` — abelian, enough projectives
  (frees), one simple (`k`), yet has infinite-length objects, for which
  `clength = (Order.height ⊤).toNat = 0` while the object is nonzero. This falsifies the
  `→` of `clength_eq_zero_iff` and the mixed case of `clength_additive`.

- **Discharging requires a planner-level definition-fidelity decision**, which a worker
  cannot make: either add the §9.6 finite-length standing assumption to
  `IsFiniteAbelianCategory` itself (modifies `Definition9_6_1.lean`; Etingof's Def 9.6.1
  *proper* is just "enough projectives + finitely many simples", so this is a fidelity
  call), or thread `HasFiniteLength` / `IsFiniteAbelianCategoryOverField` through the API
  (changes signatures and breaks the file's own downstream chain-condition lemmas
  `clength_strictMono`, `wellFoundedLT_subobject`, the Fitting inputs). Either way the
  statement signature changes, so this is not a no-new-sorry / no-weakening worker edit.

- **The categorical Jordan–Hölder infrastructure is genuinely absent from Mathlib
  v4.32.0-rc1** (verified by fresh source search, not memory):
  - no `IsModularLattice (Subobject X)` instance;
  - no `JordanHolderLattice (Subobject X)` instance (only `Submodule R M` has one, in
    `Mathlib/RingTheory/SimpleModule/Basic.lean`);
  - no categorical second isomorphism theorem for subobjects (the `second_iso` field of
    `JordanHolderLattice` exists in `Mathlib/Order/JordanHolder.lean` but is only
    instantiated for `Submodule`);
  - **no Subobject ↔ Pseudoelement membership API** — `Mathlib/CategoryTheory/Subobject/Basic.lean`
    (lines 63-71) explicitly says this "agrees ... but we haven't developed this in mathlib yet";
  - no product-order height additivity `height (a,b) = height a + height b`;
  - the categorical Jordan–Hölder theorem (Stacks 0FCK) is still flagged as **future work**
    in `Mathlib/CategoryTheory/Noetherian.lean`. (Mathlib does now have
    `IsNoetherianObject`/`IsArtinianObject` scaffolding in
    `CategoryTheory/Subobject/{Noetherian,Artinian}Object.lean`, but no
    `FiniteDimensionalOrder (Subobject X)` bridge.)

- **The issue's own SUCCESS criterion is already met on `main`**: "land the base lemmas +
  biprod first and decompose the additive proof (record the route) — that is a SUCCESS."
  Base lemmas (`clength_eq_zero_of_isZero`, `clength_simple`,
  `clength_eq_zero_iff_of_finiteDimensionalOrder`, `height_top_lt_top`) + `clength_biprod`
  are landed; the route is recorded in the docstrings; the residual is decomposed into
  the two open sub-issues.

No worker code change is appropriate: discharging the 2 sorries as stated would require a
forbidden `sorry`, a forbidden statement weakening, or a planner-level definition decision
plus categorical-JH infrastructure that Mathlib does not yet have.

## Current frontier

`clength_eq_zero_iff` (line 177) and `clength_additive` (line 226) in
`EtingofRepresentationTheory/Chapter9/KrullSchmidt/Length.lean`. The honest math of the
`→` direction is already complete and unconditional in
`clength_eq_zero_iff_of_finiteDimensionalOrder`; only the finite-length *wiring* (#5324)
and the categorical-JH *additivity* (#5325) remain.

## Overall project progress

Phase 3 formalization, Chapter 9 Krull–Schmidt, link 1/5 (`clength` API). 2 of 4 original
sorries discharged on `main`; the residual 2 are tracked by open sub-issues #5324
(finite-length wiring + definition-fidelity decision) and #5325 (categorical Jordan–Hölder
additivity; `blocked`).

## Next step

A **planner** should **close #5320 as superseded** by #5324 + #5325 (the residual scope is
exactly those two, both open with verified routes). This is now the 4th independent
confirmation; #5320 cannot be advanced by a worker. The genuinely actionable next move is
on **#5324**: make the definition-fidelity call (add finite-length to
`IsFiniteAbelianCategory`, or thread `IsFiniteAbelianCategoryOverField`), then prove
`HasFiniteLength X → FiniteDimensionalOrder (Subobject X)` — whose extension step is the
same height-bound that #5325's Schreier reflecting lemma provides. #5325 is the real
multi-session math and needs the categorical second isomorphism theorem / Subobject↔
Pseudoelement bridge built from scratch (Mathlib future work).

## Blockers

- #5320 is not dischargeable by a worker: the 2 remaining statements are false under the
  current `IsFiniteAbelianCategory` class (definition-fidelity decision required), and the
  categorical Jordan–Hölder infrastructure is absent from Mathlib v4.32.0-rc1.
- Routed to planner via `coordination skip` with a `Decomposed into #5324, #5325`
  breadcrumb recommending closure.
