## Accomplished

Closed **#4568** (feat(Ch6 #4558 sub-A): T(1,3,3) regular-simple tube —
construct `etilde7Rep_kQ` as a homogeneous tube, def + dimVec).

Replaced the refuted single-nilpotent-twist `etilde7Rep_kQ`
(`FieldGenericETilde7.lean`) — decomposable for every `m ≥ 1` (audit #4542)
— with the genuine **homogeneous tube** `R_λ^{(m+1)}` of the regular simple
`S_λ` at `δ = (4;2;3,2,1;3,2,1)`:

- **Arm 2** enters the center `F^{4(m+1)}` as a coordinate **prefix** flag
  (`starEmbed1_F`, `prefixBlockEmbed_F`).
- **Arm 3** as the opposite coordinate **suffix** flag (new
  `suffixBlockEmbed_F` / `suffixBlockProj_F`, plus `starEmbed2_F` /
  `starSecond_F`).
- **Arm 1** as the eigenvalue 2-plane carrying `Λ = λ·id + J` (new
  `etilde7Arm1Tube_F`, assembled from new `blockEmbedAt_F`), the single
  λ-bearing map.

The eigenvalue is `etilde7TubeLam F`, a generic element of the infinite field
`F` (new `exists_lam_distinct_powers` proves `1,λ,λ²,λ³` pairwise distinct via
a non-root of `∏_{i<j}(X^j − X^i)`; `etilde7TubeLam_distinct` exposes the brick
hypothesis for sub-C).

`etilde7Rep_kQ_dimVec` is unchanged (still `LinearEquiv.refl`); all `def`s are
sorry-free; `etilde7Rep_kQ_isIndecomposable` remains a single `sorry` — now of
a **true** statement (the soundness win). Recorded the explicit `S_λ` matrices
and paper brick-proof (`End(S_λ) = F`) plus the §1-peeling-defeat argument in
`progress/etilde7-tube-design.md` §7.

## Current frontier

`FieldGenericETilde7.lean` builds; the three downstream consumers
(`FieldGenericTpqr`, `FieldGenericNonAdjacentBranches`, `FieldGenericD7Tilde`)
all build with statements unchanged.

## Overall project progress

Stage 3 (formalization) ongoing. This lands the corrected sporadic-tube
construction for Ẽ₇ = T(1,3,3) (#4558 sub-A). Sibling sporadic sub-A issues
still open: #4574 (Ẽ₆ = T(2,2,2)), #4559 (T(1,2,5)).

## Next step

- **#4569** (sub-B): orientation-generic leaf-equality case tree for
  `etilde7Rep_kQ` (now unblocked against the corrected shape).
- **#4570** (sub-C, blocked on #4568+#4569): assemble
  `etilde7Rep_kQ_isIndecomposable` via the `core`/`propagate` collapse +
  `eigenvalue_jordan_invariant_compl_trivial_gen`, modelled on
  `starTubeRepGen_isIndecomposable`. Uses `etilde7TubeLam_distinct`.

## Blockers

None.
