## Accomplished

One-shot dispatch on **#5608** (Realization core det-shift packaging half, step 6 of
issue #5602). Discharged the lone `sorry` in
`exists_dominantWeight_algIrrepGL_iso_detTwistNeg_schurModule`
(`Chapter5/Theorem5_23_2_PeterWeyl.lean`), so the file is sorry-free again.

The theorem says: a `det^{-r}`-twisted Schur module
`charTwistRep (det^{-r}) (schurModuleRep k n ν)` (antitone `ν`) is `GL_n`-equivariantly
isomorphic to the irreducible `algIrrepGLRepρ n lam k` for a concrete dominant weight
`lam`.

**Construction.** Set `lam.val := fun i => (ν i : ℤ) − r` (antitone since `ν` is) and
prove a parameterized core `aux_detShift_packaging` that works uniformly in `n`:
- `lam.shift ≤ r` (from `ν(last) ≥ 0`; the `r < ν(last)` boundary gives `lam.shift = 0`),
- with `c := r − lam.shift` one has `ν i = lam.toNatWeight i + c` for all `i`
  (over ℤ: `lam.toNatWeight i = ν i − r + lam.shift`, the truncated `toNat` exact via
  `Int.toNat_of_nonneg`). So `ν` is the `c`-fold determinant shift of `lam.toNatWeight`.

The iso is then assembled from the **existing** `charTwist_detPow_iso_schurShift`
(`Chapter5/SchurModuleContragredientHalf.lean`, the iterated Proposition 5.22.2): it
gives `det^c ⊗ L_{lam.toNatWeight} ≅ L_{lam.toNatWeight + c} = L_ν`. Twisting that
intertwiner once more by `det^{-r}` (via `Intertwines.charTwist`) and collapsing the
stacked twists `det^{-r} · det^c = det^{-lam.shift}` (via `charTwistRep_charTwistRep`)
matches the `det^{-lam.shift}`-twist that *defines* `algIrrepGLRepρ n lam k`.

This mirrors the bookkeeping in `ContragredientIdentity.lean` /
`SchurModuleContragredientHalf.lean` (the issue's cited model), reusing the same
combinators (`Intertwines.charTwist`, `charTwist_detPow_iso_schurShift`,
`charTwistRep_charTwistRep`) rather than re-deriving them.

**Verification.** `lake build EtingofRepresentationTheory.Chapter5.Theorem5_23_2_PeterWeyl`
green (8688 jobs). `#print axioms exists_dominantWeight_algIrrepGL_iso_detTwistNeg_schurModule`
reports only `[propext, Classical.choice, Quot.sound]` — no `sorryAx`.

Added one explicit import (`SchurModuleContragredientHalf`, already transitively reachable,
no new cycle).

## Current frontier

With this half landed, `exists_dominantWeight_equivariant_realization` (the realization
core assembly) consumes both halves of #5602 sorry-free. The remaining open obligation in
the Cauchy/Peter-Weyl spanning argument is the wiring above it
(`simpleSubrep_localRightRep_le_iSup_range` and downstream
`peterWeylMap_surjective`/`Theorem5_23_2_ii_equivariant`).

## Overall project progress

Stage 3 ongoing. The det-shift packaging half (#5608) is closed; the realization core's
two-half decomposition (#5602) is now fully discharged.

## Next step

Continue assembling the Peter-Weyl spanning argument above the realization core
(`simpleSubrep_localRightRep_le_iSup_range` etc.).

## Blockers

None.
