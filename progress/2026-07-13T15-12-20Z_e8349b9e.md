## Accomplished

Feature session on issue #6500 (Ch5 Problem 5.24.2 step 4: tensor-trace ↔ trace-word / cycle-trace
identity). Created new file `EtingofRepresentationTheory/Chapter5/PermutationTraceWord.lean`:

- **`permTensorOp`** — the permuted tensor operator `symGroupAction σ ∘ₗ PiTensorProduct.map A`.
- **`permTensorOp_trace_eq_matrixSum`** (fully proved) — the tensor-side reduction: the trace over
  tensor factors equals `∑_{p : Fin n → ι} ∏_i (A i)_{p(σ i), p i}`, the permutation operator's
  diagonal in the `Basis.piTensorProduct` basis. Uses `trace_eq_matrix_trace`,
  `reindex_tprod`, `map_tprod`, `piTensorProduct_repr_tprod_apply`, and an `Equiv.prod_comp σ`
  reindexing.
- Real `def`s (no sorried bodies): `cyclePeriod` (orbit length via `Function.minimalPeriod`),
  `orbitReps` (≤-minimal element per orbit via `SameCycle`), `matrixCycleProd` and
  `cycleOperator` (ordered products around a cycle, matrix / End versions).
- **`trace_cycleOperator`** (fully proved) — `LinearMap.trace` of the ordered operator product =
  `Matrix.trace` of the ordered matrix product (via `toMatrixAlgEquiv` + `map_list_prod`).
- **`permTensorOp_trace_eq_prod_cycle`** — the main identity, assembled and proved **modulo one
  lemma**: `trace (permTensorOp σ A) = ∏ over σ-orbits of trace of the ordered operator product`.

The single remaining `sorry` is the combinatorial core `matrixSum_eq_prod_orbit` (the multi-index
sum factors over σ-orbits). It is clearly named, documented with a two-step proof roadmap
(single-orbit telescoping + `Fintype.prod_sum` orbit-partition assembly), and filed as follow-up
issue #6506.

## Current frontier

`matrixSum_eq_prod_orbit` in PermutationTraceWord.lean is the lone `sorry`. Everything else in the
file (and the whole project) compiles: `lake build` succeeds with one `declaration uses sorry`
warning.

## Overall project progress

Stage 3 (formalization) ongoing. This adds the self-contained step-4 machinery for Problem 5.24.2's
First Fundamental Theorem; sibling #6499 (coordinate ↔ tensor bridge) will consume
`permTensorOp_trace_eq_prod_cycle` once #6506 discharges the core.

## Next step

Claim #6506 and prove `matrixSum_eq_prod_orbit` following the docstring roadmap. Good warm-ups:
`orbitReps (1:Perm) = univ` + `matrixCycleProd 1 M i = M i` give the σ=1 case from
`Fintype.prod_sum`; the single full-cycle case is the pure telescoping.

## Blockers

None. The partial PR is independently useful (fully-proved tensor reduction + assembled main
theorem); the core is isolated and tracked in #6506.
