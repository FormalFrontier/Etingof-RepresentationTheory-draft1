## Accomplished

Claimed #4569 (Ẽ₇ = T(1,3,3) orientation-generic leaf-equality work) and
landed the **orientation-independent flag-collapse foundation (sub-B1)** in
`EtingofRepresentationTheory/Chapter6/FieldGenericETilde7.lean`. This is the
exact set of helpers that the orientation case-tree assembly #4642 (sub-B2)
enumerates as its dependency. All 13 new declarations are sorry-free; the
file's sorry count is unchanged at 6 (all pre-existing).

New declarations (Section 2, the 4-block analogue of the Ẽ₆ Section 0c/0d
infrastructure in `FieldGenericETilde6.lean`):

- `blockProjAt_F` — single-block projection `F^{4(m+1)} → F^{m+1}` at offset
  `o` (the projection counterpart of the existing `blockEmbedAt_F`).
- `blockProjAt_comp_embedAt_F` — left-inverse identity `blockProjAt o ∘
  blockEmbedAt o = id`.
- `blockProjAt_embedAt_disjoint_F` — off-diagonal vanishing
  `blockProjAt o ∘ blockEmbedAt o' = 0` for disjoint blocks.
- `etilde7_center_decomp_F` — 4-block center decomposition.
- `etilde7_center_sum_zero_F` — 4-block disjointness (proved cleanly via the
  block projections).
- `etilde7PrefixArmComp_F` / `etilde7SuffixArmComp_F` — the canonical arm-2
  prefix (`4→3→2→0`) and arm-3 suffix (`7→6→5→0`) leaf→center composites.
- `etilde7_prefixArm_comp_F` / `etilde7_suffixArm_comp_F` — the deposit
  identities: the prefix composite equals `blockEmbedAt 0` (leaf-4 → center
  block 0), the suffix composite equals `blockEmbedAt 3(m+1)` (leaf-7 →
  center block 3). This confirms the design §7.1 / §7.3 flag geometry.
- `etilde7PrefixArmComp_F_injective` / `etilde7SuffixArmComp_F_injective`.
- `etilde7_prefixArm_collapse_F` / `etilde7_suffixArm_collapse_F` — the
  three-level membership criteria `x ∈ W₁(leaf) ↔ deposit ∈ W₁(center)` via
  the generic `leaf_center_mem_iff_of_forward`.

## Current frontier

The orientation-independent foundation is complete and merged-ready. The
remaining work — the orientation case tree
`etilde7tildeRep_kQ_leaf_equalities` itself — is #4642 (sub-B2), which
consumes exactly the helpers above and was blocked on this foundation.

Note on the math: unlike the D̃-family `leaf_equalities` (uniform-dimension
spine, γ-routing, no eigenvalue input), the Ẽ₇ tube has non-constant
dimensions; the two flag arms deposit into center blocks 0 and 3 and couple
only through the arm-1 eigenvalue tube `etilde7Arm1Tube_F`. The arm-1
eigenvalue site is therefore handled in the case-tree assembly (#4642), not
here.

## Overall project progress

Stage 3 (formalization) ongoing. Ch6 Gabriel's-theorem sporadic-tube
redesign: Ẽ₇ sub-A (construction) landed previously; this turn lands the
Ẽ₇ sub-B1 flag-collapse foundation. `etilde7Rep_kQ_isIndecomposable`
remains a single `sorry` of a true statement (sub-C, #4570).

## Next step

A planner should triage this `replan` on #4569: the sub-B1 foundation is
landed, so either close #4569 (the case-tree theorem is fully covered by
#4642) or narrow #4569 to the residual case-tree theorem. Either way #4642
is now unblockable — a worker can build `etilde7tildeRep_kQ_leaf_equalities`
on top of the new helpers, proving the canonical (all-toward-center) branch
inline (prefix/suffix via the new collapse criteria, arm-1 via
`etilde7Arm1Tube_F` feeding `eigenvalue_jordan_invariant_compl_trivial_gen`)
and sorry-deferring non-canonical branches as the d-family precedents do.

## Blockers

None for the foundation. The full case-tree theorem (#4642) is the natural
follow-up.
