## Accomplished

- Added `[IsAlgClosed k]` hypothesis to `MoritaStructural` and downstream theorems (`Corollary_9_7_3_i_unique`, `Corollary_9_7_3_ii`)
- Added `import Mathlib.FieldTheory.IsAlgClosed.Basic` to both affected files
- Fixed `@Etingof.MoritaStructural` explicit calls in Corollary9_7_3.lean to account for new `[IsAlgClosed k]` parameter
- Decomposed the single sorry in `MoritaStructural` into two independently provable helper lemmas:
  1. `exists_basic_corner_ring`: existence of idempotent e with CornerRing e basic and Morita equivalent to A
  2. `basic_morita_equiv_algEquiv`: two basic Morita-equivalent algebras are isomorphic as k-algebras
- Proved `MoritaStructural` sorry-free from the two helper lemmas (3-step proof: get corner ring, compose Morita equivalences, apply uniqueness)
- Fixed universe level issue by annotating `IsBasicAlgebra.{u, u, u}` in the helper lemma
- Full project builds with no errors

## Current frontier

`MoritaStructural.lean` has 2 sorry's in the helper lemmas:
1. `exists_basic_corner_ring` (line 147) — corresponds to `exists_full_idempotent_basic_corner` + `morita_equiv_of_full_idempotent` in BasicAlgebraExistence.lean (both already sorry'd there)
2. `basic_morita_equiv_algEquiv` (line 171) — uniqueness of basic algebras up to isomorphism (new sorry, needs categorical infrastructure for equivalences preserving endomorphism rings and progenerators)

## Overall project progress

Stage 3.2 formalization. The MoritaStructural proof structure is now clear:
- Main theorem: sorry-free (proved from two helpers)
- Helper 1 (`exists_basic_corner_ring`): blocked on same infrastructure as BasicAlgebraExistence (#1848, #1849)
- Helper 2 (`basic_morita_equiv_algEquiv`): new, needs equivalence-preserves-End and basic-progenerator-uniqueness infrastructure

## Next step

Either:
- Prove `basic_morita_equiv_algEquiv` — the hardest new sorry. Needs: (a) equivalences preserve endomorphism rings, (b) basic algebras have multiplicity-free decomposition, (c) F(B₁) ≅ B₂ for basic B₁, B₂
- Or work on other unclaimed issues (#1788, #1837, #1867)

## Blockers

None blocking current work. The sorry's are well-characterized and can be addressed independently.
