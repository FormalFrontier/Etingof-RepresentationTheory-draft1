## Accomplished
Worked issue #6951 (prove `crux` — `k`-homogeneity of `e123 ∘ extMk`, the last sorry in
`extAbelianIsoExtₖ`). Deep API reconnaissance established that closing `crux` requires threading
`k`-homogeneity through the whole additive tower
`e123 = extAddEquivCohomologyClass ≫ homologyAddEquiv.symm ≫ homComplexHomologyAddEquivₖ`, and that
`k`-homogeneity is only *expressible* after landing in `(linearYonedaObj k N).homology n` (where the
`ModuleCat k` scalar lives) — so the `homComplexHomologyAddEquivₖ` step cannot be sidestepped.
Mathlib packages **none** of the needed naturality (no whole-complex `HomComplex` functoriality in
the target variable, no `homologyAddEquiv` naturality square, no `CohomologyClass.map`, no
target-naturality of `extAddEquivCohomologyClass`). This is a 2–4 session infrastructure build, not
a single-session lemma.

Decomposed #6951 into:
- **#6952** (no deps): `N`-side `HomComplex` postcomposition endo + `homologyAddEquiv` naturality.
- **#6953** (deps #6952, closes #6951): tower naturality of `homComplexHomologyAddEquivₖ` + assemble `crux`.
Skipped #6951 (`replan`) with a `Decomposed into #6952, #6953` breadcrumb.

Then claimed #6952 and landed its **deliverable 1** sorry-free in `Chapter8/HomComplexHomologyK.lean`:
- `cochainPostcompHom (g : N ⟶ N') (i)` — degreewise postcomposition of `i`-cochains with
  `(singleFunctor 0).map g`, as `Cochain … N[0] i →+ Cochain … N'[0] i`.
- `homCochainComplexPostcomp (g : N ⟶ N') : homCochainComplex N P ⟶ homCochainComplex N' P` — the
  induced chain map; chain-map square is `δ_comp_ofHom`.
- `homCochainComplexPostcomp_f_apply` simp lemma.

## Current frontier
#6952 deliverable 2 (the `homologyAddEquiv` naturality lemma
`homologyAddEquiv_symm_mk_postcomp`) remains. It needs `ShortComplex`/`LeftHomologyData` plumbing:
identify `homologyAddEquiv.symm ∘ CohomologyClass.mk` with `HomologicalComplex.homologyπ ∘ (cocycle→cycles)`
(via `leftHomologyData.cyclesIso` and `leftHomologyπ_comp_leftHomologyIso_hom`), and identify
`cyclesMap (homCochainComplexPostcomp g)` with `Cocycle.postcomp`; then conclude by
`HomologicalComplex.homologyπ_naturality`.

## Overall project progress
Stage 3 (formalization) ongoing; repo sorry-count was 2 (one in `ExtAbelianComparison.lean`, one
elsewhere) before this turn. This turn adds sorry-free infra (no new sorries) and sharpens the
`crux` residual into two well-specified sub-issues.

## Next step
Land #6952 deliverable 2 (`homologyAddEquiv` naturality under `homCochainComplexPostcomp`), reusing
the endo landed here. Then #6953 (tower naturality of `homComplexHomologyAddEquivₖ` via degreewise
`homDegEquiv`/`sIso` compatibility through the `homIso` composite, cased `n=0`/`n=m+1`) assembles
`crux` and closes #6951.

## Blockers
None. #6953 is correctly blocked on #6952 until deliverable 2 lands.
