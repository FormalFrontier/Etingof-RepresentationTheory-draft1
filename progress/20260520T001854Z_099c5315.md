## Accomplished

Closed chore issue #2990: refreshed both residual-sorry comment
blocks inside `non_adjacent_branches_leaf_case_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericNonAdjacentBranches.lean`
to cite the 4-way sub-issue decomposition (#2974, #2976, #2977,
#2978) spawned from #2955.

* Block 1 (upstream enumeration, was 3 bullets at lines 994-1003)
  is now 4 bullets matching the 4-way split:
  - `chain.length = 3`, all-leaves → #2974 (D̃₆ helper)
  - `chain.length = 3`, mixed arm degrees E.ab / E.bb → #2976 (Ẽ₇)
  - `chain.length = 5`, any sub-case → #2977 (D̃₈ helper)
  - `chain.length ≥ 6`, all-leaves → #2978 (parametric D̃_n helper)
* Block 2 (residual-sorry site, was 4 lines at 1098-1102) was
  rewritten to mirror Block 1's 4-bullet shape with the same issue
  citations. Header `===== Residual: tracked by follow-up
  sub-issues =====` preserved.
* `let _ := …` discards, the `sorry`, and surrounding code
  unchanged.

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
passes (8047 jobs, single expected `declaration uses sorry`
warning at the theorem declaration; sorry count for the file
unchanged at 1).

## Current frontier

Issue #2990 implementation pushed; PR opens against `main` with
auto-merge enabled. Remaining open chore from the same planner
cycle is #2991 (`FieldGenericTpqr.lean` TODO block — independent
of this PR, can be claimed by a separate session). The four sub-
issues themselves remain `blocked` on #2955 — unblocking is out
of scope here.

## Overall project progress

Comment-block hygiene refresh — strictly chore-level. No new
theorems, no new sorries, no change to the proof state. The two
chore issues (#2990 and #2991) from the wave-63 planner cycle
together align the FieldGenericNonAdjacentBranches and
FieldGenericTpqr files' residual-sorry citations with the active
sub-issue decomposition; #2990 lands first.

## Next step

Claim #2991 from a fresh session (or the same one if not yet
context-exhausted) to apply the analogous citation refresh inside
the TODO block at lines 1267-1279 of
`EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`
(citing #2907 / #2908 / #2909 / #2910 and parent #2905). #2564
remains externally blocked on Mathlib PR #38583 merge.

## Blockers

None for this issue. The four sub-issues #2974 / #2976 / #2977 /
#2978 referenced inline remain `blocked` on #2955 — those
blockers are tracked on the sub-issues themselves and are not in
scope for #2990.
