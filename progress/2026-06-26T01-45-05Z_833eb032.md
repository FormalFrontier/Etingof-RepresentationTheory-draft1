## Accomplished

Claimed and resolved #5292 (`feat(Ch9 §9.7): complete MoritaStructural — fill
remaining sorries`).

Investigation found the file `EtingofRepresentationTheory/Chapter9/MoritaStructural.lean`
**already has no `sorry`**. The progenerator-to-algebra steps the issue flags were
completed by earlier PRs (#2282, #2292). Verified rigorously:

- `grep` finds no `sorry`/`admit`/`sorryAx` in the code (only one stale word
  inside a doc comment).
- `lake build EtingofRepresentationTheory.Chapter9.MoritaStructural` succeeds
  with no sorry warnings (2390 jobs).
- `#print axioms` on `MoritaStructural`, `simple_of_equivalence`, and
  `equiv_preserves_indecomposable` reports only `propext, Classical.choice,
  Quot.sound` — no `sorryAx`.
- `MoritaStructural` transitively calls `basic_morita_algEquiv` →
  `basic_morita_regular_module_iso` / `equivEndAlgEquiv` → `head_isomorphism` /
  `exists_surjection_with_trivial_kernel_head`, so the whole chain is sorry-free.

The only residual artifact was stale documentation that still claimed the steps
were "sorry'd pending formalization" and "Blocked by". Updated three doc comments
(module docstring + the `basic_morita_algEquiv` proof-strategy block) to reflect
that the endomorphism-ring route is fully formalized. Doc-comment-only changes;
the file still builds.

## Current frontier

`MoritaStructural.lean` is complete and sorry-free. This unblocks the rest of the
#5139 Morita B_n cluster that depended on it.

## Overall project progress

Stage 3 (formalization) ongoing. Section 9.7 structural Morita theorem is fully
proved (no sorry, standard axioms only).

## Next step

Downstream #5139 Morita-cluster items that were waiting on a sorry-free
`MoritaStructural` can now proceed.

## Blockers

None.
