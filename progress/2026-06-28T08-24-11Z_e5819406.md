## Accomplished
Landed the representation-theoretic infrastructure for the linear-dual half of the
contragredient identity (issue #5553), sorry-free, in the new file
`Chapter5/LinearDualDetTwistCharacter.lean`:

- `glWeightSpaceℤ_charTwist_shift` — ℤ-level analogue of `glWeightSpace_detTwist_shift`:
  twisting by a character of torus weight `sh` shifts every integer weight space by `-sh`.
- `detChar_zpow_diagUnit` — `(detChar^z)(diagUnit i t) = t^z`.
- `detChar_pow_mul_inv_neg_zpow` — collapses the stacked twist character to `det^{(s+shift:ℤ)}`.
- `coeff_formalCharacter_detTwist_dual` (the coefficient formula) —
  `(formalCharacter M).coeff μ = finrank (glWeightSpaceℤ (schurModuleRep λ') (m·1 - μ))`,
  `m = s + lam.shift`. Assembles the collapse (`dual_charTwistRep` #5552 +
  `charTwistRep_charTwistRep`), the new twist-shift lemma, and the dual weight-negation
  `finrank_glWeightSpaceℤ_dual_eq` (#5533) over a Schur-module weight eigenbasis.

Wired into `Chapter5.lean`. Full `lake build` green.

## Current frontier
The #5553 deliverable (`formalCharacter … = schurPoly n (w0ShiftWeight …)`) is reduced
to a **pure-polynomial** coeff↔alternant bridge with no representation theory left. That
residual is decomposed into #5561.

## Overall project progress
Stage 3 (formalization) ongoing in Chapter 5. The contragredient identity
`algIrrepGLDual_iso_linearDual` (ContragredientIdentity.lean) still has the analytic core
`exists_common_schurModule_model_detTwist_dual` sorried; its Schur-module half (#5543) is
landed, and this PR lands the representation-theoretic heart of the linear-dual half (#5544
chain). The remaining piece is the pure-polynomial bridge #5561.

## Next step
Pick up #5561: prove `formalCharacter_detTwist_linearDual_eq_schurPoly` from the landed
`coeff_formalCharacter_detTwist_dual`, via the helper lemmas S1/S2 (Schur-module weight
multiplicity = schurPoly coefficient, and vanishing at negative weights) and the
poly-bridge from `schurPoly_inverseShift` (#5534). The alternant-coefficient extraction for
strictly-monotone exponent sequences is the substantial new piece.

## Blockers
None. #5561 is well-scoped and self-contained (pure polynomial algebra).
