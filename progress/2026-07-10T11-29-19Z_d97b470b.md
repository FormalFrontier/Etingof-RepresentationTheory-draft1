## Accomplished

- Claimed #6160 (feat Ch3 #3.9.3: irreducibles of a path algebra).
- Proved **deliverable 1** `Etingof.Problem3_9_3.simpleRep_isIrreducible` sorry-free in
  `EtingofRepresentationTheory/Chapter3/Problem3_9_3.lean`. `#print axioms` shows only
  `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).
  - Nontriviality at vertex `i`: the fibre is `Fin 1 → k`, exhibited `(0 : Fin 1 → k) ≠ 1`.
  - Lattice dichotomy: off-vertex fibres `Fin 0 → k` are subsingletons (so their submodules are
    `⊥ = ⊤`); the vertex-`i` fibre is 1-dimensional, hence a simple module. Transported the
    `IsSimpleOrder` dichotomy back to `W i` via `Submodule.orderIsoMapComap` of an identity
    `LinearEquiv`.
  - Key instance-diamond lesson: `(simpleRep i).obj i` is defeq to
    `Fin (if i = i then 1 else 0) → k` but **not** to `Fin 1 → k` (the `Decidable (i = i)`
    instance is abstract, so the `if` is stuck). Instance search cannot find
    `AddCommGroup ((simpleRep i).obj i)` since `.obj i` does not reduce to a `Pi` type; targeting
    the honest stuck-`if` `Fin (if i = i then 1 else 0) → k` (syntactically a `Pi`) makes TC and
    `is_simple_module_of_finrank_eq_one` go through.

## Current frontier

- Deliverable 1 landed; deliverable 2 split out.

## Overall project progress

- Stage 3 formalization (Chapter 3). `Problem3_9_3.lean` now has 1 of 4 theorems proved
  (`simpleRep_isIrreducible`); `irreducible_isSimpleRep`, `ext1_simpleRep_vanishes_iff`,
  `two_dim_classification` remain `sorry` (the latter two untouched, separate concerns).

## Next step

- Pick up **#6166** to prove `irreducible_isSimpleRep`. NOTE: the signature as written in #6160
  is **false** (no acyclicity/finiteness hypothesis — a one-loop quiver has a 1-dim irreducible
  not iso to `simpleRep`). #6166 documents the counterexample and the required signature change
  (`[Fintype Q] (hQ : NoOrientedCycles Q)`). A planner should confirm the exact hypothesis set
  against `blobs/Chapter3/Problem3.9.3.md` before proving.

## Blockers

- None for deliverable 1. Deliverable 2 (#6166) is blocked on a spec decision (add acyclicity +
  finiteness hypotheses), not on any missing dependency.
