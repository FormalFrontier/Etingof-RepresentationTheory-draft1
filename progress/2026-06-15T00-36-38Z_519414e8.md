## Accomplished

Issue #4702 (Ch6 #4686 sub-A): added `starRep_kQ_leaf_equalities` to
`EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean`, immediately above
`starRep_kQ_isIndecomposable`, with the signature specified in the issue
(adjusted only by spelling the fourth conjunct as an explicit
`∀ (x : Fin (m+1) → F), x ∈ W₁⟨1⟩ → …` rather than the `∀ x ∈ …` binder, which
left the element type a stuck metavariable).

The **all-forward (canonical) orientation branch is proven inline**, reproducing
the canonical `starTubeRepGen_isIndecomposable` reduction:
- `core`: `starEmbed1 x + starEmbed2 z ∈ W 0 → x ∈ W 1 ∧ z ∈ W 2`
  (center decomposition via `embed_sum_zero_F` + complement structure);
- `leaf3_sub` / `leaf4_sub`: pin leaf 3 (diag) and leaf 4 (tube) into leaves 1,2;
- `compl_le_forces_eq` chains these into `heq31`/`heq32`/`heq41`, then `h12`
  and `hN₁` (jordanShift-invariance).

The push facts are read per-orientation from the captured arrows
`a1..a4 : Q.Hom ⟨i⟩ ⟨0⟩` via `simpa only [starRep_kQ, starRepMap_kQ]`.

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericStar` passes; the
only new `sorry` warning is the four deferred reversed-leaf branches.

## Current frontier

Four reversed-leaf branches of `starRep_kQ_leaf_equalities` carry `sorry`
(leaf 1 reversed, leaf 2 reversed, leaf 3 reversed, leaf 4 reversed — each
covers all sub-combinations beneath it in the nested case-split). Follow-up
sub-issue created for them.

## Overall project progress

Stage 3 (formalization). `starRep_kQ_leaf_equalities` interface landed +
canonical branch proven; this is the first of the two pieces #4686 was
decomposed into (sub-A leaf collapse here, sub-B center crux is #4703).

## Next step

Follow-up sub-issue: close the four reversed-leaf branches. Note the genuine
subtlety surfaced during analysis: with leaf 4 reversed (`0 → 4`, map
`starProj4_F = starFirst_F`, which discards the Jordan-block second half), the
fourth conjunct (`jordanShiftLinGen`-invariance of `W₁⟨1⟩`) is **not** obviously
derivable from the leaf-4 datum, and may require either a different route or a
statement adjustment co-designed with the #4703 center crux. The three pure
equality conjuncts, by contrast, follow once `U₁ = S ⊕ S` is established (which
holds whenever leaves 1,2,3 are forward; reversed leaves need projection-core
helpers analogous to d5tilde's `core3_F_proj1/proj2`).

## Blockers

None. Canonical branch is solid; reversed branches are scoped to a follow-up.
