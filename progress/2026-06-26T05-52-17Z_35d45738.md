## Accomplished

One-shot dispatch (`/once`) on **#5320** — "discharge the 4 remaining `clength` API sorries in
`Chapter9/KrullSchmidt/Length.lean`". On arrival the issue was already partially done and decomposed
by a prior session: `clength_pos_of_not_isZero` and `clength_biprod` were landed, and the two genuine
cruxes were tracked as **#5324** (`clength_eq_zero_iff →`, the unblocker) and **#5325**
(`clength_additive`, depends on #5324).

This session (PR **#5351**, merged to `main`):

- Proved **`clength_simple : Simple X → clength X = 1`** sorry-free — the length-1 Jordan–Hölder base
  case — using Mathlib's `IsSimpleOrder (Subobject X)` instance for simple `X`
  (`simple_iff_subobject_isSimpleOrder`) and `Order.height_le_coe_iff` / `Order.height_add_one_le`.
- Sharpened the `clength_additive` docstring with the **confirmed Schreier route** and exact Mathlib
  API pointers.
- Posted detailed, actionable route comments to **#5324** and **#5325** (Schreier order-reflecting
  lemma; the missing pseudoelement-membership helper; the finiteness-routing decision — confirmed
  there are *zero* concrete `IsFiniteAbelianCategory` instances, so adding a `finiteLength` field to
  the base class breaks nothing).

No new `sorry`; no statement weakened. `lake build …KrullSchmidt.Length` succeeds.

## Current frontier

`Length.lean` still has exactly **2 sorries**: `clength_eq_zero_iff` (→) and `clength_additive`.
Both are the categorical Jordan–Hölder crux and are genuinely **not** dischargeable from the current
`IsFiniteAbelianCategory` definition or from Mathlib (Stacks-0FCK is flagged as future work in
`Mathlib/CategoryTheory/Noetherian.lean`).

## Overall project progress

Chapter 9 Krull–Schmidt link 1/5 (`clength` API): base cases complete (length 0 + length 1), chain
conditions / Fitting machinery downstream currently rest on the 2 remaining sorries. The hard
categorical Jordan–Hölder content is isolated and routed in #5324 (+ #5325).

## Next step

Work **#5324** first. Concretely: (1) build a pseudoelement-membership / "factors through a subobject"
helper for abelian categories (Mathlib has pseudoelements on objects only); (2) prove the Schreier
order-reflecting lemma for a short exact sequence; (3) route finite length into the API — adding
`finiteLength : ∀ X, HasFiniteLength X` to `Etingof.IsFiniteAbelianCategory` (option (a)) is
non-breaking (no concrete instances exist) but requires moving `HasFiniteLength` upstream of
`Definition9_6_1.lean`; (4) conclude height-finiteness, then `clength_eq_zero_iff`. Then #5325 refines
the same helper to the exact additive count.

## Blockers

None for this session's scope. The two remaining sorries are correctly tracked as #5324/#5325 and are
a deliberate multi-session build (categorical Jordan–Hölder infrastructure not in Mathlib).
