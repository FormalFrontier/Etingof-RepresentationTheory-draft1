## Accomplished

Work session `d6b66b05` (`/work` → `/feature`). Closed **#4617** (Ch5 #4609
sub-B): proved Part B of the Frame–Robinson–Thrall hook/beta cancellation,
`hookLengthProduct_mul_vandermonde_eq_prod_factorial` in
`Chapter5/CharValueHookFormula.lean`, **sorry-free**
(`#print axioms` = `[propext, Classical.choice, Quot.sound]`, no `sorryAx`).

New lemmas:
- `prod_range_sub_eq_factorial` : `∏_{x<k}(k−x) = k!` (via `prod_range_reflect`
  + `prod_range_add_one_eq_factorial`).
- `hookLengthProduct_cast` : `hookLengthProduct` is invariant under transporting
  the partition along its size-index equality (`subst; rfl`) — discharges the
  `lam.sum_eq ▸ …` cast in the target statement.
- `row_hook_gap_prod_eq_factorial` (the combinatorial heart): per-row identity
  `(∏_{c<λᵢ} h(i,c))·(∏_{k>i}(βᵢ−βₖ)) = βᵢ!`. Proved by the **cardinality
  disjoint-union** route: the hook set `image g (range λᵢ)` (where
  `g c = c + #{r>i : λᵣ ≤ c}`, so `βᵢ − g c = h(i,c)`) and the gap set
  `image β (Ioi i)` are disjoint subsets of `range βᵢ` with total size
  `λᵢ + (N−1−i) = βᵢ`, hence partition it. Disjointness is the two monotone
  leg-count bounds (`g c > βₖ` if `λₖ ≤ c` via `#(Ici k) ≤ Bc`; `g c < βₖ` if
  `c < λₖ` via `Bc ≤ #(Ioi k)`), each from antitonicity + `Fin.card_Ici/Ioi`.

Deliverable (1) `betaNum_strictAnti` was already in the project as
`shiftedExps_strictAnti` (`Proposition5_21_1.lean:440`) — used directly.

## Triage done before claiming (housekeeping)

The unclaimed queue's freshest issues are mostly blocked on mid-flight
foundations whose bodies wrongly claim them "landed":
- **#4577** (T(2,2,2) tube assembly) — claimed by another agent first.
- **#4613** (t125 sub-C assembly) — consumes `t125Rep_kQ_leaf_equalities`,
  produced by **#4620** (open, claimed), not by #4612. Added `depends-on #4620`
  (now `blocked`).
- **#4642** (etilde7 sub-B2 assembly) — consumes ~8 sub-B1 helpers
  (`blockProjAt_F`, `etilde7_*_comp_F`, `etilde7_center_decomp_F`, …) that are
  **absent from `main`** (no Section 2 in `FieldGenericETilde7.lean`, no open
  PR); real predecessor **#4569** is open+claimed. Added `depends-on #4569`,
  `skip`ped to `replan`.

## Current frontier

#4617 complete; PR up. `CharValueHookFormula.lean` now has exactly one `sorry`
left: Part A `charValue_trivialCycleType_eq_frobeniusDetForm` (line 261), which
is **#4608** (the Vandermonde determinant computation, independent of Part B).
Once #4608 lands, `charValue_trivialCycleType_eq_hookFormula` and the whole
Frobenius route to `dim V_λ = #SYT` (#4595) close.

## Overall project progress

Etingof draft 1, Stage 3. Ch5 Frobenius route (#4595): Part B (this issue) done;
Part A (#4608) is the last open piece. Ch6 sporadic-tube redesign
(#4548/#4597 program) active elsewhere; several assembly issues are blocked on
their sub-B foundations (#4569, #4620) being completed.

## Next step

A worker claims **#4608** (Part A: `charValue = n!·∏(lᵢ−lⱼ)/∏lⱼ!` via
`Matrix.det_vandermonde` on the shifted exponents) — the last sorry in the
Frobenius route. Planners should also clean up the stale "foundation landed"
claims in #4613/#4642 bodies (now correctly `depends-on` their real
predecessors #4620/#4569).

## Blockers

None for #4617 (done). #4595 transitively blocked on #4608. #4613/#4642 blocked
on #4620/#4569 (recorded).
