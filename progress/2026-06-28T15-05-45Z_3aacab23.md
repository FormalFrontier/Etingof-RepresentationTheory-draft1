## Accomplished
One-shot session on #5599 (Cauchy realization: `simpleSubrep_localRightRep_le_iSup_range`).

- Replaced the opaque `sorry` in `simpleSubrep_localRightRep_le_iSup_range`
  (`Chapter5/Theorem5_23_2_PeterWeyl.lean`) with a precisely-typed realization-core lemma
  `exists_dominantWeight_equivariant_realization` and **proved the descent sorry-free**: given the
  realization core (a `GL_n`-equivariant `ι : AlgIrrepGL n lam k →ₗ[k] R` with
  `range ι = S.toSubmodule`), the inclusion `S.toSubmodule ≤ ⨆_λ range (peterWeylSummandMap)`
  follows from `equivariant_range_le_peterWeylSummandMap` (#5578) + `le_iSup`.
- `lake build EtingofRepresentationTheory.Chapter5.Theorem5_23_2_PeterWeyl` is green; the only
  remaining `sorry` in the file is the body of `exists_dominantWeight_equivariant_realization`.
- Created successor sub-issue #5602 with a fully worked-out proof route for the realization core,
  and left a `Decomposed into #5602` breadcrumb on #5599.

## Current frontier
The realization core `exists_dominantWeight_equivariant_realization` is stated and isolated but its
body is `sorry`. This is the genuine missing highest-weight-classification step.

## Overall project progress
Stage 3 (formalization) of Chapter 5 Peter-Weyl. `peterWeylMap_surjective` rests on
`peterWeylMap_bijective` → the Cauchy spanning chain → `simpleSubrep_localRightRep_le_iSup_range`
→ (now) `exists_dominantWeight_equivariant_realization`. All wiring around the realization core is
sorry-free; the single residual sorry is the realization core itself.

## Next step
Work #5602. Route (in the issue body): (1) common denominator `r` via `exists_invSelf_normalForm`
on a basis of `S`, det^r-twist `S` into bounded-degree polynomials, algebraicity by mirroring
`rightHull_isSemisimple` (`RightTranslationHullDecomp.lean:221`); (2) homogeneity of the simple via
the right-translation grading (`PolyRightGrading.lean`); (3) weight-spanning
(`PolynomialWeightSaturation`); (4) `simple_constituent_formalCharacter_eq_schurPoly_mem`
(`ConstituentCharacterExtraction.lean:105`) → char = `schurPoly n ν`; (5)
`simpleRep_iso_schurModule_of_formalCharacter_eq` (`SchurWeylFormalCharacterIso.lean:916`) →
`M ≅ schurModuleRep ν`; (6) untwist and build `lam : DominantWeight n` with `lam.val = ν − r·1`,
noting `DominantWeight.shift` is determined (= `(r − ν(last)).toNat`), so identifying
`algIrrepGLRepρ n lam k` with `charTwist(det^{-r}) schurModuleRep ν` needs the det-shift iso
`schurModule_shift_iso_detTwist` (Prop 5.22.2) iterated `ν(last)` times (cf. `ContragredientIdentity`);
(7) assemble `ι = subtype ∘ e`.

## Blockers
None. #5602 is unblocked (all dependencies closed); it is well-scoped but large — consider splitting
steps 1-5 (polynomial Schur-module realization up to det^r-twist) from step 6 (det-shift
`DominantWeight` packaging) if it overruns a session.
