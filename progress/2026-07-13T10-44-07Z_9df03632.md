## Accomplished

Feature session for **#6480** (Ch9 Problem 9.4.6 (i), deliverable 2 of parent #6420): built the
standard short complex `A ⊗_S (V ⊗_S M) →ᵈ A ⊗_S M →ᵉ M` of a path-algebra module.

New file `EtingofRepresentationTheory/Chapter9/PathAlgebraStandardComplex.lean` (sorry-free):

- **`wSMul` arithmetic + `arrowInclusion_wSMul_src/tgt`**: source/target scaling of `V` under
  the inclusion `V → A` correspond to left/right multiplication by `f s = vertexEmbedding s`.
- **`ArrowTgt`** type synonym: carrier `ArrowIndex Q →₀ k` with the **target** `S`-action as its
  canonical `Module (Q → k)` instance (used to *form* the inner tensor). `srcHom s` is the
  source-scaling endomorphism, `(Q → k)`-linear w.r.t. the target action (via `wSMul_comm`).
- **`VtensCarrier M` / `VtensObj M`** (`V ⊗_S M`): `TensorProduct (Q → k) ArrowTgt (restrict M)`
  with the surviving **source** `S`-action registered by hand as `TensorProduct.map (srcHom s) id`
  (NOT the canonical tensor action — that is the target action; type synonym breaks the diamond).
- **`stdε`** (`ε`): defined as `homEquivSymm (𝟙 _)`, `= inducedRestrictAdj.counit.app M`
  (`stdε_eq_counit`); `ε (a ⊗ m) = a · m` (`stdε_tmul`).
- **`stdδ` / `stdd`** (`d`): `d = homEquivSymm δ` where `δ (v ⊗ m) = v ⊗ m − 1 ⊗ (v·m)` is the
  source-`S`-linear boundary datum; `d (a ⊗ v ⊗ m) = a·v ⊗ m − a ⊗ (v·m)` (`stdd_tmul`).
- **`stdd_comp_stdε`** (`d ≫ ε = 0`) and **`epi_stdε`** (`Epi ε`), both sorry-free.
- **`standardComplex M : ShortComplex (ModuleCat A)`** = `ShortComplex.mk d ε (d ≫ ε = 0)`.

Wired the new file (and its dependency `PathAlgebraInduction`, previously not in the aggregate)
into `EtingofRepresentationTheory/Chapter9.lean` so the default/CI build covers them.

## Current frontier

Deliverable 2 of #6420 complete. `lake build EtingofRepresentationTheory.Chapter9` succeeds; the
new file compiles with zero errors and zero warnings and contains no `sorry`.

## Overall project progress

Phase 3 formalization. Problem 9.4.6 (i) upper bound is being assembled bottom-up via the
noncommutative standard resolution (`A ⊗_S −`). Layers now in place: vertex subalgebra, induction
functor + adjunction + projectivity, arrow bimodule `V`, and (this PR) the standard short complex
with `d ≫ ε = 0` and `Epi ε`.

## Next step

Pick up **#6481** (blocked on #6480, now unblocked): prove `Mono d` (injectivity of `d`, via the
path-length grading) and the middle exactness `ker ε = im d`, packaging
`standardComplex.ShortExact`. Then **#6438** discharges the `sorry` in
`hasHomologicalDimensionLE_pathAlgebra_one` by dimension shifting
(`ShortExact.hasProjectiveDimensionLT_X₃`), both nonzero terms being projective via
`projective_inducedModule_obj`.

Design note for the successor: `VtensObj M` carries the **source** `S`-action; the inner tensor is
balanced over the **target** action (type synonym `ArrowTgt`). Use `vtens_smul_def` +
`TensorProduct.map_tmul` to compute the source action on a pure tensor, and
`TensorProduct.induction_on` (not bare `induction`) since `VtensCarrier` is a non-reducible `def`.

## Blockers

None.
