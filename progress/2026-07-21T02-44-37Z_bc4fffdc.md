## Accomplished

Review session for #7098 (fidelity audit of the two book-disavowed
`proof_wanted` statements). Read-and-report, no `.lean` edits.

Produced `progress/reviews/2026-07-21-proof-wanted-fidelity-audit.md`:
- Audited `Etingof.ado` (Ado's theorem, `Chapter2/Remark2_9_3.lean:47`) against
  its blob — **faithful**; `CharZero` scoping is documented and correct (Ado
  proper is char 0; Iwasawa is the positive-char analogue).
- Audited `Etingof.sl_finiteDimensional_completely_reducible`
  (`Chapter5/Remark5_23_3.lean:209`) against its blob — **faithful**; same
  `∃ N', IsCompl N N'` form as the proved `𝔰𝔩(2)` case
  `Sl2Irrep.complete_reducibility`; the deliberately-omitted highest-weight
  companion is accurately disclosed in the docstring.
- Classified every `proof_wanted`/`axiom`/`admit` grep hit: exactly **2** real
  `proof_wanted`, **0** axioms, **0** admits, **0** vacuous `: True`. All other
  hits are docstring/comment prose.
- Built both modules (`lake build` → 8666 jobs, green, only unrelated style
  warnings) confirming both signatures elaborate.

**Verdict:** no fidelity defects; no follow-up feature issue. Unproved surface
fully accounted for: 1 owned sorry (#7084) + 2 faithful `proof_wanted` + 0
axioms/admits.

## Current frontier

Terminus verification of the unproved surface complete. Repo remains at 1
genuine sorry (`finrank_g_three`, #7084) with the rest of the unproved surface
now audited as faithful.

## Overall project progress

Stage 3 formalization effectively complete: 1 genuine sorry (owned by #7084) +
2 audited-faithful book-disavowed `proof_wanted`. Late-stage cleanup/audit
phase (items.json reconciliation #7092, docstring rewordings #7093, stale-prose
sweep #7096 all in flight).

## Next step

Land the remaining cleanup issues (#7092 items.json status reconciliation,
#7093 docstring rewordings). The `finrank_g_three` feature #7084 is the last
genuine proof obligation.

## Blockers

None.
