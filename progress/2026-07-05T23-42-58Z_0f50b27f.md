## Accomplished

Closed the fidelity gap in `Chapter4/Example4.9.1` (issue #5636). The book's
Example 4.9.1 gives tensor-product multiplicity tables for `S₃`, `S₄`, and `A₅`.
On re-confirmation the finding was **partially stale**: `A₅` had already been
formalized since the Wave-2 audit; only the `S₄` table remained absent.

Added the `S₄` section to `EtingofRepresentationTheory/Chapter4/Example4_9_1.lean`,
mirroring the existing `S₃` and `A₅` sections and reusing the genuine `S₄`
irreducible catalogue built in `Example4_8_1` (`Etingof.Example4_8_1.S4.irrepS4`,
integer character table `tbl`, class representatives `classRepS4`):

* `nS4` — the tensor-product multiplicity table, transcribed from the book
  (`ℂ₊, ℂ₋, ℂ², ℂ³₊, ℂ³₋`).
* `classIdxS4` / `classIdxS4_spec` — class-index machinery (fixed-point count on
  `Fin 4` separates four classes; sign separates the two fixed-point-free classes),
  every `g` conjugate to its class representative, honest `decide` over 24 elements.
* `irrepS4_char_eq` — each character is a class function via `FDRep.char_conj`.
* `nS4_char_int` — the multiplicity identity at the five classes, a pure integer
  `5·5·5` case split by honest `decide`.
* `S4_tensor_character` / `S4_tensor_product_character` — the book's identity
  `χ_i(g)·χ_j(g) = Σ_k n_{ij}^k χ_k(g)` for every `g`, as a character identity and
  on genuine `FDRep` tensor products.
* `S4_conj_classes` — 5 conjugacy classes (reuses the `Example4_8_1` theorem).

The decomposition is self-certifying: the five class equations uniquely determine
the multiplicities (the character table is invertible), so `decide` passing on
`nS4_char_int` proves the transcribed table equals the book's. `#print axioms`
on all three new theorems shows only `propext, Classical.choice, Quot.sound` — no
`sorry`, no `native_decide`.

Updated `progress/items.json`: `Chapter4/Example4.9.1` fidelity `gap` → `verified`,
decl list extended with the `A₅` and `S₄` theorems, stale note cleared.

## Current frontier

`Chapter4/Example4.9.1` is now fully faithful to the book (all three tables). File
builds clean (`lake build EtingofRepresentationTheory.Chapter4.Example4_9_1`, exit 0,
8587 jobs).

## Overall project progress

Phase 3 formalization + Stage 3.7 fidelity sweep ongoing. This turn resolved one
Wave-2 fidelity gap.

## Next step

Continue the fidelity sweep on other open `review` issues.

## Blockers

None. Two pre-existing non-fatal notes on already-merged code, unrelated to this
change: an `info`-level linter panic (`Lean.Expr.appArg!`) on the S₃ theorem
docstring (line 334), and a `style.maxHeartbeats` comment nit on the A₅ lemma
(line 430). Both were present before this change and do not affect the build.
