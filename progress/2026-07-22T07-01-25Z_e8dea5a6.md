## Accomplished

Stage 3.7 statement-fidelity & non-vacuity audit of **Problem 4.12.3** (irreducibility of
`SⁿV` and `∧ᵐV` as `GL(V)`-representations), issue #7278. Report-only — no Lean proofs
modified.

- Audited four declarations against `blobs/Chapter4/Problem4.12.3.md`:
  `symmetricPower_eq_bot_or_top`, `Example5_19_3_symmetric_irreducible`,
  `exteriorPower_eq_bot_or_top`, `Example5_19_3_exterior_irreducible`.
- Confirmed the `⊥ ∨ ⊤` irreducibility encoding, the natural functorial GL(V)-action
  quantified over all `g : V ≃ₗ[k] V`, and coverage of both symmetric and exterior halves.
- Resolved the issue's key vacuity concern (`∧ᵐV = 0` for `m > dim V`): the `⊥∨⊤`
  conclusion is true for all `n`, non-degenerate exactly on the book's `n ≤ dim V` range
  (space nonzero), and the degenerate range is separately proved zero by
  `Example5_19_3_exterior_subsingleton_of_dim_lt` and cross-referenced. Not a gap.
- Verified `[CharZero k]` is a sound, essential generalization of ℂ, documented on the
  symmetric theorem and both wrappers (minor doc nit: the low-level
  `exteriorPower_eq_bot_or_top` docstring omits the char-0-essential note — recorded, not
  escalated).
- Spot-checked the shared criterion `DiagonalCoordinate.eq_bot_or_eq_top_of_connected` is a
  genuine Lagrange-interpolation + connectivity proof, not a vacuous stub.
- `lake build` both files exits 0; `#print axioms` on all four is clean
  `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.

**Verdict: `verified` / `covered_full`.** Report at
`progress/reviews/2026-07-22-ch4-problem4_12_3-symexterior-irreducible-fidelity.md`;
`progress/items.json` `Chapter4/Problem4.12.3` updated (fidelity, fidelity_decl, lean_file,
coverage, fidelity_note, last_updated).

## Current frontier

Problem 4.12.3 fidelity audit complete. No follow-up repair issue needed (no gap found).

## Overall project progress

Stage 3.7 (statement-fidelity & non-vacuity sweep) ongoing. This turn cleared one more
un-audited Chapter 4 item. Sibling in-flight audits at claim time: #7272 (Problem 4.12.9),
#7276 (Problem 4.12.5).

## Next step

Pick up another unclaimed `review` Stage 3.7 audit (e.g. #7272 or #7276), or continue the
sweep across remaining `fidelity: (none)` items in `progress/items.json`.

## Blockers

None.
