## Accomplished

- Resolved issue #8526 by proving `Etingof.mem_radical_iff` for arbitrary rings: the
  Jacobson radical consists exactly of the elements acting by zero on every simple module.
- Added the universe-polymorphic forward lemma `Etingof.radical_smul_eq_zero` and reused the
  public characterization in `Etingof.structure_mod_radical`.
- Updated the claim ledger and regenerated the Stage 3.4 dependency and Stage 3.5 proof-polish
  certificates from a successful 9,495-target build.
- Incorporated a fresh independent Claude review, including stronger universe-polymorphic API,
  robust maximal-left-ideal reasoning, and corrected attestation dates.

## Current frontier

Issue #8526 is complete locally on branch `codex/issue-8526` and ready to publish as its own PR.
The broader tracked batch is #8523--#8536 plus #8541; PRs #8537, #8538, and #8542 are already
open and running CI.

## Overall project progress

The formalization remains fully built and certificate-valid. This change closes the semantic gap
between the book's definition of `Rad(A)` and the existing Jacobson-radical abbreviation without
adding finiteness, commutativity, or algebraic-closedness assumptions.

## Next step

Publish the #8526 PR, enable squash auto-merge after CI, then continue the next independent issue
from a fresh `/tmp` clone of updated `main`. Rebase and regenerate certificates for overlapping
open PRs after each merge.

## Blockers

None.
