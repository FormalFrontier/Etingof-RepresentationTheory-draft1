## Accomplished

Fixed issue #7532: restored fresh-buildability of the arbitrary-extension descent
source for Problem 3.8.4 (`EtingofRepresentationTheory/Chapter3/Problem3_8_4_Descent.lean`).

Three `map_smul'` obligations (in the `exists_fg_subalgebra_baseChange_iso` equivalence
and the `i'`/`p'` maps of `exists_fg_subalgebra_baseChange_directSummand`) failed to
elaborate with `` `simp` made no progress `` at the `| zero =>` case of the
`TensorProduct.induction_on` on the scalar. The goal there is `f (0 • x) = 0 • f x`.
The `↥R ⊗[K] A`-module on `↥R ⊗[K] V`/`↥R ⊗[K] W` is a `Module.compHom` action
(`Functoriality.bcMod`), so `zero_smul` / `simp` could not resolve which
`SMulWithZero (↥R ⊗[K] A) _` instance the goal's `0 • _` uses (bare `rw [zero_smul]`
reported "did not find pattern `0 • ?m`"). Replaced each with
`rw [zero_smul (↥R ⊗[K] A) x, zero_smul (↥R ⊗[K] A) (f x), map_zero]`, passing the
element explicitly to pin the module structure. Added a one-line comment at the first
site explaining the elaborator quirk so it is not reverted.

Verification:
- `lake env lean` on Descent, General, Main, and top-level `Problem3_8_4` all exit 0.
- `lake build` of the `Problem3_8_4` / `_Main` / `_General` aggregate targets: 8591 jobs,
  build completed successfully (remaining warnings are pre-existing style lints).
- `#print axioms` on `iso_of_baseChange_iso`, `directSummand_of_baseChange_directSummand`,
  and both `Descent.exists_fg_subalgebra_baseChange_*` theorems: only
  `[propext, Classical.choice, Quot.sound]` — no `sorryAx`, no replacement axiom.
- `directSummand_of_baseChange_directSummand` (General.lean:47) genuinely consumes the
  repaired `Descent.exists_fg_subalgebra_baseChange_directSummand`, with no finiteness
  hypothesis on the field extension `L / K`, as the issue required.

## Current frontier

Issue #7532 complete. Descent chain fresh-buildable end to end.

## Overall project progress

Stage 3.7 completeness-audit / regression-repair phase. Large backlog of open
`feature`+`completeness-audit` issues remains, including many other
"restore fresh-buildable" regressions (e.g. #7518, #7523, #7527, #7531, #7534, #7550).

## Next step

Pick another "restore fresh-buildable" regression. Several share the same failure
family (post-Mathlib-bump `simp`/instance-resolution regressions on `Module.compHom`
tensor actions); the explicit-argument `zero_smul` pattern used here may transfer.

## Blockers

None.
