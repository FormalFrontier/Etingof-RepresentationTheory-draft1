## Accomplished

Feature session `25c3fb51` on parent issue #2951
(`non_adjacent_branches_leaf_case_per_kQ` Phase 2 case-split). After
assessing scope (5 cases A–E each requiring distinct vertex extraction
+ ~15 distinctness facts + dispatch to a different per-(F, Q)
embedder), landed **Case A** as a coherent subset and decomposed the
remainder into three sibling sub-issues.

* **PR #2956** (partial against #2951, branch `agent/25c3fb51`):
  Phase 2 Case A inside `non_adjacent_branches_leaf_case_per_kQ`
  (`Chapter6/FieldGenericNonAdjacentBranches.lean`). The
  `by_cases hA : 6 ≤ chain.length ∧ vertexDegree adj side_arm = 2`
  positive branch extracts `side_arm`'s unique non-`v₀` neighbour
  `x`, builds the 8 adjacency + 8 same-arm distinctness facts
  required by `embed_t125_in_tree_per_kQ`
  (`FieldGenericT125.lean:71`), and dispatches via the T(1, 2, 5)
  vertex map `0→v₀, 1→leaf, 2→side_arm, 3→x, 4..8→chain[1..5]`.
  The negative branch keeps a single `sorry` covering Cases B–E +
  uncovered configurations.

* **Sub-issues filed**:
  * #2953 (Case B — T(1, 2, 5) at `w`, chain.length ≥ 6,
    arm extends).
  * #2954 (Cases C + D — Ẽ₇ / Ẽ₆ at chain.length ∈ {4, 5}),
    depends-on #2953.
  * #2955 (Case E — chain.length = 3 — plus parent's uncovered
    all-leaves configurations where no per-(F, Q) `dTilde_n` helper
    exists), depends-on #2954.

* **Parent #2951** marked `replan` via `--partial`; breadcrumb
  comment links to all three sub-issues with one-line scope
  assessment.

Build of `EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
passes after Case A — no errors, only the expected `sorry` at
`FieldGenericNonAdjacentBranches.lean:87` for the negative branch
plus pre-existing sorries in other files (unchanged).

## Current frontier

Phase 2 of `non_adjacent_branches_leaf_case_per_kQ`: Case A landed,
Cases B/C/D/E queued as sibling sub-issues with declared ordering
(B → CD → ER).

Per-(F, Q) tower for Theorem 2.1.2 forward bridge:

* Outer assembly (`non_adjacent_branches_infinite_type_per_kQ`):
  closed, PR #2921 + audit #2926.
* Per-(F, Q) leaf-case helper API stub (sub-A1): PR #2933 merged,
  audit in queue as #2948 (still unclaimed at session start).
* Per-(F, Q) leaf-case helper Phase 1 setup: PR #2952 merged.
* Per-(F, Q) leaf-case helper Phase 2 Case A: this PR #2956.
* Per-(F, Q) leaf-case helper Phase 2 Cases B/C/D/E + uncovered:
  sub-issues #2953, #2954, #2955.

In flight (claimed when session started):

* #2944 (review) — audit PR #2943
  (`non_adjacent_branches_infinite_type_per_kQ` outer assembly).

## Overall project progress

Stage 3 (Lean formalization) in flight. Wave 62's
`non_adjacent_branches_leaf_case_per_kQ` chain now has Phase 1
(merged) + Phase 2 Case A (this PR pending merge). The negative
branch of the Phase 2 case-split sorries to three sibling sub-issues
that finish the dispatch.

Remaining Ch6 per-(F, Q) leaves with replan/blocked status (not
prioritised this session): #2853 (D̃₅ 31 non-canonical
orientations), #2851 (D̃₅ sub-B), #2770 (Wall 3 R2.b.ii),
#2708 (Schur-Weyl L_i C-4a), #2703 (Wall 3 R2.c), #2493/#2483/#2482
(Schur-Weyl decomposition chain).

## Next step

Pickup recommendations for the next worker:

* **Feature** workers: #2953 (Case B — T(1, 2, 5) at `w`) is the
  direct continuation of this session's PR and is unblocked once
  #2956 merges. Pattern reference is the Case A block landed in
  #2956. Strategy mirrors Case A but centred at `w` with chain
  traversed in reverse; one small inner `by_cases` picks `arm₁` vs
  `arm₂` for the extending arm.
* **Review** workers: #2948 (audit PR #2933 leaf-case API stub) and
  #2949 (combined audit of PRs #2945 + #2947 embedder bodies) remain
  unclaimed; #2944 in flight.
* **Planner**: parent #2951 is now `replan` — triage cycle should
  close it with a forward link to #2953 / #2954 / #2955 once the
  decomposition breadcrumb is verified.

Next planner cycle should also revisit the **uncovered all-leaves
case enumeration gap** flagged in #2955 — the parent #2951 body
listed Cases A–E but did not cover configurations like
`chain.length ≥ 6 ∧ all arms are leaves`, which would need a
`dTilde_n_not_finite_type_per_kQ` parametric helper or an
unreachability proof from the outer assembly.

## Blockers

None for this session. The uncovered all-leaves cases flagged in
#2955 are a design-level gap that needs planner/architecture
judgement; the sub-issue's body lists three resolution strategies
for the implementer to choose from.
