## Accomplished

- Claimed issue #2954: Phase 2 Cases C+D for
  `non_adjacent_branches_leaf_case_per_kQ`.
- Implemented **Case C** (Ẽ₇ = T(1, 3, 3) centred at v₀, `chain.length
  ∈ {4, 5}`, `vertexDegree adj side_arm = 2`, `vertexDegree adj x = 2`)
  in `Chapter6/FieldGenericNonAdjacentBranches.lean`:
  - Extracted `x` (side_arm's non-v₀ neighbour) and `x'` (x's
    non-side_arm neighbour) via the same `Finset.card_eq_one` /
    `Finset.erase` pattern as Case A.
  - Built the seven adjacency facts (`h_leaf_adj`, `hside_adj`,
    `hx_adj`, `hx'_adj`, `hc1_adj`, `hc12`, `hc23`) and the seven
    distinctness facts required by `embed_etilde7_in_tree_per_kQ`
    (`FieldGenericETilde7.lean:356`).
  - Vertex map: `0→v₀, 1→leaf, 2→side_arm, 3→x, 4→x', 5→chain[1],
    6→chain[2], 7→chain[3]`.
- Implemented **Case D** (Ẽ₆ = T(2, 2, 2) centred at w, `chain.length
  ∈ {4, 5}`, both `arm₁` and `arm₂` of degree 2) in the same file:
  - Extracted `y₁`, `y₂` (arm₁'s and arm₂'s non-w neighbours).
  - Reversed the chain[len-3]→chain[len-2] edge (mirror of Case B's
    `hcL_2_3`/`hcR_2_3` pattern).
  - Built the six adjacency facts and six distinctness facts required
    by `embed_etilde6_in_tree_per_kQ` (`FieldGenericETilde6.lean:372`).
  - Vertex map: `0→w, 1→chain[len-2], 2→chain[len-3], 3→arm₁, 4→y₁,
    5→arm₂, 6→y₂`.
- Filed sub-issue #2960 for the **Case C edge sub-case**
  (`vertexDegree adj x = 1` — side arm collapses to length 2). The
  issue body sketches two candidate strategies (D̃₅ embedding for
  `x.deg = 1`; triangle/acyclicity contradiction for `x.deg = 3`).
- Verified the targeted build:
  `lake build
  EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
  passes with only the documented `sorry` warning at line 87
  (transitively from the remaining `sorry`s in C.short and Case E).
  Downstream
  `EtingofRepresentationTheory.Chapter6.FieldGenericAssembly`
  also green.

## Current frontier

The `by_cases hA` negative branch in
`non_adjacent_branches_leaf_case_per_kQ` is now decomposed as:

* Case A: `6 ≤ chain.length ∧ side_arm.deg = 2` — closed (PR #2956).
* Case B: `6 ≤ chain.length ∧ (arm₁.deg = 2 ∨ arm₂.deg = 2)` —
  closed (PR #2958).
* Case C main: `chain.length ∈ {4, 5} ∧ side_arm.deg = 2 ∧
  x.deg = 2` — **closed (this PR)**.
* Case C short: `chain.length ∈ {4, 5} ∧ side_arm.deg = 2 ∧
  x.deg ≠ 2` — `sorry`, tracked by **#2960**.
* Case D: `chain.length ∈ {4, 5} ∧ arm₁.deg = 2 ∧ arm₂.deg = 2` —
  **closed (this PR)**.
* Case E: `chain.length = 3`, all-leaves at `chain.length ≥ 6`, and
  asymmetric short-chain cases — `sorry`, tracked by **#2955**.

## Overall project progress

Theorem 2.1.2's forward direction
(`Chapter2/Theorem2_1_2.lean:153-179`) remains end-to-end derivable
modulo:
- `non_adjacent_branches_leaf_case_per_kQ` (residuals #2955, #2960)
- `etilde6_not_finite_type_per_kQ` (still sorry-bodied)
- a few transitive `sorry`s in `FieldGenericETilde6/ETilde7` (the
  representation-indecomposability bodies).

Cases A/B/C-main/D landing in PRs #2956, #2958, and this PR represent
the bulk of the Phase 2 case-split for the non-adjacent-branches
leaf case.

## Next step

- Worker on #2960 (Case C short sub-arm). Self-contained; the
  necessary distinctness lattice is already in scope.
- Worker on #2955 (Case E + all-leaves). The hardest remaining piece
  — needs a forbidden-subgraph picked for the chain.length = 3 and
  all-leaves configurations.
- Worker on #2948 (audit of #2933 stub + #2941 signature delta) — can
  proceed in parallel.
- Worker on #2949 (audit of Ẽ₆/Ẽ₇ embedders) — can proceed in
  parallel.

## Blockers

None for this PR. Residuals tracked by #2960 and #2955.
