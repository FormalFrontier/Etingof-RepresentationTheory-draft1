## Accomplished

Refreshed the file docstring and the theorem docstring of
`non_adjacent_branches_leaf_case_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericNonAdjacentBranches.lean`
to match what's landed on `main` post PR #2979 (commit `2a9398f`).
Closes #2982.

Changes (comments / docstrings only — no code changes):

* File docstring "Why a different strategy" inventory now includes
  `d7tilde_not_finite_type_per_kQ`
  (`FieldGenericD7Tilde.lean:272`) and the shared embedder
  `embed_d7tilde_in_tree_per_kQ` (`FieldGenericD7Tilde.lean:323`),
  both added by PRs #2968 / #2970.
* Strategy paragraph now lists `D̃₇` alongside `Ẽ₆`, `Ẽ₇`,
  `T(1, 2, 5)` as a target embedding.
* Replaced the `## API stub` section with a `## Status` section that
  summarises the landed body: Phase 1 setup, Phase 2 dispatch via the
  `by_cases hA … hD` case-split with the per-case embedder calls
  (Cases A / B / C-main / C.short / D plus partial Case E sub-cases
  E.aa and E.s1c4), and the documented residual `sorry` tracked by
  sub-issues #2974, #2976, #2977, #2978 (all blocked on #2955).
* Theorem docstring "API stub" paragraph replaced with the same status
  summary in shorter form, cross-referencing the file docstring.

## Current frontier

Docstring refresh shipped. The actual residual `sorry` work is still
tracked by the four sub-issues (#2974 / #2976 / #2977 / #2978), all
blocked on #2955 — these are independent of this PR.

## Overall project progress

Chapter 6 Phase 2 per-(F, Q) port is in progress; the
non-adjacent-branches leaf-case body is substantially landed with one
documented residual `sorry`. This PR is a docstring-only refresh so
readers of the file see the accurate landed state rather than the
stale "API stub" framing.

## Next step

Pick up one of the residual sub-issues (#2974, #2976, #2977, #2978)
once they unblock from #2955, or audit PR #2970 under #2973.

## Blockers

None.
