## Accomplished

Discharged the final `sorry` in **Problem 9.4.5 (i)**
(`Etingof.Problem945.cartan_det_eq_pm_one`), the issue #6463 deliverable. The file
`EtingofRepresentationTheory/Chapter9/Problem9_4_5.lean` is now sorry-free.

- New `simpleModule_finite`: a simple `A`-module is cyclic, hence `Module.Finite A`.
- New `homClassVector_eq_mulVec_of_projectiveDimensionLE`: the Euler-characteristic induction.
  For every f.g. `A`-module `N` with `HasProjectiveDimensionLE (ModuleCat.of A N) d`,
  `homClassVector P N = C.mulVec e` for some `e : ι → ℤ`. Proof by induction on `d`:
  - base `d = 0`: `N` projective (`projective_iff_hasProjectiveDimensionLE_zero` +
    `ModuleCat.projective_of_module_projective`), close with the Krull–Schmidt class-vector
    lemma `homClassVector_projective_eq_mulVec` (#6462).
  - step: finite free cover `Aⁿ ↠ N` (`Module.Finite.exists_fin'`), syzygy short exact
    `0 → ker f → Aⁿ → N → 0` (`LinearMap.shortExact_shortComplexKer`); the projective middle
    term gives `HasProjectiveDimensionLE (ModuleCat.of A (ker f)) d`
    (`ShortComplex.ShortExact.hasProjectiveDimensionLT_X₃_iff`); additivity
    (`homClassVector_add_quotient` with `Aⁿ ⧸ ker f ≃ₗ[A] N`) yields
    `homClassVector P N = C.mulVec a₀ − C.mulVec eₖ = C.mulVec (a₀ − eₖ)`.
- Wired into `cartan_det_eq_pm_one`: `hfin` (finite homological dimension) + the existing
  `Etingof.homologicalDimension_eq_top` contrapositive give a uniform bound `d₀`; apply the
  induction lemma to each simple `M j`.
- Statement tweak: `M : ι → Type*` → `M : ι → Type u` (needed so `ModuleCat.{u} A` accepts
  `M j`; the completeness hypothesis already quantified only over `Type u`). No external callers.
- Added imports: `ModuleCat.Projective`, `ShortComplex.ModuleCat`, `Finiteness.Cardinality`,
  and the in-project `HomologicalDimensionReduction`.

`#print axioms Etingof.Problem945.cartan_det_eq_pm_one` = `[propext, Classical.choice, Quot.sound]`
(no `sorryAx`). `lake build EtingofRepresentationTheory.Chapter9` succeeds.

## Current frontier

Problem 9.4.5 fully complete (all three sub-parts: 9.4.5(i) Cartan det ±1, and both (ii)
infinite-homological-dimension computations). `Problem9_4_5.lean` is sorry-free.

## Overall project progress

Stage 3 formalization. Chapter 9 K₀/Cartan-matrix and homological-dimension track: Problem
9.4.5 done end-to-end.

## Next step

A planner can close #6463 on merge. Nearby open work: #6448 (Heisenberg classification via
Theorem 5.27.1), #6449 (FFT/Schur–Weyl invariant subalgebra), #6471 (semidirect orbit method).

## Blockers

None.
