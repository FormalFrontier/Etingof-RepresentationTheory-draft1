## Accomplished

- **Closed #4816** (Ch6 #4777 S3-bridge): new sorry-free file
  `Chapter6/Problem6_1_5_TitsBridge.lean` connecting the matrix Tits form of
  Definition 6.1.4 to the orbit-side dimension data, completing the arithmetic
  part of step 3 of the orbit-counting route to Gabriel's theorem.
  - `arrowCount_add_swap_eq_adj`: from `IsOrientationOf Q adj` (each edge oriented
    one way, non-edges empty), finite-subsingleton Hom types, `adj.IsSymm` and
    `adj ∈ {0,1}`, the directed arrow counts satisfy `(bᵢⱼ : ℤ) + bⱼᵢ = adjᵢⱼ`.
  - `titsForm_eq_two_mul_dim_diff`: the arithmetic bridge
    `xᵀ(2·I − adj)x = 2·((Σᵢ mᵢ²) − Σᵢⱼ bᵢⱼ mᵢ mⱼ) = 2·(dim G − dim W)` for
    `xᵢ = (mᵢ : ℤ)`. Combines PosDef's `titsBilinear_eq` with the collapse of the
    directed double sum to twice the undirected one (`i ↔ j` reindex via
    `Finset.sum_comm`).
  - `hcone_of_strict_dim_bound`: from the strict bound `dim W(m) < dim G(m)` for
    nonzero `m` (raw-sum form), produces the `hcone` hypothesis of
    `titsForm_posDef_of_cone` — each nonzero nonnegative integer `x` recovered as
    `m i = (x i).toNat`, the strict nat inequality transported to ℤ by `push_cast`.
  - `isDynkinDiagram_of_strict_dim_bound` (raw-sum) and
    `isDynkinDiagram_of_strict_finrank` (finrank-phrased): combine with
    `isDynkinDiagram_of_cone` to conclude `IsDynkinDiagram n adj` from the
    graph-shape clauses plus the strict dimension hypothesis. The finrank form
    takes exactly the `Module.finrank k (repSpace m) < finrank k (∏ᵢ Matrix …)`
    hypothesis — the `−1` strict refinement of
    `repSpace_finrank_le_repGroup_ambient_finrank` (DimBound).
  - Added the import to `Chapter6.lean`.

## Current frontier

`Problem6_1_5_TitsBridge.lean` builds clean and sorry-free; all four named results
`#print axioms` to `[propext, Classical.choice, Quot.sound]` only. The remaining
forward-direction gap in `Theorem_6_1_5` is now isolated to the single hypothesis
`dim W(m) < dim G(m)` for nonzero `m` (the `−1` strict refinement), consumable via
`isDynkinDiagram_of_strict_finrank`.

## Overall project progress

- Chapter 6 orbit-counting directive (#4777): S0/S1/S2a/S2b/S2c/S3-core merged;
  the step-2(b)/(c) non-strict dim bound `dim W ≤ dim G` is assembled (#4814); this
  turn adds the step-3 arithmetic bridge `B(m,m) = 2·(dim G − dim W)` and the
  `hcone`/`IsDynkinDiagram` interface from the strict bound.
- Remaining on the directive: (1) the `−1` strict refinement `dim W(m) < dim G(m)`
  from the trivial global-scalar action (depends on the in-flight finitely-many-
  orbits #4798), and (2) S4 (#4786) wiring `isDynkinDiagram_of_strict_finrank` into
  the forward `sorry` of `Theorem_6_1_5` + deleting the construction track.

## Next step

- A planner/worker should scope the `−1` strict refinement: refine the
  comorphism/trdeg argument in `Problem6_1_5_DimBound`/`OrbitComorphism` to
  `dim W(m) ≤ dim G(m) − 1` for nonzero `m` (the global scalars `k*` lie in every
  point stabiliser, dropping the effective group dimension by one), then feed
  `isDynkinDiagram_of_strict_finrank` into #4786.

## Blockers

- The `−1` strict refinement depends on the in-flight finitely-many-orbits result
  (#4798, claimed). Not a blocker for this PR, which only supplies the arithmetic
  interface and leaves the strict bound as a hypothesis.
