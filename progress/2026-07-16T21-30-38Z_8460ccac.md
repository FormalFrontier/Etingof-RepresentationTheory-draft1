## Accomplished

Feature session on #6854 (Ch6 #Problem6.1.3-g, tree case degree-≤3 core). Assessed scope: the
finite templates it must mirror (`branch_classification` ~700 lines, `tree_branch_iso`,
`arm_length_solutions`, `path_walk_construction`) are all `private`, so the affine analogues must be
rebuilt from scratch plus a new equality-Diophantine solver — firmly multi-session, matching the
issue's own sizing note.

Landed a compiling **top-down skeleton** in `Chapter6/Problem6_1_3_continued_tildeE.lean`:
- `affine_tree_degree_le_three_iso` (main): connected acyclic (`∑∑ adj < 2n`) affine Dynkin diagram
  with all degrees ≤ 3 ⟹ `∃ t : AffineType, ∃ σ, ∀ i j, adj (σ i) (σ j) = t.adj i j`. **Proved
  sorry-free** by dispatching on the branch count.
- Three sub-lemmas, stated with fixed interfaces, proof `sorry`:
  - `affine_tree_branch_count` — one or two degree-3 vertices.
  - `affine_tree_two_branch_iso` — two branches ⟹ D̃ₙ.
  - `affine_tree_one_branch_iso` — one branch ⟹ Ẽ₆/Ẽ₇/Ẽ₈.

Filed sub-issues #6880 (branch count), #6881 (D̃ two-fork), #6882 (Ẽ arm-length Diophantine), each
self-contained with route + reusable-lemma pointers. Decomposed #6854 via breadcrumb; landing the
skeleton as a partial PR.

## Current frontier

The main tree-case dispatch lemma is sorry-free; the three combinatorial sub-lemmas remain sorried.
The forward direction of the top `affine_dynkin_classification` is still a single `sorry` (its own
issue, #6785/#6793) and will consume `affine_tree_degree_le_three_iso` once the sub-lemmas land.

## Overall project progress

Stage 3 formalization ongoing. Ch6 affine Dynkin classification ⟹ direction: cyclic case done
(`affine_cyclic_case`, #6872), degree-4 dichotomy done (#6853); tree case now has a proven skeleton
with three open sub-issues.

## Next step

Claim #6880 (branch count — smallest, unblocks reasoning about the others), then #6881 (D̃), then
#6882 (Ẽ, largest — likely needs further splitting into the equality Diophantine solver + arm
reindexings). All three are independent (each takes its branch hypotheses directly) and can run in
parallel. When all three land, `affine_tree_degree_le_three_iso` is sorry-free automatically.

## Blockers

None. #6854's dependency #6853 is merged. The three sub-issues are unblocked.
