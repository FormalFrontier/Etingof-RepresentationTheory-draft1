## Accomplished

Worked issue #6665 (`/work` → feature): assemble `mckay_isAffineDynkin` with the
strengthened hypothesis `hm : 3 ≤ m` (excludes the ℤ/2 / Ã₁ double-edge case).

Landed in `EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean` (commit
`bcd1ff52`), building cleanly (`lake build` exits 0):

- Changed `mckay_isAffineDynkin` hypothesis `1 ≤ m` → `3 ≤ m`, relocated it below
  `mckayCartan_posSemidef` / `mckayCartan_not_posDef` (forward-reference fix).
- **Five of six conjuncts sorry-free**:
  - symmetric — `mult_symm`;
  - `0/1` entries — new lemma `mult_le_one_off` (marks vector `d > 0` in the Cartan
    kernel + connectivity force single edges once `m ≥ 3`), plus supporting
    `mckay_marks_sum` and `finrank_W_ne_zero`;
  - connectivity with every edge = 1 — `mckay_connected` + `mult_le_one`;
  - positive semidefinite / not positive definite — `mckayCartan_posSemidef` /
    `mckayCartan_not_posDef`, via `convert … using 3` (the adjacency is a bare
    function, so a hand-written `2•1 - adj` picks the wrong `Sub` instance;
    `convert` references the goal's genuine `Matrix.sub` term).
- Verified sorry-free: `mult_le_one_off`, `mckay_marks_sum`, `finrank_W_ne_zero`
  (`#print axioms` = `[propext, Classical.choice, Quot.sound]`).

## Current frontier

`mckay_isAffineDynkin` is fully assembled but transitively depends on **one**
`sorry`: `mckayAdj_no_selfLoop` (conjunct 2, `rᵢᵢ = 0`).
`#print axioms mckay_isAffineDynkin` still lists `sorryAx`.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 6.1.6 parts (a),(b),(e) were already done;
part (c) is now 5/6 conjuncts complete, blocked only on the self-loop lemma.

## Next step

Pick up sub-issue **#6674**: prove `mckayAdj_no_selfLoop` via the central
`-Id ∈ SU(2)` argument. This needs Problem 4.12.8 infrastructure that does **not**
yet exist (the parent #6665 wrongly assumed it did — line 792 is only a docstring):
`-Id ∈ SU(2)` + centrality, "non-cyclic finite SU(2)-subgroup contains `-Id`",
central-involution-acts-as-scalar, and the cyclic `V = χ ⊕ χ⁻¹` splitting. Likely
itself decomposes into an SU(2)-subgroup-structure sub-issue.

## Blockers

`mckayAdj_no_selfLoop` (conjunct 2 of `mckay_isAffineDynkin`) is blocked on the
unformalized Problem 4.12.8 SU(2)-subgroup infrastructure. Tracked in #6674.
This is why #6665 lands as a partial rather than sorry-free.
