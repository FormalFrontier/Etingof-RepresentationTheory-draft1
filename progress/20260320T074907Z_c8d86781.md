## Accomplished

### Issue #1298: Reduce sorry count in Lemma5_25_3.lean from 4 to 2

1. **Proved `frob_fixed_mem_range`**: Elements z ∈ GF(q²) with z^q = z lie in range(algebraMap GF(q) GF(q²)). Uses polynomial root counting: |{z | z^q = z}| ≤ q (roots of degree-q polynomial) and |range(algebraMap)| = q (injective from size-q field), with range ⊆ S, so S = range.

2. **Proved `fieldExtEmbed_scalar_of_frob_fixed`**: Frobenius-fixed units embed as scalar matrices via `frob_fixed_mem_range` + `fieldExtEmbed_algebraMap_val`.

3. **Proved `isScalar_of_conj_isScalar`**: IsScalar is conjugation-invariant. If x⁻¹gx is scalar (= cI), then g = x(cI)x⁻¹ = cI.

4. **Proved `charW₁_parabolic`**: Parabolic elements have exactly 1 P¹ fixed point in char ≠ 2. Two cases:
   - M₀₁ = 0: affine equation has no solutions (constant = M₁₀ ≠ 0)
   - M₀₁ ≠ 0: disc = 0 ⟹ quadratic factors as a(t-r)², giving exactly 1 root

5. **Proved `parabolic_not_in_elliptic`**: No conjugate of parabolic lies in elliptic subgroup K. Chain: disc_conj_eq → algebraMap_disc_fieldExtEmbed → z^q=z → frob_fixed → scalar → contradiction.

6. **Added `monoidHom_val_mul_conj_eq_one`**: For MonoidHom f : G →* ℂˣ from finite group G, |f(g)|² = 1. Uses `Complex.norm_eq_one_of_mem_rootsOfUnity`.

7. **Fixed file organization**: Moved parabolic lemmas after their dependencies (disc_conj_eq, algebraMap_disc_fieldExtEmbed, etc.)

PR: https://github.com/kim-em/FormalFrontier-EtingofRepresentationTheory/pull/1419

## Current frontier

2 sorry'd lemmas remaining in Lemma5_25_3.lean:

1. **`normSq_complementaryChar_parabolic`** (line ~1143): Needs `charVα₁(parabolic g) * conj(charVα₁(parabolic g)) = 1`. Requires proving charVα₁ evaluates to α(eigenvalue) on parabolic elements, which needs orbit-stabilizer counting (number of upper-triangular conjugates = |Borel|). The `monoidHom_val_mul_conj_eq_one` helper is ready for the final |α(λ)|² = 1 step.

2. **`elliptic_contribution`** (line ~1300): Total elliptic contribution = q(q-1)³. Requires: (a) conjugacy class decomposition of elliptic elements, (b) character values on K (charW₁ = -1, charVα₁ = 0, Ind = ν + ν^q), (c) character orthogonality ∑_{K} ν^{q-1} = 0 for nontrivial character.

## Overall project progress

- ~193+/583 items sorry-free
- Lemma 5.25.3: 4 sorry'd helper lemmas → 2 (50% reduction)
- File compiles cleanly with 2 remaining sorries
- PR #1419 created (partial, issue #1298 marked for replan)

## Next step

- Create sub-issues for the 2 remaining sorries
- `normSq_complementaryChar_parabolic` could be tackled by proving charVα₁(parabolic) = α(eigenvalue) using orbit-stabilizer
- `elliptic_contribution` is the hardest — needs conjugacy class orbit decomposition + character orthogonality for ν^{q-1}

## Blockers

None.
