## Accomplished
Repaired PR #6194 (feat(Ch3 #6058): assemble Problem 3.8.4(i)+(ii) via Zariski specialization),
which was `CONFLICTING` against `main`. Merged `origin/main` and resolved a structural conflict:
main had independently landed the Problem 3.8.4(ii) infrastructure (`pushHom` functoriality,
`nonempty_baseChange_directSummand`, the finite-case `directSummand_of_baseChange_directSummand_finite`,
and the general-L assembly in `Problem3_8_4_General.lean`) plus the sorry-free split-injection descent
`exists_fg_subalgebra_baseChange_directSummand`, while keeping part (i) sorried in `Problem3_8_4.lean`.
The PR had built the same part (ii) under different names (`pushMap`, `exists_baseChange_directSummand`)
and proved both parts in a single new file `Problem3_8_4_Main.lean`.

Resolution: kept main's part (ii) infrastructure everywhere (Functoriality, Finite, General), and
reduced the PR's contribution to its unique value — the sorry-free general-L part (i)
`iso_of_baseChange_iso` in `Problem3_8_4_Main.lean`. Removed the duplicate part (ii) from Main.lean,
dropped the sorried part (i) from `Problem3_8_4.lean` (now a pointer comment), and removed a
duplicate sorried `exists_fg_subalgebra_baseChange_directSummand` that the auto-merge had introduced
into `Problem3_8_4_Descent.lean` (kept main's sorry-free proof). Updated `progress/items.json`
`Chapter3/Problem3.8.4` to `sorry_free`.

## Current frontier
The whole `Problem3_8_4_*` chain is now sorry-free: part (i) in `Problem3_8_4_Main.lean`, part (ii)
in `Problem3_8_4_General.lean`, both via the book's Zariski-specialization route.

## Overall project progress
Full `lake build` passes (9148 jobs). No `error:` lines, no sorries in the 3.8.4 chain. The only
noise is pre-existing: an `info:`-level PANIC in `Chapter4/Example4_9_1.lean` and unrelated sorry
warnings in `Problem3_9_3.lean`, neither touched by this PR.

## Next step
Push the merge (force-with-lease) and mark PR #6194 salvaged. Auto-merge will land it once remote CI is green.

## Blockers
None.
