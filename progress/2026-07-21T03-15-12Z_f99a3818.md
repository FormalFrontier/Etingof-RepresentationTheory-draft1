## Accomplished

Fixed DEFECT #7118 (label `feature`). Removed the unnecessary
`[FiniteDimensional k A]` hypothesis from the signature of
`Etingof.characters_linearly_independent` (Theorem 3.6.2(i)) in
`EtingofRepresentationTheory/Chapter3/Theorem3_6_2.lean`.

The book's Theorem 3.6.2(i) states linear independence of characters for an
**arbitrary** algebra `A` with finitely many pairwise non-isomorphic
finite-dimensional irreducibles. The proof uses only the density theorem
(`Etingof.density_theorem_part2`), which requires the *representations*
finite-dimensional, not `A`. The `[FiniteDimensional k A]` instance was an
unused silent specialization that made the formalized statement strictly
weaker than the book. Part (ii) (`characters_basis_semisimple`) genuinely
needs the finite/semisimple structure and is untouched.

## Current frontier

One-line signature change. `lake build
EtingofRepresentationTheory.Chapter3.Theorem3_6_2` exits 0 (1954 jobs) with
the proof unchanged. `#print axioms Etingof.characters_linearly_independent`
remains `[propext, Classical.choice, Quot.sound]`. No callers of the theorem
exist outside its own file (grep confirmed), so no downstream fix needed;
dropping an unused instance argument keeps any future caller typechecking.

## Overall project progress

Chapter 3 statement-fidelity audits are ongoing (Stage 3.x). This turn closed
a fidelity defect surfaced by the #7114 Theorem 3.6.2 audit. The formalized
part (i) now matches the book's general statement.

## Next step

Queue has three remaining review-audit issues (#7116, #7117, #7119). Next
agent should run `coordination list-unclaimed` and pick the oldest.

## Blockers

None.
