## Accomplished

Closed the Part (ii) `sorry` in `EtingofRepresentationTheory/Chapter5/Exercise5_27_3.lean`
(issue #6426): the pairwise non-isomorphism of the induced representations `V(χ, U)` in the
orbit-method classification (Theorem 5.27.1) is now proven sorry-free.

Structure of the proof (all reusing the Part (i) character-orthogonality infrastructure):

1. **Refactor** — hoisted Part (i)'s `Simple (V χ U)` into a shared `have hVsimple` so Part (ii)
   can reuse it. Added `import EtingofRepresentationTheory.Chapter5.CharEqIso` for
   `Etingof.charEq_iso` / `FDRep.char_orthonormal`.
2. **Mixed Mackey machinery** — a half-reduced cross inner-product lemma `hcross` (the `A`-orthogonality
   applied, `G`-sums not yet collapsed), generic in `(ξ₁, W₁, ξ₂, W₂)`.
3. **Existence of the intertwiner `g`** — the isomorphism equates characters, making the cross inner
   product `∑_x χ_{V χ₁ U₁}(x) χ_{V χ₂ U₂}(x⁻¹)` nonzero (norm one); if no `g` had `dualSmul g χ₁ = χ₂`,
   every orbit condition would vanish, contradiction. Yields `∃ g, dualSmul g χ₁ = χ₂`.
4. **Transport isomorphism** — with `W := transport g χ₁ χ₂ hg U₁`:
   - conjugation `s ↦ g⁻¹ s g` is a bijection `stab χ₂ ≃ stab χ₁`;
   - `W` is simple (character norm one via the bijection + `U₁` simple);
   - `χ_{V χ₂ W} = χ_{V χ₁ U₁}` (basepoint independence, a change-of-basepoint reindexing `h ↦ g h`);
   - a same-orbit Mackey collapse (`hcollapse`, ported from Part (i)) reduces the same-χ₂ cross inner
     product to `|G|·|stab χ₂|·⟨U₂, W⟩`; equal V-characters make `⟨U₂, W⟩ ≠ 0`, and
     `FDRep.char_orthonormal` forces `Nonempty (U₂ ≅ W)`.

`lake build EtingofRepresentationTheory.Chapter5.Exercise5_27_3` succeeds; no new `axiom`/`admit`.

## Current frontier

`Exercise5_27_3` now has a single remaining `sorry`: **Part (iii)** (completeness — every simple is
some `V(χ, U)`, via the sum-of-squares dimension count). That is out of scope for #6426 (a separate
future issue).

## Overall project progress

Stage 3 formalization ongoing. This closes the (i)+(ii) content of Exercise 5.27.3; only Part (iii)
of that exercise remains. Net sorry count for the file: 2 → 1.

## Next step

Open/claim a follow-up issue for Part (iii) of Exercise 5.27.3 (completeness via
`Σ dim(V χ U)² = |A ⋊ G|`). The mixed-Mackey infrastructure built here (`hcross`, `hcollapse`,
extended class functions) is local to the Part (ii) block; Part (iii) will need the dimension /
regular-representation count instead.

## Blockers

None.
