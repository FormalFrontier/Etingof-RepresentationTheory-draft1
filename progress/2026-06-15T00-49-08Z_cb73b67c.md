## Accomplished
Closed the `sorry` in `charValue_trivialCycleType_eq_frobeniusDetForm`
(`Chapter5/CharValueHookFormula.lean:489`), the capstone (step 8) of the
Frobenius route to `dim V_λ = #SYT` (#4671 / parent #4608 / #4595). The proof
chains the two landed halves:
- Sub-A1 `charValue_trivialCycleType_eq_descPochhammer_det` (#4669): reduces
  `charValue` to `(n!/∏β!) · det(descPochhammer falling-factorial matrix)`.
- Sub-A2 `descPochhammer_alternant_det_eq_prod_sub` (#4670): the reflected
  alternant det equals `∏_{i<j}(βᵢ − βⱼ)` for strictly antitone β.
`rw` both, then `push_cast; ring` rearranges `(a/b)·c = a·c/b` and unifies the
ℕ-product-then-cast denominator with the cast-then-product form.

## Current frontier
Capstone landed. `#print axioms` shows only `propext, Classical.choice,
Quot.sound` — no `sorryAx`. `lake build CharValueHookFormula` succeeds (8058 jobs).

## Overall project progress
Part A of the Frobenius dimension formula (#4608) is now complete:
steps 1–6 (#4669), step 7 (#4670), and step 8 capstone (this) all sorry-free.
This unblocks #4595 (re-point `Theorem5_17_1` at the Frobenius route, retire Wall 3).

## Next step
Pick up #4595: re-point `Theorem5_17_1` to consume
`charValue_trivialCycleType_eq_frobeniusDetForm` (and the per-row
`row_hook_gap_prod_eq_factorial` Part-B identity already in the file) to retire
the Garnir-straightening Wall 3.

## Blockers
None.
