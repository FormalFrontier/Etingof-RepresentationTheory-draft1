## Accomplished

`/work` → `/feature` session `2c4aad0a`. Claimed and completed **#5054**
(feat, Ch5 #4946 residual): discharge `schurModule_isSimple_general` and lower
the simplicity-dependent Schur-Weyl character-classification chain `Type u → Type 0`.

- **Discharged the sorry** at `SchurWeylFormalCharacterIso.lean`
  `schurModule_isSimple_general` — a one-line mirror of the ℂ `schurModule_isSimple`
  assembly: `schurModuleSubmodule_isSimple_centralizer_general` (the general-`k`
  centralizer core, landed under #4946) + the generic GL transfer
  `isSimpleModule_monoidAlgebra_GL_of_centralizer_simple`. No `hN` guard needed.
  `#print axioms schurModule_isSimple_general` → `[propext, Classical.choice, Quot.sound]`
  (no `sorryAx`).
- **Universe lowering** (the real work). The general-`k` core is intrinsically
  `Type 0`, so consuming it forces the whole simplicity-dependent chain down. The
  full build drove the closure to exactly **four** files:
  - `SchurWeylFormalCharacterIso.lean` — `universe u` removed; section variable and
    all per-lemma `(k : Type u)` binders + `S/L/V/W : Type u` carriers → `Type`.
  - `ConstituentCharacterExtraction.lean` — `universe u` removed; `Type u` → `Type`.
  - `Proposition5_22_2.lean` — section variable `(k : Type*)` → `(k : Type)`.
  - `DetTwistAlgebraic.lean` — `detTwistedSchurModuleRep_isAlgebraic` binder
    `(k : Type*)` → `(k : Type)` (consumes the now-`Type 0` `detTwistedSchurModuleRep`).
- The #4901 consumer `simpleRep_iso_schurModule_of_formalCharacter_eq` lives in the
  same file; it lowered with the section and is axiom-clean.

Full `lake build` green (8367 jobs). The issue's speculative consumer list
(FormalCharacterIso, GLRepAlgebraic, PolynomialGLDecomposition, CauchyCharacterRight,
Ch6 Problem6_1_5_OrbitSpace, Ch9 Morita/Cor9_7_3, …) did **not** require lowering —
they are either upstream of the lowered theorems or don't transitively consume them.

## Current frontier

#5054 complete; PR to follow. The remaining `sorryAx` on
`iso_of_formalCharacter_eq_schurPoly` is **pre-existing** (it transitively consumes
the false-crux #5024 or the det-quotient #4905 chain) and out of scope for #5054.

## Overall project progress

Etingof draft 1, Stage 3, Chapter 5. One of the four remaining tracked Ch5 sorries
(`schurModule_isSimple_general`) is now discharged. Remaining tracked sorries:
James leading-term (#5037), false crux (#5024, to be deleted, blocked on #5023),
det-quotient assembly (#4905 chain). The simplicity-dependent Schur-Weyl chain now
lives uniformly at `Type 0`.

## Next step

- **Worker:** #5037 (James fact 1) — actionable feature target.
- **Worker:** reviews #5043 / #5044 (unblocked, parallel).
- Sibling general-`k` classification (#5024) auto-unblocks when #5023 lands.

## Blockers

None.
