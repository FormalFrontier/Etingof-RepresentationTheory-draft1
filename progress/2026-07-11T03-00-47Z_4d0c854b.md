## Accomplished

Worked issue #6286 (Problem 5.16.3(b), `sumTranspositionsWith1_acts_scalar_iff_rectangular`).
Landed three of the four planned deliverables **sorry-free** in
`EtingofRepresentationTheory/Chapter5/Problem5_16_3.lean`:

- **D1** `spechtModuleRep_asAlgebraHom_coe`: `asAlgebraHom (spechtModuleRep n la) a` acts on `V_λ`
  by left multiplication by `a`.
- **D2** `sumTranspositionsStab_acts_scalar_iff_content_const`: the spectrum criterion —
  `C_{n-1}` acts on `V_la` by a scalar `c` ⟺ every `ν ∈ removeSquare la` has `content ν = c`.
  Forward direction extracts an embedded `V_ν` (multiplicity one) on which `C_{n-1}` acts by
  `content ν`; backward direction shows the simple constituents of the restriction are exactly the
  `V_ν, ν ∈ removeSquare la`, via `IsSemisimpleModule.sSup_simples_eq_top` + isotypic-component
  vanishing for `ν ∉ removeSquare la`.
- **D4** final assembly: `E = C_n − C_{n-1}` plus `sumTranspositions_mul_eq_content_smul` reduces
  the theorem to D2 ∘ D3 via the bijection `c ↔ content la − c`.

The final theorem is now **sorry-free in its own body**; the single remaining `sorry` is the
combinatorial helper `content_const_removeSquare_iff_rectangular` (Deliverable 3).

## Current frontier

`content_const_removeSquare_iff_rectangular (m) (la : Nat.Partition (m+1))`:
`(∃ c, ∀ ν ∈ removeSquare la, content ν = c) ↔ IsRectangular la`. Pure `Nat.Partition`/`YoungDiagram`
combinatorics; Mathlib has no "corner" API, so it is a from-scratch ~300-line development. Split out
as **#6424** with a detailed row-length proof plan (content injective on `removeSquare la` ⟹
constancy forces a singleton ⟹ unique removable corner ⟺ rectangle).

## Overall project progress

Stage 3 (formalization) ongoing. Problem 5.16.3(b) is fully proved except the one combinatorial
lemma #6424. All representation-theoretic content (branching spectrum, Schur, isotypic
decomposition) is done.

## Next step

Claim #6424 and prove `content_const_removeSquare_iff_rectangular` following the row-length plan in
its body. That closes the last gap in Problem 5.16.3(b).

## Blockers

None. #6424 depends on this partial PR merging (it edits the same file). The parent #6286 is marked
`replan`; a planner should close it with a forward link to #6424.
