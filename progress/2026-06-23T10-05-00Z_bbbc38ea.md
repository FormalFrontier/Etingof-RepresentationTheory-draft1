## Accomplished
- Claimed #5082 (DetInvElim-clean constituent-character extractor).
- Found its dependency #5081 (`CleanQuotientHelpers.lean`) is NOT on `main` — stuck in
  stale PR #5093 (predates v4.30.0-rc1 toolchain bump, no auto-merge). Verified the file
  builds on current toolchain and **vendored** it onto current main (commit) so the critical
  path is unblocked; #5093 becomes redundant.
- Wrote `Chapter5/CleanConstituentExtraction.lean` with sorry-free infrastructure:
  - `subFDRep_iSup_glWeightSpace_eq_top` — sub-of-spanning glue (wraps #5086's crux).
  - `subFDRep_isAlgebraic` — restriction of algebraic rep is algebraic.
  - `formalCharacter_eq_sum_simple_factors` — composition-series character additivity
    (finrank induction peeling a simple submodule + SES additivity).
  - `clean_simple_constituent_formalCharacter_eq_schurPoly_mem` — main theorem (currently
    `sorry`; extractor tail to be filled).

## Current frontier
- Infrastructure file building (heavy Theorem5_22_1 import chain). Main extractor still `sorry`.

## Overall project progress
- Stage 3 (Ch5 formalization). The #4905/#5072 clean re-scope: #5074, #5075, #5078, #5081,
  #5086 closed; #5082 (this) is the clean extractor; #5076 the downstream assembly.

## Next step
- Confirm infra builds, commit. Then fill the extractor tail: peel `L ↪ M` first
  (`char M = char L + char(M/L)`), expand `char(M/L)` via `formalCharacter_eq_sum_simple_factors`,
  equate with `∑ c_ν schurPoly(ν.val) = ∑ c_ν char(SchurModule ν.val)`, and kill the resulting
  vanishing character relation by dedup-by-character + the torus-trace engine
  `formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero_general`
  (non-iso of representatives via `formalCharacter_eq_of_rep_iso`).
- If the tail does not converge this session: partial-PR the infrastructure and open a focused
  sub-issue for the dedup tail.

## Blockers
- None hard. #5082 transitively needed #5081's helper which was only in stale PR #5093;
  resolved by vendoring onto current main.
