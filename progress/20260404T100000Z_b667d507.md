## Accomplished

### Close Corollary6_8_3 sorries (2→0 sorry) (#2067 → PR #2080)

Eliminated both `sorry` placeholders in `Corollary6_8_3.lean`:

1. **Recovery lemma** (`parallel_reduce_and_recover`, "both surjective" branch):
   - Added `Iso.symm`, `Iso.trans`, `Iso.transport` helpers for composing `QuiverRepresentation.Iso`
   - Proved ρ₁ ≅ ρ₂ when F⁺(ρ₁) ≅ F⁺(ρ₂) by composing:
     - `reflectionFunctorMinus_map_iso` (F⁻ functoriality, proved in PR #2074)
     - `Iso.transport` through `reversedAtVertex_twice` (double reversal = identity)
     - `Proposition6_6_6_sink` round-trip isos (transport(F⁻(F⁺(ρ))) ≅ ρ)

2. **Iteration tracking** (`Corollary6_8_3` theorem):
   - Strengthened the `suffices` to carry representations through the Coxeter iteration
   - At each step: prove indecomposability via Prop 6.6.7, prove dim vectors match via Prop 6.6.8 + `simpleReflectionDimVector_eq_simpleReflection` bridge

#### Key technical challenge: Q_rev/Q_cur instance pollution

When `let Q_rev : Quiver (Fin n) := reversedAtVertex Q_cur i` is in scope, Lean 4's instance synthesis can find Q_rev instead of Q_cur for `Quiver (Fin n)`. This caused errors in any code using dot notation (`.obj`, `.sinkMap`, `.instAddCommMonoid`) or DirectSum types.

**Solution**: Extract the "surjective from subsingleton sources → subsingleton target" proof into helper lemmas (`h₁_sink_ss_of_src`, `h₂_sink_ss_of_src`) BEFORE introducing Q_rev. These use `DFinsupp.ext` + `Subsingleton.elim` elementwise argument, elaborated cleanly with only Q_cur in scope.

## Current frontier

Corollary6_8_3.lean is now sorry-free. PR #2080 created with auto-merge enabled.

## Overall project progress

- Corollary6_8_3 was one of the 2-sorry items tracked in issue #2067
- This eliminates 2 sorries from the project total
- Estimated: ~23 sorries remaining across ~13 files (pending exact count after merge)

## Next step

1. Wait for CI on PR #2080, then verify merge
2. Continue with other open sorry-reduction issues
3. Consider running a sorry landscape scan to update the exact count

## Blockers

None for this work item. PR submitted and auto-merge enabled.
