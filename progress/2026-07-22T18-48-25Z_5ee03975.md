## Accomplished

Stage 3.7 coverage-arm audit of three sorry-free, fidelity-verified standalone
**Chapter 3** items (issue #7371): **Problem 3.3.3**, **Exercise 3.6.1**,
**Exercise 3.10.1**. Each lacked a `coverage` field in `progress/items.json`.

- Verified all three modules build sorry-free (0 sorries each) and every headline
  decl is axiom-clean (`[propext, Classical.choice, Quot.sound]`, no `sorryAx`).
- Assigned `coverage` fields (all `covered_full`) with `coverage_arm: audited`,
  `lean_decl`, `lean_file`, `last_updated`, and reconciled `coverage_note`s.
- **Problem 3.3.3** (multi-part): added a 3-entry `derived` array —
  (a) `simpleModule_prod_iff` `covered_full`; (b) `std_isSimpleModule` +
  `simpleModule_iso_std` + `finite_iso_std_pow` `covered_full`; (c) "deduce
  Theorem 3.3.1" `non_formalizable` (expository — no distinct decl; content is
  the independently-proved `Etingof.irreducible_reps_of_matrix_algebra`).
  Roll-up = `covered_full` (min over formalizable sub-parts).
- Deliverable-2 spot checks: 3.6.1 quantifies over an arbitrary submodule `W`
  (genuine `χ_V = χ_W + χ_{V/W}`); 3.10.1 is a genuine `k`-algebra iso `≃ₐ[k]`
  (generalized to `CommRing`). No sub-part weaker than the book.
- Wrote coverage report `progress/reviews/2026-07-22-ch3-standalone-coverage.md`.
- `progress/items.json` re-validated (parses, 593 items).

## Current frontier

Chapter 3 standalone coverage audit complete. No fidelity gap found → **no
follow-up feature issue**.

## Overall project progress

Stage 3.7 exercise-coverage ratchet sweep continuing, now into Chapter 3.
Siblings merged: §5.11 #7351, §5.12.5 #7349, §5.16 #7347, §5.1 #7345, §5.24
#7340. This audit closes #7371.

## Next step

Next worker: claim another unclaimed `review` issue (e.g. §5.8 #7342, §5.3.3
#7346) and continue the Stage 3.7 sweep.

## Blockers

None.
