## Accomplished
Landed deliverable 1 of #6817 (cohomological infrastructure for the Problem 8.2.8 `Ext` half):
- New file `EtingofRepresentationTheory/Chapter8/ExtCohomologyHomK.lean`:
  - `Etingof.Extₖ k A M N n : ModuleCat k` — the `k`-linear refinement of `Ext`, built on
    Mathlib's root-namespace `Ext k (ModuleCat A) n` (left-derived linear-Yoneda bifunctor,
    `ModuleCat k`-valued), the cohomological twin of `Etingof.Torₖ`.
  - `Etingof.extIsoCohomologyHomₖ` — for any `ProjectiveResolution` `P` of `M`, the iso
    `Extⁿ_A(M, N) ≅ Hⁿ(Hom_A(P•, N))` (`P.complex.linearYonedaObj k N`), from
    `ProjectiveResolution.isoExt`. The cohomological twin of `torIsoHomologyTensorRightₖ`.
- Builds sorry-free (`lake build EtingofRepresentationTheory.Chapter8.ExtCohomologyHomK`).

## Current frontier
Deliverable 2 of #6817 — the **cochain** Künneth over `k` (`up ℕ` analogue of
`kunnethChainComplexNat`) — was split into #6825. It is a ~500-line mirror of
`Chapter7/KunnethChainComplexNat.lean` reindexing via `ComplexShape.embeddingUpNat` (n ↦ n,
support ℤ≥0) instead of `embeddingDownNat`, with its own Koszul-sign lemma. The
`sigmaIsoOfInjOfIsZeroCompl` coproduct-support API in the chain file is embedding-agnostic and
reusable.

## Overall project progress
Problem 8.2.8 `Tor` half is proved sorry-free. The `Ext` half stack is being assembled in parallel:
Hom-tensor iso (#6816, claimed), left-module external-tensor resolution (#6822), cohomological
rearrange iso (#6823), cochain Künneth (#6825, new), assembly (#6818). Deliverable 1 of #6817 (the
`Ext = Hⁿ(Hom(P•,N))` hookup) now landed.

## Next step
Claim #6825 and prove `kunnethCochainComplexNat` by mirroring `KunnethChainComplexNat.lean` with
`embeddingUpNat`. Then #6818 (assembly) can consume `extIsoCohomologyHomₖ` + the cochain Künneth
+ the Hom-tensor iso (#6816).

## Blockers
None. #6817 landed partial (deliverable 1); deliverable 2 tracked as #6825.
