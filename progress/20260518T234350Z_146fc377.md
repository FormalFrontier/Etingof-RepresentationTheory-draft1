## Accomplished

Partial work on issue #2932
(`non_adjacent_branches_leaf_case_per_kQ` proof body).

* **Signature strengthening** (Deliverable 1): added
  `h_no_adj_branch_w : ∀ u, adj w u = 1 → vertexDegree adj u < 3`
  to `non_adjacent_branches_leaf_case_per_kQ` in
  `Chapter6/FieldGenericNonAdjacentBranches.lean`. The docstring
  notes that the caller `non_adjacent_branches_infinite_type_per_kQ`
  (#2923, currently blocked) will derive both `h_no_adj_branch` and
  `h_no_adj_branch_w` from the negated `h_adj_exists` in its scope.

* **Embedder API stubs** (Deliverable 3, stub-only): added
  `embed_etilde6_in_tree_per_kQ` (7 vertices, T(2,2,2)) to
  `Chapter6/FieldGenericETilde6.lean` and
  `embed_etilde7_in_tree_per_kQ` (8 vertices, T(1,3,3)) to
  `Chapter6/FieldGenericETilde7.lean`. Both have full signatures
  matching the `embed_t125_in_tree_per_kQ` pattern
  (`FieldGenericT125.lean:71`) with edge hypotheses, distinctness
  lattice, and `(F, Q, hOrient)` parameters. Bodies are `sorry`.

* **Decomposition** of the remaining work:
  * #2937 — fill `embed_etilde6_in_tree_per_kQ` body (mirror
    `embed_t125_in_tree_per_kQ` for 7 vertices, ~21 non-edges).
  * #2938 — fill `embed_etilde7_in_tree_per_kQ` body (8 vertices,
    ~30 non-edges).
  * #2939 — Phase 1 setup + Phase 2 dispatch for
    `non_adjacent_branches_leaf_case_per_kQ` body (uses the three
    embedders). Depends on #2937 and #2938 (`blocked`).

Sorry count delta: +2 (the two new embedder stubs). The leaf-case
sorry was already counted.

## Current frontier

Wave 62 — per-(F, Q) infinite-type assembly for the non-adjacent
branches leaf case. The signature is now finalised; downstream
work proceeds in three independent threads (two embedders +
dispatch).

## Overall project progress

Stage 3 (formalisation). Same as start of session — sorry count
slightly increased due to scaffolding additions, but those are
contracts the next sessions will fill.

## Next step

Three parallel directions:

1. Pick up #2937 (Ẽ₆ embedder body) — mechanical port of
   `embed_t125_in_tree_per_kQ` for 7 vertices.
2. Pick up #2938 (Ẽ₇ embedder body) — same pattern for 8 vertices.
3. (Blocked until #2937 and #2938 land) #2939 — the full
   case-dispatch + Phase 1 setup port from
   `InfiniteTypeConstructions.lean:9779-10120`.

## Blockers

None. #2939 is blocked on #2937 and #2938 but those are
independent and ready for parallel work.
