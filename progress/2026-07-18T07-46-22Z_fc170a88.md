## Accomplished

Closed #6940 — `affine_two_branch_deleted_isD` is now **sorry-free** (the sole
`sorry` in `Chapter6/Problem6_1_3_continued_tildeE.lean` is gone). This is the
classification crux of `affine_tree_two_branch_iso` (two branch vertices ⟹ D̃ₙ).

Work done this turn:
- **Reorder** (`aa7d34d7`): moved `affine_two_branch_deleted_isD` +
  `affine_tree_two_branch_iso` to *after* `affine_two_branch_fork_leaves`
  (landed on `main` via #6949) so the former can consume the latter. No
  semantic change; both consumers of the moved lemmas are later in the file.
- **Classification, steps 1-4** (`d5bd66f0`): delete the leaf
  (`affine_delete_leaf_isDynkin`), transport `w`'s two fork-leaves through the
  deletion, classify via `branch_classification`, rule out E-types with
  `dynkinType_eq_D_of_branch_two_leaves` ⟹ survivor is `Dₖ` with `4 ≤ k`.
  New helper lemmas: `vertexDegree_map_equiv` (vertex-degree transport through a
  graph isomorphism) and `neighborFinset_delete` (neighbours after a leaf
  deletion).
- **Localization, step 5** (`6633f594` after rebase): `v'` (the survivor of `v`)
  is adjacent to `v`'s *other* leaf and has degree 2, so it is the unique
  degree-2 vertex of `Dₖ` adjacent to a leaf, namely index `1`. New helper
  lemmas: `Dk_deg_eq_filter_range` and `Dk_vertexDegree` (closed-form `Dₖ`
  vertex degrees: branch `k-3` has degree 3, leaves `0, k-2, k-1` degree 1, rest
  degree 2).

`#print axioms affine_two_branch_deleted_isD` and `affine_tree_two_branch_iso`
both give exactly `[propext, Classical.choice, Quot.sound]`.

## Current frontier

`affine_tree_two_branch_iso` is complete and sorry-free. The affine Dynkin
classification `affine_dynkin_classification` (end of the same file) now has its
two-branch case fully discharged.

## Overall project progress

Stage 3 formalization. `Chapter6/Problem6_1_3_continued_tildeE.lean` is
sorry-free after this turn. The D̃ₙ (two-branch) leg of the affine classification
is done; the one-branch (Ẽ) leg (`affine_tree_one_branch_iso`) is already present
in the file.

## Next step

Merge the PR for #6940. Then the affine classification file is fully closed;
remaining open feature work is in Ch4 (#6864 so3 polyhedral realizations,
#6946 dihedral swap — both claimed by other agents) and Ch8 (#6898 Ext final
assembly).

## Blockers

None. The dependency #6949 (`affine_two_branch_fork_leaves`) merged to `main`
mid-turn; my branch was rebased onto `main` to drop the duplicate commits.
