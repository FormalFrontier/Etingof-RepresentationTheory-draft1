## Accomplished

Proved `Etingof.Problem_8_2_6_i_tor` (issue #6109) sorry-free: `Tor₀ᴬ(M, N) ≅ M ⊗_A N`.

The remaining input was `PreservesFiniteColimits (tensorRightFunctor A N)` (right-exactness of
`- ⊗_A N` for the custom quotient tensor `tensorOver`). I supplied it via the **tensor–hom
adjunction**, in a new file `EtingofRepresentationTheory/Chapter8/Definition8_2_3_RightExact.lean`:

- `homMopModule`: the right `A`-module (= left `Aᵐᵒᵖ`-module) structure on `N →+ B`,
  `(x • f) n = f (x.unop • n)`.
- `homFunctorR A N : AddCommGrpCat ⥤ ModuleCat Aᵐᵒᵖ`, `B ↦ (N →+ B)` — the right adjoint.
- `homEquivToFun` / `homEquivInvFun`: curry/uncurry through the balanced quotient
  (`TensorProduct.liftAddHom` + `QuotientAddGroup.lift`), with the balancing identity
  `(x • m) ⊗ n = m ⊗ (x.unop • n)` (`mk_smul_tmul`).
- `tensorHomAdjunction : tensorRightFunctor A N ⊣ homFunctorR A N` via `Adjunction.mkOfHomEquiv`;
  the equiv laws and both naturalities are definitional (`rfl` after `tensorOver_hom_ext`).
- `tensorRightFunctor_preservesFiniteColimits` from `Adjunction.leftAdjoint_preservesColimits`.

Then `Problem_8_2_6_i_tor := ⟨((tensorRightFunctor A N).leftDerivedZeroIsoSelf).app M⟩`.

`#print axioms Etingof.Problem_8_2_6_i_tor` → `[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

## Current frontier

`lake build EtingofRepresentationTheory.Chapter8` is clean. New file wired into `Chapter8.lean`
and imported by `Problem8_2_6.lean`.

## Overall project progress

Phase 3 formalization ongoing. Problem 8.2.6 part (i) is now fully proved (both `Ext⁰` via #6103
and `Tor₀` via #6109). Parts (ii)–(v) remain statement-level sorries.

## Next step

Problem 8.2.6 parts (ii)–(v) are still open (long exact sequences, balancing theorem). The new
`homFunctorR` / `tensorHomAdjunction` infrastructure is reusable for the `Tor` long exact
sequences and balancing.

## Blockers

None.
