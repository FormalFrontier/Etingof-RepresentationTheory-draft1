## Accomplished
Stage 3.7 coverage-arm audit of **Chapter5/Exercise5.27.3** ("Deduce parts
(i)-(iii) of Theorem 5.27.1 from part (iv)"), issue #7333. Report-only.

Audited the single headline `Etingof.Exercise5_27_3`
(`EtingofRepresentationTheory/Chapter5/Exercise5_27_3.lean:92`) at sub-part
granularity. Verdicts, recorded as durable state in `progress/items.json`:

- **(i)** irreducibility (`Simple U → Simple (V χ U)`) — `covered_full`
- **(ii)** pairwise non-isomorphism (transport-up-to-`dualSmul` criterion) — `covered_full`
- **(iii)** completeness (exhaustive over all simple `FDRep ℂ (A ⋊[φ] G)`) — `covered_full`
- Roll-up `coverage` on the parent = `covered_full`; `coverage_arm: audited`.

Key fidelity checks passed:
- The `character_formula` hypothesis is the genuine part-(iv) explicit formula
  (`|G_χ|⁻¹ Σ_h [hgh⁻¹∈G_χ] χ(φ(h)a)·χ_U(hgh⁻¹)`), not a weaker stand-in.
- `_htransport` faithfully pins `transport` up to iso (its character equals `U`'s
  at the conjugate; `g⁻¹sg ∈ stab χ₁` for all `s ∈ stab χ₂` since `stab χ₂ =
  g(stab χ₁)g⁻¹`), so (ii) is non-vacuous — the #6375/#6383 statement-defect fix.
- `V` is pinned up to iso by `character_formula`, so (i)/(iii) are non-vacuous.
- All three are exposed headline conjuncts (not proof-local `have`s).

Verified sorry-free build and `#print axioms Etingof.Exercise5_27_3 =
[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

All three sub-parts `covered_full`, so per deliverable 3 no follow-up `feature`
issue was opened.

## Current frontier
Ch5 coverage-arm sweep. Exercise 5.27.3 now recorded. Siblings still in flight /
unclaimed: Problem 5.24.2 (#7334), Problem 5.24.1 (#7328), Exercise 5.27.2
(#7320), Problem 4.12.7 (#7306).

## Overall project progress
Stage 3.7 coverage-arm audits proceeding §-by-§ across Ch4/Ch5. This item moves
from `coverage: null` to `covered_full` with sub-part `derived` records.

## Next step
Continue the Ch5 coverage-arm sweep: audit Problem 5.24.2 (#7334) next.

## Blockers
None.
