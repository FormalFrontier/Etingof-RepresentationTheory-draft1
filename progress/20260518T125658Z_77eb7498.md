# Progress — feature session 77eb7498 (Issue #2898)

## Accomplished

Implemented `adjacent_branches_infinite_type_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean`
(D2.adjacent sub-deliverable of #2877). The new theorem is the
per-(F, Q) version of `adjacent_branches_infinite_type`
(`InfiniteTypeConstructions.lean:4764-4912`), embedding D̃₅ on the two
adjacent degree-3 branch points plus their 4 other neighbours and
dispatching through `subgraph_infinite_type_transfer_per_kQ` to
`d5tilde_not_finite_type_per_kQ`.

Structure follows the recipe from
`progress/reviews/2026-05-18-degree4-per-kQ-placement.md`, D3 "Pattern
recipe", carrying `[IsAlgClosed F]` per the audit's rule of thumb
because the dispatch leaf `d5tilde_not_finite_type_per_kQ` requires it.

- **Signature**: appends `(F : Type) [Field F] [IsAlgClosed F]`,
  `(Q : @Quiver.{0,0} (Fin n))`,
  `[∀ a b, Subsingleton (@Quiver.Hom (Fin n) Q a b)]`,
  `(hOrient : @Etingof.IsOrientationOf n Q adj)` to the `_kQ`-free
  original's argument list.
- **Body**: structurally identical to the `_kQ`-free original through
  the neighbour extraction, 15 distinctness facts, 9 non-edges, and
  injectivity / adjacency verification of the `Fin 6 ↪ Fin n`
  embedding.
- **Dispatch**: the final `subgraph_infinite_type_transfer ... d5tilde_not_finite_type`
  call is replaced by
  `subgraph_infinite_type_transfer_per_kQ φ F Q
   (d5tilde_not_finite_type_per_kQ F (restrictOrientationViaEmb φ Q)
     (restrictOrientationViaEmb_isOrientationOf φ hembed hOrient))` —
  the single-leaf dispatch shape used by
  `chordless_cycle_infinite_type_per_kQ`
  (`FieldGenericCycle.lean:373-390`).
- **Placement**: after `d5tilde_not_finite_type_per_kQ` (line 1027)
  and before `end Etingof`. No new imports — `FieldGenericD5Tilde.lean`
  already imports `InfiniteTypeConstructions` (for `d5tildeAdj`,
  `acyclic_no_triangle`, `acyclic_path_nonadj`) and
  `FieldGenericInfiniteType` (for `subgraph_infinite_type_transfer_per_kQ`
  and `restrictOrientationViaEmb_isOrientationOf`).
- **Attribute prefix**: `attribute [-instance]
  CategoryTheory.CategoryStruct.toQuiver CategoryTheory.ReflQuiver.toQuiver in`
  applied per recipe step 4.
- **maxHeartbeats**: `set_option maxHeartbeats 3200000 in` carried
  through from the `_kQ`-free original (same 15 distinctness +
  36-case `fin_cases` workload). Reason comment added to satisfy the
  style linter.

### Incidental cleanup

Removed `private` from `acyclic_no_triangle`
(`InfiniteTypeConstructions.lean:4713`) and `acyclic_path_nonadj`
(`InfiniteTypeConstructions.lean:4743`). These are general-purpose
acyclic-graph utility lemmas; making them visible is the cleanest way
for the per-(F, Q) wrapper to call them from a downstream file. No
naming conflicts (sole occurrences in the codebase). All existing
Chapter 6 builds continue to pass.

This is exactly the kind of API-visibility cleanup the per-(F, Q)
pattern needs as it spreads across files — the D2.degree4 and D2.cycle
sub-deliverables didn't hit it because their bodies were structurally
simpler and didn't reach for private helpers; D2.single and D2.nonadj
likely will, and the same un-`private` move will apply to whatever
helpers they reach for.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde`
  — clean (warnings limited to pre-existing sorries on lines 798, 974;
  no new warnings on the new theorem).
- `lake build EtingofRepresentationTheory.Chapter6` — clean (no
  downstream regressions from un-`private`-ing the acyclic helpers).
- No `sorry` in the new theorem body.
- Net sorry-count delta on `main`: **0** (no new sorries; no existing
  sorries closed — the inherited proof-level sorry chain through
  `d5tildeRep_kQ_isIndecomposable` is pre-existing and tracked by
  #2853 / #2851, out of scope per the issue body).

## Current frontier

D2.adjacent landed (PR pending creation). Remaining D2 sub-deliverables
under #2877: D2.single, D2.nonadj, D2.outer, plus D3
(`not_posdef_infinite_type_per_kQ` outer assembly + Theorem 2.1.2
bridge).

## Overall project progress

Stage 3 (Lean formalisation). Wave-62 substantive PRs since wave-61
summarize: 3 (#2891 = D2.degree4, #2897 = D2.cycle, this D2.adjacent
landing). #2894 = documentation-only audit verdict, does not count.
Sorry-count delta from this PR: **0**.

## Next step

Planner can pre-split the next D2 sub-deliverable. Per the audit
verdict (#2894) D3 recommendations table, the remaining splits in
priority order are:

- **D2.outer** → new file `FieldGenericNotPosdef.lean`. Cheapest
  remaining; assembles the disjunction outcomes into the final
  `not_posdef_infinite_type_per_kQ` statement.
- **D2.single** (`single_branch_not_posdef_infinite_type_per_kQ`,
  ~300 lines, needs `etilde6_*`/`etilde7_*`/`t125_*_per_kQ`,
  `[IsAlgClosed F]`).
- **D2.nonadj** (`non_adjacent_branches_infinite_type_per_kQ`, ~900
  lines, audit recommends dedicated file `FieldGenericNonAdjacent.lean`).

The same single-leaf dispatch shape used here applies to D2.single
(per-leaf wrappers exist already); D2.nonadj is structurally more
involved and may want its own audit pass before pre-splitting.

## Blockers

None for this work item. Standing constraints unchanged.
