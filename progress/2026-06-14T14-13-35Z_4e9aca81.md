## Accomplished

Session type: feature (/work → /feature on #4643).

- Triaged the unclaimed queue. **Flagged #4528 as unworkable**: it asks to fill
  the body of `starRep_kQ_isIndecomposable`, but #4597 documents a
  machine-checked refutation (`starRep_kQ_reversedLeaf3_decomposable`, PR #4589)
  proving that lemma false. Added a triage comment + `replan` label.
- Enabled auto-merge on PR #4646 (CI was in progress); it has since merged.
- Confirmed Mathlib PR #38583 (`eq_of_eval_eq_on_gl`) **merged via Bors** — so
  tracker #2564 is now actionable (but is a low-priority, risky revision bump).
- Claimed and **decomposed #4643** (Ch5 #4598 core, `det⁻¹` elimination):
  - Added a sorry-free `def scalarGL k N t = Matrix.diagonal (fun _ => (t:k))`
    (the `t • 1` of the strategy).
  - Rewrote `polynomialRep_homogeneous_hpoly'` as a **sorry-free assembly** over
    two private helpers, matching the issue's own "two separable pieces":
    - `scalarGL_acts_as_pow` (Piece 1): `M.ρ (scalarGL t) = t^n • id`. Sorry'd;
      its genuine content is the general weight-span lemma
      `⨆ μ, glWeightSpace k N M μ = ⊤` for arbitrary `IsAlgebraicRepresentation`
      (only known for Schur modules). → sub-issue **#4653**.
    - `hpoly'_of_scalarGL_action` (Piece 2): `det⁻¹` elimination given the scalar
      action (independent of Piece 1 — takes it as a hypothesis). Sorry'd.
      → sub-issue **#4654**.
- `lake build EtingofRepresentationTheory.Chapter5.PolynomialRepEmbedding` passes.

## Current frontier

`polynomialRep_homogeneous_hpoly'` is a clean assembly; the two mathematical
pieces are isolated, documented, and independently claimable (#4653, #4654).

## Overall project progress

Stage 3 (formalization). Schur-Weyl chain: #4598 interface landed; #4643's core
now decomposed into Piece 1 (#4653) + Piece 2 (#4654). #2482 (Schur-Weyl #5
assembly) consumes the equivariant embedding downstream.

## Next step

Claim **#4653** (general torus diagonalisability / weight-span — the reusable
GL_N rep-theory lemma; the harder, higher-leverage piece) or **#4654** (the
det⁻¹-elimination assembly, self-contained given the scalar-action hypothesis).

## Blockers

None new. #4653 is genuine GL_N representation-theory infrastructure
(diagonalisability of algebraic torus actions); budget accordingly.

## Quality metric delta

PolynomialRepEmbedding.lean: 1 sorry → 2 sorries (decomposition: +1 sorry, but
the monolithic core split into two scoped, documented, independently-claimable
sorries plus one new sorry-free `def`).
