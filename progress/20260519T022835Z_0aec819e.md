## Accomplished

- Claimed issue #2944: audit of PR #2943
  (`non_adjacent_branches_infinite_type_per_kQ` outer assembly).
- Discovered `main` is build-broken on
  `EtingofRepresentationTheory.Chapter6.FieldGenericAssembly` at
  commit `7dcc9f3`: PR #2941 (3 min after #2943 merged) strengthened
  the signature of `non_adjacent_branches_leaf_case_per_kQ` by
  inserting `h_no_adj_branch_w` without updating the three call
  sites #2943 had just added.
- Fixed `FieldGenericAssembly.lean:150-160` by deriving
  `h_no_adj_branch_w` inline from the post-`push_neg`
  `h_adj_exists` (same `omega` pattern used at line 424 to derive
  `h_no_adj`) and threading it into all three dispatches.
- Removed stale "sorry-bodied" doc-comment at lines 42-44 (body was
  filled by #2943).
- Verified `lake build EtingofRepresentationTheory.Chapter6.FieldGenericAssembly`
  green (8048/8048 jobs).
- Wrote audit report
  `progress/reviews/2026-05-19-non-adjacent-branches-outer-assembly-per-kQ.md`
  with **Verdict: FAIL on D3 (build-breaking call-site stale-arg),
  fix landed in same PR**. D1, D2, D4, D5 all PASS.

## Current frontier

Audit PR ready to push for #2944. The fix is the kind of stale-arg
race that the planner can prevent by coupling signature-strengthening
issues with caller-update deliverables in the same issue body.

## Overall project progress

`main` is now green again on this file. The forward direction of
Theorem 2.1.2 (`Chapter2/Theorem2_1_2.lean:153-179`) remains
end-to-end-derivable via `not_posdef_infinite_type_per_kQ` →
`acyclic_branch_not_posdef_infinite_type_per_kQ` →
`non_adjacent_branches_infinite_type_per_kQ` →
`non_adjacent_branches_leaf_case_per_kQ` (#2939 still in flight,
Phase 1/2 partially landed via #2952/#2956/#2958) plus
`etilde6_not_finite_type_per_kQ` (still sorry-bodied).

## Next step

- Worker on #2948 (audit of #2933 stub + #2941 signature delta) —
  this audit's discovery is highly relevant: the #2941 signature
  delta is exactly what broke `main` here, and that audit's D5
  build-sanity step should reproduce the breakage independently if
  run pre-this-PR. Post-this-PR, that audit should PASS.
- Worker on #2949 (Ẽ₆/Ẽ₇ embedder body audit) is independent of
  the build break and can proceed.
- Continue body fills for `non_adjacent_branches_leaf_case_per_kQ`
  via the Case C/D/E sub-issues (decomposed from #2939 by the
  planner cycle `25c3fb51`).

## Blockers

None. Recommendation noted in the audit report: planners should
couple signature-strengthening issues with explicit caller-update
deliverables (or pair them with a follow-up issue) to prevent the
race condition that broke `main` here.
