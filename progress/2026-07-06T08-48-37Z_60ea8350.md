## Accomplished

One-shot work session on issue #5894 (statement pass for the remaining open-ended
Chapter 2 exercises). Wrote faithful Lean 4 statements (sorry proofs, per the
statement-pass phase) for **7 of the 12** listed exercises, each in a new file added to
`EtingofRepresentationTheory/Chapter2.lean`; the full Chapter 2 aggregator builds
(8708 jobs) with only the expected sorry warnings. `progress/items.json` updated to
`statement_formalized` for these 7.

Formalized:
- **Problem 2.7.4** (`Problem2_7_4.lean`) — Weyl algebra: char 0 has no nonzero finite-dim
  rep and is a simple ring; char p has `x^p, y^p` central with center `k[x^p,y^p]`, and
  irreducibles are `p`-dimensional. Uses the repo's `Etingof.WeylAlgebra`.
- **Problem 2.8.11** (`Problem2_8_11.lean`) — Hilbert series of the polynomial, free,
  exterior, and path algebras: rendered as graded-dimension formulas
  (`C(n+m-1,m-1)`, `m^n`, `C(m,n)`, adjacency-matrix path counts) plus power-series
  generating functions.
- **Exercise 2.11.2** (`Exercise2_11_2.lean`) — `V ⊗ W` as the quotient of the free abelian
  group `FreeAbelianGroup (V×W)` by the bi-additivity/`k`-balancing relations; `AddEquiv`
  to `V ⊗[k] W`.
- **Exercise 2.11.5** (`Exercise2_11_5.lean`) — base change: the `L`-algebra structure on
  `A ⊗_K L` (fully constructed via `Algebra.TensorProduct.rightAlgebra`, structure map
  identified) and the natural `(A ⊗_K L)`-module structure on `V ⊗_K L`.
- **Problem 2.16.2** (`Problem2_16_2.lean`) — 2-dim Lie algebra `[X,Y]=Y` as a subalgebra of
  `gl(2)`; char 0 irreducibles are 1-dimensional (with `Y` acting as 0); Lie's theorem
  fails in char p.
- **Problem 2.16.3** (`Problem2_16_3.lean`) — `g_n = ⟨x,y | ad(x)²y = ad(y)^{n+1}x = 0⟩` via
  free-Lie-algebra quotient; dims `g_1=3, g_2=4, g_3=6` (Serre relations for A₂/B₂/G₂
  positive parts) and `g_4` infinite-dimensional (affine).
- **Problem 2.16.4** (`Problem2_16_4.lean`) — `sl(2)` in char p>2: the sharp dimension bound
  (every irreducible finite-dim rep has dim ≤ p, achieved). Full highest-weight
  parametrization deferred.

## Current frontier

7/12 exercises formalized and committed on branch `agent/60ea8350` (3 commits). The
remaining 5 need substantial new infrastructure or source clarification (see Blockers).

## Overall project progress

Phase 3 statement pass. This session advances Chapter 2 coverage of the open-ended
exercises from #5894. Combined with #5895, most of the Chapter 2 exercise list now has
faithful Lean statements.

## Next step

A planner should open follow-up issues for the 5 deferred exercises (2.7.5, 2.8.6, 2.9.5,
2.11.7, 2.16.5) — details and required infrastructure are in the issue #5894 comment.
The 7 formalized statements are ready for the proof phase.

## Blockers

Five exercises deferred (documented on issue #5894), each needing new definitions or
source clarification rather than a proof:
- **2.7.5** (q-Weyl algebra) — repo's Prop 2.7.1 deliberately omitted the q-Weyl algebra;
  needs its construction (invertible generators, `xy = q·yx`) before center/ideals/irreps
  can be stated.
- **2.8.6** (path-algebra presentation) — needs the presented free-algebra-on-(vertices ⊔
  edges) quotient and an `AlgEquiv` to `Etingof.PathAlgebra`; also sensitive to the
  source/target multiplication-order convention noted in `Definition2_8_4.lean`.
- **2.9.5** (example (1) is a special case of example (5), n=3) — the example numbering in
  the extracted blobs is inconsistent with the "n=3" index; the correct §2.9 example list
  cannot be recovered from the available blobs. Blocked on source.
- **2.11.7** (tensor-hom adjunction) — same statement as Problem 2.11.6(b) (the blob is its
  truncated tail): `Hom_B(V, Hom_C(W,X)) ≅ Hom_C(W ⊗_B V, X)` as (A,D)-bimodules. The
  repo's `Problem2_11_6.lean` audit already concluded this needs a full bimodule /
  relative-tensor Hom API — a large standalone effort.
- **2.16.5** (U_q(sl(2)) irreps) — needs constructing the quantum group `U_q(sl(2))`
  (free algebra on `e,f,K,K⁻¹` modulo the given relations) plus highest-weight-module
  machinery for the root-of-unity/non-root-of-unity classification.
