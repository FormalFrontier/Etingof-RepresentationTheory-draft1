## Accomplished

Feature session — claimed #4905 (`feat(Ch5 #4896 assembly): discharge
quotDetRep_irreducible_constituent_lastWeight_zero`). Its three blocking glue
sub-issues (#5062/#5063/#5064) are all merged, so the previous handoff
recommended it as the next step. On attempting the assembly I discovered the
prescribed proof route is a **genuine import/proof circularity**, confirmed by
`lake build` (`error: build cycle detected`). Routed the issue to `replan` with a
full diagnosis (comment + skip).

### The circularity (every edge a direct proof-term use, verified in source)

```
part-(a)  [CauchyDetQuotient.lean:112, the sorry]
  ⇐ extractor  simple_constituent_formalCharacter_eq_schurPoly_mem   [ConstituentCharacterExtraction.lean:126]
  ⇐ decompose_polynomial_gl_rep                                       [PolynomialGLDecomposition.lean:529]
  ⇐ polynomial_homog_rep_equivariant_embedding (algebraic → polynomial) [PolynomialRepEmbedding.lean:1129]
  ⇐ detInv_elim_of_polynomial = DetInvElim.detInv_elim               [PolynomialRepEmbedding.lean:1068 → DetInvElim.lean:165]
  ⇐ kernelLemmaK_evalAtGL / kernelLemmaK                             [DetInvElim.lean:275 → KernelLemmaK.lean:94]
  ⇐ kernelLemmaK'_submodule                                          [KernelLemmaK.lean:168]
  ⇐ quotDetTwist_nonzero_subrep_has_neg_weight                       [KernelLemmaKPrimeAssembly.lean:166→70]
  ⇐ part-(a)                                                          [KernelLemmaKPrimeAssembly.lean:100-101]
```

All three prescribed ingredient files (`CauchyDetQuotientGrading`,
`ConstituentCharacterExtraction`, `QuotDetDegreeAlgebraic`) transitively import
`CauchyDetQuotient` through `KernelLemmaKPrimeAssembly`. Adding them as imports to
discharge the sorry is a build cycle.

### Why it is genuine, not a file-placement artifact

- **Extractor ⇐ DetInvElim is essential.** The extractor takes
  `IsAlgebraicRepresentation` and must convert algebraic → bare-polynomial
  (embed in `V^⊗n` with no `det⁻¹`); that conversion *is* `DetInvElim`. The
  `Sym²(V)⊗det⁻¹` counterexample (`PolynomialRepEmbedding.lean:1030-1038`) shows
  it is genuine content. No polynomial-only classification lemma exists — every
  classification entry point requires `IsAlgebraicRepresentation`.
- **K ⇐ K′ is essential.** `kernelLemmaK` needs the GL-invariant
  `kernelLemmaK'_submodule`; the `span{X₁₁X₂₂·det⁻¹}` counterexample
  (`KernelLemmaK.lean:107-110`) rules out any per-vector substitute. K′ ⇐ part-(a)
  directly.

The universe obstruction the issue flagged (classification chain intrinsically
`Type 0`, so part-(a) would have to specialize `k : Type*` → `k : Type` and
cascade through `KernelLemmaKPrimeAssembly → KernelLemmaK → DetInvElim →
PolynomialRepEmbedding`) is real but **secondary** — moot until the circularity
is resolved.

## Current frontier

#4905 is `replan`. The glue facts #5062/#5063/#5064 are merged and valid as
statements, but they feed the circular algebraic-rep extractor route. Part-(a)
remains a `sorry` at `CauchyDetQuotient.lean:124`.

## Overall project progress

Stage 3 (formalization) ongoing. The #4896/#4905 part-(a) deliverable
(constituents of `A/det` have `ν_N = 0`) is **blocked on a proof-route replan**:
the chosen Schur-Weyl-extractor route is circular with the kernel-lemma chain it
ultimately feeds. The Cauchy/right-`GL_N` degree-component character chain
(#5065/#5066/#5064) is itself complete and sorry-free; it is the *consumer* of
the extractor that creates the loop, not the loop's cause.

## Next step

A planner should re-scope #4905 around the **direct `GL_N × GL_N`-equivariant
Cauchy decomposition of `k[Xᵢⱼ]`** + the `det·A ≅ A⊗χ` shift
(`detShiftLinearEquiv_intertwine`, sorry-free), matching part-(a)'s own docstring
(`CauchyDetQuotient.lean:97-102`). That route classifies `A/det` constituents
directly from the Cauchy structure and never invokes det⁻¹-elimination, so it
avoids the loop. The merged glue (#5062-#5064) can be retained as auxiliary
facts but cannot be the spine of part-(a).

Remaining other unclaimed work: #5037 (James leading-term combinatorial epic —
hard, 4+ prior failed sessions) and #2841 (Mathlib pin bump to v4.31.0 on
pre-existing branch `agent/64f2e40f-bump`).

## Blockers

#4905 cannot be assembled via the prescribed extractor route (genuine
circularity). Needs a planner to commission the direct-Cauchy route. Documented
in issue comment https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/issues/4905#issuecomment-4774204433
