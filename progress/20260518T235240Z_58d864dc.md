# Progress — review session 58d864dc

## Accomplished

Claimed and executed review issue #2935: audit of
`single_branch_leaf_both_extend_t122_per_kQ`
(`Chapter6/FieldGenericTpqr.lean:64-491`, +531 lines / PR #2912)
against the universal T(1, 2, 2) = D₅ posdef contradiction inline at
`Chapter6/InfiniteTypeConstructions.lean:7964-8352`. This was the
largest unaudited wave-62 PR.

**Verdict: PASS on all five deliverables.** No code changes
recommended. No follow-up issues filed.

Audit report posted at
`progress/reviews/2026-05-19-t122-leaf-both-extend-per-kQ.md`. Each
deliverable carries explicit line-pair citations between per-(F, Q)
and universal:

- **D1 (Signature fidelity):** Six named vertices, five adjacency
  hypotheses, three degree hypotheses (`h_leaf_deg, hb₂_deg1,
  hb₃_deg1` — the latter two hoisted to caller because `h_deg_le2` is
  not in the per-(F, Q) scope), five distinctness hypotheses, three
  `Finset` equalities (`hS₀_eq, hb₂_eq, hb₃_eq` spelled out via
  `Finset.univ.filter (adj v₀ · = 1)` rather than the universal's
  `set S₀`), plus `h_not_posdef`, `hconn`, `h_acyclic`, the standard
  `hsymm/hdiag/h01` adjacency bundle, and the canonical per-(F, Q)
  carriage `(F : Type) [Field F] [IsAlgClosed F] (Q) [Subsingleton …]
  (hOrient)`. `[IsAlgClosed F]` present and consistent with the four
  sibling per-(F, Q) leaves in the same file.

- **D2 (Quadratic form expansion):** RHS polynomial, the 35 `simp
  only` arguments, the 12 underlying `Ne` facts, the closing `ring`,
  and the `set V := x v₀; set L := x leaf; …` variable bindings are
  byte-identical between universal and per-(F, Q). The only
  divergence is the LHS: universal uses `QF adj x` then `unfold QF`,
  per-(F, Q) writes the spelled-out `dotProduct x ((2 • 1 -
  adj).mulVec x)` directly — forced because `QF` is `private abbrev`
  in `InfiniteTypeConstructions.lean:4131`.

- **D3 (Sum-of-squares closure):** Coefficients (30/10/5/3/3/45),
  square expressions, `nlinarith` hint list with `sq_nonneg`
  invocations, the `B₃ → A₃ → B₂ → A₂ → L → V` peeling order, and
  the final `[exact hV; exact hL; exact hA₂; exact hB₂; exact hA₃;
  exact hB₃]` case dispatch are all byte-identical.

- **D4 (Neighbor-list lemmas):** All six lemmas (`hv₀_nbrs,
  hleaf_nbrs, ha₂_nbrs, hb₂_nbrs, ha₃_nbrs, hb₃_nbrs`) present in
  the same order with the same derivations. No additional helper
  lemmas introduced. Downstream `h_closed, h_all_named, huniv,
  h_sum`, and the six `adj_eq` row equations are byte-identical
  between universal and per-(F, Q).

- **D5 (Carriage discharge):** `let _ := F; let _ := Q; let _ :=
  hOrient` at line 101 correctly absorbs the unused carriage; full
  body scan confirms no `F`, no `Q`, no `hOrient` reference in lines
  102-491. Docstring matches body. First wave-62 per-(F, Q) helper to
  use this discharge pattern visibly.

Also noted out of scope: when `single_branch_leaf_both_extend_per_kQ`
closes (#2905 chain), a future refactor could extract the inline
universal T(1, 2, 2) posdef proof (lines 7964-8352) into a standalone
universal lemma, paralleling the existing universal/per-(F, Q) layout
for `embed_t125_in_tree`. Flagged for a future planner cycle but not
filed as an issue this session.

## Current frontier

- **Unclaimed work items (4):** #2564 (Mathlib upstream tracker,
  external-blocked), #2923 (D2.nonAdj outer assembly), #2937
  (embed_etilde6_in_tree_per_kQ body), #2938
  (embed_etilde7_in_tree_per_kQ body). The two `embed_*` helpers were
  spawned during the previous planner cycle as follow-ups to #2932.
- **This audit:** PR pending creation for `progress/reviews/…`
  report-only commit.
- **Open repair PRs (not this session):** #2911 (Ẽ₇ embed arms ≥ 3 —
  highest-leverage), #2849 (etilde6 dedupe, CI failed), #2694
  (Schur-Weyl L_i γ.A), #2550 (Wall 3 C.1.a.ii).

## Overall project progress

Stage 3 (Lean formalisation). Wave-62 architectural milestone —
Theorem 2.1.2 forward-direction bridge sorry-free — was achieved in
PR #2921 and recorded in summary PR #2930. Residual work is local to
per-(F, Q) leaf bodies (Wall 1 / K_{1,4} / T(1, 2, 5) / D̃₅ Sub B +
the four pre-existing per-(F, Q) leaf chains).

Sorry count on `main`: 19 / 11 files (unchanged this session — this
was an audit-only review, no code touched). Items sorry-free per
`progress/items.json`: 582 / 583 (99.8%).

This review cycle's contribution closes one of the two audit gaps
flagged in the previous summarise (`9ea12755` / wave 62). The
remaining gap (audit of #2934 — D2 wrapper trilogy) is open for the
next review session.

## Next step

**For the dispatcher / next worker cycle:**

1. **Worker priorities (in order):**
   - Repair PR #2911 (Ẽ₇ embed arms ≥ 3) — still the
     highest-leverage repair; unblocks `Tpqr.lean:1286` dispatcher
     stub.
   - Claim #2934 (the D2 wrapper trilogy audit, still unclaimed) —
     remaining wave-62 audit gap.
   - Claim #2937 / #2938 (embed_etilde6/7_in_tree_per_kQ bodies) —
     wave-62 sub-A2 follow-ups.
   - Claim #2923 (D2.nonAdj outer assembly) once the embed_etilde*
     helpers land.

2. **Future audit gaps to consider in the next planner cycle:**
   PR #2906 (+224, single_branch leaf-case partial), #2914 (T(1, q,
   2) sub-B partial), #2916 (T(1, 2, r) sub-C partial). Deferred to
   when the wave-62 single-branch chain stabilises (per the previous
   handoff's recommendation).

3. **Refactor note (out of scope this session):** when the
   `single_branch_leaf_both_extend_per_kQ` dispatcher closes, planner
   should consider filing an extraction issue for the inline T(1, 2,
   2) posdef proof at `InfiniteTypeConstructions.lean:7964-8352` →
   standalone universal lemma, paralleling the
   `embed_t125_in_tree` / `embed_t125_in_tree_per_kQ` layout.

## Blockers

None for this session. The audit completed cleanly with PASS on all
five deliverables. No code changes required, no follow-up issues
filed.
