## Accomplished
Wired `cauchy_schur_coeff_multichoose` (`Chapter5/CauchyCharacterRightAssembly.lean`)
into its already-proven sibling `Etingof.CauchySchurBasis.cauchy_schur_coeff`,
removing a live `sorry` on the critical path (issue #5000).

- Added `import EtingofRepresentationTheory.Chapter5.CauchySchurBasis` (no cycle:
  that file only imports `CharacterOrthogonality` + `SchurWeylPolynomialIdentity`,
  both upstream of this assembly).
- Replaced the `sorry` with `simpa [Nat.cast_prod] using
  Etingof.CauchySchurBasis.cauchy_schur_coeff (N := N) (d := d) μ hμ`. The only gap
  was cast-of-product (`((∏ … : ℕ) : ℚ)`) vs product-of-casts (`∏ …, (… : ℚ)`),
  bridged by `Nat.cast_prod`; `coeff μ p` vs `p.coeff μ` is defeq.
- Dropped the stale `#4958 named dependency` framing from the docstring.

## Current frontier
`lake build EtingofRepresentationTheory.Chapter5.CauchyCharacterRightAssembly`
green (8066 jobs). `cauchy_schur_coeff_multichoose` and its consumer
`polyRightDegreeFDRep_formalCharacter` are sorry-free.

## Overall project progress
One more Ch5 sorry removed outright. Stage 3 formalization ongoing.

## Next step
Continue clearing remaining Ch5 sorries.

## Blockers
None.
