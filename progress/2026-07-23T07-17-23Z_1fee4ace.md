## Accomplished

Closed the Remark 6.4.4 source/Lean mismatch (issue #7501). The tracker marked
Remark 6.4.4 ("there can be only finitely many roots") fully covered, but the only
public finiteness endpoint (`Etingof.Theorem_6_5_2a_finiteness`) bounded the *positive*
roots only.

New file `EtingofRepresentationTheory/Chapter6/Remark6_4_4.lean`:

- `Etingof.IsRoot.neg` — the root set is symmetric under negation (`x` a root ⟹ `-x` a
  root), since the Cartan quadratic form is even.
- `Etingof.Remark_6_4_4_roots_finite` — the full root set
  `{x : Fin n → ℤ | Etingof.IsRoot n adj x}` is finite for a Dynkin diagram. Proof:
  every root is positive or negative (`Lemma_6_4_6`); every negative root is the negation
  of a positive root (`IsRoot.neg`); so the root set embeds in the union of the finite
  positive-root set with its image under negation.

Both declarations are axiom-clean: `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.

Registered the module in `EtingofRepresentationTheory/Chapter6.lean`. Corrected the
`Chapter6/Remark6.4.4` tracker record in `progress/items.json` to
`sorry_free`/`verified`/`covered_full` with `fidelity_decl` pointing at the new theorems.

## Current frontier

Issue #7501 fully addressed. `lake build EtingofRepresentationTheory.Chapter6.Remark6_4_4`
succeeds against live dependencies.

## Overall project progress

Stage 3 (formalization) ongoing; completeness-audit regressions being worked down through
the issue queue. This turn cleared one Chapter 6 root-theory audit item.

## Next step

Create the PR for #7501 and let auto-merge land it. Plenty of other unclaimed
completeness-audit / fix issues remain in the queue for the next worker.

## Blockers

None.
