## Accomplished

Closed issue #6694 (Ch7 Künneth crux, milestone (b)+(c)): proved
`Etingof.nonempty_tensorObj_extend_iso` sorry-free in
`EtingofRepresentationTheory/Chapter7/KunnethChainComplexNat.lean`, and strengthened it to an
honest construction rather than a `⟨sorry⟩`.

New declarations (inside `namespace TensorExtend`):
- `negOnePow_neg_natCast (p : ℕ) : Int.negOnePow (-(p:ℤ)) = (-1)^p` — the one non-mechanical
  Koszul-sign step; under the reindex `a = -p` the `up ℤ` vertical sign `a.negOnePow` matches the
  `down ℕ` vertical sign `(-1)^p`.
- `fwdNeg_comm (n)` (`@[reassoc]`) — **milestone (b)**: the degree-`-n` coproduct forward maps
  `fwdNeg` commute with the Koszul-signed total differentials. Proved summand-by-summand via
  `mapBifunctor.hom_ext`; case split on image membership (`e.r a`, `e.r b`), then split the total
  differential into `d₁` (factor-1, sign 1) and `d₂` (factor-2, sign matched by
  `negOnePow_neg_natCast`) via `congr_arg₂ (·+·)`, each further split on `p`/`q` being `0` (both
  sides vanish) or `succ` (the genuine matching, following Mathlib's `rightUnitor'_inv_comm` /
  `leftUnitor'_inv_comm` template with `whisker_exchange`, `curriedTensor_map_app`, and `extendXIso`
  cancellation).
- `tensorObjExtendIso` — **milestone (c)**: assembles the `tensorExtendXIso` degreewise isos
  (milestone (a), already in main) into an honest `≅` of `ℤ`-cochain complexes via
  `HomologicalComplex.Hom.isoOfComponents`, using `fwdNeg_comm_assoc` for the differential square on
  `j ≤ 0` and target-vanishing on `j > 0`.

`nonempty_tensorObj_extend_iso` is now `⟨TensorExtend.tensorObjExtendIso C D⟩`.

Verification: `lake build EtingofRepresentationTheory.Chapter7.KunnethChainComplexNat` succeeds with
no warnings. `#print axioms` for `nonempty_tensorObj_extend_iso`, `kunnethChainComplexNat`, and
`fwdNeg_comm` all report exactly `[propext, Classical.choice, Quot.sound]`. As a consequence
`kunnethChainComplexNat` (the ℕ-Künneth API consumed by #6657) is now fully sorry-free.

## Current frontier

The ℕ-indexed Künneth reindex bridge is complete and axiom-clean. Downstream, the Problem 8.2.8
`Tor` assembler #6657 can now consume `kunnethChainComplexNat` without a proof-level dependency on
the crux sorry.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 8.2.8 `Tor` half decomposition: milestone (a) (#6684,
#6693), tensor-of-resolutions pieces (#6680, #6681), rearrangement (a)/(b) (#6684, #6686) landed;
this turn lands the Künneth crux (#6694). Remaining unclaimed feature issues at turn start: #6657
(Tor assembler), #6683 (extTensorProjectiveResolution), #6687 (rearrangement milestone (c)).

## Next step

Pick up #6687 (rearrangement milestone (c)) or #6683 (extTensorProjectiveResolution) — both have
their direct dependencies closed. #6657 is the final Tor assembler and now has its Künneth input
(this issue) available.

## Blockers

None.
