## Accomplished

Closed issue #7612 (the residual bullet of #7437): connected the downstream Specht-module
construction to Etingof's source-defined left ideal.

New file `EtingofRepresentationTheory/Chapter5/Theorem5_12_2_SourceIdeal.lean`:

- `youngProjectorLeftIdeal n la := ℂ[Sₙ]·c_λ` with `c_λ = a_λ·b_λ = youngProjector`
  (the source-order, normalized generator).
- `youngProjectorLeftIdeal_eq_span_mul`: `ℂ[Sₙ]·c_λ = ℂ[Sₙ]·(a_λ b_λ)` (normalization is
  invisible to the span, via `span_singleton_smul_eq`).
- `spechtModule_eq_span_projCol_mul_projRow` (the issue's "optional easy step"):
  `SpechtModule n la = ℂ[Sₙ]·(youngProjectorCol·youngProjectorRow)`.
- `spechtModule_linearEquiv_youngProjectorLeftIdeal`: an explicit `ℂ[Sₙ]`-linear isomorphism
  `SpechtModule n la ≃ₗ youngProjectorLeftIdeal n la`. Built from the two right-multiplication
  maps `·b_λ` and `·a_λ` (left-module homs `rMul`), whose composites are the Lemma 5.13.1
  scalars: `(b_λ a_λ)² = α·(b_λ a_λ)` (Lemma 5.13.3) and dually `(a_λ b_λ)² = β·(a_λ b_λ)`
  (`Lemma5_13_1_dual` at `x = b_λ a_λ`). Both scalars nonzero — α from
  `young_symmetrizer_sq_ne_zero`; β because β=0 forces `(b_λ a_λ)³ = b_λ·(a_λ b_λ)²·a_λ = 0`
  while `(b_λ a_λ)³ = α²·(b_λ a_λ) ≠ 0`. Nonzero composites give injectivity + surjectivity
  of `·b_λ` directly (no need for α = β), then `LinearEquiv.ofBijective`.
- `youngProjectorLeftIdeal_isSimpleModule`: `V_λ = ℂ[Sₙ]·c_λ` is irreducible, transporting
  `Theorem5_12_2_irreducible` along the isomorphism (`LinearEquiv.isSimpleModule_iff`).

Registered the file in the `Chapter5.lean` aggregator. Builds clean (no warnings);
`#print axioms` shows only `propext, Classical.choice, Quot.sound` (no `sorryAx`).

## Current frontier

Issue #7612 fully implemented and verified; PR to follow.

## Overall project progress

Stage 3 formalization ongoing. This turn completed one completeness-audit gap in Chapter 5's
Young-symmetrizer / Specht-module chain, reconciling the reversed-unnormalized implementation
generator with Etingof's source `c_λ`.

## Next step

Pick the next unclaimed `feature`/`completeness-audit` issue. Many remain open across
Chapters 3–9.

## Blockers

None.
