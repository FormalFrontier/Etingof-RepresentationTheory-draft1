# Progress — feature session 5d60cec5 (#2901)

## Accomplished

Claimed and executed #2901 (D2.single pre-split of #2877): created
`EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean` containing:

- **D1.** `single_branch_not_posdef_infinite_type_per_kQ` — per-(F, Q)
  wrapper around `single_branch_not_posdef_infinite_type`
  (`InfiniteTypeConstructions.lean:8401-8706`). Body mirrors the
  `_kQ`-free original: case-splits on whether all three arms have length
  ≥ 2 (Ẽ₆ = T(2,2,2) embedding → `etilde6_not_finite_type_per_kQ` via
  `subgraph_infinite_type_transfer_per_kQ`) versus at-least-one-leaf
  (three branches dispatch to the new `single_branch_leaf_case_per_kQ`
  stub introduced in this file). Carries `[IsAlgClosed F]` because the
  leaves all do. ~330 lines of body.
- **D2.** `single_branch_leaf_case_per_kQ` — API stub with `sorry` body
  mirroring the signature of `single_branch_leaf_case`
  (`InfiniteTypeConstructions.lean:6901-8400`, ~1500 lines body). The
  real proof is tracked by a follow-up sub-issue to be filed alongside
  the PR. Docstring lists the three internal sub-dispatches (q,r ≥ 3
  embeds Ẽ₇; q = 2, r ≥ 5 embeds T(1,2,5); ADE cases contradict
  `h_not_posdef`).

Placement decision: new file `FieldGenericTpqr.lean` per the audit
recipe's preferred option (D1's body is ~330 lines; together with D2's
stub it would push `FieldGenericETilde7.lean` over the soft cap, and the
file naturally becomes the assembly point for the future Ẽ₆/Ẽ₇/T(1,2,5)
leaf-case real body). Added the new module to
`EtingofRepresentationTheory/Chapter6.lean`.

`lake build EtingofRepresentationTheory.Chapter6` passes with no new
errors, only the expected `FieldGenericTpqr.lean:64:8: declaration uses
'sorry'` warning for D2's stub.

## Current frontier

PR #2901 ready to push. Chapter 6 per-(F, Q) ↔ Theorem 2.1.2 bridge
scoreboard, one tick from cycle defeb5c1:

- D2.degree4 on `main` (PR #2891).
- D2.cycle on `main` (PR #2897).
- D2.adjacent on `main` (PR #2900).
- **D2.single** on this branch (this PR).
- **Pending**: D2.nonadj, D2.outer + D3 (still under #2877). Plus the
  follow-up `single_branch_leaf_case_per_kQ` real body issue to be filed
  on PR creation.

## Overall project progress

Stage 3 (Lean formalisation). Sorry count on `main`: 18. This PR adds
exactly one sorry (D2's `single_branch_leaf_case_per_kQ` stub) →
expected post-merge total: 19.

## Next step

- Next planner cycle should:
  - Pre-split the next D2 sub-deliverable from #2877 (D2.nonadj
    audit-recommended own pass; or D2.outer + D3 ~150 lines as the
    smaller remaining unit).
  - Triage the follow-up `single_branch_leaf_case_per_kQ` real-body
    issue filed by this worker (likely decomposable into T(1,q,r)-shape
    sub-cases).
  - Consider a light audit of this PR's structural choices (new file
    `FieldGenericTpqr.lean`; internal-stub pattern). Both are
    first-of-their-kind moves for the per-(F, Q) chain.

## Blockers

None for this worker. Standing constraints unchanged: framework wall
#2436 gates the inherited sorries in the per-(F, Q) indecomposability
proofs; the static pr-repair PRs (#2849 / #2694 / #2550) remain in
`/repair` territory.
