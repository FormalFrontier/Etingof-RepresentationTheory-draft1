## Accomplished

### Fixed 2 CI-failing PRs (#1678, #1679)
- Rebased PR #1676 (agent/9ca62c72) on main — resolved duplicate `simple_of_full_faithful_preservesMono'` from #1673 merge
- Rebased PR #1672 (agent/7a8cf2a5) on main — same conflict, resolved cleanly
- Both force-pushed with `--force-with-lease`

### Proved 3 infrastructure sorries in Theorem5_25_2.lean (#1680)

1. **Augmentation coset reindexing** (line ~1263): Built `GL₂ ≃ B × Option(GaloisField p n)` equiv using `cosetBorel`/`cosetIndex`, then used `Fintype.sum_equiv` + `Fintype.sum_prod_type` to reindex `∑_g v(cosetIndex g)` as `|B| · ∑_i v(i) = 0`.

2. **`principalSeriesSubmodule_finrank`** (line ~1294): Proved dim V(χ₁,χ₂) = p^n + 1 by constructing a linear bijection `evalMap` (evaluate at coset reps) with inverse `mkCovariantFun`, using `LinearEquiv.ofBijective` to transfer finrank. Added `[NeZero n]` hypothesis (required by `GaloisField.card`).

3. **`complementWProjection` comm** (line ~1635): Proved equivariance of the projection f ↦ f − (aug(f)/|G|)·(μ∘det) using `augOnPrincipalSeries_equivariant` and `ring`.

### Created PR #1682 with auto-merge enabled

## Current frontier

- `Theorem5_25_2.lean` still has 3 remaining sorries (lines ~1799, ~1859, ~1881)
- These are covered by issues #1647 (complementW_simple) and #1681 (complementW_iso_implies_eq + part3b)

## Overall project progress

- ~558/583 items sorry-free (95.7%)
- Theorem5_25_2.lean: reduced from 6 sorries to 3 (the 3 infrastructure sorries are now proved)
- PRs #1676 and #1672 unblocked (rebased on main, CI should pass)

## Next step

- Work on #1647 (prove `complementW_simple`) or #1681 (characterization sorries)
- Check if PRs #1676, #1672 pass CI after rebase

## Blockers

None
