## Accomplished

Feature session `469f9971`. Claimed and completed **#4600** (Ch5 #2482 sub-A2:
generic isotypic extraction).

- Added `EtingofRepresentationTheory/Chapter5/SemisimpleIsotypic.lean` with the
  generic, Schur-Weyl-agnostic lemma
  `Etingof.SemisimpleIsotypic.submodule_of_directSum_simple_iso_directSum`:
  a submodule `M` of a finite direct sum of simple `R`-modules `⨁ i, L i`
  satisfies `∃ (n : ℕ) (f : Fin n → ι), Nonempty (M ≃ₗ[R] ⨁ j, L (f j))`.
  This is the issue's permitted **Σ-indexed form** — the per-`i` multiplicity
  is `mult i = Nat.card {j // f j = i}`, directly readable from `f`.
- Plus a small helper `module_finite_of_isSimpleModule` (a simple module is
  cyclic, hence `Module.Finite`).
- Wired the file into `EtingofRepresentationTheory/Chapter5.lean`.
- `lake build` of the module succeeds, **zero sorries**, `#print axioms` shows
  only `propext, Classical.choice, Quot.sound` (not vacuous).

Proof route (all Mathlib): transport `M` through `e` into `⨁ L`; ambient is
semisimple + `Module.Finite` (via `module_finite_of_isSimpleModule` +
`Module.Finite.instDirectSum`) hence Noetherian, so `M` is too and is
`Module.Finite`; `IsSemisimpleModule.exists_linearEquiv_fin_dfinsupp` splits it
into simple submodules `S j`; each `S j` (mapped into `⨁ L = sSup` of the
coordinate lines `range (lof i)`) is `≅` some `L i` by
`Submodule.linearEquiv_of_le_sSup`; `Classical.skolem` extracts `f`;
`DFinsupp.mapRange.linearEquiv` reassembles.

Also recorded the session's two Mathlib gotchas in the `lean-formalization`
skill: `List.TFAE.out` fails with named type args (use goal-driven `‹_›`); and
the AddCommGroup/AddCommMonoid submodule diamond (use
`LinearEquiv.isSimpleModule_iff` over `IsSimpleModule.congr`).

## Current frontier

#4600 done. The optional per-`i` *grouped* form
`⨁ i, (Fin (mult i) → L i)` was deliberately **not** built: it requires
`Eq.rec` over fiber-dependent summand types (`L (f j) = L i`) and the issue
explicitly accepts the Σ-form. The #2482 concrete assembly can consume the
delivered form directly or do that regrouping if it really needs per-`i` boxes.

## Overall project progress

Etingof draft 1, Stage 3. Ch5 Schur-Weyl chain: #2493 (character-level
`V^{⊗n}` decomposition) in place; #2482 (module-level Schur decomposition / #6)
decomposed into #4598 (embedding) + #4600 (this, isotypic extraction) + a
to-be-authored abstract-form assembly. #4598 is claimed elsewhere. Ch6
affine-Dynkin frontier active in other sessions.

## Next step

A worker finishes **#4598** (if not already landed), then a **planner** triages
the #2482 `replan`: with #4598 + #4600 both available, author the abstract-form
assembly issue that combines them (embed `M ↪ (V^{⊗n})^m` via #4598; the target
is a finite ⊕ of simples by `Theorem5_18_4_GL_rep_decomposition_simple`; apply
#4600's `submodule_of_directSum_simple_iso_directSum`). The planner decides
where the concrete `SchurModule` naming / highest-weight classification lives
(see the #2482 architecture comment).

## Blockers

None.
