## Accomplished

One-shot dispatch for issue #5921 (statement pass, Chapter 8 remaining exercises). Formalized
faithful Lean statements (`sorry` proofs) for:

- **`Chapter8/Exercise8_2_9.lean`** — Exercise 8.2.9 (i) no nonzero projective objects in the
  categories of finite abelian groups and finite dimensional `k[x]`-modules (encoded via the
  projective lifting property ⇒ `Subsingleton`), and (ii) the category of finitely generated
  modules over a finitely generated commutative ring has enough projectives (every f.g. module
  admits a surjection from a finite free module).
- **`Chapter8/Problem8_2_6.lean`** — Problem 8.2.6 (i) `Tor₀ ≅ ⊗_A` and `Ext⁰ ≅ Hom_A`;
  (ii) `Ext¹` agrees with the cocycle/coboundary `Ext¹` of Problem 3.9.1; the **Ext** long exact
  sequences of (iii) (covariant, second argument) and (v) (contravariant, first argument), via
  Mathlib's `Abelian.Ext.covariantSequence` / `contravariantSequence` (whose objects are exactly
  the `Etingof.Ext` groups).

Both files added to `Chapter8.lean`; `lake build EtingofRepresentationTheory.Chapter8` succeeds
with only `sorry` warnings. `progress/items.json`: Problem8.2.6 and Exercise8.2.9 →
`statement_formalized`.

## Current frontier

The **Tor** long exact sequences of Problem 8.2.6 (iii)/(v), the **balancing** theorem (iv), and
**Problems 8.2.7** (Tor/Ext computations for ℤ and k[x]) and **8.2.8** (Künneth) were deferred to
follow-up issue **#5924**. Reason: the `Tor` of Definition 8.2.3 is left-derived only in its
first argument, so it currently lacks the second-argument functoriality / balancing API needed to
state the `Tor` connecting maps and the Künneth iso faithfully.

## Overall project progress

Phase 3 statement pass. Chapter 8 §8.2: Definitions 8.2.1/8.2.3/8.2.4, Exercise 8.2.2, Problems
8.2.5/8.2.10 already present; now Problem 8.2.6 (Ext parts + i/ii) and Exercise 8.2.9 added.
Remaining §8.2 statement work tracked in #5924.

## Next step

A worker on #5924 should add second-argument functoriality / a left-derived homology long exact
sequence for `Etingof.Tor` to state the Tor halves of 8.2.6 (iii)/(v) and (iv), then create
`Problem8_2_7.lean` and `Problem8_2_8.lean`.

## Blockers

None. Deferred work is captured in #5924 (not a blocker; needs infrastructure, not a fix).
