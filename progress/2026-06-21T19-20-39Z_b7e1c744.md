## Accomplished

Closed issue #4930 (Ch5 #4925 sub-A diagonalization). Created
`EtingofRepresentationTheory/Chapter5/DiagonalizableConj.lean` with the
sorry-free theorem

```
gl_conj_diagTorus_of_distinct_eigenvalues
    (N : ℕ) (g : Matrix.GeneralLinearGroup (Fin N) ℂ)
    (hdist : (g : Matrix (Fin N) (Fin N) ℂ).charpoly.roots.toFinset.card = N) :
    ∃ (t : Fin N → ℂˣ) (h : Matrix.GeneralLinearGroup (Fin N) ℂ),
      g = h * diagTorus ℂ N t * h⁻¹
```

exactly the shape the issue requested. Added the file to the `Chapter5.lean`
aggregation. Builds clean (no errors, no warnings, no sorry).

## Current frontier

The proof is the classical eigenbasis construction, assembled from Mathlib:
- the `N` distinct charpoly roots are eigenvalues via `Matrix.spectrum_toLin'` +
  `Module.End.hasEigenvalue_iff_mem_spectrum` + `Matrix.mem_spectrum_iff_isRoot_charpoly`;
- none is `0` because `g` is a unit (`spectrum.zero_mem_iff`);
- one eigenvector per eigenvalue gives `N` linearly independent vectors
  (`Module.End.eigenvectors_linearIndependent'`), hence an eigenbasis
  (`basisOfLinearIndependentOfCardEqFinrank`);
- the change-of-basis matrix `V = (Pi.basisFun).toMatrix eigenbasis` is invertible
  (`Basis.invertibleToMatrix`) and satisfies `A * V = V * D` columnwise (each
  column is an eigenvector), so `A = V * D * V⁻¹` with `D` the eigenvalue diagonal,
  which packages as `diagTorus`.
- `N = 0` handled separately via `Subsingleton (GL_0(ℂ))`.

## Overall project progress

Stage 3 formalization, Chapter 5. This delivers sub-piece (ii) of #4925's
density-core strategy, unblocking the residual #4932 assembly
(`trace_combination_vanishes_of_torus_vanishes`).

## Next step

Consume `gl_conj_diagTorus_of_distinct_eigenvalues` in #4932 (a class function
vanishing on the diagonal torus vanishes on every distinct-eigenvalue matrix).
Note the exact hypothesis form: `charpoly.roots.toFinset.card = N`.

## Blockers

None.
