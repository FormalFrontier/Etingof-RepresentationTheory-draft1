## Accomplished

Proved **part (i)** of Problem 4.5.2 (primitive central idempotents `ψ_V` of `ℂ[G]`)
sorry-free in `EtingofRepresentationTheory/Chapter4/Problem4_5_2.lean`:

- `Etingof.psi_acts_self` — `ψ_V` acts on a simple `V` as `LinearMap.id`.
- `Etingof.psi_acts_other` — for `W ≇ V` simple, `ψ_V` acts on `W` as `0`.

Key reusable helpers built along the way (all sorry-free):
- `endo_scalar` — categorical Schur: a `G`-equivariant endo of a simple `FDRep` is a scalar
  multiple of `id`, scalar `= trace/finrank` (via `finrank_endomorphism_simple_eq_one` +
  `invariantsEquivFDRepHom`). Avoids the `Simple`→`IsSimpleModule` bridge entirely.
- `psi_comm_single` — `ψ_V` is central in `ℂ[G]` (conjugation reindex + `char_conj`).
- `asAlgebraHom_psi`, `asAlgebraHom_psi_comm`, `trace_asAlgebraHom_psi`.
- `finrank_pos_of_simple`, `card_ne_zero_cx`.

Trace pinned by `FDRep.char_orthonormal` (= |G| on the diagonal, 0 off-diagonal).

## Current frontier

Part (ii) — `psi_idempotent` and `psi_orthogonal` — left as `sorry`. Filed as sub-issue
#6082 with a detailed proof strategy (semisimple-faithfulness of the regular representation:
`IsSemisimpleModule ℂ[G] ℂ[G]` + `sSup_simples_eq_top` + `Submodule.annihilator_iSup`, then
part (i) on each simple submodule via the module⟹FDRep bridge).

## Overall project progress

Stage 3 (formalization) ongoing. This turn: Problem 4.5.2 part (i) complete; part (ii)
deferred to #6082. File compiles with 2 sorries (both in part (ii)).

## Next step

Claim #6082 and discharge the two part-(ii) sorries following the strategy in its body.
A good first move is to add the general `psi_acts (V W) [Simple V] [Simple W]` lemma
(if `W ≅ V` then `id` else `0`) — a trivial generalisation of the two proven theorems.

## Blockers

None. #6082 is self-contained (depends-on: none); all needed Mathlib API located.
