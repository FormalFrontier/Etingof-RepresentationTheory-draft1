## Accomplished

Closed issue #7880 (deliverable 2 of #7876): the Problem 2.11.3(f) trace formulas now hold under
the book's own hypothesis, with no triangularizing basis in the statement.

Added to `EtingofRepresentationTheory/Chapter2/Problem2_11_3_Trace.lean` (new `Charpoly` section):

* `Etingof.Problem2_11_3.exists_isTriangularizedBy` — a split `charpoly A` plus
  `finrank k V = N` yields `b` and `lam` with `IsTriangularizedBy A b lam`.
* `Etingof.Problem2_11_3.trace_extPowMap_of_charpoly` —
  `Tr(⋀^n A) = ∑_{|s| = n} ∏_{i ∈ s} lam i` from `charpoly A = ∏ i, (X - C (lam i))` alone.
* `Etingof.Problem2_11_3.trace_symPowMap_of_charpoly` —
  `Tr(S^n A) = ∑_{s : Sym (Fin N) n} ∏_{i ∈ s} lam i`, likewise.

All three are sorry-free; `#print axioms` on each gives only
`propext, Classical.choice, Quot.sound`. `lake build` is clean over all 9330 jobs.

`progress/items.json`: `Chapter2/Problem2.11.3` moved from `covered_partial` to `covered_full`,
`followup_issue: 7876` dropped, and the coverage note's closing sentence ("upgrading the
triangularizing-basis hypothesis ... needs a triangularization theorem that Mathlib lacks")
replaced with what the new API actually provides. `scripts/validate_items.py` passes.

## Decisions made

* Put the new theorems in `Problem2_11_3_Trace.lean` rather than in `Infrastructure/`, as the
  issue specified, and added `import EtingofRepresentationTheory.Infrastructure.Triangularization`
  there. No import cycle: `Triangularization.lean` imports only `Mathlib`.
* Kept the existing `IsTriangularizedBy`-hypothesis theorems as the primitives and derived the
  charpoly forms from them, rather than restating the proofs. The permutation `e` coming out of
  `exists_basis_blockTriangular_diag_perm` is discharged by `sum_powersetCard_prod_comp` /
  `sum_sym_prod_comp`; the assembly is three lines each, exactly as the issue's sketch predicted.
* Updated the module docstring's "The shape of the hypothesis" section, which still asserted that
  Mathlib has no theorem producing a triangularizing basis from a split characteristic polynomial.
  That is now the project's own `Infrastructure/Triangularization.lean`, so the sentence was
  actively misleading about the file's own reach.

## Current frontier

Problem 2.11.3 is complete: parts (a)–(g), all under the book's hypotheses.

`Infrastructure/Triangularization.lean` emits a pre-existing style-linter warning
(`Copyright too short!` at line 1:1). Left alone — it predates this change and is not in scope.

## Overall project progress

Stage 3 formalization ongoing. `items.json` coverage counts after this change: 250
`covered_full`, 178 `covered_partial`, 19 `covered`, 1 `non_formalizable`, out of 583 items
(5721/5721 book lines attributed). Full build sorry-free at the declarations touched here.

## Next step

The `#7876` line of work is done. From the unclaimed queue, #7892 (triage the two unlanded Specht
decls on stranded branch `agent/13603a0c`) is small, self-contained, and time-sensitive in the
sense that the branch keeps drifting from `main`. The three oldest issues — #5639, #5644, #5648 —
have been unclaimed since 2026-06-29 and are each large fidelity gaps (reflection functors acting
on actual representations; genuine `CategoryTheory.Adjunction`s with two-variable naturality;
block-equivalence-to-VecSpaces). They look like decomposition candidates for a planner rather
than single-session work, which is plausibly why they have been passed over repeatedly.

## Blockers

None.
