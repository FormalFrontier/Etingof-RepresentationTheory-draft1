## Accomplished

Assembled `Problem_8_2_8_ext` (`Chapter8/Problem8_2_8.lean`, issue #6898) down to a **single
proof-level `sorry`** (the `hXM` object identification). The full four-deliverable wiring now
compiles; everything except `hXM` is sorry-free:

- **New helper `isScalarTower_extTensor`** (sorry-free): an abstract componentwise external
  `A₁ ⊗[k] A₂`-module on `M₁ ⊗[k] M₂` (pinned by `hM`) is automatically an `IsScalarTower k …`.
  Supplies the tensor scalar towers `barResolution` / `Problem_8_2_8_extₖ` demand of `instM` / `instN`.
- **Deliverable 1**: `barResolution k Aᵢ Mᵢ` and `barResolution k (A₁⊗A₂)(M₁⊗M₂)`, with
  `instFiniteBarResolutionComplexX` / `instProjectiveBarModule` supplying finiteness+projectivity.
  Added `[IsScalarTower k Aᵢ Mᵢ]` / `[IsScalarTower k Aᵢ Nᵢ]` section vars (only attached to
  `Problem_8_2_8_ext`).
- **Deliverable 3**: `extAbelianIsoExtₖ` (the `Ext ≃ₗ[k] Extₖ` bridge, #6968) transports the big
  `Ext` and, backwards under `TensorProduct.congr` + `DirectSum.congrLinearEquiv`, each factor `Extₖ`
  summand.
- **Deliverable 4**: `ModuleCat.coprodIsoDirectSum` (∐ → ⨁) + the monoidal `tensorObj = of (⊗)`
  defeq (⊗ → `TensorProduct k`). Added imports `ModuleCat.Products`, `DirectSum.Module`,
  `TensorProduct.Map`, and `Chapter8.ExtAbelianComparison` / `Chapter8.BarResolution`.
- The whole chain is a composition of `≃ₗ[k]` equivalences; statement unchanged. Verified: only one
  comment-stripped `sorry` token in the file; `lake build …Problem8_2_8` succeeds (exit 0).

## Current frontier

The single remaining `sorry` is `hXM`:
`ModuleCat.of (A₁⊗A₂)(M₁⊗M₂) = extTensorFunctorLeftObj k A₁ A₂ (of A₁ M₁)(of A₂ M₂)`.
It is **not a defeq**: `extTensorFunctorLeftObj` bakes in `restrictModule₁L`/`₂L`
(`Module.compHom _ (algebraMap k Aᵢ)`, the `k`-via-`Aᵢ` restriction) on each factor, whereas the
statement carries the ambient `Module k Mᵢ`. They agree by `IsScalarTower.algebraMap_smul` but the
factor `k`-module *diamond* must be reconciled (confirmed: `with_unfolding_all rfl` fails; `congr 1`
exposes exactly the `restrictModule₂L` vs ambient mismatch on the `TensorProduct k` factors).

## Overall project progress

Phase 3 formalization at its tail. This is the near-complete final assembly of the `Ext` half of
Problem 8.2.8 — one of the last large feature items. Decomposed the residual into **#6980** (prove
`hXM`). The same `k`-module diamond is reconciled elsewhere (`RearrangeBifunctorNatIso.lean`:
`extModuleK_algebraMap_smul`, `rearrangeSourceEquiv`) — reuse that.

## Next step

Pick up **#6980**: prove `hXM`. Route: `restrictModuleᵢL (of Aᵢ Mᵢ) = ‹Module k Mᵢ›` via
`Module.ext'` + `IsScalarTower.algebraMap_smul`, thread through `extTensorModuleLeft`, close the
object equality (HEq handling), reusing `RearrangeBifunctorNatIso` infrastructure. Or replace the
`rw [hXM]` with a `k`-linear `Extₖ`-functoriality iso built from the object iso.

## Blockers

None (the residual is well-isolated in #6980; a sorry'd `hXM` is a proof-level sorry, not a
definition-level blocker).
