# Review: Problem 8.2.6(iv) balancing theorem + Tor six-term / naturality infrastructure (issue #6647)

Session type: **review**. UTC 2026-07-15T06:34:45Z.

## Accomplished

Audited `EtingofRepresentationTheory/Chapter8/Problem8_2_6.lean` for `Etingof.Problem_8_2_6_iv`
(balancing theorem) and its four supporting decls: `torBalancing_sixTerm`,
`fromLeftDerivedZero_natTrans_app`, `balancing_zero_naturality`, `iso_of_sixTerm_exact`.

**1. Non-vacuousness / real objects — PASS.**
- `#print axioms` on all five decls (and on the upstream `def`s `balancingIsoZero`,
  `tensorLeftFunctor`, `tensorRightFunctor`, `Etingof.Tor`) reports only
  `[propext, Classical.choice, Quot.sound]` — no `sorryAx`. Since `Problem_8_2_6_iv`'s axiom
  closure includes every upstream `def`/`instance` body, this confirms no sorried definition
  anywhere in the chain.
- The two sides of the iso are genuinely distinct constructions, not a defeq collapse:
  `Etingof.Tor A N M n = (leftDerived (tensorRightFunctor A N) n).obj M` (derived in the *first*
  argument `M`, over a projective resolution of `M`), whereas the RHS is
  `(leftDerived (tensorLeftFunctor A M) n).obj (ModuleCat.of A N)` (derived in the *second*
  argument `N`, over a resolution of `N`). `tensorRightFunctor A N : M ↦ M ⊗_A N` and
  `tensorLeftFunctor A M : N ↦ M ⊗_A N` are real functors (obj/map/map_id/map_comp all
  constructed, `Additive` instances proved) that vary in different arguments; their left-derived
  functors are not definitionally equal.
- The `n = 1` base is a genuine kernel comparison (`kernel.mapIso` of two exact
  `ShortComplex`es whose `f`-legs are mono via the vanishing of `L₁` on the projective term),
  and `iso_of_sixTerm_exact` builds a real iso via `isIso_of_mono_of_epi` in an abelian category
  (mono from `(hW.exact' 1 2 3)`, epi from `(hW.exact' 2 3 4)`), not `Iso.refl`.

**2. Statement fidelity — PASS.** `blobs/Chapter8/Problem8.2.6.md` part (iv): "Show that
`Torᵢᴬ(M, N)` can be computed by taking a projective resolution of `N`, tensoring it with `M`,
and computing cohomology." The Lean `Nonempty (Etingof.Tor A N M n ≅ (leftDerived
(tensorLeftFunctor A M) n).obj (ModuleCat.of A N))` states exactly this — Tor (defined by
resolving `M`) is isomorphic to the left-derived-in-`N` computation (resolve `N`, tensor with
`M`, take homology). The proof takes a dimension-shift route rather than the book's
double-complex hint, but the statement is faithful. The four infra decls prove what their
docstrings claim, with no weakened hypotheses:
- `torBalancing_sixTerm` — the balancing-side six-term exact window (mirror of
  `Problem_8_2_6_iii_tor`), full short-exactness hypotheses via
  `NatTrans.leftDerived_sixTerm_exact` + flatness of projectives.
- `fromLeftDerivedZero_natTrans_app` — general naturality of `fromLeftDerivedZero` in the functor
  variable over any abelian `C` with enough projectives; no specialization.
- `balancing_zero_naturality` — genuine naturality square, composing two `leftDerivedZeroIsoSelf`
  naturality squares.
- `iso_of_sixTerm_exact` — non-trivial iso from a six-term exact window with vanishing neighbours.

**3. Axiom cleanliness — PASS.** See item 1; no `sorryAx` on any of the five decls.

**Cleanup landed:** reflowed the `balancingIsoZero` docstring (former lines 170–171) to clear two
`linter.style.longLine` warnings (>100 chars). No remaining long-line warnings in
`Problem8_2_6.lean`. (The two warnings in the out-of-scope `Problem8_2_6_ii_Crux.lean` are left
alone.)

## Current frontier

Review complete; no defects found. Only a docstring reflow changed.

## Overall project progress

Chapter 8 derived-functor `Tor`/balancing stream confirmed sound. `Problem_8_2_6_iv` and its
infrastructure are sorry-free and non-vacuous. Out of scope and still open: `Problem_8_2_6_ii`
(a separate remaining `sorry`).

## Next step

Merge this confirmation PR. Remaining Ch8 work: `Problem_8_2_6_ii`. Chapter 5 feature queue:
#6648 (induction from `A₄`), #6649 (induction from `ℤ₂×ℤ₂`).

## Blockers

None.
