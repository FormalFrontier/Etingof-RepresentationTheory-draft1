# Progress — review session 7344f31e (#2832)

## Accomplished

Audit of PR #2795 (β.3 off-block vanishing, +154 lines in
`Theorem5_22_1.lean`) for review issue #2832.

Empirical probing on top of the merged PR, restoring status quo after
each probe:

- **Heartbeat bumps.** Lowered each `set_option` independently and
  located the failure thresholds.
  - `maxHeartbeats`: status quo 800000; passes at 300000; fails at 250000
    (`whnf` timeout at line 2130).
  - `synthInstance.maxHeartbeats`: status quo 800000; passes at 32000;
    fails at 25000 (`typeclass` timeout at line 2153).
  - Recommended: 400000/200000 (matches β.2
    `trace_symGroupAction_eq_spechtModuleCharacter`,
    `Theorem5_22_1.lean:1017-1020`; ≥1.33× headroom).
  - Filed **#2840** (refactor follow-up, ≤5-line config change).

- **`isIdempotentElem_eq_zero_of_trace_eq_zero`.** Searched
  `Mathlib/LinearAlgebra/Trace.lean`,
  `Mathlib/LinearAlgebra/Projection.lean`, and adjacent for any
  CharZero specialization of `IsProj.trace`; only the unsigned
  `LinearMap.IsProj.trace` and unrelated nilpotent-trace lemmas exist.
  Filed **#2841** (Mathlib upstream tracker, mirrors #2564). Local
  `private` status is appropriate until the upstream PR is open.

- **`youngSymmetrizerK_complex_eq`** (`Theorem5_22_1.lean:2124`).
  Not a duplicate; the existing third bridge `youngSym_coeff_cast'`
  (`Theorem5_22_1.lean:1906`) is the *coefficient* cast, not the
  algebra-level equality. The new lemma is used three times in the
  β.3 proof, so factoring is a clear win. Keeping `private` is
  appropriate. No follow-up.

- **`h_label` shape.** The `(la', h_label, h_ne)` triple is the
  natural shape for callers destructuring the β.2 existential
  per-block (which is the use pattern documented in #2693 and #2708).
  A wrapper accepting the raw existential is uglier. No follow-up.

Audit summary posted on PR #2795
(https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/2795#issuecomment-4472133959).
Issue #2832 closed.

No code changes — this is a review session.

## Current frontier

Queue depth post-audit:

- **#2564** — Mathlib-upstream tracker (waiting on
  https://github.com/leanprover-community/mathlib4/pull/38583).
- **#2825** — `FieldGenericInfiniteType.lean` split (sequenced
  after K_{1,4} per-(F, Q) chain).
- **#2840** — heartbeat reduction on β.3 (refactor, ≤5 lines)
  — newly filed.
- **#2841** — Mathlib upstream tracker for
  `isIdempotentElem_eq_zero_of_trace_eq_zero` — newly filed,
  low-priority.

Active in-flight: #2802, #2694, #2550 (pr-repair); #2804 (D̃₅ Sub B
feature, claimed).

## Overall project progress

Stage 3 (Lean formalisation). β-chain of Schur-Weyl L_i C-4a-i now
fully merged on `main` with audit closed. Two small follow-ups land
on the queue, both consequence of the audit; neither blocks downstream
work on #2693 / #2708.

Sorry-count delta this turn: **0** (review session, no code changes).

## Next step

- Next worker can pick #2840 (trivial refactor, ≤5 lines) or any
  other unclaimed item. #2841 is a tracker — no immediate work
  beyond opening a Mathlib PR.
- The `/replan` cascade (#2693 / #2708 / #2612 etc.) is unchanged
  from the previous planner cycle (814121aa).

## Blockers

None for the review itself. Standing constraints unchanged.
