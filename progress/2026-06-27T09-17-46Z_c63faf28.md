## Accomplished

One-shot dispatch on **#5515** (construct the contragredient pairing
`AlgIrrepGLDual ⊗ AlgIrrepGL → k`). Assessed scope: the deliverable requires a
*nonzero* `GL_n`-invariant pairing, which exists iff `L*_λ ≅ L_λ^∨`. Since
`AlgIrrepGLDual n lam k := AlgIrrepGL n lam.w0Twist k` is the *Schur-module*
realization of the contragredient and the issue's pairing must be typed onto it,
a genuine nonzero pairing cannot be built without the **contragredient identity**
`L_{w₀λ} ≅ L_λ^∨` — a substantial GL theorem (the linear dual `L_λ^∨` has negative
weights, outside the repo's polynomial `formalCharacter` framework; it needs a
`det^s`-twist normalization plus the inverted-variable Schur identity, then
`iso_of_formalCharacter_eq_schurPoly`). This does not fit one session as a
monolith, so I landed the elementary brick and decomposed the rest.

**Landed (sorry-free, axiom-clean — `propext`/`Classical.choice`/`Quot.sound`
only):** `Chapter5/ContragredientPairing.lean` — the "contragredient pairing is the
canonical evaluation" content of the issue:
- `contractLeft_dual_invariant` — the canonical evaluation
  `contractLeft k V : Module.Dual k V ⊗[k] V → k` is `GL_n`-invariant for the
  diagonal action `(ρ.dual g, ρ g)`: `⟨ρ.dual g · f, ρ g · v⟩ = ⟨f, v⟩`.
- `contractLeft_ne_zero` — nonzero map for nonzero finite-dimensional `V`.
Wired into the `Chapter5.lean` aggregator.

**Decomposed** into:
- **#5522** — contragredient identity `L*_λ ≅ L_λ^∨` (the hard GL part: a
  `GL_n`-equivariant `AlgIrrepGLDual ≃ₗ Module.Dual k (AlgIrrepGL …)`).
- **#5523** — assembly: transport `contractLeft` along #5522's iso to get
  `algIrrepDualPairing`, prove invariance, nonzero, and perfectness via
  simple-module kernels (`blocked` on #5522).

## Current frontier

The general evaluation brick is in `main` (this PR). The genuine
`AlgIrrepGLDual ⊗ AlgIrrepGL → k` pairing awaits the contragredient identity
(#5522) and assembly (#5523).

## Overall project progress

Stage 3 ongoing. Theorem 5.23.2(ii) (`Theorem5_23_2_ii_equivariant`) still a
`sorry`; this is brick (1) of its four bricks. Brick (1) is now: canonical
evaluation done, contragredient iso (#5522) + assembly (#5523) pending.

## Next step

Pick up **#5522** (contragredient identity). Likely sub-route: (a) prove
`formalCharacter (ρ.dual)` is `formalCharacter ρ` at inverse weights; (b) the
inverted-variable Schur identity `s_λ(x⁻¹)·(x₁⋯x_n)^s = s_{w₀λ+s}(x)`; (c)
`det^s`-twist both models into the polynomial range and apply
`iso_of_formalCharacter_eq_schurPoly`; (d) untwist via `charTwistRep` invariance.
#5522 may itself warrant decomposition along (a)/(b).

## Blockers

None for the landed brick. #5523 is blocked on #5522 by design.
