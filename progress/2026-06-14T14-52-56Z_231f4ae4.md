## Accomplished

Closed bridge-1 of the Schur-Weyl #5 Step E assembly (issue #4666): proved
`glTensorRep_schurWeyl_decomposition_equivariant_simple` in
`EtingofRepresentationTheory/Chapter5/PolynomialGLDecomposition.lean` (previously
a documented `sorry`).

Route taken (issue strategy A/B hybrid):

- Added `Theorem5_18_4_GL_rep_decomposition_explicit_simple` to
  `SchurWeylGLTransfer.lean` — the merge of `Theorem5_18_4_GL_rep_decomposition_explicit`
  (`Theorem5_18_4.lean`, gives `L_carrier` / explicit iso `e` / evaluation `he` /
  post-composition action formula) and the `hL_simple` block of
  `Theorem5_18_4_GL_rep_decomposition_simple` (this file, gives per-`L i`
  simplicity). Both destructure the same `Theorem5_18_4_bimodule_decomposition_explicit`
  and build the **same** `L i = FDRep.of ρ_i`, so the merge keeps `homA_simp`
  (for simplicity) and `he` (for the eval formula) from one destructure and
  assembles both clauses over one shared `ρ`. The helper
  `isSimpleModule_monoidAlgebra_GL_of_centralizer_simple` lives here, which is
  why the merged theorem sits in `SchurWeylGLTransfer.lean` rather than next to
  `_explicit`.
- The bridge in `PolynomialGLDecomposition.lean` is now a thin wrapper:
  `obtain` from `_explicit_simple` (carrying `hLsimp`), then re-run the
  equivariance computation copied verbatim from
  `glTensorRep_equivariant_schurWeyl_decomposition` (`FormalCharacterIso.lean`),
  which only consumes `he`/`h_act`. Simplicity passes straight through.

`lake build …Chapter5.PolynomialGLDecomposition` succeeds. My two files carry no
lint warnings (heartbeat-comment placement matched the repo convention: comment
immediately after each `set_option … in`).

## Current frontier

`PolynomialGLDecomposition.lean` now has exactly **one** `sorry`:
`polynomial_homog_rep_asModule_embeds_directSum_simple` (bridge-2, #4667). The
main result `decompose_polynomial_gl_rep` remains proven modulo only that bridge.

## Overall project progress

Phase 3 (formalization). Schur-Weyl chain: #4598, #4600, and now #4666 landed.
The §5.23 Step E assembly (#2482) is proven modulo the single remaining bridge
#4667. Downstream consumer is #6 (`iso_of_formalCharacter_eq_schurPoly`, #2483).

## Next step

Claim **#4667** (now unblocked by this PR once merged):
`polynomial_homog_rep_asModule_embeds_directSum_simple` — package #4598's
equivariant embedding + the unified decomposition `_explicit_simple` + the
`asModule` (`k`-linear → `MonoidAlgebra k GL_N`-linear) transfer + the `Fin m`-fold
product / `S ⊗ L` splitting into the single `R`-linear submodule embedding the
isotypic engine consumes.

## Blockers

None. #4667 is blocked on this PR (#4666) merging; nothing else.
