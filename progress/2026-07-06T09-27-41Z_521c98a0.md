## Accomplished

One-shot dispatch for issue #5920 (statement pass for remaining Chapter 5 exercises). Formalized
faithful Lean statements (sorry proofs) for **3 of the 5** listed exercises, each in its own new
file, added to the `Chapter5.lean` aggregator, all building:

- **Chapter5/Problem5_16_1.lean** — branching rule for `Sₙ ⊆ Sₙ₊₁`. (a) restriction character of
  `V_μ` equals the sum of characters of `V_λ` over `R(μ) = {λ ⊆ μ}`; (b) the Frobenius-reciprocity
  multiplicity of `V_λ` in `Ind V_μ` is the indicator of `A(μ) = {μ ⊆ λ}`. `R(μ)`/`A(μ)` defined via
  `YoungDiagram` containment; uses the project's `spechtModuleCharacter` and
  `Equiv.Perm.viaEmbeddingHom Fin.castSuccEmb`.
- **Chapter5/Problem5_24_2.lean** — `R_{k,N}` (invariant subalgebra of `MvPolynomial` on `k`-tuples
  of `N×N` matrices under simultaneous conjugation, built as an infimum of `AlgHom.equalizer`s) is
  generated by the trace-of-word functions `T_w = Tr(w(X₁,…,X_k))`: `Algebra.adjoin ℂ (range T) =
  invariantSubalgebra`.
- **Chapter5/Exercise5_27_3.lean** — over the semidirect-product setup of Theorem 5.27.1, the
  character formula (iv) implies classification properties (i) irreducibility, (ii) pairwise
  non-isomorphism, (iii) completeness. Phrased abstractly over the same data (the constructions in
  `Theorem5_27_1.lean` are `private`), mirroring the exact expressions of `Etingof.Theorem5_27_1`.

`progress/items.json` updated to `statement_formalized` for these three.

## Current frontier

Two of the five listed exercises are **not** done — they need substantial multi-object
infrastructure that does not fit a clean single statement, so they were decomposed into a follow-up
statement-pass sub-issue:

- **Problem 5.11.1** — decompose all representations of `A₅` induced from irreducibles of `Z₂`, `Z₃`,
  `Z₅`, `A₄`, `Z₂×Z₂`. Faithful "compute the decomposition" needs the full `A₅` irreducible
  catalogue (5 irreps, dims 1,3,3,4,5) plus explicit multiplicities, analogous to the S₃/S₄
  `Discussion5_11` files.
- **Exercise 5.27.2** — redo Problems 4.12.1(a), 4.12.2, 4.12.6 using Theorem 5.27.1. Those three
  problems (dihedral group, Heisenberg group, affine `ax+b` group) are themselves not yet
  formalized, and each is a separate semidirect-product classification.

## Overall project progress

Phase 3 statement pass ongoing. This turn added 3 Chapter 5 exercise statements. All new files build
(sorry warnings expected); `lake build EtingofRepresentationTheory.Chapter5` completes successfully
(8784 jobs).

## Next step

A worker should claim the follow-up sub-issue for Problem 5.11.1 and Exercise 5.27.2. Suggested
split: (1) build the `A₅` irreducible catalogue + the five induction decompositions for 5.11.1
(follow the `Discussion5_11_examples` / `Discussion5_11_S4S3` pattern); (2) state Exercise 5.27.2 by
exhibiting each of the three groups (D_N, Heisenberg, affine) as a semidirect product `A ⋊[φ] G` with
`A` finite abelian, so Theorem 5.27.1 applies — or first formalize 4.12.1(a)/4.12.2/4.12.6.

## Blockers

None. The two deferred exercises are scoped into a follow-up sub-issue.
