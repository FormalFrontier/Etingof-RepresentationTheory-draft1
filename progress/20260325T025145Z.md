## Accomplished

Deep analysis of the 2 remaining sorrys in `Proposition6_6_6.lean` (issue #1716):

1. **Identified root cause**: Both sorrys are blocked by a `Decidable.casesOn` dependent type issue in `equivAt_eq_sink`. The definition uses `cases (‹DecidableEq Q› i i)` internally combined with `classical`, creating two incompatible `Decidable` instance sources. Outside the definition, the result is wrapped in `Decidable.rec(inst i i)` which cannot be reduced via `generalize`, `rw`, or `simp`.

2. **Documented the blocker**: Added detailed comments to the sorry at line ~940 explaining the three specific failure modes (`generalize` fails, `rw` fails, `classical` interference).

3. **Identified the fix**: `equivAt_eq_sink` needs to be redesigned to either:
   - Bundle the naturality proof inside the `cases` block (returning `PSigma`)
   - Avoid `cases` on `DecidableEq` entirely (use `if`/`dite` or `Classical.decEq` consistently)

4. **Committed documentation** and commented on issue #1716 with full technical analysis.

## Current frontier

`Proposition6_6_6.lean`: 2 sorrys remain, both blocked by definition design issue in `equivAt_eq_sink`.

## Overall project progress

- Proposition 6.6.6 sink case: structure complete, `a≠i,b≠i` naturality proved, `a≠i,b=i` naturality blocked by `Decidable.casesOn` issue
- Proposition 6.6.6 source case: entirely sorry'd, same blocker
- The blocker is a **definition design issue**, not a proof difficulty — the mathematical content is straightforward

## Next step

Redesign `equivAt_eq_sink` to avoid the `Decidable.casesOn` + `classical` interaction. Two approaches:
1. **Bundle approach**: Modify `equivAt_eq_sink` to return `PSigma (fun equiv => naturality_property)`, proving naturality inside the `cases` block where `Decidable` is already eliminated
2. **Avoid `cases` approach**: Use `Classical.decEq Q` as the single `DecidableEq` source throughout, or restructure to avoid `Decidable.casesOn` at the `(i,i)` position entirely

This redesign should be filed as a separate issue since it touches the core definition infrastructure.

## Blockers

- **`equivAt_eq_sink` design** (#1716): `Decidable.casesOn` dependent type wrapper prevents external computation. Requires definition redesign to unblock both sorrys.
