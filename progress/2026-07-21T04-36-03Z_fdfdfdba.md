## Accomplished

Review session (issue #7140): statement-fidelity & non-vacuity audit of
`Etingof.Theorem5_4_6` (finite group with a conjugacy class of size `p^k`,
`p` prime, `k > 0`, has a proper nontrivial normal subgroup / is not simple).

- **Verdict: FAITHFUL — axiom-clean, no defect. No issue filed.**
- Report written to
  `progress/reviews/2026-07-21-ch5-theorem5_4_6-prime-power-conjclass-fidelity.md`.
- Confirmed conclusion `∃ N, N.Normal ∧ N ≠ ⊥ ∧ N ≠ ⊤` = "not simple"; `⊥/⊤`
  framing correct and no trivial-`G` vacuity (`k > 0` forces `|C| ≥ 2`, `g ≠ 1`).
- Confirmed hypothesis `card {h // IsConj g h} = p^k` = conjugacy-class size,
  `k > 0` strict and load-bearing.
- Verified the proof follows the book: identity (5.4.1) column orthogonality
  appears explicitly (`sum_dim_character_eq_zero`), Theorem 5.4.4 and Proposition
  5.2.5 used, `character_isIntegral` and `no_prime_power_conjClass` are local
  `private` lemmas (not Mathlib imports). Noted proof-shape nuance: Lean uses the
  by-contradiction algebraic-integer route (`S = -1/p`) rather than the book's
  explicit `H = ⟨ab⁻¹⟩` construction; same math, not a short-circuit.
- `#print axioms` = `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.
- `lake build ...Theorem5_4_6` exits 0 (two cosmetic lint warnings only).
- Non-vacuous instance: `S₃` (transposition class size `3 = 3¹`, `A₃` witnesses
  the normal subgroup).

## Current frontier

Confidence phase: per-chapter flagship statement-fidelity audits. Ch5 Theorem
5.4.6 now audited (5.4.3 Burnside was already done).

## Overall project progress

Proof-complete; in the confidence/audit phase. This audit adds one more
FAITHFUL, axiom-clean flagship to the review record.

## Next step

Continue confidence-phase audits of remaining un-audited flagship theorems in
other chapters, or process the open PR-repair / feature queue.

## Blockers

None.
