## Accomplished
Formalized the **uniqueness half of Krull-Schmidt for finite-length modules** (issue #7185,
Remark 3.8.6), closing the fidelity gap. Added to `Chapter3/Remark3_8_6.lean`:

- `Etingof.isNilpotent_sum_of_finiteLength_indecomposable` — sum form of Fitting's lemma: a
  finite sum of nilpotent endomorphisms of a finite-length indecomposable module is nilpotent
  (ported from Lemma 3.8.2(ii) using the finite-length dichotomy, no ground field).
- `Etingof.krull_schmidt_uniqueness_finiteLength` — the headline: any two indecomposable
  decompositions of a finite-length module have equal length and match up to a reindexing
  bijection with isomorphic summands.

Method: ported the finite-dimensional Theorem 3.8.1 proof
(`krull_schmidt_find_iso_summand` + `krull_schmidt_uniqueness_aux`) with `Module.length`
replacing the k-dimension as the strictly-decreasing induction measure (`Submodule.length_lt`),
and `[IsArtinian A V] [IsNoetherian A V]` replacing `[FiniteDimensional k V]`. The two Fitting
inputs became `isNilpotent_or_isUnit_of_finiteLength_indecomposable` and the new sum corollary;
submodule Artinian/Noetherian instances resolve automatically, so no ground field is needed. The
projection/complement bookkeeping carried over verbatim.

Verified: `lake build` of the file succeeds, no sorries. `#print axioms` on both new decls shows
only `propext, Classical.choice, Quot.sound` (no `sorryAx`). Updated `progress/items.json`:
`Chapter3/Remark3.8.6` fidelity `partial` → `verified`.

## Current frontier
Item `Chapter3/Remark3.8.6` is now fully faithful (existence + uniqueness). The Krull-Schmidt
theorem for finite length is complete.

## Overall project progress
Stage 3.7 fidelity re-audit follow-ups in progress. Three sibling fidelity issues remain in the
queue: #7180 (Theorem 5.22.1 part 3, dimension formula), #7187 (Theorem 5.10.1 Frobenius
reciprocity direction — claimed by another agent this cycle), #7188 (Example 5.12.3 distinguish the
two 3-dim S₄ irreps — claimed by another agent this cycle).

## Next step
Pick up #7180 (Weyl dimension product) or the remaining sibling fidelity issues once unclaimed.

## Blockers
None.
