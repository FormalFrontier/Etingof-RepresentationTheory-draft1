## Accomplished

Feature session, issue #7037 (docstring-fidelity sweep). Corrected two stale doc
blocks in `EtingofRepresentationTheory/Chapter5/SchurModuleSpecialBlock.lean`:

1. Section banner (~line 50): removed the "This is the only genuinely
   character-theoretic ingredient ... still open. Tracked as ... issue #4679" claim.
   #4679 is CLOSED (it closed exactly this theorem). New text states the ingredient
   is proved, delegating to `simpleSubmodule_iso_of_spechtCharacter_eq` in
   `Theorem5_22_1.lean`.
2. Theorem doc-comment (~line 61): replaced the "Route (deferred): ..." lead with
   "Proof (via `simpleSubmodule_iso_of_spechtCharacter_eq` in `Theorem5_22_1.lean`):"
   describing the completed proof rather than future work.

Comment-only diff — no signatures, imports, def/instance bodies, proof terms, or
`set_option` lines touched.

Verification recorded:
- Comment-stripped `sorry` count for the file = 0.
- `#print axioms simpleSymGroupImageSubmodule_iso_of_spechtCharacter_eq` =
  `[propext, Classical.choice, Quot.sound]` — no `sorryAx`.
- `lake build EtingofRepresentationTheory.Chapter5.SchurModuleSpecialBlock` exits 0
  (8613 jobs; only pre-existing deprecation/linter warnings).

## Current frontier

Issue #7037 complete; PR being opened.

## Overall project progress

Late-stage docstring-fidelity sweep of Chapter 5 (same pattern as merged
#7028/#7029/#7036 and #7038). The one genuine code `sorry` project-wide remains
`Chapter2/Problem2_16_3.lean` (G₂, owned by #6340).

## Next step

Sibling issue #7039 (SimpleSubrepExtraction.lean stale "still open / long-term
blocker" claim about Theorem5_23_2_i) is still unclaimed — a natural next pick.

## Blockers

None.
