## Accomplished

Closed issue #4661 (Ch6 #4647 sub-A): the corrected eigenvalue-site D̃₅ tube
`d5tildeGammaTube_F` + `lam`-threaded `d5tildeRep_kQ`. Feature session.

**`FieldGenericStar.lean`** (shared, for the whole D̃ family):
- `eigTailSumLin F lam m = (Λ−I)⁻¹ = ((lam−1)·id + N)⁻¹`, the eigenvalue
  analogue of `cumTailSumLin`, built from `eigTailSumMatrix` (upper-triangular
  entry `(-(lam−1)⁻¹)^{j−i} (lam−1)⁻¹`).
- Closed-form / recursion lemmas `eigTailSumLin_apply` / `_apply_last` /
  `_apply_succ`, and the **sorry-free two-sided retraction** lemmas
  `eigSub_eigTailSumLin` ((Λ−I)∘S = id) and `eigTailSumLin_eigSub`
  (S∘(Λ−I) = id) for `lam ≠ 1`.

**`FieldGenericD5Tilde.lean`**:
- `import FieldGenericTube` (for `jordanShiftLinGen`; no import cycle).
- `d5tildeGammaTube_F = [[I,I],[I,Λ]]` and closed-form inverse
  `d5tildeGammaTubeInv_F = [[I+K,−K],[−K,K]]` (`K = eigTailSumLin`), both real
  `def`s built from the shared star primitives.
- Section 3c: sorry-free retraction lemmas `d5tildeGammaTube_gammaTubeInv_F`
  and `d5tildeGammaTubeInv_gammaTube_F` (`lam ≠ 1`), via the bridge
  `jordan_eigTailSumLin_sub_F` and the `eigTailSumLin` retractions.
- `d5tildeTubeLam F` generic pick (∈ F ∖ {0,1}, via `Infinite.exists_notMem_finset`
  since `IsAlgClosed F ⟹ Infinite F`) with `_ne_zero` / `_ne_one` corollaries.
- `d5tildeRepMap_kQ` gains a `(lam : F)` parameter; `{2,3}`/`{3,2}` edges now use
  the tube maps. `d5tildeRep_kQ` gains `[IsAlgClosed F]` and threads
  `d5tildeTubeLam F` (t125 pattern). `[IsAlgClosed F]` added to the 9 dependent
  theorems (`dimVec`, the leaf-collapse helpers, `gamma_containment`,
  `leaf_equalities`). `d5tildeRep_kQ_dimVec` keeps its statement.
- `d5tildeRep_kQ_leaf_equalities` restated against the corrected rep; its body is
  now `sorry` (the old canonical-branch proof reasoned about the refuted
  `[[I,I],[I,N]]` γ and is invalid on the corrected tube — preserved in git
  history for sub-B).

## Current frontier

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde` passes;
all importers (D6/D7/D8/parametric D̃, NonAdjacentBranches, Assembly, Tpqr) build.
The legacy `d5tildeGamma_F` / `gamma_from_embed*` / `core_F` / `gamma_containment`
lemmas remain (map-agnostic; reusable by sub-B).

## Overall project progress

Stage 3 (formalization) in progress. This retires the D̃₅ pattern-setter
construction blocker: the corrected rep + sorry-free `(Λ−I)⁻¹` machinery are now
in place for sub-B (#4662, leaf equalities) and sub-C (#4663, indecomposability),
and as the template for D̃₆/₇/₈/parametric (#4649–#4652).

## Next step

- sub-B (#4662): prove `d5tildeRep_kQ_leaf_equalities` on the corrected tube
  (true in all branches now; central transport rewritten around the Section 3c
  retractions). Combo-C (#2853) acceptance test.
- sub-C (#4663): close `d5tildeRep_kQ_isIndecomposable` via the §4 reduction
  (`eigenvalue_jordan_invariant_compl_trivial_gen`), then re-point
  `d5tilde_not_finite_type_per_kQ`.

## Blockers

None. Sorry delta in `FieldGenericD5Tilde.lean`: 4 real `sorry` tactics → 2
(`leaf_equalities`, `isIndecomposable`); all new defs/lemmas are sorry-free.
