## Accomplished

Completed the full classification of the complex irreducibles of `DihedralGroup N`
(Problem 4.12.1(a), issue #7248), closing the part (a) fidelity gap (#7219/#7225) that had
reduced part (a) to just the dimension dichotomy.

Added to `EtingofRepresentationTheory/Chapter4/Problem4_12_1.lean` (all sorry-free,
`#print axioms` = `[propext, Classical.choice, Quot.sound]`):

- `TwoDimIdx N` — canonical index `{j // 0 < j.val ∧ 2·j.val < N}` for the 2-dim irreducibles
  (one representative of each pair `{j, -j}` with `2j ≠ 0`), with `card_TwoDimIdx : card = (N-1)/2`.
- `Vrep_fdRep_simple`, `Vrep_finrank`, `Vrep_fdRep_character_r` — `FDRep`-level facts about the
  2-dim family, bridging `Vrep_irreducible`/`Vrep_trace_r` into `FDRep ℂ (DihedralGroup N)`.
- `TwoDimIdx.eq_of_char_eq` — two canonical indices with equal character at `r 1` are equal
  (`a + a⁻¹ = b + b⁻¹ ⇒ a = b ∨ ab = 1`; the `ab = 1` branch is excluded by the normalization).
- `surj_of_injective_of_sum_eq` — local copy of the positive-weight pigeonhole lemma (kept local
  to avoid importing the 1000-line Heisenberg file).
- **`simple_iso_char_or_Vrep`** — the exhaustiveness theorem: every simple `FDRep ℂ (DihedralGroup N)`
  is isomorphic to a 1-dim `charRep χ` or a 2-dim `Vrep N j` (`2j ≠ 0`). Mirrors
  `Problem4_12_2.simple_iso_charRep_or_rhoHom`: builds the family `(chars) ⊕ TwoDimIdx N`, proves
  pairwise non-isomorphism, matches `∑ dim² = 2N` against `exists_simples_sum_finrank_sq_eq_card`,
  and pigeonholes.
- Count headlines: `two_dim_simples_card_odd` (=(N-1)/2), `two_dim_simples_card_even` (=(N-2)/2),
  `irreps_sum_sq` (#1dim·1 + #2dim·4 = 2N), `total_irreps_card_odd` (=2+(N-1)/2),
  `total_irreps_card_even` (=4+(N-2)/2).

Added three project imports (`Example4_3_S3`, `Exercise4_2_3`, `Infrastructure.IrreducibleEnumeration`).
Updated `progress/items.json`: `Chapter4/Problem4.12.1` → `status: sorry_free`, refreshed
`coverage_note`, `fidelity` reset to `unchecked` (complete part (a) awaits fresh Stage 3.7 re-audit).

## Current frontier

Problem 4.12.1 part (a) is now fully classified and sorry-free. The branch also carries the
1-dim character count from PR #7249 (issue #7225 deliverable 1), which this work depends on;
that PR was OPEN and MERGEABLE (CI running) at the start of this session.

## Overall project progress

Stage 3 formalization ongoing. Problem 4.12.1 (a) + (b) now fully formalized. The dihedral
classification joins the Heisenberg (4.12.2) and affine (4.12.6) full classifications as
completed Artin-Wedderburn-style enumerations.

## Next step

- A Stage 3.7 fidelity review of the now-complete Problem 4.12.1 (fidelity reset to `unchecked`)
  can flip it to `verified`.
- The remaining unclaimed review issues (#7243 Dixmier, #7244 central idempotents, #7245 metadata
  reconciliation) are available for review sessions.

## Blockers

None. Depends on PR #7249 (1-dim character counts) which is merged into this branch; once #7249
lands on `main` the PR diff for this work reduces to the classification additions.
