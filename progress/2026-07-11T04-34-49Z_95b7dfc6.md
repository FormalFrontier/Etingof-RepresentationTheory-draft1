## Accomplished

Discharged the `sorry` in `Etingof.Problem953.compositionFactors_areLinked`
(Ch9 Problem 9.5.3(ii), steps 4–5, issue #6405): the composition factors of an
indecomposable finite-length module all lie in a single linkage class. The proof
is sorry-free and depends only on `propext`, `Classical.choice`, `Quot.sound`.

Work done:

1. **Refactor to break an import cycle.** The proof needs the Ext dévissage
   (`Problem9_5_3_Devissage.lean`) and splitting (`Problem9_5_3_Splitting.lean`),
   but `Devissage` imported `Problem9_5_3` for the composition-factor helpers.
   Moved the `CompositionFactor` section out of `Problem9_5_3.lean` into a new
   low-level file `Problem9_5_3_CompositionFactor.lean`; `Devissage` now imports
   that instead, so `Problem9_5_3` can import the Ext machinery without a cycle.
   Names/namespaces unchanged, so downstream is unaffected (re-exported through
   `Problem9_5_3`).

2. **New composition-factor helpers** (in `Problem9_5_3_CompositionFactor.lean`):
   `IsCompositionFactor.nontrivial`, `.of_linearEquiv` (iso-transport),
   `.of_simple` (a factor of a simple module is isomorphic to it), and the key
   **SES subadditivity** `.of_submodule_or_quotient` (a factor of `Y` is a factor
   of a submodule `K` or of `Y ⧸ K`).

3. **New file `Problem9_5_3_Connectivity.lean`** (steps 4–5):
   - `exists_isCompl_of_ext_subsingleton`: internal-submodule form of step 3 —
     `Ext¹(X/Q, Q)` subsingleton yields a complementary submodule `P` (retraction
     → `LinearMap.IsProj` → `IsProj.isCompl`), with `P ≅ X/Q`.
   - `factor_quot_of_isCompl`: sorts factors of `X/Q` (for `Q` the image of a
     complemented summand `B ≤ N`) into factors of the complement `A` or of the
     top `X/N`, via subadditivity + `quotientQuotientEquivQuotient`.
   - `exists_isCompl_split`: induction on `IsFiniteLength`, producing complementary
     `P ⊞ Q = X` with factors of `P` linked to `S` and factors of `Q` unlinked;
     each layer sorts its simple top and splits off the complementary part.
   - `compositionFactors_areLinked_aux`: apply `Indecomposable.2` to
     `M ≅ ↥P ⊞ ↥Q`; the vanishing summand forces `S`, `T` into one class.

`Problem9_5_3.lean` now discharges the theorem by calling the aux lemma.

## Current frontier

`Etingof.Problem953.compositionFactors_areLinked` is proved. `Chapter9`
aggregate builds (2682 jobs). The only remaining `sorry` in `Problem9_5_3.lean`
is part (i), `blocks_equiv_indecomposableCentralIdempotents` — out of scope for
#6405.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 9.5.3(ii) is now fully proved (steps 1–5
complete); `exists_block_of_indecomposable` is consequently sorry-free too.

## Next step

Part (i) `blocks_equiv_indecomposableCentralIdempotents` remains for a follow-up.
The new `IsCompositionFactor.of_submodule_or_quotient` (SES subadditivity) is a
reusable tool for other Jordan–Hölder-flavoured arguments.

## Blockers

None.
