## Accomplished
- Claimed #6191 (Problem 2.7.5(a): center + simple ring of the q-Weyl algebra, q not a root of unity).
- Proved **deliverable 1**, `center_eq_bot_of_not_isOfFinOrder`, **sorry-free** in
  `EtingofRepresentationTheory/Chapter2/Problem2_7_5.lean`. Added reusable coordinate
  infrastructure: `linearCombination_qMono_apply_zero`, `mem_qWeyl_eq_linearCombination`
  (coefficients via evaluation at b(0,0)), `finsupp_sum_single_emb_apply` (injective-reindex
  single-coefficient extraction), `zpow_eq_one_imp` (not-a-root-of-unity fact). The proof commutes
  a central element with x and y, forcing every non-origin monomial coefficient to vanish.
- Split **deliverable 2** (`isSimpleRing_of_not_isOfFinOrder`) into new issue #6202, with a
  self-contained plan that reuses the coordinate infrastructure above.

## Current frontier
`Problem2_7_5.lean` compiles with three sorries remaining, all owned elsewhere:
`isSimpleRing_of_not_isOfFinOrder` (#6202), `center_of_isOfFinOrder` and
`finrank_irreducible_eq_orderOf` (sibling issues, untouched).

## Overall project progress
Phase 3 formalization in progress across chapters. This turn advances Chapter 2 Problem 2.7.5:
parts (b) already done; part (a) center now done; part (a) simplicity queued as #6202.

## Next step
Claim #6202 and prove `isSimpleRing_of_not_isOfFinOrder` via the conjugation coefficient lemma +
minimal-support descent, following the plan in that issue and reusing the coordinate helpers.

## Blockers
None. #6202 is unblocked (its dependency, the coordinate infra, lands with this PR).
