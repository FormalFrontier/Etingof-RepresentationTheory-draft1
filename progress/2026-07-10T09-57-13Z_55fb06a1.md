## Accomplished
Worked issue #6136 (Ch4, mathematical crux of base-change monotonicity #6127/#6098):
`#(simple A) ≤ #(simple (K ⊗[k] A))` for a fin-dim semisimple k-algebra A and any field
extension K/k, separability-free.

New file `EtingofRepresentationTheory/Chapter4/Exercise4_2_3_SemisimpleBaseChange.lean`
proves the main deliverable `Etingof.natCard_simpleModuleClasses_le_baseChange_of_isSemisimpleRing`,
assembled from Artin–Wedderburn via **five proven reusable helpers**:
- `simpleModuleClassesCongr` — transport simple iso-classes across a `ModuleCat` equivalence
  (uses `simpleProp_iff_of_equivalence` + `Equivalence.congrFullSubcategory` + `isoClassesEquivOfEquivalence`).
- `simpleModuleClassesCongrRingEquiv` — transport along a ring iso (`restrictScalarsEquivalenceOfRingEquiv`).
- `simpleModuleClassesMatrixEquiv` — matrix Morita invariance of the simple count (`ModuleCat.matrixEquivalence`).
- `nonempty_simpleModuleClasses` — a nonzero fin-dim algebra has ≥1 simple module (Artinian ⇒ IsAtomic ⇒ atom).
- `subsingleton_simpleModuleClasses_divisionRing` — a division ring has ≤1 simple module (only maximal left ideal is ⊥).

Main theorem rests on exactly TWO deferred `sorry`ed reusable lemmas (both stated precisely,
Prop-level so allowed):
- `natCard_simpleModuleClasses_pi` — product-ring simple count = sum over factors.
- `nonempty_baseChange_pi_matrix_algEquiv` — K⊗ distributes over ∏ Matrix(D i).
Added file to `Chapter4.lean` aggregator so CI builds it.

## Current frontier
Two deferred lemmas remain sorried; parent theorem otherwise complete and compiling.

## Overall project progress
Ch4 base-change chain (#6127): #6135 (rad-quotient) and #6134 (surjection) claimed elsewhere;
this issue #6136 (semisimple base-change crux) now landed as a top-down partial. Remaining crux
sub-lemmas are #6146 and #6147.

## Next step
Pick up #6146 (product-ring simple count) and/or #6147 (base-change algebra iso chain). Once both
land, `#print axioms` on the main theorem should be sorry-free, and #6127 assembly can proceed.

## Blockers
None. Both sub-lemmas are independent and self-contained.
