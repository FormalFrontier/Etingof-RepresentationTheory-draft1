## Accomplished

Issue #6074 (Ch4 #6047 deliverable 2): the **trace form on the cocenter** `τ_M`.
Added a `TraceForm` section to
`EtingofRepresentationTheory/Chapter4/Exercise4_2_3_Cocenter.lean`:

- `moduleEnd k M : k[G] →ₐ[k] Module.End k M` — the `k`-algebra action map `x ↦ (x • ·)`
  (via `Algebra.lsmul`), plus `moduleEnd_apply`.
- `traceForm k M : k[G] →ₗ[k] k` — the trace functional `x ↦ trace(x acting on M)`
  (`(LinearMap.trace k M).comp (moduleEnd k M).toLinearMap`), with `traceForm_apply`.
- `traceForm_mul_comm` — `trace(x*y) = trace(y*x)` via `LinearMap.trace_mul_comm`.
- `commSubmodule_le_ker_traceForm` — the trace form vanishes on `[k[G], k[G]]`.
- `traceFormQ k M : Cocenter k G →ₗ[k] k` — the descent `τ_M` via `Submodule.liftQ`.
- `traceFormQ_mkQ` (simp), `traceFormQ_mkQ_single`, `traceFormQ_classRep` — deliverable 3
  eval lemmas: `τ_M [single g 1] = trace(g acting on M) = χ_M(g)`.

All three deliverables complete. Sorry-free; `lake build` of the file is clean (no errors,
no warnings in the new section).

## Current frontier

Trace-form API for the cocenter is in place. Next in the #6047 chain is the
linear-independence argument (#6064, currently blocked): the trace forms of distinct simple
`k[G]`-modules are linearly independent functionals on `C`, giving
`#(simple modules) ≤ finrank C = #ConjClasses`.

## Overall project progress

Phase 3 formalization, Chapter 4. Exercise 4.2.3 counting half: deliverable 1
(`finrank_cocenter_eq_card_conjClasses`, PR #6079) + deliverable 2 (this PR) done;
deliverable 3 linear-independence (#6064) remains.

## Next step

Land #6079 (deliverable 1) and this PR, then pick up #6064 (Wedderburn linear-independence
of trace forms) once unblocked.

## Blockers

This work depends on the Cocenter file introduced by PR #6079 (deliverable 1 of #6047),
which is not yet merged to `main` (no auto-merge set, CI in progress). This branch is stacked
on `agent/ec05c716` so it builds; the PR carries #6079's commits until #6079 lands, after
which the diff reduces to just the `TraceForm` section. The issue's `depends-on: none` was
stale ("merged from #6047" — it is in an open PR, not merged).
