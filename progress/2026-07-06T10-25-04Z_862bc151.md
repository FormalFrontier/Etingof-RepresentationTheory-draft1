## Accomplished

Statement pass for Chapter 4 exercises under issue #5944 (remaining 4.12.x):

- **Problem 4.12.2** (`Problem4_12_2.lean`): constructed the Heisenberg group over `𝔽_p`
  (triples `⟨a,b,c⟩`, group axioms proved, `Fintype`/`DecidableEq`, order `p³` stated).
  Faithful statements: (a) `exists_unique_rep`, (b) `irreducible_iff` (irreducible ⇔ `z≠1`),
  (c) `one_dim_reps_card` (`p²`) and `R1_decomposes`, (d) `irreducible_dim` (dim `1` or `p`).
- **Problem 4.12.6** (`Problem4_12_6.lean`): constructed the affine `ax+b` group over a finite
  field `K` (order `q(q-1)`). Statements: `one_dim_reps_card` (`q-1`), the zero-sum
  representation `V` with `zeroSum_invariant`/`zeroSum_finrank`/`zeroSum_irreducible`, and
  `irreducible_dim` (dim `1` or `q-1`).
- **Problem 4.12.8** (`Problem4_12_8.lean`): (a) classification of finite subgroups of `SO(3)`
  as a disjunction (cyclic / dihedral / `A₄` / `S₄` / `A₅`); (b) the `SU(2)` double-cover
  relation via `SU(2) → SO(3)`.
- **Problem 4.12.9** (`Problem4_12_9.lean`, imports 4.12.2): character of `R_z` and
  tensor-product characters (`R_z ⊗ R_w ≅ p·R_{zw}` for `zw≠1`; central `p²` for `zw=1`).

All four files build (only `sorry`/expected warnings). `items.json` updated to
`statement_formalized` for 4.12.2/4.12.6/4.12.8/4.12.9. Added to `Chapter4.lean` aggregator.

## Current frontier

Problem **4.12.5** (A₅ on the icosahedron) was NOT formalized here: a faithful statement needs
the icosahedron geometry (A₅ acting on 12 vertices / 20 faces / 30 edges) and the A₅
irreducibles / character table, none of which are in Mathlib. Split into follow-up
statement-pass issue **#5948** (with the verified decompositions recorded there).

## Overall project progress

Stage 3 (formalization) statement pass. Chapter 4 exercise backlog: 4.12.1, 4.12.4, 4.12.7,
4.12.10, 4.12.11 previously done; this turn adds 4.12.2, 4.12.6, 4.12.8, 4.12.9. Only 4.12.5
remains (deferred to #5948).

## Next step

Pick up #5948 (Problem 4.12.5) once A₅ / icosahedron scaffolding is available, or state it via
hypothesized fixed-point counts + an explicitly-defined A₅ character table. Later: fill the
`sorry` proofs in the four files landed this turn.

## Blockers

None for #5944. #5948 is blocked on missing Mathlib infrastructure (icosahedron action, A₅
irreducibles) — a statement can still be written via hypothesized characters (see #5948).
