## Accomplished

Worked **#4722** (Ch6 #4649 sub-B: orientation-generic `d6tildeRep_kQ_leaf_equalities`
on the corrected D̃₆ eigenvalue-site tube). Landed a **partial**:

- Added the sorry-free corrected-tube containment helper
  `d6tilde_gammaTube_containment_F` in `Chapter6/FieldGenericD6Tilde.lean`
  (γ_λ = `d5tildeGammaTube_F`, `Λ = λ·id + J`), the D̃₆ analogue of
  `d5tilde_gammaTube_containment_F`, threading the extra internal `3-4`
  identity edge via `hcol_main : Wmain⟨3⟩ = Wmain⟨4⟩` and routing through
  `gammaTube_from_embed1_F` / `gammaTube_from_embed2_F` + `d6tilde_core4_F`.
- Proved the **all-canonical** orientation branch of
  `d6tildeRep_kQ_leaf_equalities` inline (replacing the monolithic `sorry`
  with a structured 5-edge `rcases` tree). The internal `3-4` edge collapses
  orientation-independently via `d6tildeRep_kQ_chain_collapse`; three
  `compl_le_forces_eq` applications chain the containments into
  `W₁⟨0⟩ = W₁⟨1⟩ = W₁⟨5⟩ = W₁⟨6⟩`. Mirrors the landed D̃₅ all-canonical branch.
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD6Tilde` passes.

## Current frontier

`d6tildeRep_kQ_leaf_equalities` now carries `sorry` only in the 4 remaining
non-canonical leaf branches (mixed `v=4` leaves and central-reversed) plus the
pre-existing `d6tildeRep_kQ_isIndecomposable` stub.

## Overall project progress

Ch6 affine-tube leaf-equality track: D̃₆ now matches the landed D̃₅ "canonical
branch" state (#4527 sub-A analogue) on the corrected eigenvalue-site rep, with
a reusable sorry-free tube-containment helper in place.

## Next step

The mixed-direction (`v=4`-leaf) branches are **not separately derivable** from
leaf data alone — per the D̃₅ architectural finding **#4743** (filed after #4722),
the eigenvalue twist `Λ` lives entirely inside the central `γ_λ` (never a vertex
self-map), so untwisting through `γ_λ⁻¹` to pin the leaf-4 subspace is circular.
These branches must fold into `d6tildeRep_kQ_isIndecomposable` (**#4724**), where
the two centers fold and `M = Λ − I` becomes a usable self-map for
`eigenvalue_jordan_invariant_compl_trivial_gen`. The all-leaves-reversed combo-D
branch transports across `γ_λ` like the canonical branch and is the natural next
sorry-reducing increment (needs the reversed-leaf `core_F_proj` siblings, already
present in the file). Planner should re-scope #4722 to the achievable
eigenvalue-free orientations + defer mixed branches to #4724, mirroring how #4743
re-scopes D̃₅.

## Blockers

None for the partial. The "all orientations" deliverable of #4722 as originally
stated rests on a premise (the D̃₅ model closing all orientations) that does not
hold — D̃₅'s `d5tildeRep_kQ_leaf_equalities` is still `sorry` in its mixed
branches on `main`, and #4743 re-scopes it. The residual belongs in #4724.
