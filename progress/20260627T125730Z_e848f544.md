## Accomplished

One-shot dispatch on **#5517** (Peter-Weyl brick (4): assemble the direct-sum
equivariant matrix-coefficient map and reduce the capstone to bijectivity). Landed in
`Chapter5/Theorem5_23_2_PeterWeyl.lean`:

- `peterWeylMap (n) (k)` — the assembled map
  `⊕_λ L*_λ ⊗ L_λ →ₗ[k] Localization.Away (detPoly k n)`, built from the per-summand
  `peterWeylSummandMap` (#5516, `PeterWeylMatrixCoeff.lean`) via `DirectSum.toModule`.
- `peterWeylMap_of` (simp) — `peterWeylMap (DirectSum.of _ lam y) = peterWeylSummandMap`.
- `peterWeylSummandMap_map_equivariant` — per-summand equivariance lifted from pure
  tensors to all of `L*_λ ⊗ L_λ` via `TensorProduct.induction_on`.
- `peterWeylMap_equivariant` — `GL_n × GL_n`-equivariance of the assembled map
  (`peterWeylMap (peterWeylRHS (g,h) x) = localBiRep (g,h) (peterWeylMap x)`), by
  `DirectSum.induction_on` + `DirectSum.lmap_of` (since `peterWeylRHS` acts as
  `DirectSum.lmap` of the per-summand `TensorProduct.map` actions).
- `nonempty_equivariantEquiv_of_bijective` — the **reduction lemma**: given
  `Function.Bijective peterWeylMap`, `LinearEquiv.ofBijective` + `.symm` +
  `IsEquivariantEquiv.symm` of `peterWeylMap_equivariant` yields the capstone witness.
- `peterWeylMap_bijective` — the sole remaining `sorry` (the Cauchy decomposition).
- `Theorem5_23_2_ii_equivariant` rewritten to consume the reduction lemma; its previous
  standalone `sorry` is gone, so the only `sorry` in the file is now the bijectivity.

Supporting changes: added `instDecidableEqDominantWeight` (DominantWeight is a `def`, so
instance search won't unfold it to the underlying `Subtype`; `DirectSum.toModule`/`of`/
`induction_on` need `DecidableEq`, whereas `Representation.directSum` sidesteps it by
using only `DirectSum.lmap`). Narrowed the capstone's `k : Type*` to `k : Type` to match
the matrix-coefficient infrastructure (`peterWeylSummandMap` is `Type`-only). Wired the
file into the `Chapter5.lean` aggregator (it was previously omitted; CI already built it
per-file via the `find` rule).

`#print axioms` not run (sorry present). `lake build ...Theorem5_23_2_PeterWeyl` green;
only warning is the expected `sorry` on `peterWeylMap_bijective`.

## Current frontier

The equivariant *structure* of Peter-Weyl 5.23.2(ii) is complete. The single open input
is `peterWeylMap_bijective : Function.Bijective (peterWeylMap n k)` — the Cauchy
decomposition (linear independence + spanning of the matrix coefficients of the
pairwise-distinct `L_λ`), to be supplied from `PolynomialGLDecomposition.lean` /
`CauchyDetQuotient*`.

## Overall project progress

Stage 3 ongoing. Peter-Weyl bricks (1)-(4) now landed: per-summand map + equivariance
(#5516) and the assembled direct-sum map + equivariance + reduction-to-bijectivity
(#5517, this turn). Only the Cauchy bijectivity remains before 5.23.2(ii) is closed.

## Next step

Prove `peterWeylMap_bijective` via the Cauchy decomposition: the matrix coefficients of
the distinct irreducibles `L_λ` are linearly independent and span
`R = k[gᵢⱼ][1/det]`. This is the substantial representation-theoretic input and will
likely need its own predecessor bricks from `PolynomialGLDecomposition` /
`CauchyDetQuotient*`.

## Blockers

None.
