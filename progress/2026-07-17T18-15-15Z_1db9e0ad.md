## Accomplished
Claimed #6918 (extraction crux of #6913: `affine_one_branch_three_arms`, three-arm
decomposition of a one-branch affine tree). Assessed it as one large, irreducible
tree-decomposition proof and split off the reusable engine:

- Landed **`affine_arm_walk`** (sorry-free) in `Chapter6/Problem6_1_3_continued_tildeE.lean`
  — a public building block that linearizes a single hub-deleted component `S` (given as a
  nonempty, `v`-avoiding, internally-connected finset whose unique `v`-neighbour is `nb`) into
  a rooted arm `g 0 = nb, …, g (L-1)` with `L = S.card`, recording: `g` bijects `range L` onto
  `S`, injectivity, the only arm vertex adjacent to `v` is `g 0`, and the only intra-arm edges
  are consecutive. Proof reduces to `path_walk_construction` on the induced Dynkin path via
  `affine_properInduced_isDynkin`, with degree bounds (`≤ 2` inside the arm; `nb` a leaf since
  it loses its hub-edge).

## Current frontier
Engine done. The remaining work is the **tree-partition** graph theory: build `G.IsTree`,
enumerate `v`'s three neighbours, form the three hub-deleted components `S t`, prove they
partition `Fin n \ {v}` (acyclic path-uniqueness), and feed each to `affine_arm_walk`.

## Overall project progress
Phase 3 formalization. `affine_dynkin_classification` ⟹ direction is being assembled;
Ẽ₆/Ẽ₇/Ẽ₈ one-branch case rests on `affine_one_branch_arm_layout` (#6919) ← the reciprocal
core `affine_tree_one_arm_reciprocal` ← `affine_one_branch_three_arms` (#6918, this crux).
This turn provides the per-arm engine; the partition/assembly is now #6928.

## Next step
Do #6928: assemble `affine_one_branch_three_arms` from `affine_arm_walk` + the three-component
partition of the hub-deleted tree. Full route (scaffolding → neighbours → components →
acyclic partition → assemble) is in the #6928 body.

## Blockers
None. #6928 depends only on `affine_arm_walk`, which lands with this PR.
