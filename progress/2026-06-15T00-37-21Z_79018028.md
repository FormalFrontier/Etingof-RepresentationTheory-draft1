## Accomplished

Closed issue #4669 (Ch5 #4608 sub-A1): Part A steps 1–6 of the Frobenius route to
`dim V_λ = #SYT`. Added to `EtingofRepresentationTheory/Chapter5/CharValueHookFormula.lean`
(before `charValue_trivialCycleType_eq_frobeniusDetForm`):

- `coeff_sum_X_pow_eq_multinomial` — step 4: the coefficient of `x^γ` in `(∑ᵢ Xᵢ)ⁿ`
  is `Nat.multinomial univ γ` when `∑ γ = n`. Proved via `Finset.sum_pow_eq_sum_piAntidiag`
  (multinomial expansion over `piAntidiag`), `coeff_C_mul`, `prod_X_pow_eq_monomial'`, and
  `coeff_monomial` (single-term survival).
- `multinomial_mul_prod_factorial_eq` — step 5: per-permutation `ℕ` identity
  `(if e∘τ ≤ β then multinomial(β−e∘τ) else 0)·∏βⱼ! = n!·∏ᵢ (βᵢ).descFactorial(e(τ i))`.
  True branch uses `Nat.factorial_mul_descFactorial` + `Nat.multinomial_spec`; false branch
  uses `Nat.descFactorial_eq_zero_iff_lt` (support gap → both sides 0).
- `charValue_trivialCycleType_eq_descPochhammer_det` — the deliverable (steps 1–6):
  `charValue = (n!/∏βⱼ!) · det(of fun i j => (descPochhammer ℤ (N−1−j)).eval βᵢ)`.
  Expands the alternant determinant as a signed monomial sum (`Matrix.det_apply`,
  `prod_X_pow_eq_monomial'`), shifts each via `coeff_monomial_mul'`, converts the surviving
  coefficient to a multinomial, and reindexes the determinant (`Int.cast_det`,
  `descPochhammer_eval_eq_descFactorial`, `Equiv.prod_comp`). Assembly does the `σ↦σ.symm`
  reindex implicitly by expanding the alternant via `e∘σ.symm` and the det via the row form.

Verification: `lake build …CharValueHookFormula` succeeds; the only `sorry` in the file is the
pre-existing `charValue_trivialCycleType_eq_frobeniusDetForm` capstone (step 8, #4671). All
three new lemmas are sorry-free — `#print axioms` of the deliverable shows
`[propext, Classical.choice, Quot.sound]` (no `sorryAx`).

## Current frontier

Part A sub-A1 (#4669) and sub-A2 (`descPochhammer_alternant_det_eq_prod_sub`, #4670, already
present) are both done. The capstone `charValue_trivialCycleType_eq_frobeniusDetForm` (step 8,
#4671) now has both ingredients in the file and only needs the cast/`field_simp` assembly.

## Overall project progress

Stage 3 formalization ongoing. Frobenius dimension route (#4595) Part A is now fully reduced to
proven lemmas; the capstone #4671 (blocked on #4669 + #4670) is unblocked by this PR.

## Next step

Pick up #4671: combine `charValue_trivialCycleType_eq_descPochhammer_det` (this PR) with
`descPochhammer_alternant_det_eq_prod_sub` (uses `shiftedExps_strictAnti`) and cast ℤ→ℚ to
close the `frobeniusDetForm` sorry. The det of the two lemmas matches syntactically:
sub-A1 gives `(n!/∏β!)·(detℚ)`, sub-A2 gives `detℤ = (∏_{i<j}(βᵢ−βⱼ):ℤ)`.

## Blockers

None.
