# Progress handoff — 2026-08-01 16:34 UTC

Branch: `codex/finish-stage3-4-3-5` (base `aac8dd1a`).

Stage 3.4 and Stage 3.5 completion work is implemented and locally validated,
but not yet committed or merged. The final per-file build evidence is
`/tmp/stage35-final4.log`; its compressed checked copy is
`progress/reviews/2026-08-01-stage3-5-build.log.gz`. The root aggregate also
builds successfully (9,419 jobs).

## Current results

- Stage 3.4 inspected 32,904 kernel declarations in 839 imported project
  modules, including a conservative inventory of 11,427 source-facing
  theorem/opaque declarations. It includes private source declarations and
  public names containing `.eq_`, excludes only anchored compiler-helper name
  forms, and may retain other generated declarations rather than risk omission.
- Re-export hubs are expanded through deterministic ownership. The 208 imported
  modules without an unambiguous item owner remain source-hash bound in the
  declaration inventory and are explicitly omitted from the item projection.
- Against the immutable 521-edge import baseline, 133 old edges were not
  recovered through the owned-module kernel mapping, while 203 mapped
  associations were discovered beyond it. The mapped relation has 591 edges;
  eight explicitly listed cycle edges are excluded to produce the validated
  583-edge DAG in `dependencies/internal.json`.
- Stage 3.5 covers 403 provider-backed partition items and ten derived overlays.
  All 403 required `proof-polish` GitHub issues exist and their numbers are in
  `progress/items.json`.
- The final linter evidence has 1,145 unique diagnostics in 199 files and zero
  blocking or unknown classes. The metadata calls this a source-bound linter
  screening, not blanket Mathlib-quality verification.
- Raw proof TSV and full build log are committed as deterministic gzip archives
  and hash-verified by `validate_postformalization_reviews.py`.

## Verified commands

- `lake build EtingofRepresentationTheory` — success (9,419 jobs)
- full per-file sweep — success
- placeholder, item, dependency, blob, exercise, external-dependency, Mathlib
  coverage, linter, Stage 3.5 unit, and strict post-formalization validators —
  success
- `python3 scripts/create_proof_polish_issues.py` — 403 existing, 0 missing

## Immediate remaining workflow

1. Wait for the rerun of PR #8521 after fixing the clean-runner-only copyright
   header warning in `Infrastructure/HasFiniteLengthSubobject.lean`; merge only
   when green.
2. Confirm the post-merge `main` run.
3. Close all 403 linked `proof-polish` issues with the merged PR as evidence.
4. Report only genuine pre-release remainder.

Preserve `.pod/` and `sessions/`; they are unrelated user data.
