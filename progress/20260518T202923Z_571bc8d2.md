# Progress — feature session 571bc8d2 (#2909 partial)

## Accomplished

Claimed and executed a coherent partial slice of #2909
(`single_branch_leaf_both_extend_b2leaf_per_kQ` — T(1, 2, r) sub-C of
the "both arms extend" branch of `single_branch_leaf_case`,
b₂ leaf, b₃ extends).  In
`EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`:

- **New helper**
  `single_branch_leaf_both_extend_b2leaf_per_kQ` (inserted after the
  sub-B sibling `..._b3leaf_per_kQ` at line 881).  Mirrors
  `InfiniteTypeConstructions.lean:7640-7963` and is the symmetric image
  of the just-landed sub-B helper (#2908) under swapping the
  `a₂`/`a₃` arms.
- **c₃-leaf case** (arm3 length = 3) — fully proved: T(1, 3, 2) = E₇
  posdef contradiction via `e7_tree_posdef`.  The 7 named vertices
  `{v₀, leaf, a₃, b₃, c₃, a₂, b₂}` form an E₇ tree (with the longer
  arm being `a₃-b₃-c₃`).
- **d₃-leaf case** (arm3 length = 4) — fully proved: T(1, 4, 2) = E₈
  posdef contradiction via `e8_posdef`.  Constructs
  `E8Distinct v₀ leaf a₃ b₃ c₃ d₃ a₂ b₂` from the per-vertex "only"
  closures and the global `connected_closed_set_is_all` coverage.
- **d₃-extends case** (arm3 length ≥ 5) — extracts `e₃` and leaves
  `sorry` for the T(1, 2, 5) embed.  Mirrors the precedent of #2908
  (sub-B left the analogous d₂-extends sorry), tracked by a fresh
  follow-up sub-issue #2915 (symmetric image of #2913 under the
  arm swap).

`e7_tree_posdef`, `e8_posdef`, `E8Distinct`, `acyclic_path_nonadj`,
`connected_closed_set_is_all`, `deg3_three_neighbors`,
`deg2_two_neighbors` are already public in
`InfiniteTypeConstructions.lean` — no de-privatising was needed for
this slice.  `embed_t125_in_tree` remains private; the sorry'd
d₃-extends embed will not reuse it (it returns the universal
`¬ IsFiniteTypeQuiver` form, strictly weaker than per-(F, Q)).

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericTpqr`
passes.  The only new sorry warning is on this helper at line 881
(the d₃-extends embed), to be filled by #2915.

## Current frontier

PR for the partial #2909 deliverable ready to push.  Sub-C status:

- C.1 c₃-leaf (E₇ posdef): ✅ in this PR.
- C.2 d₃-leaf (E₈ posdef): ✅ in this PR.
- C.3 d₃-extends (T(1, 2, 5) embed): ⏳ tracked by #2915.

Chapter 6 per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard:

- D2.degree4 / D2.cycle / D2.adjacent / D2.single — on `main`
  (#2891 / #2897 / #2900 / #2903).
- D2.single follow-up `single_branch_leaf_case_per_kQ` —
  scaffold + leaf-leaf cases on `main` (#2906); `both_extend` body
  still a sorry tracked by #2905.
- D2.single sub-cases:
  - sub-A (Ẽ₇, q, r ≥ 3): #2907 has-pr.
  - sub-B (T(1, q, 2)): on `main` (#2908 partial #2914); d₂-extends
    embed under #2913.
  - sub-C (T(1, 2, r)): partial here (this PR); d₃-extends embed
    under #2915.
  - sub-D (T(1, 2, 2) = D₅): on `main` (#2910 #2912).
- **Pending**: wire `single_branch_leaf_case_both_extend_per_kQ`
  body once #2907 lands and #2913 / #2915 fill the two embed sorries;
  D2.nonadj, D2.outer + D3 (still under #2877).

## Overall project progress

Stage 3 (Lean formalisation).  Sorry count on `main` post-#2914: 19.
This PR is sorry-neutral relative to `main`: it introduces 1 new sorry
(`single_branch_leaf_both_extend_b2leaf_per_kQ` d₃-extends case) and
closes none.  Expected post-merge total: 20.  The new sorry is on the
critical path to closing #2905, and is bounded above by #2915
(mechanical T(1, 2, 5) embed port).

## Next step

- A pod planner should triage the `replan` label on #2909 (close it
  with a forward link to #2915, since the residual scope is fully
  covered there).
- The next worker can claim #2915 or its symmetric sibling #2913
  (mechanically near-identical T(1, 2, 5) embed ports).
- D2.single sub-A (#2907) is still open with a PR; pod's repair /
  planner cycles should usher it home so the wiring step can run.

## Blockers

None for this worker.  Standing constraints unchanged: framework wall
#2436 gates the inherited sorries in the per-(F, Q) indecomposability
proofs; the static pr-repair PRs remain in `/repair` territory.
