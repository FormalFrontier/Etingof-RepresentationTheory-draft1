# Stage 3.2 claim audit: Chapter 2, §2.9

Scope: the exact 19-item reading-order interval from
`Chapter2/Discussion_2.9_heading` through `Chapter2/Discussion_2.9_Lie_groups`.

## Result

- Audited all 19 source blobs claim by claim: 54 claims total.
- Verdicts: 36 formalized locally, 10 covered by an identified declaration elsewhere, and 8
  historical, motivational, or differential-geometric claims recorded as non-formalizable with
  an item-specific reason.
- Verified definition integrity, statement fidelity, and non-vacuity for every item.
- Recorded canonical `claim_coverage` Stage 3.2 metadata on all 19 items.

## Statement and data repairs

- Added a source-facing formalization of the section's opening bracket assumptions: additivity,
  scalar bilinearity, alternation, and skew-symmetry.
- Exposed equation (2.9.1) in the book's exact cyclic, left-nested Jacobi form as
  `Etingof.jacobi_identity`.
- Corrected `Etingof.ado` from the unnecessarily restricted characteristic-zero statement to the
  Ado–Iwasawa statement over the chapter's arbitrary field.
- Identified the coordinate Heisenberg algebra with the displayed strictly upper-triangular
  `3 × 3` matrix algebra by an explicit Lie equivalence, including the three displayed basis
  matrices.
- Added the exact free-algebra quotient presenting `U(ℋ)` by the three displayed relations; the
  completeness equivalence is now an elaborated theorem statement instead of an unrecorded gap.
- Added a source-facing file for Remark 2.9.10's equivalence between the chosen-basis and
  coordinate-free enveloping-algebra definitions.
- Imported the previously orphaned `aff(1)` discussion module and all new §2.9 bridge modules into
  the chapter aggregator.

## Stage 3.3 proof obligations

Exactly two genuine theorem-level gaps remain:

1. `Etingof.ado`: the Ado–Iwasawa embedding theorem.
2. `Etingof.Example2_9_13.heisenbergPresentationEquiv`: completeness of the three-relation
   presentation of the Heisenberg enveloping algebra.

Both use `proof_wanted`, so the Stage 3.2 source remains free of `sorry`, `admit`, and new axioms.

## Validation

- Direct elaboration of every changed/scoped module.
- `lake build EtingofRepresentationTheory.Chapter2`.
- Repository JSON and dependency validators.
- Scoped source scan for `sorry`, `admit`, `axiom`, and unexpected `proof_wanted` declarations.
- Representative `#print axioms` checks for the new bracket, matrix-equivalence, and
  basis-presentation declarations.
