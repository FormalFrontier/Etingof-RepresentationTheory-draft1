## Accomplished

Coverage-arm audit of `Chapter5/Exercise5.27.2` (#7320), second pass. Report-only: no
Lean source was modified.

The issue body was **stale**. It said `progress/items.json` had `Chapter5/Exercise5.27.2`
at `status: proved` with no `coverage` field and no `lean_decl`. In fact the first pass
(session `5b1300c8`, PR #7335) recorded the audit, the issue was then *reopened* on
2026-07-22T22:12 with a substantive objection, and the record was refreshed in #7384.
So the live scope was the reopening objection, not a from-scratch audit.

**Verdicts, re-verified against `main` @ `7c90a4e4`, unchanged from 2026-07-22:**

| sub-part | verdict | `lean_decl` |
|---|---|---|
| (i) dihedral, 4.12.1(a) | `covered_full` | `dihedral_classification` |
| (ii) Heisenberg, 4.12.2 | `covered_partial` | `heisenberg_classification` |
| (iii) affine, 4.12.6 | `covered_partial` | `affine_classification`, `affine_classification_two` |

Parent roll-up: `covered_partial` (min across sub-parts).

What was actually checked this pass, rather than taken from the prior record:

* **The "via Theorem 5.27.1" requirement is real in all three arms.** Each
  `*_classification` opens by destructuring `Etingof.Theorem5_27_1` applied to its own
  semidirect-product model: Dihedral:207, Heisenberg:345, Affine:209 and 454. None is an
  independent re-proof that merely imports the file. (The issue body's "26/32/38 refs to
  `Theorem5_27_1`" counts were misleading — there is one application site per theorem,
  plus namespace-qualified uses of the structure it returns.)
* **`dihedralEquiv` is a genuine `≃*`**, not a stub: concrete `toFun`
  (`r i ↦ ⟨ofAdd i, 1⟩`, `sr i ↦ ⟨ofAdd (-i), ofAdd 1⟩`), concrete `invFun`, and
  `left_inv` / `right_inv` / `map_mul'` all proved.
* **All conclusions are classifications, not one-directional existence.** Each carries
  `∀ i, Simple (W i)`, pairwise non-isomorphy, completeness
  (`∀ S, Simple S → ∃ i, Nonempty (S ≅ W i)`), and dimension-filter counts. The counts
  check against `∑ dim² = |G|`: dihedral `2 + 4·(N-1)/2 = 2N` odd, `4 + 4·(N-2)/2 = 2N`
  even; Heisenberg `p²·1 + (p-1)·p² = p³`; affine `(q-1)·1 + (q-1)² = q(q-1)`.
* **The affine `2 < Fintype.card K` scope worry from deliverable 2 is resolved.**
  `affine_classification_two (hq : Fintype.card K = 2)` exists (line 443) and handles
  `𝔽₂`, where the two dimension filters would otherwise overlap because `q - 1 = 1`.
  The two theorems together span every finite field, so nothing the book intends is
  dropped. (This is why (iii) is *not* partial for scope reasons.)
* **Sorry-freeness by ground truth, not grep.** `lake build` of all three modules
  succeeds (8590 jobs), and `#print axioms` on `dihedral_classification`,
  `semidirect_classification`, `dihedralEquiv`, `heisenberg_classification`,
  `affine_classification`, `affine_classification_two` and `Etingof.Theorem5_27_1`
  reports only `propext, Classical.choice, Quot.sound` — no `sorryAx`.

**Why (ii) and (iii) stay partial.** Both classify a *newly defined* group type, not the
group the problem being "redone" actually uses:

* `Exercise5_27_2.HeisenbergGroup p = Multiplicative (ZMod p × ZMod p) ⋊[heisenbergφ p] Multiplicative (ZMod p)`
  vs `Problem4_12_2.Heisenberg p`, the `structure` with `a b c : ZMod p`.
* `Exercise5_27_2.AffineGroup K = Multiplicative K ⋊[affineφ K] Kˣ`
  vs `Problem4_12_6.Affine K`, the `structure` with `a : Kˣ`, `b : K`.

Neither exercise file imports its Chapter 4 counterpart; the identification is prose
only. The dihedral arm is full precisely because it does not have this problem — Mathlib's
`DihedralGroup N` *is* the type Chapter 4 uses, and the classification is transported onto
it through `Action.resEquiv (FGModuleCat ℂ) (dihedralEquiv N)`. The named representations
are also unreached: the book's `R_z` (`Problem4_12_2.rhoHom`) and the zero-sum `V`
(`Problem4_12_6.Vsub`, which the book's own hint singles out).

**Defect fixed in the durable record.** The prior pass set `followup_issue: 7320` on both
partial sub-parts and on the parent — self-referential, so #7320 was its own follow-up and
the required feature work had no home. Replaced with two real issues:

* #7788 — transport the Heisenberg classification onto `Problem4_12_2.Heisenberg`
* #7789 — transport the affine classification onto `Problem4_12_6.Affine`

Both carry the worked-out `MulEquiv`, verified against the two multiplication laws:

* Heisenberg `⟨a,b,c⟩ ↦ ⟨ofAdd (b,c), ofAdd a⟩`. `Heisenberg` multiplies as
  `⟨a₁+a₂, b₁+b₂, c₁+c₂+a₁·b₂⟩`; the semidirect product with
  `heisenbergShear a : (u,v) ↦ (u, v + a·u)` multiplies as
  `⟨(u₁+u₂, v₁+v₂+a₁·u₂), a₁+a₂⟩`. Same law under `u ↔ b`, `v ↔ c`.
* Affine `⟨a,b⟩ ↦ ⟨ofAdd b, a⟩`. `Affine` multiplies as `⟨a₁a₂, a₁·b₂+b₁⟩`; the
  semidirect product with `affineφ K a : b ↦ a·b` multiplies as `⟨b₁+a₁·b₂, a₁a₂⟩`.

Both issues point at the dihedral file's Transport section (`simple_of_functor_obj`,
`simple_equivalence_functor`, `dihedralRepEquiv`, `finrank_dihedralRepEquiv_functor`) as
the template, and ask that it be factored into a shared file rather than copied a third
time. #7789 also flags a `Type` vs `Type*` binder mismatch between the two files.

## Current frontier

`Chapter5/Exercise5.27.2` is coverage-audited at sub-part granularity with a
non-self-referential follow-up chain. The §4.12 ↔ §5.27 coverage thread has no
un-audited exercise left.

## Overall project progress

Phase 3 formalization. This turn touched no Lean source; it edited one entry of
`progress/items.json` (8 lines changed, diff localized as required) and opened two feature
issues. The three exercise modules build clean and are sorry-free by `#print axioms`.

## Next step

#7788 and #7789 are both small and independent — each is one `MulEquiv` with a supplied
formula plus a transport that already has a working template in the same directory.
Whichever lands first should factor the dihedral Transport section into
`Chapter5/Exercise5_27_2_Transport.lean` so the second does not copy it. When both land,
promote sub-parts (ii)/(iii) and the parent roll-up to `covered_full`.

Separately: five PRs (#7762, #7771, #7772, #7773, and others) sit `MERGEABLE` with
`CANCELLED` CI and no auto-merge. Auto-merge cannot be enabled on this repo at all —
`gh pr merge --auto` fails with `Protected branch rules not configured for this branch`,
so the `coordination create-pr` auto-merge step is silently a no-op and every PR needs an
explicit merge. That is a coordination-level problem worth a repair or planner pass.

## Blockers

None.

## Errors made this session

Recorded for the next agent, not buried.

**This session ran on a truncated copy of `agent-worker-flow`.** The worktree was
reused and arrived with 291 lines of uncommitted *deletions* across
`.claude/skills/agent-worker-flow/SKILL.md`, `.claude/commands/review.md` and
`.claude/commands/summarize.md`. The skill was invoked before `git status` was read, so
the whole session ran on the stale version and never saw Step 7's rules. The files were
restored afterward, but the skill was never re-invoked — the skill's own instruction is
to restore *and re-invoke*, then restart from Step 1.

Two things broke as a direct result:

1. **Two other sessions' PRs were merged immediately with CI still running.** Following
   `.claude/CLAUDE.md`'s merge-sweep instruction, `gh pr merge --auto --squash` was run
   on #7787, #7786, #7781, #7780, #7779. Because auto-merge is unavailable on this repo,
   `--auto` does not queue behind CI — it merges now. #7787 and #7786 merged at
   14:53:27/29; the other three errored out. #7786 is a Lean change (Chapter 2), and as
   of the end of this session `main`'s build for it is still `in_progress`, so it landed
   unverified. The full skill forbids exactly this and says so overrides CLAUDE.md, which
   is off-limits and still carries the unsafe command. **If `main` is broken, #7786 is
   the cause and this session merged it.**
2. **PR #7790 shipped with `create-pr`'s placeholder body.** Fixed afterward with
   `gh pr edit`, but only because reflection surfaced the rule.

Guidance fixes are in
https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/7791: the
staleness check now also lives in the five worker command files, since the copy inside
the skill only reaches an agent who already has the intact file, and Step 1 now says to
read an issue's comments rather than trusting its body.

The audit work itself was unaffected — it touched no Lean source and its conclusions were
re-derived from the source files and `#print axioms`, not from the stale skill.
