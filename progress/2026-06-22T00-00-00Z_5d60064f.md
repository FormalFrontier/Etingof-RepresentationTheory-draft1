## Accomplished

Worked #4926 (Ch5 #4849 biduality core). Reduced the entire biduality theorem
to a single, self-contained `k`-dimension equality. All module-theoretic
framing the predecessor flagged as the hardest part is now done sorry-free.

In `Chapter5/MultiplicitySpaceBiduality.lean`:

1. **`centralizerModuleHom_isScalarTower`** (instance, sorry-free) — the
   `IsScalarTower k ↥C (V →ₗ[A] E)` companion of
   `centralizerModuleHom_smulCommClass`. This is what lets
   `Theorem5_18_1.centralizerModuleHom` fire a *second* time, with `C` in the
   `A`-slot, so the double hom-space `(↥S →ₗ[A] E) →ₗ[C] E` synthesizes a
   `Module ↥(centralizer C)` structure. (Confirmed by a synthesis check during
   development.) Reusable for the symmetric re-application.

2. **`multiplicitySpace_biduality_Aiso`** — now **sorry-free**, depending only
   on the new finrank lemma. The proof:
   - builds the `k`-linear curried-evaluation maps `evS_lin/evT_lin :
     ↥S →ₗ[k] ((↥S →ₗ[A] E) →ₗ[C] E)`, `v ↦ (l ↦ l v)` (inner map `C`-linear,
     outer `k`-linear);
   - proves them injective directly (evaluate at `S.subtype` recovers `↑v` —
     no Schur needed);
   - upgrades to `k`-linear equivs `evS_k/evT_k` via
     `LinearEquiv.ofInjectiveOfFinrankEq` using the dimension equality;
   - threads the given `C`-iso `ψ : M_S ≃ₗ[C] M_T` through the existing
     `homCongrLeftOverSubring` (`k`-linear precomposition);
   - verifies the resulting `k`-linear bijection `↥S ≃ₗ[k] ↥T` is `A`-linear by
     a hands-on `map_smul'` (apply `evT_k`, `ext m`, use
     `m (Φ v) = (ψ.symm m) v`). **Key design choice: avoid putting an
     `A`-module on the double-hom space entirely** — work with `k`-linear
     equivs and prove `A`-linearity of the final composite directly. This
     sidesteps all `Module.compHom` / scalar-transport pain.

3. **`multiplicitySpace_double_hom_finrank`** — the lone remaining `sorry`, a
   pure `k`-dimension count: `finrank k ↥S = finrank k ((↥S →ₗ[A] E) →ₗ[C] E)`.
   Its statement uses only `k`-finrank (no `A`-module-on-double-hom needed), so
   it is clean to state and to attack independently.

`multiplicitySpace_Cdistinct` remains sorry-free (unchanged).
Build green; exactly one `sorry` in the file.

## Current frontier

`multiplicitySpace_double_hom_finrank` is the genuine multi-session research
kernel (the multiplicity matching). Intended route: apply
`Theorem5_18_1_bimodule_decomposition_explicit` with `C` in the `A`-slot
(needs `IsSemisimpleRing ↥C = Theorem5_18_1_commutant_semisimple`,
`FaithfulSMul ↥C E` (automatic for subalgebras of `End k E`), `IsAlgClosed k`)
to get `E ≅_k ⨁_p W_p ⊗ (W_p →ₗ[C] E)` with `W_p` simple/distinct over `C` and
`W_p →ₗ[C] E` simple over `centralizer C = A`. Then:
- `dim_k (M_S →ₗ[C] E) = Σ_p dim Hom_C(M_S, W_p) · dim(W_p →ₗ[C] E)`
  (Hom_C commutes with finite `⨁` and `⊗_k L_p`); by Schur over alg-closed `C`,
  `dim Hom_C(M_S, W_p) = δ_{[M_S],[W_p]}`, so `= dim(W_{p0} →ₗ[C] E)` for the
  unique `p0` with `M_S ≅_C W_{p0}`.
- Match this against the `A`-side decomposition
  (`Theorem5_18_1_bimodule_decomposition_explicit` with `A`) to get `= dim_k S`.
The finrank arithmetic + identifying `p0` + Schur-dimension lemmas are the work.

## Overall project progress

Chapter 5 Schur-Weyl GL-side distinctness (parent #4849). Part 1 landed;
Part 2 reduction (`multiplicitySpace_Cdistinct`) landed (#4926 prior PR);
this PR makes the biduality theorem itself sorry-free modulo one dimension
count, completing all the module-theoretic plumbing.

## Next step

Prove `multiplicitySpace_double_hom_finrank` following the route above. It is
now a standalone `k`-dimension equality with no module-transport overhead.

## Blockers

None. The remaining `sorry` is a clean, isolated finrank lemma (partial PR).
