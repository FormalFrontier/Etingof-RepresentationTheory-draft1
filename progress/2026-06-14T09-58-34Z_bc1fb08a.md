## Accomplished

Closed issue **#2977** (feat(Ch6 #2955 sub-3): D̃₈ per-(F, Q) helper for
the `chain.length = 5` residual).

- Added `EtingofRepresentationTheory/Chapter6/FieldGenericD8Tilde.lean`, a
  line-for-line analogue of `FieldGenericD7Tilde.lean` for the affine D̃₈
  diagram (9 vertices, two degree-3 branch points at distance 4 connected
  by an internal length-4 chain `2-3-4-5-6`). Provides:
  * `d8tildeAdj` (+ `_symm`, `_diag`, `_01`), `d8tildeQuiver`,
    `d8tildeOrientation_isOrientationOf`.
  * `d8tildeDim`, `d8tildeRepMap_kQ` (real body — star embeddings/
    projections on the four leaf edges, `d5tildeGamma_F` on the `2-3`
    edge, `id` on the three internal chain edges `3-4`, `4-5`, `5-6`),
    `d8tildeRep_kQ` (real `def`, no sorry in the body) and
    `d8tildeRep_kQ_dimVec`.
  * `d8tildeRep_kQ_isIndecomposable` — statement only, body deferred to a
    documented `sorry` exactly as the d5/d6/d7 precedents do (mirrors
    #2967/#2834).
  * `d8tilde_not_finite_type_per_kQ` (no direct sorry; transitively
    depends on the indec stub).
  * `embed_d8tilde_in_tree_per_kQ` — the per-(F, Q) subgraph-embedding
    helper deriving all 27 host non-edges (9 triangle + 6 distance-3 +
    4 distance-4 + 4 distance-5 + 4 distance-6) from the 8 edges +
    acyclicity, dispatching via `subgraph_infinite_type_transfer_per_kQ`.
    Added `path_nodup7` / `path_edges7` for the distance-6 (7-vertex)
    paths.
- Registered the new file in `EtingofRepresentationTheory/Chapter6.lean`.
- Wired the dispatch in `FieldGenericNonAdjacentBranches.lean`: added a
  `by_cases hE_c5 : chain.length = 5` branch (nested in the residual after
  the D̃₆ `hE_c3` branch) calling `embed_d8tilde_in_tree_per_kQ` (vertex
  map `0→leaf, 1→side_arm, 2→v₀, 3→chain[1], 4→chain[2], 5→chain[3], 6→w,
  7→arm₁, 8→arm₂`). Like the D̃₆ branch, D̃₈ embeds for *every*
  `chain.length = 5` configuration, so the single branch subsumes both
  all-leaves and mixed-arm chain-length-5 sub-cases.

Key decision: because the two branch points are now at distance 4 (vs.
distance 3 in D̃₇), the three interior path-distinctness facts
`p ≠ r`, `q ≠ s`, `r ≠ t` are **not** derivable from the branch non-edge
alone (a host graph with `p = r` satisfies every edge + `p–t` non-edge
hypothesis), so they are taken as extra inputs to the helper. The
dispatch supplies them from `chain.Nodup` via `hchain_nodup.get_inj_iff`.

Full `lake build` passes (8325 jobs). The only new `sorry` is the
documented indecomposability stub; the residual `sorry` in
`non_adjacent_branches_leaf_case_per_kQ` now covers only
`chain.length = 4` with `side_arm` degree ≠ 1 and `chain.length ≥ 6`.

## Current frontier

Per-(F, Q) Chapter 6 forbidden-subgraph helpers. With D̃₆ (#2974) and now
D̃₈ (#2977) landed, the non-adjacent-branches residual still needs the
general parametric D̃_n helper (#2978, `chain.length ≥ 6`). The
indecomposability stubs `d6/d7/d8tildeRep_kQ_isIndecomposable` (#2967 for
d7) remain open as the deeper proof work.

## Overall project progress

Stage 3 (formalization) in progress. Directive #4516 path (FieldGeneric
subgraph-embedding) continues to land. Open PRs needing repair handled by
repair agents.

## Next step

Pick up #2978 (general parametric D̃_n for `chain.length ≥ 6`), the last
forbidden-subgraph residual. #2976 (Ẽ₇ at chain.length=3 mixed arms) is
now subsumed by the D̃₆ branch and can likely be closed by a planner. Or
close an indecomposability stub (#2967).

## Blockers

None for this item.
