## Accomplished

Review session for #2815 (per-(F, Q) wave infrastructure: PRs #2799 + #2805).

Audited `compl_le_forces_eq`, `cycleRepGen_kQ`, `cycle_not_finite_type_per_kQ`,
`restrictOrientationViaEmb`, `restrictOrientationViaEmb_isOrientationOf`, and
`subgraph_infinite_type_transfer_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericInfiniteType.lean`.

Answered all five questions in the issue body:
- Q1 (`compl_le_forces_eq` API): only one call site exists (the cycle case); downstream Ẽ₆/Ẽ₇ consumers are framework-wall sorries. Reverse-direction `compl_le_forces_eq'` premature.
- Q2 (transfer API readiness for #2774 deliverable 2): cross-checked the five case-analysis branches of `not_posdef_infinite_type` (degree-4, list-cycle, adjacent branches, single branch, non-adjacent branches). All use single embeddings — no composition lemmas needed. Surfaced two dead parameters (`_adj`, `_adj_sub`) in `subgraph_infinite_type_transfer_per_kQ` signature.
- Q3 (three-step pattern reuse): identified that Step 1 (orientation-agnostic adjacent-vertex equality) is the only piece the Ẽ proofs will reuse, but abstraction is premature until at least one etilde indecomposability proof is desorried.
- Q4 (naming consistency): four suffix conventions in play (`_F`/`_gen`, `_kQ`, `_per_kQ`, no-suffix) — followed consistently but undocumented.
- Q5 (sorry classification): zero sorries in `FieldGenericInfiniteType.lean` on current `main`.

Filed two follow-up issues:
- **#2818** — chore(Ch6): remove dead `_adj`/`_adj_sub` parameters from `subgraph_infinite_type_transfer_per_kQ`
- **#2819** — doc(Ch6): document `_kQ` / `_per_kQ` / `_F` naming convention in module docstring

Posted review summary comment on #2815 and closed the issue.

## Current frontier

Review of the per-(F, Q) wave infrastructure half is complete. Sibling issue
#2816 (per-graph constructions: #2798 K_{1,4}, #2808/#2809 Ẽ₆, #2810 Ẽ₇)
is still unclaimed and would be a natural next pick for a follow-up review
session.

Open PRs in the per-(F, Q) wave: #2802 (K_{1,4} per-(F, Q), in pr-repair
queue), #2813 (D̃₅ per-(F, Q) Sub A). T(1,2,5) (#2793) is replan-labelled.

## Overall project progress

- Stage 3 (formalization) is the active stage. Chapter 6 forbidden-subgraph
  per-(field, orientation) refactor is mid-wave (wave-58).
- The wave landed six PRs between 2026-05-17 and 2026-05-18, refactoring the
  ℂ-only infinite-type constructions into per-(F, Q) form.
- Infrastructure half (`compl_le_forces_eq`, `restrictOrientationViaEmb`,
  `subgraph_infinite_type_transfer_per_kQ`, cycle case) reviewed and closed.
- Per-graph half (K_{1,4}, Ẽ₆, Ẽ₇) and D̃₅ / T(1,2,5) per-(F, Q) work
  still in flight.
- The Ẽ₆ and Ẽ₇ per-(F, Q) indecomposability proofs both carry a
  framework-wall sorry inherited from the wave-54 wall (documented in
  `progress/indecomposability-framework-investigation.md`).
- Zero sorries in `FieldGenericInfiniteType.lean`; framework wall sorries
  remain in `FieldGenericETilde6.lean` and `FieldGenericETilde7.lean`.

## Next step

The natural next review is the sibling issue #2816 (per-graph constructions).
After that, the remaining critical-path feature work is:
- #2818 (dead parameter cleanup, small)
- #2819 (naming convention doc, small)
- #2774 deliverable 2 (per-(k, Q) assembly of `not_posdef_infinite_type` — depends on all per-(F, Q) base cases landing, including the framework-wall sorries being addressed)

## Blockers

None for this review. The framework-wall sorries in Ẽ₆/Ẽ₇ indecomposability
are an out-of-scope concern for the infrastructure-half review (and are
tracked in `progress/indecomposability-framework-investigation.md`).
