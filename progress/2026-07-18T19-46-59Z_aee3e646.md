## Accomplished

Report-only review of issue **#6986** — audit of the two freshly-merged Ch8/Ch9 tail assemblies
(#6981 `Problem_8_2_8_ext`, #6979 unconditional Cartan = path-count) for vacuous defs, stale
docstrings, and `items.json` drift. Verified against `origin/main` HEAD `a6cbf556`; both target
modules build (`8638 jobs`). Verdict: **both assemblies sound.**

Key verification was `#print axioms` (authoritative `sorryAx`-leak detector), not just a
comment-stripped grep:
- **Ch9 — all clean:** `cartanMatrix_pathAlgebra_eq_pathCount'` (unconditional), the conditional
  version, both homological-dimension theorems, and the covering data `pathAlgebraHomEquiv` return
  `[propext, Classical.choice, Quot.sound]` only. The covering (`pathAlgebraProj = A·eᵢ`,
  `pathAlgebraHomEquiv = homCutEquiv ≫ cutPathEquiv`) is real data; entire `Chapter9/` is sorry-free.
  **9.4.6 is a complete deliverable (both parts).**
- **Ch8 — core/data clean, gap isolated:** `Problem_8_2_8_extₖ`, `barResolution`, `extAbelianIsoExtₖ`,
  `Problem_8_2_8_tor` all clean; the *only* `sorryAx` in the chain is in `Problem_8_2_8_ext` itself
  at the `hXM` object identification (tracked by #6980). No `def`/`instance` data sorry, no
  vacuous/circular construction. Statement matches book's Ext claim (stronger `≃ₗ[k]`); the extra
  `Mᵢ`/`Aᵢ` finite-dimensionality vs the book's "`Nᵢ` f.d." is a justified strengthening (docstring
  counterexample; already recorded in items.json #6814).

## Current frontier

Review complete and posted as a comment on #6986. Findings routed:
- **#6989** (opened this turn, `feature`): Ch9 stale statement-pass header docstring + redundant
  `hacyclic` hypothesis (unused; path-finiteness is separately assumed via a `Finite` instance, so
  the book's "acyclic ⟹ finite path count" content is not captured). Recommend deriving `Finite`
  from `hacyclic`+`Fintype Q` and dropping the instance arg.
- **Summarize/status cycle:** `items.json` `Chapter9/Problem9.4.6` is `statement_formalized` with a
  stale coverage_note (claims part (i) / `homologicalDimension_*_eq_one` still sorry — both false
  now). Should move to `sorry_free`. The prior sorry-landscape flagged it as a hold *because it was
  conditional*; that reason is now obsolete.
- **No action:** Ch8 `items.json` `statement_formalized` is a correct hold (live `hXM` sorry); Ch8
  book-hypothesis divergence is documented and justified.

## Overall project progress

Phase 3 formalization at its tail. This review confirms two large tail assemblies are sound: Ch9
Problem 9.4.6 is fully complete (both parts, sorry-free, unconditional), and Ch8 Problem 8.2.8-Ext
is sound wiring with a single well-isolated tracked gap (#6980). No new formalization debt beyond
the two cosmetic/modeling cleanups in #6989 and the items.json status refresh for summarize.

## Next step

A `feature` worker picks up **#6989** (Ch9 docstring + `hacyclic` cleanup). The next `summarize`
cycle moves `Chapter9/Problem9.4.6` to `sorry_free` in items.json. Ch8 stays blocked on **#6980**
(the `hXM` object identification) for full completion.

## Blockers

None. Report-only review; no code changes to the audited files. Findings are all captured on GitHub
(#6986 comment, #6989 issue).
