## Accomplished
Stated the kernel lemma (K′) over the now-existing `polyRightRep` / `A-det`
objects (#4826), with every object it quantifies over constructed for real (no
placeholder sorries). New file `Chapter5/KernelLemmaKPrime.lean`:

- `quotDetRep : Representation k GL_N (A/det)` — the **genuine** induced
  right-translation rep on `A/det = k[Xᵢⱼ]/(det)`, descended from `polyRightRep`
  via `Submodule.mapQ` using the GL-stability of `(det)`
  (`rTransAlgHom_mem_detIdeal`). With `detSubmodule`, `polyRightRep_mem_detSubmodule`,
  `quotDetRep_mk`.
- `charTwistRep c ρ` — character twist `g ↦ c(g) • ρ(g)` (general).
- `detChar = Matrix.GeneralLinearGroup.det` — the determinant character.
- `quotDetTwistRep r = charTwistRep (detChar ^ (-r)) quotDetRep` = `(A/det) ⊗ χ⁻ʳ`.
- `glWeightSpaceℤ ρ μ` — integer weight space (ℤ-graded analogue of
  `Etingof.glWeightSpace`, needed because the χ⁻ʳ twist makes weights negative).
- `kernelLemmaK'` (the single residual `sorry`): for `r ≥ 1`,
  `⨆ μ:Fin N→ℕ, glWeightSpaceℤ (quotDetTwistRep r) (μ:ℤ) = ⊥`.
- `kernelLemmaK'_submodule` (sorry-free, consumable): every nonneg-weight
  submodule of `(A/det) ⊗ χ⁻ʳ` is `⊥`. This is the form the #4694
  det-power-filtration assembly contradicts.

Wired into `Chapter5.lean`. Builds clean (`lake build …KernelLemmaKPrime`), one
intended `sorry`, no lint warnings.

## Key facts pinned down
`detChar(diagUnit i t) = t` (weight `(1,…,1)`, cf. `det_diagUnit` in
`Proposition5_22_2`), so a ν-weight vector of `A/det` twists to weight
`ν − (r,…,r)`. (K′) thus reduces to: every weight ν of `A/det` has `ν_N = 0`
(then twisted last coord `= −r < 0`, no nonneg weight, supremum `⊥`). That is the
Cauchy-decomposition core.

## Current frontier
`kernelLemmaK'` has one `sorry` — the `GL×GL`-equivariant Cauchy decomposition
(`A/det` keeps exactly the `ν_N = 0` constituents). Decomposed into **#4832**.

## Overall project progress
Ch5 det⁻¹-elimination kernel (#4694): the (K′) statement and all its objects now
exist over `polyRightRep`/`A-det`. The remaining gaps are #4832 (the Cauchy core,
itself likely splitting into equivariant Cauchy + GL highest-weight theory,
cf. #4699) and the #4694 assembly that consumes `kernelLemmaK'_submodule`.

## Next step
Pick up #4832 (the Cauchy-decomposition core). Expect it to need an *equivariant*
upgrade of `Theorem5_23_2_ii` (currently a rank iso only) and GL_N highest-weight
theory (#4699). Likely a further multi-issue decomposition.

## Blockers
None new. #4832 depends on research-level Cauchy/highest-weight infrastructure
(shared with #4699), not on a definition-level sorry.
