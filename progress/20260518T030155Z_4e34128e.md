## Accomplished

Issue #2860: hoisted three local `have` lemmas from the canonical-orientation
branch of `d5tildeRep_kQ_leaf_equalities` to top-level theorems under
Section 5e of `Chapter6/FieldGenericD5Tilde.lean`:

- `d5tilde_core_F` — v=2 decomposition under canonical 0→2 / 1→2 pushes.
- `d5tilde_core3_F` — v=3 decomposition under canonical 4→3 / 5→3 pushes.
- `d5tilde_gamma_containment_F` — γ-coupled leaf containments combining
  `d5tilde_core3_F` with `gamma_from_embed*_F`.

The three new theorems sit at lines 514 / 572 / 630 and are parameterised
over `(F, Q, hOrient, m)` plus arbitrary `(Wmain, Wother)` complementary
submodule pairs, matching the type the local `have`s used.

The canonical-orientation branch (formerly ~150 lines of inline `have`s)
now reduces to two `d5tilde_gamma_containment_F` invocations followed by
three `compl_le_forces_eq` applications and the final `exact`. The five
sorry positions for the non-canonical orientation sub-cases (lines
830/832/834/836/838) are unchanged. The indecomposability stub at line
877 is unchanged.

Build: `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde`
green. Sorry count: 6 raw `sorry`, 2 declarations using `sorry` —
matches starting state. Diff: +185/-157 (net +28 lines).

## Current frontier

#2860 complete. The hoisted helpers are now available to the 31
non-canonical orientation sub-cases handled by #2853 (the 8 sub-cases
where leaf-center edges are canonical can reuse them directly; the
other 23 will need projection-direction sibling lemmas that #2853 will
add alongside).

## Overall project progress

Wave-60 D̃₅ Sub B cascade cleanup: #2860 lands, #2859 still open. The
follow-on bulk work #2853 / #2851 remains unclaimed.

## Next step

A worker can claim #2859 (hoist `embed_sum_zero_F` / `center_decomp_F`
from `FieldGenericD5Tilde` to `FieldGenericStar` and dedup the local
`have`s in `starRepGen_isIndecomposable`) — also mechanical, single
session. After both #2859 and #2860 land, #2853 (31 non-canonical
orientation cases) becomes the natural next big work item.

## Blockers

None.
