## Accomplished

Worked issue #4642 (Ch6 #4569 sub-B2: `etilde7tildeRep_kQ_leaf_equalities`
orientation case-tree assembly). Landed the **clean standalone half** the issue
itself names — the obj-form canonical leaf→center collapse criteria — as a
partial:

- `etilde7Rep_kQ_prefixArm_collapse` (leaf-4 ↔ center block 0, canonical
  `4→3→2→0`)
- `etilde7Rep_kQ_suffixArm_collapse` (leaf-7 ↔ center block 3, canonical
  `7→6→5→0`)

Both are obj-form membership criteria over the rep's own `mapLinear` composites,
built from `leaf_center_mem_iff_of_forward`: containments from `W`-invariance,
injectivity descending through `etilde7{Prefix,Suffix}ArmComp_F_injective`. No
new `sorry`; `FieldGenericETilde7` builds (only the pre-existing sub-C `sorry`
at `etilde7Rep_kQ_isIndecomposable` remains).

**Obstacle #1 of the sub-B2 investigation is cracked.** `etilde7Dim` is a stuck
`match v.val`, so instance synthesis cannot find `AddCommGroup`/`Module` on the
leaf/center obj-types, and obj-typed `W`s cannot be passed into the concrete
foundation lemmas. The working recipe, now in the file:

1. State the criterion over the rep's obj-typed `mapLinear` composite (not the
   concrete `Comp_F` maps). Containments are then pure invariance chaining.
2. Pin the leaf/center algebra instances with `inferInstanceAs` using the
   **stuck** index form `Fin (etilde7Dim m ⟨v, by omega⟩) → F` (NOT the reduced
   `Fin (k*(m+1)) → F`) — this makes the supplied `AddCommGroup.toAddCommMonoid`
   syntactically match the rep's bundled `instAddCommMonoid ⟨v,_⟩`, closing the
   instance diamond. Using the reduced form reintroduces a mismatch.
3. Injectivity: `simp only [LinearMap.comp_apply, etilde7Rep_kQ, etilde7RepMap_kQ]`
   reduces the obj composite to the concrete `*ArmComp_F`; apply its injectivity
   (term-mode defeq at the leaves unfolds the match).
4. In the conclusion, write the membership submodule index as `W₁ _` (inferred
   from the element type), not `W₁ ⟨v, by omega⟩` — a second explicit `by omega`
   collides under unification and reports a spurious "No goals to be solved".

## Current frontier

The bridge is in; the **eigenvalue-coupled arm-1 core + final
`etilde7Rep_kQ_leaf_equalities` assembly** is not. Filed as a follow-up
sub-issue (see Next step) and #4642 partial-PR'd.

## Overall project progress

Ch6 affine-tube indecomposability frontier. The etilde7 (Ẽ₇ = T(1,3,3)) tube
has its sub-A construction + sub-B1 flag-collapse foundation on `main`; sub-B2
now has its orientation-independent collapse-criteria half landed. sub-C (#4570,
`etilde7Rep_kQ_isIndecomposable`) stays `sorry`/blocked.

## Next step

Claim the follow-up sub-issue (eigenvalue core). It consumes the two new
`*Arm_collapse` criteria + `etilde7_center_decomp_F` / `etilde7_center_sum_zero_F`
+ `etilde7Arm1Tube_F` + `eigenvalue_jordan_invariant_compl_trivial_gen`
(`FieldGenericStar.lean`) + `compl_le_forces_eq` to prove the canonical branch of
`etilde7Rep_kQ_leaf_equalities`, then the non-canonical orientation case tree
(reuse `reversed_leaf_subspace_eq` / `forward_leaf_subspace_eq`). Pin the
conclusion to the on-`main` `d7tildeRep_kQ_leaf_equalities` shape so sub-C #4570's
interface stays fixed. Model: `starTubeRepGen_isIndecomposable`
(`FieldGenericTube.lean`).

## Blockers

None. The follow-up is unblocked (foundation + bridge both on the branch / about
to merge).
