## Accomplished

Feature session on issue #6150 (Ch4, `Exercise4_2_3_SplitSimples.lean`): strengthened
`SplitData` and completed the modular simple-module enumeration. All three deliverables
landed, sorry-free.

1. **Deliverable 1 — strengthen `SplitData`.** Added the field
   `π_ker : RingHom.ker π.toRingHom = Ring.jacobson (MonoidAlgebra K G)`, pinning the kernel
   of the structure hom to the radical (without it the enumeration is false — a projection
   onto one Wedderburn factor is a valid surjective `π` with a larger kernel). Supplied in
   `SplitData.of` via `ker(iso ∘ quotient) = ker quotient = J`
   (`RingHom.ker_comp_of_injective`, `Ideal.mk_ker`).

2. **Deliverable 2 — `exists_Std_linearEquiv` (exhaustiveness).** Every simple `K[G]`-module
   `M ≃ₗ` some `Std i`. Route: `ker π = rad` annihilates `M`
   (`IsSemisimpleModule.jacobson_le_annihilator`); the block central idempotents
   `Pi.single i 1` have preimages `ε i` that act as orthogonal idempotent `K[G]`-linear
   endomorphisms summing to `id`, so on the simple `M` exactly one `ε i₀` acts as the
   identity. Then `ker(blockHom i₀)` annihilates both `M` and `Std i₀`, making them simple
   modules over the block quotient `Q = K[G] ⧸ ker(blockHom i₀) ≅ Matrix` — a simple
   artinian ring. A new general helper `linearEquiv_of_isSimpleRing` (two simple modules over
   a simple artinian ring are isomorphic, via `IsSimpleRing.isIsotypic`) gives `M ≃ₗ[Q] Std i₀`,
   demoted to `K[G]`-linear.

3. **Deliverable 3 — `card_simpleModuleClasses = D.n`.** Built `Fin D.n ≃ SimpleModuleClasses K[G]`
   sending `i ↦ ⟦Std i⟧`: injective from `Std_injective`, surjective from
   `exists_Std_linearEquiv`, using `simple_iff_isSimpleModule` and the fully-faithful
   full-subcategory inclusion.

`#print axioms` on `exists_splitSimples_count`, `exists_Std_linearEquiv`,
`card_simpleModuleClasses` all show only `[propext, Classical.choice, Quot.sound]`.

## Current frontier

`Exercise4_2_3_SplitSimples.lean` is fully sorry-free. `lake build` of the module succeeds
(8584 jobs).

## Overall project progress

Phase 3 formalization, Chapter 4. The modular enumeration foundation
(`Nat.card (SimpleModuleClasses K[G]) = n`) that issue #6138 depends on is now available and
sorry-free. `SplitData` is used only in this file, so no downstream files were affected.

## Next step

The assembly issue #6138 (`natCard_irrepClasses_le_conjClasses_of_isAlgClosed`, split-field
trace-form bound) can now consume this foundation. A worker should claim #6138 next. Siblings
#6146 / #6135 (base-change count route) remain independent.

## Blockers

None.
