## Accomplished

Formalized the coverage-gap claim from issue #5682 (Stage 3.7 COVERAGE arm,
`Chapter5/Discussion_footnote_5.15`). Created
`EtingofRepresentationTheory/Chapter5/Discussion_footnote_5_15.lean` and wired it
into `Chapter5.lean`.

The footnote asserts the lexicographic minimality of the staircase
`ρ = (N−1, …, 0)` under coordinate permutation, and the "subtract from `λ + ρ`"
corollary. Formalized statements (all proved, no `sorry`, axioms = propext /
Classical.choice / Quot.sound):

- `toLex_comp_perm_lt` — abstract core: permuting a strictly antitone `f : Fin N → ℤ`
  by `σ ≠ 1` strictly lowers it lexicographically (`toLex (f ∘ σ) < toLex f`), via the
  least non-fixed point argument.
- `toLex_comp_perm_le`, `toLex_comp_perm_eq_iff` — the `≤` and equality-iff-`σ=1` forms.
- `toLex_const_sub_lt`, `toLex_const_sub_le` — subtracting from a fixed vector reverses
  the lex order (the algebraic "subtract from `λ + ρ`" step).
- `rhoVec_comp_perm_le`, `rhoVec_comp_perm_eq_iff` — footnote first claim: `σ(ρ) ≤ ρ`
  with equality iff `σ = 1`.
- `laVec_le_shifted_sub_perm`, `laVec_eq_shifted_sub_perm_iff` — footnote second claim:
  `λ + ρ − σ(ρ) ≥ λ` with equality iff `σ = 1`.

Re-confirmed the gap before formalizing: the existing
`Etingof.rev_of_permExponent_eq_rhoShift` handles only the `σ = revPerm` case, and
`Proposition5_21_1.degLex_alternant_monomial_le` gives only the non-strict *degree*-lex
`≤` (no strict/equality-iff, no subtraction corollary). So this is genuinely new content.

## Current frontier

Issue #5682 complete. File builds cleanly (no warnings on the new file).

## Overall project progress

Stage 3.7 COVERAGE arm: one more prose-embedded formalizable claim closed. Reuses the
`Etingof.rhoShift` staircase and `Nat.Partition.toFinsupp` from `Theorem5_15_1`.

## Next step

Other Stage 3.7 COVERAGE issues remain in the queue; a follow-up could connect these
lex lemmas into the actual `Theorem5_15_1` / `Discussion_proof_of_Frobenius_character_formula`
leading-term argument if a future item calls for it.

## Blockers

None.
