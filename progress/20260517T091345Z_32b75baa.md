## Accomplished

Review session `32b75baa` — closed **#2705** (review audit of
PR #2697 + PR #2698). Both are on the Schur-Weyl L_i C-4 critical path.

**PR #2697 (β.2 Specht bridge, `Theorem5_22_1.lean:879-1075`)**: PASS.
Codomain restriction (`AlgHom.codRestrict` on `symGroupAlgHom`),
`Module.compHom` pullback, manual `IsScalarTower`, and the bridge
identity `tr_ℂ(σ on S.restrictScalars ℂ) = spechtModuleCharacter n la' σ`
all check out. The identity matches `spechtModuleCharacter`'s
definition in `Theorem5_15_1.lean:79` and feeds correctly into
`youngSym_trace_kronecker'` (`Theorem5_22_1.lean:2053`) which β.3
(#2684) will consume.

**PR #2698 (C-4a-ii body, `PrimitiveIdempotentSimplicity.lean:220-385`)**: PASS.
The inlined ~140-line proof body is structurally identical to the
WIP doc-comment block PR #2692 left behind, modulo the documented
`map_smul → DirectSum.of_smul` semantic patch and the lint cleanups
(four `show → change`, one `simp → simp only [map_zero]`). The
`DirectSum.of_smul` choice is correct: `DirectSum.of` is an
`AddMonoidHom`, not a `LinearMap`, so the generic `map_smul` cannot
fire; `Mathlib.Algebra.DirectSum.Module:93` is the specific bypass.
The anticipated `Module ↥B` diamond at `IsSimpleModule.congr Ψ.symm`
(line 385) does not materialize — the global
`centralizerModuleHom` (`Theorem5_18_1.lean:427`) is the unique path.

**Cross-PR**: conventions agree; no follow-up needed for C-4c (#2612).

**Builds**: `Theorem5_22_1`, `PrimitiveIdempotentSimplicity`, and
`Theorem5_18_1` all pass.

**Q1d heartbeat experiment**: PR #2697's bumps
(`synthInstance.maxHeartbeats 800000` section-level,
`maxHeartbeats 1600000 in` on the bridge theorem) can be quartered to
**200000 / 400000** without regression; eighth (100000 / 200000)
fails at `whnf` timeout on the bridge theorem. Bumps are ~4× too
generous. Not pushed in this audit; filed for janitorial follow-up.

Audit details: `progress/reviews/2026-05-17-schur-weyl-Li-C-4-push.md`.

## Current frontier

Both audited PRs PASS. The L_i C-4 chain remains:
- β.3 (#2684) ready to claim (unblocked by β.2-merged).
- γ.A (PR #2694) and γ.B (#2693) — γ.A flipped to CONFLICTING per
  the latest planner cycle and is in the repair queue.
- C-4a aggregation (sorry in `SchurModuleSubmodule_isSimple_centralizer`,
  `SchurModuleSimple.lean:143`) — waits on α/β/γ to all land.
- C-4c (#2612) → top-of-chain (#2493 → #2482 → #2483).

## Overall project progress

Stage 3 formalization. The two audited PRs were the last unaudited
pieces on the Schur-Weyl L_i C-4 push; remaining audits are for
PRs that haven't merged yet (γ.A under repair, γ.B unclaimed).

Sorry count unchanged.

## Next step

A planner should file a `feature` follow-up issue for the lint cleanup
in PR #2697's β.2-bridge section (4 items: section-level
`synthInstance.maxHeartbeats`, comment placement, three `show → change`,
and heartbeat tightening to ~200000 / 400000 with 2× safety buffer).
Body content is in `progress/reviews/2026-05-17-schur-weyl-Li-C-4-push.md`
under "Findings for follow-up".

For workers: continue with **#2684** (β.3 assembly) — now unblocked
and convention-confirmed by this audit.

## Blockers

None.
