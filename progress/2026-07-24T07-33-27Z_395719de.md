## Accomplished

Feature session on **#7659** — package the explicit `V ↦ Hom_A(V, E)`
simple-class bijection for Double Centralizer Theorem 5.18.1(iii).

New file `EtingofRepresentationTheory/Chapter5/Theorem5_18_1_Bijection.lean`
(sorry-free, only the standard `propext / Classical.choice / Quot.sound`
axioms), registered in the `Chapter5` aggregator:

- `homACongrLeftCentralizer` — **well-definedness**: an `A`-iso `M ≃ₗ[A] N`
  induces a `B`-iso `Hom_A(N,E) ≃ₗ[B] Hom_A(M,E)` by precomposition
  (`B = centralizer A`), the `B`-linear refinement of `homCongrLeftOverSubring`.
- `isSimpleModule_homA_centralizer'` — **simplicity** of `Hom_A(V,E)` for any
  abstract simple `A`-module `V` (transported from the submodule case).
- `homA_iso_of_homA_congr` — **injectivity** on iso-classes: `Hom_A(V,E) ≃ₗ[B]
  Hom_A(W,E) ⟹ V ≃ₗ[A] W`, via `exists_simpleSubmodule_iso_of_faithful` +
  `multiplicitySpace_biduality_Aiso`.
- `homRealizationComponent` (+ `_injective`) — a transport-free generic
  realization map `isotypicComponents D E → isotypicComponents (centralizer D) E`,
  injective by the biduality bridge run backwards.
- `homRealizationComponent_bijective` / `homRealizationComponentEquiv` — the
  **bijection** of the finite index sets `isotypicComponents A E ≃
  isotypicComponents B E`, forced by two injections (at `A` and at `B`) plus the
  double-centralizer cardinality balance (transported through `Nat.card` to dodge
  the `Fintype.card` motive problem).
- `exists_simple_A_submodule_homA_iso` — **surjectivity**: every simple
  `B`-module is `≃ₗ[B] Hom_A(V,E)` for a simple `A`-submodule `V ≤ E`.

`items.json` coverage metadata for `Chapter5/Theorem5.18.1` updated: #7659 closed,
only A⊗B-equivariance (#2489) remains partial.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5` completes (8831 jobs), so the
downstream Schur-Weyl consumers still compile against the new endpoints.

## Overall project progress

Phase 3 formalization; Chapter 5 Double Centralizer theorem now has the complete
simple-class classification: exhaustiveness (both families) + the explicit
`V ↦ Hom_A(V,E)` bijection. Remaining refinement: A⊗B-equivariance (#2489).

## Next step

Pick the next unclaimed feature issue. If revisiting 5.18.1, #2489 (A⊗B-equivariant
decomposition) is the last open thread; the bijection endpoints in
`Theorem5_18_1_Bijection.lean` are now available to consume.

## Blockers

None.
