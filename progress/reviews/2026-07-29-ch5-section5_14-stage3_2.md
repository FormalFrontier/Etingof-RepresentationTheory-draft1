# Stage 3.2 review — Chapter 5, §5.14

All six §5.14 records are complete. `Etingof.Introduction5_14.equiv` gives the explicit
equivariant equivalence between the induced Young-subgroup representation and the row-symmetrizer
left ideal, resolving the source-elaboration regression recorded in #7548. The provider now passes
a fresh source check; its tensor-representation abbreviation was simplified to avoid an unnecessary
definitional-equality blow-up, and compatibility settings are scoped to the individual proofs that
need them.

The vanishing and diagonal Hom-space statements are `Proposition5_14_1_vanishing_general` and
`Proposition5_14_1_diagonal`. The full Young-rule decomposition and its semistandard-tableau
multiplicity identification are public as `youngRuleKostkaDecomposition` and
`youngRuleMultiplicity_eq_kostkaNumber`, based on the definition `Etingof.KostkaNumber`.

`Etingof.Theorem5_14_3` proves the source's power-sum coefficient formula through fixed cosets and
monochromatic cycle colorings. The source proof's centralizer structure and cardinality are also
formalized by `centralizerMulEquiv`, `centralizerMulEquivFin`, and
`nat_card_centralizer_eq_prod`, with the cycle-count conventions reconciled to Mathlib by
`cycleCount_eq_cycleType_count` and `cycleCount_one`. Every core provider and every `YoungRule*`
provider passes a fresh source check without warnings.
