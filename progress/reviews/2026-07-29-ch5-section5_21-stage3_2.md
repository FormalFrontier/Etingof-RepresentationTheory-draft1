# Stage 3.2 review — Chapter 5, §5.21

Section 5.21 is complete. The implementation defines the alternant determinant, Vandermonde and
shifted exponent sequences, proves the required divisibility, and defines the Schur polynomial
with the exact identity `S_λ D₀ = D_λ`. Proposition 5.21.1 is the book-faithful Frobenius
character expansion indexed by all partitions with at most `N` parts. The stale tracker reference
to #7546 is removed because that issue is closed and the source now elaborates.

Proposition 5.21.2 supplies the geometric-progression formula over characteristic-zero fields,
the source's complex-variable and integer-exponent forms, and the all-ones Weyl dimension formula.
Fresh source checks pass for both proposition providers without warnings; the check also removed
current Mathlib deprecations and unused simplifier arguments.
