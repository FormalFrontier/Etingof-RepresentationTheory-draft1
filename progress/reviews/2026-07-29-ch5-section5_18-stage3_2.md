# Stage 3.2 review — Chapter 5, §5.18

Section 5.18 is complete. The Double Centralizer Theorem includes double centralization,
semisimplicity of the commutant, exhaustive classifications of the simple modules on both sides,
and the explicit bijection induced by `V ↦ Hom_A(V,E)`. Its decomposition is not merely an
underlying linear equivalence: `BimoduleDecompositionEquiv` records all-vector equivariance for A
and `centralizer(A)`, and `Theorem5_18_1_bimodule_decomposition_equivariant` packages that endpoint.
This closes the stale A⊗B-equivariance gap formerly tracked as #2489.

For tensor powers, `centralizer_eq_universalEnvelopingImage` proves Theorem 5.18.2. Both parts of
Lemma 5.18.3 are formalized: diagonal pure tensors span the symmetric power, and additive
diagonals generate the same tensor-power algebra as multiplicative diagonals for an arbitrary
algebra. Theorem 5.18.4 then supplies mutual centralizers, semisimplicity, full simplicity and
distinctness of both factor families, and a partition-indexed structured bimodule equivalence.

All nine principal §5.18 source providers pass fresh checks. The audit repaired current Lean
instance-selection regressions in the centralizer hom-spaces and tensor-power submodules, replaced
a fragile finite-dimensional inference with an explicit injective linear-map argument, and
removed current Mathlib deprecations and linter warnings without weakening the statements.
