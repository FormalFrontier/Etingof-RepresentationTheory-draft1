## Accomplished

Partial coverage of issue #2955 — Case E + uncovered all-leaves
configurations for `non_adjacent_branches_leaf_case_per_kQ`
(`Chapter6/FieldGenericNonAdjacentBranches.lean`).

Closed two sub-cases of the negative `by_cases hA … hD` residual:

* **E.aa** — `chain.length = 3 ∧ arm₁.deg = 2 ∧ arm₂.deg = 2`:
  Ẽ₆ at `w` via `chain[1] – chain[0]` (`c – v₀`) as the third
  length-2 leg. Reuses Case D's dispatch pattern (`chain.length - 3 = 0`
  / `chain.length - 2 = 1` arithmetic falls out of `omega` from
  `hchain_len`).
* **E.s1c4** — `chain.length = 4 ∧ side.deg = 1`: D̃₇ at `(v₀, w)`
  directly. The 8-vertex induced subgraph
  `{leaf, side_arm, v₀, chain[1], chain[2], w, arm₁, arm₂}` matches
  D̃₇ exactly because `h_no_adj_branch{,_w}` force
  `chain[1].deg = chain[2].deg = 2`. Works regardless of `arm₁.deg`
  / `arm₂.deg`.

Build green: `lake build
EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`.
File sorry count unchanged (1, at the top-level theorem) — but the
residual configuration space shrank substantially.

Decomposed the remaining residual into four follow-up sub-issues
(#2974, #2976, #2977, #2978) covering:

* `chain.length = 3` all-leaves → D̃₆ helper (#2974)
* `chain.length = 3` mixed arm degrees → Ẽ₇ extension splits (#2976)
* `chain.length = 5` residual → D̃₈ helper (#2977)
* `chain.length ≥ 6` all-leaves → general parametric D̃_n (#2978)

Breadcrumb left on #2955; partial PR landed on the parent.

## Current frontier

Two sub-cases of the `non_adjacent_branches_leaf_case_per_kQ`
residual closed. Four follow-up sub-issues blocked on #2955 merge,
will unblock automatically once the partial PR merges and triggers
`check-blocked`.

## Overall project progress

Chapter 6 field-generic infinite-type chain continues to progress
incrementally on the leaf-case `non_adjacent_branches_leaf_case_per_kQ`.
After this PR:

* Covered configurations (via existing per-(F, Q) helpers):
  * Case A: T(1, 2, 5) at v₀ — `chain.length ≥ 6 ∧ side.deg = 2`
  * Case B: T(1, 2, 5) at w — `chain.length ≥ 6 ∧ arm{1,2}.deg = 2`
  * Case C: Ẽ₇ at v₀ — `chain.length ∈ {4, 5} ∧ side.deg = 2`
    (split into C.main / C.short.3 / C.short.1 with all-leaves D̃₇ at
    chain.length = 4)
  * Case D: Ẽ₆ at w — `chain.length ∈ {4, 5} ∧
    arm{1, 2}.deg = 2`
  * **E.aa** (new): Ẽ₆ at w — `chain.length = 3 ∧
    arm{1, 2}.deg = 2`
  * **E.s1c4** (new): D̃₇ at (v₀, w) — `chain.length = 4 ∧
    side.deg = 1`
* Residual (sub-#2974, #2976, #2977, #2978): chain.length = 3 mixed
  arms, chain.length = 5 residual, chain.length ≥ 6 all-leaves.

Outstanding gates besides this assembly: indecomposability proofs
(d5tilde / d7tilde / etilde6 / etilde7 — all transitively gated on the
wave-54 framework wall).

## Next step

Next worker can pick from #2974 (D̃₆), #2976 (Ẽ₇ splits), #2977 (D̃₈),
or #2978 (parametric D̃_n). #2976 is the smallest, #2974 / #2977 are
template ports of D̃₇, #2978 is the largest (multi-session).

## Blockers

Sub-issues blocked on #2955 until the partial PR merges. (External
blocker on #2564 unchanged.)
