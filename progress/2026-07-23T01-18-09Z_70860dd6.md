## Accomplished

Worked issue #7510 (fix(Ch8 §8.2): restore the additive tensor functor and Tor definition).
The source `Chapter8/Definition8_2_3.lean` no longer elaborated: the `Additive` instance for
`tensorRightFunctor A N` failed in its `map_add` proof — an unsolved `zero` case plus
quotient-carrier type mismatches in the `tmul`/`add` cases, caused by `simp only [tensorRightFunctor, …]`
unfolding the functor and breaking defeq.

Changes (sorry/axiom-free; `lake build EtingofRepresentationTheory.Chapter8` green, 8643 jobs):

- `Chapter8/Definition8_2_3.lean`: rewrote the `map_add` proof of the `Additive` instance to work
  at the level of the underlying additive maps. After `ext x` and `QuotientAddGroup.mk_surjective`,
  a defeq `show` pins the goal to `tensorRightMap A N _` on the balanced quotient (relying on
  `AddCommGrpCat.hom_ofHom`/`hom_add` being `rfl`), then `TensorProduct` induction handles each case:
  `zero` by `simp`; `tmul` via `tensorRightMap_mk` + `ModuleCat.hom_add` + `LinearMap.add_apply` +
  `add_tmul`, closed by the existing `tensorOver_mk_add`; `add` via `tensorOver_mk_add` + `map_add`
  and the induction hypotheses, closed by `abel`.

- `Chapter8/Definition8_2_3_Test.lean` (new, wired into `Chapter8.lean`): downstream import/`#check`
  test pinning the public API — `balancedSubgroup`, `tensorOver`, `tensorRightMap`,
  `tensorRightFunctor`, its `Additive` instance, `TorFunctor`, `Tor`, `torIsoHomologyTensorRight` —
  with `#check` plus `example` signature locks, so a future source regression is caught by a
  downstream importer rather than hidden behind cached oleans (same pattern as `Theorem4_2_1_Test`).

Verified `#print axioms Etingof.tensorRightFunctor` = `[propext, Classical.choice, Quot.sound]`
(no `sorryAx`). Public signatures unchanged, so downstream importers are unaffected.

## Current frontier

#7510 fully complete. Public general-ring Tor API preserved and now regression-guarded.

## Overall project progress

Phase 3 (formalization) ongoing. This closes one of the large cluster of "restore
fresh-buildable" / "restore importability" regression issues (§8.2 Tor). Many similar issues
remain unclaimed (Ch3/Ch5/Ch6/Ch7/Ch9).

## Next step

Pick another "restore fresh-buildable" issue from the queue — these are build-regression fixes
and are high priority because they represent files that fail `lake env lean <file>` from a fresh
check even though the aggregate cached build passes. The `#check`/`example` test-file pattern used
here (and in #7519) is the recommended guard to add alongside each fix.

## Blockers

None.
