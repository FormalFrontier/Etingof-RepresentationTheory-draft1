# Progress — feature session 1053f0d0 (issue #2818)

## Accomplished

Implemented #2818: removed the two dead `Matrix` parameters from
`subgraph_infinite_type_transfer_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericInfiniteType.lean:942-944`.

The deleted parameters:

```lean
(_adj : Matrix (Fin n) (Fin n) ℤ) (_adj_sub : Matrix (Fin m) (Fin m) ℤ)
```

The underscore prefix told Lean to suppress the "unused variable"
warning; the variables were genuinely vacuous and never referenced
inside the proof body (lines 959-1143). They were carried over from
the planned #2774 signature but never wired in, since the matrix-level
bridge for #2774 deliverable 2 will go through the companion lemma
`restrictOrientationViaEmb_isOrientationOf` (which keeps its
`{adj}`/`{adj_sub}` arguments and `hembed` hypothesis), not through
this transfer theorem.

Audited callers: `grep -rn 'subgraph_infinite_type_transfer_per_kQ'`
shows no source-level callers — the deliverable-2 assembly in #2774
hasn't landed yet, so no call-site updates were needed. Match results
were limited to: the definition site itself, this progress file, plan
notes, and one prior progress file.

Verification: targeted build
`lake build EtingofRepresentationTheory.Chapter6.FieldGenericInfiniteType`
passes (8039 jobs, 0 errors). Project-wide build kicked off in
parallel and runs to completion before PR creation.

## Current frontier

PR ready to land. The two-line signature simplification leaves the
proof body untouched. No follow-up issues filed.

## Overall project progress

Stage 3 (Lean formalisation). Cleanup work on the per-(F, Q) Ch6 wave
infrastructure. No mathematical content changed; sorry count delta
this turn: **0**.

## Next step

After this PR merges, planners can pick up the related cleanup issues
surfaced by the wave-58 reviews: #2819 (docstring naming convention)
and #2825 (file split, gated on #2802/#2801 sequencing). The
deliverable-2 assembly in #2774 now sees a cleaner signature when it
lands.

## Blockers

None.
