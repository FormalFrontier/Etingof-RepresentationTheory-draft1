## Accomplished

Landed **deliverable 1** of issue #6898 (final assembly of `Problem_8_2_8_ext`, the `Ext` half of
Problem 8.2.8): the **finitely generated projective resolution** infrastructure.

`Etingof.barResolution k A W` (`Chapter8/BarResolution.lean`) is already a
`ProjectiveResolution (ModuleCat.of A W)` with `Module.Projective` terms. This turn added the
`Module.Finite` half, so its terms are now finitely generated projective when `A` and `W` are finite
dimensional over `k` — exactly the hypothesis `Problem_8_2_8_extₖ` requires of its resolving
complexes `P₁ P₂`:

- `instFiniteBarCoeff` — `Module.Finite k (barCoeff k A W n)` (`= A^{⊗n} ⊗_k W`), from
  `PiTensorProduct.finite` (tensor power finite over `k`) and `Module.Finite.tensorProduct`.
- `instFiniteBarModule` — `Module.Finite A (barModule k A W n)` (`= A ⊗_k barCoeff`), via
  `Module.Finite.base_change`.
- `instFiniteBarObj`, `instFiniteBarResolutionComplexX` — the same as objects of `ModuleCat A` and as
  the terms `(barResolution k A W).complex.X n`.

All four are one-line `inferInstanceAs` instances — the Mathlib chain resolves automatically. The
`import Mathlib` header means no new imports were needed. Also updated the deliverable-1 bullet in the
`Problem_8_2_8_ext` residual comment (`Chapter8/Problem8_2_8.lean`) to point the next agent at this
infrastructure and to flag the one wiring caveat: `barResolution` needs `IsScalarTower k Aᵢ Mᵢ`,
which is not yet in the `Ext` section variables.

`lake build EtingofRepresentationTheory.Chapter8.BarResolution` completes (8580 jobs), no warnings in
the touched declarations.

## Current frontier

`Problem_8_2_8_ext` is still `sorry` (line 215). Deliverable 1 of its 4-part residual is now in place.

## Overall project progress

Stage 3, Chapter 8 Problem 8.2.8 (`Ext` half). The `Extₖ`-level Künneth core (`Problem_8_2_8_extₖ`)
and the `Ext ≃ₗ[k] Extₖ` bridge (`extAbelianIsoExtₖ`, #6897/#6901, with a residual `map_smul'` sorry)
are landed. The final assembly `Problem_8_2_8_ext` needs four residual pieces; **#1 is now done**.
Remaining:

- **Deliverable 2 — module-structure reconciliation.** Identify `ModuleCat.of (A₁⊗A₂)(M₁⊗M₂)` and
  `(N₁⊗N₂)` (with `instM`/`instN` pinned componentwise by `hM`/`hN`) with the canonical external
  `extTensorFunctorLeftObj k A₁ A₂ (ModuleCat.of A₁ M₁)(ModuleCat.of A₂ M₂)` used by
  `Problem_8_2_8_extₖ`. Mirror how `Problem_8_2_8_tor` handles `instN`/`hN`.
- **Deliverable 3 — transport through `extAbelianIsoExtₖ`.** Apply the bridge on the LHS (a
  resolution of `ModuleCat.of (A₁⊗A₂)(M₁⊗M₂)` — use `barResolution`, needs `IsScalarTower k (A₁⊗A₂)`)
  and on each factor `Etingof.Ext (ModuleCat.of Aᵢ Mᵢ)(…)` (bridge `.symm`, using `barResolution Aᵢ Mᵢ`).
- **Deliverable 4 — `∐`/monoidal-`⊗` → `⨁`/`TensorProduct k`.** Convert the `ModuleCat k` coproduct of
  monoidal tensor products (RHS of `Problem_8_2_8_extₖ`) to the `DirectSum` of `TensorProduct k` in the
  stated signature.

## Next step

Pick up deliverable 2 (module reconciliation) — it is the next foundational piece and unblocks the
transport in deliverable 3. `IsScalarTower k Aᵢ Mᵢ` will need adding to the `Ext` section variables in
`Problem8_2_8.lean` before the wiring can use `barResolution`.

## Blockers

None. Issue #6898 is marked `replan` (partial completion) for a planner to re-scope deliverables 2–4.
