# 2026-07-25T15:18:48Z — work session (issue #7788)

## Accomplished

Closed #7788, the Heisenberg arm of Exercise 5.27.2 ("redo Problem 4.12.2 using Theorem
5.27.1"), all three deliverables including the optional one.

* **New shared file `Chapter5/Exercise5_27_2_Transport.lean`.** The transport machinery that
  was living as a `private` section inside `Exercise5_27_2_Dihedral.lean` now lives here, plus
  two new pieces:
  * `simple_of_functor_obj`, `simple_equivalence_functor` (moved, no longer private);
  * `repEquiv (e : G ≃* H) : FDRep ℂ H ≌ FDRep ℂ G` — restriction of scalars along a group
    isomorphism, `Action.resEquiv` specialized;
  * `transport_classification` — takes a simple / pairwise-non-iso / complete family of
    `FDRep ℂ H` and returns one for `G` with the *same index type and the same dimensions*;
  * `filter_finrank_congr` — the dimension filters agree, so every dimension-indexed count
    transports by `rw`.

  `dihedral_classification` now goes through `transport_classification` and lost ~30 lines of
  duplicated transport reasoning.

* **`heisenbergEquiv : Problem4_12_2.Heisenberg p ≃* HeisenbergGroup p`** — a genuine `≃*`,
  `⟨a,b,c⟩ ↦ ⟨ofAdd (b, c), ofAdd a⟩`. No shear correction is needed; `map_mul'` is one
  `SemidirectProduct.ext` plus a `ring`.

* **`heisenberg_classification'`** — the orbit-method classification stated over
  `FDRep ℂ (Problem4_12_2.Heisenberg p)`: exhaustive, dimensions `1` or `p`,
  `n = p² + (p-1)`, `p²` of dimension `1` and `p-1` of dimension `p`.

* **`heisenberg_classification_named`** (deliverable 3, marked optional in the issue) — the
  transported family *is* the book's family: every `1`-dimensional member is a `charRep χ`,
  every `p`-dimensional member is an `R_z = rhoHom z` with `z ≠ 1`, and conversely every
  character and every such `R_z` occurs in the family. Uses Problem 4.12.2's
  `simple_iso_charRep_or_rhoHom` for the forward direction and `irreducible_iff` /
  `charRep_simple` plus the family's completeness for the converse.

* `progress/items.json`: arm **(ii)** of `Chapter5/Exercise5.27.2` promoted to `covered_full`;
  the item-level note and follow-up pointer now name #7789 (the remaining affine arm).

Everything sorry-free; `#print axioms` on the three new endpoints lists only
`propext, Classical.choice, Quot.sound`.

## Current frontier

Arm (iii) of Exercise 5.27.2 — the affine group — is the only remaining `covered_partial`
piece of that exercise, tracked as #7789. The shared `Exercise5_27_2_Transport.lean` is now on
`main`-bound branch `agent/e0e8cf85`, so that issue's "factor the Transport section into a
shared file" instruction is already done: it only needs `affineEquiv` plus two applications of
`transport_classification`.

## Overall project progress

Stage 3 formalization continues; ~44 unclaimed feature issues in the queue. This session moved
one coverage-arm audit follow-up from `covered_partial` to `covered_full`.

## Next step

Claim #7789 and mirror this session's work for
`Problem4_12_6.Affine K` — `affineEquiv`, then `affine_classification'` and
`affine_classification_two'` via `transport_classification`, and optionally the identification
of the `(q-1)`-dimensional member with `Problem4_12_6.Vsub`. Watch the `Type*` vs `Type` binder
friction the issue flags: `repEquiv`/`transport_classification` are stated for
`{G H : Type}` (forced by `FDRep ℂ` living in one universe), so the affine file's
universe-polymorphic `K` has to be instantiated at `Type`, not worked around.

## Blockers

None for this item.

Note for planners: #7783 (the Gabber-Kac imaginary-bidegree bound for 𝔤₄) is still the only
thing on the critical path — #7759 and #7720 are blocked behind it — and it is still flagged as
needing a Gröbner-Shirshov-scale build-out. It was not attempted this session.
