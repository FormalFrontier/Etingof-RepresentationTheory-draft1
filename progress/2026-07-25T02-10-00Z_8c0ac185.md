## Accomplished

Worked issue #7719 (`feat(Ch2 #7394): upper bound — an explicit spanning family for 𝔤₄ closed
under ad x and ad y`) in `EtingofRepresentationTheory/Chapter2/Problem2_16_3.lean`.

1. **The reusable closure principle** the issue asked for:
   * `eq_top_of_closed_under_ad` — a submodule of `𝔤ₙ` containing `x̄` and `ȳ` and stable under
     `⁅x̄, ·⁆` and `⁅ȳ, ·⁆` is `⊤`. Proof: the elements whose adjoint action preserves `M` form a
     Lie subalgebra, so by `lieSpan_gens_eq_top` it is all of `𝔤ₙ`; then `M` itself is a Lie
     subalgebra containing the generators.
   * `span_eq_top_of_closed_under_ad` — the spanning-set corollary.

2. **`span_eq_top_one/two/three` routed through it.** Each previously built its own Lie subalgebra
   and computed the full pairwise bracket table (9, 16 and 36 cases); only the `ad(x̄)` and `ad(ȳ)`
   rows are needed. Net −126 lines, identical statements.

3. **The degree-`1` and degree-`2` layers of `𝔤₄`:**
   * `aElt k n i = ad(ȳ)ⁱ(x̄)`, `proj_iterate_ad_y`, and `aElt_five_eq_zero : aElt k 4 5 = 0`, so
     the degree-`1` layer is `a₀, …, a₄` — five elements.
   * `lie_yb_lie_aElt` — the derivation formula `ad(ȳ)⁅aᵢ,aⱼ⁆ = ⁅aᵢ₊₁,aⱼ⁆ + ⁅aᵢ,aⱼ₊₁⁆`. Iterating
     it from `⁅a₀,a₁⁆ = 0` produces the whole degree-`2` relation set: `lie_a0_a2`,
     `lie_a1_a2_add`, `lie_a1_a3_add`, `lie_a2_a3_add`, `lie_a2_a4_smul`, `lie_a3_a4_smul`.
   * Over a field with `2 ≠ 0` and `5 ≠ 0`: `lie_aElt_mem_layerTwo` — every one of the ten
     brackets `⁅aᵢ,aⱼ⁆` lies in the span of `⁅a₀,a₃⁆, ⁅a₀,a₄⁆, ⁅a₁,a₄⁆`, so the degree-`2` layer
     has dimension at most `3`.
   * `lie_a0_lie_a0_a3 : ⁅x̄, ⁅a₀,a₃⁆⁆ = 0`, derived by Jacobi inside `𝔤₄`.

   Degrees `0, 1, 2` therefore have the graded dimensions `1, 5, 3` of `loopPos`.

## Current frontier

Layers of `t`-degree `≥ 3`. Successor issue #7732 carries the design worked out this session:
each layer is a single `ad(ȳ)`-string, and the two alternating steps (3-dim → 5-dim via
`cₙ₊₁ = ⁅x̄, D cₙ⁆`, 5-dim → 3-dim via `cₙ₊₁ = ⁅x̄, D³cₙ⁆`) with the structure constants read off
the loop model, plus the warning that every vanishing statement must be re-derived inside `𝔤₄` by
Jacobi because the loop model is only a lower bound.

## Overall project progress

Stage 3 formalization. `Problem2_16_3.lean` builds clean, no `sorry`/`admit`/axiom. Parts (a)
(`finrank_g_one/two/three` = 3, 4, 6) are complete; part (b) has the characteristic-`3` infinite
dimensionality (`not_finite_g_four_of_three_eq_zero`) and, for #7394, the twisted loop
realisation `loopPos` plus the lower bound `matHom₄`. This session adds the first three layers of
the matching upper bound.

## Next step

Claim #7732 and do the general layer induction. Everything it needs is now in the file; the issue
body spells out the element choices and the structure constants so the next agent does not have to
re-derive them.

## Blockers

None. #7719 is landed partial: the reusable lemma and layers `0, 1, 2` are complete and verified,
the general layer is split out to #7732.
