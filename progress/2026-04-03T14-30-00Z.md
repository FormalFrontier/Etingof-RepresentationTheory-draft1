## Accomplished

Claimed issue #1782 (Stage 3.2: Prove Theorem5_27_1 remaining Mackey machine sorries). Reduced sorry count in `Theorem5_27_1.lean` from 5 to 2:

1. **Proved `hWχ_nz`**: Bridge from `weightSpace ≠ ⊥` to `¬ IsZero (weightSpaceRep ...)`. Used `IsZero.eq_of_src` to get `𝟙 = 0`, then extracted element-level equality via `f.hom.hom.hom` accessor chain to derive `Subsingleton`, contradicting the nonzero weight vector.

2. **Proved `exists_simple_subrep`**: Any nonzero `FDRep ℂ H` has a simple subrepresentation. Strong induction on finrank: if V is simple, done; otherwise extract a non-iso mono, split it via Maschke (Injective), get biproduct decomposition with strict finrank decrease, recurse. Added helper lemmas `finrank_iso'` and `finrank_biprod'`.

3. **Fixed `exists_nonzero_map_from_induced` signature**: Eliminated `by sorry` in the type by adding explicit `hχ` parameter.

Created PR #2034 with auto-merge enabled.

## Current frontier

Two sorries remain in `Theorem5_27_1.lean`:
- `exists_character_in_rep` (line 1185): Needs to extract character `χ : A →* ℂˣ` from a 1-dim simple `ℂ[A]`-submodule. Scaffolding in place (restriction to A, Maschke, simple submodule extraction). Blocked on type class resolution for `FiniteDimensional ℂ ↥m` when `m : Submodule (MonoidAlgebra ℂ A) ρ_A.asModule`.
- `exists_nonzero_map_from_induced` (line 1389): Frobenius reciprocity — construct iso `inducedRepV φ χ U ≅ W` from `U ⟶ weightSpaceRep`. Hardest remaining sorry.

## Overall project progress

- Theorem5_27_1.lean: 5 → 2 sorries (60% reduction this session)
- The completeness proof (`inducedRepV_completeness`) is structurally complete — all sorry-free except the two helper lemmas above
- The main theorem `Etingof.Theorem5_27_1` delegates to `inducedRepV_completeness`

## Next step

1. Resolve `exists_character_in_rep`: The key blocker is getting `FiniteDimensional ℂ ↥m` for a submodule `m` of `ρ_A.asModule`. Try `Submodule.restrictScalars` to view `m` as a ℂ-submodule of W, then use standard `FiniteDimensional` inheritance. Alternatively, work directly with `finrank_eq_one_iff_of_nonzero'` on the ambient space.
2. `exists_nonzero_map_from_induced` requires Frobenius reciprocity infrastructure that may need significant new development.

## Blockers

- Type class plumbing for scalar tower `ℂ → MonoidAlgebra ℂ A → ρ_A.asModule` through submodule coercion. The instance `FiniteDimensional ℂ ↥m` doesn't synthesize automatically.
