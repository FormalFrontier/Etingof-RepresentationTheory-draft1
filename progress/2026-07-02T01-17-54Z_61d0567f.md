## Accomplished

Session type: `once` one-shot on review/fidelity issue #5635 (Chapter4/Example4.8.1).

**Re-confirmed the finding (mandatory for this fidelity issue):** the public A₅ API
`Etingof.Example4_8_1_A5_irrep : Fin 3 → FDRep ℂ G = ![repTriv, repC4, repC5]`
(`Example4_8_1.lean:1998`) still covers only rows 0/3/4 (`ℂ`, `ℂ⁴`, `ℂ⁵`); the two
3-dim icosahedral reps `repC3plus`/`repC3minus` exist as eigenspace subreps but are
not in the API, not proved 3-dim, not character-correct, not proved simple. **Finding real.**

**Corrected the record (important).** The last reopen comment (2026-07-01T04:21:58Z)
asserts "all infra is now on main — do ONLY the narrow API extension." This is
**factually incorrect.** Verified against `main`/the file:
- **No operator identity** `zEnd² = 20·zEnd + 400·1`. Only trace-level facts exist:
  `zEnd_trace = 60`, `zEnd_sq_trace = 3600`, `lam2_hom_finrank = 2`.
- **`finrank(eigenspace zEnd muPlus) = 3` is NOT proved.** `repC3plusSub_finrank_eq`
  only *reduces* `dim ℂ³₊ = dim(eigenspace)`; its own doc comment (line 1743) says
  `= 3` is "the last combinatorial input still needed."
- **No character lemma** for `repC3plus`/`repC3minus` (grep: none).
- **No simplicity** for `repC3plus`/`repC3minus`.
- #5459's "current state" (projector `P⁺` available, idempotent, `tr P⁺ = 3`) is
  **also not in the code.**

So the "narrow API extension" cannot be executed as stated — extending `irrepA5` to
`Fin 5` with `_simple`/`_character` over the 3-dim rows *requires first* the Phase C
math (operator identity → eigenspace dims → characters → simplicity).

**Genuine Lean landed this session** (sorry-free, native_decide-free, file builds):
- `lam2_finrank : Module.finrank ℂ Λ²(ℂ⁴) = 6` — via `FDRep.char_one` (character at `1`).
- `zEnd_not_scalar : ∀ c, zEnd ≠ c • 1` — if `z = c·1` then `tr z = 6c = 60 ⇒ c = 10`,
  but `tr z² = 6c² = 600 ≠ 3600`. With `dim End_{A₅}(Λ²) = 2` this makes `{1, z}` a
  basis, so `z`'s minimal polynomial has degree **exactly 2** — the linchpin the
  `lam2_hom_finrank` doc comment names ("1, Z, Z² linearly dependent").

## Current frontier

`z` is now known non-scalar with `dim End_{A₅}(Λ²) = 2`, so `z² = a·z + b·1` for unique
`a, b` (existence still to formalize). Pinning `a = 20, b = 400` is the open crux.

## Overall project progress

Q₈ and S₄ rows of Example 4.8.1: complete (5/5 each, simple + character + pairwise).
A₅: 3/5 rows in the public API. The two 3-dim icosahedral rows remain the only gap in
the whole example, and are a genuine multi-session effort tracked by open **#5459**.

## Next step (precise remaining chain, for #5459)

1. **Degree-2 relation** `∃ a b, zEnd² = a•zEnd + b•1`: `1, zEnd, zEnd²` all lie in the
   commutant `End_{A₅}(Λ²)` (finrank 2 via `lam2_hom_finrank`), so 3 vectors in a
   2-dim space are dependent; with `zEnd_not_scalar` this normalizes to the relation.
   Bridge: `Representation.linHom.invariantsEquivFDRepHom lam2 lam2` identifies the
   commutant with `(lam2 ⟶ lam2)`; membership via `mem_invariants_iff_comm` +
   `zEnd_central`. **Deferred this session** — the `FGModuleCat`/`Action` coercion layer
   (`lam2.V` vs `↥lam2Sub.toSubmodule`) is its own focused sub-task.
2. **Pin `a = 20, b = 400`.** One trace moment gives `60a + 6b = 3600`; a *second*
   independent relation is required. Two routes:
   (a) a third moment `tr(z³) = 96000` (equivalently `tr(z²·ρ(r)) = 1600`), via an
   honest ~3600-term `decide` (12× the existing `zEnd_comp_char_val`; expensive but
   in-character) — collapse `tr(z³) = 60·tr(z²ρ(r))` by centrality first;
   (b) the isotypic route: `lam2_character_eq_sum` + `A5_orthonormal` give that Λ² is
   multiplicity-free with two constituents of character `chiA5 1`, `chiA5 2` (both = 3
   at identity), hence both 3-dim — needs Mathlib semisimple-decomposition API.
3. Operator identity ⇒ `(z − μ⁺)(z − μ⁻) = 0` ⇒ eigenspaces complementary, each
   `finrank = 3` (`repC3plusSub_finrank_eq` then closes).
4. Characters via projector `P₊ = (z − μ⁻)/(μ⁺ − μ⁻)`:
   `repC3plus.character g = (S(g) − μ⁻·χ_Λ²(g))/(μ⁺ − μ⁻)` — all numerators already
   proven (`zEnd_comp_char_val`, `lam2_character`, `muPlus_sub_muMinus`).
5. Simplicity via `FDRep.simple_iff_char_is_norm_one`; then `irrepA5 : Fin 5`,
   `rowA5 = id`, `_simple`/`_character`/`_pairwise` over `Fin 5`. (No external consumers
   of the `Fin 3` API — grep confirms nothing outside the file references it.)

## Blockers

Not closeable in one session — genuine multi-session math, tracked by open #5459.
Recommendation: either dedicate a focused multi-session push to #5459 (steps 1–5 above),
or accept documented partials without false-closing #5635. Do **not** re-issue "narrow API
extension" instructions premised on infra being landed — it is not.
