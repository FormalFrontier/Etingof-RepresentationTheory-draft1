## Accomplished
- Claimed and **completed** issue #6300 (Problem 5.16.1a, `res_spechtModule_character` —
  the symmetric-group restriction branching rule `Res V_μ = ⨁_{λ∈R(μ)} V_λ`).
- Filled the one remaining `sorry` in `Chapter5/Problem5_16_1.lean`, proving the genuine
  hard content (Frobenius character formula / Pieri rule for `p₁`). Both parts of Problem
  5.16.1 are now **axiom-clean** (`propext, Classical.choice, Quot.sound`).
- New supporting infrastructure proved along the way:
  - `fullCycleType_permEmb` / `psumPart_permEmb`: the embedding `Sₙ↪Sₙ₊₁` adds a fixed
    point, so `p_ν(permEmb σ) = (∑ⱼXⱼ)·p_ν(σ)` (via Mathlib `cycleType_extendDomain`).
  - `resBP` + `weightToPartition_resBP`, `sum_rowLen_fin`,
    `toYoungDiagram_rowLen_eq_zero_of_ge`, `youngDiagram_le_iff_rowLen`,
    `Partition_eq_of_rowLen_eq`, `list_ext_getD_of_pos`, `bp_parts_eq_rowLen`:
    row-length ↔ containment bridge for partitions.
  - `IsLegalRem` / `bpDecr`: the box-removal bounded partition.
  - `res_charValue_sum`: the core coefficient identity — extracting the `(μ+ρ)`-coefficient
    of `Δ·(∑ⱼXⱼ)·p_ν` sums `charValue`s over `removeSquare μ`. `p₁`-multiplication expands
    via `coeff_X_mul'`; illegal rows vanish by antisymmetry
    (`coeff_zero_of_antisym_repeated`); legal rows biject with `removeSquare μ`
    (`Finset.sum_bij`) using row-length containment.
- Added a 5-item Finsupp/MvPolynomial-coefficient gotcha entry to
  `.claude/skills/lean-formalization/SKILL.md` (tsub_apply vs sub_apply, `(1:ℕ)` annotation
  on `Finsupp.single`, function-valued `set` beta, `coe_equivFunOnFinite_symm` unfolding a
  `set` var, `subst` eliminating a parameter).

## Current frontier
Problem 5.16.1 fully proved (both parts, no sorries, axiom-clean). PR being opened.

## Overall project progress
Stage 3 (formalization) ongoing. Problem 5.16.1 complete.

## Next step
Pick the next unclaimed `feature` issue. Remaining known items at session start included
#6230 (Ch4 faces_decomposition), #6285 (Ch5 5.16.3a diagonalizable), #6297 (Ch8 relative
bar resolution).

## Blockers
None.
