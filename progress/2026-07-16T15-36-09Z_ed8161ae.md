## Accomplished
- Investigated #6818 (Ext Künneth assembly `Problem_8_2_8_ext`). Verified against `main` that
  its stated deps (#6815/#6816/#6817) are insufficient: step 3 needs a **cochain rearrange complex**
  `rearrangeHomComplex` (the Hom-cochain twin of `Etingof.rearrangeComplex`), which does not exist
  and is the deliverable of the still-open #6844 (needing the bidegree bifunctor natiso #6843).
  Added `depends-on: #6843,#6844` to #6818, commented, released the claim.
- Formalized #6843 (cohomological bidegree bifunctor natiso, lighter package) in new file
  `Chapter8/RearrangeHomBifunctorNatIso.lean`, sorry-free, axiom-clean:
  - `towerExtL` — the `IsScalarTower k (A₁⊗A₂) (X⊗Y)` `homTensorHomEquiv` requires.
  - `rearrangeHomComponentEquiv` / `rearrangeHomComponentIso` — the component
    `Hom_{A₁⊗A₂}(extTensorFunctorLeftObj X₁ X₂, N₁⊗N₂) ≅ Hom_{A₁}(X₁,N₁) ⊗ₖ Hom_{A₂}(X₂,N₂)`
    for f.g.-projective X₁, X₂ (inverse of `homTensorHomEquiv`).
  - `homTensorHom_comp_lcompₖ_left` / `homTensorHom_comp_lcompₖ_right` — the two contravariant
    naturality squares, as k-linear-map equalities out of `Hom(X₁,N₁)⊗ₖHom(X₂,N₂)`, with the big-Hom
    precomposition by `extTensorFunctorLeftMapHom` (the source cochain complex's degreewise map).
  - Added to the `Chapter8.lean` aggregator.

## Current frontier
#6843 complete. The critical path to `Problem_8_2_8_ext` (#6818) is now:
#6843 (this PR) → #6844 (assemble `rearrangeHomComplex` from these naturality squares) → #6818.

## Overall project progress
Stage 3 formalization, Chapter 8 Problem 8.2.8: `Tor` half done sorry-free; `Ext` half assembling.
The degreewise Hom-tensor iso (#6816), left-module resolution (#6815), cochain Künneth + `Extₖ`
identification (#6817) all landed; this PR adds the categorical bidegree layer (#6843).

## Next step
#6844: build the `CochainComplex (ModuleCat k) ℕ` iso
`Hom_{A₁⊗A₂}(P•₁⊗P•₂, N₁⊗N₂) ≅ Hom_{A₁}(P•₁,N₁) ⊗ₖ Hom_{A₂}(P•₂,N₂)` (total complex), consuming
`rearrangeHomComponentIso` (component) and the two `homTensorHom_comp_lcompₖ_*` squares
(differential commutation). Then #6818 assembles the four steps as the `Tor` half does.

## Blockers
None for #6843. #6818 remains blocked on #6844 (and #6843 until this merges).
