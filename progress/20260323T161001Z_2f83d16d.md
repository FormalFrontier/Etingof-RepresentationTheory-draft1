## Accomplished

- Claimed and worked on issue #1646: prove `principalSeries_simple_of_ne` (V(χ₁,χ₂) irreducible when χ₁ ≠ χ₂)
- Added bridge lemmas `simple_of_full_faithful_preservesMono'` and `simple_of_isSimpleModule_FDRep` to connect `IsSimpleModule` to `Simple FDRep` (Mathlib's versions are private)
- Fully proved `principalSeries_construct_delta_none`: the core construction that, given any nonzero f in a subrepresentation S, produces g ∈ S with g(rep(none)) ≠ 0 and g(rep(some t)) = 0 for all t
  - Uses Weyl element to handle f(rep(none)) = 0 case
  - Averages over translations to make "some" values constant
  - Applies diagonal scaling with character orthogonality (∑(χ₁/χ₂)(c) = 0 when χ₁ ≠ χ₂) to kill the "some" part
- Set up full proof structure for `principalSeries_simple_of_ne` via `IsSimpleOrder` on subrepresentations
- Fixed `action_diagonal_some` proof (field_simp for (1,1) entry)
- Created PR #1663 with auto-merge enabled

## Current frontier

- `Theorem5_25_2.lean` compiles with sorry's
- Two helper lemmas in the irreducibility proof path remain sorry'd:
  - `principalSeries_nontrivial` (line ~492): needs explicit construction of a nonzero covariant function
  - `principalSeries_delta_spans_top` (line ~617): needs to show delta-like functions (from construct_delta_none applied at each coset rep) span the full space V

## Overall project progress

- Chapter 5 principal series irreducibility (Theorem 5.25.2 part 1): core construction proved, 2 helper lemmas remain
- Chapter 6 Dynkin classification: sorry-free (modulo pre-existing timeouts)
- Other Chapter 5 items (parts 2-4 of Theorem 5.25.2, Theorem 5.26): have sorry'd stubs

## Next step

- Fill in `principalSeries_nontrivial` — construct a nonzero element of the principal series space (e.g., indicator function on coset B·I)
- Fill in `principalSeries_delta_spans_top` — show that applying the construction at each coset representative produces a spanning set
- These could be separate follow-up issues

## Blockers

None.
