## Accomplished

Worked issue #6378 (Ch9 Problem 9.5.3(ii) decomposition), partial completion via
decomposition. In `EtingofRepresentationTheory/Chapter9/Problem9_5_3.lean`:

- **Corrected the signature** of `Etingof.Problem953.exists_block_of_indecomposable` — the
  explicit point of the replan. Replaced the too-weak `hfl : ∃ composition factor` (which does
  not encode finite length; false as written, `M = ℤ` over `R = ℤ`) with the genuine
  `(hfl : IsFiniteLength R M)`. `IsFiniteLength` is an inductive `Prop`, so it is an explicit
  hypothesis, not an instance.
- **Proved the wrapper sorry-free modulo one helper.** `exists_block_of_indecomposable` now
  derives `Nontrivial ↥M` from `Indecomposable`'s `¬ IsZero M`
  (`ModuleCat.isZero_iff_subsingleton` + `not_subsingleton_iff_nontrivial`), gets a composition
  factor `S` via `exists_isCompositionFactor`, and closes `InBlock` by linking every factor to
  `S` through the new helper.
- **Isolated the deep content** as `compositionFactors_areLinked` (factors of an indecomposable
  finite-length module are pairwise linked), stated with `sorry` and a full Ext-splitting proof
  strategy in its docstring. Filed as sub-issue **#6398**.

`lake build EtingofRepresentationTheory.Chapter9.Problem9_5_3` succeeds. Remaining sorries:
`compositionFactors_areLinked` (#6398) and the preexisting out-of-scope part-(i)
`blocks_equiv_indecomposableCentralIdempotents`.

## Current frontier

The block-connectivity core `compositionFactors_areLinked` (#6398) is the last sorry gating
`exists_block_of_indecomposable`. It needs Ext¹-vanishing dévissage (Mathlib's covariant /
contravariant Ext long exact sequences exist), SES-splitting from `Ext¹ = 0`, and the
trace-submodule decomposition `M ≅ M_𝒮 ⊞ M_{𝒮'}`.

## Overall project progress

Phase 3 formalization, Chapter 9. Problem 9.5.3(ii) orthogonality half already on `main`; the
decomposition half now has the corrected statement + reduction landed, deep lemma isolated.

## Next step

Claim #6398 and prove `compositionFactors_areLinked` following the staged strategy in its body
(base case → dévissage → splitting → trace decomposition → indecomposability contradiction).
Consider splitting into an Ext-vanishing infra issue and a decomposition issue if oversized.

## Blockers

None. All infrastructure for #6398 is on `main` or in Mathlib.
