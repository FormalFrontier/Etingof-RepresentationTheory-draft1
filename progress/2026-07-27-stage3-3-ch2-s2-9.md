# Stage 3.3 proof verification: Chapter 2, §2.9

## Scope

This pass covers the exact 19-item interval from `Chapter2/Discussion_2.9_heading` through
`Chapter2/Discussion_2.9_Lie_groups`, using the Stage 3.2 claim inventory from the preceding
stacked branch.

## Result

Eighteen items are proof-complete or have no formal proof obligation. Example 2.9.13's former
gap is closed: the free algebra on `x`, `y`, `c` modulo exactly the three Heisenberg relations is
proved algebra-equivalent to `U(ℋ)`. The construction supplies maps in both directions from the
two universal properties, proves both composites are identities, and matches all three
generators.

Remark 2.9.3 remains partial. Its arbitrary-field Ado–Iwasawa statement is a genuine theorem-level
obligation recorded by `proof_wanted`; it has not been reclassified or certified as proved.
Mathlib contains neither Ado–Iwasawa nor the general PBW/injectivity infrastructure normally used
to construct a separating finite-dimensional quotient of `U(L)`. Work on that dependency remains
part of this Stage 3.3 substage.

All 19 tracker records now have `stage3_3` data. Eighteen say `complete`; the Ado item says
`partial` with `proof_integrity: proof_wanted` and names its remaining obligation.

## Build isolation and validation

The worktree has its own `.lake/build`. Only `.lake/packages` is a symlink to the root package
cache; no project build artifacts are shared with another worktree.

- built `EtingofRepresentationTheory.Chapter2.Example2_9_13` after the presentation proof;
- scoped source scan for `sorry`, `admit`, `axiom`, and `proof_wanted`;
- `#print axioms` checks for the new presentation equivalence and the other local §2.9 providers;
- full `lake build EtingofRepresentationTheory.Chapter2`;
- `jq empty progress/items.json` and `git diff --check`.

The only expected placeholder in scope is `Etingof.ado`.
