## Accomplished

Feature session for #6797 (Ch5 #Problem5.24.2): proved
`Etingof.weightedHomogeneous_mem_range_endTensorEval` — the surjectivity (B1) half of the FFT range
identification. Every `matrixWeight`-homogeneous polynomial of multidegree `d` lies in
`LinearMap.range (endTensorEval slot)` for any slot assignment compatible with `d`.

Sorry-free; `#print axioms` = `[propext, Classical.choice, Quot.sound]`.

Supporting lemmas added (all `private`, in `Problem5_24_2_Bridge.lean`):
- `exists_fun_sum_single` — per-fibre realizability: a `ℕ`-Finsupp `m` with `m.sum = S.card` is
  `∑_{j∈S} single (h j) 1` for some `h`. Proved by `Finset.induction`, peeling one support element
  of `m` per new point of `S` (via `add_tsub_cancel_of_le`). Avoids the toList/toMultiset route the
  issue suggested — cleaner.
- `curry_sum_eq_weight` — the per-letter mass `(u.curry i).sum id` equals `(weight matrixWeight u) i`.
  `Finsupp.induction_linear` (both sides additive in `u`).
- `exists_fg_realizes` — realizes an exponent vector `u` by `(f, g)` via a fibrewise distribution
  reassembled through `Finsupp.curry`/`curryAddEquiv`. Handles the empty-letter (`N = 0`) edge case.
- `prod_monomial_single` — `∏ⱼ monomial (single vⱼ 1) 1 = monomial (∑ⱼ single vⱼ 1) 1`.

## File-placement decision

The issue directed the lemma to `Problem5_24_2_Supporting.lean`, but that file is created by the
in-flight PR #6798 (still open, CI pending) and does not exist on `main`. Creating it here would
cause an add/add merge conflict. My lemma depends only on `endTensorEval`/`genericTensorMatrix`
(Bridge, on main) and `matrixWeight` (Core) — nothing from #6798 — so I placed it in
`Problem5_24_2_Bridge.lean`, which is conflict-free and already imported by the downstream assembly
#6789. The lemma name is unchanged, so #6789 consumes it as specified.

## Current frontier

`weightedHomogeneous_mem_range_endTensorEval` lands sorry-free in Bridge. The invariant version
(`weightedHomogeneous_invariant_mem_range_endTensorEval`, Bridge:130) remains `sorry` — that is
#6789's deliverable, which now has its surjectivity dependency (B1) available.

## Overall project progress

Phase 3 formalization ongoing. FFT (Problem 5.24.2) range identification: B1 (surjectivity, this
issue) done; B2 (`symGroupImage` = GL-invariants, #6798) in review; equivariance (#6787) claimed;
final assembly (#6789) blocked on #6787 + this.

## Next step

Land #6789 once #6787 and this merge: it uses `weightedHomogeneous_mem_range_endTensorEval` (from
Bridge) plus `mem_symGroupImage_iff_commute_diag` (from #6798's Supporting file) to build the
equivariant section and discharge the invariant `sorry` at Bridge:130.

## Blockers

None.
