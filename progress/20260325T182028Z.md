## Accomplished

Continued work on Proposition 6.6.6 source case (issue #1716):

1. **Cleaned up broken `suffices` approach** in source naturality proof — reverted to documented `sorry` after quiver instance pollution errors (`instDR` vs `inst` synthesis conflicts).

2. **Switched `equivAt_eq_source` from `linearEquivOfInjective` to `LinearEquiv.ofBijective`** — eliminates one source of the instance diamond (`linearEquivOfInjective` requires `[AddCommGroup V₂]` which creates conflicting `AddCommMonoid` paths; `ofBijective` takes instances from `f` directly).

3. **Verified build compiles cleanly** with exactly 2 sorrys (down from full sorry of entire source case when issue was created).

### Instance diamond diagnosis

The `set`/`generalize`/`have` approaches all fail on the `ofBijective` term due to:
- `Module k (DirectSum (ArrowsInto Q i) ...)` instance synthesis failure — Lean can't find the instance inside the `match inst_dec i i | .isTrue _` branch
- Quiver instance pollution: `let instDR := reversedAtVertex Q i` gets picked up by synthesis instead of `inst`, causing "synthesized type class instance is not definitionally equal" errors

## Current frontier

`Proposition6_6_6.lean`: 2 sorrys remain, both in `equivAt_eq_source` definition:

| Sorry | Location | Blocker |
|-------|----------|---------|
| hdim | line ~1277 | `finrank(V_i) = finrank(ker(sinkMap))` — requires establishing `sinkMap = mkQ ∘ T` through Decidable.casesOn wrappers |
| source naturality | line ~1451 | Instance diamond prevents naming `ofBijective` term via `set`/`generalize`/`have` |

## Overall project progress

- Proposition 6.6.6 sink case: **COMPLETE** (0 sorrys)
- Proposition 6.6.6 source case: 2 localized sorrys remain (was 1 large sorry covering entire proof)
  - Full proof structure built: `equivAt_eq_source` definition, `f_inj`, `h_ker`, `f_ds`, `f_component`
  - All 3 naturality arrow cases structured (b=i vacuous, a=i/b≠i sorry'd, a≠i/b≠i proved)
  - Switched to `ofBijective` to reduce instance diamond surface

## Next step

Two possible approaches to unblock:

1. **Refactor `equivAt_eq_source`**: Define `f_ds` as a top-level definition (not local `let`), making its components accessible without unfolding through `match inst_dec i i`. Then `hdim` and source naturality can be proved against the opaque `f_ds` API.

2. **Alternative construction**: Replace `ofBijective(codRestrict(f_ds))` with an explicit `LinearEquiv` built from `T⁻¹ ∘ ofInjective(sourceMap)` where `T` is the componentwise `equivAt_ne` isomorphism. This avoids `hdim` entirely but requires proving `ker(sinkMap) = T⁻¹(range(sourceMap))`.

## Blockers

- **Instance diamond** (fundamental): `Submodule.addCommMonoid` vs `AddCommGroup.toAddCommMonoid` for `↥(ker sinkMap)` prevents Lean tactics from naming or manipulating the `ofBijective`/`linearEquivOfInjective` term. All standard approaches (`set`, `generalize`, `have`, `suffices`) fail with synthesis errors.
- **Quiver instance pollution**: Multiple `let`-bound quiver instances (`inst`, `instR`, `instDR`) cause synthesis to pick wrong instances in type annotations.
