## Accomplished

Proved `Etingof.Problem2_7_4.isSimpleRing_charZero` sorry-free (issue #6187): in
characteristic `0` the Weyl algebra `WeylAlgebra k` is a simple ring.

Built the book's ad-lowering argument in `Chapter2/Problem2_7_4.lean`:

- `monBasis` : the monomial basis `{xⁱyʲ}` (from `Proposition_2_7_1`, valid over a
  char-0 field since a field is a domain), with `monBasis_apply`, `repr_monomial`.
- `adx`, `ady` : the inner derivations `[x,-]`, `[y,-]` as `k`-linear maps, with
  `adx_monomial_zero/succ`, `ady_monomial_zero/succ` computing their action on basis
  monomials (`ad_x(xⁱyʲ⁺¹) = −(j+1)·xⁱyʲ`, `ad_y(xⁱ⁺¹yʲ) = (i+1)·xⁱyʲ`).
- `repr_adx_apply`, `repr_ady_apply` : the coordinate formulas
  `repr(ad_x a)(i,j) = −(j+1)·repr a (i,j+1)` and `repr(ad_y a)(i,j) = (i+1)·repr a (i+1,j)`,
  proved by `Basis.ext` on the composed linear maps.
- `yDeg`, `xDeg` : degree functions via `Finset.sup` of the repr support; descent lemmas
  `adx_ne_zero`, `yDeg_adx_lt`, `ady_ne_zero`, `xDeg_ady_lt`, `yDeg_ady_eq_zero`
  (char-0 keeps leading coefficients nonzero).
- `one_mem_of_yDeg_zero`, `one_mem_of_mem` : strong induction lowering `y`-degree via
  `ad_x` to a polynomial in `x`, then `x`-degree via `ad_y` to a nonzero scalar (a unit),
  forcing `1 ∈ I`. `isSimpleRing_charZero` then applies `IsSimpleRing.of_eq_bot_or_eq_top`
  (`Nontrivial` derived from the faithful `polyRep`).

`lake build EtingofRepresentationTheory.Chapter2.Problem2_7_4` succeeds (1549 jobs).

## Current frontier

Chapter 2 Problem 2.7.4: part (a) simple-ring done. Remaining sorries in the file are
separate deliverables: `finrank_eq_zero_of_charZero` (trace half, PR #6190 in flight),
`center_charP` (part b, #6188/#6186), `finrank_irreducible_charP` (part c, #6189).

## Overall project progress

Phase 3 formalization ongoing. This turn closed one of the five Chapter-2 char-p/char-0
sorries in `Problem2_7_4.lean`.

## Next step

Land this PR (#6187). Sibling char-2 issues #6189 (finrank_irreducible_charP, needs
`IsAlgClosed`) and #6191 (q-Weyl 2.7.5) remain; note #6189 edits the same file so it
should rebase on this once merged.

## Blockers

None.
