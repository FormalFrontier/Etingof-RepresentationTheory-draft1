## Accomplished

Closed issue #6388 (Ch2 Problem 2.16.3(b), characteristic-3 case): proved
`Etingof.Problem2_16_3.not_finite_g_four_of_three_eq_zero (k) [Field k]
(h3 : (3 : k) = 0) : ¬ Module.Finite k (g k 4)`, **sorry-free**
(`#print axioms` shows only `propext, Classical.choice, Quot.sound`).

The `𝔰𝔩₃`-loop witness `matHom₄` collapses to 4 dimensions mod 3 (verified the
collapse is representation-independent: every root vector of `𝔰𝔩₃` has `ad`-weight
divisible by 3). Found a genuinely different `gl₄(k[t])` witness by computational
search over 𝔽₃:

- `x ↦ t·(E₁₃ + E₃₂)`, `y ↦ E₀₂ + 2·E₂₃ + E₃₁` (`matHom₄c`).
- Both relators map to 0 when `3 = 0` (relator2 `ad(y)⁵x` already vanishes over ℤ).
- `⁅y, x⁆` acts as `t·diag(0,-1,2,-1)`, multiplying each `E₀ⱼ` by `-2 = 1` mod 3.
- Tower `S₀ = ⁅x, ad(y)³x⁆ ↦ t²·E₀₂`, `Sₙ₊₁ = ⁅⁅y,x⁆, Sₙ⁆ ↦ tⁿ⁺²·E₀₂`.
- The `(0,2)`-entry functional sends `proj(Sₙ) ↦ tⁿ⁺²`; distinct monomials ⟹
  the family is linearly independent ⟹ `¬ Module.Finite`.

Key technique for the matrix identities: express each bracket as a ℤ-clean
closed form (collapse products via `single_mul_single`, then `Matrix.ext` +
`ring`), folding all char-3 content into a single `3 • (junk)` term killed by
`h3`. Deep brackets (relator2, the base) are staged one `ad`-step at a time to
avoid `whnf` timeouts.

## Current frontier

Issue #6388 complete; PR created. All new declarations in
`EtingofRepresentationTheory/Chapter2/Problem2_16_3.lean` build and are sorry-free.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 2.16.3 parts (a) `𝔤₁`, `𝔤₂` done;
`𝔤₃` (`finrank_g_three`) still `sorry`. Part (b): char ≠ 3 is #6387 (via
`matHom₄`), char = 3 now done (#6388). The combined top-level
`not_finiteDimensional_g_four` remains `sorry` — it needs BOTH the char = 3 lemma
(now available) and the char ≠ 3 lemma from #6387; a follow-up should case-split
on `(3 : k) = 0` and combine them.

## Next step

A small follow-up issue: once #6387 lands, close `not_finiteDimensional_g_four`
by `rcases eq_or_ne (3 : k) 0` and dispatch to `not_finite_g_four_of_three_eq_zero`
(char = 3) and the #6387 lemma (char ≠ 3).

## Blockers

None. (3 remaining weak style-linter warnings `<;>` vs `;` on e1/e2/e3 — cosmetic,
non-blocking.)
