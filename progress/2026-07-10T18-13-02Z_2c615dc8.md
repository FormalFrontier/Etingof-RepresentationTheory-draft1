## Accomplished

Feature session on **#6253** (Problem 5.12.5: `∑ dim(irrep Sₙ) = #{g : g²=1}`). Both
deliverables landed; `Problem5_12_5.lean` is now sorry-free.

- **Reverse Frobenius-Schur bridge** (`FrobeniusSchurRealType.lean`, new theorem
  `Etingof.frobeniusSchurIndicator_eq_one_of_isRealType`): a simple complex representation of
  *real type* has FS indicator `1`. This is the genuine open crux (the same content as the
  still-`sorry` `sum_char_sq_eq_card_of_isRealType`, #6242). Proof reuses the projector
  machinery of `exists_nonzero_invariant_symmetric_of_FS_eq_one` but keeps the trace identity
  general (`2·dim(sym∩Bilᴳ) = dim(Bilᴳ) + FS`): a real form gives `s ≥ 1`, its nondegeneracy
  makes `ρ` self-dual so Schur forces `d = 1`, hence `s = 1` and `FS = 2s − d = 1`.
- **Reality for `Sₙ`** (new file `RealTypeSymmetricGroup.lean`):
  `isRealType_of_isSimpleModule_symmetricGroup n` — the `n`-generic version of
  `Example5_1_3_S4` (classification → Specht module `ℂ[Sₙ]·c_λ` → transport coordinate form via
  the integer Young symmetrizer). Plus `frobeniusSchurIndicator_symmetricGroup_eq_one n`
  combining it with the bridge.
- **Assembly** (`Problem5_12_5.lean`): discharged the `sorry`. Added the FDRep↔representation FS
  bridge `fdRep_frobeniusSchurIndicator_eq`, applied `Corollary5_1_6` to the Wedderburn family
  `D.columnFDRep` with `h_all_real` from the reality crux, and aligned the arbitrary complete
  irredundant family `V : ι → FDRep` with `columnFDRep` via a dimension-preserving bijection
  (`columnFDRep_surjective`/`_injective`), then cast the `ℂ` identity back to `ℕ`.

`lake build EtingofRepresentationTheory.Chapter5.Problem5_12_5` succeeds.
`#print axioms Etingof.sum_finrank_irreducible_eq_card_sq_eq_one` → only `propext`,
`Classical.choice`, `Quot.sound` (no `sorryAx`): the whole chain is genuinely complete.

## Current frontier

#6253 complete. PR pending.

## Overall project progress

Stage 3 formalization ongoing. Problem 5.12.5 closed. Note: `sum_char_sq_eq_card_of_isRealType`
(Exercise5_3_3.lean, #6242) is still `sorry` — it is now trivially derivable from the new
`frobeniusSchurIndicator_eq_one_of_isRealType` bridge (a good follow-up cleanup, not touched
here to avoid scope creep / conflict with #6242's owner).

## Next step

Optional follow-up: fill the `sum_char_sq_eq_card_of_isRealType` sorry in Exercise5_3_3.lean
using `frobeniusSchurIndicator_eq_one_of_isRealType` (unfold FS = |G|⁻¹∑χ(g²) and clear the
`⅟`/`⁻¹`). Otherwise pick up the next unclaimed feature issue.

## Blockers

None.
