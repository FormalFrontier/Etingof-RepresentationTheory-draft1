## Accomplished

Issue #4973 (Ch5 #4946 sub-A, general-`k` S_n simple-module classification), **deliverable 1** —
general-`k` Specht-module simplicity. Landed in
`EtingofRepresentationTheory/Infrastructure/SpechtModuleSimple.lean`, sorry-free:

- `YoungSymmetrizerK_sandwich_general (k) [CommRing k]` — sandwich property `c*x*c = f x • c`
  over any commutative ring. The proportionality constants `β σ ∈ ℤ` come from the ℤ result
  `YoungSymmetrizerZ_sandwich_basis` and are merely cast into `k`, so **no injectivity / no
  characteristic hypothesis** is needed (the ℚ proof's base-change-injectivity was only used in
  the ℤ lemma, which is reused unchanged).
- `monoidAlgebra_trace_mulLeft_eq_general (k) [CommRing k]` — `tr(mulLeft c) = |G| · c(1)`,
  field-agnostic copy of the ℚ-only private lemma in `Theorem5_22_1.lean`.
- `YoungSymmetrizerK_sq_scalar_ne_zero_general (k) [Field k] [CharZero k]` — the scalar `α`
  in `c² = α·c` is nonzero (trace-nilpotency vs. `n! ≠ 0` in char zero).
- `neZero_card_perm_general` instance — `(|S_n| : k) ≠ 0`, which makes Mathlib's Maschke
  instance fire so `k[S_n]` is semisimple.
- `SpechtModuleK_isSimpleModule_general (k) [Field k] [CharZero k]` — the deliverable:
  `IsSimpleModule (k[S_n]) (SpechtModuleK k n la)`. Same atom argument as the ℚ proof.

`lake build EtingofRepresentationTheory.Infrastructure.SpechtModuleSimple` succeeds (only
pre-existing warnings in other files).

## Current frontier

Deliverable 1 of #4973 is done. The second deliverable (general-`k`
`Theorem5_12_2_classification`) was split into **#4979** — it requires generalizing the
ℂ-hardcoded Wedderburn block machinery in `Theorem5_12_2_Distinct.lean`
(`blockOf` / `same_block_iso` / `exists_young_symmetrizer_nontrivial`), a separate session.
The underlying `IrrepDecomp` structure is already generic over `[Field k] [IsAlgClosed k]`.

## Overall project progress

Stage 3 (formalization) of Chapter 5 ongoing. This unblocks the #4946 general-`k`
`schurModule_isSimple` chain at its root: Sub-B (#4974, Specht character bridge) can now use
`SpechtModuleK_isSimpleModule_general` as the general-`k` replacement for
`Theorem5_12_2_irreducible`.

## Next step

A planner triages #4973 (marked `replan` via the partial PR; breadcrumb points to #4979) —
either close it forwarding to #4979 or narrow it to the classification residual. A worker can
then pick up #4979 (classification) and #4974 (character bridge).

## Blockers

None.
