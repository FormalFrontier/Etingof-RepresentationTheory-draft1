## Accomplished

One-shot dispatch for issue #5898 (statement pass, Chapter 4 exercises). Formalized
faithful Lean statements (proofs `sorry`, as the phase requires) for the two exercises
in #5898's scope that had fallen through the cracks — claimed "landed" in the #5898
decomposition comment but never merged to `main` and not covered by the follow-up
#5904:

- **Chapter4/Exercise4_2_3.lean** — Exercise 4.2.3: if `|G| = 0` in `k`, the number of
  isomorphism classes of irreducible representations of `G` over `k` is strictly less
  than the number of conjugacy classes. Introduced `Etingof.IrrepClasses k G` (a genuine
  type: isomorphism classes in the full subcategory of `FDRep k G` on the simple objects,
  via `CategoryTheory.isIsomorphicSetoid`) and `Etingof.Exercise4_2_3` stated as
  `Nat.card (IrrepClasses k G) < Nat.card (ConjClasses G)` under `(Fintype.card G : k) = 0`.
- **Chapter4/Problem4_5_2.lean** — Problem 4.5.2: the primitive central idempotents
  `ψ_V = (dim V/|G|) • ∑_g χ_V(g) • g⁻¹ ∈ ℂ[G]`. Defined `Etingof.psi` (genuinely
  constructed) and four theorems: `psi_acts_self` / `psi_acts_other` (part (i), action as
  identity resp. null map via `Representation.asAlgebraHom W.ρ`), `psi_idempotent` and
  `psi_orthogonal` (part (ii), `ψ² = ψ`, `ψ_W ψ_V = 0` for non-isomorphic simples).

Both files build with only the expected `sorry` warnings. Added both to the `Chapter4.lean`
aggregator and set `items.json` status to `statement_formalized` for both (surgical edits,
dropped stale `coverage_note`s).

## Current frontier

Chapter 4 statement pass: with these two, the only remaining `not_formalized` Chapter 4
exercises are the 9 "deep" ones tracked in #5904 (Exercise4.3.1, Problem4.12.{1,2,5,6,7,8,9,11}).

## Overall project progress

Chapter 4 exercises now: 5 `statement_formalized` (4.1.4, 4.2.3, 4.5.2, 4.12.4, 4.12.10),
1 `sorry_free` (4.12.3), 9 `not_formalized` (all in #5904). Note: `Problem4_12_4.lean` /
`Problem4_12_10.lean` / `Problem4_1_4.lean` were already on `main`; the earlier
"5 landed" claim on #5898 had actually merged only 3, leaving 4.2.3 and 4.5.2 unmerged —
this session closes that gap.

## Next step

A planner should close #5898 (its scope is now fully covered: 5 statements landed + 9 in
#5904) and route the remaining 9 through #5904. Proof phase for these statements comes
later; Problem 4.5.2's proof overlaps with the open #5349 (`renormChar_isPrimitiveIdempotent`
in Remark 4.5.3).

## Blockers

None.
