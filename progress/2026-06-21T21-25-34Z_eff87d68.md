## Accomplished

Worked issue #4905 (feat(Ch5 #4896 assembly): discharge
`quotDetRep_irreducible_constituent_lastWeight_zero`). After mapping the
available infrastructure, found the residual assembly still needs two
**missing** infrastructure pieces, so decomposed it and landed the one
self-contained, fully-supported piece:

- New sorry-free file `Chapter5/CauchyCharDiff.lean` with the **combinatorial
  crux** of the part-(a) character computation:
  - `bddPartShift` / `bddPartUnshift`: the all-ones shift bijection
    `BoundedPartition N (d-N) ≃ {ν : BoundedPartition N d // 0 ∉ Set.range ν.parts}`.
  - `eval_one_prod_X`, `eval_one_schurPoly_bddPartShift`: the Schur-multiplicity
    `s_ν(1,…,1)` is invariant under the all-ones shift.
  - `cauchyMult_mul_prodX_eq_lastPart_pos`: multiplying the degree-`(d-N)` Cauchy
    character by `∏ᵢ Xᵢ` (the det-shift character) equals the part of the
    degree-`d` Cauchy character supported on partitions with all parts `≥ 1`.
    This is exactly the term subtracted from `char A_d` to leave the `ν_N = 0`
    constituents.
  - Wired into `EtingofRepresentationTheory/Chapter5.lean` (CI builds it).
  - `lake build …Chapter5.CauchyCharDiff` and `…Chapter5` both succeed, no `sorry`.

- Decomposed #4905 into:
  - #4960 part-B: FDRep packaging + formal character of `(A/det)_d` (consumes
    the new `cauchyMult_mul_prodX_eq_lastPart_pos`).
  - #4961 part-A: GL-grading of `A/det` + single-degree reduction for simple
    finite-dim subreps (depends-on #4960).
  - #4962 part-C: generic simple-constituent character-extraction lemma.
  - Breadcrumb left on #4905; partial PR marks it `replan`.

## Current frontier

`quotDetRep_irreducible_constituent_lastWeight_zero` (CauchyDetQuotient.lean:112)
still a single documented `sorry`. The combinatorial character difference is now
sorry-free and ready to consume; the FDRep-level pieces (#4960/#4961) and the
extraction lemma (#4962) remain.

## Overall project progress

Stages 1–2 complete; Stage 3 formalization ongoing. Chapter 5 kernel-lemma-K′
core (#4896) is being assembled top-down. This turn converted the stuck
research item #4905 into three scoped sub-issues and landed its combinatorial
crux.

## Next step

Pick up #4960 (part-B): define `quotDetDegreeFDRep k N d` and prove its formal
character via the per-degree SES (`detSubmodule_inf_homogeneous`),
`detShiftLinearEquiv_intertwine`, `polyRightDegreeFDRep_formalCharacter`, and the
newly-landed `cauchyMult_mul_prodX_eq_lastPart_pos`.

## Blockers

None. #4961 is `blocked` on #4960 by design (needs the FDRep packaging).
Note: GitHub secondary rate limits were hit at session start (shared token,
many agents) but cleared within ~2 minutes.
