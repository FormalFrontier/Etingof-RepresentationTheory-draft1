## Accomplished

Replan triage session (`95518fc7`). One candidate in `list-replan`: **#5010**
(`twistedPolytabloid_residual_invariant`, the James/Fulton column-straightening
nut — last `sorry` in `Chapter5/SpechtModuleBasis.lean`).

- **Triaged #5010 as "plan stale / approach changed."** The most recent claim
  (session `c9674d6f`) established, with independent second-opinion confirmation
  and consistent with 4+ prior sessions, that the issue's framing as a
  single-session R1/R2 sorry-fill is **infeasible**. The honest shape is a
  three-fact multi-session epic; only **fact 1** is currently actionable (facts
  2 and 3 depend on it).
- **Created corrected replacement #5037** (`feature`): "prove James leading-term
  fact — dominance-maximal support tabloid of `twistedPolytabloid` is
  column-standard (fact 1 of 3)." Body carries the validated mechanism
  (per-tableau Garnir-style sign-reversing involution, NOT the refuted global
  `H`), the sorry-free lemmas to build on (`twistedPolytabloid_support_bound`
  `:1939`, `twistedPolytabloid_fibre_sign_sum_zero`), the explicit refuted
  routes, and the downstream chain (facts 2/3) for a future planner to spawn
  once #5037 lands.
- **Closed #5010** ("not planned") with a forward link to #5037.
- Cleaned up an accidental issue **#5036** created by probing
  `coordination plan --help` (the shim treats `--help` as a title); closed
  immediately.

## Current frontier

`list-replan` is empty. #5037 is unclaimed/claimable (labels: `agent-plan`,
`feature`; no `replan`). The James nut is now correctly scoped as a multi-session
epic with fact 1 as the live blocker.

## Overall project progress

Stage 3 formalization, Chapter 5. `SpechtModuleBasis.lean` still has exactly one
real `sorry` (`:2345`), now tracked via the #5037 epic instead of the
mis-scoped #5010. Other open frontier sorries unchanged (A/det chain #4905,
`schurModule_isSimple_general` #4976, classification core #5023/#5024).

## Next step

A `/plan` session should treat #5037 as live actionable work. Do NOT create
facts 2 (`M`-subtraction stability bridge) and 3 (equal-tabloid row-inversion
descent) until #5037 lands — they are blocked on it and would be filler now.

## Blockers

None for this triage session (complete). Project-level: `twistedPolytabloid_residual_invariant`
remains blocked on the James leading-term structural lemma (#5037, fact 1).
