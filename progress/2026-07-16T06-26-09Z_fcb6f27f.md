## Accomplished

Closed the three elementary sorries in `EtingofRepresentationTheory/Chapter5/Problem5_24_1_b.lean`
(issue #6750):

- **`signTwist_smul_of`**: `rw [signTwist_of, smul_assoc]` — one line, pushing the ℂ-scalar out
  past the group-algebra action via the `IsScalarTower` hypothesis.
- **`signTwist_bijective`**: `φ` is an involution. Built `(signTwist n).comp (signTwist n) = id`
  by `MonoidAlgebra.algHom_ext` on generators; the key step is `sign(g)·sign(g) = 1`, proved via
  `Int.units_mul_self`. Note `algHom_ext` hands you the goal on `single g 1`, so a `show ... =
  MonoidAlgebra.of ...` (defeq) is needed before `signTwist_of` will fire. Concluded with
  `Function.Involutive.bijective`.
- **`conjugatePartition.parts_sum`**: `∑ (transpose λ).rowLens = (transpose λ).card = λ.card = n`.
  Mathlib lacks these, so added three `private` helpers:
  - `rowLens_sum_eq_card`: rows partition the cells (`Finset.card_eq_sum_card_fiberwise` on
    `Prod.fst`, fibers are `row i`, `rowLen_eq_card`).
  - `card_transpose`: transpose permutes cells (`Equiv.finsetCongr`).
  - `card_toYoungDiagram`: `ofRowLens` round-trip + `Multiset.sort_eq` + `parts_sum`.
  Plus `sum_list_range_map` bridging `(List.range n).map f).sum` and `Finset.range` sums.

`lake build EtingofRepresentationTheory.Chapter5.Problem5_24_1_b` succeeds.
`#print axioms` on `signTwist_bijective`, `signTwist_smul_of`, `conjugatePartition` shows only
`propext, Classical.choice, Quot.sound` — no `sorryAx`.

## Current frontier

Two out-of-scope sorries remain in the same file (intentionally untouched):
`signTwist_map_leftIdeal` (line ~161) and `spechtModule_signTwist_iso_conjugate` (line ~176, the
hard main theorem, depends on part (a) 5.24.1(a) which is not yet formalized).

## Overall project progress

Phase 3 formalization ongoing across chapters. This closes the elementary half of Problem 5.24.1(b).

## Next step

The main theorem `spechtModule_signTwist_iso_conjugate` needs Problem 5.24.1(a)
(`V'_λ ≅ V_λ`) formalized first, plus `signTwist_map_leftIdeal`.

## Blockers

None.
