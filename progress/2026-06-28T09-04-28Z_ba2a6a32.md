## Accomplished

One-shot dispatch on **#5561** (Ch5: assemble `formalCharacter` of the `det^s`-twisted
linear dual `= schurPoly (w0Shift)` from the landed coefficient formula). Landed the
**representation-theoretic reduction sorry-free** and isolated the residual to a single
pure-polynomial lemma.

**New in `Chapter5/LinearDualDetTwistCharacter.lean`:**

- `finrank_glWeightSpaceℤ_schurModule_natWeight` (**S1**, sorry-free): for antitone
  `lz` and a nonnegative weight `w'`,
  `dim (L_{lz})_{w'} = [x^{w'}] s_{lz}`. Transports
  `schurModule_weight_eq_schurPoly_coeff` from the `ℕ`-weight space `glWeightSpace`
  to the integer weight space `glWeightSpaceℤ` (via `glWeightSpace_eq_glWeightSpaceℤ`,
  unfolding `SchurModule`/`FDRep.of_ρ'`; closes by defeq).
- `finrank_glWeightSpaceℤ_schurModule_neg` (**S2**, sorry-free): the Schur module
  carries only nonnegative torus weights, so `dim (L_{lz})_w = 0` whenever some
  `w i < 0`. Uses a torus weight eigenbasis (`exists_weight_eigenbasis` +
  `glWeightSpace_schurModule_iSup_eq_top`) and `finrank_glWeightSpaceℤ_of_eigenbasis`:
  the matching-weight filter is empty because every eigenweight is `ℕ`-valued.
- `formalCharacter_detTwist_linearDual_eq_schurPoly` (**the deliverable**, sorry-free
  *modulo the bridge*): `apply MvPolynomial.ext`, rewrite the left coefficient with the
  landed `coeff_formalCharacter_detTwist_dual`, then S1 (case `∀ i, μ i ≤ m`) / S2
  (case some `μ i > m`) reduce it to the inverted-Schur coefficient bridge.
- `schurPoly_w0Shift_coeff` (**the residual, `sorry`**): the pure-polynomial
  inverted-Schur coefficient identity
  `[x^μ] s_ν = if (∀ i, μ i ≤ m) then [x^{m·1 − μ}] s_{lz} else 0`,
  `ν = w0ShiftWeight n lz m`. This is the coefficientwise avatar of
  `s_lz(x⁻¹)·(x₁⋯x_N)^m = s_ν(x)` (fact (b), `schurPoly_inverseShift`).

`lake build EtingofRepresentationTheory.Chapter5.LinearDualDetTwistCharacter` green
(8668 jobs); the only `sorry` in the file is the bridge.

## Current frontier

`formalCharacter_detTwist_linearDual_eq_schurPoly` is sorry-free except for the
pure-polynomial bridge `schurPoly_w0Shift_coeff` (extracted to sub-issue). The
rep-theoretic 80% of the issue's "Route" (S1/S2 + assembly) is landed and reusable.

## Overall project progress

Stage 3 ongoing. The linear-dual half of the contragredient identity (#5544/#5535):
the analytic infrastructure (`coeff_formalCharacter_detTwist_dual`, #5553) is landed;
the deliverable now reduces — sorry-free on the rep-theory side — to a single inverted-
Schur **polynomial** coefficient identity.

## Next step

Pick up the new sub-issue for `schurPoly_w0Shift_coeff`. Route (the genuine content):
prove `schurPoly n ν = revBox_m (schurPoly n lz)` via `mul_right_cancel₀` against the
Vandermonde, where `revBox_D` is the per-variable box reversal
`monomial c ↦ monomial (D·1 − c)`. Needs: (a) `revBox` linear map with
`revBox_{D+E}(P·Q) = revBox_D P · revBox_E Q` (bounded exponents); (b) `revBox` of an
alternant = alternant of complemented exponents and `revBox_{N-1}(Δ) = sgn(w₀)·Δ`;
(c) the **Schur per-coordinate weight bound** `[x^μ] s_{lz} ≠ 0 ⟹ μ i ≤ lz 0`
(dominance/highest-weight; the crux — not in the repo). Then combine with
`schurPoly_inverseShift` (`SchurPolyInverseShift.lean`).

## Blockers

`schurPoly_w0Shift_coeff` (the inverted-Schur coefficient identity) is a multi-lemma
pure-polynomial effort (box-reversal infrastructure + the Schur dominance bound), out
of scope for one session. Landed as a partial PR; #5561 routed to replan with a
focused sub-issue created.
