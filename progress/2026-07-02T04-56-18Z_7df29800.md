## Accomplished

Worked issue #5678 (Stage 3.7 COVERAGE arm, epic #5338): the `Ind_{S₃}^{S₄}`
decompositions of Etingof Discussion 5.11(3), absent from
`Discussion5_11_examples.lean`.

- **Re-confirmed** the coverage gap: `Discussion5_11_examples.lean` formalizes only
  the `S₃/Z₂` and `S₃/Z₃` examples (5.11(1), (2)); the `S₄/S₃` example (5.11(3)) is
  genuinely absent. The claim is formalizable but large (see below), so this session
  landed the key reusable prerequisite rather than the full example.
- **New file** `EtingofRepresentationTheory/Chapter5/Discussion5_11_S4S3.lean`
  (registered in `Chapter5.lean`), sorry-free:
  - Ten pairwise-non-isomorphism lemmas among the five `S₄` irreducibles
    (`trivRep`, `signRep`, `twoDimRep`, `stdRep`, `rotRep` from `Etingof.Example4_3_S4`).
    Eight pairs separated by dimension; `ℂ₊/ℂ₋` by the sign character on a transposition;
    `ℂ³₋/ℂ³₊` by the existing `stdRep_character_ne_rotRep_character`.
  - `S4_simple_iso`: **every simple `FDRep ℂ S₄` is isomorphic to one of the five
    irreducibles**, via the Artin–Wedderburn count `1²+1²+2²+3²+3² = 24 = |S₄|`
    (`irreps_dim_sum_of_squares`). This is the `S₄` analogue of the existing
    `Discussion5_11.S3_simple_iso`, and is the catalogue-completeness input every
    Frobenius-reciprocity decomposition proof depends on.

## Current frontier

`S4_simple_iso` compiles clean (`lake build …Discussion5_11_S4S3` succeeds, no warnings
on the new file). The three induction isomorphisms of 5.11(3) are not yet formalized.

## Overall project progress

Stage 3.7 COVERAGE arm ongoing. Discussion 5.11 now has: examples (1),(2) fully proved
(pre-existing) and the `S₄` catalogue-completeness prerequisite for example (3) proved
here. The three `Ind_{S₃}^{S₄}` isomorphisms remain (follow-up issue created).

## Next step

Follow-up work (new issue) to complete Discussion 5.11(3):
1. Realise `S₃` as the stabiliser subgroup `H` of a point (e.g. `3`) in `S₄`, with
   `↥H ≃* S₃`.
2. Transport the three `S₃` irreducibles (`ℂ₊`, `ℂ₋`, the 2-dim `ℂ²`) to reps of `↥H`.
3. Generalise `Discussion5_11.frobenius_finrank` (currently stated for `Subgroup S3`; the
   proof is group-generic) to `Subgroup S4` / arbitrary finite `G`.
4. Compute the fifteen restricted-character scalar products over `H` (order 6,
   non-cyclic; organise by the three `S₃` conjugacy classes: id / transposition /
   3-cycle) and assemble the three isomorphisms via `iso_of_forall_finrank_hom_eq` +
   `S4_simple_iso`. Expected results (verified numerically):
   `Ind ℂ₊ ≅ trivRep ⊞ stdRep`, `Ind ℂ₋ ≅ signRep ⊞ rotRep`,
   `Ind ℂ² ≅ twoDimRep ⊞ stdRep ⊞ rotRep`.

Note the inducing subgroup `S₃` is non-abelian and one inducing rep (`ℂ²`) is
2-dimensional, so the `charRep`/`sum_cyclic` machinery of the `S₃/Zₙ` examples does not
transfer directly; the generic `frobenius_finrank` route does.

## Blockers

None. `S4_simple_iso` is landed and independent; the follow-up is well-scoped.
