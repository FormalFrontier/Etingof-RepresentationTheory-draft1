## Accomplished
Completed review issue #7167: statement-fidelity & non-vacuity audit of
**Theorem 5.15.1 (Frobenius character formula)** for the irreducible characters of Sₙ.
Report at `progress/reviews/2026-07-21-ch5-theorem5_15_1-frobenius-character-formula-fidelity.md`.

Verdict: **FAITHFUL** — `Etingof.Theorem5_15_1` and all supporting declarations
(`spechtModuleCharacter`, `spechtModuleRep`, `spechtModuleFDRep`,
`spechtModuleFDRep_character`, `spechtModuleFDRep_simple`, `vandermondePoly`,
`rhoShift`, `cycleTypePsumProduct`) faithfully formalize the book.

Key confirmations:
- `sign(revPerm)` = (-1)^{n(n-1)/2} (verified n=2,3,4) is exactly the global sign fix
  between Mathlib's Vandermonde ∏_{i<j}(x_j-x_i) and the book's ∏_{i<j}(x_i-x_j) — a
  documented, correct normalization, not a masked error.
- `toFinsupp la + rhoShift n` = λ+ρ (verified rhoShift 3 = (2,1,0)).
- `cycleTypePsumProduct` = ∏_{m≥1} p_m^{i_m} via genuine `psum` power sums, depends on
  σ only through cycle type (`cycleTypePsumProduct_inv`, `_isSymmetric`).
- LHS is the true irreducible Specht character (irreducibility via
  `spechtModuleFDRep_simple`); non-vacuity certified by the norm²=n! orthonormality
  (`specht_char_inner` / `spechtModuleCharacter_injective`).

Verification: `lake build …Theorem5_15_1` exit 0; `#print axioms` shows only
`[propext, Classical.choice, Quot.sound]` (no sorryAx, no custom axioms) for all
five headline/supporting decls.

Report-only; no Lean changes, no follow-up feature issue filed (no defect).

## Current frontier
Review issue #7167 done. Sibling review #7166 (Ch3 Jordan-Hölder) remains unclaimed.

## Overall project progress
Stage-6 flagship fidelity audits continuing. Two review issues were queued (#7166, #7167);
#7167 now audited FAITHFUL.

## Next step
A worker can claim #7166 (Jordan-Hölder theorem 3.7.1 fidelity audit).

## Blockers
None.
