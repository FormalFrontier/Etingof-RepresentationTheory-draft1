## Accomplished
- Claimed and completed issue #6692: `mckayAdj_no_selfLoop_cyclic` (Ch6 Problem 6.1.6(c),
  piece 4 of #6674), the **cyclic** branch of the McKay no-self-loop result.
- Added `EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean`:
  - `nontrivial_of_cyclic` — from `3 ≤ m` + cyclic `G`, derive `Nontrivial G` via the
    abelian classification: each irreducible `Wⱼ ≅ charFDRep ξⱼ`, the map `j ↦ ξⱼ` is
    injective, so `m ≤ |G →* ℂˣ| = |G|` (`Etingof.AbelianFDRep.card_charFDRep_dual`), hence
    `|G| ≥ 3 > 1`.
  - `finrank_invariants_V_eq_zero` — the tautological rep `V` of a nontrivial `G ⊂ SU(2)`
    has no nonzero invariant vector. A fixed vector is fixed by some `g ≠ 1`; then
    `det(A - 1) = 0`, and the 2×2 identity `det(A - 1) = 2 - tr A` forces `χ_V(g) = 2`,
    so `g = 1` by `taut_char_eq_two_imp_one` — contradiction.
  - `mckayAdj_no_selfLoop_cyclic` — main theorem. `Wᵢ ≅ charFDRep ξ` is 1-dim, so
    `χ_{Wᵢ}(g)·χ_{Wᵢ}(g⁻¹) = ξ(g)·ξ(g)⁻¹ = 1`; the character scalar-product formula
    (`FDRep.scalar_product_char_eq_finrank_equivariant`, `FDRep.char_tensor`) collapses to
    `rᵢᵢ = ⅟|G| ∑_g χ_V(g) = dim V^G = 0` (`FDRep.average_char_eq_finrank_invariants`).
- Added import `EtingofRepresentationTheory.Chapter5.AbelianFDRep` (no import cycle).

## Current frontier
- `lake build EtingofRepresentationTheory.Chapter6.Problem6_1_6` exits 0.
- `#print axioms mckayAdj_no_selfLoop_cyclic` = `[propext, Classical.choice, Quot.sound]`
  (sorry-free).

## Overall project progress
- Chapter 6 Problem 6.1.6(c): the parent `mckayAdj_no_selfLoop` (#6674) is still `sorry`;
  this delivers its cyclic branch. Remaining pieces: non-cyclic (#6690), dichotomy (#6691),
  central −Id (#6689, has PR).

## Next step
- Assemble `mckayAdj_no_selfLoop` once the non-cyclic pieces (#6689/#6690/#6691) land, using
  `mckayAdj_no_selfLoop_cyclic` for the cyclic case.

## Blockers
None.
