## Accomplished

Worked issue #6299 (Ch8 Problem 8.2.7(i): Tor/Ext for ℤ/a, ℤ/b ≅ ℤ/gcd(a,b)).

- Built the reusable `Etingof.ZModGcd` namespace in `Chapter8/Problem8_2_7.lean` proving the
  number-theory core (not in Mathlib):
  - `zmodCokerEquiv a b : (ZMod b ⧸ (span{(a:ℤ)} • ⊤)) ≃ₗ[ℤ] ZMod (gcd a b)` (cokernel of ·a),
  - `zmodKerEquiv a b : ker (mulByCast a b) ≃ₗ[ℤ] ZMod (gcd a b)` (a-torsion of ZMod b),
  - `tensorEquiv : TensorProduct ℤ (ZMod a) (ZMod b) ≃ₗ[ℤ] ZMod (gcd a b)`,
  - `homEquiv : (ZMod a →ₗ[ℤ] ZMod b) ≃+ ZMod (gcd a b)`.
- Proved the two **degree-0** part-(i) theorems `sorry`-free:
  - `Problem_8_2_7_i_tor_zero` (via Problem 8.2.6(i) `Tor₀ ≅ ⊗`, balancing subgroup trivial over ℤ,
    then `tensorEquiv`),
  - `Problem_8_2_7_i_ext_zero` (via Problem 8.2.6(i) `Ext⁰ ≅ Hom`, `ModuleCat.homAddEquiv`, `homEquiv`).
- Updated the module docstring; added imports (ZMod.QuotientRing, TensorProduct.Quotient,
  Isomorphisms, Ideal.Operations, Problem8_2_6).
- `lake build EtingofRepresentationTheory.Chapter8.Problem8_2_7` succeeds; remaining part-(i)
  `sorry`s are only `Problem_8_2_7_i_tor_one` / `_ext_one`.

## Current frontier

Degree-1 (`Tor₁`, `Ext¹`) for part (i). The reusable isos they need (`zmodKerEquiv` for Tor₁,
`zmodCokerEquiv` for Ext¹) are done; what remains is the categorical extraction of the
kernel/cokernel of `·a` from the derived six-term exact sequence
(`leftDerived_sixTerm_exact` / `Abelian.Ext.covariantSequence`) plus identifying `Tor₀(ℤ)`/`Ext⁰(ℤ)`
with `ZMod b`. Split into sub-issue **#6307**.

## Overall project progress

Stage 3 formalization ongoing. Problem 8.2.7(i): degree-0 + all higher-vanishing proved;
degree-1 remains (#6307). Part (ii) k[x] out of scope.

## Next step

Claim #6307 and fill `Tor₁`/`Ext¹` using the six-term sequence and the ready `ZModGcd` isos.

## Blockers

None. #6307 is unblocked (all its dependencies are in `main` once this PR lands).
