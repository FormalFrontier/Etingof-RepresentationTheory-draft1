## Accomplished

Closed the last open sorry of **Problem 3.8.4** (scalar extension + Noether-Deuring),
issue #6058 — assembled the general-`L` part (i) `iso_of_baseChange_iso`.

- Discovered the issue body was partly stale: part (ii)
  `directSummand_of_baseChange_directSummand` was **already sorry-free** on `main` in
  `Problem3_8_4_General.lean`. Only part (i) remained (the `sorry` at `Problem3_8_4.lean:69`).
- Part (i) could not be discharged in place: `Problem3_8_4.lean` is upstream of the
  Descent/Finite/Functoriality machinery (they import it), so calling those lemmas there
  would be an import cycle. Mirrored the part-(ii) solution: moved the general-`L` part (i)
  theorem into the downstream `Problem3_8_4_General.lean`, proved via the same Zariski
  specialization route:
  1. `Descent.exists_fg_subalgebra_baseChange_iso` — descend the iso to a fg `K`-subalgebra `R ⊆ L`.
  2. `finite_of_finite_type_of_isJacobsonRing` — residue field `κ = R ⧸ m` is finite over `K`.
  3. `Functoriality.nonempty_baseChange_iso` — push the iso forward along `R →ₐ[K] κ`.
  4. `iso_of_baseChange_iso_finite` — finish over the finite extension `κ / K`.
- Replaced the sorried theorem in the upstream `Problem3_8_4.lean` with a pointer comment
  (symmetric with how part (ii) was already handled), and refreshed the stale forward-reference
  comment in `Problem3_8_4_Finite.lean`.
- Marked `Chapter3/Problem3.8.4` as `sorry_free` in `progress/items.json`, repointed its
  `lean_file` to `Problem3_8_4_General.lean`.

## Verification

- `lake build EtingofRepresentationTheory.Chapter3.Problem3_8_4{,_General}` succeeds.
- No `sorry` in either file.
- `#print axioms` on both `iso_of_baseChange_iso` and
  `directSummand_of_baseChange_directSummand` → `[propext, Classical.choice, Quot.sound]` only.

## Current frontier

Problem 3.8.4 is fully closed (both parts sorry-free and axiom-clean). Note: the earlier
partial PR #6194 (`Problem3_8_4_Main.lean`, CONFLICTING) is now superseded — this assembly lives
in the existing `Problem3_8_4_General.lean` instead of a new `_Main.lean`; that PR can be closed.

## Overall project progress

Broad statement-pass proof-fill wave (per `progress/sorry-landscape.md`). This removes one more
`statement_formalized` item from the Chapter 3 backlog. Chapter 3's descent/Noether-Deuring
cluster (#6050 decomposition) is now complete.

## Next step

A planner should close the stale partial PR #6194 with a pointer to this work. Other unclaimed
`assemble` feature issues remain (#6139 Ch4 4.2.3 — blocked on #6127 sorry; #6162 Ch8 8.2.6v —
blocked on horseshoe #6161).

## Blockers

None.
