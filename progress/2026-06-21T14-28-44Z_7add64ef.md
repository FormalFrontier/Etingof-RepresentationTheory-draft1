## Accomplished

Investigated and scoped the Mathlib pin bump for #2841 (canonical pin-bump +
local-copy cleanup, also covering #2564). Concrete deliverables produced:

- **Identified the minimal target: Mathlib `v4.31.0`.** Both upstream lemmas land
  there and are absent before it:
  - `IsIdempotentElem.trace_eq_zero_iff` + forward alias
    `IsIdempotentElem.eq_zero_of_trace_eq_zero` (PR #39523), in
    `Mathlib/LinearAlgebra/Trace.lean`.
  - `MvPolynomial.eq_of_eval_eq_on_gl` (PR #38583), in
    `Mathlib/LinearAlgebra/Matrix/GeneralLinearGroup/MvPolynomial.lean`.
  Verified neither is present at v4.29.1 or v4.30.0; both present at v4.31.0.
- **Bumped the pin** (`lakefile.toml` rev → v4.31.0, `lean-toolchain` →
  `leanprover/lean4:v4.31.0`, `lake-manifest.json` regenerated via
  `lake update mathlib`; all transitive deps resolved to v4.31.0-compatible
  revisions). `lake exe cache get` succeeds — **all of Mathlib builds from cache**
  (8512 oleans), so the bump infrastructure itself is sound.
- **Applied both local-copy cleanups:**
  - `Chapter5/Theorem5_22_1.lean`: removed local
    `isIdempotentElem_eq_zero_of_trace_eq_zero`, forwarded its call site to
    `hg_idem.eq_zero_of_trace_eq_zero hg_tr_zero`.
  - `Chapter5/PolynomialRepEmbedding.lean`: removed local
    `MvPolynomial.eq_of_eval_eq_on_gl` (call sites already use the fully-qualified
    name, now resolving to upstream).
- Work committed to branch `agent/7add64ef` and pushed (no PR — see Blockers).

## Current frontier

Verification done in an isolated `/tmp/pinbump-build` clone (the pod worktree
shares `.lake/packages/mathlib` by symlink with the main repo, so it cannot hold
v4.31.0 while sibling agents build against v4.28.1 — any sibling `lake build`
reverts the shared mathlib checkout). Full sandbox build: **Mathlib green from
cache; ~29 project modules fail with 177 errors** from 3 minor-versions of
Mathlib API drift. The two cleanup files sit downstream of broken Chapter 5
deps, so they could not be verified end-to-end (their forwards are signature-
correct against the upstream lemmas).

## Overall project progress

Pin-bump target and infrastructure are settled and verified. The blocker is the
project-wide API-drift fallout, which is an atomic migration (a pin bump cannot
merge to main until every module builds, and cannot be split across
auto-merging PRs).

## Next step

This needs a **coordinated migration**, not a single worker session. Root-cause
buckets (from the sandbox build log):
- 21× `Unknown identifier pow_eq_zero` — removed upstream; replace with
  `eq_zero_of_pow_eq_zero` or `pow_eq_zero_iff …` per call site.
- 3× `Equiv.Perm.inv_apply_self` / `Equiv.Perm.apply_inv_self` — renamed.
- ~22× `rewrite` "did not find pattern", 12× `simp made no progress`,
  12× `split_ifs` no-ite, 14× `unsolved goals`, 11× `failed to synthesize
  instance`, 6× type-mismatch-after-simp — scattered simp-normal-form /
  instance drift across ~25 modules (Ch 2,3,4,5,6,7,8,9 + Infrastructure).
- Deprecations to sweep: `AddSubmonoidClass.coe_finset_sum` →
  `coe_finsetSum`, `push_neg` → `push Not`.

Recommended: a planner sets up one long-lived migration branch (base off
`agent/7add64ef`, which already has the verified manifest + cleanups) and either
runs a single dedicated long session or fans chapter-by-chapter fixes onto that
one branch, merging to main only once the whole project is green.

## Blockers

- **Atomic migration too large for one worker session** (~29 modules / 177
  errors) and structurally incompatible with pod's per-issue auto-merge model
  (cannot split an atomic pin bump across PRs to main). Skipped #2841 → replan
  with this analysis. Branch `agent/7add64ef` is pushed with the verified
  bump + cleanups for a successor to continue from.
</content>
</invoke>
