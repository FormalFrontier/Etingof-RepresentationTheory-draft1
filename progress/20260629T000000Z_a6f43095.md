## Accomplished

One-shot dispatch on **#5587** (Cauchy step 3: hull-spanning bridge
`rightHull_le_iSup_range_peterWeylSummandMap`).

Reduced the single research-level `sorry` to an isolated, sharply-scoped
*realization* lemma, with all the surrounding plumbing proved sorry-free in
`Chapter5/Theorem5_23_2_PeterWeyl.lean`:

- **`localRightRep_mapsTo_range_peterWeylSummandMap`** (sorry-free): each
  per-summand range `range (peterWeylSummandMap n λ k)` is stable under right
  translation `localRightRep k n g`, via the `(1, g)`-specialization of
  `peterWeylSummandMap_equivariant` (right action = the `L_λ`-action).
- **`localRightRep_mem_iSup_range_peterWeylSummandMap`** (sorry-free): the full
  matrix-coefficient span `T = ⨆_λ range (peterWeylSummandMap n λ k)` is
  `localRightRep`-stable (join of stable pieces).
- **`simpleSubrep_localRightRep_le_iSup_range`** (the lone remaining `sorry`):
  every simple finite-dimensional `localRightRep`-subrepresentation `S ↪ R` lies
  in `T`. This is the genuine *realization* step — identifying a simple algebraic
  constituent with a concrete `AlgIrrepGL n λ k` via the highest-weight
  classification and `equivariant_range_le_peterWeylSummandMap`.
- **`rightHull_le_iSup_range_peterWeylSummandMap`** (now sorry-free *modulo* the
  realization lemma): packages `T` as a `k[GL_n]`-submodule `T_KG` (using its
  right-stability), decomposes the semisimple hull
  (`rightHull_isSemisimple`, #5577) as `sSup` of its simple `k[GL_n]`-submodules
  via the `k[GL_n]`-linear inclusion `asModuleHomOfIntertwiner`, bounds each
  simple piece by the realization lemma, and restricts scalars back to `k`.

`lake build EtingofRepresentationTheory.Chapter5.Theorem5_23_2_PeterWeyl` is green
(8686 jobs); the only `sorry` is `simpleSubrep_localRightRep_le_iSup_range`.

## Current frontier

The Cauchy/Peter-Weyl spanning argument is now reduced to the single realization
lemma `simpleSubrep_localRightRep_le_iSup_range`. The reduction (semisimplicity →
sup of simples → matrix-coefficient bound, plus right-stability of the
matrix-coefficient span) is fully in place.

## Overall project progress

Stage 3 ongoing. `peterWeylMap_surjective` / `peterWeylMap_bijective` /
`Theorem5_23_2_ii_equivariant` still depend (transitively) on the one realization
`sorry`; `#print axioms` therefore still carries `sorryAx` until it is discharged.

## Next step

Discharge `simpleSubrep_localRightRep_le_iSup_range` (filed as a sub-issue): twist
the simple subrep `S` by `det^r`, read its character off as a Schur polynomial
`schurPoly N ν`, build the dominant weight `λ` (with `λ.toNatWeight = ν` after the
shift), produce the equivariant iso `AlgIrrepGL n λ k ≃ S` via
`iso_of_formalCharacter_eq_schurPoly` / `simpleRep_iso_schurModule_of_formalCharacter_eq`,
and finish with `equivariant_range_le_peterWeylSummandMap`.

## Blockers

None new. The realization lemma is the isolated remaining obligation, tracked in
its own sub-issue.
