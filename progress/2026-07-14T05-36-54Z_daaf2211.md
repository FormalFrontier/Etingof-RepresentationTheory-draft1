## Accomplished
Formalized the **injectivity half of the cons-splitting** `A_n ⊗_S V ≅ A_{n+1}` (issue #6545),
in the form actually consumed downstream: a genuine (non-`sorry`) left inverse of the top-half
boundary map `Φ = stdΦ M : A ⊗_S (V ⊗_S M) → A ⊗_S M`.

New file `EtingofRepresentationTheory/Chapter9/PathAlgebraConsSplittingRetraction.lean`:
- `consTermC` — the cons-decomposition of a basis path (nil ↦ 0; `cons p e` ↦ `ofPath p ⊗ (single e c ⊗ m)`),
  with the coefficient placed on the arrow factor so the retraction is `S`-balanced with no field scalars.
- `stdΦRet : A ⊗_S M →+ A ⊗_S (V ⊗_S M)` — the retraction, via `TensorProduct.liftAddHom`.
- `stdΦRet_leftInverse : Function.LeftInverse (stdΦRet M) (stdΦ M).hom` — `R (Φ ξ) = ξ`, pure-tensor
  by pure-tensor: composable case via `compSingle_eq` + `cons_tensor_coeff_move`; non-composable via
  `compSingle_eq_zero` + `cons_tensor_noncomp`.
- `stdΦ_injective : Function.Injective (stdΦ M).hom` — the crux `Mono (stdd M)` needs (issue #6561).

`#print axioms stdΦ_injective` → only `propext, Classical.choice, Quot.sound` (no `sorryAx`).
Registered the file in `EtingofRepresentationTheory/Chapter9.lean`.

## Current frontier
Injectivity of `stdΦ` is complete and sorry-free. It is the reusable ingredient #6561 (`stdd_mono`)
and #6545's bundled `≃ₗ` both build on.

## Overall project progress
Stage 3 (formalization) ongoing. Chapter 9 standard resolution: surjectivity half
(`exists_stdΦ_preimage_topDegree`), middle exactness (`standardComplex_exact`, in open PR #6563),
and now the injectivity half (`stdΦ_injective`) are all in place.

## Next step
- #6561 (`stdd_mono` + `standardResolution_shortExact`): use `stdΦ_injective` here (whole-map
  injectivity of `Φ`) to discharge the top-degree base case, once #6563 (standardComplex_exact)
  merges. The retraction route the issue describes can be replaced by `stdΦ_injective`.
- #6545's optional bundled `≃ₗ A_n ⊗_S V ≅ A_{n+1}` (submodule iso) remains if wanted as standalone
  infra; the operational injectivity it would provide is now available as `stdΦ_injective`.

## Blockers
None. Key lesson: the field `k` does NOT act on `A ⊗_S -` tensors (only `S = Q → k` does), and the
`VtensObj` source-action instance is only selected when the whole tensor is ascribed to the
`inducedVtensObj M` abbrev — bare `(v ⊗ₜ m : VtensObj M)` picks the canonical (target) instance.
