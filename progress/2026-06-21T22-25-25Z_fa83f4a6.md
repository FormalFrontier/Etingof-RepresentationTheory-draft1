## Accomplished

Feature session for #4944 (right-`GL_N` Cauchy character identity, residual
assembly). Discharged the `sorry` for `polyRightDegreeFDRep_formalCharacter`:

- Proved the theorem sorry-free via `MvPolynomial.ext`. Per weight `μ`, the LHS
  coefficient is #4957's multichoose count
  (`formalCharacter_polyRightDegreeFDRep_coeff`), and the RHS coefficient
  `∑_ν s_ν(1^N)·(schurPoly N ν.parts).coeff μ` is the same count by the #4958
  Schur-basis Cauchy core. Off the degree-`d` locus both sides vanish: the LHS
  by #4957's `if`, the RHS by `schurPoly_isHomogeneous` (degree `∑ ν.parts = d`)
  via `IsHomogeneous.coeff_eq_zero` + `Finsupp.degree_eq_sum`.
- **Layering fix:** the theorem was stated in `CauchyCharacterRight.lean`, but
  its #4957 ingredient lives in `CauchyWeightSpaceDimension.lean`, which *imports*
  `CauchyCharacterRight.lean`. Proving it in place is a circular import. Moved the
  theorem to a new downstream file `CauchyCharacterRightAssembly.lean`, re-opening
  the `Etingof.CauchyCharacterRight` namespace so the fully-qualified name is
  unchanged. Left a pointer comment in `CauchyCharacterRight.lean`.
- The #4958 obligation is stated as a named, documented `sorry` dependency
  `cauchy_schur_coeff_multichoose` (the Schur-basis Cauchy identity at `y = 1^N`).
  The issue explicitly permits referencing #4957/#4958 lemmas while they carry
  sorries; the assembly itself is sorry-free.

`lake build` green (8063 jobs). Only new `sorry` warning is the #4958 named
dependency.

## Current frontier

`polyRightDegreeFDRep_formalCharacter` is proved modulo #4958. The residual is
purely the symmetric-function content of #4958 (open, research-level).

## Overall project progress

Chapter 5 Schur-Weyl / Cauchy program. #4944 deliverable assembled. Net sorry
count unchanged (removed one in `CauchyCharacterRight.lean`, added the #4958
named dependency in the assembly file).

## Next step

When #4958 lands its Schur-basis Cauchy coefficient lemma, replace the `sorry`
in `cauchy_schur_coeff_multichoose` (`CauchyCharacterRightAssembly.lean:72`) with
`exact <#4958 lemma>` (or delete the local stub and reference #4958's directly).

## Blockers

None for this issue. #4958 remains open (it is the named dependency, not a
blocker per project policy).
