## Accomplished
Worked #6906 (piece (b) of the Ẽ₆/Ẽ₇/Ẽ₈ one-branch tree case): the **reciprocal equality** for the
three arm lengths of a one-branch affine Dynkin diagram.

- Stated `Etingof.Problem6_1_3_tildeE.affine_tree_one_arm_reciprocal` and the layout predicate
  `armAdjIdx p q r i j` (the explicit edge relation of the arm layout: arms `p, q` join through the
  hub into the path `0 … p+q` at hub index `p`, arm `r` hangs off the hub from index `p+q+1`).
- Proved helper `arm_linear`: a harmonic sequence on an arm (leaf condition `2 f0 = f1`, interior
  `2 fᵢ = fᵢ₋₁ + fᵢ₊₁`) is linear, `fᵢ = (i+1)·f0`.
- Proved helper `reciprocal_of_arm_data`: the pure arithmetic `W = (p+1)a = (q+1)b = (r+1)c`,
  `a+b+c = W`, `W > 0` ⟹ the cleared-denominator reciprocal equality.
- Proved `affine_tree_one_arm_reciprocal` **modulo one structural lemma**: took the strictly-positive
  null vector (`affineNullVector_pos`), reindexed the Cartan harmonic row equation along the layout
  (`hlap` + neighbour-set reductions `hlap_one/two/three`), applied `arm_linear` to each of the three
  arms (arm R needs the hub value plugged as its `(len)`-th term), derived hub harmonicity
  `a+b+c = W`, and closed via `reciprocal_of_arm_data`.
- Factored the remaining graph-combinatorial extraction into `affine_one_branch_arm_layout` (stated,
  doc'd, `sorry`), and filed it as **#6913**.

All of `Chapter6/Problem6_1_3_continued_tildeE.lean` builds; the only new `sorry` is
`affine_one_branch_arm_layout` (the arm-walk).

Note: `omega` would not close the two neighbour-set iffs involving the special hub↔R-head edge with
`simp only [armAdjIdx]`; `unfold armAdjIdx; omega` works. `armAdjIdx` was written without `min`/`max`
(explicit `≤` bounds) for omega-friendliness.

## Current frontier
`affine_one_branch_arm_layout` (`Chapter6/Problem6_1_3_continued_tildeE.lean`) is `sorry` — the pure
graph walk producing the three arms as an explicit `armAdjIdx` layout `σ`.

## Overall project progress
Phase 3 formalization ongoing. `affine_dynkin_classification` (⟹ direction, #6785) tree case
sub-tree: `affine_tree_one_branch_iso` (#6907) needs `affine_tree_one_arm_reciprocal` (this issue,
now reduced to the layout #6913) + `affine_arm_length_solutions` (landed).

## Next step
Discharge `affine_one_branch_arm_layout` (#6913): walk the three arms from the hub via the
degree-`≤ 2` neighbour structure (`path_walk_construction` / `otherNbr` / tree API), order the arm
lengths, assemble `σ`. Then #6906 is fully sorry-free and #6907 unblocks.

## Blockers
None hard. #6906 landed partial (reciprocal core proved); its residual is the well-specified #6913.
