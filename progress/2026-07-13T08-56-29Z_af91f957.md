## Accomplished

Claimed **#6458** (`affine_classification` via Theorem 5.27.1). Landed the reusable
**dual `Kˣ`-orbit analysis** for the affine group in
`EtingofRepresentationTheory/Chapter5/Exercise5_27_2_Affine.lean` (sorry-free lemmas above
the still-sorried main theorem):

- `affineDualSmul` — the concrete dual `Kˣ`-action on `Â = Multiplicative K →* ℂˣ`,
  `g · χ = a ↦ χ(g⁻¹ · a)`; matches the engine's existential `dualSmul` via Theorem 5.27.1's
  `hdual` clause.
- `affineφ_apply'`, `affineDualSmul_apply`, `affineDualSmul_trivial`,
  `affineDualSmul_eq_mulShift` — computational glue; the dual action is the multiplicative shift
  `AddChar.mulShift` of the associated additive character.
- `affineDualSmul_eq_self_iff` — **stabilizer dichotomy**: a nontrivial character is fixed by `g`
  iff `g = 1` (so its stabilizer is trivial); the trivial character is fixed by all of `Kˣ`.
- `affineDualSmul_transitive` — `Kˣ` acts **transitively** on the `q-1` nontrivial characters
  (single free orbit).

The proofs use Mathlib's primitive additive-character machinery over a field
(`AddChar.IsPrimitive.of_ne_one`, `AddChar.to_mulShift_inj_of_isPrimitive`) plus
`AbelianFDRep.card_charFDRep_dual` for the `|K| = |Â|` count that upgrades injectivity to
bijectivity.

## Current frontier

**Correctness defect found in the committed statement** (reported on #6458): the two dimension
counts are FALSE at `|K| = 2`. For `K = 𝔽₂` (`q = 2`) the classification gives `q-1 = 1`
trivial-orbit character (dim 1) plus one free-orbit rep of dim `q-1 = 1`, i.e. TWO reps of
dimension 1, but the statement claims `filter(finrank = 1).card = q - 1 = 1`. Both count
conjuncts collapse to the same false claim. The theorem is true for `q ≥ 3` and unprovable as
written. A planner must correct the statement before the assembly can be discharged
(recommended fix in the issue comment: replace the two counts by the structural form
"∃ i₀, finrank (W i₀) = q-1 ∧ ∀ i ≠ i₀, finrank (W i) = 1", true for all q and equivalent to
the original counts for q ≥ 3).

## Overall project progress

Stage 3 (formalization) ongoing. Chapter 5 Exercise 5.27.2: dihedral (`semidirect_classification`
sorried — #6471, claimed by another agent), affine (this file — orbit infra landed, assembly +
statement correction remain), Heisenberg (#6478 helper merged). The orbit-method engine
`Theorem5_27_1` is sorry-free.

## Next step

After a planner corrects the `affine_classification` statement (see #6458 comment), the assembly
consumes `Etingof.Theorem5_27_1 (Multiplicative K) Kˣ (affineφ K)` and builds the `Fin q` family
from the now-landed orbit lemmas:
- trivial orbit: `stab 1 = ⊤ ≅ Kˣ`, its `q-1` characters give `V(1, charFDRep ξ)` (dim 1) each;
- free orbit: pick a nontrivial `χ₀` (`stab χ₀ = ⊥` via `affineDualSmul_eq_self_iff`), one
  `V(χ₀, charFDRep 1)` of dim `[Kˣ:⊥] = q-1`.
Discharge Simple (i)+`charFDRep_simple`, non-iso (ii)+`charFDRep_iso_iff`+orbit distinctness,
completeness (iii)+`affineDualSmul_transitive`+`exists_charFDRep_iso`, and the corrected counts.

## Blockers

The committed `affine_classification` statement is false at `|K| = 2` — the assembly cannot be
completed until the statement is corrected (replan). Reported on #6458; partial PR lands the
orbit infrastructure.
