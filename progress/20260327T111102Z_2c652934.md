## Accomplished

- Claimed issue #1840 (essential surjectivity of cornerFunctor)
- Decomposed the single `morita_equiv_of_full_idempotent` sorry into three clean sub-lemmas:
  - `cornerFunctor_full` (sorry) — lifts eAe-linear maps to A-linear maps
  - `cornerFunctor_essSurj` (sorry with witness) — balanced tensor product witness M = (A ⊗_k N) / S fully constructed
  - `morita_equiv_of_full_idempotent` — now PROVED from Full + Faithful + EssSurj via `Functor.asEquivalence`
- Constructed the balanced tensor product A ⊗_{eAe} N in Lean:
  - k-module structure on N via restriction of scalars through CornerRing's k-algebra
  - A-module structure on A ⊗_k N via `TensorProduct.leftModule`
  - Balanced submodule S as A-span of {ar ⊗ n - a ⊗ rn | r ∈ eAe}
  - Quotient Q = (A ⊗_k N) / S as the A-module witness
- Added import for `Mathlib.LinearAlgebra.TensorProduct.Defs`

## Current frontier

The essential surjectivity witness M is constructed. The remaining sorry is the isomorphism eM ≅ N as CornerRing-modules, where:
- ψ : N → eM sends n ↦ [e ⊗ n]
- ξ : eM → N sends [ea ⊗ n] ↦ (eae) • n
- These are mutually inverse (verified on paper using balanced relation + e² = e + e is unit of eAe)

For fullness (`cornerFunctor_full`), the standard proof uses the balanced tensor product adjunction. This is harder than essential surjectivity.

## Overall project progress

27 sorries across 19 files (from sorry landscape wave 38). This PR adds 1 sorry (decomposition: 1 → 3, but 1 is proved from the other 2, and the original was already counted). The structural improvement is significant: `morita_equiv_of_full_idempotent` is now proved modulo clearly scoped sub-goals.

## Next step

1. Prove the isomorphism `eM ≅ N` in `cornerFunctor_essSurj` — construct ψ and ξ as CornerRing-linear maps, prove they're inverse
2. Prove `cornerFunctor_full` — either via the balanced tensor product adjunction or direct construction
3. These two together would make `morita_equiv_of_full_idempotent` sorry-free

## Blockers

None — the balanced tensor product construction works in Lean and the mathematical proof is clear. The remaining work is formalization effort (constructing the linear maps and proving properties).
