## Accomplished
Worked issue #5336 (Ch4 Remark 4.5.3, character-recovery sorries). Discharged
3 of the 4 sorries in `EtingofRepresentationTheory/Chapter4/Remark4_5_3.lean`:

- `mem_classFunctions_iff` — centre of `ℂ[G]` ↔ class functions. Forward:
  test centrality against `single y 1`, evaluate at `y*x`, use
  `single_mul_apply`/`mul_single_apply`. Backward: compare the two
  convolution expansions (`mul_apply_left`/`mul_apply_right`) termwise via
  `Finsupp.sum_congr`, using the class-function property as a conjugation.
- `renormCharElt_mem_classFunctions` — immediate from `mem_classFunctions_iff`
  + `FDRep.char_conj`.
- `character_recovery` — witness `c = |G|/χ_V(1)`; `χ_V(1) = dim V ≠ 0` for
  simple `V` via a new private helper `finrank_pos_of_simple` (mirrors the
  one in `Corollary4_2_4.lean`, made universe-polymorphic). Both
  square-root and recovery equations close with `field_simp`.

File builds clean with exactly 1 remaining sorry.

## Current frontier
Only `renormChar_isPrimitiveIdempotent` remains (line ~183). It bundles
idempotency (`e*e=e`) and primitivity. Idempotency needs the convolution
Schur identity `∑_x χ(x)χ(x⁻¹z) = (|G|/dim) χ(z)` (provable via the
"central element acts as a scalar" / Schur argument). Primitivity needs the
centre's `∏ ℂ` structure, which is **not** in Mathlib.

## Overall project progress
Stage 3 formalization, sorry-filling. Remark4_5_3 went 4→1 sorries.

## Next step
Issue #5349 was created with the full recorded mathematical route (Parts
A/B/C: nonzero, idempotency via Schur scalar, primitivity via central
character / orthogonal idempotent family). Next agent picks up #5349.

## Blockers
Primitivity (Part C of #5349) likely needs new infrastructure (central
character of `ℂ[G]`, or `e_V * e_W = δ e_V` orthogonality) — Mathlib lacks
the class-functions = centre `∏ ℂ` decomposition.
