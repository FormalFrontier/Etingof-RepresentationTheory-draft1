## Accomplished
Assembled `mckayAdj_no_selfLoop` (issue #6674) in
`EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean`, replacing its raw
`sorry` with the real proof: case-split on the dichotomy
`su2_finite_cyclic_or_contains_negId` and delegate to the two landed
case lemmas (`mckayAdj_no_selfLoop_cyclic` for the cyclic case;
`mckayAdj_no_selfLoop_of_central_neg` for the −Id case, constructing the
central `z = ⟨negIdSU, hneg⟩` acting as −1 on `V` via `tautRep_negId`).
`lake build EtingofRepresentationTheory.Chapter6.Problem6_1_6` exits 0.

## Current frontier
The four assembly pieces (#6689–#6692) are combined. `mckayAdj_no_selfLoop`
itself is now sorry-free. However `mckay_isAffineDynkin` still shows
`sorryAx` under `#print axioms`, because the dichotomy
`su2_finite_cyclic_or_contains_negId` invokes `isCyclic_of_odd_card`
(line 1060), which is still a `sorry` — the odd-order crux. That crux is
NOT one of #6674's listed dependencies; it is tracked separately by
#6715 (assemble `isCyclic_of_odd_card`), which is blocked on #6714
(dimension of a complex irrep divides |G|, Frobenius).

The remaining `sorry` is now the ONLY one in the file (line 1060).

## Overall project progress
Phase 3 formalization ongoing. `mckay_isAffineDynkin` (McKay
correspondence, Problem 6.1.6(c)) is fully assembled except for the single
odd-order-cyclic crux. Once `isCyclic_of_odd_card` (#6715 ← #6714) lands,
the whole theorem is sorry-free with no further assembly work.

## Next step
Land #6714 (Frobenius: dim ∣ |G|), then #6715 (assemble
`isCyclic_of_odd_card`). That automatically makes `mckay_isAffineDynkin`
sorry-free.

## Blockers
Full sorry-freeness of #6674's verification criterion is blocked on
#6715 ← #6714 (odd-order crux). The assembly deliverable itself is complete.
