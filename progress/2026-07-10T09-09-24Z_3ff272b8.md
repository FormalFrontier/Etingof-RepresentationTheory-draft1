## Accomplished

Worked issue **#6098** (feat, Ch4): field-general counting bound
`#(simple k[G]-modules) ≤ #ConjClasses` via base change to a splitting field. This is a large,
multi-chapter theorem (Wedderburn of `A/rad` from Ch9, cocenter dimension from Ch4, base-change
count monotonicity), so — following the issue author's own "split step 2 into a further
sub-issue" guidance — I landed a **top-down scaffold** and decomposed the two hard cores into
sibling issues.

New file `EtingofRepresentationTheory/Chapter4/Exercise4_2_3_FieldGeneral.lean` (registered in
`Chapter4.lean`), field-general (`[Field k]` only):

- `Etingof.natCard_irrepClasses_le_conjClasses (k G)` — the final target
  `Nat.card (IrrepClasses k G) ≤ Nat.card (ConjClasses G)`, **proved outright** (no sorry in the
  assembly itself) by `le_trans` through `K = AlgebraicClosure k`: base-change monotonicity
  `k → K`, then the split-field bound over the algebraically closed `K`. Uses that
  `Nat.card (ConjClasses G)` is field-independent, so both bounds share the RHS.
- `natCard_irrepClasses_le_conjClasses_of_isAlgClosed` — split-field bound, **sorry** (→ #6126).
- `natCard_irrepClasses_le_of_ringHom_field` — base-change monotonicity, **sorry** (→ #6127).

Both sorried lemmas are stated precisely (exact types/universes) with rich doc-comments giving
the intended proof route and the relevant existing infrastructure, so the assembly's interface is
now fixed and the two sub-proofs compose by construction.

## Current frontier

`Exercise4_2_3_FieldGeneral.lean` builds (only the two expected `sorry` warnings). The scaffold is
staged; about to commit and open a `--partial` PR on #6098.

## Overall project progress

Stage 3 formalization ongoing. Exercise 4.2.3's counting half is now scaffolded field-general; the
two genuinely field-sensitive/heavy cores are queued as #6126 (split-field Wedderburn + cocenter
trace independence) and #6127 (base-change count monotonicity, watch inseparability of
`AlgebraicClosure k / k`). The parent `Exercise4_2_3` strict bound (`<`, modular case) and the
`Finite (IrrepClasses)` fact remain separate downstream items.

## Next step

Claim **#6126** (split-field bound) — it has the most existing infra
(`Chapter9/Theorem9_2_1.lean` Wedderburn-of-`A/J`, `Exercise4_2_3_Cocenter.lean` trace forms,
`Exercise4_2_3_CountingBound.lean` packaging). #6127 (base-change) is independent; note the
separability caveat documented in its body and in the scaffold docstring.

## Blockers

None. #6126 and #6127 are unblocked (the scaffold merges via the partial PR on #6098).
