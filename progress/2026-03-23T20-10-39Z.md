## Accomplished
- Proved `coeff_invOfUnit_single`: coefficient extraction for a single geometric series factor `invOfUnit (1 - X_a * X_b) 1`, showing the coefficient at exponent `e` is 1 iff `e` is supported on `{a, b}` and `e(a) = e(b)`, and 0 otherwise. Proof by induction on `e(a)` using the recurrence `g = 1 + X_a X_b · g`.
- Proved `cauchyProd_coeff_perm`: the main product theorem showing `coeff d (∏_j invOfUnit (1 - X_{inl j} · X_{inr(σ j)}) 1) = if (∀ j, d(inl j) = d(inr(σ j))) then 1 else 0`. Proof uses `MvPowerSeries.coeff_prod` to expand as a sum over `finsuppAntidiag`, applies the single-factor lemma via `Finset.prod_boole`, then collapses the sum by showing there is at most one valid decomposition (by disjoint variable support uniqueness).
- Both theorems are sorry-free. The downstream theorem `cauchyRHS_coeff_diag` was already proved in terms of `cauchyProd_coeff_perm` and is now also sorry-free.

## Current frontier
`PowerSumCauchyIdentity.lean` has zero sorries. All three theorems (`coeff_invOfUnit_single`, `cauchyProd_coeff_perm`, `cauchyRHS_coeff_diag`) are complete.

## Overall project progress
Stage 3.2 proof work for Chapter 5 power series coefficient extraction is complete for this issue (#1670). The file builds cleanly with no errors and no warnings (except one cosmetic `show` → `change` style note that was already fixed).

## Next step
Merge this PR and continue with downstream items that depend on `cauchyProd_coeff_perm` or `cauchyRHS_coeff_diag`.

## Blockers
None.
