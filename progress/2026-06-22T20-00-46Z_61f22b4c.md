## Accomplished

Claimed #4905 (`feat(Ch5 #4896 assembly): discharge
quotDetRep_irreducible_constituent_lastWeight_zero from Cauchy + grading + det-shift`)
via `/work`, scoped it thoroughly, and found the issue's premise — "discharge one
`sorry` by combining the three merged ingredients #4960/#4961/#4962" — materially
undercounts the assembly. Worker-led decomposition: created three self-contained
successor issues and skipped the parent for planner triage (narrow to residual).

### Scoping findings (the real glue)

Applying the part-C extractor `Etingof.simple_constituent_formalCharacter_eq_schurPoly_mem`
(`ConstituentCharacterExtraction.lean:105`) to `M = quotDetDegreeFDRep k N d`
(`CauchyDetQuotientDegree.lean:280`) needs four inputs that do **not** exist:

1. `halg : IsAlgebraicRepresentation N M.ρ` — no quotient/cokernel-algebraicity lemma;
   even the source `polyRightDegreeFDRep` is not known algebraic. **Chief blocker.** → #5062
2. `h_span` (ℕ-weight spaces span ⊤) + `h_homog` (degree `d`) for `quotDetDegreeFDRep` —
   derivable from the SES machinery (`formalCharacter_add_of_shortExact:106-114`,
   `polyRight_iSup_glWeightSpace_eq_top:220`) but only inlined, no standalone lemma. → #5063
3. character in the part-C shape (antitone-subtype `Finset`, ℕ coefficients) and the
   `d < N` case — part B `quotDetDegreeFDRep_formalCharacter` requires `N ≤ d`, but
   part A (`exists_degree_embedding_of_simple`) lands `L` in an **arbitrary** degree `d`.
   For `d < N`: `det·A ∩ A_d = 0` so `(A/det)_d ≅ A_d`, full Cauchy character, and every
   `ν` (with `∑ ν.parts = d < N`) automatically has `0 ∈ range`. → #5064
4. **universe obstruction**: the part-C extractor and the `iso_of_formalCharacter_eq_schurPoly`
   classification chain (`SchurWeylFormalCharacterIso.lean:133`,
   `ConstituentCharacterExtraction.lean:93`) are `k : Type` (universe 0), but the consumable
   interface `quotDetRep_irreducible_constituent_lastWeight_zero` is `k : Type*`.
   (`decompose_polynomial_gl_rep` is already universe-polymorphic; the `Type` pin is in the
   `SchurWeylFormalCharacterIso` layer.) The narrowed assembly must specialize the interface
   to `Type` (if consumer #4897 only uses universe 0) or generalize the chain to `Type*`.

Part A's `exists_degree_embedding_of_simple` (#4961) signature matches the main theorem's
hypotheses exactly — that ingredient is genuinely ready. Parts B/C are also sound; the gap
is the un-tracked supporting infrastructure above.

## Current frontier

#4905 skipped → `replan`. Three feature sub-issues open: #5062 (algebraicity, the blocker),
#5063 (span + homogeneity, tractable), #5064 (character translation + `d < N`, tractable).
No code landed this turn (a compiling scaffold of the `Type*` main theorem via the `Type 0`
part-C extractor is impossible without first resolving the universe gap).

## Overall project progress

Stage 3 (formalization), Chapter 5 Cauchy/determinant-quotient (K′) cluster. The (K′)
part-(a) deliverable `quotDetRep_irreducible_constituent_lastWeight_zero` remains a single
documented `sorry` (`CauchyDetQuotient.lean:124`), now correctly decomposed into its true
residual inputs.

## Next step

Workers: claim #5063 or #5064 first (both tractable, ~1 session each, no exotic deps).
#5062 (algebraicity) is the hard, high-value one — route: prove `polyRightDegreeFDRep`
algebraic (right-translation coefficients are polynomial, model on `glTensorRep_isAlgebraic`)
+ a general `IsAlgebraicRepresentation.of_surjective_equivariant` (quotient of algebraic is
algebraic via a linear section), then apply to `A_d → (A/det)_d`.

Planner: narrow #4905 to the final assembly residual with `depends-on: #5062 #5063 #5064`
and the universe resolution noted in the breadcrumb comment.

## Blockers

#4905 assembly is blocked on #5062/#5063/#5064 and the `Type` vs `Type*` universe
reconciliation (item 4 above). No definition-level sorries introduced.
