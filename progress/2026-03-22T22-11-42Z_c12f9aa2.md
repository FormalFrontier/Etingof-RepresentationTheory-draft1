## Accomplished

- **Analyzed** the last sorry in Theorem5_15_1.lean (`alternatingKostka_norm_sq_eq_one` at line 2143). Determined it requires substantial infrastructure: FDRep bridge + Vandermonde coefficient orthogonality (or equivalently, Jacobi-Trudi identity). None of these exist in the project or Mathlib.

- **Decomposed** issue #1553 into three sub-issues:
  - #1591: Infrastructure: Specht module FDRep bridge (Representation + Simple + character equality)
  - #1592: Infrastructure: Vandermonde coefficient orthogonality (∑_σ θ_λ(σ)² = n!)
  - #1593: Prove alternatingKostka_norm_sq_eq_one (assemble from #1591 + #1592)

- **Built FDRep bridge** (partial, issue #1591):
  - `spechtModuleRep`: proved the representation structure (map_one, map_mul)
  - `spechtModuleFDRep`: wrapped as FDRep via `FDRep.of`
  - `spechtModuleFDRep_character`: character equality is definitional (rfl!)
  - `spechtModuleFDRep_simple`: sorry — requires bridging `IsSimpleModule` across different Module instances (`Representation.asModule` vs `Submodule.module`)

## Current frontier

The `Simple` instance for `spechtModuleFDRep` requires showing the `Representation.asModule` SMul and `Submodule.module` SMul on `↥(SpechtModule n la)` agree. Both act by left multiplication in the group algebra, but `asModule` goes through `MonoidAlgebra.lift` while `Submodule.module` uses direct multiplication. A similar bridge exists in `IrreducibleEnumeration.lean` (private `FDRep.simple_of_isSimpleModule_asModule`) but uses the `columnRep` structure, not `SpechtModule`.

## Overall project progress

- Theorem5_15_1.lean has 2 sorries:
  1. `spechtModuleFDRep_simple` (new, line 121) — Module instance bridge
  2. `alternatingKostka_norm_sq_eq_one` (pre-existing, line 2184) — needs full FDRep bridge + Vandermonde orthogonality
- Both blocked on the `Simple` instance (which is blocked on the Module instance equality)

## Next step

1. Prove `spechtModuleFDRep_simple` by showing the two `Module (MonoidAlgebra ℂ G)` instances on `↥(SpechtModule n la)` agree. Key approach: induction on `MonoidAlgebra` elements using `MonoidAlgebra.induction_on`, showing the `asAlgebraHom` action equals left multiplication. The `hM` (generator) case should be straightforward; the `hadd` case is proved; the `hsmul` (scalar) case needs `MonoidAlgebra.algebraMap` unfolding.

2. After `Simple`, prove Vandermonde coefficient orthogonality (#1592).

3. Assemble `alternatingKostka_norm_sq_eq_one` (#1593).

## Blockers

The Module instance bridge (`Representation.asModule` vs `Submodule.module` SMul equality) is a Lean type class diamond that requires careful unfolding through `MonoidAlgebra.lift` and `asAlgebraHom`. The same pattern exists in `IrreducibleEnumeration.lean` but is private and uses a different representation structure.
