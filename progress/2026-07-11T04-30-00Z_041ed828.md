## Accomplished

Worked issue #6435 (dihedral classification via Theorem 5.27.1). After scope
assessment, **decomposed** it into three self-contained sub-issues and completed
the first:

- Created #6450 (engine dimension formula), #6451 (character/orbit combinatorics),
  #6452 (group iso to semidirect product + assembly; blocked on #6450 + #6451).
  Skipped #6435 with a `Decomposed into #6450, #6451, #6452` breadcrumb.
- **Completed #6450**: exposed the induced-representation dimension formula in
  `Theorem5_27_1.lean`. Added a private helper `inducedRepV_finrank`
  (`finrank ℂ (inducedRepV φ χ U) = (stabAux φ χ).index * finrank ℂ U`; the carrier
  is the Pi type `(G ⧸ stab χ) → U`, so `Module.finrank_pi_fintype` +
  `Finset.sum_const` + `Subgroup.index_eq_card` + `Nat.card_eq_fintype_card`) and
  appended it as conjunct (v) of the packaged existential `Etingof.Theorem5_27_1`.

## Current frontier

`Etingof.Theorem5_27_1` now also asserts the dimension formula. Engine builds
sorry-free (8580 jobs); `#print axioms` = {propext, Classical.choice, Quot.sound}.
Downstream `Exercise5_27_3` still builds (no regression — nothing destructures the
packaged existential; only a comment references it).

## Overall project progress

Stage 3 formalization ongoing. The dihedral classification (#6435) is now a
three-part plan; the engine API gap that blocked any *counting* classification
(dimensions were hidden by the existential) is closed. Remaining dihedral work:
#6451 (combinatorics) and #6452 (group iso + assembly).

## Next step

Pick up #6451 (inversion-orbit combinatorics on the ℤ/N character group) — it is
unblocked and independent. Then #6452 (group iso + assembly) once #6450 (this PR)
and #6451 land.

## Blockers

None. #6452 is intentionally blocked on #6450 (this PR) and #6451.
