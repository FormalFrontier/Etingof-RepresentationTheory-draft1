## Accomplished
- Claimed #6103 (Problem 8.2.6(i): Tor₀ ≅ M⊗_A N and Ext⁰ ≅ Hom_A(M,N)).
- Proved the **Ext⁰ half** sorry-free: `Etingof.Problem_8_2_6_i_ext` is now
  `⟨CategoryTheory.Abelian.Ext.addEquiv₀⟩`. `#print axioms` shows only
  propext / Classical.choice / Quot.sound. Builds clean.
- Split off the **Tor₀ half** as follow-up #6109 (blocked on #6103): it needs
  `PreservesFiniteColimits (tensorRightFunctor A N)` (right-exactness of the custom
  `tensorOver` quotient), then `Functor.leftDerivedZeroIsoSelf`. No such right-exactness
  / tensor-hom-adjunction infra exists in the codebase yet, so it is a standalone construction.

## Current frontier
`EtingofRepresentationTheory/Chapter8/Problem8_2_6.lean`: part (i) Ext half done; Tor half
(`Problem_8_2_6_i_tor`, ~line 165) still `sorry`. Parts (ii)–(v) untouched (out of scope).

## Overall project progress
Stage 3 formalization ongoing. This lands the Ext⁰ = Hom base case, one of the two
doc-flagged highest-leverage homological identifications used across Chapters 8–9.

## Next step
Execute #6109: build the tensor–hom adjunction (right A-module on `N →+ B`, adjoint functor,
natural hom-equivalence) to get `PreservesFiniteColimits (tensorRightFunctor A N)`, then
`leftDerivedZeroIsoSelf` gives the Tor₀ iso as a one-liner. Full plan is in #6109.

## Blockers
None. Tor half is deferred to #6109 by design (needs its own construction), not blocked.
