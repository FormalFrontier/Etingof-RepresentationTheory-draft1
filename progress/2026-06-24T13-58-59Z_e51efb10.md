## Accomplished
One-shot dispatch on #5137 (feat(Ch5): formalize prose-claim gaps). The issue bundled five
independent prose-claim gaps of varying difficulty (E / M×2 / M / M / H).

- Landed the **[E]** item **Remark 5.8.2**: `Ind_H^G V ≅ Hom_H(k[G], V)`.
  New file `EtingofRepresentationTheory/Chapter5/Remark5_8_2.lean`, imported into
  `Chapter5.lean`. The iso is `Rep.indCoindIso A ≪≫ Rep.coindIso H.subtype A`, identifying
  `Rep.coind' H.subtype A` (= `H`-equivariant maps `k[G] → V`, Mathlib's `coind'`) with
  Etingof's `Hom_H(k[G], V)`. Sorry-free, type-checks under `lake env lean`.
- Decomposed the remaining four items into self-contained sub-issues:
  - #5169 — Discussion_5.25.4: count `½q(q−1)` complementary-series reps (pure character-group
    orbit count: involution `x ↦ q·x` on `ZMod (q²−1)`).
  - #5170 — Discussion_5.11_examples: `S₃` induced decompositions from `Z₂`, `Z₃` (Frobenius
    multiplicities; needs an S₃ irrep catalogue). `S₄`-from-`S₃` left out of scope.
  - #5171 — Introduction_5.14: `U_λ = Ind_{P_λ}^{S_n} ℂ ≅ ℂ[S_n]·a_λ` (`k[G]⊗_{k[H]}k ≅ k[G]·a_λ`).
  - #5172 — Discussion_semidirect_products: x-independence iso `V_(O,x,U) ≅ V_(O,y,g(U))` on the
    `inducedRepV` little-group construction (the [H] item).
- Left `Decomposed into #5169, #5170, #5171, #5172` breadcrumb on #5137.

## Current frontier
Partial PR for #5137 carrying Remark 5.8.2 only. Parent marked `replan` via `--partial`.

## Overall project progress
Stage 3 formalization, Chapter 5 coverage-audit cleanup. One of five prose-claim gaps closed;
four sub-issues queued for future workers.

## Next step
A planner closes #5137 (sub-issues cover the residual scope) with a forward link. Workers pick up
#5169 (cleanest, pure number theory) → #5170 → #5171 → #5172 (hardest).

## Blockers
None. Each sub-issue is independent and unblocked.
