## Accomplished

Continued the atomic Mathlib v4.31.0 pin-bump migration (#2841) on branch
`agent/64f2e40f-bump`. A fresh full-project build in an isolated `/tmp` clone
inventoried **130 errors across 22 modules** (the branch tip's actual count, up
from the ~120 estimate as frontier fixes surface new failures). Fixed and
individually `lake build`-verified **6 leaf modules** (15 errors cleared),
committed and pushed one per commit:

- **Chapter4/Theorem4_5_4** (1) — dropped a redundant trailing `group` that ran
  on an already-equal goal (`x * h = x * h` after `rw [hx]`); v4.31.0's `group`
  errors "made no progress" instead of closing it.
- **Chapter4/Theorem4_2_1** (1) — `simp [h]` left `(0 : MonoidAlgebra) g⁻¹ = 0`
  undischarged; `rw [h]; rfl` closes it definitionally.
- **Chapter9/KoszulHelpers** (2) — `PolynomialModule R N` now carries its own
  derived `FunLike` (distinct coercion head from `Finsupp.instFunLike`, still
  defeq). `Finsupp.sub_apply` no longer matches the `(X•f - map f) n` term:
  transport via a local pointwise-sub lemma proved by `exact Finsupp.sub_apply`.
  The top-coefficient contradiction mixed PolynomialModule-coe (`hshift`) with
  Finsupp-coe (`mem_support_iff`); state the vanishing in PolynomialModule coe
  and bridge with `exact` (defeq) instead of `rw` (syntactic).
- **Chapter5/FRTHelpers** (4) — four `simp at this; omega` closers hit "simp
  made no progress" (the `congr_arg Prod.fst/snd` hyp is already omega-ready);
  guard with `(try simp at this); omega`. Pre-existing sorries unchanged.
- **Chapter6/Example6_3_1** (4) — `Submodule.linearProjOfIsCompl` & friends are
  now deprecated aliases for the `projectionOnto` API, *as distinct constants*,
  so `ker_projectionOnto` could not rewrite the `let`-bound projections (body
  head `linearProjOfIsCompl`). Switched the `let`s + ker/apply_left/
  projection_add references to the new `projectionOnto` names.
- **Chapter5/Theorem5_18_2** (3) — `LieRing.ofAssociativeRing` is `local
  instance` only upstream now; re-enabled with `attribute [local instance 100]`
  so `Module.End k V` is a Lie ring for the `→ₗ⁅k⁆` action (the downstream
  rcases failure was a cascade of the same missing instance; the global
  `LieAlgebra.ofAssociativeAlgebra` resolves once LieRing is in scope).

## Current frontier

Error count reduced **130 → ~115 across 16 remaining modules** on the branch.
All 6 fixes target modules NOT touched by the (now 34) pending `main` commits,
so they survive a future rebase. Remaining failing modules and the obstruction
seen, roughly easiest-last:

- **Theorem9_2_1** (1) — single deep unsolved goal in a ~2400-line proof.
- **Definition6_6_3** (2) — `rw` "motive is not type correct" over a
  `Decidable.rec`/`ArrowsInto` dependent family; needs a non-`rw` route.
- **BasicAlgebraExistence** (4, Infra) — Morita-lift well-definedness: `rw`
  `?f (?x+?y)` (map_add) failures plus a confusing `he.left`/`IsFullIdempotent`
  elaboration cascade under `instances` transparency. Deep.
- **Theorem3_10_2** (4) — `set W₀ := V₀ →ₗ[A] M` blocks instance synthesis of
  `Module k ↥S` / `IsScalarTower k B W₀` (custom `homIsScalarTowerKB` registered
  under head `LinearMap`, not the let-var W₀). A `haveI : IsScalarTower k B W₀`
  re-exposure did NOT fix it (attempt 1) — synthesis of the `Submodule.module'`
  chain still fails. Likely needs avoiding the `set` abbreviation entirely, or
  providing `Module k ↥S` directly at each `restrictScalars k` site.
- **Theorem5_4_4** (4) — mixed: unsolved goals + `IsScalarTower ℤ ℤ
  (MonoidAlgebra ℤ G)` synth fail; several distinct roots.
- **Proposition5_21_1** (4) — varied MvPolynomial `coeff`/`smul`/`monomial`
  rewrite-pattern mismatches; no single root.
- **ShapiroLemma** (4) — adjunction-naturality `rw` (`F` vs `adj.leftAdjoint`
  syntactic mismatch) + "Function expected at" + an Equiv-coe motive issue.
- Also: **DynkinTypes** (22 — mostly `split_ifs` "no ite to split" because the
  preceding `simp only` now fully resolves the decidable condition; mixed with
  omega/linarith/dsimp failures), **Problem6_9_1** (10), **Theorem5_25_2** (9 —
  `complementWProjection` def body fails, cascading "unknown"), **Lemma5_13_1**
  (7), **Theorem9_6_4** (6), **GL2CharacterValues** (6), **Theorem5_27_1** (4),
  **Sl2Irrep** (4 — TOUCHED by main, defer to post-rebase), **Lemma4_10_3** (1 —
  TOUCHED by main).

## Overall project progress

Stage 3 formalization ongoing. The v4.31.0 pin bump (#2841) is an atomic
migration: it cannot merge until the whole project builds green, so it
accumulates on `agent/64f2e40f-bump` with NO PR until done. The branch is now
~34 commits behind `main` (and 13+ ahead). A rebase onto `main` is advisable
before the final green push but will need conflict resolution; deferred to keep
this session's fixes isolated and rebase-safe.

## Next step

Continue fixing leaf modules on `agent/64f2e40f-bump` in an **isolated /tmp
clone** (the pod worktree shares `.lake/packages/mathlib` by symlink and cannot
hold v4.31.0 while sibling agents build v4.28.1). The clone `/tmp/etingof-bump`
is set up (toolchain + cache at v4.31.0; full inventory in
`/tmp/bump-build-full.log`). Recurring v4.31.0 fix patterns now confirmed:
`linearProjOfIsCompl`→`projectionOnto`; `LieRing.ofAssociativeRing` needs
`attribute [local instance 100]`; PolynomialModule's own FunLike breaks
`Finsupp.*_apply` rw (transport via local lemma / `exact` for defeq coe);
`set`-bound abbreviations block instance synthesis and `rw` keyed matching;
guard now-no-op `simp` with `try`. Prefer modules NOT touched by the pending
main commits. When close to green, rebase onto main and re-verify.

## Blockers

None for the migration grind itself (mechanical, just large and varied). The
atomic nature means no incremental PR is possible — value is delivered as
fully-green modules committed to the branch.
