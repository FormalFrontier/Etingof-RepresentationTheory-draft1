## Accomplished

`/work` → `/feature` session `ad630be2`. Claimed **#4934** (the residual research
core of #4904: the right-`GL_N` multiplicity-one Cauchy decomposition of
`A = k[Xᵢⱼ]`). Landed the **precise `formalCharacter`-form interface** plus a
sorry-free finite-dimensional building block, in a new file
`Chapter5/CauchyCharacterRight.lean`:

- **`finiteDimensional_homogeneousSubmodule`** (sorry-free) — the degree-`d`
  homogeneous component `A_d = homogeneousSubmodule (Fin N × Fin N) k d` is
  finite-dimensional: it sits inside `restrictTotalDegree (Fin N × Fin N) k d`
  (a degree-`d` homogeneous polynomial has total degree `≤ d`), which is finite
  for finitely many variables (`Submodule.finiteDimensional_of_le`).
- **`polyRightDegreeFDRep k N d : FDRep k (GL (Fin N) k)`** (sorry-free *def*) —
  `A_d` as a genuine finite-dimensional representation under right translation,
  built from `polyRightHomogeneousSubrep` (`PolyRightGrading.lean`) via
  `Subrepresentation.toRepresentation` + `FDRep.of`. This is the finite-dim
  object on which `formalCharacter` is defined.
- **`polyRightDegreeFDRep_formalCharacter`** (the lone `sorry`) — the right-`GL_N`
  Cauchy multiplicity-one identity
  `formalCharacter k N (polyRightDegreeFDRep k N d) = ∑_{ν : BoundedPartition N d} schurPoly N ν.parts`.
  Indexed by `BoundedPartition N d` (Fintype: antitone `ν : Fin N → ℕ` with
  `∑ ν = d`), so each dominant `ν ∈ ℕ^N` of size `d` appears **exactly once** —
  multiplicity one. This is the genuine research epic (multiplicity-one Cauchy +
  highest-weight ⟺ `ν ∈ ℕ^N`), overlapping #4699/#4882.

Wired into `Chapter5.lean`; full `EtingofRepresentationTheory.Chapter5` builds,
new file's only `sorry` is the one research identity above.

### Key facts discovered

- `BoundedPartition N d` (`Proposition5_21_1.lean`) already has `Fintype` +
  `DecidableEq` — the clean indexing set for "dominant `ν ∈ ℕ^N` of size `d`",
  letting the multiplicity-one identity be a single `Finset.sum` with no ad-hoc
  partition bookkeeping.
- The per-degree SES (`detSubmodule_inf_homogeneous`, `PolyRightGrading.lean`)
  and det-shift (`detShiftLinearEquiv_intertwine`, `DetShiftIso.lean`) — item 2
  of #4904 — are already complete and sorry-free. The remaining gap is purely
  item 3 (this character identity) + the assembly into the consumer.
- `MvPolynomial.homogeneousSubmodule` / `restrictTotalDegree` /
  `mem_restrictTotalDegree` did not resolve through `open MvPolynomial` in the
  instance signature (relaxedAutoImplicit false); fully qualify them.

## Current frontier

`Chapter5/CauchyCharacterRight.lean` provides the compiling interface. Two
threads remain:
1. **Prove `polyRightDegreeFDRep_formalCharacter`** — the multiplicity-one Cauchy
   character identity (research core of #4934; coordinate with #4699/#4882).
2. **Assemble the consumer** `quotDetRep_irreducible_constituent_lastWeight_zero`
   (`CauchyDetQuotient.lean`) from this identity + the SES + det-shift — now a
   concretely-scoped task; recorded on **#4905** (I closed a near-duplicate #4942
   I had filed, folding its refinement into #4905's thread).

## Overall project progress

Etingof draft 1, Stage 3, Ch5 Schur-Weyl / kernel-lemma-K′ backbone. The Cauchy
decomposition (#4904) now has: the `GL×GL` bi-action (prior session), the
sorry-free per-degree SES + det-shift, and (this session) the finite-dim
`FDRep` of each degree component plus the precise multiplicity-one character
interface. The genuine representation-theoretic identity remains the single open
research `sorry`, feeding `#4905 → #4896 part-(a) → #4890` det⁻¹ elimination.

## Next step

- **Worker:** pick up **#4905** (consumer assembly) — it now consumes the landed
  `polyRightDegreeFDRep_formalCharacter` interface and the sorry-free SES; this
  is more tractable than the research identity.
- **Worker/research:** the multiplicity-one identity `polyRightDegreeFDRep_formalCharacter`
  itself (residual of #4934) — coordinate with the highest-weight uniqueness
  thread #4699/#4882 to avoid duplicating the `ν ∈ ℕ^N ⟺ weights ≥ 0` theory.
- **Planner:** triage #4934 — the interface + finite-dim FDRep landed (partial);
  residual scope is exactly proving `polyRightDegreeFDRep_formalCharacter`.

## Blockers

- The research identity `polyRightDegreeFDRep_formalCharacter` is genuinely
  multi-session (multiplicity-one Cauchy + highest-weight theory). Not blocking
  any other queue item — the interface is consumable as a sorry'd dependency.
