## Accomplished

Worked issue #5078 (DetInvElim-clean constituent-character extractor, B1). Landed the
**additivity foundation** and decomposed the remaining (genuinely multi-lemma) work.

- Created `EtingofRepresentationTheory/Chapter5/CleanFormalCharacterAdditivity.lean`
  (namespace `Etingof.CleanCharExtraction`), a DetInvElim-CLEAN relocation of the
  formal-character additivity engine the clean extractor is built on:
  `mem_glWeightSpace_iff`, `glWeightSpace_map_le`, `glWeightSpace_inf_range`,
  `formalCharacter_add_of_shortExact` (out of polluted `CauchyDetQuotientDegree.lean`) and
  `glWeightSpace_iSup_eq_top_of_equivariant_surjective` (out of polluted
  `SchurWeylFormalCharacterIso.lean`). Builds sorry-free, no warnings; verified clean by
  transitive-import trace (28 imports, depends only on `FormalCharacterIso`). Registered in
  `Chapter5.lean`.
- This resolves obstacle 2(a) of #5078 (additivity stranded in a polluted file).

### Key findings (corrections to earlier assumptions)

- An Explore agent **mis-reported** `FormalCharacterTorusTrace.lean` as importing
  `DetInvElim`. Verified by a real transitive-import trace: it is CLEAN. The actually-polluted
  files relevant here are `CauchyDetQuotientDegree.lean` and `SchurWeylFormalCharacterIso.lean`
  — which is exactly why the additivity engine and the quotient-spanning lemma had to be
  relocated. Always run the import trace, do not trust an agent's pollution claim.
- `IsAlgebraicRepresentation` (`Definition5_23_1.lean:43`) is defined over
  `GLCoordVars n = (Fin n × Fin n) ⊕ Unit` — the `Unit` is the inverse-det slot. So algebraic
  reps may carry det⁻¹ and are **not** automatically ℕ-weight-spanning (the retired #4969
  `det⁻¹ ⊗ Sym³` counterexample is genuinely algebraic). `h_span` must stay a hypothesis;
  the sub-spanning helper is about invariant subs of an *already-spanning* rep, not algebraic
  reps in general.

### Decomposition

- #5081 — clean induction helpers: (1) torus-invariant sub of an ℕ-weight-spanning rep is
  ℕ-weight-spanning (Vandermonde / distinct-character separation; the technical crux),
  (2) quotient of an algebraic rep by an invariant sub is algebraic (adapted basis),
  (3) quotient-FDRep + SES glue.
- #5082 — the extractor `clean_simple_constituent_formalCharacter_eq_schurPoly_mem` itself
  (finrank induction `char M = ∑ factor chars` via the landed additivity lemma + #5081 helpers,
  then character-independence matching). `depends-on #5081`.
- Breadcrumb `Decomposed into #5081, #5082` left on #5078; landing as a `--partial` PR.

## Current frontier

Additivity foundation landed and clean. The two induction helpers (#5081) are unstarted;
the extractor (#5082) is blocked on #5081.

## Overall project progress

Stage 3 (formalization) ongoing in Chapter 5; the #4896/#4905 det-quotient highest-weight
assembly. The clean extractor now has a clean additivity engine to build on; the route is
fully specified (composition series + character additivity, NO semisimple direct sum).

## Next step

Pick up **#5081** (clean induction helpers). Deliverable (1), the sub-spanning lemma, is the
hard part: `M = ⊕_μ M_μ` (use `glWeightSpace_iSupIndep` + `h_span` for the internal direct
sum) and a torus-invariant subspace of a sum of distinct-character eigenspaces is the direct
sum of its weight intersections (distinct-character separation over the infinite field).
Deliverable (2) is an adapted-basis argument. Check Mathlib's `Subrepresentation` /
`Representation.Quotient` API before hand-building deliverable (3).

## Blockers

None. #5082 is intentionally `blocked` on #5081 (dependency declared).
