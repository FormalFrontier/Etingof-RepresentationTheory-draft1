## Accomplished

Continued work on Proposition 6.6.6 source case (issue #1724):

1. **Eliminated `hdim` sorry** — replaced `linearEquivOfInjective`/`ofBijective` construction with explicit `LinearEquiv` via `Function.surjInv`. Proved surjectivity of `f` by constructing the Phi transport map (componentwise `equivAt_ne` reindexed from `ArrowsInto Q̄ᵢ i` to `ArrowsOutOf Q i`), establishing `sinkMap = mkQ ∘ Phi` and `Phi ∘ f_ds = sourceMap`, then using `mkQ_ker`/`mkQ_surjective` helper theorems.

2. **Added 3 helper theorems**:
   - `sigma_out_eq_arrowReindexEquivSource_symm`: sigma pair reindexing identity
   - `reflFunctorMinus_mkQ_ker`: mkQ kernel equals range(sourceMap)
   - `reflFunctorMinus_mkQ_surjective`: mkQ is surjective (quotient projection)

3. **Advanced source naturality proof** (`equivAt_eq_source_naturality`): Used `symm_apply_apply` technique — save the identity BEFORE unfolding, then revert/unfold/match/intro to get `h_sym : codRestrict(f_ds)(surjInv(x)) = x`. Applied `congr_arg Subtype.val` + `dsimp [LinearMap.codRestrict]` to get `h_val : f_ds(surjInv(x)) = ↑x`, then `rw [← h_val]` + `generalize` to reduce goal to component extraction.

4. **Build compiles cleanly** with exactly 1 sorry (down from 2 at start of issue).

## Current frontier

`Proposition6_6_6.lean` line 1563: 1 sorry in `equivAt_eq_source_naturality`.

| Sorry | Location | Blocker |
|-------|----------|---------|
| source naturality component extraction | line 1563 | After `generalize`, goal is `equivAt_ne(component_b(f_ds(v))) = mapLinear(e)(v)`. Quiver instance pollution inside `isTrue` match branch prevents stating helper lemma — `.obj b.fst` synthesizes `inst` instead of `reversedAtVertex Q i`. |

## Overall project progress

- Proposition 6.6.6 sink case: **COMPLETE** (0 sorrys)
- Proposition 6.6.6 source case: **1 localized sorry** (down from 2 at issue creation)
  - `equivAt_eq_source` definition: COMPLETE (0 sorrys) — surjInv-based construction works
  - `equivAt_eq_source_naturality`: 1 sorry — component extraction blocked by instance pollution
  - All 3 arrow cases structured (b=i vacuous, a=i/b≠i sorry'd, a≠i/b≠i proved)

## Next step

The remaining sorry requires extracting components from a direct sum inside a `match inst_dec i i | .isTrue _` branch. Two approaches:

1. **Factor out component extraction as a top-level lemma** stated entirely with `@`-explicit instances to avoid synthesis. The lemma would state: `∀ v, component_b(f_ds(v)) = equivAt_ne⁻¹(mapLinear(arrow_b)(v))`.

2. **Restructure to avoid the match branch entirely**: Define `equivAt_eq_source_naturality` using the same Phi-transport approach used for `f_surj`, where all instance-sensitive operations happen outside the match.

## Blockers

- **Quiver instance pollution** (fundamental): Inside `match inst_dec i i | .isTrue _`, Lean's instance synthesis picks `inst` instead of `reversedAtVertex Q i` for operations on `ρ_minus.obj b.fst`. All standard approaches (`have`, `let`, explicit `@`) fail because the mismatch is in synthesized implicit arguments deep in the type.
