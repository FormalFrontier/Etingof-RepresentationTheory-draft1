## Accomplished

One-shot dispatch on **#5559** (general-`k`, all-weights simplicity of `algIrrepGLRepρ` as
a `k[GL_n]`-module). **Resolved the simplicity gap sorry-free**, filling the lone `sorry` in
`peterWeylSummandMap_injective`.

The key discovery: the multi-session Sub-A…Sub-D route in
`progress/schurModule-isSimple-general-route.md` is **already landed**. General-`k`,
all-weights Schur-module simplicity `schurModule_isSimple_general`
(`CleanCharacterExtractionBase.lean:94`) is fully proved sorry-free (it rests on
`schurModuleSubmodule_isSimple_centralizer_general`, `SchurModuleSimple.lean:430`, also
sorry-free). It is already transitively imported by `Theorem5_23_2_PeterWeyl.lean` — no new
import, no cycle. So #5559 collapses to a short det-twist transfer.

**`Theorem5_23_2_PeterWeyl.lean`:**
- New reusable `algIrrepGLRepρ_isSimpleModule (n) (k) (lam)`: general-`k`, all-weights
  simplicity of `L_λ = algIrrepGLRepρ n lam k`. Proof: `algIrrepGLRepρ` is the
  `det^{-λ.shift}`-twist of `schurModuleRep k n lam.toNatWeight`; supply Schur-module
  simplicity via `schurModule_isSimple_general`, `unfold algIrrepGLRepρ`, then transfer
  through the det-twist with `isSimpleModule_charTwistRep`. No `∑ λ ≤ n` degree guard.
  **Axiom-clean** (`#print axioms`: only `propext`/`Classical.choice`/`Quot.sound`). This
  lifts the `ℂ`-only `algIrrepGLRep_isSimple` and is the shared simplicity input for #5555,
  #5556, #5549.
- `peterWeylSummandMap_injective`: the `:253` `sorry` is replaced by
  `algIrrepGLRepρ_isSimpleModule n k lam`. Proof body is now sorry-free; docstring updated.
- **Fixed two pre-existing build breaks** (present in `main` at HEAD, masked behind the
  hsimple `sorry`'s hard-timeout in earlier runs): the `add` case of the `key` induction
  needed `Pi.add_apply` and the `hcond` `rw` chain needed `Pi.zero_apply` (simp-normal-form
  drift). With the `sorry` in place the committed file did **not** build (errors at
  273/276); both are now closed.

`lake build EtingofRepresentationTheory.Chapter5.Theorem5_23_2_PeterWeyl` green (exit 0).

## Current frontier

`peterWeylSummandMap_injective` proof body is sorry-free. Note: `#print axioms
peterWeylSummandMap_injective` still reports `sorryAx` via a **pre-existing, out-of-scope**
transitive dependency — `algIrrepGLDualIso` / `peterWeylSummandMap` inherit a def-level
`sorry` from `ContragredientIdentity.lean:164` (the contragredient dual iso). That is
unrelated to #5559's simplicity scope and tracked by the contragredient-identity work.

The file's two remaining explicit `sorry`s are in separate theorems:
`peterWeylSummandMap_range_iSupIndep` (#5556) and `peterWeylMap_surjective` (#5550). Both
can now consume `algIrrepGLRepρ_isSimpleModule`.

## Overall project progress

Stage 3 ongoing. Peter-Weyl injectivity: the shared general-`k` simplicity input (#5559) is
now sorry-free and factored as `algIrrepGLRepρ_isSimpleModule`. `peterWeylSummandMap_injective`
(#5555) body is sorry-free. Remaining in the injectivity half:
`peterWeylSummandMap_range_iSupIndep` (#5556) → `peterWeylMap_injective` (#5549). Surjectivity
half (`peterWeylMap_surjective`, #5550) and the Cauchy `peterWeylMap_bijective` remain.

## Next step

Pick up **#5556** (`peterWeylSummandMap_range_iSupIndep`, cross-summand independence): it now
has `algIrrepGLRepρ_isSimpleModule` available for the per-summand simplicity it needs.
Resolving #5556 unblocks `peterWeylMap_injective` (#5549). Separately, the contragredient
def-level `sorry` (`ContragredientIdentity.lean:164`) feeding `algIrrepGLDualIso` deserves a
dedicated issue if not already tracked — it is the residual transitive `sorryAx` under the
Peter-Weyl injectivity cluster.

## Blockers

None for #5559 — fully resolved. The transitive `sorryAx` under
`peterWeylSummandMap_injective` is a pre-existing contragredient-iso def-level `sorry`, out of
scope for this issue.
