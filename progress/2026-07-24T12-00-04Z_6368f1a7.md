## Accomplished

Worked issue #7673 (finite-dimensional refinement of the truncated projective resolution,
Problem 9.4.2(iii) final clause). Added to
`EtingofRepresentationTheory/Chapter9/Problem9_4_2.lean`:

- `finite_syzygy` — over a left-Noetherian ring `R`, if `M` and every term `P.complex.X n` of a
  `ProjectiveResolution M` are `Module.Finite R`, then every syzygy `syzygy P n` is
  `Module.Finite R`. Proof: induction; `Ω⁰ M = M`; `Ωⁿ⁺¹ M` embeds (via the short-exact
  `(syzygySES P n).f`, which is mono) into the f.g. module `Pₙ`, and a submodule of a f.g.
  module over a Noetherian ring is f.g. (`Module.Finite.of_injective` + the
  `isNoetherian_of_isNoetherianRing_of_finite` instance).
- `finite_truncated_projective_resolution` — packages the length-`d` truncation: `Ω^d M`
  projective, all syzygies f.g., `Ω⁰ M = M`, syzygy SESs. Reuses the existing
  `projective_syzygy_of_hasProjectiveDimensionLE` and the new `finite_syzygy`.
- `finite_of_finiteDimensional_algebra` — `Module.Finite k R` + `Module.Finite R N` (+ scalar
  tower) ⟹ `Module.Finite k N`, i.e. "f.g. over a f.d. algebra ⟹ finite dimensional over `k`"
  (thin wrapper over `Module.Finite.trans`).

`lake build` clean; `#print axioms` shows only `propext, Classical.choice, Quot.sound`; no
`sorry`. Chapter9 aggregate builds.

## Current frontier

Deliverable 2 ("combine with the truncation so the length-`d` truncated resolution has
finite-dimensional terms") is complete, together with the key "syzygies stay f.g." step of
deliverable 1. The *existence* half of deliverable 1 (constructing a resolution with f.g. free
terms over a Noetherian ring) is NOT done — it is a standalone categorical construction (a
finite-free analogue of Mathlib's `ProjectiveResolution.ofComplex`).

## Overall project progress

Stage 3 (formalization) ongoing. Chapter 9 projective-resolution / syzygy line: syzygy SESs
(#7674), d-th syzygy projective (#7677), truncation (#7672), and now finiteness propagation +
finite truncation (#7673, this turn). Remaining: existence of an f.g.-term resolution (#7678).

## Next step

Pick up #7678: construct `∃ P : ProjectiveResolution M, ∀ n, Module.Finite R ↥(P.complex.X n)`
for f.g. `M` over a left-Noetherian ring, using finite free covers
(`Module.Finite.exists_fin'`, `ModuleCat.free`/`freeDesc`) with a reproved exactness step.
Composing it with `finite_truncated_projective_resolution` closes the book claim entirely.

## Blockers

None. #7673 landed partially (PR); existence split into #7678 for a follow-up worker.
