## Accomplished

Issue #2995 — consolidated six-dimension audit of the four wave-63
chore PRs (#2983, #2989, #2993, #2994) that refreshed citations and
docstrings on `FieldGenericNonAdjacentBranches.lean` and
`FieldGenericTpqr.lean`.

Audit report written to
`progress/reviews/2026-05-20-wave-63-chore-citation-refresh.md`.
Verdicts:

| Dim | Verdict |
|---|:-:|
| D1 — `FieldGenericNonAdjacentBranches.lean` Block 1 / Block 2 citations | **PASS** |
| D2 — `FieldGenericTpqr.lean` TODO block citations | **CONCERN** |
| D3 — `FieldGenericNonAdjacentBranches.lean` file docstring | **PASS** |
| D4 — Closure of PR #2984's D3/D5(a)/D5(b)(c) concerns | **PASS** |
| D5 — Build sanity + cross-file consistency | **PASS** |
| D6 — `FieldGenericTpqr.lean` file docstring | **CONCERN** |

Two real findings (both in the Tpqr cleanup, both CONCERN-level):

* **D2 / TODO bullet 4 misattribution.** PR #2994 claims PR #2912
  covers T(1, 2, 2), T(1, 2, 3), and T(1, 2, 4). Verified via
  `gh pr diff 2912` that PR #2912 only landed T(1, 2, 2) = D₅. T(1, 2, 3)
  and T(1, 2, 4) are sub-sub-cases of #2909 (b₂ leaf, r ∈ {3, 4}); the
  symmetric T(1, 3, 2) / T(1, 4, 2) are sub-sub-cases of #2908. All
  four remain open.
* **D6 / stale line number.** PR #2989's file docstring cites the
  residual API stub `single_branch_leaf_case_both_extend_per_kQ` as
  `(line 1233)`; actual line is 1240. PR #2989 was authored against
  a pre-#2994 base and merged 60 seconds after PR #2994 (which added
  a 5-line TODO block) without rebasing the line number.

Follow-up chore issue #3002 opened with both fixes (textual edits,
< 10 lines).

Build sanity at HEAD `711068e`:

* `lake build FieldGenericNonAdjacentBranches` → 1 sorry at line 103 ✓
* `lake build FieldGenericTpqr` → 1 sorry at line 1240 ✓
* `lake build FieldGenericAssembly` → no new warnings ✓

## Current frontier

Audit landed; #3002 ready for the next worker to claim (tiny chore).
PR #2984's three concerns (D3 enumeration mismatch, D5(a)
cross-reference, D5(b)/(c) docstring staleness on
`FieldGenericNonAdjacentBranches.lean`) are confirmed resolved by the
wave-63 chore PRs. One minor sub-concern from PR #2984's D3 — the
redundant `let _ := arm₂_ne_chain` at `FieldGenericNonAdjacentBranches.lean:1118`
— remains; explicitly out of the chore PRs' scope, noted in the audit
report for future cleanup.

## Overall project progress

Stage 3 ongoing. Wave-63 cleanup cascade audited. No new sorries
introduced; the existing residual sorries at the two documented sites
(NonAdjacentBranches:103, Tpqr:1240) are unchanged.

## Next step

A planner can pick up #3002 and route it to a `feature` worker, or
collapse the two textual edits inline into a fresh planner cycle. The
underlying open sub-issues for the residual sorries remain
#2974/#2976/#2977/#2978 (blocked on #2955) and
#2907/#2908/#2909 (under #2905, with #2910 landed via PR #2912).

## Blockers

None.
