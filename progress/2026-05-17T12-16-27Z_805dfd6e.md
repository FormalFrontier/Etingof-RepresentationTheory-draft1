# Session 805dfd6e — orientation-generic K_{1,4} construction (#2800)

## Accomplished

- Claimed #2797 (orientation-generic K_{1,4}) and assessed scope: the
  orientation-generic indecomposability proof is too large for a single
  session. Canonical orientation alone took ~230 lines (#2796); the
  16-orientation case-split or BGP-reflection-chain approach is expected
  to require 400-600 more lines.
- Decomposed #2797 into two sub-issues:
  - #2800 (this session): direction-aware leaf projections + the
    `starRep_kQ` representation + dim-vector lemma.
  - #2801: indecomposability proof + `star_not_finite_type_per_kQ`
    final theorem (depends on #2800).
- Skipped #2797 with the `Decomposed into #2800, #2801` breadcrumb.
- Claimed and executed #2800. New declarations in
  `EtingofRepresentationTheory/Chapter6/FieldGenericInfiniteType.lean`:
  - Helpers `starFirst_F`, `starSecond_F` (the two half-projections).
  - Four direction-aware projections `starProj1_F`, `starProj2_F`,
    `starProj3_F`, `starProj4_F` with pairwise distinct kernels (each
    kernel equals the image of one of the four leaf embeddings).
  - Left-inverse identities `starProj_i_F_starEmbed_i_F` for i ∈ {1, 2, 3, 4}.
  - `starRep_kQ` parameterised by an orientation `Q` of `starAdj`.
  - `starRep_kQ_dimVec` confirming the dim vector
    `(2(m+1), m+1, m+1, m+1, m+1)`.
- Build green: `lake build EtingofRepresentationTheory.Chapter6.FieldGenericInfiniteType`.
  Zero new sorries.

## Current frontier

`starRep_kQ` is well-defined and well-typed, but its indecomposability
is not yet proved. Until that proof lands (in #2801), the orientation-
generic K_{1,4} case cannot be plugged into the per-(k, Q) assembly in
#2774.

## Overall project progress

Stage 3 (Lean formalization) in flight. Chapter 6's Gabriel-forward-bridge
(#2401) needs six per-(k, Q) infinite-type theorems:
- cycle: **landed** (#2787).
- canonical K_{1,4}: **landed** (#2796).
- orientation-generic K_{1,4}: construction landed here; indecomposability
  is the remaining work (#2801).
- D̃₅: open (#2790).
- Ẽ₆, Ẽ₇, T(1,2,5): three open sub-issues (#2791-#2793) with
  unfixed-ℂ-prerequisite blockers flagged.

## Next step

1. After this PR merges, a worker can claim #2801 to prove
   `starRep_kQ_isIndecomposable` and `star_not_finite_type_per_kQ`.
   The kernel choices in this PR (each projection's kernel = image of
   another embedding) were picked specifically to support that proof —
   the four leaves see four distinct subspaces of the centre.
2. In parallel, a worker can claim #2790 (D̃₅) which has a working
   ℂ-specific source.

## Blockers

None for the work in this PR. #2801 (indecomposability) is now
unblocked once this PR merges. The Ẽ-family / T(1,2,5) sub-issues
(#2791, #2792, #2793) still need planner triage for their unfixed-ℂ
prerequisite, as flagged in session 286c9be9.

## Quality metrics

- Pre-existing sorries in `Chapter6/FieldGenericInfiniteType.lean`: 0
  before, 0 after.
- New lines: ~175 (projections + left-inverse lemmas + rep + dim-vector).
