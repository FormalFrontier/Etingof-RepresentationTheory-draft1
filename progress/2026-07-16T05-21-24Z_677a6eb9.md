## Accomplished

- **Feature #6715** (`feat(Ch6 #Problem6.1.6-c): assemble isCyclic_of_odd_card`): filled the
  `isCyclic_of_odd_card` sorry in `EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean`,
  completing the `su2_finite_cyclic_or_contains_negId` dichotomy.
  - Added a private helper `exists_eigen_character_of_not_simple`: from `¬ Simple (V G)` it
    extracts a `G`-invariant line in `ℂ²` (via `simple_fdRepOf_of_isSimpleModule` +
    `Subrepresentation.ofSubmodule'` + finrank counting), reads off the eigenvalue character
    `χ : G →* ℂˣ`, and proves `χ_V(g) = χ(g) + χ(g)⁻¹` from `det = 1` on `SU(2)` (the 2×2
    characteristic-polynomial identity, mirroring the existing `finrank_invariants_V_eq_zero`).
  - `isCyclic_of_odd_card`: `|G|` odd ⇒ a 2-dim irreducible would force `2 ∣ |G|`
    (`finrank_dvd_card_of_irreducible`, #6714), so `V G` is reducible; the eigenvalue character
    is injective by faithfulness (`taut_char_eq_two_imp_one`), so `G` embeds in `ℂˣ` and is
    cyclic (`isCyclic_of_injective_ringHom`).
  - Added `import EtingofRepresentationTheory.Chapter6.DimDvdCard`.
  - `lake build EtingofRepresentationTheory.Chapter6.Problem6_1_6` succeeds. My two declarations
    are locally sorry-free. `#print axioms isCyclic_of_odd_card` shows
    `[propext, sorryAx, Classical.choice, Quot.sound]`; the `sorryAx` is **entirely** the
    transitive dependency `finrank_dvd_card_of_irreducible` (#6714), which is still `sorry` on
    `main`. This is the accepted "sorry'd dependency" situation and matches #6715's own framing
    ("fills the sorry given the general theorem from #6714"). The axiom footprint becomes clean
    automatically once #6714 lands sorry-free.

- **Housekeeping on #6657** (Künneth `Tor` assembler): while first attempting it, discovered it was
  surfaced prematurely — its three `depends-on` parents (#6666/#6667/#6668) were closed by *further
  decomposition* into still-open grandchildren, and the two def-level pieces the assembler needs are
  **missing on `main`** (no `extTensorProjectiveResolution` object / no `ExternalTensorResolution.lean`
  and no PR in flight; no chain-level rearrangement iso). Re-blocked #6657 on the real open leaves
  #6729 and #6727 (`coordination add-dep`) and skipped it back with a detailed explanation. Recommended
  a planner re-verify the whole 8.2.8-Tor subtree before re-surfacing.

## Current frontier

`isCyclic_of_odd_card` is complete. The remaining sorry in `Problem6_1_6.lean` (line ~1409,
`mckayAdj_no_selfLoop`) is a separate, out-of-scope item.

## Overall project progress

Phase 3 formalization ongoing. Chapter 6 Problem 6.1.6 part (c) dichotomy is now sorry-free (modulo
the #6714 dependency). Chapter 8 Künneth `Tor` (#6657) infrastructure is still mid-flight.

## Next step

- Land #6714 (`finrank_dvd_card_of_irreducible`) sorry-free to clean up the axiom footprint of the
  6.1.6 dichotomy.
- For #6657: a planner should re-verify #6729/#6727 and their dependencies before the assembler is
  re-queued.

## Blockers

None for #6715 (PR created). #6657 is legitimately blocked (missing def-level infrastructure).
