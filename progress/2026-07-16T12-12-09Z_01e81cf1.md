## Accomplished

Feature session on #6816 (Ch8 #Problem8.2.8-Ext: f.g.-projective Hom-tensor iso).
Created `EtingofRepresentationTheory/Chapter8/HomTensorFGProjective.lean` (registered in
`Chapter8.lean`) with the full construction and all but one proof:

- `extRep` / `extModule` / `extModule_smul_tmul` / `extModule_isScalarTower`: the canonical
  **left** external `A₁ ⊗ₖ A₂`-module on `X₁ ⊗ₖ X₂` (left-module twin of the existing
  `Etingof.extTensorModule`), pinned on simple tensors and shown to be a `k`-scalar tower.
- `lcompₖ`: precomposition `φ ↦ φ ∘ₗ s` as a `k`-linear operator on Hom-spaces (used for the
  retract maps).
- `extMap`: `TensorProduct.map f₁ f₂` promoted to an `A₁ ⊗ₖ A₂`-linear map; its linearity is
  forced by the two pinning hypotheses (proved by tensor induction). Sorry-free.
- `homTensorHom`: the `k`-linear comparison map `f ⊗ g ↦ (p₁ ⊗ p₂ ↦ f p₁ ⊗ g p₂)`, with
  `homTensorHom_tmul_tmul`. Sorry-free.
- `homTensorHom_bijective`: proved **sorry-free modulo the free base case** via the
  retract-of-an-isomorphism argument — naturality of `homTensorHom` in the source (`NAT1`, `NAT2`),
  a f.g. projective module is a retract of a finite free one
  (`Module.Finite.exists_comp_eq_id_of_projective`), and `S ∘ R = id`, `t_r ∘ₗ t_s = id`.
- `homTensorHomEquiv`: the packaged `≃ₗ[k]`.

`lake build EtingofRepresentationTheory.Chapter8.HomTensorFGProjective` succeeds with exactly one
`sorry` (the free base case) and no errors.

## Current frontier

The single remaining `sorry` is `homTensorHom_bijective_free` (the base case: source modules are the
finite free `Fin n → A₁`, `Fin m → A₂`). Everything else — construction, `k`-bilinearity, the
`A₁ ⊗ A₂`-linearity, and the projective→free reduction — is done.

## Overall project progress

Stage 3 formalization. This is a sub-issue of #6814 (`Problem_8_2_8_ext`, proof still `sorry`). The
Hom-tensor comparison iso is now a real construction usable by the eventual Ext Künneth assembly,
modulo the free base case (scoped as a new sub-issue).

## Next step

Prove `homTensorHom_bijective_free`. Suggested route (see the new sub-issue): identify
`Hom_{Aᵢ}(Aᵢ^{nᵢ}, Nᵢ) ≅ Nᵢ^{nᵢ}` via `(Pi.basisFun Aᵢ _).constr k` (k-linear, needs
`SMulCommClass Aᵢ k Nᵢ`), build a `Fin n × Fin m` basis of `(Fin n → A₁) ⊗ₖ (Fin m → A₂)` over
`A₁ ⊗ₖ A₂` for the external module (the crux sub-step; `Module.Basis.tensorProduct` does not directly
apply because neither factor is an `A₁ ⊗ A₂`-module), and check `homTensorHom` matches the index
bijection composed with `(Fin n → N₁) ⊗ₖ (Fin m → N₂) ≅ (Fin n × Fin m → N₁ ⊗ₖ N₂)`.

## Blockers

None. The free base case is isolated and self-contained; the rest of #6816 is complete.
