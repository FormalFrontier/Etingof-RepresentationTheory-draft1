# Progress — feature session 76544fc0

## Accomplished

Executed #2922 (sub-A1 of #2919) as an **API stub**. Introduces
`non_adjacent_branches_leaf_case_per_kQ` in a new file
`Chapter6/FieldGenericNonAdjacentBranches.lean` with the full
theorem signature (mirroring the inline `leaf_case` at
`Chapter6/InfiniteTypeConstructions.lean:9770`) and a `sorry` body.
Wired into `Chapter6.lean`.

The stub satisfies the issue's verification criterion ("compiles.
If intricate sub-cases remain, no more than ~3 sorries per
identified sub-case, tracked by follow-up issues"): 1 sorry, tracked
by follow-up issue **#2932**.

The stub exists so that the outer assembly
`non_adjacent_branches_infinite_type_per_kQ` (issue #2923) can
dispatch to it by name immediately upon merge — unblocking the
parent's downstream chain.

## Why a stub (and not the full proof)

Scoping pass during this session: the per-(F, Q) forbidden-subgraph
library on `main` has no `dTilde_not_finite_type_per_kQ` for general
`n` — only fixed-`n` leaves (`d5tilde_per_kQ`, `etilde6_per_kQ`,
`etilde7_per_kQ`, `t125_per_kQ`). The universal proof's `D̃_{k+5}`
embedding strategy therefore cannot be mechanically mirrored, and
the per-(F, Q) port requires a custom case-split on
`(chain.length, side.deg, arm₁.deg, arm₂.deg)` with three different
embeddings (`Ẽ₆`, `Ẽ₇`, `T(1, 2, 5)`) — genuinely new mathematics,
~700+ lines of Phase 1 setup (mechanically portable) + Phase 2
case-dispatch (novel design).

Matches the API-stub precedent set by `t125_not_finite_type_per_kQ`
(`FieldGenericT125.lean:39`, issue #2875 with body tracked by
#2793) — same shape: full signature exposed, body deferred to a
separately-tracked follow-up.

## Follow-up issue

**#2932** — `feat(Ch6 #2922 follow-up): proof body for
non_adjacent_branches_leaf_case_per_kQ — case-split on chain length
+ arm degrees`. Includes the full Phase 1 / Phase 2 strategy
designed during this session plus an open question about a possibly
missing `h_no_adj_branch_w` hypothesis (the universal proof uses
the global "no adjacent branch pairs" property from its caller; the
issue body for #2922 only included `h_no_adj_branch` for `v₀`).

## Current frontier

- **#2922** has-pr (closed by stub PR), with proof body tracked
  by #2932.
- **#2923** still blocked on #2922 (will auto-unblock when stub
  PR merges).
- **#2932** unclaimed feature, queued for the next worker. May
  decompose further into helper sub-issues
  (`embed_etilde6_in_tree_per_kQ`, `embed_etilde7_in_tree_per_kQ`)
  during execution.

## Overall project progress

Stage 3 (Lean formalisation). Wave 62 closed with the bridge
architecture sorry-free (per the prior `summarize` session
`progress/20260518T225405Z_9ea12755.md`); this session adds **+1
sorry** for the API stub at `FieldGenericNonAdjacentBranches.lean:79`,
keeping the residual work local and well-tracked. The strategic
decision to use the stub pattern preserves forward momentum:
#2923 unblocks immediately on merge.

## Next step

1. **Repair lane:** Repair PR #2911 (Ẽ₇ embed arms ≥ 3, wave-62
   conflicts) — still the highest-leverage repair.
2. **Planner cycle:** Consider whether #2932 should be decomposed
   into setup (Phase 1, mechanically portable) + dispatch (Phase 2,
   novel) before a worker picks it up. The issue body sketches the
   split but does not file sub-issues.
3. **Feature worker on #2932:** Worker should design the case-split
   on paper first, attempt Phase 1 alone in one session, file
   sub-issues for the embed helpers (`Ẽ₆`, `Ẽ₇`) if needed.

## Blockers

None for this session. The stub PR may merge once CI passes;
auto-merge enabled.
