## Accomplished

Planner cycle `6c37165b`. Created 2 new atomic chore issues to
refill the queue (POD deficit = 2):

* **#2990** — `chore(Ch6): cite sub-issues #2974/#2976/#2977/#2978
  in FieldGenericNonAdjacentBranches.lean residual-sorry comment
  blocks`. Addresses the **D3** and **D5(a)** CONCERNs flagged by
  the wave-63 partial Case E audit
  (`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-cases-e-aa-and-e-s1c4.md`):
  the upstream enumeration at lines 994-1003 and the residual
  sorry-site block at lines 1098-1102 both list 3 bullets while
  the sub-issue decomposition is 4 items wide (the
  `chain.length = 3` case splits along all-leaves / mixed-arm),
  and neither block cites the sub-issue numbers.
* **#2991** — `chore(Ch6): cite sub-issues #2907/#2908/#2909/#2910
  in FieldGenericTpqr.lean TODO block at
  single_branch_leaf_case_both_extend_per_kQ`. Parallel chore for
  the inner TODO comment block at lines 1267-1279 inside the
  body of `single_branch_leaf_case_both_extend_per_kQ`. Issue
  #2986 (the wave-63 docstring refresh for this same file)
  explicitly scoped itself to the file-level docstring (lines
  11-44) and the `single_branch_leaf_case_per_kQ` docstring at
  lines 1300-1305 — the inner TODO block was left untouched.

Also:

* PR #2988 (wave-63 summarize, planner cycle `ffed68b7`) already
  on master at orient time? — actually still IN_PROGRESS CI;
  enabled `gh pr merge --auto --squash --delete-branch` so it
  lands on green.
* No `replan`-labelled issues touched (`/replan` owns those).
* No `directive` issues open (verified via the
  `gh issue list --label directive` query).
* No new audit issues needed: every "feat" PR merged this wave
  (#2966, #2968, #2970, #2979, #2952, #2956, #2958, #2961,
  #2945, #2947, #2943, #2941, #2933) has a closed audit issue
  (#2981, #2980, #2975, #2971, #2959, #2969).

## Current frontier

* **non-adjacent-branches leaf case**: ~95% landed; residual
  sorry at `FieldGenericNonAdjacentBranches.lean:1108` covers
  four configurations decomposed into #2974/#2976/#2977/#2978
  (all `blocked` on #2955).
* **single-branch leaf case (both extend)**: dispatcher sorry at
  `FieldGenericTpqr.lean:1286`; sub-A (#2907) in `/repair`
  (PR #2911 CONFLICTS), sub-B (#2908) and sub-C (#2909) claimed
  (`replan`), sub-D landed via PR #2912. Assembly issue #2905
  remains `replan`.

These two chores (#2990, #2991) do not unblock anything — they
add navigability so the downstream worker that picks up the
sub-issues once #2955 / #2905 resolve can find the four-way
decomposition from the residual `sorry` comment.

## Overall project progress

* Stage 3 / Phase 2 / non-adjacent-branches leaf case: ~95%
  complete; residual decomposed into #2974/#2976/#2977/#2978
  (all blocked).
* Stage 3 / Phase 2 / single-branch leaf case (both extend):
  sub-A in `/repair`, sub-B/C claimed, sub-D landed.
  Dispatcher assembly waits on sub-A/B/C.
* Forbidden-subgraph per-(F, Q) library: D̃₅, D̃₇, Ẽ₆, Ẽ₇,
  T(1, 2, 5). Indecomposability bodies for D̃₅ (#2834 chain) and
  D̃₇ (#2967) remain `replan`-labelled.
* Sorry inventory: 20 across 12 files (wave 62 was 19/11; wave 63
  added 1 with `FieldGenericD7Tilde.lean`).
* Wall 1 (framework decision #2436) — still human-escalated.

## Next step

Same conclusion as the previous planner cycle (`ffed68b7`): the
next planner cycle should pick up after `/replan` unblocks the
Case E sub-residuals (#2974 / #2976 / #2977 / #2978) and the
`single_branch_leaf_case_both_extend` sub-A/B/C chain. If those
remain blocked at the next planner cycle, the planner can still
pre-decompose the assembly issue #2905 once sub-A/B/C land (none
have landed as of this cycle), or open follow-up chores once
PR #2988 (wave-63 summarize) merges to refresh
`progress/sorry-landscape.md` and `progress/items.json` (the
latter is stale since wave 50 / PR #2313).

## Blockers

None for the planner cycle itself. Upstream blockers (#2436
framework decision; the d5tilde Sub-A / Sub-B cascade
#2834 → #2839 → #2850 → #2853 sitting in `replan`; the Case E
sub-issues blocked on #2955) are outside the planner's scope and
tracked by `/replan` / human review.
