## Accomplished

Closed #4824 (Ch6 #4777 S2c-strict): the strict `−1` dimension bound
`dim W(m) < dim G(m)` for nonzero `m`, the genuine remaining geometric crux of
the Gabriel forward direction. New file
`EtingofRepresentationTheory/Chapter6/Problem6_1_5_StrictDimBound.lean`, all
sorry-free, `lake build` green.

Delivered:

- `scalingAlgEquiv` / `scalingFieldEquiv`: the scaling `k`-automorphism of `k[τ]`
  (`X t ↦ λ·X t`) and its lift to `K = FractionRing (k[τ])`, with the action on
  generators `scalingFieldEquiv_g`.
- `Etingof.trdeg_fractionRing_mvPolynomial'`: index-agnostic
  `trdeg k k(τ) = card τ`.
- `card_lt_of_injective_scalingInvariant` (reusable abstract core): an injective
  `k[σ] → K` with scaling-invariant image forces `card σ < card τ`. Proof: the
  distinguished generator `g₀` is transcendental over the scaling-fixed image
  (root-counting under the scaling automorphism over an infinite field —
  infinitely many `λ·g₀` would be roots), so it adjoins preserving algebraic
  independence: `card σ + 1 ≤ trdeg k K = card τ`.
- `card_wIdx_lt_card_gIdx` and `repSpace_finrank_lt_repGroup_ambient_finrank`
  (the two issue deliverables): the orbit comorphism is composed into `K` via the
  localization→fraction-field embedding; Part A shows its image is scaling-invariant
  (`Gⱼ·v₀·Gᵢ⁻¹` entries: the `λ` of `Gⱼ` and `λ⁻¹` of `Gᵢ⁻¹` cancel, `v₀` is
  scalar/fixed); then the abstract lemma fires.

## Current frontier

The strict bound interface is complete and is the directly-consumable `hstrict`
hook for `Etingof.isDynkinDiagram_of_strict_finrank` (`Problem6_1_5_TitsBridge`).

## Overall project progress

Stage 3 formalization, Chapter 6 orbit-counting redirect (directive #4777). With
#4824 closed, S2c (strict bound) is done; the forward direction now has its
strict-positivity input. #4786 (S4 assembly: wire finite⟹q-pos-def into
`Theorem_6_1_5`, delete the retired explicit-construction track) was blocked on
#4824 and is now unblockable.

## Next step

Pick up #4786: feed `repSpace_finrank_lt_repGroup_ambient_finrank` (supplying the
`[Finite (orbitRel.Quotient …)]` instance from the finite-type hypothesis, for all
nonzero `m`) to `isDynkinDiagram_of_strict_finrank`, completing
`Theorem_6_1_5`'s forward direction, then delete `InfiniteTypeConstructions` +
`FieldGeneric*`.

## Blockers

None.
