## Accomplished

Stage 3.7 statement-fidelity & non-vacuity audit of **Problem 4.5.2** (primitive
central idempotents `ψ_i` of `ℂ[G]`), issue #7244. Verdict: **VERIFIED**.

- Built `EtingofRepresentationTheory.Chapter4.Problem4_5_2` (exit 0).
- `#print axioms` on `psi_acts_self`, `psi_acts_other`, `psi_idempotent`,
  `psi_orthogonal` → all `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.
- Confirmed `psi` def is faithful (scalar `dim V/|G|`, sum over `g` of `χ_V(g)·g⁻¹`
  with the inverse present), `FDRep.character` = trace, `[Simple]` = genuine
  irreducibility, `IsEmpty (W ≅ V)` = genuine non-isomorphism.
- Key check discharged: part (i) proves action = `LinearMap.id` / `0` (scalars pinned
  to `1` / `0`), not the weaker "acts as *a* scalar". Part (ii) products are in the
  group algebra `ℂ[G]`; iso symmetry gives both orderings of orthogonality.
- Wrote `progress/reviews/2026-07-22-ch4-problem4_5_2-central-idempotents-fidelity.md`.
- Updated `progress/items.json` `Chapter4/Problem4.5.2`: `fidelity: verified`,
  `coverage: covered_full`, `fidelity_decl`, `lean_file`, `fidelity_note`,
  `last_updated`.

## Current frontier

Problem 4.5.2 audit complete, no gap, no `.lean` edits (report-only). No follow-up
issue needed.

## Overall project progress

Stage 3.7 (statement-fidelity sweep) ongoing; many Chapter 4 items still `fidelity:
(none)`. Sibling audits in flight: #7265 (Problem 4.12.4), #7270 (Problem 4.1.4).

## Next step

Claim the next oldest Stage 3.7 review issue (#7265 or #7270).

## Blockers

None.
