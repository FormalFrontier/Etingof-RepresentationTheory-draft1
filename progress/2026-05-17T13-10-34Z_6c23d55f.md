# Session 6c23d55f — #2774 Deliverable 1 (subgraph transfer, per-(F,Q))

## Accomplished

- Skipped #2693 (Schur-Weyl γ.B): its dependency PR #2694 (γ.A) is
  CONFLICTING / DIRTY against main, so the required APIs
  `youngSymEndomorphism_restrict_normalized_isProj` and
  `trace_youngSymEndomorphism_restrict_eq_alpha_smul_finrank` are not
  present in the codebase. Recommended that a planner add
  `depends-on: #2694` (or its successor) on #2693, or block #2693 until
  γ.A lands.

- Claimed #2774 (per-(k,Q) subgraph transfer + assembly into the
  Theorem 2.1.2 forward bridge). The full issue requires all six
  per-(F,Q) sub-cases (#2787, #2790, #2791, #2792, #2793, #2797), of
  which only #2787 (cycle) and #2796 (star F-generic) have landed; the
  other four are open and three (#2791, #2792, #2793) are blocked on
  framework-wall sorries in their ℂ sources.

- Landed **Deliverable 1 only** of #2774: in
  `EtingofRepresentationTheory/Chapter6/FieldGenericInfiniteType.lean`,
  added a new `SubgraphTransferPerKQ` section with:
  - `restrictOrientationViaEmb φ Q` — restricts a quiver `Q : Quiver (Fin n)`
    to `Quiver (Fin m)` via an embedding `φ : Fin m ↪ Fin n`.
  - `restrictOrientationViaEmb_subsingleton` (instance).
  - `restrictOrientationViaEmb_isOrientationOf` — the restriction is
    an orientation of the principal submatrix.
  - `subgraph_infinite_type_transfer_per_kQ` — per-(F, Q) transfer
    theorem: if the restricted orientation admits infinitely many
    indecomposable dimension vectors, so does Q. The proof extends
    each subgraph indecomposable by zero on vertices outside `range φ`,
    mirroring the existing universal-quantification version
    (`subgraph_infinite_type_transfer` in `InfiniteTypeConstructions.lean`)
    but with `Q` fixed externally.

- Build green for
  `EtingofRepresentationTheory.Chapter6.FieldGenericInfiniteType`
  and downstream `InfiniteTypeConstructions`. Zero new sorries.

## Notes on theorem shape vs. issue spec

The issue's deliverable-1 signature includes `hadj_symm`, `hadj_noloop`,
`hembed`, `[IsAlgClosed F]`, and `hOrient` as hypotheses. My version
omits them because the linear-algebra extension-by-zero construction
does not use any of them: the restricted quiver is built purely from
`φ` and `Q`, and the indecomposability transfer is a pure module
argument over an arbitrary field. The downstream call site
(deliverable 2, `not_posdef_infinite_type_per_kQ`) can still pass these
hypotheses; they'll be unused at this step but are still needed for the
per-(F,Q) base cases. If the planner wants strict API conformance, the
hypotheses can be added as `_`-prefixed unused arguments later.

## Current frontier

Deliverable 1 of #2774 is in. Deliverables 2 (`not_posdef_infinite_type_per_kQ`)
and 3 (`Theorem2_1_2.lean` wiring) remain. Both depend on all six
per-(F,Q) sub-cases:
- #2787 (cycle) — landed
- #2796 (star F-generic) — landed
- #2790 (D̃₅) — still open, has working ℂ source
- #2797 (star Q-extension) — has `replan` label
- #2791 (Ẽ₆), #2792 (Ẽ₇), #2793 (T(1,2,5)) — open, blocked on
  framework-wall sorries in their ℂ sources, planner-triage pending

## Overall project progress

Stage 3 (Lean formalization). Chapter 6's Gabriel-forward-bridge
(#2401) is the active workstream; #2774 is the assembly step. With
deliverable 1 landed, the bridge can be wired once the remaining
sub-cases land. Chapter 5 Schur-Weyl track is parked on γ.A (PR #2694
CI/conflict).

## Next step

1. Land or repair γ.A (PR #2694) so #2693 can be unblocked. This is
   not a worker action — it's a `repair` agent task.
2. Triage the framework-wall trio (#2791, #2792, #2793): per the
   previous session's recommendation, either split off the
   framework-wall fix as a prerequisite, or rescope to accept a
   sorry'd indecomposability stub mirroring the ℂ wall. Planner action.
3. A worker should claim #2790 (D̃₅) — has working ℂ source, no
   blockers.
4. After the remaining per-(F,Q) cases land, a planner can narrow
   #2774's residual scope to deliverables 2 + 3.

## Blockers

None for this session's output. Downstream issues (#2693, #2774
deliverable 2, #2401) remain blocked as listed above.
