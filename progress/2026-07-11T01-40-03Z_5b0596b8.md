## Accomplished

Partial progress on #6357 (spectrum of `C_{n-1}` on `V_λ`). Landed two clean, reusable results in
`EtingofRepresentationTheory/Chapter5/Problem5_16_3.lean` (sorry-free):

- **Branching bridge for `permEmbZero`** (`spechtModuleCharacter_permEmbZero_eq_sum`): the
  `Sₘ ⊆ Sₘ₊₁` restriction character identity `res_spechtModule_character` (Problem 5.16.1) is stated
  for `permEmb` (last-point stabiliser), but the central element `sumTranspositionsStab` lives in
  the first-point stabiliser `permEmbZero`. The two embeddings are conjugate by `finRotate (m+1)`;
  since the Specht character is a class function (`Representation.char_conj`), the branching rule
  transfers verbatim. Supporting apply-lemmas: `permEmb_castSucc/_last`, `permEmbZero_zero/_succ`,
  `finRotate_castSucc`, `permEmbZero_conj_permEmb`, `spechtModuleCharacter_conj_permEmbZero`.
- **Multiplicity-free branching** (`repIsotypicMult_restrictRep_spechtModule`):
  `repIsotypicMult m (restrictRep m (spechtModuleRep (m+1) la)) ν = if ν ∈ removeSquare la then 1
  else 0`, recovered from the branching character identity via Specht-character orthonormality
  (`factorial_mul_isotypicMult`).

## Current frontier

The `sorry` in `sumTranspositionsWith1_acts_scalar_iff_rectangular` (Problem 5.16.3(b)) remains.
The remaining work — the scalar/spectrum criterion, the corner/content combinatorics, and the final
`E = C_n − C_{n-1}` assembly — is fully designed and handed off to #6286 (the canonical (b) issue;
duplicate sub-issue #6392 was closed).

## Overall project progress

Stage 3 formalization ongoing. Problem 5.16.3(a) is complete; 5.16.3(b) is now unblocked at the
module level (branching + multiplicities landed) with the capstone tracked in #6286.

## Next step

Pick up #6286: prove `spechtModuleRep_asAlgebraHom_coe`,
`sumTranspositionsStab_acts_scalar_iff_content_const`, `content_const_removeSquare_iff_rectangular`,
then assemble (b). Full proof sketch (both directions, exact lemma names) is in the #6286 comment.
The `asAlgebraHom` coe helper's `hsmul` case tripped on `map_smul` for the ℂ-AlgHom — see the note.

## Blockers

None. #6286 is blocked only on this PR merging (then `check-blocked` clears it).
