## Accomplished

Feature session on #6590 (`blocks_equiv_indecomposableCentralIdempotents`, Ch9 Problem 9.5.3-i).

- **Corrected the false statement.** The original theorem asserted a bijection
  `Etingof.Block R ≃ {indecomposable central idempotents}` for a general `[Ring R]`, which is
  `Nonempty (infinite ≃ singleton) = False` (counterexample `R = ℤ`: one block per prime, one
  primitive central idempotent). Re-stated for a finite dimensional algebra over a field,
  `{k} [Field k] [Algebra k R] [FiniteDimensional k R] [Small.{v} R]`, keeping the ambient ring
  `R` so the block API (`Block R`, `IsIndecomposableCentralIdempotent R`) is unchanged. Body
  remains `sorry` (assembly decomposed to #6598).
- **Landed a sorry-free building block** `centralIdempotent_smul_simple`: a central idempotent of
  an arbitrary ring acts on any simple module as the scalar `0` or `1` (Schur — the action is an
  idempotent endomorphism, `Module.End R S` is a division ring, its only idempotents are `0`/`1`).
  `#print axioms` = `[propext, Classical.choice, Quot.sound]`, no `sorryAx`.
- File `EtingofRepresentationTheory/Chapter9/Problem9_5_3.lean` builds. Added imports
  `Mathlib.Algebra.GroupWithZero.Idempotent`, `Mathlib.LinearAlgebra.FiniteDimensional.Defs`.

## Current frontier

Statement corrected + one general-ring lemma proven. The full bijection proof is not attempted
this turn; it is decomposed into three sub-issues.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 9.5.3(ii) was already sorry-free; (i) was false and is
now correctly stated with one supporting lemma proven. Remaining (i) work: #6596, #6597, #6598.

## Next step

Work #6596 (central character map + linkage invariance, general ring — builds directly on the new
lemma) or #6597 (primitive central idempotent decomposition of a fin-dim algebra). Both are
independent; #6598 (assembly) is blocked on both.

## Blockers

None. Parent #6590 marked `replan` via a partial PR with a `Decomposed into #6596, #6597, #6598`
breadcrumb for the next planner.
