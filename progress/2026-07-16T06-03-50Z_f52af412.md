## Accomplished

Completed issue #6738: assembled the complex-level restriction-of-scalars
commutation isomorphism in `Chapter8/ExternalTensorRestriction.lean`.

- `extRestrictComplexXIso` — the degreewise comparison iso, built from
  `PreservesCoproduct.iso` (`restrictScalars` preserves the degree-`n`
  coproduct) composed with `Sigma.mapIso` of the pointwise `extRestrictObjIso`.
- `ι_extRestrictComplexXIso_inv` / `ι_extRestrictComplexXIso_hom` — summand
  compatibility lemmas for the degreewise iso and its inverse.
- `resExt_map_d₁_comp` / `resExt_map_d₂_comp` — Koszul-signed differential
  compatibility, each reduced to `extRestrictObjIso_naturality` in one variable.
- `extTensorComplex_restrictIso` — the assembled chain isomorphism via
  `isoOfComponents`, sorry-free; axioms limited to
  `propext, Classical.choice, Quot.sound`.
- `ι_extRestrictComplexXIso_aug₀` — degree-0 π-compatibility (the map-level
  `i = 0` square #6735 consumes), reduced to naturality + functoriality.

Updated the module docstring Status section.

## Current frontier

`ExternalTensorRestriction.lean` now delivers both the complex-level iso and the
degree-0 π-compatibility. #6738 fully complete.

## Overall project progress

Stage 3 formalization ongoing. Ch8 Problem 8.2.8 Tor/Künneth stream: this lands
the second half of the restriction commutation (#6734 delivered the pointwise
half). #6735 (quasiIso assembly, final glue for #6729) is now unblocked.

## Next step

A worker can pick up #6735: use `extTensorComplex_restrictIso` +
`ι_extRestrictComplexXIso_aug₀` to transport `QuasiIso` per its route (restrict
scalars reflects QuasiIso, then tensor-of-quasi-isos over a field).

## Blockers

None.
