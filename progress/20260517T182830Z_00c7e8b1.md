# Progress — feature session 00c7e8b1

## Accomplished

Partial-PR completion of #2804 (D̃₅ Sub B, the per-(F, Q)
indecomposability + infiniteness sub-issue of #2790). Decomposed the
parent on scope grounds; landed a coherent subset (helpers + API
stubs); filed a follow-up sub-issue (#2834) tracking the remaining
proof body.

Landed in `Chapter6/FieldGenericD5Tilde.lean`:

- **Section 5** (F-generic helper lemmas; no `sorry`s):
  - `embed_sum_zero_F`: leaf disjointness at the center
    (`starEmbed1_F x + starEmbed2_F y = 0 → x = 0 ∧ y = 0`).
  - `center_decomp_F`: `w = starEmbed1_F(starProj1_F w) + starEmbed2_F(starProj2_F w)`.
  - `gamma_from_embed1_F`: `γ(starEmbed1_F x) = starEmbed1_F x + starEmbed2_F x`.
  - `gamma_from_embed2_F`: `γ(starEmbed2_F y) = starEmbed1_F y + starEmbed2_F(N y)`.

  These are F-generic extractions of inline computations in the
  ℂ-specific `d5tildeRep_isIndecomposable`
  (`InfiniteTypeConstructions.lean:1569-1913`).

- **Section 6** (`d5tildeRep_kQ_isIndecomposable`): API stub with
  `sorry`. Docstring ties the deferral to #2834 (the proof body
  follow-up sub-issue) and lists the two strategic approaches the
  follow-up worker can take.

- **Section 7** (`d5tilde_not_finite_type_per_kQ`): per-(F, Q)
  infiniteness theorem, complete in form, transitively depending on the
  Section 6 stub. Mirrors `d5tilde_not_finite_type`
  (`InfiniteTypeConstructions.lean:1962`) injectivity argument at
  vertex 0.

## Decision: decompose rather than attempt full proof

The direct per-(F, Q) generalisation of `d5tildeRep_isIndecomposable`
case-splits on the direction of each of the five edges of `d5tildeAdj`
(2⁵ = 32 patterns, modulo symmetry). The ℂ-specific source is ~350
lines; the per-(F, Q) version is ~600-1000 lines. Decomposing on
session-budget grounds rather than overrunning was the choice
explicitly anticipated by the issue body
("Consider further decomposition during work if `d5tildeRep_kQ_isIndecomposable`
overruns its session").

The follow-up sub-issue (#2834) lists two strategy options for the
remaining work:

1. Direct case-analysis (~600-1000 lines).
2. BGP reflection reduction via Proposition 6.6.7 (cleaner conceptually,
   but identifying the reflected rep with `d5tildeRep_kQ` is its own
   technical bottleneck).

## Current frontier

- #2834 (D̃₅ Sub B.proof-body): proof body of
  `d5tildeRep_kQ_isIndecomposable` — the bulk of the per-(F, Q) work.
- The transitive `sorry` from #2804's stub will propagate to consumers
  of `d5tilde_not_finite_type_per_kQ` until #2834 lands.

## Overall project progress

Stage 3 (Lean formalisation). After the partial PR for #2804 lands:

- Six per-(F, Q) constructions complete (cycle, K_{1,4} F-generic,
  D̃₅ Sub A, D̃₅ Sub B's helpers + API, Ẽ₆ Sub A + B, Ẽ₇).
- Three per-(F, Q) proof bodies outstanding (#2801 — K_{1,4};
  #2834 — D̃₅ Sub B; the framework-walled cases in #2807 etc.).

Sorry-count delta this turn: **+1** (the API stub in
`d5tildeRep_kQ_isIndecomposable`). This is by design — the per-(F, Q)
infiniteness theorem is now wired up at the API level, only the
indecomposability proof body remains.

## Next step

- Dispatch a feature worker on #2834 once this PR merges; that worker
  will inherit the helpers and API, with only the proof body to fill in.
- Per the issue body, #2834 itself is sized large (~600-1000 lines) and
  may need further decomposition during work — the suggested split is
  "canonical-orientation lemma + reflection reduction" vs "direct case
  analysis".

## Blockers

None for the partial-PR work. The follow-up #2834 inherits the design
choice between direct case-analysis and BGP reflection; both are
known-feasible paths, the choice is a session-budget call.
