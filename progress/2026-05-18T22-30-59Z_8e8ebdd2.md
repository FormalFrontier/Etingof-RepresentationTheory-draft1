## Accomplished

Reviewed PR #2921 (`not_posdef_infinite_type_per_kQ` outer assembly +
Theorem 2.1.2 forward bridge) per issue #2924. Verdict: **PASS** on
all 5 deliverables. No code changes recommended; no follow-up issues
filed. Full report in
`progress/reviews/2026-05-18-not-posdef-per-kQ-and-theorem2_1_2-bridge.md`.

Key findings:

- D1 (outer assembly fidelity): 4-case tree at
  `Chapter6/FieldGenericAssembly.lean:177-237` mirrors universal
  `not_posdef_infinite_type` (`InfiniteTypeConstructions.lean:10661`)
  line-for-line; cycle-case `getLast_eq_getElem` rewrite is a genuine
  index-form conversion, not a strengthening.
- D2 (acyclic-branch dispatch fidelity): 3-case dispatch at
  `FieldGenericAssembly.lean:113-161` mirrors universal version at
  `InfiniteTypeConstructions.lean:10609`; `h_no_adj` derivation
  sound; non-adjacent call passes `h_no_adj` in the
  `h_no_adj_branch` position correctly.
- D3 (bridge correctness): `not_posdef_not_HasFiniteRepresentationType`
  at `Theorem2_1_2.lean:153-179` composes correctly; `hn : 1 ≤ n`
  derivation via `Fin 0` vacuity is sound and non-vacuous; subset
  construction with `Module.Finite.equiv (hV_dim v).some.symm`
  pulls finiteness in the right direction.
- D4 (sorry-propagation): `FieldGenericAssembly.lean` has exactly 1
  proof-body sorry (line 96, in `non_adjacent_branches_infinite_type_per_kQ`,
  tracked by #2919 → #2922 + #2923); `Theorem2_1_2.lean` is
  sorry-free. Sorry-count delta = −1.
- D5 (light pattern audit): no non-trivial divergences from universal
  original; placement and `[IsAlgClosed F]` carriage consistent with
  the D2.degree4 audit catalog (#2894).

## Current frontier

PR #2921 audited PASS. The forward direction of Gabriel's theorem
(`Theorem_2_1_2` in `Chapter2/Theorem2_1_2.lean:276-294`) is now
sorry-free modulo the per-(F, Q) leaf chain — specifically:
- `non_adjacent_branches_infinite_type_per_kQ` body (#2919 →
  #2922 + #2923)
- `single_branch_leaf_case_both_extend_per_kQ` (#2905 chain)

## Overall project progress

Pipeline Stage 3 (Lean formalization). Chapter 2 forward bridge of
Gabriel's theorem now has its outer assembly audited and sorry-free
at the assembly level. Remaining sorries on `main` are in the
per-(F, Q) leaf bodies, tracked by issues #2919/#2922/#2923 (Wall 2
non-adjacent branches) and #2905 chain (Wall 1 T(p,q,r) both-extend).

## Next step

Pick up #2922 (`non_adjacent_branches_leaf_case_per_kQ` — leaf
neighbour helper) or, for a planner, triage the open replan / decompose
queue. #2922 is a substantial new-file feature that the issue itself
flags as warranting paper-sketching before implementation; pair with
a feature worker who can commit time for a multi-case design.

## Blockers

None for the audit. #2922 has a high cognitive-load proof design and
may itself want decomposition once a worker starts on it; that's a
future-worker decision, not an audit blocker.
