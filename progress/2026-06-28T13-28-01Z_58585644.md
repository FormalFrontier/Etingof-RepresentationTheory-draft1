## Accomplished

Formalized **Remark 4.6.4** (issue #5379), closing a fidelity gap flagged by the
wave-1 fidelity sweep: the item was marked `sorry_free` but had no Lean declaration
covering its claim.

New file `EtingofRepresentationTheory/Chapter4/Remark4_6_4.lean`:

- `repZ : Representation ℂ (Multiplicative ℤ) (Fin 2 → ℂ)` — the representation of the
  infinite group `ℤ` on `ℂ²` by powers of the unipotent Jordan block `J_{1,2} = [[1,1],[0,1]]`.
  Built as `(Units.coeHom _).comp (zpowersHom _ jUnit)`, where `jUnit` packages `J_{1,2}`
  as a unit of `Module.End ℂ ℂ²` (inverse `1 - N`, using `N² = 0`).
- `repZ_not_irreducible` — the eigenline `ℂ·e₀` is a proper nonzero subrepresentation,
  so `¬ Representation.IsIrreducible repZ`.
- `repZ_indecomposable` — any two nonzero subrepresentations both contain `e₀`
  (via `Etingof.Example_2_3_14.e0_mem_of_invariant`), so they cannot be complementary.
- `Etingof.Remark4_6_4` — packaged existence statement: `Multiplicative ℤ` is infinite,
  and there is a finite-dimensional rep on `ℂ²` that is indecomposable but not irreducible.

The heavy lifting (eigenvector-in-every-invariant-subspace) was reused verbatim from the
existing `Chapter2/Example2_3_14.lean` Jordan-block infrastructure; the new work is the
bridge from `J_{1,2}`-invariance to `ℤ`-invariance (`repZ_fix_e0` by `Int.induction_on`)
plus the `Subrepresentation`/`IsSimpleOrder` glue.

Updated `progress/items.json`: `Chapter4/Remark4.6.4` fidelity `gap` → `verified`,
`fidelity_decl` set to `Etingof.Remark4_6_4`.

## Current frontier

Item complete. `lake build EtingofRepresentationTheory.Chapter4.Remark4_6_4` succeeds with
no warnings; no `sorry`.

## Overall project progress

Phase 3 formalization ongoing; one more fidelity-sweep gap closed. Chapter 4 §4.6 now has
Lean coverage for Definition 4.6.1, Theorems 4.6.2/4.6.3, the discussion blobs, and now
Remark 4.6.4.

## Next step

Continue closing remaining wave-1 fidelity-sweep findings (epic #5338).

## Blockers

None.
