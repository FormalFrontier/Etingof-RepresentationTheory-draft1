## Accomplished

Feature session on #6519 (Ch8 Problem 8.2.7(ii): Tor/Ext for `k[x]/(f)`, `k[x]/(g)`).
Landed as **partial PR #6525** (three commits):

1. **`PolyGcd` bridge namespace** (mirrors `ZModGcd`, over `k[X]`, targeting the sum ideal
   `Ideal.span {f,g} = (f) ⊔ (g)` directly — no explicit `gcd`):
   - `mulBy`, `castLin`, `span_smul_top_eq_ker`, `cokerEquiv` (coker of `·f` on `k[x]/g` ≅ `k[x]/(f,g)`)
   - `tensorEquiv` : `(k[x]/f) ⊗_{k[x]} (k[x]/g) ≃ₗ k[x]/(f,g)` (via `quotTensorEquivQuotSMul` + `cokerEquiv`)
   - `kerEquiv` : `ker(·f on k[x]/g) ≃ₗ k[x]/(f,g)` (PID generator of `(f,g)` + coprime cofactors)
   - `homToKer`, `homEquiv` : `Hom_{k[x]}(k[x]/f, k[x]/g) ≃+ k[x]/(f,g)`
2. **`Problem_8_2_7_ii_ext_zero`** — sorry-free (via `homEquiv`).
3. **`Problem_8_2_7_ii_ext_one`** — sorry-free (mirror of `i_ext_one` via `cokerEquiv`; uses
   `restrictScalars ℤ` to bridge the ℤ-linear `Ext` side to the `k[x]`-linear cokernel).

**Statement correction:** part (ii) theorems dropped part (i)'s nonzero hypotheses, making
`ext_zero`/`ext_one`/`tor_one` false in degenerate cases (e.g. `g=0, f=X`:
`Hom(k[x]/(X), k[x]) = 0` but `k[x]/(X,0) = k ≠ 0`). Added the needed `f ≠ 0` / `g ≠ 0`
hypotheses (mirroring part (i)); `tor_zero` is true unconditionally so left hypothesis-free.

## Current frontier

The two **`Tor`** identifications (`ii_tor_zero`, `ii_tor_one`) remain `sorry`. They need the
glue `tensorOver k[x] (k[x]/g) (k[x]/f) ≃+ (k[x]/f) ⊗_{k[x]} (k[x]/g)` (the ℤ-tensor-mod-balancing
= ring-tensor fact; balancing is nontrivial for `A = k[x]`, unlike `A = ℤ` in part (i)).
I built this glue (`polyTensorOverEquiv`) but it is **blocked on an instance diamond**: the
forward/backward maps need `op a • m = a • m` on `k[x]/(f)` for the `mopPolyQuot` action pinned
inside `balancedSubgroup`, which is not `rfl` (it *was* for `ZMod`) and where `op_smul_eq_smul`
fails because `IsCentralScalar` exists for the global `Submodule.Quotient.module'` instance but
not the `mopPolyQuot` one.

## Overall project progress

Phase 3 formalization ongoing. Problem 8.2.7: part (i) complete; part (ii) now has all shared
bridge infra + both `Ext` identifications + all vanishing theorems; only the two `Tor` degree-0/1
identifications remain. Follow-up filed as **#6524**.

## Next step

Claim **#6524** (Tor half). Fastest route to unblock: add a general
`tensorOver A N M ≃+ M ⊗[A] N` (commutative `A`) to `Definition8_2_3(_RightExact).lean` using the
existing `mk_smul_tmul` / `homEquivInvFun` / `tensorOver_hom_ext` adjunction helpers (which already
handle the mop balancing), OR prove `op a • m = a • m` for `mopPolyQuot` directly / reconcile it
with `Submodule.Quotient.module'` (verify no break to `polyMopResolution`/`_tor_vanish`). Then
`tor_zero` mirrors `i_tor_zero` (glue + `PolyGcd.tensorEquiv`) and `tor_one` mirrors `i_tor_one`
(~130 lines, six-term sequence, `PolyGcd.kerEquiv`).

## Blockers

None hard — #6524 is fully specified and all reusable bridges are on the PR/`main`. The only
subtlety is the `mopPolyQuot` action diamond, documented in #6524.
