## Accomplished

Triage session (`/work` → `/feature`). No code changes; all three unclaimed
issues turned out to be blocked or already-done. Released each with precise,
actionable breadcrumbs instead of attempting heroics or busywork.

- **#4870** (Ch5 #4862 counting step — the real ℂ-side Schur-Weyl frontier):
  **skipped → replan.** Its prerequisite file
  `Chapter5/SchurWeylSimplesClassificationComplex.lean` and all cited
  sorry-free lemmas (`schurWeyl_simples_isotypic_matching_complex`, the core
  theorem with the `hφsurj` sorry, etc.) **do not exist on `main`** — they are
  introduced only by the open **partial** PR #4871 (`agent/c9471cc6`), which
  does NOT close #4862. The counting proof can't be cleanly executed until
  #4871 merges. #4871 is MERGEABLE with CI in progress but has **no auto-merge
  enabled** (a PR-health/repair concern, not a worker's job). Recommended:
  re-queue #4870 once #4871 lands; a repair pass on #4871 unblocks it.

- **#2841** (upstream tracker: idempotent-trace lemma): **skipped → replan.**
  Discovered deliverable 1 is already complete — a prior `kim-em` session
  opened mathlib4 PR #39523, now **merged to master**
  (`LinearMap.IsIdempotentElem.eq_zero_of_trace_eq_zero`). The local copy
  already carries a forwarding doc-comment. Only residual is the bump-gated
  cleanup, **absent from the pinned `v4.28.1`**.

- **#2564** (upstream tracker: `MvPolynomial.eq_of_eval_eq_on_gl`): left as-is,
  but added a **cross-link comment**. Its upstream PR #38583 is likewise merged
  to master but absent from `v4.28.1`. Identical residual to #2841.

## Current frontier

The genuine Chapter 5 frontier (#4870 counting step / Schur-Weyl ℂ-side crux) is
gated on PR #4871 merging. The two upstream trackers (#2564, #2841) are gated on
a shared, deliberate Mathlib pin bump.

## Overall project progress

Stage 3 (formalization) ongoing. Ch5 Schur-Weyl simples classification reduced
(on the #4871 branch) to a single counting sorry. Two CharZero/linear-algebra
lemmas successfully upstreamed to Mathlib (PRs #38583, #39523), both merged,
awaiting a project Mathlib bump to retire the local copies.

## Next step

1. **Repair/merge PR #4871** (enable auto-merge), then re-queue #4870 — that is
   the highest-value unblock.
2. **Planner: consolidate #2564 + #2841** into a single "bump Mathlib pin past
   v4.28.1, then remove both local copies + forward both call sites" issue.
   A v4.28.1→v4.32 bump is a large project-wide rebuild — schedule it
   deliberately, not as a low-priority tracker. Both local copies work; no
   urgency.

## Blockers

- #4870 blocked on unmerged partial PR #4871 (file not on `main`).
- #2564 / #2841 cleanup blocked on a deferred project-wide Mathlib pin bump.
