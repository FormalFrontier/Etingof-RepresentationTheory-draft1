## Accomplished

Executed review issue #7176: statement-fidelity & non-vacuity audit of Ch5
Theorem 5.22.1 (Weyl character formula for GL(V)). Report-only review.

- Wrote `progress/reviews/2026-07-21-ch5-theorem5_22_1-weyl-character-formula-fidelity.md`.
- **Verdict: FAITHFUL (but partial).** Both headline decls
  (`Etingof.Theorem5_22_1`, `Etingof.schurModule_weight_eq_schurPoly_coeff`) are
  genuine, sorry-free, axiom-clean formalizations of the book's part 2
  (`ch(L_λ) = S_λ`). Confirmed: `SchurModule` is the real `Im(c_λ)` (and simple, per
  `SchurModuleSimple.lean`); `formalCharacter` is the true torus character
  `∑_μ dim(M_μ)·x^μ` over genuine weight spaces with finite support; `schurPoly` is
  pinned by `schurPoly_mul_vandermonde` (not an arbitrary `Exists.choose`);
  `Antitone lam` faithfully encodes "partition with ≤ N parts".
- Build: `lake build …Theorem5_22_1` exit 0 (8604 jobs). Axioms of both decls:
  `[propext, Classical.choice, Quot.sound]` — no sorryAx, no custom axioms.
- Non-vacuity: PASS, formally backed via `schurPoly_coeff_self_ne_zero` /
  `schurPoly_ne_zero` (highest-weight multiplicity ⇒ `L_λ ≠ ⊥`), plus concrete
  witness `λ=(1,0,…,0)` = standard rep, character `x_1+…+x_N`.

## Current frontier

Scope finding: the file formalizes only part 2 of the 3-part theorem.
- Part 1 (`L_λ=0 ⇔ N<p`) is *structurally subsumed* by the `λ : Fin N → ℕ`
  encoding (always `p ≤ N`) — not a gap.
- Part 3 (dimension product `∏(λ_i−λ_j+j−i)/(j−i)`) is a *genuine omission* — filed
  as feature follow-up **#7180**. This is additive coverage, not a defect fix; the
  two audited decls are faithful as-is, so no Lean change made in this review.

## Overall project progress

Confidence/review phase ongoing. Two Ch5 review issues remain unclaimed after this
(#7168 Ch8 injective modules, #7170 Ch4 second orthogonality). This review adds the
Theorem 5.22.1 fidelity report and one feature issue (#7180) for part 3.

## Next step

A worker can pick up #7180 (formalize the Weyl dimension formula, part 3) or the
remaining review issues #7168 / #7170.

## Blockers

None.
