## Accomplished

Session type: **feature** (via `/work`). Claimed #6001 (Problem 3.8.4: scalar extension +
Noether-Deuring), assessed it as too large for one session, and **decomposed** it into three
self-contained sub-issues:

- **#6020** — base-change `A`-module `L ⊗[K] V` is a power `Vⁿ` for finite `L/K`.
- **#6021** — Krull-Schmidt cancellation `Vⁿ ≅ Wⁿ ⟹ V ≅ W` over an arbitrary field.
- **#6022** — assemble Problem 3.8.4(i),(ii): finite case + finite-extension descent +
  Noether-Deuring (depends on #6020, #6021).

Left the `Decomposed into #6020, #6021, #6022` breadcrumb on #6001 and `coordination skip`ped
it (now `replan`).

Then claimed **#6021** and landed real, sorry-free infrastructure in a new file
`EtingofRepresentationTheory/Chapter3/Problem3_8_4_Cancellation.lean`:

- `Etingof.IsIndecomposable.of_linearEquiv` — indecomposability transfers along an
  `A`-linear iso (via `Submodule.orderIsoMapComap` + `OrderIso.isCompl_iff`). **sorry-free.**
- `Etingof.Problem3_8_4.indecDecomp_map` — transport a full internal indecomposable
  decomposition across an `A`-linear iso (preserves indecomposable / nonzero / spanning /
  independent), using `equivMapOfInjective`, `Submodule.map_iSup`, `LinearMap.iSupIndep_map`.
  **sorry-free.**
- `Etingof.Problem3_8_4.iso_pow_cancel` — the target statement, proof still `sorry` but with a
  detailed 6-step roadmap in the body (decompose V,W → power-decompose Vⁿ → transport W-power
  across the iso → `krull_schmidt_uniqueness` → fibre-count to `τ : Fin p ≃ Fin q` → assemble
  `⨁` iso).

Registered the file in `EtingofRepresentationTheory/Chapter3.lean` (CI also auto-globs it).

## Current frontier

`iso_pow_cancel` (#6021) proof body. The two reusable helpers it needs beyond what is written
are: (a) the coordinate power-decomposition of `Fin n → V` (indexed `Fin n × Fin p`), and
(b) the "equal per-iso-class multiplicity ⇒ class-preserving bijection" fibre-counting step,
followed by the `DirectSum.IsInternal` assembly. See the roadmap comment in the theorem body.

## Overall project progress

Stage 3 (formalization) ongoing. Problem 3.8.4 (#6001) is now a 3-issue subtree; #6021 has a
partial PR with 2 sorry-free lemmas + roadmap. Krull-Schmidt over arbitrary fields
(Problem 3.8.3) is the foundation these build on.

## Next step

- Land #6021 fully: implement the roadmap in `Problem3_8_4_Cancellation.lean`. The power-
  decomposition (step 2) and fibre-counting (step 5) are the two remaining hard pieces.
- In parallel, #6020 (base-change ↔ power) is independent and unblocked.
- #6022 stays `blocked` until #6020 and #6021 close.

## Blockers

None. All three sub-issues are actionable (#6022 is dependency-blocked by design).
