## Accomplished

Machine-checked the issue #4566 refutation of `starRep_kQ_isIndecomposable`
(the orientation-generic D̃₄ indecomposability theorem,
`Chapter6/FieldGenericStar.lean:764`, body `sorry`).

New, sorry-free theorem `starRep_kQ_reversedLeaf3_decomposable`
(`FieldGenericStar.lean`, end of file): for the orientation
`reversedAtVertex starQuiver 3` (reversed diagonal leaf), the K_{1,4} (D̃₄)
representation `starRep_kQ F Q hOrient 1` is **decomposable**. Supporting
material added in the same file:

* `isCompl_coordLines_two`, `isCompl_coordPlanes_four` — reusable
  complementarity facts for coordinate subspaces in `Fin 2 → F` / `Fin 4 → F`.
* `starCexW1` / `starCexW2` — the explicit complementary invariant pair from
  the issue (center planes `span{e₀,e₃}` / `span{e₁,e₂}`, leaf lines).
* image-computation lemmas (`cex_embed1_e0`, …, `cex_proj3_h2`) for the four
  leaf maps and the reversed-leaf projection at `m = 1`.
* `starRevLeaf3_subsingleton` instance + `starRevLeaf3Quiver` abbreviation.

The proof discharges invariance over all 4 arrows (the other 21 vertex pairs
have empty Hom in this orientation) and `IsCompl` at all 5 vertices; both
summands are nonzero at leaf 1. `#print axioms` shows only
`propext, Classical.choice, Quot.sound` — no `sorryAx`.

Also flagged the false theorem's docstring as refuted, pointing to the new
theorem and to issue #4566's redesign.

## Current frontier

`star_not_finite_type_per_kQ` (and the whole D̃ₙ `_kQ` family) still routes
its infinite-type proof through an indecomposability lemma that is false for
reversed orientations. The lemma `sorry` at `FieldGenericStar.lean:780` is now
known-unfillable.

## Overall project progress

Stage 3 (formalization) ongoing. This turn converts the issue #4566 prose
refutation into a machine-checked Lean fact — the rigorous foundation for
treating the orientation-generic D̃ indecomposability lemmas as refuted.

## Next step

Two pieces remain for #4566 (left for a planner / follow-up issue):

1. **Audit + replan the D̃ₙ family.** The same reversed-leaf coupling loss
   affects `d5/d6/d7/d8/dTildeRep_kQ_isIndecomposable` and their
   `_leaf_equalities` sub-issues. Open issues attempting to *prove* these
   false statements should be replanned: #4527, #4529, #4535, #4543, #4571
   (and the d5/d7 analogues #2851, #2853, #4533). Their `_not_finite_type`
   conclusions remain true; only the explicit-rep indecomposability route is
   unsound.
2. **Homogeneous-tube redesign for the D̃ family** (issue #4566
   recommendation 2), mirroring the sporadic-case program (#4548, #4574,
   #4568, #4559) — or switch the per-(F,Q) infinite-type proofs to the
   book's Tits-form / orbit-counting argument.

## Blockers

None for the landed work. The redesign (item 2 above) is a substantial,
multi-session effort that needs the corrected D̃₄ tube matrices worked out
first (the sporadic `sporadic-tube-redesign-design.md` §5 defers the
analogous two-level derivation).
