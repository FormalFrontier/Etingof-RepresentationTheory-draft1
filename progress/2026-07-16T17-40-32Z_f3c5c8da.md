## Accomplished

Formalized issue #6847 (Ch6 #Problem6.1.3-g, combinatorial core of the cyclic
case): proved `two_regular_connected_iso_Atilde` **sorry-free** in
`EtingofRepresentationTheory/Chapter6/Problem6_1_3_continued_tildeE.lean`.

A connected 2-regular simply-laced graph on `Fin n` (`n ≥ 3`) is graph-isomorphic
to the `n`-cycle `Ãₙ`. Construction and proof:

- New `noncomputable def otherNbr adj b a`: swaps `b`'s two neighbours, fixes
  everything else (so `a ↦ otherNbr adj b a` is an involution). Helper lemmas:
  `otherNbr_mem_erase`, `otherNbr_adj`, `otherNbr_ne`, `otherNbr_eq`,
  `otherNbr_eq_self`, `otherNbr_involutive`, `stepPair_injective`.
- The walk is the pair-iteration `g k = Tᵏ e₀` with
  `T (a,b) = (b, otherNbr adj b a)`, `e₀ = (0, chosen neighbour)`, `f k = (g k).1`.
- `Function.minimalPeriod T e₀` gives the wrap-around (`f p = f 0`, `f (p+1) = f 1`)
  and pure periodicity; `iterate_injOn_Iio_minimalPeriod` gives pair-injectivity.
- Surjectivity (`S = univ`): the visited set is closed under adjacency; combined
  with connectivity (via `ReflTransGen (AdjEdge adj)` from `hconn`) it is all of
  `Fin n`.
- Injectivity of `f` on `[0,p)`: minimal-gap strong induction — a coincidence at
  gap `d ≥ 3` yields one at gap `d-2` (neighbour swap), gaps `1,2` ruled out by
  self-loop / no-backtrack. Hence `p = n`.
- Assembly: `Equiv.ofBijective`, adjacency correspondence proved via a
  cyclic successor/predecessor characterisation plus a small modular-arithmetic
  lemma (`hpred`).

`#print axioms two_regular_connected_iso_Atilde` → `[propext, Classical.choice,
Quot.sound]`. The target module builds with no new warnings.

## Current frontier

`Problem6_1_3_continued_tildeE.lean` builds. The only remaining `sorry` in the
file is the pre-existing ⟹ direction of `affine_dynkin_classification` (parent
#6785), which is out of scope for #6847.

## Overall project progress

Stage 3 (formalization) ongoing. This lands the cyclic combinatorial core that
#6792 / #6785 depend on (the "connected 2-regular ⟹ n-cycle" reindexing, which
Mathlib lacks). Unblocks #6848 (degree-2 reduction + assembly).

## Next step

A worker can take #6848 (uses this lemma) or #6793 (tree case, D̃/Ẽ). The
assembly of #6792's cyclic branch now has its graph-theory core available.

## Blockers

None.
