# Session a7064b32 — embed_etilde7_in_tree_per_kQ body (#2938)

## Accomplished

Filled the body of `embed_etilde7_in_tree_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericETilde7.lean:355`,
closing issue #2938 (sub-2 of #2932).

Structure (mirrors `embed_t125_in_tree_per_kQ` /
`embed_etilde6_in_tree_per_kQ`):

1. `adj_comm`, `ne_of_adj'` setup.
2. Seven same-arm distinctness facts from edge hypotheses
   (`hv₀_ne_u₁`, `hv₀_ne_cᵢ`, `hcᵢ_ne_dᵢ`, `hdᵢ_ne_eᵢ`).
3. Seven reversed-edge facts.
4. Seven distance-2 triangle non-edges via `acyclic_no_triangle`:
   `hu₁cᵢ` and `hc₂c₃` via apex `v₀`; `hv₀dᵢ` via apex `cᵢ`;
   `hcᵢeᵢ` via apex `dᵢ`.
5. Six level-1 cross-arm `≠` facts (`hu₁_ne_dᵢ`, `hcᵢ_ne_dⱼ`,
   `hv₀_ne_eᵢ`).
6. Local `path_nodup{4..7}` and `path_edges{4..7}` helpers,
   copy-paste verbatim from `FieldGenericT125.lean`.
7. Six distance-3 non-edges via `acyclic_path_nonadj` on
   4-vertex paths.
8. Five level-2 cross-arm `≠` facts.
9. Five distance-4 non-edges via 5-vertex paths.
10. Two level-3 cross-arm `≠` facts (`hd₂_ne_e₃`, `hd₃_ne_e₂`).
11. Two distance-5 non-edges via 6-vertex paths
    (`hd₂e₃`, `he₂d₃`).
12. One level-4 cross-arm `≠` (`he₂_ne_e₃`).
13. One distance-6 non-edge via 7-vertex path (`he₂e₃`).
14. `φ_fun : Fin 8 → Fin n` via `match` on `i.val` with
    `fin_cases`-based injectivity.
15. `hembed : ∀ i j, etilde7Adj i j = adj (φ i) (φ j)` via
    64-case split with `simp only [etilde7Adj, φ, φ_fun] <;>
    norm_num <;> linarith [...]` listing 8 `hdiag`, 7 edges,
    28 `adj_comm`, and all 21 non-edges.
16. Final dispatch to `etilde7_not_finite_type_per_kQ` via
    `subgraph_infinite_type_transfer_per_kQ` and
    `restrictOrientationViaEmb_isOrientationOf`.

Bugs fixed during development (caught by Lean's type checker):
* `acyclic_no_triangle` for `cᵢ-eᵢ` triangles needed
  `hdᵢ_ne_eᵢ.symm` (the `y ≠ apex` slot expects `eᵢ ≠ dᵢ`).
* `path_nodup{4,5,6,7}` calls listing `c₃ ≠ d₂` needed plain
  `hc₃_ne_d₂` (not `.symm`); same for `c₃ ≠ e₂` using
  `hc₃_ne_e₂`.

## Current frontier

Build:
* `lake build EtingofRepresentationTheory.Chapter6.FieldGenericETilde7`
  passes (8043 jobs, 63s when rebuilding the file). Sole
  remaining `sorry` in the file is the pre-existing
  `etilde7Rep_kQ_isIndecomposable` (wave-54 framework wall,
  unrelated to this PR).

Sorry count delta on `FieldGenericETilde7.lean`: 2 → 1 (closed
the proof-body sorry on `embed_etilde7_in_tree_per_kQ`).

## Overall project progress

Stage 3 ongoing. The Ẽ₇ embedding helper for #2932's
non-adjacent-branches leaf case is now sorry-free, completing
sub-2 of #2932. Remaining #2932 work (sub-3, issue #2939) is
the `non_adjacent_branches_leaf_case_per_kQ` Phase 1/2 body,
which is blocked on this sub-2 PR landing.

## Next step

After this PR merges, the next planner cycle should unblock
#2939 (its `depends-on: #2938` should clear automatically via
`coordination check-blocked`).

## Blockers

None.
