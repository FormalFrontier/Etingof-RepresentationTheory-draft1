## Accomplished

Resolved issue #4969 (route-B fix for Ch5 #4901): retired the false ingredient (c)
and threaded polynomiality of `L` as a hypothesis, with no new `sorry`.

- **Retired the false lemma.** Deleted `glWeightSpace_top_of_simple_formalCharacter_eq_schurPoly`
  (the unfillable `sorry` at `SchurWeylFormalCharacterIso.lean:165`). `formalCharacter` is blind
  to non-`ℕ` weights, so it cannot certify polynomiality of a simple (counterexample
  `det⁻¹ ⊗ Sym³(std)` at `N = 2`).
- **New elementary lemma** `glWeightSpace_iSup_eq_top_of_equivariant_surjective`: a
  `GL_N`-equivariant surjection `φ : M → P` carries `ℕ`-weight vectors to `ℕ`-weight vectors of
  the same weight, so weight saturation transfers from `M` to `P`. One clean fact behind both
  polynomiality needs.
- **New true lemma** `glWeightSpace_schurModule_iSup_eq_top` (in `SchurWeylFormalCharacterIso`):
  the Schur module is the equivariant image of the tensor power under the Young symmetrizer's
  corestriction (`glTensor_comm_youngSym` + `LinearMap.surjective_rangeRestrict`), and the tensor
  power's weight spaces span (`glTensorRep_iSup_glWeightSpace_eq_top`, FormalCharacterIso). This
  upstream-correct placement replaces the previously `private`, **downstream** copy in
  `Proposition5_22_2` (which imports the iso file — the planner's "de-private it" step would have
  created an import cycle).
- **Threaded `hLtop`** into `simpleRep_iso_schurModule_of_formalCharacter_eq` (L-side saturation
  as a hypothesis; `L_λ`-side via the new true lemma).
- **Discharged `hLtop` at the real caller** `iso_of_formalCharacter_eq_schurPoly`: the simple
  summand `L (f 0)` is the equivariant image of the polynomial `M` (via `kEquivOfAsModuleEquiv hφ'`),
  so saturation transfers from `h_span`.
- Removed the now-redundant private copy + its two saturation-only helpers (`colorWeight`,
  `youngSym_tBasis_weightVector`) from `Proposition5_22_2`; updated stale doc references in
  `PolynomialRepEmbedding`.

## Current frontier

`simpleRep_iso_schurModule_of_formalCharacter_eq` and `iso_of_formalCharacter_eq_schurPoly` are
sorry-free. Full `lake build` succeeds (8357 jobs).

## Overall project progress

Stage 3 formalization, Chapter 5. The highest-weight-uniqueness assembly (route B, #4901) now
rests on exactly two genuine ingredients: #4946 (general-`k` Schur-module simplicity) and #4947
(general-`k` torus→full-group character independence). The false third ingredient is gone.

## Next step

Pick up #4946 or #4947 (the two remaining deep ingredients of #4901), or another unclaimed
Chapter 5 assembly issue.

## Blockers

None.
