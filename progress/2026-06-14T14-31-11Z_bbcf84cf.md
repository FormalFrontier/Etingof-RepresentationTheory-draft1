## Accomplished

Scaffolded the Schur-Weyl #5 Step E assembly (issue #2482) in a new file
`EtingofRepresentationTheory/Chapter5/PolynomialGLDecomposition.lean`:

- **`decompose_polynomial_gl_rep`** — the main result, stated in **abstract-summand
  form** as requested by #2482 (no concrete `SchurModule`; keeps the dependency
  graph with consumer #6 acyclic). It produces a `MonoidAlgebra k GL_N`-module
  equivalence `M.asModule ≃ ⨁_{j : Fin p} (L (f j)).asModule` with each `L i`
  simple. **Proved sorry-free** modulo the two bridge lemmas below, by applying
  the isotypic engine `submodule_of_directSum_simple_iso_directSum` (#4600) and
  composing the equivalences. This verifies the assembly reduction is correct.
- Two bridge lemmas left as documented `sorry` (the genuine remaining
  infrastructure), each filed as a sub-issue:
  - `glTensorRep_schurWeyl_decomposition_equivariant_simple` → **#4666**.
  - `polynomial_homog_rep_asModule_embeds_directSum_simple` → **#4667** (depends
    on #4666).

File compiles: `lake build …Chapter5.PolynomialGLDecomposition` succeeds with
exactly the two expected bridge sorries. Added the import to `Chapter5.lean`.

## Current frontier

Key finding: the issue framed this as a "mechanical assembly of merged pieces",
but `glTensorRep_equivariant_schurWeyl_decomposition` (equivariance, no
simplicity) and `Theorem5_18_4_GL_rep_decomposition_simple` (simplicity, no
equivariance/equiv-formula) are **separate existentials**. They build the *same*
`L i = FDRep.of ρ_i`, but neither exposes both clauses, and the isotypic engine
needs an `R`-linear equiv to *simple* summands. So a unifying bridge (#4666) plus
the `k`-linear → `asModule` `R`-linear transfer (#4667) are genuinely required —
this is the GL_N complete-reducibility infrastructure, not glue.

## Overall project progress

Phase 3 (formalization). Schur-Weyl chain: #4598 (embedding) and #4600 (isotypic
extraction) merged; the §5.23 Step E assembly framework now lands with the main
theorem proven modulo #4666/#4667. Downstream consumer is #6
(`iso_of_formalCharacter_eq_schurPoly`, still its own sorry) and #2483.

## Next step

Claim **#4666** (unify equivariance + L_i simplicity): cleanest route is to add
the `IsSimpleModule (MonoidAlgebra k GL_N) (asModule (FDRep.of ρ_i).ρ)` clause
to `Theorem5_18_4_GL_rep_decomposition_explicit` (it already builds
`L = FDRep.of ρ_i`; transplant the `hL_simple` block from
`Theorem5_18_4_GL_rep_decomposition_simple`), then make #4666 a thin wrapper.
Then **#4667** (asModule transfer + S⊗L splitting), then #2482 needs no further
code (the framework already consumes both).

## Blockers

None. #4666 is ready to claim; #4667 is blocked on #4666; #2482 itself is
landed as a partial framework PR (the two bridges are the residual work).
