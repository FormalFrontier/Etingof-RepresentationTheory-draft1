## Accomplished

Worked issue #6061 (Ch4, FDRep↔Rep simple equivalence + count). Completed Deliverables 1
and 2 in `EtingofRepresentationTheory/Chapter4/Exercise4_2_3.lean` (`Bridge` section),
sorry-free (`#print axioms` reports only `propext`/`Classical.choice`/`Quot.sound`):

- `isSimpleModule_asModule_of_simple` — **preservation of simplicity** (the mathematical
  crux): a simple `FDRep k G` object has a simple underlying `k[G]`-module. Proof: every
  `k[G]`-submodule is a finite-dimensional subrepresentation (`Subrepresentation.ofSubmodule'`),
  giving a mono into `V` in `FDRep k G` (built in `Rep` via `Rep.ofHom` on the inclusion
  intertwining map, then reflected through the faithful `forget₂` via
  `mono_of_mono_map`); `Simple V` forces it to `⊥` (mono `= 0`) or `⊤` (mono iso ⟹ surjective).
- `subInclusion`, `nontrivial_carrier_of_simple` (helpers).
- `simple_rep_iff_isSimpleModule`, `simple_forget₂_of_simple`.
- `fdRepSimpleToRepSimple` + `EssSurj`/`IsEquivalence` instances (needs `[Finite G]`).
- `irrepClassesEquivRepSimpleClasses` (**D1**).
- `SimpleModuleClasses` and `card_irrepClasses_eq_card_simpleModuleClasses` (**D2**).

Key facts discovered: `Rep.of ρ = (forget₂ (FDRep k G) (Rep k G)).obj (FDRep.of ρ)` and
`V ≅ FDRep.of V.ρ` both hold by `Iso.refl`/`rfl` (defeq), which makes essential surjectivity
a `Iso.refl`. Universe bookkeeping: `SimpleModuleClasses` needs an explicit carrier-universe
parameter pinned to `u` (the `Rep`/`FDRep` carrier universe = `k`'s universe) so it matches
the `Rep.equivalenceModuleMonoidAlgebra` carrier universe.

## Current frontier

D3 (the `Finite (IrrepClasses k G)` instance) is NOT done — split into new issue #6090
(semisimple quotient `k[G]/rad` route). The main `Exercise4_2_3` theorem is still `sorry`
(the strict-inequality assembly, tracked by #6048).

## Overall project progress

Stage 3 (formalization) ongoing across chapters. This turn advanced the Ch4 Exercise 4.2.3
counting infrastructure: D1 (FDRep↔Rep simple equivalence) and D2 (count lemma) landed.

## Next step

Pick up #6090 for the `Finite` instance, then #6048 (assembly) once its other deps land.

## Blockers

None. D3 deferred to #6090 (not a blocker — it is separable, as anticipated by #6061).
