## Accomplished

Completed the residual of #6867 (substep of #6844, the `Ext` half of Problem 8.2.8): the full
degreewise object iso `rearrangeHomComplexXIso` in
`EtingofRepresentationTheory/Chapter8/RearrangeHomComplexX.lean`, sorry-free.

For each degree `i`,

    ((extTensorComplexLeft P₁ P₂).linearYonedaObj k (N₁⊗N₂)).X i
      ≅ (tensorObj (P₁.complex.linearYonedaObj k N₁) (P₂.complex.linearYonedaObj k N₂)).X i

i.e. `Hom(⊕_{j+m=i} (P₁ⱼ ⊗ₖ P₂ₘ), N₁⊗N₂) ≅ ⊕_{j+m=i} Hom(P₁ⱼ,N₁) ⊗ₖ Hom(P₂ₘ,N₂)`.

Route (as the issue prescribed): `Hom(-, N₁⊗N₂)` is an additive functor, so it sends the finite
biproduct `⊕Sⱼₘ` (the degree-`i` object of the external tensor complex) to a biproduct. `coreFwd` is
the finite sum `∑ (restrict ιS) ≫ summandIso ≫ ιT`; `coreInv` is `mapBifunctorDesc(summandIso⁻¹ ≫
extend πS)`; the two composites are identities via the source biproduct relations `srcInc_srcProj`
(ι≫π = δ) and `sum_srcProj_srcInc` (∑ π≫ι = 𝟙), transported through the additive `homYoneda`.

Delivered items: `rearrangeHomComplexXIso` (the iso), plus the two ι/π-reduction lemmas the assembler
(#6868) needs — `srcInc_srcProj` (source ι≫π = δ) and `ιMapBifunctor_rearrangeHomComplexXIso_inv`
(target ι ≫ inv). Building blocks `coreFwd`/`coreInv`/`coreIso`, `fullSummandIso`, `srcInc`/`srcProj`
are all sorry-free data.

Key obstacle solved: a genuine `k`-module diamond on `N₁⊗N₂` (and each `Nᵢ`). The complex `.X`
objects carry the categorical `Linear.homModule` k-structure; `summandIso` carries
`ModuleCat.Hom.instModule` with the *external* `Module k` (TensorProduct). These are propositionally
equal but not defeq. Reconciled with the reusable bridge lemmas `linYonedaXEq`/`srcSummandEq`, each
proved via `Module.ext'` + `algebraMap_smul` (the scalar tower), fed into `fullSummandIso` through
`eqToIso`.

Verification: `lake build EtingofRepresentationTheory.Chapter8.RearrangeHomComplexX` succeeds, 0
sorries. Only residual warnings are `unusedSectionVars` (auto-included finiteness instances on the
bridge lemmas that don't need them), consistent with the rest of the Chapter 8 files.

## Current frontier

#6867 complete. Its dependent #6868 (`rearrangeHomComplexX` cochain complex via `isoOfComponents` +
#6843 naturality, using the degreewise iso here) is unblocked once this lands on `main`.

## Overall project progress

Stage 3 (formalization) ongoing. The `Ext` Künneth assembly stack (#6844) now has the degreewise
object iso; remaining `Ext`-side residuals are #6868 (complex assembly) and #6818 (final
`Problem_8_2_8_ext`).

## Next step

Land the PR, then a worker can pick up #6868: assemble `rearrangeHomComplexX` (the cochain-complex
iso) from `rearrangeHomComplexXIso` + `ιMapBifunctor_rearrangeHomComplexXIso_inv` and the #6843
naturality squares, mirroring `Chapter8/RearrangeComplex.lean`.

## Blockers

None.
