## Accomplished

One-shot dispatch for issue #5772 (Foundation for Remark 5.2.8 Steps 3-4:
cyclotomic Galois invariance of β). Formalized the **representation-theoretic core
of Step 4** in `Chapter5/Remark5_2_8.lean`, all sorry-free and axiom-clean
(`propext`/`Classical.choice`/`Quot.sound` only):

- `pow_apply_of_mem_eigenspace` — `fᵏ x = μᵏ • x` for `x` in the `μ`-eigenspace.
- `eigenvalue_pow_eq_one` — an eigenvalue of a finite-order operator (`f^N = 1`) is
  an `N`-th root of unity.
- `trace_pow_eq_sum_eigenvalues` — the trace-power-sum identity `trace(fᵏ) =
  ∑_μ (dim Eμ)·μᵏ` for a finite-order operator over ℂ (via semisimplicity: minpoly
  divides squarefree `X^N-1`, so `V` = ⨁ eigenspaces; on `Eμ`, `fᵏ = μᵏ·id`). This
  power-sum with multiplicity is not in Mathlib and is the reusable linear-algebra
  core.
- `character_ringHom_pow` — **Step 4 core**: `σ(χ_V(g)) = χ_V(gʲ)` for any ring
  endomorphism `σ` of ℂ raising every `|G|`-th root of unity to its `j`-th power.
- `ringHom_prod_char_inv` — combining with Step 2 (`prod_ne_one_pow`): `σ(β) = β`
  for `β = ∏_{g≠1} χ_V(g)·χ_V(g⁻¹)` (honest polynomial form of `∏|χ_V(g)|²`).

Updated the file header docstring and the `Chapter5/Remark5.2.8` note in
`progress/items.json`.

## Current frontier

The eigenvalue/invariance content of Step 4 is done. What remains is the pure
**field-theoretic** half: constructing the automorphisms `σ_j` from
`Gal(ℚ(ζ_N)/ℚ) ≅ (ℤ/Nℤ)ˣ` and deducing `β ∈ ℚ` from "fixed by every `σ_j`" (fixed
field of the full Galois group is ℚ). That needs `IsCyclotomicExtension` / `IsGalois`
infrastructure. Plus the `0 < β < 1` bound (main argument of Problem 5.2.7(b)) is
still unformalized.

## Overall project progress

Phase 3 (formalization), endgame. Remark 5.2.8: Steps 1, 2, 3 (root-of-unity
content), 4 (rep-theoretic core), and 5 + contradiction are all sorry-free; only the
cyclotomic-Galois field theory and the `0<β<1` bound remain (both tracked under
#5772 / #5654).

## Next step

For the field-theoretic half: use Mathlib's `IsCyclotomicExtension`/`CyclotomicField`
and `IsGalois` fixed-field API to (a) obtain, for each `j` coprime to `N`, a ring
endomorphism `σ_j` of ℂ (or of a cyclotomic subfield containing all `χ_V(g)`) with
`σ_j ζ = ζʲ`, then feed `ringHom_prod_char_inv`; (b) conclude `β ∈ ℚ` from being
fixed by the whole Galois group. Separately, formalize the `0 < β < 1` bound.

## Blockers

None for the landed work. The remaining Step 4 half is gated on cyclotomic-field
infrastructure (a substantial standalone development), not on anything broken.
