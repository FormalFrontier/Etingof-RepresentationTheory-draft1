## Accomplished

Worked issue #6449 (Problem 5.24.2, FFT for matrix invariants). This is genuinely deep
(the First Fundamental Theorem of matrix invariants), so I decomposed top-down:

- **Proved the elementary `⊇` inclusion outright**: `traceWord_mem_invariantSubalgebra` —
  each trace-of-word function `T_w` is invariant under simultaneous conjugation. The proof
  shows `conjAlgHom g` conjugates each generic matrix `Xᵢ ↦ G·Xᵢ·G⁻¹` (with `G = g` mapped
  into the coordinate ring), telescopes this over the word via a monoid conjugation-product
  helper `prod_map_conj`, and closes by trace cyclicity (`Matrix.trace_mul_comm`).
- **Reduced the main theorem**: `invariantSubalgebra_eq_adjoin_traceWord` is now a sorry-free
  `le_antisymm` of the elementary inclusion and the deep inclusion.
- **Isolated the deep content** as a named lemma `invariantSubalgebra_le_adjoin_traceWord`
  (the FFT/Schur-Weyl `⊆` inclusion), currently the single remaining `sorry`, with a docstring
  recording the book's `⨂ Sᵈ(V⊗V*) ↪ End(V)^{⊗n}` + Schur-Weyl strategy.

## Current frontier

`EtingofRepresentationTheory/Chapter5/Problem5_24_2.lean` builds with exactly one sorry
(`invariantSubalgebra_le_adjoin_traceWord`, line 134). Created sub-issue #6485 for that
deep inclusion.

## Overall project progress

Stage 3 (formalization) ongoing across Chapters 2/5/9. Problem 5.24.2's setup and elementary
half are now landed; only the FFT/Schur-Weyl core remains (#6485).

## Next step

A worker claims #6485 and proves the FFT/Schur-Weyl deep inclusion, following the book's
route (read `blobs/Chapter5/Problem5.24.2.md` and check for an existing Chapter-5 Schur-Weyl /
double-centralizer result to reuse).

## Blockers

None. #6485 is deep but well-scoped and unblocked.
