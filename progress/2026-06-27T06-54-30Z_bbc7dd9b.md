## Accomplished

Discharged the `Theorem5_23_2_i` `sorry` (#5478) — the honest `GL_n`
complete-reducibility obligation — and broke the import cycle that blocked it.

- **`Theorem5_23_2Core.lean`** (new): factored the `DominantWeight` /
  `AlgIrrepGL` / `AlgIrrepGLDual` definitions (and their instances) together with
  `schurModuleSubmodule_ne_bot` out of `Theorem5_23_2.lean`. These are the only
  names the downstream GL-representation machinery (`AlgIrrepGLRep`,
  `CauchyDetQuotient`, `SchurModuleSpecialBlock`) needs; those three files now
  import `Theorem5_23_2Core` instead of `Theorem5_23_2`. This removes the cycle
  `PolynomialGLSemisimple → … → CauchyDetQuotient → Theorem5_23_2`, so
  `Theorem5_23_2.lean` can finally import the complete-reducibility
  infrastructure.
- **`Theorem5_23_2_i`** is now proved (axiom-clean: `propext`,
  `Classical.choice`, `Quot.sound` only — no `sorryAx`, no `native_decide`) by
  the `det`-twist reduction:
  1. *det-clearing* (`IsAlgebraicRepresentation.exists_detPow_twist_isPolynomial`,
     #5486): some `s` makes `g ↦ det(g)^s • ρ(g)` a polynomial (`det⁻¹`-free) rep;
  2. realize that twist as the genuine representation `charTwistRep (det^s) ρ`
     (`⇑(charTwistRep (detChar k n ^ s) ρ) = fun g => det(g)^s • ρ g`, via
     `MonoidHom.pow_apply` + `Units.val_pow_eq_pow_val`);
  3. *homogeneous splitting + decompose* (`polynomialRep_isSemisimple`, #5487):
     the polynomial twist is `IsSemisimpleModule (k[GL_n]) ·.asModule`;
  4. *untwist* (`isSemisimpleModule_charTwistRep`, #5489): `det^{-s} · det^s = 1`,
     so `charTwistRep (det^s)⁻¹ (charTwistRep (det^s) ρ) = ρ` and semisimplicity
     transfers back to `ρ`.
- The statement is specialized to `k Y : Type` (universe 0), matching the
  polynomial-decomposition machinery (`decompose_polynomial_gl_rep` /
  `polynomialRep_isSemisimple` fix universe 0 and `FDRep`). Documented in the
  theorem docstring. No code (only docstrings) referenced `Theorem5_23_2_i`, so
  the universe specialization breaks nothing.

`lake build` of the whole project succeeds (8975 jobs, 0 errors).
`Theorem5_23_2.lean` and `Theorem5_23_2Core.lean` are sorry-free.

## Current frontier

`Theorem5_23_2_i` is fully proved. The only remaining `sorry` for Theorem 5.23.2
is part (ii): the `GL_n × GL_n`-equivariant Peter-Weyl iso
`Theorem5_23_2_ii_equivariant` (`Theorem5_23_2_PeterWeyl.lean`), whose book proof
(matrix coefficients + Cauchy decomposition) is still sorried.

## Overall project progress

Phase 3 formalization ongoing. Theorem 5.23.2(i) is now a genuine, fully proved
complete-reducibility theorem for finite-dimensional algebraic `GL_n`-reps. The
remaining Theorem 5.23.2 work is the Peter-Weyl (ii) equivariant-iso proof.

## Next step

Discharge `Theorem5_23_2_ii_equivariant` using the matrix-coefficient map
`L*_λ ⊗ L_λ → R` and the Cauchy-decomposition machinery
(`PolynomialGLDecomposition.lean`, `CauchyDetQuotient*`).

## Blockers

None.
