## Accomplished

Completed issue #6471: proved `Etingof.Exercise5_27_2.semidirect_classification` sorry-free
(Roadmap steps 4-6), so `EtingofRepresentationTheory/Chapter5/Exercise5_27_2_Dihedral.lean` now
builds with **zero sorries**. `#print axioms` for both `semidirect_classification` and
`dihedral_classification` shows only `[propext, Classical.choice, Quot.sound]`.

Proof structure (abstract orbit-method assembly on top of Theorem 5.27.1, avoiding all character
formulas — much shorter than the Heisenberg character-computation route):

- **Stabilizer dichotomy** `hstab_top`/`hstab_bot`: `stab χ = ⊤` iff `χ` self-inverse, else `⊥`
  (from `hstab_gen` + the two-element decomposition of `Multiplicative (ZMod 2)`).
- **Free-orbit transversal** `T = {χ // χ ≠ χ⁻¹ ∧ key χ < key χ⁻¹}` using an injective `key`
  from `Fintype.equivFin`; a bijection `T ⊕ T ≃ {χ // χ ≠ χ⁻¹}` gives
  `Nat.card T = (N - gcd 2 N)/2` via `DihedralCharacterCombinatorics.card_freeOrbitPairs`.
- **Index type** `ι = ({χ // χ = χ⁻¹} × (ℤ/2 chars)) ⊕ T`; family `F` = `V χ (charFDRep …)`.
- Six conjuncts: Simple (hi), non-iso (hii + `hviii` central-transport + `charFDRep_iso_iff` +
  transversal/orbit distinctness, dims to separate 1-dim vs 2-dim), completeness (hiii + `hvii`
  base-point move + `hix` + `exists_charFDRep_iso`, with `ρ`-recovery via `MulEquiv.subgroupCongr`
  ∘ `topEquiv` for the self-inverse case), dims (hv), and Odd/Even counts via `card_selfInverse` /
  `card_freeOrbitPairs` (`gcd 2 N = 1`/`2` by parity).

## Current frontier

Issue #6471 done. Two sibling classification issues remain unclaimed: #6490 (affine, Fin q family)
and #6492 (Problem 5.24.2 Schur-Weyl permutation-spanning core).

## Overall project progress

Chapter 5 orbit-method exercises: Heisenberg (#6489) and now Dihedral (#6471) classifications are
fully proved sorry-free. The abstract-conjunct pattern established here (using engine conjuncts
vii/viii/ix rather than character formulas) is a reusable template for the affine sibling.

## Next step

Claim #6490 (affine `Exercise5_27_2_Affine.lean`). It is the simpler two-orbit case; the transversal
machinery here is overkill — the affine dual action has an explicit orbit coordinate like Heisenberg.
The `ρ`-recovery / abstract non-iso / completeness patterns transfer directly.

## Blockers

None.
