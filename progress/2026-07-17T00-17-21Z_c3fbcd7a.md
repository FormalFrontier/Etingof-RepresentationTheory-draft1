## Accomplished

- **Corrected a stale-plan assumption on #6893.** #6893 ("affine_tree_two_branch_iso (B) —
  reattach") was framed as consuming already-landed inputs, but #6881 (which supposedly landed
  the (A) deliverable) was **skipped to replan before any code landed**. Verified in the tree:
  `affine_two_branch_has_leaf` did **not** exist, and `branch_classification` / `tree_branch_iso`
  / `branch_has_leaf_neighbor` were still `private`.
- **Proved `affine_two_branch_has_leaf` sorry-free** (the missing (A) prerequisite) in
  `EtingofRepresentationTheory/Chapter6/Problem6_1_3_continued_tildeE.lean`, just above
  `affine_tree_two_branch_iso`. Found a cleaner argument than the issue's arm-mark route: delete
  **any** leaf → finite Dynkin (`affine_delete_leaf_isDynkin`); a finite Dynkin diagram has at most
  one degree-3 vertex (`dynkin_unique_degree_three`); a branch vertex not adjacent to the deleted
  leaf keeps degree 3, so if the leaf touched neither `v` nor `w` both would remain degree 3 —
  contradiction. Reuses the leaf-existence + `hsubdeg` machinery already inside
  `affine_tree_branch_count`. Axioms: `propext, Classical.choice, Quot.sound`.
- **De-privatized** `branch_classification`, `tree_branch_iso`, `branch_has_leaf_neighbor` in
  `Theorem_Dynkin_classification.lean` so the (B) reattach can consume them cross-file.

## Current frontier

- `affine_tree_two_branch_iso` (~line 2585) still `sorry` — the (B) reattach reindexing onto
  `AffineType.Dtilde`. Sorry count in the affine file is unchanged (3 → 3); no regression.

## Overall project progress

- Stage 3 endgame; Ch6 affine Dynkin classification (#6785 tree case) frontier. This turn landed
  the genuine (A) prerequisite that #6893 wrongly assumed done, plus the cross-file de-privatization,
  leaving (B) as a clean well-scoped residual.

## Next step

The (B) reattach for #6893, now with real inputs available:
1. Leaf `ℓ` adjacent to `v` (WLOG) from `affine_two_branch_has_leaf`; reindex `n = k+1`, delete `ℓ`.
2. `D' := adj.submatrix ℓ.succAbove ℓ.succAbove` is finite Dynkin (`affine_delete_leaf_isDynkin`);
   in `D'`, `v` has degree 2 and `w` is the **unique** degree-3 vertex. Apply
   `Etingof.branch_classification` to graph-iso `D'` to a finite `DynkinType`. Rule out E-types
   (the two length-1 forks at `w` pin it to `D_m`), yielding `σ' : Fin k ≃ Fin k`.
3. Reattach `ℓ` at `v` to rebuild `D̃_m`; build `σ : Fin (Dtilde m).rank ≃ Fin (k+1)` from `σ'`
   plus the reattached vertex, mirroring the finite `tree_branch_iso` index arithmetic (now public).
   This step is the heavy one (~150+ lines of index bookkeeping) and may warrant its own sub-issue.

## Blockers

- None for (A). (B) is unblocked but large; a planner should re-scope #6893 to pure (B) reattach
  (its stated (A) inputs now exist) or split step 3 into its own sub-issue.
