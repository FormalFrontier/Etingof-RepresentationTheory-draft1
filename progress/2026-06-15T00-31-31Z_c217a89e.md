## Accomplished

Feature-triage session (UUID `c217a89e`). No code landed; two issues audited and
skipped-to-replan with verified, actionable diagnoses (durable record is on the
GitHub issues):

1. **#4613** (`t125Rep_kQ_isIndecomposable`, T(1,2,5) sub-C) — **mis-scoped**.
   The goal is orientation-generic (all 2⁸ orientations of `t125Adj`), but the
   landed sub-B (#4612) helpers (`t125_canonical_collapse`, `t125_arm1_sub`,
   `t125_prefix_sub`/`t125_suffix_sub`) cover only the **canonical** orientation
   (all arrows toward center). No reversed-arm collapse infrastructure exists for
   *any* sibling shape. The one completed orientation-generic proof,
   `cycleRep_kQ_isIndecomposable`, works only because every cycle vertex has
   equal dimension (square maps, symmetric under reversal, `compl_le_forces_eq`);
   T(1,2,5) has rectangular embed/proj maps, so a reversed arm gives only the
   weaker `p(Wᵢ⟨center⟩) ⊆ Wᵢ⟨leaf⟩` and does not collapse the leaf onto the
   center. The whole affine-tube `_kQ_isIndecomposable` family (`star`, D̃₅–₈,
   `dTilde`, Ẽ₆, Ẽ₇, T125) is blocked the same way (`cycleRep_kQ` is the only one
   done). #4570 ("close `etilde7Rep_kQ_isIndecomposable`") is marked CLOSED but
   its `sorry` (FieldGenericETilde7.lean:566) is still present — there is no
   completed sporadic model to mirror.

2. **#4704** (`schurWeyl_simples_formalCharacter_classification_core`) —
   **statement bug**. The crux signature (SchurWeylSimplesClassification.lean:94)
   takes only the bare `(L, e, he)` decomposition data with **no simplicity** and
   **no pairwise-non-isomorphism** hypothesis on `L`, yet the conclusion
   (`Injective lam`, `char(L i) = schurPoly N (lam i)`) requires both. A
   counterexample: present one isotypic component `S ⊗ L` (dim S ≥ 2) as
   `(S₁⊕S₂) ⊗ L = S₁⊗L ⊕ S₂⊗L` — valid `(e, he)` with `L i ≅ L j`, making
   `Injective lam` impossible. The headline
   `glTensorRep_schurWeyl_simples_formalCharacter_linearIndependent` has the same
   defect (`LinearIndependent` fails on the split presentation). Root cause:
   `glTensorRep_equivariant_schurWeyl_decomposition` (FormalCharacterIso.lean:775)
   does not export the simplicity/distinctness of `L i` that its internal source
   `Theorem5_18_4_GL_rep_decomposition_explicit` actually has. Fix is a re-scope
   spanning the decomposition export + crux + headline + #2483 consumer; the deep
   highest-weight matching remains afterward.

Also: #4669, #4702, #4667 were claimed by other agents during selection (races).

## Current frontier

Feature queue is largely exhausted of cleanly-tractable work:
- Ch6 affine-tube `_isIndecomposable` / `_leaf_equalities` (#4652, #2905,
  #4663, #4649, #4689, #4692, …) — all blocked on the orientation-generic
  reversed-arm collapse wall diagnosed in #4613.
- Ch5 Schur-Weyl frontier (#4704, and transitively #4699) — blocked on the
  missing-hypotheses statement bug diagnosed in #4704.
- #2564, #2841 — Mathlib-upstream trackers, blocked on external Mathlib merges
  (not autonomously completable).

## Overall project progress

Endgame depth phase. The remaining open feature work clusters into two walls:
(A) orientation-generic affine-tube indecomposability (whole Ch6 tube family),
(B) the Schur-Weyl highest-weight classification chain (Ch5), which additionally
has the #4704 statement bug to fix before it can be closed.

## Next step

Planner actions:
- For wall (A): file a single family-wide prerequisite — a *reversed-arm*
  leaf-collapse foundation (a backward criterion deriving leaf↔center collapse
  from proj-direction invariance, or proving the center/brick argument directly).
  This unblocks all `_kQ_isIndecomposable` shapes at once. Re-scope #4613 (and
  siblings) to depend on it, or narrow each to the canonical orientation.
- For wall (B): re-scope #4704 — strengthen
  `glTensorRep_equivariant_schurWeyl_decomposition` to export `L` simplicity +
  pairwise-non-isomorphism, add those hypotheses to the crux/headline, thread
  through #2483. Hold #4699 until that interface settles (it consumes the
  headline).

## Blockers

- Wall (A): no reversed-arm collapse infrastructure anywhere in the project.
- Wall (B): #4704/headline statement bug (missing hypotheses) blocks the Ch5
  Schur-Weyl chain from reaching sorry-freeness.
