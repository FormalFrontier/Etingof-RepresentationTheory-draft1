## Accomplished

Filled the C.short residual sorry (issue #2963) at
`Chapter6/FieldGenericNonAdjacentBranches.lean:707` with the
tractable case-split:

* **C.short.3** (`vertexDegree adj x = 3`) — Ẽ₇ T(1,3,3) dispatch at
  v₀ via `embed_etilde7_in_tree_per_kQ`. Extracts y₁ as any
  non-side_arm neighbour of x (`Sx.erase side_arm` is nonempty since
  `Sx.card = 3`).
* **C.short.1 / chain.length = 5** — T(1,2,5) at v₀ via
  `embed_t125_in_tree_per_kQ`. Length-5 arm uses
  chain[1..3] + chain[4]=w + arm₁.
* **C.short.1 / chain.length = 4 / arm₁.deg = 2** — T(1,2,5) at v₀.
  Extracts z₁ as arm₁'s non-w neighbour. Length-5 arm uses
  chain[1], chain[2], w, arm₁, z₁.
* **C.short.1 / chain.length = 4 / arm₂.deg = 2** — symmetric via arm₂.
* **All-leaves residual** (chain.length = 4 ∧ x.deg = 1 ∧
  arm₁.deg = arm₂.deg = 1) — left as a documented `sorry` with a
  TODO breadcrumb pointing at #2964 (the D̃₇ per-(F, Q) helper).

The chain.length = 5 dispatch reuses the standard
`have h_nat : (4 : ℕ) = chain.length - 1 := by omega` /
`Fin.ext h_nat` pattern from existing Phase 1 code (e.g. line 218)
instead of the inline `Fin.ext (by omega)`; the inline form fails
because `omega` does not unfold `Fin.val` of fixed-literal indices.

## Current frontier

`Chapter6/FieldGenericNonAdjacentBranches.lean` builds with exactly
two documented sorries:

* `:884` — new all-leaves residual, blocked on #2964
  (`d7tilde_not_finite_type_per_kQ` per-(F, Q) helper).
* `:958` — pre-existing Case E sorry, tracked by #2955.

Full Chapter6 (`lake build EtingofRepresentationTheory.Chapter6`)
builds clean.

## Overall project progress

Phase 2 of `non_adjacent_branches_leaf_case_per_kQ` is now
near-complete:

* Case A (chain.length ≥ 6, side_arm extends) — landed earlier.
* Case B (chain.length ≥ 6, arm₁ or arm₂ extends) — landed in #2958.
* Case C.main (4 ≤ chain.length < 6, side_arm extends, x.deg = 2) — landed in #2961.
* Case C.short — **this PR** (residual is one all-leaves sorry).
* Case D (4 ≤ chain.length < 6, both arms extend) — landed in #2961.
* Case E (chain.length = 3 + uncovered configurations) — still open as #2955.

## Next step

Two remaining feature items in the Case-C/E vicinity:

1. **#2964** — construct `d7tilde_not_finite_type_per_kQ` (the D̃₇
   per-(F, Q) helper). Unblocks the all-leaves residual sorry at
   `:884`.
2. **#2955** — Case E (chain.length = 3) + uncovered all-leaves
   configurations at chain.length ≥ 6. May overlap with #2964.

Once #2964 lands, the all-leaves residual at `:884` can be filled
with a single embedder dispatch (D̃₇ as an induced subgraph of the
8-vertex host).

## Blockers

None for this PR. Downstream all-leaves residual fill is blocked on
#2964.
