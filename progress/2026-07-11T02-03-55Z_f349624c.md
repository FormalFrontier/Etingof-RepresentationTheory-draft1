## Accomplished

Completed issue #6367 (Ch8 Problem 8.2.6): the bar-differential square-zero relation and the
assembled bar chain complex, all sorry-free, in
`EtingofRepresentationTheory/Chapter8/BarResolution.lean`.

New declarations (all proved, `#print axioms` shows only `propext, Classical.choice, Quot.sound`):

- `barFace_comp_barFace` — the simplicial identity `barFace i ∘ barFace j = barFace (j-1) ∘ barFace i`
  (`i < j`), the combinatorial core of `d ∘ d = 0`.
- `barDiff_comp_barDiff` — `d ∘ d = 0`, via the sign-reversing bijection
  `φ(i,j) = (castLT j, i.succ)` on the index set (mirrors Mathlib's
  `AlternatingFaceMapComplex.d_squared`), element-level.
- `barComplex : ChainComplex (ModuleCat A) ℕ` — the assembled complex.

Supporting `Fin.contractNth`/`cons`/`snoc` lemmas added:
- `contractNth_succ_cons`, `contractNth_zero_cons` — contracting `Fin.cons a g`.
- `barFace_castSucc_apply` — **uniform description of a merge face** as a single `Fin.contractNth`
  on `Fin.cons a₀ v`. This is what lets the merge-merge case of the simplicial identity reduce to the
  pre-existing `contractNth_contractNth`.
- `contractNth_castSucc_eq_snoc` — a non-last merge commutes with dropping the last entry.
- `contractNth_lastMerge_eq_snoc` — the last-pair merge keeps the leading block and appends a product.

## Current frontier

Issue #6367 is fully done. The follow-up work named in the file header (exactness via the contracting
homotopy `s(x) = 1 ⊗ x`, and packaging `barComplex` as a
`CategoryTheory.ProjectiveResolution (ModuleCat.of A W)`) is not yet started — that is the natural
next Ch8 step and would unblock computing `Ext_A` / `Problem_8_2_6_ii`.

## Overall project progress

Phase 3 (formalization) ongoing across chapters. Ch8 bar resolution now has: projective terms +
augmentation (#6336), face primitives + differential + faces-as-alternating-sum (prior), and now the
chain-complex structure (`d∘d=0`, `barComplex`) from this issue.

## Next step

Open/claim a follow-up issue to (a) prove the bar complex is exact using the `k`-split contracting
homotopy, then (b) build the `ProjectiveResolution` instance. Both build directly on `barComplex`.

## Blockers

None. Note: three pre-existing uncommitted edits to `.claude/commands/feature.md`,
`.claude/commands/review.md`, `.claude/skills/agent-worker-flow/SKILL.md` (skill-content deletions,
not mine) were `git stash`ed at the start of this session to keep them out of this feature branch;
they remain in the worktree stash.
