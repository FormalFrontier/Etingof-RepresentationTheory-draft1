## Accomplished

Worked issue #4881 (Ch5: complete `garnir_twisted_in_lower_span`, the last hole
in the Specht standard-basis development). **Did not** fully close it — the
genuine mathematical crux is a James-chapter column-straightening theorem that
has resisted 6+ prior sessions and is not a single-session deliverable. Instead
landed the high-value architectural step that the validated route (route 1 in
the issue) needs, reducing the difficulty-8 monolith to exactly its validated
crux with all surrounding scaffolding proven sorry-free.

In `Chapter5/SpechtModuleBasis.lean`:

1. **Threaded the inductive hypothesis through the Garnir straightening.**
   `garnir_twisted_in_lower_span` and `garnir_straightening_step` previously had
   **no** `ih` parameter, yet proving `f_w(σ) ∈ V` provably requires the lex
   `(srRank, rowInvCount')` IH (to discharge the peeled polytabloids). This was
   the architectural gap that confused prior sessions. Both lemmas now take the
   standard `ih`, and the master induction `polytabloidTab_column_standard_in_span`
   constructs it from `ih_outer`/`ih_inner` (same packaging already used in its
   `h_sub` block) and passes it down.

2. **`garnir_twisted_in_lower_span` is now sorry-free.** Proof:
   `f_w(σ) = twistedIHPart + Δ` via `twistedPolytabloid_per_q_decomp`;
   `twistedIHPart ∈ V` via `twistedIHPart_mem_span`; `Δ ∈ V` via the new
   `twistedPolytabloid_residual_in_V` (the single remaining `sorry`); hence
   `f_w(σ) ∈ V`. Support bound from `twistedPolytabloid_support_bound`. The R1
   bridge `in_L_of_in_V_of_supp_bounded` lifts `V` + support-bound to the lower
   span `L_σ`. The `w` "neither column- nor row-preserving" hypotheses turn out
   to be unnecessary for this route (V-membership is general); they are kept in
   the signature (underscored) so the call site is unchanged.

3. **`garnir_straightening_step` is now sorry-free** (passes `ih` through).

4. **New lemma `twistedPolytabloid_residual_in_V` — the single, sharply-scoped
   remaining `sorry`** (`SpechtModuleBasis.lean:2177`): for col-std σ, any `w`,
   and the standard `ih`, `(f_w σ − twistedIHPart σ w) ∈ V`. Its docstring
   states the validated route (leading-term elimination + corrected invariant)
   and points at the refuted-routes docs.

Full build green. Sorry count in the file unchanged at 1 — but **relocated and
sharpened** from a vague difficulty-8 statement to the precise validated crux,
with the IH now available to whoever proves it.

## Current frontier

`twistedPolytabloid_residual_in_V` (`Δ ∈ V`). This is the genuine crux. The
validated route (route 1, issue #4881):
- extend `exists_dominance_maximal` (`TabloidModule.lean:855`, currently SYT
  finsets only) to a general `Finsupp (Tabloid n la) ℂ` support;
- prove the corrected invariant: for col-std σ, any w, any `M ∈ V` with
  `supp(f_w σ − M) ≼ [σ]`, every dominance-maximal tabloid of `X := f_w σ − M`
  is column-standardizable with an IH-available rep `β'` (`[β'] ≼ [σ]`);
- leading-term elimination: subtract `c·ψ_{β'}` (discharged by `ih`), recurse;
  termination measure lex `(maxSrRankOfSupport X, cardOfSupportAtMaxSrRank X)`.

## Overall project progress

Chapter 5 Specht standard-basis straightening. The entire Garnir straightening
chain is now sorry-free **except** the one validated-true residual lemma. The
master straightening theorem `generalizedPolytabloidTab_mem_span_polytabloidTab`
(public API, unchanged) and all downstream consumers compile.

## Next step

Prove `twistedPolytabloid_residual_in_V` via route 1. Start by extending
`exists_dominance_maximal` to general tabloid `Finsupp` support (reusable,
standalone, sorry-free), then the corrected invariant, then the elimination.
Do NOT re-attempt the refuted routes (see the docstring + the three
`progress/r2b-*` docs).

## Blockers

None at the architecture level — the IH is now threaded and available. The
remaining work is the genuine column-straightening combinatorics, isolated in
one clearly-scoped lemma.
