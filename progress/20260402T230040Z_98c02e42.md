## Accomplished

### Fixed broken main branch (PR #2039)
- PR #2033 was auto-merged before CI completed, introducing forward reference errors
- Fixed ordering: moved `charValue_extend_zero`, `wtp_dropLast`, `wtp_extend` AFTER `charValue_remove_trailing_zero`
- Fixed ordering: moved `canonicalBP_eq_of_weightToPartition_eq` BEFORE `charValue_reduce_to_n`
- Fixed API name: `Multiset.coe_filter` → `Multiset.filter_coe`
- Sorry'd 3 proofs with API compatibility issues pending proper fixes

### charValue chain is correctly ordered
The full chain `charValue_remove_trailing_zero → charValue_extend_zero → wtp_dropLast → wtp_extend → canonicalBP_eq_of_weightToPartition_eq → charValue_reduce_to_n → charValue_stability` is now in correct dependency order.

## Current frontier

4 sorries remain in `Theorem5_22_1.lean`:

| Line | Lemma | Difficulty | Dependency |
|------|-------|-----------|------------|
| 787 | `finrank_weight_eq_card_sum` | Very hard | Independent (needs trace/IsProj infrastructure) |
| 1492 | `coeff_restrictLastVar` | Medium | Independent |
| 1500 | `restrictLastVar_alternantDet` | Hard | Independent (needs matrix det manipulation) |
| 1550 | `charValue_remove_trailing_zero` | Medium | Depends on 1492 + 1500 |

## Overall project progress

- Issue #2021: "Prove Theorem5_22_1 remaining sorries"
- 3 of 7 original sorries proved (restrictLastVar_psum, restrictLastVar_psumPart, charValue_reduce_to_canonical chain)
- New chain proofs added: charValue_extend_zero, wtp_dropLast, wtp_extend, charValue_reduce_to_n
- PR #2039 submitted to fix broken main, auto-merge enabled

## Next step

1. **`coeff_restrictLastVar`** — The proof was written previously but had API issues. Key approach: `MvPolynomial.induction_on'` (monomial induction), `Finsupp.prod_fintype`, `Fin.prod_univ_castSucc`. The `constructor` tactic after `congr 1` needs to be replaced with proper `ext` + `simp` for Finsupp equality.
2. **`restrictLastVar_alternantDet`** — Use `AlgHom.map_det`, `Matrix.det_succ_row (Fin.last N)`, then `Matrix.det_mul_column`.
3. **`charValue_remove_trailing_zero`** — Follows from the above two + exponent shifting via `coeff_prod_X_mul`.

## Blockers

- Disk space critically low (~100MB free) — cannot run `lake build` locally. CI verification only.
