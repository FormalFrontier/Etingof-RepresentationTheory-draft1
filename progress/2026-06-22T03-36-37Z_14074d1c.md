## Accomplished

Issue #5005 (Ch5 #4946 sub-C2): added the general-`k` (`[Field k] [IsAlgClosed k]
[CharZero k]`) analogues of the special-block assembly in
`Chapter5/SchurModuleSpecialBlock.lean`, alongside the existing ℂ versions:

- `youngSymEndomorphism_ne_zero_general` — Young symmetrizer endomorphism on
  `(k^N)^{⊗n}` nonzero for antitone `lam` (via `schurModuleSubmodule_ne_bot (k := k)`).
- `youngSymEndomorphism_eq_zero_of_blocks_vanish_general` (private) — block
  factorization forces the endomorphism to zero when it vanishes on every block.
  Built on the already-generic `youngSym_block_factorization`.
- `exists_unique_special_block_general` — the unique `weightToPartition N lam`-labelled
  Schur-Weyl block over `k`.

Key facts used:
- Bridge `trace_symGroupAction_eq_spechtModuleCharacterK` + iso wrapper
  `simpleSymGroupImageSubmodule_iso_of_spechtCharacterK_eq` (#4992, `SpechtBridgeGeneral.lean`).
- Off-block vanishing `youngSym_action_vanishes_off_block_general` (#5004,
  `SchurWeylSpecialBlockGeneral.lean`).
- `spechtModuleCharacterK` (bridge) and `spechtBlockCharacterK` (off-block) are
  definitionally equal, so the bridge's per-block label hypotheses feed the
  off-block lemma's `h_label` directly with no conversion.

Added two imports (`SpechtBridgeGeneral`, `SchurWeylSpecialBlockGeneral`); confirmed no
import cycle. ℂ call-sites (`SchurModuleSimple.lean`) left untouched and still build.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5.SchurModuleSpecialBlock` green, no
new `sorry`. ℂ consumer `SchurModuleSimple` rebuilt green. Only residual warning on
new code is the `[Fintype ι]` unused-in-type lint, intentionally mirroring the ℂ
`exists_unique_special_block` statement shape for downstream consumer #4976.

## Overall project progress

Stage 3 (Chapter 5) general-`k` Schur-Weyl special-block track: sub-C1 (#5004),
sub-C2 (this), and the #4992 bridge are now all landed. This unblocks #4976
(general-`k` `schurModule_isSimple_general` assembly + resolve hN).

## Next step

Pick up #4976 (was blocked on #5005): general-`k`
`schurModule_isSimple_general` assembly, consuming `exists_unique_special_block_general`.

## Blockers

None.
