## Accomplished

Worked issue #4923 (assemble `quotDetTwist_nonzero_subrep_has_neg_weight`).

- **Assembled the genuine core of (K′).** The full 6-step proof of
  `quotDetTwist_nonzero_subrep_has_neg_weight` is now in place and compiles:
  untwist `W` (the `χ⁻ʳ` twist only rescales by a unit, so `W.toSubmodule` is
  `quotDetRep`-invariant) → extract a simple constituent `L`, `φ`
  (`exists_simple_subrep_of_quotDetRep`, #4922) → `ν_N = 0` from part (a)
  (`quotDetRep_irreducible_constituent_lastWeight_zero`, #4896) → highest-weight
  vector `w` from the multiplicity-one coefficient → push `w` through `φ` into
  `glWeightSpaceℤ (quotDetRep) (↑ν)` by equivariance → twist-shift
  (`glWeightSpaceℤ_quotDetTwist`) to land in `glWeightSpaceℤ (quotDetTwistRep) (↑ν − r)`
  with last coordinate `0 − r = −r < 0`.

- **Resolved the import cycle by relocation.** Both dependency files
  (`CauchyDetQuotient`, `SimpleSubrepExtraction`) import `KernelLemmaKPrime`, so
  the assembly cannot live there. Created
  `Chapter5/KernelLemmaKPrimeAssembly.lean` (imports both deps) holding the core
  plus its consumers `kernelLemmaK'` and `kernelLemmaK'_submodule`, moved out of
  `KernelLemmaKPrime.lean`. Updated `KernelLemmaK.lean` (sole consumer of
  `kernelLemmaK'_submodule`) and `Chapter5.lean` to import the new file.

- **`KernelLemmaKPrime.lean` is now sorry-free** (the relocated core's `sorry`
  left the file). Full library `lake build` confirms no downstream breakage.

- **Filed sub-issue #4949** for the one isolated remaining hole.

## Current frontier

`schurPoly_coeff_self_ne_zero` (`KernelLemmaKPrimeAssembly.lean`, ~line 67) is the
lone new `sorry`: `(schurPoly N lam).coeff lam ≠ 0` for antitone `lam`
(highest-weight multiplicity one, Kostka `K_{λλ} = 1`). It is the only nontrivial
mathematical content the assembly needed; everything else is wired sorry-free.
Tracked as #4949 with a full leading-monomial proof route.

## Overall project progress

Stage 3 (formalization). The kernel-lemma-K′ program is nearly closed:
`KernelLemmaKPrime.lean` sorry-free; the (K′) core assembled modulo the part-(a)
`CauchyDetQuotient` sorry (#4896, separate) and the new schurPoly leading-coeff
sorry (#4949). Submitting #4923 as a partial PR.

## Next step

Pick up #4949: prove `schurPoly_coeff_self_ne_zero` via the monomial-order
leading-term argument (degree of an alternant det = its sorted exponent vector;
`degree_mul` on `schurPoly · Δ = D_{lam+δ}`). Once it lands,
`quotDetTwist_nonzero_subrep_has_neg_weight` becomes sorry-free (modulo #4896).

## Blockers

None. #4949 and #4896 are sorry'd dependencies, not blockers.
