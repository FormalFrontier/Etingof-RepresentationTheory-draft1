## Accomplished

Audited PRs #2966 (Case C.short tractable sub-cases), #2968
(per-(F, Q) D̃₇ helper), and #2970 (Case C.short all-leaves
residual via D̃₇ dispatch) under issue #2973. Produced the five-
dimension audit report at
`progress/reviews/2026-05-19-case-c-short-and-d7tilde-helper.md`.

Verdict per dimension:
- **D1** (D̃₇ module fidelity to D̃₅ precedent, PR #2968): PASS
- **D2** (Case C.short dispatch coverage, PR #2966): PASS
- **D3** (`embed_d7tilde_in_tree_per_kQ` + dispatch, PR #2970): PASS
- **D4** (build sanity, all three PRs): PASS (`lake build` green
  with zero errors over 8049 jobs; net `+1` sorry across the three
  PRs, namely `d7tildeRep_kQ_isIndecomposable` tracked by #2967)
- **D5** (cross-file consistency): CONCERN — file docstring at
  `FieldGenericNonAdjacentBranches.lean:28-33` still omits the new
  D̃₇ helpers from its enumeration, already flagged by the prior
  api-stub review. Non-blocking.

No code changes pushed. All findings recorded in the review file.

## Current frontier

Issue #2973 closed by this audit (PR pending). Sibling unclaimed
audits in flight:
- #2980 — Case E.aa + E.s1c4 partial coverage (PR #2979)
- #2564 — Mathlib upstream tracker (blocked on
  https://github.com/leanprover-community/mathlib4/pull/38583)

Downstream of `non_adjacent_branches_leaf_case_per_kQ`:
- #2967 (fill `d7tildeRep_kQ_isIndecomposable`)
- #2974/#2976/#2977/#2978 (Case E sub-residuals)

## Overall project progress

Chapter 6 forbidden-subgraph library now includes D̃₇ (alongside
D̃₅, Ẽ₆, Ẽ₇, T(1,2,5)). Case C of
`non_adjacent_branches_leaf_case_per_kQ` is fully closed at the
leaf level (subject to the pre-existing
`d7tildeRep_kQ_isIndecomposable` deferred-sorry). Case E remains
partially open per #2974/#2976/#2977/#2978.

## Next step

Pick up the sibling audit (#2980 — Case E.aa + E.s1c4) or proceed
with one of the Case E sub-issues if planner has unblocked them.

## Blockers

None for this audit. Externally, #2564 remains blocked on Mathlib
PR 38583.
