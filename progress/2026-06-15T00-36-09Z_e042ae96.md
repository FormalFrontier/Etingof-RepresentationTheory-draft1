## Accomplished

Issue #4649 (D̃₆ corrected homogeneous-tube), **sub-A landed** (corrected
construction). Mirrors the D̃₅ sub-A pattern from #4647 /
`progress/dtilde-tube-redesign-design.md`:

- `d6tildeRepMap_kQ` now takes a `(lam : F)` parameter; the central edge
  `{2,3}` carries the corrected full-rank eigenvalue-site iso
  `d5tildeGammaTube_F F lam m` / `d5tildeGammaTubeInv_F F lam m`
  (`Λ = λ·id + J`, `jordanShiftLinGen`) instead of the refuted
  rank-deficient `d5tildeGamma_F = [[I,I],[I,N]]`. The other central edge
  `{3,4}` stays the honest `LinearMap.id` pass-through.
- `d6tildeRep_kQ` gains `[IsAlgClosed F]` and threads
  `d5tildeTubeLam F` (the generic `λ ∉ {0,1}`). It is a real `def`.
- `[IsAlgClosed F]` propagated to every declaration that mentions the rep
  (`d6tildeRep_kQ_dimVec` and the leaf-collapse/core helpers
  `d6tildeRep_kQ_chain_collapse`, `d6tilde_core_F`, `d6tilde_core4_F`,
  `d6tilde_core_F_proj1/2`, `d6tilde_core4_F_proj1/2`,
  `d6tilde_gamma_containment_F`). `isIndecomposable`,
  `not_finite_type_per_kQ`, `embed_..._in_tree_per_kQ` already had it.
- `d6tildeRep_kQ_dimVec` statement unchanged (dim still `d6tildeDim m`).
- `d6tildeRep_kQ_leaf_equalities` and `d6tildeRep_kQ_isIndecomposable`
  **restated against the corrected rep** with deferred `sorry` bodies (the
  old partial leaf-equalities proof was tied to the refuted `d5tildeGamma_F`
  containment helpers and would have been false in the mixed/reversed
  branches). Docstrings updated to point at sub-B / sub-C.
- `d6tilde_not_finite_type_per_kQ` re-pointed (statement unchanged).

Builds: `FieldGenericD6Tilde` green with exactly 2 (restated) sorries;
downstream `FieldGenericNonAdjacentBranches` and `FieldGenericD8Tilde`
build green.

## Current frontier

Corrected D̃₆ object is in place. The two restated lemmas
(`d6tildeRep_kQ_leaf_equalities`, `d6tildeRep_kQ_isIndecomposable`) are now
**true** on the corrected rep but carry `sorry` bodies.

## Overall project progress

Stage 3 (formalization) ongoing. D̃ affine family tube redesign: D̃₅ sub-A
done (corrected `d5tildeGammaTube_F` etc.), D̃₅ sub-B (#4692) and sub-C
(#4663) in progress. D̃₆ sub-A done this turn; sub-B/sub-C filed as
follow-ups.

## Next step

Pick up D̃₆ **sub-B** (orientation-generic `d6tildeRep_kQ_leaf_equalities`
on the corrected rep) once the D̃₅ sub-B (#4692) pattern lands, then
**sub-C** (`d6tildeRep_kQ_isIndecomposable` assembly), mirroring the D̃₅
sub-C (#4663). The leaf-collapse helpers are already in place and reusable;
the central-edge transport is rewritten around `gammaTube_from_embed1_F` /
`gammaTube_from_embed2_F` and the `Λ`-geometric-series retractions.

## Blockers

None for sub-A. Sub-B/sub-C are best done after the D̃₅ sub-B/sub-C
patterns (#4692 / #4663) establish the eigenvalue-site collapse template.
