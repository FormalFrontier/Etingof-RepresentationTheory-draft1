## Accomplished

Worked issue #6230 (Ch4 Problem 4.12.5 part (b): `faces_decomposition`).

- Proved `faces_decomposition` in `EtingofRepresentationTheory/Chapter4/Problem4_12_5.lean`,
  removing its sole `sorry`. The theorem: the icosahedral face action of `A₅` (any transitive
  `A₅`-action on `Fin 20` with order-`3` point stabilizers) decomposes `Fin 20 → ℂ` as an
  internal direct sum of six irreducible summands of dimensions `1, 3, 3, 4, 4, 5`
  (`1 ⊕ 3 ⊕ 3' ⊕ 4² ⊕ 5`), with the two dim-`3` summands non-isomorphic (`subChar` witness at
  the 5-cycle class).
- Reused the landed machinery exactly as the issue described: `exists_sorted_isotypic_decomposition`
  (sorted engine), `fixCount_faces` (fixed-point character `![20,2,0,0,0]`), `sum_classfn` +
  `fixCount_conj` for the class-sum collapse, and `monotone_initial` to pin the monotone type
  vector `![0,1,2,3,3,4]` from its fibre cardinalities.
- Structure follows the already-proved `edges_decomposition` (the other repeated-type part).
  Adapted constants: multiplicity `⟨χ_perm,χ_i⟩ = ![1,1,1,2,1]` (verified by hand), `m = 6`,
  down-set cumulatives `![1,2,3,5,6]` / strict `![0,1,2,3,5]`.
- Added the `open Finset Etingof.Example4_8_1 Etingof.Example4_8_1.A5 CategoryTheory in` prefix
  (matching `edges_decomposition`) so `card_A5`, `Q5.*` names resolve.

## Verification

- `lake build EtingofRepresentationTheory.Chapter4.Problem4_12_5` succeeds (8594 jobs).
- No `sorry` in the file (only a docstring mentions the word).
- `#print axioms faces_decomposition` → `[propext, Classical.choice, Quot.sound]` (axiom-clean).

## Current frontier

Problem 4.12.5 parts (a) vertices and (b) faces are now sorry-free. Part (b) edges
(`edges_decomposition`) was already proved. The three per-part decomposition theorems are all
complete.

## Overall project progress

Stage 3 formalization ongoing. Problem 4.12.5 fully proved (all three decomposition theorems
sorry-free and axiom-clean).

## Next step

Pick up another unclaimed feature issue (e.g. #6311 Ch5 5.16.3a eigenbasis/unitarizability, or
#6312 Ch6 6.1.6a McKay symmetry). #6286 (5.16.3b) remains blocked on module-level branching.

## Blockers

None.
