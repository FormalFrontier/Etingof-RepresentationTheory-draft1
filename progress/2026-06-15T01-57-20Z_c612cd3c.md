## Accomplished

Partial progress on #4755 (close the 4 residual reversed-orientation `sorry`s of
`d5tildeRep_kQ_isIndecomposable`, `Chapter6/FieldGenericD5Tilde.lean`).

- Added a reusable uniform collapse helper **`center_collapse_F`**: a center
  vertex (`v=2` leaves `0,1`; `v=3` leaves `4,5`) collapses to `⊥` once both its
  leaves are `⊥`, for *any* orientation of its two incident leaf edges. Each leaf
  slot supplies either a canonical `W'`-push (`Or.inl`) or a reversed `W`-pull
  (`Or.inr`); `center_decomp_F` then forces `w ∈ Wc ⊓ Wc' = ⊥`. This packages the
  per-orientation `v=2`/`v=3` collapse boilerplate that was previously written
  inline in every branch.
- **Closed deliverable 1's tractable half**: the `e43`-reversed `sorry` is now a
  case-split on the free `e53` edge. The **`e43`-reversed + `e53`-reversed**
  orientation is fully proven (deposit via canonical center `γ_λ` + the reversed
  `3→5` projection `d5tilde_core3_F_proj2`, exactly the `e53`-reversed pattern
  from #4753 since the deposit is insensitive to `e43`'s direction; both-reversed
  `v=3` collapse via `center_collapse_F`).
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde` succeeds
  (8043 jobs). Only pre-existing `linter.style.show` warnings remain (the
  all-canonical and `e53`-reversed branches use the same `show … = ⊥` idiom).

## Current frontier

`d5tildeRep_kQ_isIndecomposable` still carries 4 `sorry`s, all genuinely blocked
by machinery not yet built (count unchanged: the `e43` `sorry` became the
combo-C `sorry`):

1. **combo-C** — `e02,e12,e23` canonical, `e43` reversed (`3→4`), `e53` canonical
   (`5→3`). The canonical central `γ_λ` deposits the twist `Λx` in the embed-2
   component at center 3, but the only `v=3` extraction route is the reversed
   `e43 = 3→4` projection (`d5tilde_core3_F_proj1`), which reads embed-1 (`x`, no
   twist); the canonical `e53` edge pushes *into* center 3 and cannot extract.
   **Route identified for a successor**: apply `d5tilde_centralCanon_untwist_core_F`
   (central is canonical here) to `w = starEmbed1 x + starEmbed2 (Λx) ∈ W⟨3⟩`
   with `p = x, q = Λx`. It yields `K(x − Λx) ∈ W⟨0⟩` with `K = eigTailSumLin`.
   The remaining gap is the **joint eigenvalue-fold**: turning this `K`-deposit
   (`K∘((1−λ)·id − N)`-invariance of `W⟨0⟩`) into the `Λ = λ·id + N`-invariance
   that `eigenvalue_jordan_invariant_compl_trivial_gen` consumes. (line ~1696)
2. **`e23` reversed** (central edge carries `γ_λ⁻¹`): 4 orientations. Deposit must
   route center 3 → center 2 through `d5tildeGammaTubeInv_F` /
   `gammaTubeInv_embed_general_F`; the resulting `K`-deposit needs the same
   eigenvalue-fold bridge as combo-C. (line ~1797)
3. **`e12` reversed** (`2→1`): 8 orientations. With `e23` canonical the standard
   deposit has no `starEmbed2`-injection into center 2 (the `1→2` push is gone);
   with `e23` reversed it needs the `γ_λ⁻¹` route. (line ~1799)
4. **`e02` reversed** (`2→0`): 16 orientations. Same obstruction family. (line ~1801)

## Overall project progress

Stage 3 (formalization) of Chapter 6 D̃₅. `d5tildeRep_kQ` and its dimension
vector are real objects. `d5tildeRep_kQ_isIndecomposable` now proves the
all-canonical, `e53`-reversed, and `e43`+`e53`-reversed orientations; the 4
residual branches above remain. `d5tildeRep_kQ_leaf_equalities` (transitive,
non-blocking) still has its own 5 `sorry`s (#4692/#4743).

## Next step

The single highest-leverage piece of new infrastructure is the **eigenvalue-fold
lemma**: `W` invariant under `eigTailSumLin F lam m ∘ ((1−λ)·id − N)` ⟹ `W`
invariant under `jordanShiftLinGen F lam m` (`= λ·id + N`). It unlocks combo-C
(via `d5tilde_centralCanon_untwist_core_F`) *and* the `e23`-reversed family in
one stroke, after which `e12`/`e02`-reversed reduce to routing + `center_collapse_F`.
Build it as its own lemma next to the eigenvalue workhorse, then revisit #4755.

## Blockers

None hard — the remaining branches are research-level but the route is mapped
above. No dependency or definition-level blocker.
