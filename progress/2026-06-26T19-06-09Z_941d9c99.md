## Accomplished

One-shot dispatch on #5358 (fidelity gap: Chapter2/Example2.3.14). Closed the substantive
gap in part (2): the Jordan-block representations of `k[x]` were entirely absent; only the
1-dimensional irreducible sub-claim was formalized.

Added to `EtingofRepresentationTheory/Chapter2/Example2_3_14.lean` (namespace
`Etingof.Example_2_3_14`):

- `jordanBlock lam n` — the Jordan block `J_{λ,n}` on `Fin n → k` as a genuine linear map
  (`λ • id + shift`, with `shift` the nilpotent down-shift).
- `jordanRep lam n := Module.AEval' (jordanBlock lam n)` — the representation `V_{λ,n}` as a
  genuine `k[X]`-module (definition constructed, not sorried).
- `jordanRep_indecomposable` — **`V_{λ,n}` is indecomposable** (for `n ≥ 1`). Engine:
  `e0_mem_of_invariant` proves every nonzero `J`-invariant subspace contains the eigenvector
  `e₀` (via `shift` nilpotent + restricted to the subspace has a nonzero kernel vector, and
  `ker shift ≤ span{e₀}`), so two complementary nonzero submodules would both contain `e₀`,
  contradicting disjointness.
- `jordanRep_not_isSimpleModule` — **for `n ≥ 2`, `V_{λ,n}` is not simple**: the cyclic
  submodule generated by `e₀` is the 1-dim eigenline (every element is a scalar multiple of
  `e₀`, via `Module.End.aeval_apply_of_mem_apply_eq_smul`), so `e₁ ∉` it, hence proper.
- `exists_indecomposable_not_simple` — the book's punchline "an indecomposable representation
  need not be irreducible", witnessed by `V_{0,2}`.

Supporting general lemmas proved from scratch (not in Mathlib): `shift_pow_apply` (iterate
formula), `shift_nilpotent`, `ker_shift_le`, `exists_mem_ker_of_isNilpotent` (nilpotent endo
on a nontrivial module has a nonzero kernel vector). Defined a module-level `IsIndecomposable`.

Set `progress/items.json` `Chapter2/Example2.3.14` fidelity to `verified`, updated
`fidelity_decl`/`fidelity_note`.

## Current frontier

`Example2_3_14.lean` builds clean (no sorry, no native_decide; axioms = propext/Classical.choice/
Quot.sound). Whole `EtingofRepresentationTheory.Chapter2` builds.

## Overall project progress

Fidelity-sweep wave-1 item #5358 fixed. The only remaining (noted in the file) part of the
book's claim is the JNF completeness direction — that *every* f.d. indecomposable `k[x]`-rep is
some `V_{λ,n}` — which is Jordan-normal-form existence/uniqueness, cited (not proved) by the
book, and out of scope for this example.

## Next step

Land the PR for #5358. Other wave-1 fidelity issues (#5359 for Example2.3.14_continued, etc.)
remain for other workers.

## Blockers

None.
