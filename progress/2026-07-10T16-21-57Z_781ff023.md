## Accomplished

Feature session for issue #6219 (Ch4 Problem 4.12.6 — the book's hint: the zero-sum
representation `V` of the affine group `x ↦ ax + b` over a finite field `K`). Proved all
four target `sorry`s in `EtingofRepresentationTheory/Chapter4/Problem4_12_6.lean`:

- `Affine.card_eq` — order `q(q-1)` via `equivProd`, `Fintype.card_prod`, `Fintype.card_units`.
- `zeroSum_invariant` — `G`-invariance of the zero-sum subspace by reindexing the sum along
  the affine permutation `actEquiv g⁻¹` (`Equiv.sum_comp`).
- `zeroSum_finrank` — `dim V = q - 1` via rank-nullity on the summation functional
  (its kernel is `zeroSum K`, range is all of `ℂ`).
- `zeroSum_irreducible` — the mathematical heart. Elementary 2-transitivity argument avoiding
  character theory / inner products.

Supporting additions (all sorry-free): action lemmas `act_one/act_mul/act_apply_inv/
act_inv_apply/act_inv_eq_iff` and `actEquiv`; the `spike` vectors `q·δ_t − 1`; equivariance
lemmas `rho_single` (action permutes deltas by `act`), `rho_const` (fixes constants),
`rho_spike`; and `sum_units_eq` (summing over `Kˣ` misses the value at `0`).

Irreducibility proof idea: given a nonzero `G`-invariant `U ≤ zeroSum K`, translate a nonzero
vector so it is nonzero at `0`, average over the scaling subgroup `Kˣ` (stabilizer of `0`) to
extract a scalar multiple of `spike 0`, translate to get every `spike t ∈ U`, and show the
spikes span `zeroSum K` (`f = ∑ t (f t / q) • spike t` for `f` of zero sum). Hence `U = zeroSum K`.

## Current frontier

File builds cleanly (`lake build EtingofRepresentationTheory.Chapter4.Problem4_12_6`, 8580 jobs).
Two `sorry`s remain, both explicitly out of scope for #6219: `one_dim_reps_card` (line 126) and
`irreducible_dim` (line 327) — the character-count / full-classification half.

## Overall project progress

Phase 3 formalization ongoing. This item advances Problem 4.12.6 from statement-pass to
hint-proved (4 of 6 sorries discharged).

## Next step

Create the PR for #6219 and enable auto-merge. A follow-up issue should cover the remaining two
sorries (`one_dim_reps_card`: `q-1` one-dimensional reps pulled back from `G^ab ≅ Kˣ`;
`irreducible_dim`: every irreducible has dim `1` or `q-1` via the sum-of-squares count).

## Blockers

None.
