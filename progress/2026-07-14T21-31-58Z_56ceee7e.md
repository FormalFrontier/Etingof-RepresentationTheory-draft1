## Accomplished

Work on #6613 (De Concini–Kac dimension bound, `finrank_irreducible_le_of_isOfFinOrder`), in
`EtingofRepresentationTheory/Chapter2/Problem2_16_5.lean`:

- **Refactor:** generalized `ef_action` and `e_ladder` from `¬ IsOfFinOrder q` to the weaker
  `(q - q⁻¹ ≠ 0)`, so the e-action-on-ladder formula is reusable at roots of unity. Updated the
  sole caller (`highest_weight_eigenvalue_of_not_isOfFinOrder`).
- **Corrected the statement:** added the required `hq2 : (q : ℂ) ^ 2 ≠ 1` hypothesis (the theorem
  is false at `q = ±1`). No downstream callers, so safe.
- **Dispatch structure:** the main theorem now case-splits on the central scalars `a = e^ℓ`,
  `b = f^ℓ` (`epow_orderOf_isScalar` / `fpow_orderOf_isScalar`) into three named lemmas.
- **Case a = 0 (highest weight), proved sorry-free:** `finrank_le_of_epow_orderOf_eq_zero`. Walk up
  with `e` to a highest weight vector; its closed `f`-ladder (length ≤ ℓ, top folded back by
  `f^ℓ = b`) spans `V`.
- **Case b = 0 (lowest weight), proved sorry-free:** `finrank_le_of_fpow_orderOf_eq_zero`. Added the
  mirror e-ladder toolkit (`eladder`, `nu`, `K_eladder`, `L_eladder`, `fe_action`, `fcoef`,
  `f_eladder`) and ran the symmetric argument with the `e`-ladder of a lowest weight vector
  (top folded back by `e^ℓ = a`).

Both case lemmas verified sorry-free via `#print axioms` (only `propext`, `Classical.choice`,
`Quot.sound`).

## Current frontier

Exactly one case remains: `finrank_le_of_cyclic` (`a, b ≠ 0`, both `e` and `f` bijective) is
`sorry`. The main theorem `finrank_irreducible_le_of_isOfFinOrder` is fully assembled and reduces to
this single lemma. Filed as sub-issue **#6620** with the full proof route.

## Overall project progress

Phase 3 formalization ongoing. This turn advanced Ch2 #6613 from a single `sorry` (whole theorem) to
2 of 3 cases proved sorry-free plus the complete dispatch scaffolding; only the hard cyclic case is
outstanding.

## Next step

Pick up **#6620**: prove `finrank_le_of_cyclic`. Route: `e`, `f` bijective ⇒ `V = ⨁ E_j` over one
`q²`-orbit with `e : E_j ≅ E_{j+1}`; all `E_j` share dimension `d`, `dim V = s·d` with
`s = orderOf (q²)`; bound `d ≤ orderOf q / s` (analyse `e^s`, `f^s` on one eigenspace). Expected to
need further decomposition — the parent flagged it as the hard case.

## Blockers

None. #6620 is a self-contained follow-up.
