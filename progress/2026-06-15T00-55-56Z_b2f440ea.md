## Accomplished

Feature session on #4712 (det⁻¹-elimination kernel-lemma prerequisite). The
issue had three deliverables; I landed **deliverable 3** sorry-free and
decomposed the remaining two into successor issues.

New file `Chapter5/DetLocalization.lean` (added to `Chapter5.lean` aggregator):

- `detPoly k N := Matrix.det (Matrix.mvPolynomialX (Fin N) (Fin N) k)`, with
  `detPoly_ne_zero` and `powers_detPoly_le_nonZeroDivisors`.
- `evalGLHom : A →+* (GL_N → k)`, `p ↦ (g ↦ eval g p)`; `evalGLHom_apply`,
  `evalGLHom_detPoly_apply` (`= det g`), `evalGLHom_injective` (Zariski-density,
  via `MvPolynomial.eq_of_eval_eq_on_gl`), `isUnit_evalGLHom_detPoly`.
- `evalGLAway : A[det⁻¹] →+* (GL_N → k)` (the localization lift),
  `evalGLAway_comp_algebraMap` / `evalGLAway_algebraMap` (factors `evalGLHom`),
  and **`evalGLAway_injective`** — the faithful functions-on-GL model.
- `coordToAway : k[Xᵢⱼ,D] →+* A[det⁻¹]` (`Xᵢⱼ ↦ Xᵢⱼ`, `D ↦ detPoly⁻¹`),
  `evalGLAway_invSelf_apply` (`= (det g)⁻¹`), and
  `evalAtGL_eq_evalGLAway_coordToAway` — `evalAtGL` (Definition 5.23.1) factors
  through `A[det⁻¹]`.

All sorry-free; module builds clean (8054 jobs, no warnings on the new file).

## Current frontier

`Chapter5/DetLocalization.lean` complete for the faithful-model half of #4712.
The two algebraic halves are now separate issues.

## Overall project progress

Stage 3 (formalization) ongoing. This adds the localization scaffolding the
det⁻¹-elimination kernel lemma (#4694) needs to move between honest
`A[det⁻¹]` elements and functions on `GL_N`.

## Next step

- **#4736** (det irreducible/prime in `MvPolynomial (Fin N × Fin N) k`): the
  hard inductive determinant-irreducibility formalization. Mathlib lacks it; the
  classic route (cofactor expansion + primitive degree-1 in `X(0,0)`) is sketched
  in the issue. Sub-decompose if needed.
- **#4737** (det-power normal form `f = Q/detʳ` on `A[det⁻¹]`): blocked on
  #4736 (uniqueness is the `detPoly`-adic valuation). Builds on
  `DetLocalization.lean`.
- Then #4694 assembles (K) from the normal form + the Cauchy core (#K-cauchy).

## Blockers

None for the landed work. #4737 is blocked on #4736 (declared via `depends-on`).
The planner underestimated #4712 as "elementary, session-sized": det
irreducibility from scratch is a substantial formalization, hence the
decomposition.
