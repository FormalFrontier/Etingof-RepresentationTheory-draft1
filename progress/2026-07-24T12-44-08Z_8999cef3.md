## Accomplished

Fixed issue #7542: restored `EtingofRepresentationTheory/Chapter5/Problem5_1_2.lean` so it
elaborates from source again.

- Root cause: the quaternionic-type surjectivity proof (`realGEndAlgebra_equiv_quaternion_of_isQuaternionicType`,
  around line 900) rewrote `mul_neg_one` into a goal of the form `realMinus f = -(realMinus f * -1)`.
  The `-1` there comes from `hj'sq : j' * j' = -1` (the ring's `Neg`), whereas `mul_neg_one`'s
  `-1` is synthesized through `HasDistribNeg`. These are defeq but not syntactically equal, so
  neither `rw [mul_neg_one]` nor `simp only [mul_neg_one]` matched (simp reported the lemma unused).
- Fix: state the `* -1` collapse as a standalone `have hmulneg : realMinus f * (-1 : realGEndAlgebra ρ) = -realMinus f`,
  proved by `mul_neg_one (realMinus f)` (term-mode elaboration bridges the diamond). Then derive
  `hRM` via `rw [hj'sq, hmulneg] at hstep` and `neg_eq_iff_eq_neg.mp`, avoiding any keyed rewrite
  of a neg-literal lemma against the diamond-y `-1`.
- No statements weakened; no `sorry`/`admit`/axiom introduced.

## Current frontier

Issue #7542 complete. Diff is 10 insertions / 4 deletions in one file.

## Overall project progress

Stage 3 formalization, chapter-by-chapter regression repair. This turn cleared one Chapter 5
source regression. Many sibling `fix(Ch5)` / `fix(Ch4)` restore issues remain unclaimed
(#7543, #7544, #7545, #7547, #7548, #7555, ...).

## Verification

- `lake env lean EtingofRepresentationTheory/Chapter5/Problem5_1_2.lean` passes from source (exit 0).
- `#print axioms` on all six public endpoints
  (`realGEndAlgebra_equiv_complex_of_isComplexType`, `realGEndAlgebra_equiv_matrix_of_isRealType`,
  `realGEndAlgebra_equiv_quaternion_of_isQuaternionicType`, `exists_real_form_of_isRealType`,
  `isRealType_of_exists_real_form`, `isRealType_iff_exists_real_form`) shows only
  `[propext, Classical.choice, Quot.sound]` — no `sorryAx`.

## Next step

Pick up another unclaimed Chapter 5 restore regression (e.g. #7547 Proposition 5.22.2 or #7555
Exercise 5.8.5). Watch for the same instance-diamond pattern with `-1` literals in subalgebra
carriers.

## Blockers

None.
