## Accomplished
Proved both `sorry`-carrying theorems in
`EtingofRepresentationTheory/Chapter6/Problem6_1_3.lean` (issue #6254):
- `cartanMatrix_diag`: diagonal of `A = 2·Id − R` is `2` when `R` has no
  self-loops. Proof: `rw [cartanMatrix, Matrix.sub_apply, Matrix.smul_apply,
  Matrix.one_apply_eq, hloop i]; norm_num`. (Note: `simp` collapsed `2 • 1`
  into the matrix numeral `2` before `smul_apply` could fire, so explicit
  ordered `rw` was needed.)
- `cartanMatrix_isSymm`: `A` symmetric when `R` is. Proof: unfold, then
  `rw [transpose_sub, transpose_smul, transpose_one, hR.eq]`.

`#print axioms` on both shows only `[propext, Quot.sound]` — no `sorryAx`.

## Current frontier
Issue #6254 complete. File is sorry-free.

## Overall project progress
Stage 3 formalization ongoing. This closes an independent Ch6 setup item.

## Next step
Remaining unclaimed feature issues: #6239 (Ch3 Ext¹, hard) and #6255
(Ch7 cochain splitting, hard crux). Both are heavier category/homological
work.

## Blockers
None.
