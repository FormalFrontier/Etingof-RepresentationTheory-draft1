# Progress — planner session defeb5c1

## Accomplished

Planner cycle. Queue state on entry: `POD_QUEUE_DEPTH=2`,
`POD_MIN_QUEUE=3`, `POD_QUEUE_DEFICIT=1`.

Pre-plan checks:

- **Directives**: none open.
- **Merge sweep**: zero PRs satisfied the mergeable + non-failing
  predicate. Same three open PRs all in pr-repair territory
  (unchanged from cycle 68d03f5f):
  - **#2849** (chore dedupe): MERGEABLE but CI FAILURE.
  - **#2694** (Schur-Weyl γ.A): CONFLICTING (~16 days static).
  - **#2550** (Wall 3 C.1.a.ii): CONFLICTING (~24 days static).
- **Replan triage**: handled by `/replan` — out of scope here.

Recent `main` movement since cycle 68d03f5f (201a2ba):

- PR **#2897** merged (closes #2895) — D2.cycle:
  `graph_with_list_cycle_infinite_type_per_kQ` in
  `FieldGenericCycle.lean:440`. Clean per-(F, Q) wrapper, no
  `[IsAlgClosed F]`, natural-host placement. Recipe-cited body.
- PR **#2900** merged (closes #2898) — D2.adjacent:
  `adjacent_branches_infinite_type_per_kQ` in
  `FieldGenericD5Tilde.lean:1043`. Carries `[IsAlgClosed F]`,
  single-leaf dispatch shape. Removed `private` from
  `acyclic_no_triangle` and `acyclic_path_nonadj` in
  `InfiniteTypeConstructions.lean` (incidental API-visibility
  cleanup the worker flagged as the kind of move D2.single /
  D2.nonadj are likely to need too).

Wave-62 baseline (post-#2900): substantive non-summarize PRs since
wave-61 close = **3** (#2891 D2.degree4, #2897 D2.cycle, #2900
D2.adjacent). Next summarize trigger at +7.

Step 4 overlap audit: re-read all open `agent-plan` titles plus
the bodies of #2877 (umbrella body) and #2875 (parent). The new
issue addresses one specific sub-deliverable explicitly listed in
#2877's body ("D2.single"), no overlap with any other open issue.
The `coordination plan` overlap warning fired on #2877 — expected,
since #2877 is the umbrella and #2901 is its first formally
pre-split D2.single child. The other three D2 sub-pre-splits
followed the same overlap pattern.

**Decision: planner-led split of D2.single from #2877.**

Per the audit verdict (PR #2894) D3 "Recommended placement"
table, D2.single is the next natural pre-split among the three
remaining sub-deliverables (D2.single, D2.nonadj, D2.outer + D3):

- **D2.nonadj** (~900 lines) is too big to file as a single
  feature issue; the audit explicitly recommends its own audit
  pass before pre-splitting.
- **D2.outer + D3** (~150 lines) requires stubs for D2.single
  AND D2.nonadj to close, introducing 2 new sorries into the
  Theorem 2.1.2 chain. Continuing the proven real-proof pattern
  is the better play this cycle.
- **D2.single** (~305 lines outer + ~30-line internal stub for
  `single_branch_leaf_case_per_kQ`) is the right next unit:
  - All per-(F, Q) leaves it dispatches to are live on `main`
    (`etilde6_*` LIVE w/ Wall-1 sorry, `etilde7_*` LIVE w/ Wall-1
    sorry, `t125_*` LIVE as #2875 D1 stub).
  - The inline Ẽ₆ embedding (lines 8458-8672 of the `_kQ`-free
    original, ~215 lines) is the structurally heaviest part — fits
    one session per the D2.adjacent precedent (also ~150 lines of
    embedding + 15 distinctness facts + 36-case `fin_cases`).
  - The `single_branch_leaf_case` private helper is ~1500 lines
    and must be stubbed; its real body is a follow-up sub-issue
    (worker files on PR creation).
  - Continues the consistent one-sub-deliverable-per-cycle cadence
    that's been working (D2.degree4 / D2.cycle / D2.adjacent all
    landed cleanly with audit-cited recipe).

Created one new feature issue:

- **#2901** — `feat(Ch6 #2877 sub D2.single):
  single_branch_not_posdef_infinite_type_per_kQ — per-(F, Q)
  wrapper + leaf-case stub`. Single primary deliverable (D1, ~305
  lines body) + one supporting stub (D2,
  `single_branch_leaf_case_per_kQ` ~30 lines). Body cites the
  `_kQ`-free source range
  (`InfiniteTypeConstructions.lean:8401-8706`), the three
  per-(F, Q) leaf dispatch targets, the existing single-leaf
  dispatch precedent (`adjacent_branches_infinite_type_per_kQ`),
  the audit-recipe file
  (`progress/reviews/2026-05-18-degree4-per-kQ-placement.md`),
  and two placement options (preferred: new file
  `FieldGenericTpqr.lean`; alternative: cross-import in
  `FieldGenericETilde7.lean`). Net sorry delta: **+1** (D2 stub).

Posted a comment on #2877 noting the pre-split, the remaining
sub-deliverables (D2.nonadj, D2.outer + D3, plus the follow-up
single_branch_leaf_case_per_kQ real-body issue to be filed by
#2901's worker), and the stable pattern.

**Second issue not filed.** Considered but ruled out:

- **Further #2877 pre-splits**: queue depth post-#2901 is
  `POD_MIN_QUEUE = 3` — no further deficit. Pre-splitting D2.outer
  + D3 now would risk introducing the stub-then-assemble path
  before the audit recipe has been validated on D2.single. The
  next planner cycle can pre-split it cleanly once #2901's PR
  exists and the leaf-case decomposition shape is observed.
- **Wave-62 summarize**: 3 substantive PRs since wave-61 close;
  well below the 10-PR trigger.
- **Mathlib upstream trackers**: #2564 and #2841 remain
  external-blocked.
- **New review issue**: per the wave-60 audit calibration note
  in #2894 ("audit cadence has caught no defects, indicating the
  per-(F, Q) recipe is stable. The remaining audit value is
  pattern documentation for downstream workers, not defect
  detection"), no review issue filed for the most recent two
  per-(F, Q) PRs (#2897 / #2900). If #2901 introduces structural
  novelty (the new-file placement, the internal-stub pattern),
  a light follow-up audit is warranted next cycle.

Queue depth post-planning: **3** actionable issues
(= `POD_MIN_QUEUE`, verified via `coordination queue-depth`).
Composition:

- **#2901** (new feature, just filed) — D2.single per-(F, Q)
  wrapper + leaf-case stub, ~335 lines total, worker-ready.
- **#2877** — umbrella for D2.nonadj + D2.outer + D3 (worker
  self-decomposes per body).
- **#2564** — Mathlib upstream tracker, external-blocked.

Deficit fully refilled (1 of 1). `coordination set-target` not
called — active development phase, operator-configured pool size
stands.

## Current frontier

Unclaimed actionable issues:

- **#2901** — D2.single per-(F, Q) wrapper + leaf-case stub (just
  filed). Third-largest worker-ready unit on the per-(F, Q)
  Theorem 2.1.2 bridge; first one requiring a new file or
  cross-import + internal stub.
- **#2877** — outer assembly umbrella. Two D2 sub-cases (D2.nonadj
  audit-recommended own pass, D2.outer + D3 ~150 lines) remain.
- **#2564** — Mathlib upstream tracker, external-blocked.

Active in-flight PRs (all in pr-repair territory, unchanged):
#2849 (CI FAILURE), #2694 (CONFLICTING — ~16 days static), #2550
(CONFLICTING — ~24 days static).

Per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard (one tick from
cycle 68d03f5f):

- All six per-(F, Q) forbidden-subgraph leaves exist on `main`
  (4 proven, 2 stubbed pending #2789/#2801/#2793).
- All three per-(F, Q) subgraph dispatch wrappers exist on `main`
  (PR #2882).
- D2.degree4 (`degree_ge_4_infinite_type_per_kQ`) on `main`
  (PR #2891) — pattern audited and locked in (PR #2894).
- D2.cycle (`graph_with_list_cycle_infinite_type_per_kQ`) on `main`
  (PR #2897).
- D2.adjacent (`adjacent_branches_infinite_type_per_kQ`) on `main`
  (PR #2900).
- **Pending**: D2.single (#2901, just filed), D2.nonadj, D2.outer +
  D3 (still under #2877). Theorem 2.1.2 closes once all of these
  and the Wall 1 + per-(F, Q) indecomposability replans land.

## Overall project progress

Stage 3 (Lean formalisation). Wave-62: 3 substantive PRs landed
(#2891 D2.degree4, #2897 D2.cycle, #2900 D2.adjacent) + 1
documentation-only review PR (#2894). Pattern recipe stable and
re-validated.

Sorry count on `main`: **18** (Ch2 1, Ch5 4, Ch6 13). No change
from cycle 68d03f5f close — PRs #2897 and #2900 added real-body
theorems with no new sorries.

Expected delta if #2901 lands per spec: **+1** (D2's
`single_branch_leaf_case_per_kQ` stub introduces one new sorry; the
real-body follow-up issue would close it later).

Sorry-count delta this turn: **0** (planner cycle, no code
changes).

## Next step

- Dispatcher should run `/replan` first (substantial replan queue
  unchanged: #2839, #2850, #2789, #2793, #2801, plus the
  long-static Schur-Weyl chain), then pr-repair on the three
  static broken PRs, then claim workers on the queue. **#2901**
  is the natural pick for a feature slot (~335 lines, recipe-cited,
  new placement decision is the most interesting structural choice
  since D2.cycle's). **#2877** is the natural pick for a second
  feature slot (worker self-decomposes; recommended next
  sub-deliverable is D2.outer + D3 if D2.nonadj is judged too big
  to start fresh, otherwise D2.nonadj outer structure with
  internal stubs).
- Next planner cycle should re-evaluate:
  (a) status of #2901 (landed? if so, file the follow-up issue for
      `single_branch_leaf_case_per_kQ` real body — likely
      decomposed by T(1,q,r) shape into Ẽ₆-arm / Ẽ₇-arm / T(1,2,5)-
      arm sub-issues; also note any new placement-precedent if
      worker created `FieldGenericTpqr.lean`);
  (b) whether a light audit of #2901's PR is warranted (probably
      yes if it introduces the new file `FieldGenericTpqr.lean`
      and the internal-stub pattern — both are first-of-their-kind
      structural choices the audit would want to lock in);
  (c) state of #2877 self-decomposition (worker may have filed
      D2.nonadj or D2.outer pre-splits);
  (d) wave-62 substantive-PR count (currently 3; trigger at +10);
  (e) `replan` resolutions on the D̃₅ Sub B and K_{1,4}/T(1,2,5)
      per-(F, Q) chains.

## Blockers

None for the planner work itself. Standing constraints unchanged:
framework wall #2436 gates the inherited sorries in the per-(F, Q)
indecomposability proofs (#2789, #2793, #2801, #2839); the three
static pr-repair PRs (#2849 / #2694 / #2550) remain in `/repair`
territory.
