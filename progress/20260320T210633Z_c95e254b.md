## Accomplished

Proved the inequality direction of `sorted_shifted_strict_dominates` in `Theorem5_15_1.lean`:

- **Inequality direction** (π ≠ rev → partitions not equal): ~100 lines of non-trivial proof using the rearrangement inequality / Monovary framework. Key steps:
  1. From partition equality, derive inner product equality (`inner_product_eq_of_partition_eq`)
  2. Show `la.toFinsupp + rhoShift` is strictly anti-monotone (`shifted_partition_strict_mono`)
  3. Use `Monovary.sum_mul_comp_perm_eq_sum_mul_iff` to conclude `g ∘ σ` monovaries with `f`
  4. Derive `permExponent n π = rhoShift n` via antitone bijection uniqueness on Fin n
  5. Conclude `π = Fin.revPerm` via `rev_of_permExponent_eq_rhoShift`

- **Helper lemma infrastructure** for the dominance direction:
  - `sum_fin_subset_le_sum_top`: any k-subset of Fin n has val-sum ≤ top k sum (sorry)
  - `rhoShift_partial_sum_ge`: partial sums of ρ dominate partial sums of e_π (sorry)

## Current frontier

- `sorted_shifted_strict_dominates` dominance direction (line 1582): sorry — needs `sum_fin_subset_le_sum_top` proved, which requires formalizing "sorted distinct elements s_i ≤ n-k+i"
- `alternating_kostka_eq_delta` orthogonality (line 1770): sorry — deprioritized by issue
- File compiles with 6 sorry warnings (2 main + 4 in helpers, vs 2 before)

## Overall project progress

- ~194/583 items sorry-free (33.3%), ~100 sorries across ~36 files
- Theorem5_15_1.lean: 2 main sorries remain (dominance + orthogonality), inequality direction fully proved
- The inequality direction was the harder conceptual part of `sorted_shifted_strict_dominates`

## Next step

- Prove `sum_fin_subset_le_sum_top` using sorted element bound: for k distinct elements s_0 < ... < s_{k-1} from Fin n, each s_i ≤ n-k+i, so ∑ s_i ≤ ∑ (n-k+i) = ∑ (n-1-j). Use `Finset.orderEmbOfFin` for the sorted element access.
- Then connect `rhoShift_partial_sum_ge` to `sum_fin_subset_le_sum_top`
- Then prove the main dominance sorry using these helpers

## Blockers

None — proof strategy is clear, just needs more formalization time.
