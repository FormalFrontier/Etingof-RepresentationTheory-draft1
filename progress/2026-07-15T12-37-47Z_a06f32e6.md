## Accomplished

Claimed #6667 (tensor-of-resolutions for the Künneth Tor formula, Problem 8.2.8). Assessed scope
with an Explore agent: constructing the full `ProjectiveResolution` of `M₁ ⊗_k M₂` over
`(A₁⊗A₂)ᵐᵒᵖ` requires three substantial connective pieces (external-tensor functor, tensor of
complexes across categories over different rings, external-tensor projectivity) plus exactness +
assembly — none present in the project or Mathlib. Decomposed accordingly.

**Landed (partial PR on #6667):** `Chapter8/ExternalTensorModule.lean` — the keystone **pointwise**
external tensor product module, built representation-theoretically (mirrors `Problem3_8_4.rep`):
- `Etingof.extTensorRepAux` / `extTensorRep`: algebra hom `(A₁⊗A₂)ᵐᵒᵖ →ₐ[k] End k (M₁⊗M₂)` via
  `Algebra.lsmul`, `Algebra.TensorProduct.map`, `Module.endTensorEndAlgHom`, transported along
  `Algebra.TensorProduct.opAlgEquiv`.
- `Etingof.extTensorModule : Module (A₁⊗[k]A₂)ᵐᵒᵖ (M₁⊗[k]M₂)` via `Module.compHom`.
- `Etingof.extTensorModule_op_smul_tmul`: pins the action on simple tensors
  `op(a₁⊗a₂) • (m₁⊗m₂) = (op a₁•m₁) ⊗ (op a₂•m₂)` — exactly the `hM` axiomatised in
  `Problem_8_2_8_tor`, now realised. Axioms: `propext, Quot.sound` (sorry-free).

**Decomposition sub-issues created:** #6679 (external-tensor bifunctor on module categories),
#6680 (external tensor complex + augmentation), #6681 (degreewise projectivity), #6683 (exactness
via Ch7 Künneth + ProjectiveResolution assembly). Dependencies wired: #6680,#6681 ← #6679;
#6683 ← #6680,#6681,#6672.

## Current frontier

Keystone module structure done and sorry-free. Next: the bifunctor (#6679) packaging the pointwise
construction as `ModuleCat A₁ᵐᵒᵖ ⥤ ModuleCat A₂ᵐᵒᵖ ⥤ ModuleCat (A₁⊗A₂)ᵐᵒᵖ`.

## Overall project progress

Stage 3 (formalization) ongoing. Chapter 8 homological algebra: Problem 8.2.8 Künneth-for-Tor
route (#6657 assembler) now has step 1 (`torIsoHomologyTensorRight`) done and step 2
(tensor-of-resolutions, #6667) decomposed with its keystone module structure landed.

## Next step

Claim #6679 and build `extTensorFunctor` on top of `Chapter8/ExternalTensorModule.lean`
(derive `Module k X` + `IsScalarTower k Aᵢᵐᵒᵖ X` on ModuleCat objects by restriction of scalars;
morphism map = `TensorProduct.map`, linear over the external action by
`extTensorModule_op_smul_tmul`).

## Blockers

None for #6679–#6681. #6683 (assembly) additionally needs the universe-general Künneth bridge
(#6672, built on PR #6673) for the exactness obligation.
