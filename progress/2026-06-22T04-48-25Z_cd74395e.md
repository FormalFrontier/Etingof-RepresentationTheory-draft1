## Accomplished

Finished **#5024** (feature, Ch5 #4994 sub-B): retargeted the polynomial-GL
decomposition chain onto the relocated general-`k` classification core and deleted
the false classification crux.

- **Removed the false crux + dead corollaries** from
  `Chapter5/SchurWeylSimplesClassification.lean`:
  `schurWeyl_simples_formalCharacter_classification_core` (the `sorry` that was
  *false as stated* — it lacked the nonzero-multiplicity hypothesis `hSne`, so a
  degenerate `S i₀ = 0` carried an unconstrained "ghost" simple),
  `glTensorRep_schurWeyl_simples_schurPoly_classified`, and
  `glTensorRep_schurWeyl_simples_formalCharacter_linearIndependent`. Kept the
  reusable sorry-free reduction `formalCharacter_linearIndependent_of_eq_schurPoly`.
  Updated the file/decl docstrings to record the removal.

- **Exposed the Schur-Weyl witnesses** from
  `polynomial_homog_rep_asModule_embeds_directSum_simple` and
  `decompose_polynomial_gl_rep` (`Chapter5/PolynomialGLDecomposition.lean`): the
  output now carries `S` (+ `AddCommGroup`/`Module k`/`Module.Finite k`), the
  equivariant iso `e`, the action formula `he`, `hLdist`, and `hSne` instead of the
  pre-baked (false) `hclass` field. These were already obtained internally from
  `glTensorRep_schurWeyl_decomposition_equivariant_simple` (which emits `hSne` since
  sub-A #5047) and were previously consumed-then-dropped.

- **Retargeted both consumers** onto
  `schurWeyl_simples_formalCharacter_classification_core_general` (the relocated,
  `hSne`-carrying true core in `Chapter5/SchurWeylFormalCharacterIso.lean`):
  `iso_of_formalCharacter_eq_schurPoly` (SchurWeylFormalCharacterIso) and
  `simple_constituent_formalCharacter_eq_schurPoly_mem` (ConstituentCharacterExtraction).
  Each `letI`-registers the exposed `S`/index instances and calls the core to recover
  `(lam_cl, lam_inj, hchar)`; the downstream proofs are unchanged.

- **Dropped vestigial `hN : n ≤ N`** from the core
  `schurWeyl_simples_formalCharacter_classification_core_general` and its three
  sub-lemmas (`schurWeyl_simples_isotypic_matching_general`,
  `schurWeyl_simples_formalCharacter_linearIndependent_general`,
  `schurWeyl_simple_summand_glWeightSpace_top`). `hN` was never referenced in any
  body (only passed through), and the genuine call site
  `iso_of_formalCharacter_eq_schurPoly` is applied at `n = ∑ lam i` (and
  `Proposition5_22_2` at `n = ∑ (lam i + 1)`), which can exceed `N` — so threading a
  real `n ≤ N` would have wrongly narrowed the theorem. Removing the dead hypothesis
  keeps the core maximally general and callable from the consumers.

## Current frontier

All four modified files build green; targeted endpoint builds
(`ConstituentCharacterExtraction`, `Proposition5_22_2`, `CauchyDetQuotient`) pass.
A full `lake build` was launched for whole-project confirmation.

## Overall project progress

Stage 3 (formalization) ongoing in Chapter 5. This change removes a false `sorry`'d
theorem (net sorry reduction by 1) and rewires the Schur-Weyl #6 character-iso chain
onto the sound general-`k` core. Tracked seam sorries (#4946/#4947/#4932) are
untouched.

## Next step

Pick up the remaining unclaimed Ch5 items: reviews #5044 (right-torus eigenbasis)
and #5048 (general-`k` special-block assembly), or feature #5037 (James leading-term
fact, the multi-session combinatorial nut).

## Blockers

None.
