## Accomplished

Work session `715cdb88` (`/work` → `/feature`). Claimed **#4650** (Ch6 #4597
sub-4: corrected D̃₇ homogeneous tube) and landed its **sub-A** (the corrected
construction) on `FieldGenericD7Tilde.lean`, mirroring the merged D̃₅ sub-A
(#4678 / #4661).

- Swapped the refuted rank-deficient nilpotent central map `d5tildeGamma_F =
  [[I,I],[I,N]]` on the D̃₇ `{2,3}` edge for the full-rank corrected
  eigenvalue-site tube `d5tildeGammaTube_F = [[I,I],[I,Λ]]` (`Λ = λ·id + J`),
  **reusing the already-merged D̃₅ tube maps** `d5tildeGammaTube_F` /
  `d5tildeGammaTubeInv_F` and their two-sided retraction lemmas (no new gamma
  defs needed — D̃₇ differs from D̃₅ only by the internal identity chain
  `3-4-5` plus this one central edge).
- Threaded `lam` through `d7tildeRepMap_kQ`; `d7tildeRep_kQ` now picks the
  generic eigenvalue `d5tildeTubeLam F` (∉ {0,1}). Added `[IsAlgClosed F]` to
  the rep `def` and the 10 helper lemmas that reference it
  (`d7tildeRep_kQ_dimVec`, `d7tilde_chain_collapse`, `d7tilde_core_F`,
  `d7tilde_core5_F`, the four `_proj` siblings, `d7tilde_gamma_containment_F`,
  `d7tildeRep_kQ_leaf_equalities`). The bottom theorems
  (`isIndecomposable`, `not_finite_type_per_kQ`, `embed_d7tilde_in_tree_per_kQ`)
  already carried `[IsAlgClosed F]` — the file was partially prepared.
- Dimension vector and `d7tildeRep_kQ_dimVec` statement are unchanged.
- `d7tildeRep_kQ_leaf_equalities` is restated against the corrected rep and its
  proof body is now a single `sorry` (the old inline canonical/combo-D branches
  reasoned about the refuted `d5tildeGamma_F`); deferred to sub-B.
  `d7tildeRep_kQ_isIndecomposable` remains `sorry` (sub-C).

Verified: `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD7Tilde`
passes with exactly the two expected sorries; downstream consumers
`FieldGenericD6Tilde` / `FieldGenericD8Tilde` /
`FieldGenericNonAdjacentBranches` and the full `lake build
EtingofRepresentationTheory` pass (exit 0).

## Decomposition

#4650 bundled construction + indecomposability for all orientations; per the
issue's own "sub-decompose into sub-A/B/C" guidance and design doc §7, split:
- **sub-A** (this PR): corrected construction. Done.
- **#4689 sub-B**: orientation-generic `d7tildeRep_kQ_leaf_equalities` on the
  corrected tube (all branches; combo-C acceptance). `depends-on #4650`.
- **#4690 sub-C**: `d7tildeRep_kQ_isIndecomposable` assembly + re-point
  `d7tilde_not_finite_type_per_kQ`. `depends-on #4689`.

Parent #4650 partial-PR'd (`replan`) with the `Decomposed into #4689, #4690`
breadcrumb; next planner closes it (subs cover the residual scope).

## Current frontier

D̃₇ corrected tube constructed and building. The two deferred lemmas
(leaf_equalities sub-B, isIndecomposable sub-C) are the remaining math, mirroring
the still-open D̃₅ sub-B (#4662) / sub-C (#4663) program.

## Overall project progress

Etingof draft 1, Stage 3. Ch6 D̃-family corrected-tube redesign (#4597): D̃₄
(#4675), D̃₅ sub-A (#4678) merged; D̃₅ sub-B/C (#4662/#4663) in flight; D̃₆
(#4649) claimed; **D̃₇ sub-A done (this PR)**; D̃₈ (#4651), parametric (#4652)
unclaimed. Ch5 Frobenius route (#4595) blocked on #4608 (#4669/#4670/#4671).

## Next step

A worker claims **#4689** (D̃₇ sub-B) once #4650 is closed by a planner; then
**#4690** (sub-C). Both have direct D̃₅ precedents (#4662/#4663) to mirror once
those land. D̃₈ (#4651) and parametric D̃_{k+5} (#4652) sub-A's are the same
mechanical central-edge swap as this session (reuse the merged D̃₅ tube maps).

## Blockers

None for sub-A (done, PR up). #4689 blocked on #4650 (planner closure post-merge);
#4690 blocked on #4689.
