## Accomplished

Proved Problem 2.7.4(b) centrality (issue #5975) in
`EtingofRepresentationTheory/Chapter2/Problem2_7_4.lean`:

- `x_pow_char_mem_center`: `xᵖ ∈ center` in characteristic `p`.
- `y_pow_char_mem_center`: `yᵖ ∈ center` in characteristic `p`.

Supporting private lemmas added:

- `y_mul_x_pow_succ`: `y·xⁿ⁺¹ = xⁿ⁺¹·y + (n+1)•xⁿ` (commutator induction from `WeylAlgebra.yx_eq`).
- `x_mul_y_pow_succ`: `x·yⁿ⁺¹ = yⁿ⁺¹·x − (n+1)•yⁿ`.
- `mem_center_of_comm_gen`: an element commuting with both generators `x, y` is central,
  proved via the free-algebra surjection `RingQuot.mkAlgHom_surjective` and
  `Algebra.adjoin_induction` (mirrors the existing `WeylAlgebra.monomials_span` proof).

The centrality of `xᵖ`/`yᵖ` follows because the commutator coefficient `(n+1) = p` vanishes
in characteristic `p` (`Nat.cast_smul_eq_nsmul` + `CharP.cast_eq_zero`), plus the trivial fact
that a power of a generator commutes with that generator (`Commute.pow_left`).

`items.json`: `Chapter2/Problem2.7.4` moved `statement_formalized` → `proof_partial`.

## Current frontier

Problem 2.7.4 parts (a), (c) and the full `center_charP` (`= k[xᵖ, yᵖ]`) remain `sorry`
(explicitly out of scope for #5975).

## Overall project progress

Phase 3 formalization ongoing, filling proofs item by item. This turn discharged the two
2.7.4(b) centrality lemmas.

## Next step

A future issue can tackle 2.7.4(a) (char-0: no nonzero fin-dim reps, `A` simple), 2.7.4(c)
(char-p: irreducibles have dimension `p`), and `center_charP` (the center is exactly
`k[xᵖ, yᵖ]`, the hard reverse inclusion).

## Blockers

None.
