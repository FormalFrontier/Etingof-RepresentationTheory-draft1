## Accomplished

Session type: `/work` (feature). Claimed #5081 (clean induction helpers for the
DetInvElim-clean constituent-character extractor, parent #5078/#5075). Delivered
**deliverables 2 and 3** of the three; deliverable 1 (sub-of-spanning-is-spanning, the
crux) is separately claimed as #5086.

New file `EtingofRepresentationTheory/Chapter5/CleanQuotientHelpers.lean` (sorry-free,
builds clean, no warnings), importing only DetInvElim-clean files
(`CleanFormalCharacterAdditivity`, `GLRepAlgebraic`):

- **Deliverable 2** — `Etingof.IsAlgebraicRepresentation.quotient`: the quotient of an
  algebraic representation by an invariant submodule is algebraic. Proof mirrors the
  existing `IsAlgebraicRepresentation.restrict`: a complement of `K` supplies a linear
  section `s : Y ⧸ K → Y` of `K.mkQ`, and the quotient matrix coefficients are the
  polynomial sub-block `∑_{d,e} (B.repr (s (b' c)) d) · P e d · (b'.repr (K.mkQ (B e)) a)`.

- **Deliverable 3** — sub/quotient FDReps + short-exact-sequence glue, in namespace
  `Etingof.CleanCharExtraction`:
  - `subFDRep M σ := FDRep.of σ.toRepresentation`, `quotientFDRep M σ :=
    FDRep.of (Representation.quotient M.ρ σ.toSubmodule …)`;
  - equivariant inclusion (`subFDRep_subtype_equivariant`, by `rfl`) + injectivity;
  - equivariant projection (`quotientFDRep_mkQ_equivariant`, by `rfl`) + surjectivity;
  - exactness `range subtype = ker mkQ` (`subFDRep_range_eq_quotientFDRep_ker`);
  - `quotientFDRep_isAlgebraic` (ties deliverable 2 in), `quotientFDRep_iSup_glWeightSpace_eq_top`
    (spanning passes to the quotient, via the existing surjection lemma);
  - capstone `formalCharacter_eq_sub_add_quotient`: `char M = char (subFDRep) +
    char (quotientFDRep)`, a direct application of `formalCharacter_add_of_shortExact`.
    Its sub-`FDRep` spanning hypothesis is exactly what deliverable 1 (#5086) provides.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5.CleanQuotientHelpers` succeeds (8279 jobs),
no errors / warnings / sorries. Transitive import closure verified free of `DetInvElim` and
`CauchyDetQuotient` (32 project modules) — no build cycle.

## Overall project progress

Stage 3 (formalization) on the part-(a) chain: #5078/#5075 foundation
(`CleanFormalCharacterAdditivity`) landed; helper deliverables 2 & 3 now landed (this PR);
deliverable 1 in progress (#5086). The extractor #5082 (blocked on #5081) can be written once
deliverable 1 lands. Then #5076 (final assembly, discharging the lone `sorry` at
`CauchyDetQuotient.lean:124`) can proceed.

## Next step

1. Land #5086 (sub-of-spanning-is-spanning), which supplies the `hsub` hypothesis of
   `formalCharacter_eq_sub_add_quotient`.
2. Write #5082 (the extractor): `finrank` induction extracting a simple `S ≤ M` via
   `exists_isSimpleModule_le`, packaging it as a `Subrepresentation`, and recursing on
   `quotientFDRep M σ` (algebraic via `quotientFDRep_isAlgebraic`, spanning via
   `quotientFDRep_iSup_glWeightSpace_eq_top`, strictly smaller finrank), using
   `formalCharacter_eq_sub_add_quotient` at each step.

## Blockers

None. #5076 remains blocked in practice on #5082 (the extractor lemma
`clean_simple_constituent_formalCharacter_eq_schurPoly_mem` does not yet exist); #5082 is
blocked on #5081's deliverable 1 (#5086).
