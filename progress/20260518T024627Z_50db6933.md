## Accomplished

Audited the three wave-60 D̃₅ Sub B cascade helper PRs (#2835, #2843,
#2854) for issue #2858. Findings in
`progress/reviews/2026-05-18-d5tilde-subB-cascade-helpers.md`.

Verdicts:

- PR #2835 (helpers + API stubs): PASS-with-followups. Two helpers
  duplicate local `have`s in `FieldGenericStar.lean`.
- PR #2843 (γ⁻¹ closed forms): PASS. No issues; `cumTailSumLin_*`
  recurrences correct, `_oneSubNilp` direction verified, no
  heartbeat bumps.
- PR #2854 (canonical-orientation case): PASS-with-followups. Proof
  mirrors ℂ-source line-for-line; arithmetic 1+2+4+8+16=31 verified;
  local `have core_F`/`core3_F`/`gamma_containment_F` worth hoisting
  before #2853 starts.

Filed:

- #2859 — refactor: hoist `embed_sum_zero_F` / `center_decomp_F` from
  `FieldGenericD5Tilde` to `FieldGenericStar`, dedup local `have`s.
- #2860 — refactor: hoist `core_F` / `core3_F` / `gamma_containment_F`
  to top-level before #2853 fills 31 non-canonical cases.

Posted audit summary as comment on #2804.

Build green (`lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde`):
2 declarations using `sorry`, 6 raw `sorry` keywords — matches
`progress/sorry-landscape.md`.

## Current frontier

D̃₅ Sub B cascade helpers cleared the audit. Cleanup follow-ups filed
ahead of the bulk follow-on work (#2853, #2851).

## Overall project progress

Wave 60 audit phase: complete for the D̃₅ Sub B cascade. Wave-60
PR-repair queue still carries #2550 (~24 days static), #2694 (~15
days static), and #2849 (CI FAILURE). No movement on Wall 1 (#2436,
6 waves stale). The D̃₅ Sub B cascade itself is unblocked once #2853
+ #2851 land — #2860 is a suggested precondition for #2853.

## Next step

Recommendation for the next planner cycle: pick up #2859 + #2860 as
single-session refactors before any worker claims #2853. The 31
non-canonical orientation cases will be substantially easier to write
against hoisted top-level helpers than against the local `have`s
inside the canonical branch.

Alternative: a worker can directly claim #2860 (single-session,
mechanical hoist). #2859 can land anytime.

## Blockers

None for this audit. Audit verdict published; follow-ups filed and
linked from the audit report.
