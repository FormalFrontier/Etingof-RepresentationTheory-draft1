## Accomplished
One-shot session on issue #5320 (the 4 `clength` API sorries in
`Chapter9/KrullSchmidt/Length.lean`). Two of the four (`clength_pos_of_not_isZero`,
`clength_biprod`) were already discharged on `main` by reduction; the base cases
(`clength_eq_zero_of_isZero`, `clength_simple`) landed via #5351. The two residual
sorries (`clength_eq_zero_iff →`, `clength_additive`) are genuine multi-session
categorical Jordan–Hölder content, already decomposed into #5324/#5325.

This session added **complete, no-sorry** mathematics that discharges the *math* of
the first residual and sharpens the route for both:

- `finiteDimensionalOrder_subobject_of_isZero` — finiteness base case (zero object ⇒
  singleton subobject lattice ⇒ `FiniteDimensionalOrder`).
- `height_top_lt_top` — `Order.height (⊤ : Subobject X) < ⊤` from
  `FiniteDimensionalOrder (Subobject X)` (mirrors Mathlib `coheight_lt_top`).
- `clength_eq_zero_iff_of_finiteDimensionalOrder` — the honest, unconditional `→`
  direction of `clength_eq_zero_iff`, proved under exactly the order-theoretic
  finite-length condition. This is the full mathematical content of #5324; only the
  *wiring* (making finiteness available from the ambient category) remains.

Sharpened the docstrings on both remaining sorries with the precise single crux (the
Schreier reflecting lemma) and the concrete, now-verified-available Mathlib tooling:
`Abelian.Pseudoelement.sub_of_eq_image`, `pseudo_pullback`, `pseudo_exact_of_exact`,
and the categorical snake lemma (`ShortComplex.SnakeLemma`). Recorded the two residual
gaps for the additivity *equality*: missing product-order height arithmetic
(`height (a,b) = height a + height b`) and the reverse inequality (series splicing).

## Current frontier
`Length.lean` builds clean (2155 jobs), exactly the same 2 sorries as before (no new
sorry; no existing statement weakened). Downstream consumer `Existence.lean` build
launched to confirm no regression.

## Overall project progress
Stage 3 formalization. Ch9 Krull–Schmidt `clength` API: base lemmas + biprod + positivity
done; `clength_eq_zero_iff →` math done (gated only on wiring, #5324); `clength_additive`
reduced to the single Schreier second-isomorphism reflecting lemma (#5325).

## Next step
For **#5324**: route the §9.6 finite-length standing assumption into the category, then
replace the `clength_eq_zero_iff` body with `clength_eq_zero_iff_of_finiteDimensionalOrder`.
Two faithful options recorded in the docstring — carry `IsFiniteAbelianCategoryOverField`
(already has `finiteLength : ∀ X, HasFiniteLength X`) and prove
`HasFiniteLength X → FiniteDimensionalOrder (Subobject X)` (only the short-exact extension
step is open — it is the #5325 crux), **or** add finite length to `IsFiniteAbelianCategory`
(non-breaking: no hand-built instance exists; but a Def-9.6.1 fidelity call for a planner).

For **#5325**: prove the Schreier reflecting lemma via `sub_of_eq_image`/`pseudo_pullback`
or the snake lemma; derive the order embedding `Subobject X₂ ↪ Subobject X₁ × Subobject X₃`;
prove the missing product-order height lemma; then the height bound (finiteness, feeding
#5324) and, with series splicing, the additivity equality.

## Blockers
None for this session's deliverable. The two residual sorries remain genuinely
multi-session (Schreier reflecting lemma + product-height arithmetic + wiring), correctly
tracked in #5324/#5325 — not blocked, just not completable in a single one-shot.
