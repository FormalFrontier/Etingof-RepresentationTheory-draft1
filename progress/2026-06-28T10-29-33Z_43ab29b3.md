## Accomplished

One-shot dispatch on **#5556** (Ch5: prove `peterWeylSummandMap_range_iSupIndep`, the
cross-summand / distinct-irreducible Schur orthogonality input to `peterWeylMap_injective`).
Landed **fully sorry-free**, including the previously-missing distinguishing invariant
(highest-weight non-isomorphism). The simplicity blocker (point a) was already cleared by
#5559; the external-tensor-simplicity blocker (point b) is **avoided entirely** by a
right-action-only density argument.

**New file `Chapter5/CrossSummandMatrixCoeff.lean` (sorry-free):**

- `crossMatrixCoeff_indep` — abstract cross-summand engine: for a finite family of
  finite-dimensional representations `ρ i` over an algebraically closed field whose
  `asModule`s are simple and pairwise non-isomorphic `k[G]`-modules, a tuple of matrix
  coefficients summing to zero (`∀ g, ∑ contractLeft (id ⊗ ρ i g) (z i) = 0`) vanishes
  termwise. Reuses `CharacterIndependence.exists_separating` (Jacobson density) to extract
  a separating algebra element acting as identity on `V j` and zero elsewhere, then plugs
  `a * g` into the group-algebra-extended hypothesis to recover the single-summand
  hypothesis fed to `matrixCoeff_injective_of_isSimpleModule`. **No external-tensor
  `GL × GL` simplicity needed.**
- `crossMatrixCoeff_indep_finset` — `Finset`-indexed corollary (subtype packaging in one place).

**New file `Chapter5/AlgIrrepGLNonIso.lean` (sorry-free):**

- `formalCharacter_charTwist_detChar_pow` — the `p`-fold determinant-twist character formula
  `formalCharacter (charTwistRep (detChar^p) (schurModuleRep w)) = schurPoly (w + p·1)`,
  by induction on `p` from `formalCharacter_shift_of_weightSpace_finrank` (one `∏ Xᵢ` per
  twist) + `schurPoly_shift`, with per-step weight-space bookkeeping at the integer-graded
  level (`glWeightSpaceℤ_charTwist_shift`, `finrank_glWeightSpaceℤ_schurModule_neg`).
- `algIrrepGLRepρ_noniso` — **distinct dominant weights give non-isomorphic `L_λ`**: an
  `asModule` equiv restricts to a `GL`-equivariant `k`-linear intertwiner; the same map
  intertwines the `det^M`-twists (`M = λ.shift + μ.shift`), clearing both shifts to honest
  polynomial Schur modules; equal formal characters + `schurPoly_injective` + the
  `toNatWeight` recovery `(λ.toNatWeight i : ℤ) = λ.val i + λ.shift` force `λ = μ`.

**`Chapter5/Theorem5_23_2_PeterWeyl.lean`:**

- `evalGLAway_peterWeylSummandMap_contractLeft` — the transported matrix-coefficient identity
  (factored out of `peterWeylSummandMap_injective`'s `key`).
- `peterWeylSummandMap_finsetSum_eq_zero` — finite-family independence of the per-summand
  maps inside `R = k[gᵢⱼ][1/det]`, by `evalGLAway` (faithful) → `crossMatrixCoeff_indep_finset`.
- `peterWeylSummandMap_range_iSupIndep` — **the deliverable, sorry-free**, via
  `iSupIndep_iff_finsetSum_eq_zero_imp_eq_zero` + the finite-family lemma.

## Current frontier

`peterWeylMap_injective` (#5549) is now sorry-free (both its inputs —
`peterWeylSummandMap_injective` #5555/#5559 and `peterWeylSummandMap_range_iSupIndep`
#5556 — are complete). The only remaining `sorry` in `Theorem5_23_2_PeterWeyl.lean` is
`peterWeylMap_surjective` (#5550, the Cauchy spanning half), untouched by this work.

## Overall project progress

Stage 3 ongoing. The injectivity half of the `GL_n × GL_n` Peter-Weyl decomposition
(Theorem 5.23.2(ii)) is now fully assembled and sorry-free. Bijectivity
(`peterWeylMap_bijective`) reduces to the single outstanding surjectivity sorry #5550.

## Next step

Pick up #5550 (`peterWeylMap_surjective`): transport the per-degree polynomial Cauchy
decomposition across the determinant localization to the matrix-coefficient spanning
statement for `R`. The Cauchy machinery (`Cauchy*`, `PolynomialGL*`,
`CauchyDetQuotient*`) it consumes is already sorry-free.

## Blockers

None for #5556 (closed sorry-free). #5550 (surjectivity) remains the sole open input to
`peterWeylMap_bijective`.
