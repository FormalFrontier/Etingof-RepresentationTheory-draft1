## Accomplished

Discharged the single documented `sorry` in
`EtingofRepresentationTheory/Chapter9/KrullSchmidt/Fitting.lean`: proved
`Etingof.fitting_decomposition` (the algebraic core of Fitting's lemma, link 3/5 of #5207, issue
#5275). The proof consumes only `Etingof.exists_pow_stabilizes` (from #5274) — the single
finite-length input giving a positive `n` at which `g' := image.ι (fⁿ) ≫ factorThruImage (fⁿ)` is
an isomorphism — and is otherwise elementary abelian-category algebra:

1. With `g := fⁿ`, `i := image.ι g`, `p := factorThruImage g`, the iso `g' = i ≫ p` makes `p` a
   split epi with explicit section `s := (g')⁻¹ ≫ i`.
2. `K := ker p` and `I := im g` split `X` as a biproduct via an explicitly assembled
   `BinaryBicone` (point `X`, total condition) and `biprod.uniqueUpToIso`, avoiding the abstract
   `section_` from `binaryBiconeOfIsSplitEpiOfKernel` (whose chosen section need not equal `s`,
   which would break the image-preservation step).
3. `f` is block-diagonal: it preserves `K` (off-diagonal A, `i` mono + `f` commutes with `g`) and
   preserves `I` (off-diagonal B, via the morphism-level factorisation
   `i ≫ f = (g'⁻¹ ≫ i ≫ f ≫ p) ≫ i`). No pseudoelements were needed.
4. Blocks identified from the conjugated power: `(fK)ⁿ = kernel.ι p ≫ g ≫ fstB = 0` (nilpotent)
   and `(fI)ⁿ = s ≫ g ≫ p = g'` is an iso, so `fI` is an iso via `isUnit_pow_iff`.

`lake build` of `Fitting` and the downstream `Exchange` is clean, no lint warnings.
`#print axioms Etingof.fitting_decomposition` lists `sorryAx` only transitively through the
`clength_*` lemmas that `exists_pow_stabilizes` rests on (my proof adds no `sorry`), exactly as the
issue's verification note anticipates. Updated the module and theorem docstrings to record that the
decomposition is now proved rather than deferred.

## Current frontier

`fitting_decomposition`, `isNilpotent_or_isIso_of_indecomposable`, and
`isLocalRing_End_of_indecomposable` are all `sorry`-free in `Fitting.lean`. The remaining `sorry`s
in the Krull–Schmidt chain are the `clength_*` finiteness/monotonicity lemmas in `Length.lean`
(link 1/5, the categorical Jordan–Hölder content).

## Overall project progress

Stage 3 formalization. Link 3/5 of the Krull–Schmidt chain (#5207) is now `sorry`-free modulo the
`clength_*` inputs of link 1/5. Once those land, `isLocalRing_End_of_indecomposable` will be free
of `sorryAx`.

## Next step

Attack the `clength_*` sorries in `Length.lean` (`clength_additive`, `clength_pos_of_not_isZero`,
`clength_eq_zero_iff`) — the categorical Jordan–Hölder content of link 1/5 — to clear `sorryAx`
from the whole chain.

## Blockers

None.
