## Accomplished

Session type: feature (/work → /feature on #4647, the D̃₅ pattern-setter).

- Claimed #4647 and assessed scope: it bundles (1) a D̃-family design doc,
  (2) a corrected `d5tildeRep_kQ` construction, (3) `isIndecomposable` for all
  orientations, (4) downstream re-pointing — a multi-session task that the issue
  itself authorises sub-decomposing into sub-A/B/C.
- **Authored `progress/dtilde-tube-redesign-design.md`** (deliverable 1, the
  pattern-setter consumed by D̃₆/₇/₈/parametric #4649–#4652). It pins down:
  - the D̃ family shape (two end-forks + degree-2 pass-through centers; D̃₅ is
    the smallest two-degree-3-vertex / central-edge shape, hence pattern-setter);
  - the bug (central iso `γ = [[I,I],[I,N]]`, `N` rank-deficient → free `e_m`
    direction peels off in mixed orientations — combo-C, #2853);
  - **the one-line fix**: replace `N` with the full-rank eigenvalue site
    `Λ = λ·id + J` (`jordanShiftLinGen`), `γ_λ = [[I,I],[I,Λ]]`;
  - the m=0 regular-simple sanity check (four lines `⟨(1,1)⟩,⟨(1,λ)⟩,⟨(1,0)⟩,
    ⟨(0,1)⟩` in general position iff `λ ∉ {0,1,∞}`);
  - the two-center reduction template (leaf collapse → central transport across
    `γ_λ`/`γ_λ⁻¹`, both `J`-polynomials → single `(λ·id+J)`-invariant splitting
    → `eigenvalue_jordan_invariant_compl_trivial_gen`), with the closed-form
    inverse `γ_λ⁻¹ = [[I+(Λ−I)⁻¹, −(Λ−I)⁻¹],[−(Λ−I)⁻¹,(Λ−I)⁻¹]]`;
  - the honest open part + recommended sub-A/B/C split.
- **Decomposed deliverables 2–4** into self-contained sub-issues:
  - #4661 sub-A: `d5tildeGammaTube_F`/`Inv_F` + `lam`-threaded `d5tildeRep_kQ`
    (depends on #4647);
  - #4662 sub-B: orientation-generic `d5tildeRep_kQ_leaf_equalities` (depends
    on #4661);
  - #4663 sub-C: `d5tildeRep_kQ_isIndecomposable` assembly + downstream
    re-point (depends on #4662).

## Current frontier

The D̃-family construction is mathematically nailed down and the implementation
is sequenced (sub-A → sub-B → sub-C). The design doc is the consumable artifact
for the four blocked D̃₆₊ issues.

## Overall project progress

Stage 3 (formalization), Ch6 infinite-type tube redesign. The sporadic shapes
(D̃₄ star, Ẽ₆, Ẽ₇, T(1,2,5)) are well underway; this turn opens the D̃ family
proper with the pattern-setter design + D̃₅ sub-program.

## Next step

Once this PR merges and a planner closes #4647 (design doc covers deliverable 1;
sub-A/B/C cover 2–4), claim **#4661** (sub-A): add the corrected
`d5tildeGammaTube_F`/`Inv_F` maps and thread `lam` through `d5tildeRep_kQ`,
keeping the file building (leaf-equality / isIndecomposable lemmas restated,
proofs deferred to sub-B/C).

## Blockers

None. This is a clean Step-4b decomposition; the design doc is the completed
coherent subset, landed as a partial PR with a `Decomposed into #4661, #4662,
#4663` breadcrumb on #4647 for the next planner.

## Quality metric delta

No Lean change this turn (design doc + decomposition only); sorry count
unchanged. `FieldGenericD5Tilde.lean` still carries the 4 sorries (3 in
`leaf_equalities` reversed branches + 1 in `isIndecomposable`) against the
refuted rep — sub-A/B/C retire them on the corrected rep.
