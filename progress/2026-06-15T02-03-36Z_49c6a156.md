## Accomplished
- Claimed #4736 (`detPoly` irreducible/prime in `MvPolynomial (Fin N × Fin N) k`).
- Added `Chapter5/DetIrreducible.lean` with two fully-proven, reusable lemmas:
  - `irreducible_C_mul_X_add_C`: over an integral domain, `a·X + b` is irreducible
    when `a` is prime and `a ∤ b` (the engine of the determinant induction).
  - `prime_rename_of_injective`: primeness in `MvPolynomial` transfers across
    `rename` along an injective re-indexing (the IH-plumbing; built on Mathlib's
    `MvPolynomial.prime_rename_iff`).
- Stated `detPoly_irreducible (hN : 0 < N)` (sorry, full route in docstring) and
  derived `detPoly_prime` from it via the `MvPolynomial` UFD instance.
- Registered the module in `Chapter5.lean`. Builds clean (only the one documented
  `sorry` in `detPoly_irreducible`).

## Current frontier
The determinant-specific induction remains (the `sorry` in `detPoly_irreducible`).
Worked out a complete proof route and filed it as successor issue #4775.

## Overall project progress
Stage 3 formalization, Chapter 5 (det-localization / kernel-lemma thread #4712).
`detPoly` foundations now in place; the irreducibility induction is the residual.

## Next step
Pick up #4775: prove `detPoly_irreducible` by induction (cofactor expansion →
minor-as-rename via `AlgHom.map_det` → coprimality `M₀ ∤ R` via a column-0
substitution + variable-occurrence → single-variable isolation + the new
`irreducible_C_mul_X_add_C`). All required Mathlib API verified
(`Matrix.det_succ_column_zero`, `det_fin_one`, `MvPolynomial.X_prime`,
`vars_rename`, `AlgHom.map_det`).

## Blockers
None. #4736 decomposed; foundations land in this partial PR, induction tracked in #4775.
