## Accomplished

- Claimed #6395 (Exercise 5.27.3 parts (i)+(ii), character-orthogonality route). Its blocker
  PR #6397 had already merged, so the issue was unblocked.
- **Proved Part (i) fully, sorry-free**: `Simple (V χ U)` from `Simple U`, via the
  induced-character norm `∑_{x∈A⋊G} χ_{VχU}(x)·χ_{VχU}(x⁻¹) = |A⋊G|` and
  `FDRep.simple_iff_char_is_norm_one`. Key steps landed in
  `EtingofRepresentationTheory/Chapter5/Exercise5_27_3.lean`:
  - `sum_monoidHom_units_cast_eq`: character orthogonality on the finite abelian `A`
    (`∑_a ψ(a) = |A|·[ψ=1]`), from `sum_hom_units_eq_zero`.
  - `hcf`/`hinv`: reformulated character formula (iv) through a class-function extension
    `Uc` of `χ_U`, and the semidirect-product inverse.
  - `hasum` + `hiff`: the A-sum collapses to `if h·g·h'⁻¹ ∈ stab χ then |A| else 0`.
  - `hstep_h`: two conjugation reindexings `g ↦ h·g·h⁻¹`, `h' ↦ h·h'⁻¹` on the pair `(g,h')`,
    via an explicit `(G×G) ≃ (G×G)` bijection.
  - `hreindex`+`hinner`+`hUnorm`: the internal triple `G`-sum collapses to
    `|G|·|G_χ|·⟨χ_U,χ_U⟩ = |G|·|G_χ|·|G_χ|`.
  - Assembly: `|G_χ|⁻²·|A|·|G|·|G_χ|² = |A|·|G| = |A⋊G|`.
- Found Part (ii) needs infrastructure absent from Mathlib (character injectivity
  `χ_V = χ_W → Nonempty (V ≅ W)` for `FDRep ℂ`) plus an off-diagonal Mackey computation —
  a separate lift. Decomposed:
  - #6425 — character injectivity for `FDRep ℂ` (reusable helper).
  - #6426 — Part (ii) (depends on #6425; reuses the Part (i) infra in the file).
  Left a breadcrumb on #6395 and am landing Part (i) as a partial PR.

## Current frontier

`Exercise5_27_3.lean` builds cleanly (8581 jobs). Part (i) sorry-free; Parts (ii) and (iii)
still `sorry`. No `axiom`/`admit`.

## Overall project progress

Phase 3 formalization ongoing. This turn advanced Ch5 Exercise 5.27.3 by proving Part (i)
(irreducibility) via a full induced-character / Mackey norm computation — the abstract
exercise's character-orthogonality route (distinct from the structural proof in
`Theorem5_27_1.lean`).

## Next step

- Merge this partial PR; a planner narrows/closes #6395 to the residual (Part (ii)).
- Pick up #6425 (char injectivity) — unblocks #6426 (Part (ii)).
- Part (iii) remains tracked in its existing sibling sub-issue.

## Blockers

None. #6426 is correctly `blocked` on #6425 (character injectivity). #6425 is a genuine
Mathlib gap and is the highest-value next piece for finishing Exercise 5.27.3.
