## Accomplished

Closed #6679 (Ch8 Problem8.2.8-Tor): packaged the pointwise external-tensor module
construction `Etingof.extTensorModule` as a **bifunctor**

```
Etingof.extTensorFunctor :
  ModuleCat.{u} A₁ᵐᵒᵖ ⥤ ModuleCat.{u} A₂ᵐᵒᵖ ⥤ ModuleCat.{u} (A₁ ⊗[k] A₂)ᵐᵒᵖ
```

New file `EtingofRepresentationTheory/Chapter8/ExternalTensorFunctor.lean`, sorry-free,
axioms `propext, Classical.choice, Quot.sound` only. Added to the `Chapter8.lean` aggregator.

Contents:
- `restrictModule₁/₂`, `tower₁/₂`, `extModule` — `local instance`s giving each `ModuleCat Aᵢᵐᵒᵖ`
  object its restricted `Module k` and `IsScalarTower k Aᵢᵐᵒᵖ` (via `Module.compHom` along
  `algebraMap k Aᵢᵐᵒᵖ`) and the external `(A₁⊗A₂)ᵐᵒᵖ`-action on `X ⊗ Y`.
- `extTensorFunctorObj`, `extTensorFunctorMapHom` (with the real `map_smul'` proof — a double
  `TensorProduct.induction_on` + `MulOpposite.rec'`), `extTensorFunctorMap`.
- `extTensorFunctorMapHom_id/_comp` (proved at the `X ⊗ Y`-domain level) → `extTensorFunctorMap_id/
  _comp` → the assembled `extTensorFunctor` (obj/map/map_id/map_comp/naturality).

## Current frontier

`extTensorFunctor` exists and is sorry-free. It unblocks #6680 (external tensor complex
`P•₁ ⊗ P•₂`) and #6681 (projectivity of `(P₁)ⱼ ⊗ (P₂)ₘ`), both currently `blocked on #6679`.

## Overall project progress

Phase 3 formalization. Problem 8.2.8 (Künneth for Tor over `A₁ ⊗ A₂`) is being assembled from
`ExternalTensorModule` (pointwise action, landed) → `ExternalTensorFunctor` (this PR) →
external tensor complex + projectivity → `extTensorProjectiveResolution`.

## Next step

Pick up #6680 / #6681 (both now unblocked once this merges) — apply `extTensorFunctor` degreewise
to two projective resolutions.

## Blockers

None. Key patterns for future consumers of this file:
- The restricted `Module k`/`IsScalarTower`/external-action instances are `local instance`s keyed
  on `ModuleCat Aᵢᵐᵒᵖ` carriers; import this file to get them in scope.
- `extTensorModule_op_smul_tmul` is restated as `extTensorFunctor_op_smul_tmul` with the ambient
  `•` so `simp`/`rw` fire (the original uses `.toSMul.smul` via a differently-named instance).
- Functor-law proofs go through the `X ⊗ Y`-domain `extTensorFunctorMapHom_id/_comp`; the
  `ModuleCat.of` carrier-coercion means `map_add`/`map_zero`/`rw` do NOT fire on an abstract
  `φ.hom` whose argument was introduced at `↑X ⊗ ↑Y` (defeq to `↑(obj)` but not syntactic).
