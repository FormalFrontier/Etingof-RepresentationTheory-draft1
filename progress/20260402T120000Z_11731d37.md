## Accomplished

Closed issue #1936: Prove CoxeterInfrastructure type-changing iterated reflection functors.

### Eliminated all sorries in `one_round_iterate` and `one_round_or_simpleRoot`

1. **Dim vector bridge proof (hDimVec_next)**: Connected Proposition 6.6.8 (finrankAt' = simpleReflectionDimVector) with the bridge lemma (simpleReflectionDimVector = simpleReflection) despite Fintype instance mismatch. Proved Fintype-independence of simpleReflectionDimVector sums, allowing rewrite between instances.

2. **Zero case contradiction (h_neg: d_next p = -(d_cur p))**: In the zero reflected representation case, proved that d_cur p = 0 by:
   - Using hsub_other to get Subsingleton(ρ_cur.obj j) for j ≠ p, hence d_cur j = 0
   - Computing the Cartan matrix dot product: ∑ d_cur j * A(j,p) = d_cur p * 2 + 0 = 2 * d_cur p
   - Concluding d_next p = d_cur p - 2 * d_cur p = -(d_cur p)
   - Combined with d_next p ≥ 0 and d_cur p ≥ 0, forced d_cur p = 0
   - Contradicting Nontrivial(ρ_cur.obj p)

3. **Instance synthesis conflicts**: All uses of Q_cur vs reversedAtVertex Q_cur p resolved via explicit @ notation. Used `rw [hv]` instead of `subst hv` to avoid eliminating pattern variables.

### Build status

CoxeterInfrastructure.lean: **0 sorries**, build clean (warnings only).

## Current frontier

All proofs in CoxeterInfrastructure.lean are complete. The file provides:
- `one_round_iterate`: type-changing induction over admissible ordering
- `one_round_or_simpleRoot`: single round yields simple root or full Coxeter transform
- `indecomposable_reduces_to_simpleRoot`: iterated rounds find simple root (Theorem 6.8.1)
- `indecomposable_bilinearForm_eq_two`: B(d,d) = 2 for indecomposable reps

## Overall project progress

The Gabriel theorem chain through CoxeterInfrastructure is now complete. This unblocks 6+ downstream theorems that depended on the sorry'd `one_round_iterate`.

## Next step

Check downstream files that import CoxeterInfrastructure for newly-unblocked proofs.

## Blockers

None.
