## Accomplished

Session type: feature (/work → /feature on #4654).

- Claimed #4654 (`hpoly'_of_scalarGL_action`, Piece 2 of the `det⁻¹` elimination).
- **Found the decomposition's interface is false**, not just unproven. The
  `det⁻¹`-elimination chain conflated *algebraic* representations (matrix
  coefficients rational in the entries, weights in `ℤ^N` via the `det⁻¹`
  variable) with *polynomial* representations (matrix coefficients polynomial,
  weights in `ℕ^N`). As stated, four theorems were **false**:
  - `scalarGL_acts_as_pow` (#4653)
  - `hpoly'_of_scalarGL_action` (#4654)
  - `polynomialRep_homogeneous_hpoly'` (already merged, #4598 core)
  - `polynomial_homog_rep_equivariant_embedding` (already merged)
- **Counterexamples** (documented in docstrings + on the issues):
  - Piece 2 / assembly (N=2, n=0): `M = Sym²(V) ⊗ det⁻¹`. Algebraic; center
    acts by `t²·t⁻² = t⁰ = 1` (so `h_scalar` holds, n=0); `h_homog` vacuous
    (only ℕ-weight is (0,0)). But conclusion ⟹ constant matrix coeffs ⟹ ρ
    trivial — contradiction (nontrivial 3-dim rep).
  - Piece 1 (N=2, n=1): `M = V ⊕ det⁻¹`. Center acts by `t` on V, `t⁻²` on
    det⁻¹ — not the scalar `t¹•id`; yet `halg`, `h_homog` hold.
- **Correctness fix landed** (commit e84e7f77): threaded the
  genuinely-sufficient hypothesis
  `h_span : ⨆ (μ : Fin N →₀ ℕ), glWeightSpace k N M (fun i => μ i) = ⊤`
  (the all-weights-nonnegative / genuinely-polynomial condition; holds for Schur
  modules via `glWeightSpace_schurModule_iSup_eq_top`) through all four
  theorems. Statements are now **true**; proofs remain `sorry` (honest).
- `lake build EtingofRepresentationTheory.Chapter5.PolynomialRepEmbedding` passes
  (2 sorries: lines 806, 850 — same count as before, but now true statements).
- Commented on #4653 (signature change + it's now tractable) and #4654 (the
  finding; partial).

## Current frontier

The `det⁻¹`-elimination chain now has correct hypotheses. The two `sorry`s
(`scalarGL_acts_as_pow`, `hpoly'_of_scalarGL_action`) are unchanged in count but
now provable in principle.

## Overall project progress

Stage 3 (formalization). Schur-Weyl chain: #4598 core decomposition was
flawed (false statements); now corrected. With `h_span` a hypothesis:
- #4653 (`scalarGL_acts_as_pow`) is now the *short* half — exact-eigenspace
  action + `iSup` spanning. Self-contained.
- #4654 (`hpoly'_of_scalarGL_action`) is the hard det⁻¹-elimination proof
  (clear denominator, use weight-nonnegativity to force r=0, then
  `eq_of_eval_eq_on_gl`). Research-grade.

## Next step

- **Replan #4654**: re-scope to "prove `hpoly'_of_scalarGL_action` against the
  corrected (h_span) signature".
- Claim **#4653** — now tractable as a self-contained proof.
- Whoever consumes `polynomialRep_homogeneous_hpoly'` /
  `polynomial_homog_rep_equivariant_embedding` downstream (#2482, Schur-Weyl #5)
  must now supply `h_span` (a genuine polynomial rep does satisfy it).

## Blockers

None. The fix is contained (no external consumers of the four theorems yet).
The remaining proofs are hard but the statements are now correct.

## Quality metric delta

PolynomialRepEmbedding.lean: 2 sorries → 2 sorries (unchanged count), but four
theorems corrected from **false** to **true** statements (two of them
previously merged). Net correctness gain.
