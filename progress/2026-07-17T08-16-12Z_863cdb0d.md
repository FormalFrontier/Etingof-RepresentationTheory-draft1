## Accomplished

- Claimed **#6900** (`homComplexHomologyAddEquivₖ` — the homology-comparison crux of #6897, the
  `Etingof.Ext ≃ₗ[k] Extₖ` bridge for Problem 8.2.8 `Ext`).
- Landed the **genuinely-new mathematical content** of the crux in
  `EtingofRepresentationTheory/Chapter8/HomComplexHomologyK.lean` (sorry-free, builds clean):
  - `Etingof.homDegEquiv (i : ℕ) : Cochain P.cochainComplex (single 0 N) ↑i ≃+ (P.complex.X i ⟶ N)`
    — degreewise identification of the `AddCommGrp`-valued `HomComplex` term with the categorical
    hom underlying `linearYonedaObj`, via `Cochain.toSingleEquiv` + `cochainComplexXIso`.
  - `Etingof.homDegEquiv_δ` — the **degreewise sign twist**: `δ i (i+1)` becomes
    `(i+1).negOnePow •` precomposition with `P.complex.d (i+1) i`, versus the unsigned
    `linearYonedaObj` differential. This is "the one genuinely new piece" the issue isolates.
- Decomposed the residual (homology assembly) into **#6904** with a precise recipe, mirrored in a
  doc comment at the end of the new file.

## Current frontier

- `homComplexHomologyAddEquivₖ` itself is not yet built. Reduction (see #6904 / the file's doc
  comment): `ShortComplex.isoMk` from the three `homDegEquiv`s + `homDegEquiv_δ` comm squares, then
  `ShortComplex.homologyMapIso` + `mapHomologyIso` for `forget₂ (ModuleCat k) Ab` (which has
  `PreservesHomology`). A genuine `n = 0` boundary case is required: on the `ℤ`-indexed `HomComplex`
  side `L.X (-1) ≅ 0`, but on the `ℕ`-indexed `linearYonedaObj` side `(up ℕ).prev 0 = 0` gives
  `R.X 0 ≠ 0`, so the `sc` short complexes are not uniformly isomorphic — handle via
  `HomologicalComplex.isoHomologyπ` (homology = outgoing kernel when the incoming differential is 0).

## Overall project progress

- Stage 3 endgame, Ch8 Problem 8.2.8 `Ext` half. The Künneth core `Problem_8_2_8_extₖ` is assembled
  (earlier). This turn advanced the `Ext ≃ Extₖ` comparison (#6897) by landing the reusable
  homology-comparison sign-twist core; residual assembly is #6904, then #6897, then #6898/final.

## Next step

- Pick up **#6904**: assemble `homComplexHomologyAddEquivₖ` from `homDegEquiv`/`homDegEquiv_δ`
  following the file's doc-comment recipe (ShortComplex transport + `forget₂` homology preservation +
  the `n = 0` kernel boundary).

## Blockers

- None. #6904 is `depends-on: #6900` and unblocks when the planner closes #6900 (replan) after this
  partial PR. The core content merges with this PR regardless.
