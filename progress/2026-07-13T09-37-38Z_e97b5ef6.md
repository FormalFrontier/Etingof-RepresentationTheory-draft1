## Accomplished
- `/work` → picked #6479 (assembly of `heisenberg_classification`), whose dependency
  #6478 (orbit helpers) had merged. **Fully completed it**: the classification theorem in
  `Chapter5/Exercise5_27_2_Heisenberg.lean` is now sorry-free; `#print axioms` shows only
  `propext`/`Classical.choice`/`Quot.sound`.
- Key realization: the exposed interface of `Etingof.Theorem5_27_1` ((i)-(vi)) does **not**
  include base-point independence ("same orbit ⟹ V iso"), so the sibling assemblies were
  stuck. Routed the entire proof through the explicit **character formula (iv)** instead:
  - Added `heisenbergPsi` (ℂ-valued additive character of `ZMod p`), its primitivity, and
    the orthogonality-collapse lemma `heisenberg_shear_sum` (via `AddChar.sum_mulShift`).
  - Derived closed-form characters `fixed_charρ` (fixed orbit, dim 1: char at `⟨(b,c),g⟩`
    is `ρ(g)·ζ^(β·b)`) and `free_char` (free orbit, dim p: `p·ζ^(γc)` if `g=1 ∧ b=0`, else
    `0` — independent of `β`).
  - **Pairwise non-iso** (`hFinj`): `FDRep.char_iso` gives equal characters; four index-pair
    cases distinguished by evaluating the closed forms at `(1,0,1)`, `(0,0,g)`, `(0,1,1)`.
  - **Completeness** (`hFcomplete`): fixed case recovers `ρ` from the `⊤`-stabilizer via
    `MulEquiv.subgroupCongr`+`topEquiv` and `hvi`; free case moves the base point
    `χ_{β,γ} ⇝ χ_{0,γ}` by proving equal characters (β-independence) and `charEq_iso`.
  - Dims from (v) + `index_top`/`index_bot`; counts via reindexing `Fin (card ι)` back to
    `ι = (ZMod p × Ĝ) ⊕ {γ ≠ 0}`.
- Merged clean PR #6482 at cycle start.

## Current frontier
#6479 done and sorry-free. PR created.

## Overall project progress
Stage 3 formalization ongoing. Ch5 Exercise 5.27.2 Heisenberg is now fully classified.
The **character-formula-driven** technique (compute char via (iv), distinguish/complete via
`FDRep.char_iso`/`charEq_iso`, no reliance on unexposed base-point independence) is the
template for the still-`sorry` sibling `semidirect_classification` (Dihedral) and
`affine_classification`.

## Next step
Apply the same character-formula template to `Exercise5_27_2_Dihedral.lean`'s
`semidirect_classification` (still `sorry` at ~line 243) and `Exercise5_27_2_Affine.lean`'s
`affine_classification`. Alternatively, expose base-point independence
(`inducedRepV_basepoint_independent`, already proved privately) as a conjunct (vii) of
`Etingof.Theorem5_27_1` to make future assemblies shorter.

## Blockers
None.
