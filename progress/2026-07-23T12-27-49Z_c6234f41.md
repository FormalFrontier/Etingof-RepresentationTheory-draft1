## Accomplished
Fixed #7516: `Theorem5_25_2.lean` failed a fresh-source check at line 1506 —
`is_simple_module_of_finrank_eq_one (Module.finrank_self ℂ)` pinned `Module ℂ V`
to `Complex.instModule`, so `IsScalarTower ℂ (MonoidAlgebra ℂ (GL2 p n)) ρ.asModule`
could not be synthesized. Applied the same fix already used in `Theorem5_4_6.lean`:
fix `V := ρ.asModule` explicitly and route finrank through `ρ.asModuleEquiv.finrank_eq`
+ `Module.finrank_self`. `lake env lean` now succeeds from source (EXIT=0), no new
sorry/admit/axiom.

## Current frontier
Single-file source regression repaired. No hypothesis strengthening, no changes to
the irreducibility / diagonal-decomposition / isomorphism-classification / swap endpoints.

## Overall project progress
Stage 3 formalization ongoing; this was a build-regression fix from the completeness audit.
#7563 (public principal-series dimension q+1 API) remains open and untouched.

## Next step
Pick another unclaimed completeness-audit issue.

## Blockers
None.
