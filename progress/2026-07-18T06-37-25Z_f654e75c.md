## Accomplished
Discharged #6941: proved `affine_two_branch_fork_leaves` in
`Chapter6/Problem6_1_3_continued_tildeE.lean`, sorry-free (axioms
`[propext, Classical.choice, Quot.sound]`). This is the `D̃ₙ` fork discriminator:
a connected acyclic affine Dynkin diagram with exactly two degree-3 vertices `v, w`
has two distinct leaf-neighbours at `w` (equivalently at each branch vertex).

New supporting lemmas added in the same file (all sorry-free):
- `linear_of_harmonic`, `spine_endpoint_sum` — spine-linearity arithmetic core
  (`2·g i = g(i-1)+g(i+1)` interior ⟹ `g 1 + g(k-1) = g 0 + g k`).
- `harm_neighbor_finset`, `harm_one/two/three` — reduce a null vector's harmonic
  row sum `2·f x = ∑ adjₓⱼ fⱼ` to a sum over an explicit neighbour finset.
- `outer_arm_linear` — linearise one outer arm at a hub: from a degree-≤2 component
  `S` with the `affine_arm_walk'` preconditions + adjacency-closedness, derive
  `f h = (L+1)·a`, `f nb = L·a` (tip value `a>0`), and `nb` is a leaf when `L=1`.

The main proof builds `G` as a tree, sets up generic `comp hub c` component
machinery (membership, closedness, unique hub-neighbour via acyclicity,
disjointness, internal connectivity), extracts the spine as the unique `v–w`
`getVert`-indexed path, runs `outer_arm_linear` on the four outer arms, proves
hub harmonicity (`harm_three`) and spine linearity (`spine_endpoint_sum`), then
`affine_two_branch_pinch` forces `L = M = P = Q = 1`.

## Current frontier
`affine_two_branch_fork_leaves` is landed. Its consumer
`affine_two_branch_deleted_isD` (~line 2775) still carries its own `sorry` — that
is a *separate* issue (the E-type ruling-out + reattach-point localisation that
uses this discriminator).

## Overall project progress
Stage 3 (formalization) ongoing. The `D̃ₙ` two-branch classification chain now has
its fork discriminator in place; the remaining gap toward
`affine_tree_two_branch_iso` is the `affine_two_branch_deleted_isD` assembly.

## Next step
A follow-up issue should fill `affine_two_branch_deleted_isD`'s sorry: delete the
leaf, transport to the finite `Dₖ` classification, and use
`dynkinType_eq_D_of_branch_two_leaves` + `affine_two_branch_fork_leaves` to rule
out `E₆/E₇/E₈` and localise the reattach point. (Tracked by the #6940 family.)

## Blockers
None.
