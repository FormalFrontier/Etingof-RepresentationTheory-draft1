## Accomplished
- Claimed #6019 (Exercise 7.9.8(a): the adjunction hom-set bijection
  `Hom(F⁻ᵢV, W) ≃ Hom(V, F⁺ᵢW)`).
- Assessed scope: the assembly is two Proposition-6.6.6-scale hom-set equivalences
  (the cokernel side for `F⁻` and the kernel/transport-heterogeneous side for `F⁺`)
  glued by `eL.trans eR.symm`. Too large for one session → worker-led decomposition.
- Landed a sorry-free decomposition scaffold in
  `EtingofRepresentationTheory/Chapter7/Exercise7_9_8.lean`:
  - shared structure `Etingof.AdjReducedData hi V W` (family `h v` for `v ≠ i` with
    arrow-compat (A) `compat` and source constraint (C) `constraint`);
  - helpers `arrowsOutOf_target_ne_source`, `revOut`;
  - two sub-equivalence lemmas `homFMinusEquivReduced`, `homTransportPlusEquivReduced`
    (each `sorry`, with detailed proof blueprints in their docstrings);
  - `Exactly Exercise7_9_8` is now assembled sorry-free from the two sub-equivalences.
  File builds (`lake build EtingofRepresentationTheory.Chapter7.Exercise7_9_8` ✔);
  only the two sub-lemmas carry `sorry`.
- Filed self-contained sub-issues #6031 (cokernel side, `homFMinusEquivReduced`) and
  #6032 (kernel/transport side, `homTransportPlusEquivReduced`); left the
  `Decomposed into #6031, #6032` breadcrumb on #6019.

## Current frontier
Two independent sub-proofs remain: #6031 (F⁻ cokernel side, the more tractable one —
`Submodule.liftQ` + `reflFunctorMinus_mapLinear_*`) and #6032 (F⁺ kernel side —
`LinearMap.codRestrict` + `transportReversedTwice_*` heterogeneity, mirrors Prop 6.6.6).

## Overall project progress
Phase 3 formalization ongoing. `Chapter7/Exercise7.9.8` remains `statement_formalized`
(main theorem still has transitive `sorry` via the two sub-lemmas); will move to
`sorry_free` once #6031 and #6032 both land.

## Next step
Claim #6031 (cokernel side) — it avoids the transport heterogeneity and its docstring
carries the full construction. Then #6032.

## Blockers
None. Sub-issues are independent; both build on the `AdjReducedData` structure landing
on `main` via this PR.
