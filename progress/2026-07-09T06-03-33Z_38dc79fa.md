## Accomplished

Session type: **feature** (issue #6022 — assemble Problem 3.8.4(i),(ii)).

Landed the **finite-extension case of part (i)**, sorry-free, in a new additive file
`EtingofRepresentationTheory/Chapter3/Problem3_8_4_Finite.lean` (imported by the Chapter3
aggregator):

- `Etingof.Problem3_8_4.bcRestrictScalars` — restricts an `L ⊗[K] A`-linear iso
  `L ⊗[K] V ≅ L ⊗[K] W` to an `A`-linear iso for the restricted-scalars module
  `bcModRestrict` (an `L ⊗[K] A`-linear map already commutes with the action of `1 ⊗ a`).
- `Etingof.Problem3_8_4.iso_of_baseChange_iso_finite` — for `[FiniteDimensional K L]`, the
  base-change iso forces `V ≃ₗ[A] W`. Proof: compose the power identification
  `baseChange_alinEquiv_pow` (#6020) on both sides with `bcRestrictScalars` to get
  `Vⁿ ≃ₗ[A] Wⁿ`, then cancel with `iso_pow_cancel` (#6021). `#print axioms` = only
  `propext, Classical.choice, Quot.sound`.

Both landed prerequisites (#6020 power identification, #6021 Krull-Schmidt cancellation) are
now consumed as intended.

## Current frontier

The two **general-`L`** main theorems in `Problem3_8_4.lean`
(`iso_of_baseChange_iso`, `directSummand_of_baseChange_directSummand`) remain sorried. Their
remaining ingredients are each prerequisite-sized and were split into sub-issues:

- **#6049** — direct-summand power cancellation `Vⁿ | Wⁿ ⟹ V | W` (needed for part (ii); a
  fibre-inequality refinement of the existing `fibre_cancel` + a complement construction,
  comparable in size to `iso_pow_cancel`).
- **#6050** — finite-extension descent reducing general `L` to a finite subextension `L₀`
  (the book's "finitely generated, then finite" step), which discharges both main theorems
  by calling `iso_of_baseChange_iso_finite` / the finite case of (ii). Heaviest ingredient.

## Overall project progress

Stage 3 formalization. Problem 3.8.4: statement + base-change infrastructure + both
prerequisites + finite case of (i) all sorry-free; general-`L` (i) and (ii) pending #6050
(and #6049 for (ii)). Item `Chapter3/Problem3.8.4` stays `statement_formalized` (main
theorems still carry sorry); coverage note updated.

## Next step

Pick up **#6050** part-(i) descent first (it is independent of #6049 and discharges
`iso_of_baseChange_iso` on its own), then **#6049**, then #6050 part (ii).

## Blockers

None. The remaining work is captured in #6049 and #6050. This PR is partial (finite case of
(i) only); #6022 is marked `replan` for a planner to narrow to the residual scope now covered
by the two sub-issues.
