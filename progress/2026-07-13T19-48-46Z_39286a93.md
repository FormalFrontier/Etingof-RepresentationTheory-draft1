## Accomplished

Session type: **feature** (issue #6514, Ch9 — deliverables 2–3 of the standard resolution
length grading). Landed a coherent subset: the **coordinate map on `A ⊗_S M`** (deliverable 3's
reusable core), the noncommutative analogue of `coordMapCH`.

New file `EtingofRepresentationTheory/Chapter9/PathAlgebraInducedGrading.lean` (sorry-free,
builds clean, added to `Chapter9.lean`):

- `ofPath_mul_vertexIdem`, `ofPath_mul_vertexEmbedding`, `single_mul_vertexEmbedding` — right
  multiplication by `f s = vertexEmbedding s` scales a basis path by its target coordinate, hence
  preserves length degree.
- `lengthProj_mul_vertexEmbedding`, `lengthGrading_mul_vertexEmbedding` — the length grading is
  `S`-linear for the right action `s • a = a * f s` (the action the tensor `A ⊗_S M` is over).
- `lengthGradingS`, `lengthTotalizeS` — the grading and reassembly maps bundled as
  `(Q → k)`-linear maps, with `lengthTotalizeS_comp_lengthGradingS` (left inverse).
- `inducedCarrier`, `inducedCoordMap M : A ⊗_S M →ₗ[Q → k] (ℕ →₀ A ⊗_S M)`, `inducedCoordMap_tmul`
  (`inducedCoordMap M (a ⊗ₜ m) n = lengthProj n a ⊗ₜ m`), and `inducedCoordMap_injective`
  (via the `TensorProduct.map` left inverse + `TensorProduct.finsuppLeft` bijection).

## Current frontier

Deliverable 3's coordinate map + injectivity landed. Deliverable 2 (cons-splitting iso
`A_n ⊗_S V ≅ A_{n+1}`) and the coupled `d` top-degree/shift packaging are **not** done — they
require a genuine bundled `≃ₗ` with a real (non-`sorry`) inverse over the graded pieces `A_n`,
plus the `ArrowTgt`/`VtensCarrier` diamond machinery applied to `A_n`. Scoped as a self-contained
sub-issue **#6526**; parent #6514 decomposed (breadcrumb comment left).

## Overall project progress

Problem 9.4.6 (i) chain: length grading of `A` (#6510 d1) → coordinate map on `A ⊗_S M` (this PR)
→ cons-splitting iso + d-shift (#6526) → `standardResolution_shortExact` (#6512) →
`hasHomologicalDimensionLE_pathAlgebra_one` (#6438). This PR advances the third link's coordinate
half.

## Next step

Claim **#6526**: build the cons-splitting iso `A_n ⊗_S V ≅ A_{n+1}` (real inverse decomposing a
length-`(n+1)` path via `Quiver.Path.length_ne_zero_iff_eq_cons`) and the top-degree/shift lemma
for `d`, reusing `inducedCoordMap` / `inducedCoordMap_tmul` / `lengthProj_ofPath_mul_arrowElt`.
Mind the `PathAlgebra` def-opacity trap (see `single_mul_vertexEmbedding`).

## Blockers

None. #6526 builds on `PathAlgebraInducedGrading.lean` once this partial PR merges to `main`.
