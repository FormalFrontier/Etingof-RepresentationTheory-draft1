## Accomplished

Formalized the honest conclusion of **Problem 5.2.7(b)** (issue #7353): for an
irreducible complex representation `V` of a finite group `G` with `dim V > 1`,
there exists `g` with `χ_V(g) = 0`.

New file `EtingofRepresentationTheory/Chapter5/Problem5_2_7.lean` (sorry-free,
axiom-clean `[propext, Classical.choice, Quot.sound]`):

- `beta_lt_one` — the `0 < β < 1` bound, the only step using irreducibility and
  `dim V > 1`. First orthogonality relation (`FDRep.char_orthonormal`) gives
  `∑_g |χ_V(g)|² = |G|`, hence `∑_{g≠1} |χ_V(g)|² = |G| - (dim V)²`, which is
  `< |G| - 1 = #{g≠1}` because `(dim V)² ≥ 4`. AM-GM
  (`Real.geom_mean_le_arith_mean`, all weights `1`) forces the product `< 1`;
  positivity of each factor (`Complex.normSq_pos` under the contradiction
  hypothesis) gives `0 < β`.
- `sum_char_mul_inv_eq_card` — the orthonormality sum cleared of its `⅟|G|`.
- `exists_character_eq_zero` — the top-level theorem. Assuming `χ_V(g) ≠ 0`
  everywhere, `β = ∏_{g≠1} χ_V(g)·χ_V(g⁻¹)` is rational
  (`Remark5_2_8.character_prod_rat`) with value `q`, and `beta_lt_one` gives
  `0 < q < 1`; `Remark5_2_8.beta_rat_not_mem_Ioo` derives `False`.

Wired into `Chapter5.lean`. Updated `progress/items.json`: item
`Chapter5/Problem5.2.7` gains `coverage: covered_partial` (part (a), the finite
Galois extension, is still unformalized) with a refreshed `coverage_note`, plus a
new part-(b) `derived` entry marked `covered_full` (`coverage_issue: 7353`).

## Current frontier

Problem 5.2.7 part (b) fully formalized. Part (a) — existence of a finite Galois
extension `K ⊂ ℂ` in which every finite-dimensional complex representation of `G`
has a matrix realization — remains the open gap for this item.

## Overall project progress

Stage 3.7 exercise-coverage ratchet sweep ongoing. This turn completed a feature
item (not an audit): the honest Problem 5.2.7(b) conclusion now exists as a
sorry-free, axiom-clean theorem, discharging the `0 < q < 1` hypothesis that
`Remark5_2_8.beta_rat_not_mem_Ioo` previously left open.

## Next step

Consider a `feature` item for Problem 5.2.7 part (a) (the finite Galois extension
carrying all matrix entries) to bring the top-level item to `covered_full`. The
remaining unclaimed reviews (#7342 §5.8 induction, #7346 Exercise 5.3.3) are
independent coverage-arm audits.

## Blockers

None.
