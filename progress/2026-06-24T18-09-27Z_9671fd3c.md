## Accomplished

Worked issue #5201 (feat(Ch5): FS-indicator bridge — `frobeniusSchurIndicator ρ = 1 ⟹ IsRealType ρ`
for simple reps). Created `EtingofRepresentationTheory/Chapter5/FrobeniusSchurRealType.lean`
and added it to the `Chapter5.lean` aggregator.

- **`Etingof.nondegenerate_of_invariant_of_simple`** — fully proved (no `sorry`,
  no `sorryAx`). A nonzero `G`-invariant bilinear form on a simple representation
  is automatically nondegenerate: its left radical `ker B` is shown to be a
  `ρ`-invariant submodule, then `IsSimpleModule (MonoidAlgebra ℂ G) ρ.asModule` is
  transported to `IsSimpleOrder ρ.invtSubmodule` via `Representation.mapSubmodule`,
  forcing `ker B ∈ {⊥, ⊤}`; `B ≠ 0` rules out `⊤`, so `ker B = ⊥`, which is exactly
  nondegeneracy. Reusable; needs neither symmetry nor finite-dimensionality.
- **`Etingof.isRealType_of_frobeniusSchurIndicator_eq_one`** — the bridge theorem
  from the issue, assembled: extract a nonzero invariant symmetric form, attach
  nondegeneracy from the lemma above, package as `IsRealType`. Compiles; depends on
  `sorryAx` only through the one isolated helper.
- **`Etingof.exists_nonzero_invariant_symmetric_of_FS_eq_one`** — steps 1–3 of the
  Etingof strategy (the trace identity `dim(sym∩Bil^G) − dim(skew∩Bil^G) = FS(ρ)`
  plus the Schur bound `dim Bil^G ≤ 1`). Isolated as a single `sorry` with a full
  strategy docstring.

`lake build EtingofRepresentationTheory.Chapter5.FrobeniusSchurRealType` succeeds
(one expected `sorry` warning at the helper).

## Current frontier

The bridge theorem and the nondegeneracy half are done. The remaining content is
the trace-identity heart, isolated in `exists_nonzero_invariant_symmetric_of_FS_eq_one`.

## Overall project progress

Stage 3 (formalization), Chapter 5 §5.1. Example 5.1.3 (parent #5186) S₃/S₄/A₅
cases can now consume the bridge theorem once the helper lands; the nondegeneracy
bridge is independently usable today.

## Next step

Prove `exists_nonzero_invariant_symmetric_of_FS_eq_one`. Concrete plan (see the
docstring): realise `Bil = Representation.linHom ρ ρ.dual`; build the swap
involution `τ` and its `+1`/`-1` (symmetric/skew) eigenspaces inside `Bil^G`;
prove the Schur bound `finrank ℂ (linHom ρ ρ.dual).invariants ≤ 1` via
`invariantsEquivIntertwiningMap` + `card_inv_mul_sum_char_mul_char_eq_finrank`
(needs dual-of-simple-is-simple); then the crux `trace(τ ∘ P) = (|G|)⁻¹ ∑_g χ(g²)`
with `trace(τ ∘ (g·)) = χ_V(g²)` on `V* ⊗ V*`. A follow-up issue tracks this.

## Blockers

None for the landed content. The remaining helper is a hard (difficulty-H) trace
computation, tracked in a follow-up issue.
