## Accomplished

- Confirmed redundant post-merge `main` CI run 30700258835 completed successfully, including the full module build and warning ratchet.
- Prototyped a declaration-reference certificate, ran an independent Opus review, and rejected/removed that certificate after the review correctly identified that it did not satisfy the project's proof-term or proof-by-proof review requirements.
- Verified from Lean 4.32's API and a live project theorem that imported theorem bodies are available through `ConstantInfo.value? (allowOpaque := true)`.
- Added `scripts/extract_proof_dependencies.lean`, which emits direct kernel-term and type dependencies for project declarations.
- Restored `progress/items.json`, `dependencies/internal.json`, `PROGRESS.md`, and `README.md` to their honest pre-review state.

## Current frontier

Stage 3.4's actual proof-term extractor works and now needs a Python mapping/certificate layer, source hashes, validation, and reconciliation with the conservative 521-edge import DAG. Stage 3.5 remains at `diagnostic_pass_complete`; no metadata promotion is acceptable until proofs are actually reviewed and polished.

## Overall project progress

Stages through 3.3 and Stage 3.6 remain complete. Stage 3.4 has a complete import-DAG pass and a working kernel-term extraction primitive. Stage 3.5 has a repository-wide diagnostic pass with 1,461 ratcheted warnings, but proof-by-proof review remains. Post-merge `main` CI is green.

## Next step

Build the Stage 3.4 certificate from the kernel-term extractor, conservatively union/reconcile it with import evidence, bind it to source/toolchain hashes, validate it, and independently review it. Then perform substantive Stage 3.5 provider/proof cleanup rather than relabeling diagnostics.

## Blockers

None.
