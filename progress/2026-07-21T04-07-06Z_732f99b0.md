## Accomplished

Feature session on issue #7130 (cleanup of Ch4 Theorem 4.1.1 / Maschke).

- **Deliverable 1 (done):** Dropped the unused `[DecidableEq G]` hypothesis from
  `Etingof.Theorem4_1_1_semisimple` in
  `EtingofRepresentationTheory/Chapter4/Theorem4_1_1.lean`. The
  `unusedDecidableInType` linter warning (line 30) confirmed it was unused in the
  type; the proof (`infer_instance` for `IsSemisimpleRing`) needed no decidability
  instance, so no `classical` was required. Mirrors the #7118 / PR #7122 precedent.
  - `lake build EtingofRepresentationTheory.Chapter4.Theorem4_1_1` exits 0, warning gone.
  - `#print axioms Etingof.Theorem4_1_1_semisimple` = `[propext, Classical.choice,
    Quot.sound]` (verified via a temporary scratch import, since removed) — unchanged.

- **Deliverable 2 (deferred, optional):** Recording the representation-level regular-rep
  decomposition `k[G] ≅ ⊕ᵢ dim(Vᵢ)·Vᵢ` as an `FDRep` iso is genuinely heavy — the
  infrastructure has only the **algebra** iso `endIso` and no `regularFDRep`, so it needs
  the regular representation packaged as an `FDRep` plus an equivariant (not merely
  algebra-level) Wedderburn decomposition. Per the issue's "leave a proof-wanted note if
  heavy" guidance, I added a `proof-wanted` block at the end of `Theorem4_1_1.lean` and
  filed follow-up feature issue **#7134** with a fix recipe.

## Current frontier

Ch4 Theorem 4.1.1 file is warning-clean. The equivariant/representation-level content of
part (ii) remains a documented `proof-wanted` (issue #7134).

## Overall project progress

Confidence phase: per-chapter statement-fidelity audits and their surfaced cleanups.
This turn cleared one such cleanup (#7130 deliverable 1) and routed the coverage half to
#7134. Proof tree remains complete.

## Next step

Remaining unclaimed work includes review #7128 (Ch2 Prop 2.3.9 Schur's Lemma audit) and
the new #7134 (equivariant regular-rep decomposition). #7128 is the lighter pickup.

## Blockers

None.
