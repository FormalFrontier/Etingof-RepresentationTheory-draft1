## Accomplished

Completed issue #5062 (Ch5 #4905 glue-1): all three deliverables, sorry-free, in a
new file `EtingofRepresentationTheory/Chapter5/QuotDetDegreeAlgebraic.lean` (added to
`Chapter5.lean`).

1. **`IsAlgebraicRepresentation.of_surjective_equivariant`** — general transport lemma:
   algebraicity passes along a surjective `GL_N`-equivariant `k`-linear map onto a
   finite-dimensional target. Proved by mirroring `IsAlgebraicRepresentation.restrict`,
   replacing the invariant-submodule inclusion with a `k`-linear section `s` of the
   surjection (`LinearMap.exists_rightInverse_of_surjective`) and the projection with the
   given map.

2. **`polyRightDegreeFDRep_isAlgebraic`** — the degree-`d` coordinate-ring component
   `A_d` is algebraic. Built the degree-`d` monomial basis of the homogeneous submodule
   (`Module.Basis.mk` over `Finset.univ.finsuppAntidiag d`, with `repr = coeff`), and
   exhibited the right-translation matrix coefficients as the polynomials
   `rightTransPoly`. Key device: `rightTransPoly` runs `rTransAlgHom` over the *generic*
   matrix of coordinate variables; `evalAtGL_rightTransPoly` proves naturality
   (`MvPolynomial.map (evalAtGL g)` turns the generic right translation into the genuine
   one) via `MvPolynomial.ringHom_ext`. This avoids all explicit monomial-expansion
   combinatorics.

3. **`quotDetDegreeFDRep_isAlgebraic`** — `(A/det)_d` is algebraic, obtained from (1)+(2)
   through the surjective equivariant quotient map `A_d → (A/det)_d` (restriction of
   `Submodule.mkQ (detSubmodule)`), equivariance from `quotDetRep_mk`.

`#print axioms` confirms all three depend only on `propext, Classical.choice, Quot.sound`
(no `sorryAx`). Full build green (8110 jobs), no lint warnings.

## Current frontier

#5062 is fully delivered. The named result `Etingof.quotDetDegreeFDRep_isAlgebraic` is
ready to supply the `halg : IsAlgebraicRepresentation N M.ρ` hypothesis that the #4905
final assembly (`quotDetRep_irreducible_constituent_lastWeight_zero`,
`CauchyDetQuotient.lean:124`) needs for `M = quotDetDegreeFDRep k N d`.

## Overall project progress

Stage 3 (formalization) ongoing in Chapter 5. This closes glue-1 of the #4905 epic;
glue-2 (#5063, weight-space spanning + degree-`d` homogeneity) and glue-3 (#5064,
character as antitone-indexed Schur sum) remain unclaimed, and #4905 stays blocked on all
three.

## Next step

Pick up #5063 or #5064 (the other two #4905 glue inputs), or any other unclaimed
`feature` issue. Once all three glue issues land, #4905's assembly can be discharged.

## Blockers

None.
