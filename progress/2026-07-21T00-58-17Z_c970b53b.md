## Accomplished

Resolved issue #7072 (Ch6 docstring-fidelity). Corrected two stale
"separate / not-yet-formalized item. Tracked as a sub-issue" claims in
`EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean`, which is sorry-free:

- `mckayAdj_no_selfLoop` docstring (was lines 1404-1406): rewrote to state the
  SU(2) infrastructure of Problem 4.12.8 is proved sorry-free in-file, naming
  `negIdSU` / `negIdSU_central` (central `-Id`),
  `su2_finite_cyclic_or_contains_negId` (dichotomy), and
  `mckayAdj_no_selfLoop_cyclic` (cyclic case). Dropped "not-yet-formalized item.
  Tracked as a sub-issue."
- The `### The central element -Id` section comment (line 919): corrected the
  "separate sub-issues" clause to state `rᵢᵢ = 0` is proved sorry-free in-file,
  in both the non-cyclic case (`mckayAdj_no_selfLoop_of_central_neg`) and the
  cyclic case (`mckayAdj_no_selfLoop_cyclic`), assembled in
  `mckayAdj_no_selfLoop`.

Comment-only edit; kept accurate references to Problem 4.12.8 as the book source.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter6.Problem6_1_6` succeeds (only
pre-existing `push_neg` deprecation warnings). Verification greps for
`not-yet-formalized|Tracked as a sub-issue|separate sub-issues` return nothing;
no proof-level sorry in the file.

## Overall project progress

Ongoing docstring-fidelity sweep. This is one file corrected. Issue #7074
(Ch4 `Exercise4_2_3.lean`) remains unclaimed in the same sweep.

## Next step

Land the PR for #7072. A subsequent worker can pick up #7074.

## Blockers

None.
