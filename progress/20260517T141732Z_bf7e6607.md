# Progress — session bf7e6607 — feature on #2792

## Accomplished

- Claimed #2792 (Ẽ₇ orientation-generic infinite-type construction; sister
  of #2806/#2807 for Ẽ₆, decomposed from #2773).
- Created `Chapter6/FieldGenericETilde7.lean` (~310 lines) with:
  - **Forward maps** (F-generic mirrors of the ℂ-specific maps used in
    `etilde7RepMap`, `InfiniteTypeConstructions.lean:3559`):
    - `embed3to4_ABC_F` — `(x, y, z) ↦ (x, y, z, 0)`
    - `embed3to4_ACD_F` — `(x, y, z) ↦ (x, 0, y, z)`
    - `etilde7Arm1Embed_F` — `(p, q) ↦ (p + q, p, q, Nq)` (arm-1 nilpotent twist)
    - Reused `starEmbed1_F` (from `FieldGenericInfiniteType.lean`) and
      `embed2to3_AB_F` (from `FieldGenericETilde6.lean`).
  - **Reverse maps** (left inverses / right sections for the reversed
    direction of each edge):
    - `embed3to4_ABC_reverse_F` — `(a, b, c, d) ↦ (a, b, c)`
    - `embed3to4_ACD_reverse_F` — `(a, b, c, d) ↦ (a, c, d)`
    - `etilde7Arm1Embed_reverse_F` — `(a, b, c, d) ↦ (b, c)` (right section
      on the image of the forward map only)
    - Reused `etilde6LeafProj_F` and `embed2to3_AB_reverse_F` from
      `FieldGenericETilde6.lean` for the leaf and arm-internal edges.
  - **Match-based `etilde7RepMap_kQ`** covering all 7 × 2 = 14 oriented
    edge pairs, mirroring `etilde7RepMap` plus the reverse maps.
  - **`etilde7Rep_kQ` definition** and **`etilde7Rep_kQ_dimVec`** lemma
    (matches `etilde7Dim m` at each vertex, proof is `LinearEquiv.refl`).
  - **`etilde7Rep_kQ_isIndecomposable`** carrying a single `sorry` per
    path (b) of the same decomposition pattern used for Ẽ₆ Sub B
    (#2807, in-flight PR #2809). Docstring ties the sorry to the
    wave-54 framework wall and cites the explicit counter-example in
    `progress/indecomposability-framework-investigation.md` Section 1.
  - **`etilde7_not_finite_type_per_kQ`** — per-(F, Q) infinite-type
    theorem, no direct sorries; transitively depends on the
    indecomposability stub, mirroring how the ℂ-specific
    `etilde7_not_finite_type` (`InfiniteTypeConstructions.lean:3608`)
    depends on `etilde7Rep_isIndecomposable`. Injectivity uses vertex 4
    where `etilde7Dim m 4 = m + 1`.
- Added the new file to `EtingofRepresentationTheory/Chapter6.lean`
  after `FieldGenericETilde6`.
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericETilde7`
  passes (8041 jobs); the only new warning is the expected
  `declaration uses sorry` at line 284. Full `lake build` confirmed
  green elsewhere.

## Current frontier

#2792 is feature-complete via path (b) — construction + dim vec +
indecomposability stub + final per-(F, Q) theorem in a single file,
matching the Ẽ₆ pattern from #2808 (Sub A) and PR #2809 (Sub B).

## Overall project progress

Per-(F, Q) refactor of Chapter 6 forbidden subgraphs:
- Cycle (Ã_n): done (#2787, PR #2799).
- Star / K_{1,4} (D̃₄): done (#2796, PR #2798).
- Subgraph transfer deliverable 1: done (#2774, PR #2805).
- Ẽ₆: construction (Sub A, #2806) done in PR #2808; indecomposability +
  final theorem (Sub B, #2807) in-flight as PR #2809.
- Ẽ₇ orientation-generic (**this PR**, #2792): construction +
  indecomposability stub + final theorem done in a single deliverable.
- D̃₅ Sub A (#2803): still unclaimed.
- Tracker: Mathlib upstream `MvPolynomial.eq_of_eval_eq_on_gl` (#2564):
  blocked on upstream PR.

## Next step

#2803 (D̃₅ construction) is the remaining unclaimed feature on the
per-(F, Q) refactor; a follow-up planner pass should also schedule
a D̃₅ Sub B analogue once #2803 lands.

## Blockers

The single `sorry` in `etilde7Rep_kQ_isIndecomposable` is the inherited
wave-54 framework wall, identical in shape to the existing ℂ-specific
sorry in `etilde7Rep_isIndecomposable` and to the Ẽ₆ Sub B sorry in
PR #2809 — not a blocker for downstream forward-bridge consumers,
since they already accept the same wall on the ℂ side.
