## Accomplished
Claimed #6683 (assemble `extTensorProjectiveResolution`). Created
`EtingofRepresentationTheory/Chapter8/ExternalTensorResolution.lean`:
- `Etingof.extTensorComplex_projective` — **sorry-free** (axioms:
  propext, Classical.choice, Quot.sound). Each degree `(extTensorComplex P₁ P₂).X n`
  is the coproduct `∐_{i₁+i₂=n} (P₁)_{i₁} ⊗ₖ (P₂)_{i₂}`, projective via
  `extTensor_projective` (#6681). The lifting property is built by hand from
  `Sigma.desc`/`Sigma.ι` because the coproduct-`Projective` instance uses
  `backward.isDefEq.respectTransparency false`, whose full-transparency defeq does
  not terminate on the heavy bifunctor summands.
- `Etingof.extTensorProjectiveResolution` — the `ProjectiveResolution` `def` with
  real data (`complex`, `π`, `projective`); the `quasiIso` field is a `sorry`
  (a proof obligation *within* the def; the object itself is real).
- Added the file to `Chapter8.lean` aggregate.
- Helper `extTensorComplex_X_eq` (rfl at the `mapObj` level; unfolding to `∐`
  directly explodes `isDefEq`, so the proof stops one step short and `show`s the
  coproduct form).

## Current frontier
The `quasiIso` obligation (resolution exactness) is the only remaining piece of
#6683. Filed as follow-up #6729 with the full route (restriction-of-scalars reflects
QuasiIso → `res` carries `extTensorComplex` to the k-tensor total complex → Ch7
`Problem7_8_7_ii` acyclicity + ℕ/ℤ reindex).

## Overall project progress
Stage 3 formalization, Chapter 8 Problem 8.2.8 (Tor). Tensor-of-resolutions
sub-tree: external tensor complex (#6680), degreewise projectivity (#6681), and now
the `ProjectiveResolution` assembly with projectivity discharged. Remaining for the
Tor half: quasiIso (#6729), the four-fold rearrangement (milestone c #6687), and the
final Künneth Tor assembler #6657.

## Next step
Pick up #6729 (quasiIso). Consider splitting into (a) exact-conservative-functor
reflects QuasiIso, (b) res∘mapBifunctor commutation iso, (c) acyclicity assembly.

## Blockers
None. #6729 is unblocked (all its inputs — Problem7_8_7_ii, KunnethChainComplexNat —
are in `main`).
