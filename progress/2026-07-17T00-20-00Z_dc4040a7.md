## Accomplished

Worked #6881 (`affine_tree_two_branch_iso`, Ch6 tree case). Landed the **(A) deliverable**
of the delete-leaf reduction strategy, and decomposed the remaining crux into #6893.

- **De-privatized** `branch_classification`, `tree_branch_iso`, `branch_has_leaf_neighbor` in
  `Chapter6/Theorem_Dynkin_classification.lean` (removed `private`; build stays green). These
  finite-classification helpers are now consumable by the affine tree-case reductions
  (#6893 two-branch, #6882 one-branch).
- **Proved `affine_two_branch_has_leaf` sorry-free** in `Problem6_1_3_continued_tildeE.lean`:
  in a connected acyclic affine Dynkin diagram with two branch vertices `v, w`, some leaf is
  adjacent to `v` or `w`. Axiom-clean (`propext, Classical.choice, Quot.sound`).
- **Cleaner argument found:** the leaf-existence does *not* need the balance/arm-length
  computation. A tree leaf `u` deletes to a finite Dynkin diagram (`affine_delete_leaf_isDynkin`);
  if `u` neighboured neither `v` nor `w` both would keep degree 3 there, giving two branch
  vertices in a finite Dynkin diagram, contradicting `dynkin_unique_degree_three`. Reuses the
  `hsubdeg` degree-preservation machinery already inside `affine_tree_branch_count`.
- **Filed #6893** for the residual (B): the reattach reindexing onto `AffineType.Dtilde`
  (delete leaf → finite `branch_classification` → reattach), consuming `affine_two_branch_has_leaf`.
  Left a `Decomposed into #6893` breadcrumb on #6881.

## Current frontier

`affine_tree_two_branch_iso` (~line 2585 of `Problem6_1_3_continued_tildeE.lean`) still `sorry`.
Its affine-specific structural input (`affine_two_branch_has_leaf`) and all finite helpers are now
in place; only the reattach reindexing bookkeeping remains — tracked in #6893.

## Overall project progress

Stage 3 (formalization) endgame; Ch6 affine Dynkin classification (#6785 tree case) frontier.
This turn: 1 helper lemma landed sorry-free + a mechanical de-privatization; sorry count in
`Problem6_1_3_continued_tildeE.lean` unchanged at 3 (`two_branch_iso`, `one_branch_iso`,
`affine_dynkin_classification` ⟹ direction).

## Next step

Pick up #6893: consume `affine_two_branch_has_leaf` + the now-public finite `branch_classification`
to build the `AffineType.Dtilde m` reindex. Mirror the finite `tree_branch_iso` leaf-reattach
(`Fin.succAbove`, `Equiv.ofBijective`). The same reduction applies to #6882 (one-branch → Ẽ).

## Blockers

None. #6893 is unblocked (all its inputs landed sorry-free on this branch / to be merged).
