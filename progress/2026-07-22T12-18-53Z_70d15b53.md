## Accomplished

Feature session on issue #7307 (iso-level classification for Problem 4.12.6). All four
deliverables landed, sorry-free and axiom-clean (`[propext, Classical.choice, Quot.sound]`):

1. **Reusable bridge** `Etingof.equivOfAsModuleLinearEquiv` in
   `Infrastructure/SimpleModuleCount.lean`: a `k[G]`-linear equivalence
   `ρ.asModule ≃ₗ[k[G]] σ.asModule` underlies a `Representation.Equiv ρ σ`. Built from
   `Representation.IntertwiningMap.equivLinearMapAsModule ρ σ |>.symm` composed with
   `IntertwiningMap.ofBijective` (already in Mathlib — no need to hand-roll `LinearEquiv.ofBijective`
   plus the intertwining proof).
2. **`exists_simpleFDRep'`** (Problem4_12_6.lean): strengthens `exists_simpleFDRep` to also return
   `Nonempty (ρ.Equiv U.ρ)` for the transported `Type 0` model, via deliverable 1 applied to
   `eR ≪≫ₗ (repOfModuleAsModuleEquiv _).symm`.
3. **Uniqueness of the `(q-1)`-dim simple**: `simple_FDRep_finrank_eq_unique` (FDRep level) and
   `Vrep_unique` (Representation level: every `(q-1)`-dim irreducible is iso to `V`), via character
   equality + `equiv_of_character_eq`.
4. **Full classification**: factored the enumeration completeness into `simple_FDRep_iso_enum`
   (reused by all uniqueness lemmas), added `simple_FDRep_finrank_one_iso_charRep` / `charRep_exists`
   (1-dim irreducibles are characters), and `irreducible_classification`: every irreducible is iso
   to some `charRep χ` or to `V`.

Key discoveries: `FDRep.ρ` returns a raw `MonoidHom` (not a `Representation`), so `U.ρ.character`
dot-notation misresolves to `MonoidHom.character` — use `Representation.character U.ρ` instead.
`FDRep.character U` and `Representation.character U.ρ` are definitionally the same trace, so
`FDRep.char_iso` retypes to a `Representation.character` equality directly. Also
`FDRep.complete_of_sum_finrank_sq_eq_card` is constrained to same-universe `{k G : Type u}`, so with
`K : Type*` it cannot be used; instead reuse the universe-polymorphic
`exists_simples_sum_finrank_sq_eq_card` enumeration (as `irreducible_dim` does).

## Current frontier

Problem 4.12.6 iso-level classification complete. Full `lake build` (8587 jobs) passes.

## Overall project progress

Stage 3 formalization + Stage 3.7 fidelity auditing ongoing across Chapter 4. Problem 4.12.6 now
has the complete up-to-isomorphism classification of its irreducibles (both the characters and the
zero-sum representation), in addition to the previously landed characters/tensor-product results.

## Next step

Open review issues remain: #7302 (Problem 4.12.11 elasticity), #7306 (Problem 4.12.7 quaternions).
Feature #7308 (V⊗V ≅ (⊕χ)⊕(q-2)V, reducible characters-determine-rep) is still open and is the
hardest remaining piece of 4.12.6; it needs isotypic/multiplicity decomposition machinery.

## Blockers

None.
