# Progress — feature session 83fe2673 (#2915)

## Accomplished

Closed #2915: wired `embed_t125_in_tree_per_kQ` (the shared T(1, 2, 5)
per-(F, Q) embed helper landed by #2913 / PR #2917) into the
`d₃-extends` branch of `single_branch_leaf_both_extend_b2leaf_per_kQ`
in `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`.

The renaming
`(v₀, u₁, p₁, p₂, q₁, q₂, q₃, q₄, q₅) ↦ (v₀, leaf, a₂, b₂, a₃, b₃, c₃, d₃, e₃)`
is the symmetric image of the sub-B sibling under swapping arms 2 and 3.
Unlike the sub-B sibling (#2917) the arm role is *not* swapped — arm 2
(length 2, with `b₂` as leaf) maps to `(p₁, p₂)` and arm 3 (length ≥ 5,
with `b₃` extending) maps to `(q₁, ..., q₅)` — so the 3rd
non-equality argument is `ha₂₃` (no `.symm`), whereas the sibling
needed `ha₂₃.symm`. All other named-vertex hypotheses are already
constructed earlier in the helper body.

Two `_he₃_adj` / `_he₃_ne_c₃` placeholders extracted at the top of the
branch were renamed (underscore stripped) so they could be passed by
name. `let _ := h_a2_ext` was dropped (the degree-2 fact is absorbed by
the embedded subgraph). Diff is 10/11 lines — pure plumbing.

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericTpqr`
passes. The d₃-extends `sorry` (previously at line 977 of `FieldGenericTpqr.lean`) is gone;
`single_branch_leaf_both_extend_b2leaf_per_kQ` is now sorry-free
modulo the propagating inherited sorries from
`t125_not_finite_type_per_kQ` (D1 stub, Wall 1).

## Current frontier

`FieldGenericTpqr.lean` post-session sorry inventory (1 sorry):

- `single_branch_leaf_case_both_extend_per_kQ` (line 1286) — outer
  wiring helper. The "both arms extend" body that dispatches sub-A
  (Ẽ₇) / sub-B / sub-C / sub-D. Tracked by #2905; depends on sub-A
  PR #2907 landing first.

Sub-C status:

- C.1 c₃-leaf (E₇ posdef): ✅ on main (#2916).
- C.2 d₃-leaf (E₈ posdef): ✅ on main (#2916).
- C.3 d₃-extends (T(1, 2, 5) embed): ✅ this PR.

## Overall project progress

Stage 3 (Lean formalisation). Sorry count delta: -1 (closes the
d₃-extends sorry, introduces none). Expected post-merge total: 19
(down from 20 after #2917 landed).

Chapter 6 per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard:

- D2.degree4 / D2.cycle / D2.adjacent / D2.single — on `main`.
- D2.single follow-up `single_branch_leaf_case_per_kQ` — scaffold +
  leaf-leaf on `main` (#2906); `both_extend` body still under #2905.
- D2.single sub-cases:
  - sub-A (Ẽ₇, q, r ≥ 3): #2907 has-pr.
  - sub-B (T(1, q, 2)): fully landed (#2914 + #2917).
  - sub-C (T(1, 2, r)): fully landed (#2916 + this PR).
  - sub-D (T(1, 2, 2) = D₅): on `main` (#2912).
- **Pending**: `single_branch_leaf_case_both_extend_per_kQ` wiring
  (#2905) once sub-A (#2907) lands; D2.nonadj, D2.outer + D3 (#2877).

## Next step

- Pod planner triages #2909's `replan` label (sub-C is now complete —
  close with forward link to #2915 + this PR).
- Pod repair / planner cycles should usher sub-A (#2907) to merge so
  the outer wiring (#2905) becomes claimable.
- Once #2905 lands, the per-(F, Q) D2.single helper is sorry-free
  (modulo inherited Wall-1 propagation), unblocking D2.outer + D3.

## Blockers

None for this worker. Standing constraints unchanged: framework wall
#2436 gates the inherited sorries in per-(F, Q) indecomposability;
sub-A (#2907) still has-pr and must land before the outer wiring.
