## Accomplished

Session type: **feature** (issue #6169 — general-`L` Problem 3.8.4(ii), Noether-Deuring).

Found the issue's "Current state" materially stale (no `Problem3_8_4_General.lean`; part (i)
`iso_of_baseChange_iso` itself still `sorry`; the claimed-done finite case
`directSummand_of_baseChange_directSummand_finite` did **not** exist; iso-track descent still
`sorry` under #6158). Noted this on the issue, then built the genuinely-buildable pieces:

1. **Finite case (sorry-free).** `directSummand_of_baseChange_directSummand_finite` +
   `bcRestrictScalarsMap` in `Problem3_8_4_Finite.lean`, mirroring `iso_of_baseChange_iso_finite`
   via the power identification and `directSummand_pow_cancel`. Axiom-clean.
2. **Split-injection pushforward functoriality (sorry-free).** `pushHom`, `pushHom_tmul`,
   `pushHom_cancelBaseChange`, `pushHom_comp`, `pushHom_id`, and
   `nonempty_baseChange_directSummand` in `Problem3_8_4_Functoriality.lean`, mirroring
   `pushEquiv`/`nonempty_baseChange_iso`. Axiom-clean.
3. **Descent twin (sorried).** `exists_fg_subalgebra_baseChange_directSummand` in
   `Problem3_8_4_Descent.lean` — the split-injection twin of the (also-sorried, #6158) iso descent
   `exists_fg_subalgebra_baseChange_iso`. Issue explicitly permitted this to be sorried.
4. **General assembly (sorry-free reduction).** New `Problem3_8_4_General.lean` proves
   `directSummand_of_baseChange_directSummand` for an arbitrary field extension `L/K` by
   descent → Zariski specialization to a residue field (`finite_of_finite_type_of_isJacobsonRing`)
   → pushforward → finite case. Removed the superseded part-(ii) stub from `Problem3_8_4.lean` and
   registered the new file in `Chapter3.lean`.

## Current frontier

`Problem3_8_4_General.directSummand_of_baseChange_directSummand` compiles and reduces the general
theorem entirely to the sorried split-injection descent lemma. The instance-matching between
`Functoriality.bcMod` (over the residue field, from the pushforward) and `Problem3_8_4.bcMod`
(expected by the finite case) resolves by defeq (helped by the project's
`weak.backward.isDefEq.respectTransparency = false`).

## Overall project progress

Phase 3 (formalization) ongoing. Problem 3.8.4(ii) general case is now proved modulo one shared
descent lemma. Two supporting lemmas are axiom-clean (`propext, Classical.choice, Quot.sound`).

## Next step

Fill `exists_fg_subalgebra_baseChange_directSummand` (the split-injection descent). It is the twin
of the iso descent `exists_fg_subalgebra_baseChange_iso` (#6158) — the matrix-entry /
filtered-colimit argument documented in `Problem3_8_4_Descent.lean` applies verbatim, with the
splitting relation `p ∘ i = id` descending exactly as `e.symm ∘ e = id` does. Best filled
alongside / by refactoring shared machinery with #6158. Once done, both descent lemmas and both
general theorems (with part (i) #6058) become axiom-clean and Problem 3.8.4 can move to
`sorry_free` in `progress/items.json`.

## Blockers

None hard. The general part-(ii) theorem carries a transitive `sorry` via the descent lemma
(shared with #6158); this is the only remaining gap and is explicitly in scope for a follow-up.
