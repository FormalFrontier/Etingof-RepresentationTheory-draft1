## Accomplished

Repaired PR https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/7875
(`docs: verify #7873 is already fixed on main`) for the **third** time. It was
`CONFLICTING` again; both `build` checks were already `SUCCESS`, so the merge state was
the only defect. Merged `origin/main`, resolved two `.claude/` conflicts, verified, pushed.

No Lean changes: `git diff --cached origin/main -- '*.lean' 'lakefile*' lake-manifest.json
lean-toolchain` is empty, so the merged tree's Lean content is byte-identical to `main`.

### `main` is now carrying the duplication damage this PR was repaired for

The previous repair session (`69e5e7c2`,
`progress/2026-07-26T11-46-48Z_69e5e7c2-repair7875.md`) documented a degenerate-merge trap:
when the merge base lacks a section that *both* parents have, git treats it as two unrelated
additions and concatenates them, producing a duplicated block with **no conflict markers**.
That session avoided the trap on this branch.

It then happened to somebody else. PR
https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/7856 (`8621db24`)
merged to `main` with exactly that damage. On `origin/main` today:

```
git show origin/main:.claude/skills/agent-worker-flow/SKILL.md \
  | grep -c "Before that .git reset --hard"        # 2
git show origin/main:.claude/skills/agent-worker-flow/SKILL.md \
  | grep -c "If you branched off an unmerged PR"   # 2
```

Both blocks appear twice, verbatim, a few dozen lines apart. It is visible in #7856's own
diff: the `@@ -154,7 +169,67 @@` hunk *adds* "Before that `git reset --hard`…" and the
context lines immediately after it are that same paragraph, already present.

**Merging this PR removes both duplicates** — a side effect of taking the branch's side,
not extra scope.

### The conflict, and why the branch side wins on every hunk

`SKILL.md`: one marked hunk, 54 lines, empty on the branch side. Every one of main's four
sub-blocks already exists on this branch:

| main's sub-block | branch |
|---|---|
| "Before that `git reset --hard`…" | verbatim |
| "Judge that inspection in *both* directions…" | fuller — "four times… 169, 279, 320 and 364" vs main's "three times… 169, 279 and 320", plus the **"Keep that duplication"** note on why the check is copied into `.claude/commands/*.md` |
| "If the restored files include this skill…" | the **corrected** version — this PR's entire point is that the Skill tool caches and must not be used to reload; main still leads with "re-invoke the skill (not just `Read`)" and appends a caveat |
| "If you branched off an unmerged PR's head…" | verbatim |

So I took the empty side. Main's one genuinely new contribution from #7856, the
`coordination list-unclaimed` FIFO-cap block, merged *without* conflict and is kept.

`feature.md`: both sides independently added a "read `lean-formalization` before writing
Lean" paragraph. Kept main's (it names the `grep` and gives a worked example) and grafted
the branch's unique sentence — a `rw`/`simp` that misses a pattern the goal visibly contains
is usually a documented instance trap, not a bad proof.

### Losslessness check

For each of the seven `.claude/` files, diffed `origin/main`'s copy against the merged tree
and read every line present on `main` and absent here. All of them are the superseded
wordings above plus three already known from the previous repair (`summarize.md`'s bare
"Commit ONLY the progress document changes"; the Step 4 "foundation landed in #N" bullet
that says *skip* where the branch says *merge #N first if it is mergeable*; an older
"twice… 169 and 279 lines" count). `lean-formalization/SKILL.md` was not conflicted and the
merged copy is byte-identical to `main`, so #7856's and #7901's additions there survive.

Then checked no block appears twice and no conflict markers remain. `SKILL.md` is 643 lines
= the branch's 628 plus main's 15-line FIFO-cap block.

### This worktree arrived stale, again

`agent/e185a351` came up with `.claude/commands/feature.md` (-7) and
`.claude/skills/agent-worker-flow/SKILL.md` (-294) uncommitted, pure deletions, nothing
added — the failure mode the skill's Step 2 documents, now on its sixth recorded instance.
Backed up to `/tmp/e185a351-stale-backup/` and restored with `git checkout HEAD --` before
doing anything.

## Current frontier

PR #7875 is merged with `main`, verified, pushed, and `mark-pr-salvaged`. Auto-merge
(`--auto --squash`) was already enabled by the previous repair session.

## Overall project progress

Unchanged — no Lean content moved. Stage 3 formalization continues. Full-project `lake build`
after the merge: **exit 0, zero errors**, only pre-existing style-linter warnings.

## Next step

`coordination list-pr-repair` reported only #7875. The two PRs the previous two sessions
flagged as the natural next repair candidates are still worth checking:

- https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/7826
- https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/7856 (now merged
  as `8621db24`, and the source of the duplication described above)

## Blockers

None for this PR.

Worth a planner's attention, though: the `.claude/` corpus has now been damaged three
distinct ways in two days — truncated by stale worktrees (six times), reverted by
`git add -A` in a Lean PR (#7872, #7834), and silently duplicated by hunk-by-hunk
resolution of a degenerate merge (#7856). All three are invisible to review, because the
first two show up as a large diff on an unrelated PR and the third produces no conflict
markers at all. The previous session proposed a CI check rejecting net-negative `.claude/`
diffs on PRs whose stated scope is Lean work; the duplication case suggests a companion
check for repeated blocks (e.g. flag any `.claude/**.md` where a run of 5+ non-blank lines
occurs twice). Both would survive a truncated `SKILL.md` in a way that more prose inside
the truncated file cannot. Out of scope for a repair session.
