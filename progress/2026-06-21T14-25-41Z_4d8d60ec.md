## Accomplished

Closed `detInv_elim_of_polynomial` (issue #4695, `PolynomialRepEmbedding.lean`) —
the lone `sorry` blocking the polynomial-rep → `V^⊗n` embedding chain — by
assembling the kernel lemma (#4694). Two structural problems surfaced and were
fixed along the way:

1. **`KernelLemmaK.lean` did not compile** (it was absent from the Chapter5
   aggregator, so CI never built it; it rotted when `kernelLemmaK'` was corrected
   in #4847). Its descent applied the now-false per-generator "nonneg-weight
   vector = ⊥" step. Reformulated the descent to build the `GL_N`-stable image
   `W̄` of the whole span in the subquotient and apply `kernelLemmaK'_submodule`
   (the invariant-submodule form designed for exactly this), then read off that
   each generator maps to `0`. Commit 1.

2. **Import cycle** `DetLocalization → PolynomialRepEmbedding → … →
   DetLocalization` blocked `PolynomialRepEmbedding` from consuming the kernel
   lemma. Broke it by relocating `MvPolynomial.eq_of_eval_eq_on_gl` (the only
   thing `DetLocalization` needed from `PolynomialRepEmbedding`) to a new
   Mathlib-only file `EvalEqOnGL.lean`, consumed by both sides.

New file `DetInvElim.lean` contains the assembly:
- `exists_weight_eigenbasis` — weight eigenbasis from `h_span` (internal direct
  sum of weight spaces via `IsInternal.collectedBasis`);
- `eval_rTransAlgHom` / `evalGLAway_localRightRep` / `evalGLAway_smul` —
  right-translation intertwining for `evalGLAway`;
- `detInv_elim` — the matrix coefficients in the weight basis, as elements of
  `A[det⁻¹]`, are right-torus weight vectors (weight `wt c ∈ ℕ^N`) spanning a
  `GL_N`-stable subspace, so `kernelLemmaK_evalAtGL` lands each in `k[Xᵢⱼ]`.

`KernelLemmaK` and `DetInvElim` added to the Chapter5 aggregator (so CI now
compiles them — preventing the rot that hit `KernelLemmaK`).

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5` passes (8129 jobs, 0 errors).
`detInv_elim_of_polynomial` is `sorry`-free.

## Overall project progress

Stage 3 (formalization) ongoing. The polynomial-rep embedding chain
(`hpoly'_of_scalarGL_action` → `polynomialRep_homogeneous_hpoly'` →
`polynomial_homog_rep_equivariant_embedding`) is now `sorry`-free above the
former `detInv_elim_of_polynomial` hole. The remaining research-level residual
under this chain is `kernelLemmaK'` (the Cauchy-decomposition core, #4847,
`KernelLemmaKPrime.lean:185`), still a tracked `sorry`.

## Next step

The genuine remaining mathematical hole feeding `detInv_elim` is `kernelLemmaK'`
(#4847 / follow-up of #4826): for `r ≥ 1` no nonzero `GL_N`-invariant
nonneg-weight submodule of `(A/det) ⊗ χ⁻ʳ` exists (the Cauchy-decomposition
highest-weight argument). That is the next item to attack for a fully
`sorry`-free embedding chain.

## Blockers

None for this issue (delivered). `kernelLemmaK'` (#4847) remains the deep
sorry under the chain, tracked separately and permitted for this assembly.
