## Accomplished

One-shot feature session `d48a2f8a` (`/once 5248`): **completed all four §5.11 `S₃`
induced-representation decompositions, sorry-free**, in
`EtingofRepresentationTheory/Chapter5/Discussion5_11_examples.lean`. This closes #5248.

The four theorems (now proved, standard axioms only — `propext, Classical.choice, Quot.sound`,
no `sorryAx`):

- `indZ2_trivPlus_decomp  : Ind_{Z₂} ℂ₊ ≅ stdRep ⊞ trivRep`
- `indZ2_signMinus_decomp : Ind_{Z₂} ℂ₋ ≅ stdRep ⊞ signRep`
- `indZ3_trivPlus_decomp  : Ind_{Z₃} ℂ₊ ≅ trivRep ⊞ signRep`
- `indZ3_eps_decomp       : Ind_{Z₃} ℂ_ε ≅ stdRep`

Method (issue-faithful — uses Frobenius reciprocity `Etingof.Theorem5_10_1`, **not** the
still-`sorry` induced-character formula `Theorem5_9_1`). Reusable machinery added, all
sorry-free:

1. `finrank_hom_symm` — `dim Hom(V,W) = dim Hom(W,V)` in `FDRep ℂ G` (symmetric scalar product).
2. Catalogue characters/dimensions/non-isomorphism + `S3_simple_iso` (every simple `S₃`-rep ≅
   one of `trivRep`/`signRep`/`stdRep`, via the `1²+1²+2²=6` Artin–Wedderburn count from
   `exists_simples_sum_finrank_sq_eq_card`).
3. `frobenius_finrank` — the dimension-level Frobenius bridge
   `dim Hom_{S₃}(Ind_H ρ, S) = dim Hom_H(ρ, Res_H S)`, crossing `FDRep → Rep` via
   `FDRep.forget₂HomLinearEquiv`, applying `Rep.indResHomEquiv`, and returning. The induced
   object `FDRep.of (Definition5_8_1 H ρ)` is *definitionally* `Rep.ind H.subtype (Rep.of ρ)`
   after `forget₂` (all object identifications were `rfl`).
4. `sum_cyclic` (enumerate a finite cyclic group via `finEquivZPowers`), `ind_finrank_eq_scalar`
   (multiplicity = scalar product over `H`), and per-subgroup `indZ2_finrank_eq`/
   `indZ3_finrank_eq` (explicit 2-/3-term scalar products).
5. Cube-root arithmetic for `ℂ_ε`: `zeta3_primitive` (via `Complex.isPrimitiveRoot_exp`),
   `zeta3_sum` (`ζ²+ζ+1=0`), `zeta3_inv`/`zeta3_sq_inv`.

Each theorem: `iso_of_forall_finrank_hom_eq`, casing each simple `S` over the catalogue; LHS
multiplicity from the scalar product, RHS multiplicity from `FDRep.finrank_hom_simple_simple` +
`finrank_hom_biprod`.

## Verification

- `lake build EtingofRepresentationTheory.Chapter5.Discussion5_11_examples` succeeds (8585 jobs).
- `#print axioms` on all four theorems: standard axioms only, no `sorryAx`.
- Sorry count in the file: 4 → 0.

## Current frontier

§5.11 `S₃` induced-rep catalogue and all four decompositions are complete and sorry-free.

## Overall project progress

Etingof draft 1, Stage 3. `Discussion_5.11_examples` is genuinely sorry-free now (the
items.json status `sorry_free` is finally accurate for the proofs, not just statements).

## Next step

Pick the next unclaimed feature issue. The Frobenius dimension bridge (`frobenius_finrank`),
`finrank_hom_symm`, `sum_cyclic`, and the `S3_simple_iso` completeness pattern are reusable for
any other small-group induced-rep decomposition.

## Blockers

None.
