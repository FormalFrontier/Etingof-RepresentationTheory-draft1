## Accomplished

Session `068bbc48` (feature, claimed #2939) landed Phase 1 setup for
`non_adjacent_branches_leaf_case_per_kQ` in
`Chapter6/FieldGenericNonAdjacentBranches.lean`:

- Replaced the body's blanket `sorry` with the ~340-line mechanical
  port from the universal `leaf_case`
  (`InfiniteTypeConstructions.lean:9707-10120`), adapted to the
  per-(F, Q) signature:
  - Inlined `adj_comm`, `ne_of_adj`, `S₀`, `Sw` (universal proof has
    these in outer scope).
  - Used `h_v₀w_nonadj` directly as a hypothesis (universal proof
    extracted it from a negated existential `h_adj_exists`).
- All Phase 1 declarations now in scope:
  `chain` (Nodup, length ≥ 3, head v₀ / last w, edges),
  `side_arm` (v₀'s third neighbour ≠ leaf, ≠ chain[1]),
  `arm₁, arm₂` (w's non-chain neighbours, distinct),
  `leaf_only` (leaf has unique neighbour v₀),
  `leaf_ne_chain`, `side_ne_chain`, `arm₁_ne_chain`, `arm₂_ne_chain`
  (universal distinctness),
  `hleaf_ne_arm₁`, `hleaf_ne_arm₂`, `hside_ne_arm₁`, `hside_ne_arm₂`
  (cross-region distinctness — `hside` ones via the cycle-closing
  inline helper).
- Exposed `walk_to_nodup_path` and its dependency `exists_dup_indices`
  (previously `private` in `InfiniteTypeConstructions.lean`) so the
  per-(F, Q) file can call them.
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
  green; only the documented Phase 2 `sorry` warning remains.

Filed follow-up issue #2951 (Phase 2 case-split + embedder dispatch).
Left `Decomposed into #2951` breadcrumb on parent #2939.

## Current frontier

`non_adjacent_branches_leaf_case_per_kQ`
(`Chapter6/FieldGenericNonAdjacentBranches.lean:87`) body has Phase 1
done; Phase 2 (`sorry` near the end) is the only remaining gap.

## Overall project progress

Stage 4 (per-(F, Q) port chain through to the Chapter-6 main theorem).
The non-adjacent-branches leaf-case helper is the last forbidden-subgraph
helper needed by the outer assembly
`non_adjacent_branches_infinite_type_per_kQ`
(`FieldGenericAssembly.lean:74`). With Phase 1 in place, Phase 2 is a
tractable per-case dispatch problem — each case extracts one or two
additional vertices and calls one of the existing embedder helpers
(`embed_t125_in_tree_per_kQ`, `embed_etilde6_in_tree_per_kQ`,
`embed_etilde7_in_tree_per_kQ`).

## Next step

Claim #2951 and fill Phase 2. The four primary cases (T(1,2,5) at v₀,
T(1,2,5) at w, Ẽ₇ at v₀, Ẽ₆ at w) plus edge cases (D̃₅ for chain
length = 3) are each ~80-150 lines of vertex extraction + edge fact
threading + embedder call. Phase 1's declarations provide all the
shared infrastructure.

## Blockers

None. #2951 has Phase 1 in scope; Phase 2 work is bounded and well-
patterned.
