## Accomplished
Proved the four "free generator" vanishing theorems of Problem 8.2.7 (issue #6124),
sorry-free:
- `Problem_8_2_7_i_tor_free_vanish`, `Problem_8_2_7_ii_tor_free_vanish` — higher `Tor`
  vanishes on the projective right regular module, via
  `Functor.isZero_leftDerived_obj_projective_succ`.
- `Problem_8_2_7_i_ext_free_vanish`, `Problem_8_2_7_ii_ext_free_vanish` — higher `Ext`
  out of a projective is `Subsingleton`, via `Abelian.Ext.subsingleton_of_projective`.

Added a small helper: `opRegularEquiv A : A ≃ₗ[Aᵐᵒᵖ] Aᵐᵒᵖ` (right regular module `A` is
free rank one over `Aᵐᵒᵖ`) and a `private instance opRegularProjective` giving
`Module.Projective Aᵐᵒᵖ A`, which supplies the `Projective (ModuleCat.of Aᵐᵒᵖ A)`
instance the Tor lemma needs. `Ext` side uses the standard `Module.Projective A A`.

`#print axioms` for all four shows only `[propext, Classical.choice, Quot.sound]`.

## Current frontier
`Problem8_2_7.lean` builds cleanly; 12 sorries remain — the cyclic-pair / gcd theorems
and the higher-`Tor`/`Ext` cyclic vanishing theorems (out of scope for #6124).

## Overall project progress
Stage 3 formalization ongoing. Chapter 8 Problem 8.2.7: 4/16 theorems now sorry-free.
`items.json` status for `Chapter8/Problem8.2.7` left at `statement_formalized` (per issue,
since 12 sorries remain and the tracker holds one status per item).

## Next step
The cyclic-pair theorems (`*_tor_zero/one`, `*_ext_zero/one`, `gcd`) and cyclic higher
vanishing require the length-1 free resolution of `ℤ/a` (resp. `k[x]/(f)`) and Problem
8.2.6(i) for degree 0 — a separate, higher-risk work item.

## Blockers
None.
