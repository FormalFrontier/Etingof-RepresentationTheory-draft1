## Accomplished

Feature session on #4541 (D̃₆ `d6tildeRep_kQ_leaf_equalities` non-canonical
branches). On investigation the plan's premise was **partially stale**: it
framed the work as a port of the d5tilde precedent, but
`d5tildeRep_kQ_leaf_equalities` is itself only **2 of 32 branches** proven
(all-canonical + one combo-D), carrying the same five coarse sorries
d6tilde had. The genuine blocker for the remaining branches is shared
**N-invariance infrastructure** that exists nowhere in the codebase.

Landed the tractable subset (parity with the d5tilde precedent):

* **Four reversed-edge projection siblings** ported to D̃₆:
  `d6tilde_core_F_proj1/2` (v=2, leaves 0/1) and
  `d6tilde_core4_F_proj1/2` (v=4, leaves 5/6). Purely algebraic
  (`starFirst_F`/`starSecond_F` left-inverse identities), vertex-relabel
  ports of `d5tilde_core{_,3_}F_proj1/2`.
* **combo D branch** of `d6tildeRep_kQ_leaf_equalities` (`0→2, 1→2, 2→3`
  canonical, both v=4 leaf edges reversed `4→5, 4→6`): proven via the new
  proj siblings threaded through the chain-collapse equality
  (`W⟨3⟩ = W⟨4⟩`). The `e54`-reversed coarse sorry was refined into
  combo C′ (mixed, still sorry) + combo D (proven).

Build of `FieldGenericD6Tilde` succeeds; all new code is `sorry`-free.

## Current frontier

`d6tildeRep_kQ_leaf_equalities` now has five coarse sorries, all genuinely
blocked:
* combo C (`e54` canon, `e64` rev) and combo C′ (`e54` rev, `e64` canon):
  **mixed-direction v=4**, need N-invariance.
* `e23` reversed (γ⁻¹), `e12` reversed, `e02` reversed: outer branches that
  each nest the mixed v=4 sub-tree.

## Overall project progress

Stage 3 (formalization) ongoing. D̃-family orientation-generic
indecomposability frontier: D̃₅/D̃₆ both at "all-canonical + combo D"
parity; D̃₇ sub-A in progress (#4531), its leaf_equalities not started.
The mixed-direction case is a **shared cross-family blocker** (d5 #2850 /
#2839 sub-B, d6 #4551, d7 #4533) gated on a missing N-invariance lemma.

## Next step

Created **#4551** (sub-B′) for the blocked remainder: build the
field-generic N-invariance infrastructure (factor it out for d5/d6/d7
reuse), then close combo C/C′ and the three outer reversed branches.
A partial PR on #4541 lands the combo-D + proj-sibling subset.

## Blockers

The mixed-direction v=4 branches require N-invariance infrastructure that
does not exist anywhere in the codebase — a genuine math-infrastructure
gap, not a porting gap. Tracked by #4551; shared with d5/d7.
