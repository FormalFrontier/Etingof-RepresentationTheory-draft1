## Accomplished

Formalized the **root-of-unity content of Step 3** of Remark 5.2.8 (issue #5654),
`sorry`-free, in `EtingofRepresentationTheory/Chapter5/Remark5_2_8.lean`:

- `character_eq_sum_rootsOfUnity` — for a finite-dimensional complex representation
  `V` of a finite group `G`, every character value `χ_V(g)` is a sum of `N`-th roots
  of unity, `N = |G|`. Concretely: the multiset of eigenvalues of `ρ(g)` (roots of
  its characteristic polynomial) consists of `N`-th roots of unity and sums to
  `χ_V(g)`. Each eigenvalue `μ` satisfies `μ^{orderOf g} = 1`, hence `μ^N = 1` since
  `orderOf g ∣ N` (Lagrange); the trace is the sum of charpoly roots over `ℂ`.

This is the concrete meaning of "`χ_V(g) ∈ ℚ(ζ_N)`" underlying Step 3, and places
`β = ∏_{g≠1} |χ_V(g)|²` in the ring generated by the `N`-th roots of unity. It
reuses the eigenvalue techniques already proven in `Theorem5_4_4.lean` but is kept
self-contained in the Remark file (no forward file dependency). Updated the file
docstring and `progress/items.json` note.

`lake build EtingofRepresentationTheory.Chapter5.Remark5_2_8` clean, no warnings, no
`sorry`/`native_decide`.

## Current frontier

Remark 5.2.8 now has, all sorry-free: Steps 1, 2, 5 + contradiction, the
algebraic-integer half of `β`, and the root-of-unity content of Step 3. The only
remaining piece is **Step 4**: that `β` is fixed by the Galois automorphism
`ζ ↦ ζʲ` of `ℚ(ζ_N)` (via `σ_j(χ_V(g)) = χ_V(gʲ)`), forcing `β ∈ ℚ`.

## Overall project progress

Phase 3 formalization ongoing. Remark 5.2.8 (#5654) is `proof_partial`, blocked on
its foundation sub-issue #5772 for the final Galois step.

## Next step

Foundation sub-issue #5772: construct `ℚ(ζ_N)` (or `CyclotomicField N ℚ`) with its
`Gal ≅ (ZMod N)ˣ` action, prove the representation-theoretic identity
`σ_j(χ_V(g)) = χ_V(gʲ)` (eigenvalues of `ρ(gʲ) = ρ(g)ʲ` are the `j`-th powers of
those of `ρ(g)`, and `σ_j` acts on each root-of-unity eigenvalue by `λ ↦ λʲ`), then
combine with `prod_ne_one_pow` and `IsGalois.mem_bot_iff_fixed` to conclude
`β ∈ ℚ`, finally feeding `not_isIntegral_rat_mem_Ioo` once `0 < β < 1`.

## Blockers

Step 4 / full closure of #5654 is blocked on foundation sub-issue #5772 (cyclotomic
field + Galois action). This is a multi-hour standalone foundation, correctly
decomposed out per the issue directive.
