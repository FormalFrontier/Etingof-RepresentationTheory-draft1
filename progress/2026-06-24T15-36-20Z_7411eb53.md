## Accomplished

Closed issue #5145 (minimal basic algebra, discussion after Definition 9.7.1). Extended
`EtingofRepresentationTheory/Chapter9/Discussion_after_Definition9_7_1.lean` (building on the
`dim B_𝐧 = ∑ c_{ij} n_i n_j` formula from #5144) with both deliverables:

**Deliverable 1 — dimension minimized at 𝐧 = (1,…,1).**
- `cartan_term_le` / `cartan_term_lt`: a single Cartan term `c·n_i·n_j` is `≥ c` when
  `n_i,n_j ≥ 1`, and a diagonal term `c·n·n` is strictly `> c` when `c ≥ 1`, `n ≥ 2`.
- `sum_cartan_le_finrank_cartanAlgebra`: lower bound `∑ c_{ij} ≤ dim B_𝐧` for all `n_i ≥ 1`.
- `finrank_cartanAlgebra_one`: the minimal member has dimension exactly `∑ c_{ij}`.
- `finrank_cartanAlgebra_eq_sum_cartan_iff`: `dim B_𝐧 = ∑ c_{ij}` **iff** every `n_i = 1`
  (uses `Finset.sum_lt_sum` twice — any `n_i ≥ 2` strictly enlarges the diagonal term).
- `one_le_cartanEntry_self`: convenience deriving the `c_{ii} ≥ 1` hypothesis from
  `Nontrivial (End P_i)` via `Module.finrank_pos`.

**Deliverable 2 — semisimple quotient commutative iff 𝐧 = (1,…,1).**
- `exists_matrix_not_comm_of_ne`: `Mat_m(k)` is noncommutative once `m` has two distinct
  indices (`E_{ab}E_{ba}=E_{aa}≠E_{bb}=E_{ba}E_{ab}`).
- `matrix_mul_comm_of_subsingleton`: `Mat_m(k)` commutes when `m` is a subsingleton.
- `semisimpleQuotient_comm_iff`: the Wedderburn quotient `B_𝐧/Rad ≅ ⊕_i Mat_{n_i}(k)`,
  modelled by `∀ i, Matrix (Fin (n i)) (Fin (n i)) k`, is commutative **iff** every `n_i = 1`.

## Current frontier

Issue #5145 complete. Both prose claims from the discussion after Definition 9.7.1 are now
formalized. `lake build` of the file succeeds with no sorries and no warnings.

## Overall project progress

Stage 3 (formalization) ongoing. Chapter 9 discussion after Definition 9.7.1 fully formalized:
Cartan matrix + dimension formula (#5144) and now the minimal-algebra characterization (#5145).

## Next step

Continue with sibling issues under parent #5139 (Morita class structure) or other open Ch9 work.

## Blockers

None.
