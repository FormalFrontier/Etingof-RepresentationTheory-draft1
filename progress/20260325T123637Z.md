## Accomplished

Built the full proof structure for `equivAt_eq_source` (ker(sinkMap) ≅ V_i) and `Proposition6_6_6_source`:

1. **sinkMap_lof helper** (proved): `sinkMap(lof b w) = mapLinear(b.snd)(w)` via `delta sinkMap; erw [DirectSum.toModule_lof]`

2. **Kernel proof h_ker** (mostly proved): Reduced `sinkMap(f_ds(v)) = 0` to individual summands via `simp_rw [comp_apply, sinkMap_lof]`, then applied `reflFunctorMinus_mapLinear_ne_eq` using a local wrapper `h_mapL` to handle the DecidableEq instance mismatch after `classical`. Final step (`mkQ(sum) = 0`) is sorry'd — blocked by `Decidable.casesOn` in `reflFunctorMinus_mkQ`.

3. **FiniteDimensional instances** (proved): All module instances for `linearEquivOfInjective` compile.

4. **Proposition6_6_6_source structure** (compiles with sorrys): Uses `crossIsoToIso` with `equivAt_eq_source` at vertex i and `reflFunctorPlus_equivAt_ne.trans reflFunctorMinus_equivAt_ne` at non-source vertices.

### Technical finding: DecidableEq instance mismatch under `classical`

After `classical`, Lean resolves DecidableEq to `Classical.dec` instead of the original `inst_dec`. This means `simp_rw` with lemmas like `reflFunctorMinus_mapLinear_ne_eq` fails because the lemma's LHS uses `inst_dec` while the goal uses `Classical.dec`. **Fix**: Define a local wrapper via `have h_mapL := fun x w => @lemma ... inst_dec ...` and use `simp_rw [h_mapL]`.

## Current frontier

`Proposition6_6_6.lean`: 4 sorrys remain (was 2 originally, but now more localized):

| Sorry | Location | Blocker |
|-------|----------|---------|
| h_ker final step | line ~1102 | `Decidable.casesOn` in `reflFunctorMinus_mkQ` blocks reducing `mkQ(sum) = 0` |
| f_inj | line ~1109 | DirectSum component extraction (`DFinsupp.finset_sum_apply` rewrite fails) |
| hdim | line ~1115 | Rank-nullity argument (not yet attempted) |
| source naturality | line ~1253 | Requires unfolding `equivAt_eq_source` through `linearEquivOfInjective` |

## Overall project progress

- Proposition 6.6.6 sink case: **COMPLETE** (0 sorrys)
- Proposition 6.6.6 source case: structure complete, 4 localized sorrys remain
- All sorrys are technical (DirectSum manipulation, DecidableEq instance management, quotient reduction) — the mathematical arguments are clear and documented
- The `Decidable.casesOn` issue from #1716 remains the primary blocker

## Next step

1. **API lemma approach**: Add `reflFunctorMinus_mkQ_comp_sourceMap_eq_zero` to Definition6_6_4.lean, proving `mkQ ∘ sourceMap = 0` inside the definition where `Decidable` is already eliminated. This would unblock h_ker.
2. **DirectSum component lemma**: Add a helper `directSum_sum_lof_component` proving `(∑ b, lof(b)(f(b))) c = f(c)` for DirectSum. This would unblock f_inj.
3. Once h_ker and f_inj are proved, hdim follows from rank-nullity, and source naturality can be attempted.

## Blockers

- **`Decidable.casesOn` wrapper** (#1716): The core blocker remains. All 4 sorrys ultimately stem from `reflFunctorMinus_mkQ` and `equivAt_eq_source` being defined via `match inst_dec i i`, creating opaque `Decidable.rec` wrappers that prevent external rewriting.
- **Recommended fix**: Add API lemmas to Definition6_6_4.lean that prove key properties INSIDE the match block where `Decidable` is eliminated, then export them for use in Proposition6_6_6.lean.
