## Accomplished

Work session `431ae372` (`/work` → `/feature` on **#4613**, T(1,2,5) = Ẽ₈
`t125Rep_kQ_isIndecomposable` assembly).

Landed `t125_center_decomp_F` (`Chapter6/FieldGenericT125.lean`, sorry-free):
the center spanning decomposition for the 6-block center `F^{6(m+1)}` — every
center vector is `prefixBlockEmbed_F 4 6` (arm-2, blocks 0-3) plus
`suffixBlockEmbed_F 5 6` (arm-3, suffix summand chosen supported on blocks 4-5,
no overlap). This is the T(1,2,5) analogue of `center_decomp_F`
(`FieldGenericStar.lean`) and is the spanning fact the canonical-orientation
indecomposability propagation needs to collapse the center vertex. `lake build
FieldGenericT125` passes (8045 jobs); the only remaining `sorry` in the file is
`t125Rep_kQ_isIndecomposable` itself.

Decomposed #4613 (Step 4b) into two sub-issues:
- **#4787** (sub-A): canonical-orientation branch + the **backward
  leaf-collapse foundation** — the genuine open content.
- **#4789** (sub-B): non-canonical orientation branches (reversed-arm foundation
  or refutation); depends on #4787.

## Key findings

- The forward-only helpers (`t125_canonical_collapse`, `t125_prefix_sub`,
  `t125_suffix_sub`, `t125_arm1_sub`) push leaf→center but provide **no backward
  extraction**. The indecomposability argument needs the backward direction to
  pin all arm leaves to a common `F^{m+1}` eigenvalue site.
- The star/D̃ `core` + `compl_eq_of_le` leaf-equality mechanism does **not**
  transfer: T(1,2,5)'s arm leaves have **different dims** (v1 `3(m+1)`, v3
  `2(m+1)`, v8 `m+1`) and the three arm images **overlap** at the center (arm-2
  blocks 0-3, arm-3 blocks 1-5, arm-1 the moment-curve tube across all 6 blocks).
  No clean direct sum ⟹ the per-edge `rcases`/`compl_eq_of_le` pattern fails.
  This is the cross-arm disentangling at the center; likely needs the
  eigenvalue-site/brick argument (cf. ~350-line ℂ-source
  `d5tildeRep_isIndecomposable`).
- Once the foundation (common-site collapse with the per-vertex `U = ⊥ → W v = ⊥`
  propagation) exists, the assembly is thin: `t125_arm1_sub` →
  `U = ⊥ ∨ U' = ⊥` → `propagate` (using `t125_center_decomp_F` for the center
  vertex), mirroring `d5tildeRep_kQ_isIndecomposable`'s all-canonical branch.
- Lean detail: `congrArg w (Fin.ext h)` with `h` precomputed (not `by omega`
  inline) avoids the under-determined-metavar omega failure when finishing
  `w j = w ⟨idx, _⟩` index-equality goals.

## Current frontier

#4613 partial PR up (`t125_center_decomp_F` landed). The monolithic
indecomposability `sorry` is now backed by the spanning lemma and a precise
two-issue decomposition. Genuine open content lives in #4787 (canonical backward
foundation) and #4789 (reversed orientations).

## Overall project progress

Etingof draft 1, Stage 3. Ch6 sporadic/D̃-family tube redesign (#4548/#4597)
active. D̃₅ canonical indecomposability landed (first of the family); T(1,2,5)
foundation work now scoped with the center spanning lemma in hand.

## Next step

A worker claims **#4787** (canonical branch + backward foundation). Before
investing in #4789, resolve the reversed-orientation truth question (cf. the
star `starProj4` refutation #4728) — a reversed T(1,2,5) orientation may be
genuinely decomposable, requiring construction correction rather than proof.

## Blockers

#4789 is blocked on #4787. The backward leaf-collapse foundation (#4787) is
genuine research-level content (cross-arm disentangling at the overlapping
center) and may itself warrant a design pass.
