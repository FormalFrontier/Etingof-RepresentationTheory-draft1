## Accomplished
Proved `exists_octahedral_faithful_hom` (issue #6972) **sorry-free** in
`EtingofRepresentationTheory/Chapter4/Problem4_12_8.lean` — the four-body-diagonal faithful action
that is the crux of the octahedral disjunct of the SO(3) finite-subgroup classification. With it,
`so3_octahedral_of_poleData` is now also sorry-free (`#print axioms` on both reports only
`[propext, Classical.choice, Quot.sound]`, no `sorryAx`).

The assembly (parent #6972 was the "residual assembly" after worker-decomposition into helpers
#6987 and #6988):
- **Step 1–2.** Order-`3` pole `b`, its `G`-orbit `O` (`|O| = 8`), and antipode-closure of `O`
  built from `octahedral_order3_pole_neg_mem_orbit` (#6987, on `main`).
- **Step 3.** Antipodal setoid `S` on `↥O` (`x ≈ y ↔ x.1.1 = ±y.1.1`), the quotient `D`, and a
  hand-built `MulAction ↥G D` via `Quotient.map'` (`G`-invariance of `S`).
- **Step 4.** `|D| = 4` by fibre counting: `Nat.card ↥O = ∑_d Nat.card (fibre d)`
  (`Equiv.sigmaFiberEquiv` + `Nat.card_sigma`), each fibre `= 2` (`Nat.card_eq_two_iff'`, the two
  elements `w`, its antipode).
- **Step 5.** Faithfulness: a kernel element sends every `w` to `±w`, discharged by
  `octahedral_kernel_negates_or_fixes_trivial`.
- **Step 6.** Transport to `Equiv.Perm (Fin 4)` (`Fintype.equivFinOfCardEq`, `permCongrHom`,
  `MulAction.toPermHom`), mirroring `so3_tetrahedral_of_poleData` Step 4.

Because #6988's PR #6995 (the faithfulness helper) was `CONFLICTING`/stalled and not on `main`, I
brought its verified `octahedral_kernel_negates_or_fixes_trivial` block onto this branch (rebased
over #6987) so the assembly is self-contained and green. This PR therefore also lands #6988's
deliverable.

Needed one `set_option maxHeartbeats 800000 in` on `exists_octahedral_faithful_hom` (custom
quotient action + fibre-cardinality defeq).

## Current frontier
`Problem4_12_8.lean` builds clean. The only residual `sorry` in the file is
`so3_icosahedral_exists_faithful_perm5` (the geometric `5`-point icosahedral action, a different
issue). The tetrahedral and octahedral disjuncts are now fully sorry-free.

## Overall project progress
Stage 3 formalization. The SO(3) classification's octahedral disjunct (`G ≃* S₄`) is complete and
sorry-free. Remaining for the full classification: the icosahedral `5`-point action.

## Next step
Coordination: PR #6995 (#6988) is now redundant — its helper is included here. If this PR lands
first, #6995 should be closed/abandoned and #6988 closed. Then finish the icosahedral disjunct
(`so3_icosahedral_exists_faithful_perm5`).

## Blockers
None. (Potential duplicate-definition conflict with the stalled PR #6995 if it is repaired and
merged before this one; whichever merges second will need a trivial dedup.)
