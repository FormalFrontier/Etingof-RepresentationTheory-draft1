## Accomplished

Refreshed the file-level docstring of
`EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean` to match
what's currently on `main` (issue #2986). Three edits, all inside the
`/-! … -/` block at lines 11-48:

* Rewrote the leaf-arm dispatch bullet (was "delegates to
  `single_branch_leaf_case_per_kQ`, which internally dispatches to
  Ẽ₇ / T(1, 2, 5) depending on the T(1, q, r) shape"). It now describes
  the actual case-split implemented in the body: case-split on whether
  `a₂` / `a₃` have degree 2; if both extend, dispatch to
  `single_branch_leaf_case_both_extend_per_kQ` (still an API stub,
  tracked by #2905 sub-chain #2907 / #2908 / #2909 / #2910); if either
  is a leaf, the graph is a D-type tree and `tree_two_leaf_posdef`
  contradicts `h_not_posdef`.
* Replaced the "introduced here as an API stub" sentence (which
  referred to `single_branch_leaf_case_per_kQ`, whose body has since
  landed) with a single sentence pointing at the current API stub —
  `single_branch_leaf_case_both_extend_per_kQ` at line 1233, tracked by
  the #2905 sub-chain.
* Added `non_adjacent_branches_infinite_type_per_kQ`
  (`FieldGenericAssembly.lean:75`, PR #2943) to the sibling-wrapper
  enumeration. The other three siblings were already accurate.

The per-theorem docstring of `single_branch_leaf_case_per_kQ` at lines
1300-1305 already accurately describes the case-split (does not call
itself an API stub); no change needed there.

## Current frontier

Docstring refresh chore landed; build verified for
`EtingofRepresentationTheory.Chapter6.FieldGenericTpqr` (only warning
is the pre-existing `sorry` at line 1240 — known, tracked by the #2905
sub-chain). Mirrors the surgical scope of PR #2983 (issue #2982) for
`FieldGenericNonAdjacentBranches.lean`.

## Overall project progress

Stage 3 ongoing. Wave 63 of the non-adjacent-branches leaf-case
cascade landed. No new sorries introduced; the existing
`single_branch_leaf_case_both_extend_per_kQ` stub remains tracked.

## Next step

Open PRs to land; the wave-63 summarise issue (#2985) is already
claimed by another session. Planner should continue triaging the
remaining open `replan` issues per the wave-63 summary file once it
lands.

## Blockers

None.
