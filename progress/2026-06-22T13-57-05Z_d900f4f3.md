## Accomplished

Worked issue #4946 (general-`k` `schurModule_isSimple_general` assembly). Landed the
**centralizer-level general-`k` Schur-module simplicity core**, sorry-free, in
`EtingofRepresentationTheory/Chapter5/SchurModuleSimple.lean`:

- `schurBlock_imageSubmoduleB_isSimple_general` — general-`k` analogue of the ℂ
  `schurBlock_imageSubmoduleB_isSimple`: the image of the Young symmetrizer is simple over
  `centralizer(symGroupImage)`. Mirrors the ℂ proof, driven by the now-landed general-`k`
  special-block lemmas (`exists_unique_special_block_general`,
  `youngSym_action_vanishes_off_block_general`,
  `youngSym_action_on_special_block_rank_one_scaled_proj_general`).
- `schurModuleSubmodule_isSimple_centralizer_general` — general-`k` analogue of
  `schurModuleSubmodule_isSimple_centralizer`: `SchurModuleSubmodule k N λ` is simple as a
  `diagonalActionImage`-module.

Both `#print axioms`-clean (`propext, Classical.choice, Quot.sound`; no `sorryAx`).

**Resolved the `hN` question** (route doc's open item): `hN : (∑ i, lam i) ≤ N` is **not**
needed. `Theorem5_18_4_centralizers` requires only `[CharZero k]`, and
`exists_unique_special_block_general` has no degree guard, so the general-`k` simplicity
argument goes through unconditionally. (The ℂ `schurBlock_imageSubmoduleB_isSimple` already
carried `_hN` unused.)

## Current frontier

`schurModule_isSimple_general` (`SchurWeylFormalCharacterIso.lean`) is still `sorry`, now
with a detailed docstring pointing at the proven core and the universe blocker below. Its
proof is otherwise a one-line mirror of the ℂ final assembly
(`schurModuleSubmodule_isSimple_centralizer_general` + the generic GL transfer
`isSimpleModule_monoidAlgebra_GL_of_centralizer_simple`).

## Overall project progress

Stage 3 (formalization) ongoing. This turn completed the hard mathematical core of #4946
(general-`k` centralizer simplicity) but uncovered a genuine **universe obstruction** that
blocks wiring the final theorem in place.

## Blocker — project-wide `Type u → Type 0` universe cascade

The general-`k` simplicity core is intrinsically `Type 0`: it factors through the general-`k`
Specht classification `Theorem5_12_2_classification_general` → `IrrepDecomp k (Equiv.Perm
(Fin n))`, and Mathlib's `Rep`/`FDRep` pin the field and group to one common universe. Since
`S_n = Equiv.Perm (Fin n)` is `Type 0`, the field is forced to `Type 0` throughout the
Schur-Weyl/Specht core (verified empirically: lifting `Theorem5_12_2_ClassificationGeneral`,
`SpechtBridgeGeneral`, `SchurModuleSpecialBlock` to `Type*` fails at `IrrepDecomp (k G :
Type u)`'s same-universe constraint).

`SchurWeylFormalCharacterIso.lean` uses section variable `k : Type u`. Before this work the
whole chain was `Type u` **relying on the Type-u `sorry`**. Discharging the `sorry` with the
real (`Type 0`) proof forces the entire simplicity-dependent chain down to `Type 0`:

- in-file: `schurModule_isSimple_general`, `schurWeyl_simples_isotypic_matching_general`,
  `schurWeyl_simples_formalCharacter_classification_core_general`,
  `simpleRep_iso_schurModule_of_formalCharacter_eq`, `iso_of_formalCharacter_eq_schurPoly`;
- external consumers of those (≈10 files across Ch 5/6/9: `SchurWeylSimplesClassification`,
  `ConstituentCharacterExtraction`, `PolynomialGLDecomposition`, `Proposition5_22_2`,
  `CauchyCharacterRight`, `GLRepAlgebraic`, `FormalCharacterIso`, `CauchyDetQuotient`, …).

Lowering the whole `SchurWeylFormalCharacterIso` file to `Type 0` builds locally but breaks
those external consumers (confirmed: `ConstituentCharacterExtraction`, `Proposition5_22_2`).
The mechanical `Type u → Type` change is tractable but spans many capstone files and
permanently restricts these results to `Type 0`, so it merits a planner-scoped, coordinated
sweep rather than a single feature session. A follow-up issue captures this.

## Next step

A planner should create a dedicated issue to lower the Schur-Weyl character-classification
chain (the files listed above) from `Type u` to `Type 0`, then wire
`schurModule_isSimple_general` via `schurModuleSubmodule_isSimple_centralizer_general` (a
one-liner). The mathematical content is done; only the universe sweep remains.
