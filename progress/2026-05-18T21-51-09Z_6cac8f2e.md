## Accomplished

Claimed #2877 (the per-(F, Q) outer assembly + Theorem 2.1.2 bridge).
Issue body claimed ~1600 lines of mirror work remained — but a survey of
`Chapter6/FieldGeneric*.lean` showed that 12+ per-(F, Q) leaf helpers
landed since the issue was authored, including the largest ones
(`single_branch_not_posdef_infinite_type_per_kQ`,
`adjacent_branches_infinite_type_per_kQ`,
`graph_with_list_cycle_infinite_type_per_kQ`,
`degree_ge_4_infinite_type_per_kQ`). Only one leaf — the ~900-line
`non_adjacent_branches_infinite_type` mirror — is missing.

Landed (partial PR closing #2877):

- New file `Chapter6/FieldGenericAssembly.lean` with three theorems:
  * `non_adjacent_branches_infinite_type_per_kQ` — statement + sorry'd
    body (tracked by new sub-issue #2919).
  * `acyclic_branch_not_posdef_infinite_type_per_kQ` — real body,
    dispatching to `adjacent_branches_infinite_type_per_kQ`,
    `single_branch_not_posdef_infinite_type_per_kQ`, and the sorry'd
    non-adjacent helper.
  * `not_posdef_infinite_type_per_kQ` — real body mirroring
    `Chapter6/InfiniteTypeConstructions.lean:10661-10714` with each
    leaf dispatch swapped for its per-(F, Q) variant. The no-branch
    contradiction uses the universal `acyclic_deg_le_2_posdef`
    (positive-definiteness of path graphs is field-independent).
- Closed `Chapter2/Theorem2_1_2.lean:173` —
  `not_posdef_not_HasFiniteRepresentationType` is no longer `sorry`.
  Proof composes `not_posdef_infinite_type_per_kQ` with
  `HasFiniteRepresentationType.finite_dimVectors` via `Set.Finite.subset`,
  using `Module.Finite.equiv` to discharge the Module.Finite witness
  required by the Chapter 2 set predicate.
- Renamed `_hconn` to `hconn` in the lemma signature now that the
  connectivity hypothesis is used.

Decomposition (sub-issues created):

- #2919: `non_adjacent_branches_infinite_type_per_kQ` body (~900
  lines, further-decomposable by branch geometry).
- #2920: was opened for `acyclic_branch_not_posdef_infinite_type_per_kQ`
  body, but that body landed in this PR (real, not a stub). Will close
  with a forward-link comment.

## Current frontier

`Chapter6/FieldGenericAssembly.lean` and
`Chapter2/Theorem2_1_2.lean` both compile (build at 8069 jobs;
`Theorem2_1_2.lean` itself in 11s). Sorry budget unchanged for the
project — the parent-issue work transferred one `sorry` from
`Theorem2_1_2.lean:173` to `FieldGenericAssembly.lean:64` (the
non-adjacent stub), and added zero new sorry chains.

## Overall project progress

Stage 3 (Lean formalization). Theorem 2.1.2 forward direction is now
closed up to the per-(F, Q) leaf sorry chain:

* The forward direction `HasFiniteRepresentationType → IsDynkinDiagram`
  flows: not-Dynkin → not-positive-definite → infinite type per (F, Q)
  → contradiction with `finite_dimVectors`.
* The only remaining sorry on this chain is
  `non_adjacent_branches_infinite_type_per_kQ`'s body (#2919) — every
  other per-(F, Q) leaf used by the assembly is either fully proved or
  has its own tracked sub-issue (#2789/#2801 for K_{1,4}; #2853/#2851
  for D̃₅; #2793 for T(1, 2, 5); #2436 for Wall 1 in Ẽ₆/Ẽ₇; #2905 for
  T(p, q, r) single-branch leaf cases).

Backward direction is unchanged (still uses the existing
`isDynkinDiagram_HasFiniteRepresentationType` infrastructure).

## Next step

A worker should claim #2919 (non-adjacent branches helper). The
issue body recommends further-decomposing the proof — the universal
version's internal `leaf_case` helper is invoked three times (once per
choice of which neighbour of v₀ is a leaf), so a single `leaf_case_per_kQ`
helper handles the bulk of the body; the rest is an adjacent-pair
fallback. Splitting into ~3 sub-issues by branch geometry is the
recommended path.

## Blockers

None. #2920 will be closed by the planner / the next worker as
superseded; #2919 is unblocked and ready to claim.
