## Accomplished

Proved `indZ2_sign` in `EtingofRepresentationTheory/Chapter5/Problem5_11_1.lean` (issue #6291):
`Ind_{ℤ₂}^{A₅} 1₋ ≅ 3² ⊕ 3'² ⊕ 4² ⊕ 5²`. The file now has parts (a) fully proved
(`indZ2_triv` was merged via #6292; `indZ2_sign` added here). Build succeeds; `#print axioms`
shows only `propext, Classical.choice, Quot.sound` (no `sorryAx`, no `native_decide`).

Added helper lemmas:
- `sign_char` — the crux. Pins the sign character of a simple `σ : FDRep ℂ ↥H` (`|H|=2`) from
  `Simple σ` alone: `σ.character x = if x = 1 then 1 else -1`. Route:
  - `↥H = {1, t'}` with `t'² = 1` (from `Nat.card_eq_two_iff`); every element self-inverse.
  - `FDRep.simple_iff_char_is_norm_one` gives `χ(1)² + χ(t')² = 2` (both elements self-inverse).
  - The involution `σ.ρ t'` yields an idempotent `p = (1 + σ.ρ t')/2`; `IsProj.trace`
    (`LinearMap.IsIdempotentElem.isProj_range`) gives `χ(t') = 2K − d` with `K,d ∈ ℕ`.
  - The integer Diophantine `d² + (2K−d)² = 2` forces `d = 1` (`interval_cases` + `omega` parity),
    so `χ(t')² = 1`, i.e. `χ(t') = ±1`; `hntriv` rules out `+1`.
- `oneCount` — `#{x : x·gⱼ·x⁻¹ = 1} = ![60,0,0,0,0]` by `decide` (mirrors `twisted_p2'`).
- `indZ2_sign_value` / `_char_all` — the induced character equals `(30,0,-2,0,0)` on class reps.
  The Frobenius sum splits as `2·A(g) − countH(g)` where `A(g) = #{x : x·g·x⁻¹ = 1}`, reusing the
  merged `ind_character_eq`, `countH_eq`, `twisted_p2'`.
- `indZ2_sign_target_value` / `_char_all` — target biproduct character also `(30,0,-2,0,0)`
  (mirrors `indZ2_triv_target_value`).
- `indZ2_sign` — `charEq_iso` + the two class-function character identities.

Updated `progress/items.json` coverage note for `Chapter5/Problem5.11.1` (part (a) complete).

## Current frontier

Problem 5.11.1 part (a) is done. Parts (b)-(e) (`indZ3_triv`, `indZ3_nontriv`, `indZ5_triv`,
`indZ5_nontriv`, `indA4_triv`, `indA4_nontriv_linear`, `indA4_threeDim`, `indV4_triv`,
`indV4_nontriv`) remain `sorry` in the same file — out of scope for #6291.

## Overall project progress

Stage 3 formalization ongoing. This turn completed one item-part (Problem 5.11.1(a)) fully.

## Next step

Pick up parts (b)-(e) of Problem 5.11.1. Each follows the same `charEq_iso` + Frobenius pattern;
the nontrivial-character cases (`indZ3_nontriv`, `indZ5_nontriv`, `indV4_nontriv`) will need a
`sign_char`-style pinning of the inducing character's values (roots of unity of the appropriate
order) rather than the ±1 involution argument. The reusable induced-character infra
(`ind_character_eq`, `character_biprod`, the conjugacy-reduction pattern) is already in place.

## Blockers

None.
