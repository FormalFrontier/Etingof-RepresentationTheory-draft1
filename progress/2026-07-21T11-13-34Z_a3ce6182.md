## Accomplished

Proved `schurPoly_eval_one_eq_weylDimension` (issue #7212), replacing the `sorry`
in `EtingofRepresentationTheory/Chapter5/Theorem5_22_1.lean`:

  `S_λ(1, …, 1) = ∏_{i<j} (λ_i − λ_j + j − i)/(j − i) = weylDimension N lam`.

Strategy (avoids limits entirely — the `q → 1` L'Hôpital step becomes honest
polynomial algebra in `ℚ[X]`):

1. Apply the principal specialization `x_i ↦ q^i` (`MvPolynomial.aeval`) to the
   defining alternant identity `schurPoly · Δ = A_{λ+δ}`.
2. `aeval_pow_alternant_det`: under this map each alternant determinant
   `det(x_i^{e_j})` becomes the transpose of a Vandermonde matrix in `q^{e_j}`,
   so `det ↦ ∏_{i<j}(q^{e_j} − q^{e_i})` via `Matrix.det_vandermonde`.
3. `qFactor`/`qFactor_mul`: `q^b − q^a = (q−1)·qFactor a b` with
   `qFactor a b = −q^b·∑_{t<a−b} q^t` (from `geom_sum_mul`); `eval_one_qFactor`
   gives `qFactor a b |_{q=1} = −(a−b)`.
4. `prod_factor_X_sub_one`: pull `(q−1)^P` (`P = ∑_i |Ioi i|`) out of both
   Vandermonde products, cancel it (`mul_left_cancel₀`, domain `ℚ[X]`).
5. Evaluate at `q = 1`: `eval_one_aeval_pow` identifies
   `(aeval … schurPoly)|_{q=1}` with `S_λ(1,…,1)`; the remaining ratio of
   `qFactor` evaluations is exactly `weylDimension` (`Finset.prod_div_distrib`,
   plus cast lemmas turning `ε_i−ε_j ↦ (λ_i−λ_j)+(j−i)` and `δ_i−δ_j ↦ j−i`).

Verified: `lake build EtingofRepresentationTheory.Chapter5.Theorem5_22_1`
succeeds; `#print axioms schurPoly_eval_one_eq_weylDimension` = `[propext,
Classical.choice, Quot.sound]` (no `sorryAx`). Numeric spot check `λ = ![2,1,0]`,
`N = 3` gives `2·2·2 = 8` as expected.

## Current frontier

Theorem 5.22.1 part 3 (`Etingof.Theorem5_22_1_dim`, the Weyl dimension formula)
now has one of its two lemmas fully proven. The other, `finrank_schurModule_eq_eval_one`
(issue #7210), still has a `sorry` at Theorem5_22_1.lean:3731.

## Overall project progress

Stage 3.7 (formalization/review of Chapter 5 items). `Theorem5_22_1_dim` reduces
to two lemmas; #7212 done here, #7210 (weight-space decomposition / semisimplicity
of the diagonal torus) remains.

## Next step

Claim #7210 (`finrank_schurModule_eq_eval_one`): prove `dim L_λ = ∑_μ dim (L_λ)_μ`
via the internal direct-sum decomposition of the Schur module into diagonal-torus
weight spaces. Once it lands, `Theorem5_22_1_dim` is `sorryAx`-free.

## Blockers

None.
