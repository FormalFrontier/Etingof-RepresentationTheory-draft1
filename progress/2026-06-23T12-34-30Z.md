## Accomplished
- Bumped toolchain + Mathlib to **v4.31.0-rc1** (minor-version crossing). Full build green (0 errors, 8842 jobs).
- Migrated the v4.31 `simp`/`dsimp` normal-form change across ~25 files (fixed in parallel sweeps + direct edits). v4.31's simp/dsimp no longer perform several trivial reductions they used to, so previously-load-bearing or cleanup `simp`/`simpa`/`dsimp` calls became no-ops (now an error) or left a differently-normalized term ("Type mismatch: After simplification"). Recurring fixes:
  - Drop the now-redundant `simp only []`/`dsimp`/`simp ... at h` (the next tactic or `exact h` closes by defeq); `<;> dsimp <;> omega` → `<;> omega`.
  - Bare beta-reduction no-ops → `beta_reduce at h`.
  - `simpa using h` type mismatch → `exact h` (defeq), or `simp only [<lemmas that still fire>]` (e.g. `Submodule.mem_bot`, `Function.comp_def`, `Pi.one_apply`, `Submodule.coe_subtype`), or `(Submodule.mem_bot _).mp h`.
  - `Function.comp` (bare) no longer unfolds in simp → use `Function.comp_def`.
  - `conv`/`simp` that used to close now leaves `rfl` → append `rfl`.
  - `group` tactic now "made no progress" on already-normal goals → explicit cancellation rewrites.
  - Plus a `g.inv` vs `(↑g)⁻¹` whnf unification loop fixed in GL2NormalizerInfra.

## Current frontier
v4.31.0-rc1 PR being opened with auto-merge. Next: v4.31.0-rc2.

## Overall project progress
Sweep: v4.28.1 → v4.30.0 merged (13 versions). v4.31.0-rc1 green and merging. Remaining: v4.31.0-rc2, v4.31.0, v4.32.0-rc1.

## Next step
After v4.31.0-rc1 merges, run bumpstep for v4.31.0-rc2 (small delta expected).

## Blockers
None.
