## Accomplished

Closed the single `sorry` in `glTensorRep_schurWeyl_decomposition_equivariant_simple`
(`Chapter5/PolynomialGLDecomposition.lean`), the first of the two bridge lemmas
behind `decompose_polynomial_gl_rep` (Schur-Weyl #5 Step E, issue #4666 / parent
#2482).

Two-file change:

1. **`Chapter5/SchurWeylGLTransfer.lean`** — added
   `Theorem5_18_4_GL_rep_decomposition_explicit_simple`: the explicit-decomposition
   output of `Theorem5_18_4_GL_rep_decomposition_explicit` (concrete `Submodule`
   `S i`, carrier identification `L_carrier i`, explicit iso `e`, evaluation and
   post-composition action formulas) **plus** the per-`i` GL_N simplicity clause
   `∀ i, IsSimpleModule (MonoidAlgebra k GL_N) (asModule (L i).ρ)`. Both source
   theorems (`_explicit` and `_GL_rep_decomposition_simple`) destructure the same
   `Theorem5_18_4_bimodule_decomposition_explicit` and build the same
   `L i = FDRep.of ρ_i`; their existential witnesses were separately opaque, so
   they could not be combined post hoc. The new theorem destructures once and
   exposes both clauses over one shared `L`. Proof = `_explicit`'s body (keeping
   the discarded `homA_simp` centralizer-simplicity clause) followed by the
   `hL_simple` centralizer→GL_N transfer block transplanted from `_simple`. This
   is the natural home: SchurWeylGLTransfer is the only file with both the
   explicit construction (via the `Theorem5_18_4` import) and the simplicity
   helper `isSimpleModule_monoidAlgebra_GL_of_centralizer_simple`.

2. **`Chapter5/PolynomialGLDecomposition.lean`** — proved the bridge by obtaining
   from `_explicit_simple` and re-running the equivariance reduction of
   `glTensorRep_equivariant_schurWeyl_decomposition` (it lives in
   FormalCharacterIso, which SchurWeylGLTransfer does not import, so the join can
   only happen here where both `glTensorRep` and the simplicity helper are in
   scope), threading `hL_simple` through.

Heartbeat budgets bumped to `maxHeartbeats 3200000` / `synthInstance.maxHeartbeats
1600000` on both new declarations, matching the source proofs (the issue flagged
this; the `Submodule → Module k` instance synthesis on the 14-binder existential
exceeds the default budget).

`lake build EtingofRepresentationTheory.Chapter5.PolynomialGLDecomposition` passes,
zero errors.

## Current frontier

`decompose_polynomial_gl_rep` now has exactly one remaining `sorry`, the **second**
bridge `polynomial_homog_rep_asModule_embeds_directSum_simple`
(`PolynomialGLDecomposition.lean:179`) — packaging #4598 + the unified
decomposition + the `asModule` transfer + the `Fin m`-fold product / `S ⊗ L`
splitting. That is a separate sub-issue of #2482, not #4666.

## Overall project progress

Stage 3 (formalization) ongoing. Schur-Weyl #5 Step E: first bridge done; one
bridge left before `decompose_polynomial_gl_rep` is sorry-free, which in turn
feeds the final Schur-Weyl #6 assembly `iso_of_formalCharacter_eq_schurPoly`
(#2483).

## Next step

Tackle the second bridge `polynomial_homog_rep_asModule_embeds_directSum_simple`
(#2482 sub-issue). It consumes #4598's equivariant embedding `M ↪ (V^⊗n)^m`, the
now-available unified `glTensorRep_schurWeyl_decomposition_equivariant_simple`, the
`asModule` transfer (GL_N-equivariant k-linear map ⇔ MonoidAlgebra-linear map),
and the `Fin m`-fold product / `S ⊗ L` splitting.

## Blockers

None.
