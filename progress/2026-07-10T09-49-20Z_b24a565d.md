## Accomplished

Claimed #6137 (modular split-simples foundation for the split-field bound #6126). Scoped it
as too large for one session (it mirrors the 800-line `Infrastructure/IrreducibleEnumeration.lean`
but at the `K[G]`-module level with a surjection instead of an isomorphism) and split it:

- **Landed (deliverables 1-2, sorry-free):** new file
  `EtingofRepresentationTheory/Chapter4/Exercise4_2_3_SplitSimples.lean`:
  - `Etingof.SplitSimples.SplitData` — bundles `n`, block sizes `d` (each `NeZero`), and a
    **surjective** algebra hom `π : K[G] →ₐ[K] Π i, Matrix (Fin (d i)) (Fin (d i)) K`.
  - `SplitData.of` — builds it from the Wedderburn decomposition of the semisimple quotient
    `K[G] ⧸ rad` (`IsSemiprimaryRing.isSemisimpleRing` +
    `IsSemisimpleRing.exists_algEquiv_pi_matrix_of_isAlgClosed`, precomposed with `mkₐ`).
  - `SplitData.blockHom i` + `blockHom_surjective`.
  - Standard modules `SplitData.Std D i := Fin (D.d i) → K` with `Module (K[G])` (via
    `Module.compHom` along `blockHom i`), `IsScalarTower K K[G]`, `Module.Finite K`, and
    simplicity `isSimpleModule_Std` (via `IsSimpleModule.compHom` along the surjection).
  - `#print axioms` on the proven pieces: only `propext, Classical.choice, Quot.sound`.
- **Deferred (deliverables 3-4, stated with `sorry`):** `Std_injective`,
  `exists_Std_linearEquiv`, `card_simpleModuleClasses`, bundled as
  `exists_splitSimples_count`. Split into new issue **#6143**.

## Current frontier

`Exercise4_2_3_SplitSimples.lean` compiles (exit 0, warnings only: the three deferred
sorries + `unusedFintypeInType` noise that cannot be omitted since `SplitData K G` requires
`[Fintype G]`). Partial PR on #6137 being created.

## Overall project progress

Stage 3 (formalization) ongoing. This advances the Exercise 4.2.3 / split-field-bound
subtree (#6126, #6098). The foundation (π + standard modules) is in `main`-bound; the
enumeration + count remains in #6143.

## Next step

Claim **#6143**: discharge the three sorries in `Exercise4_2_3_SplitSimples.lean` by adapting
`IrrepDecomp.columnFDRep_injective` / `columnFDRep_surjective` / `n_eq_card_simples` from
`Infrastructure/IrreducibleEnumeration.lean` to the surjection `π` (central idempotents = any
π-preimage of `Pi.single i 1`; radical acts as 0 on simples via `isTorsionBySet_jacobson`).

## Blockers

None. #6143 is self-contained against the landed file.
