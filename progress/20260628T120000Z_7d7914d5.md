## Accomplished

One-shot dispatch on **#5565** (residual of #5561/#5553): the pure-polynomial
inverted-Schur coefficient bridge `schurPoly_w0Shift_coeff`
(`Chapter5/LinearDualDetTwistCharacter.lean`). **Resolved sorry-free.** The whole
deliverable `formalCharacter_detTwist_linearDual_eq_schurPoly` is now sorry-free
(axioms: only `propext`/`Classical.choice`/`Quot.sound`).

Three new reusable pieces, all in `LinearDualDetTwistCharacter.lean`:

- **`schurPoly_degreeOf_le` / `schurPoly_coeff_le`** (the crux): every variable-degree
  of `schurPoly N lz` is `≤ m` when `lz j ≤ m`. Proof by single-variable degree
  additivity over the integral domain (`MvPolynomial.degreeOf_mul_eq`): from
  `schurPoly·Δ = a_{λ+δ}`, `degreeOf t schurPoly + degreeOf t Δ = degreeOf t a_{λ+δ}`,
  with `degreeOf t a_{λ+δ} ≤ m+(N-1)` (`degreeOf_alternant_le`) and
  `degreeOf t Δ ≥ N-1` (`vandermonde_degreeOf_ge`, via the antisymmetry
  `rename_alternant_det` to exhibit a leading monomial). This is the
  elementary no-truncation guarantee — no dominance machinery needed.

- **`revBox` infrastructure**: the per-variable box reversal `monomial c ↦ monomial (D·1−c)`
  as a `Finsupp.lsum` linear map. Lemmas `revBox_monomial`, `revBox_eq_sum`,
  graded multiplicativity `revBox_mul`, alternant reversal `revBox_alternant`,
  Vandermonde reversal `revBox_vandermonde` (= `sgn(w₀)·Δ`), and the coefficient
  read-off `revBox_coeff`.

- **Assembly**: `schurPoly n ν = revBox m (schurPoly n lz)` by cancelling the
  Vandermonde (`mul_right_cancel₀`), combining `schurPoly_inverseShift`,
  `revBox_mul`, `revBox_vandermonde`, `revBox_alternant`; then `revBox_coeff`
  (fed by `schurPoly_coeff_le`) reads off the coefficient formula.

`lake build EtingofRepresentationTheory.Chapter5.LinearDualDetTwistCharacter` green.

## Current frontier

#5565 fully resolved; the linear-dual half of the contragredient character identity
(#5553 deliverable) is sorry-free.

## Overall project progress

Stage 3 ongoing. The `det^s`-twisted linear-dual formal-character identity is
complete and axiom-clean. Pre-existing unrelated sorry remains at
`ContragredientIdentity.lean:151` (separate theorem, outside this chain).

## Next step

Pick up the next open Ch5 issue (e.g. the Peter-Weyl injectivity cluster #5556 →
#5549, or surjectivity #5550). The `revBox` and `schurPoly_degreeOf_le` lemmas are
now available for any future Schur-polynomial degree/reversal arguments.

## Blockers

None for #5565 — fully resolved.
