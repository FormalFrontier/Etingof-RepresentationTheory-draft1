## Accomplished

Issue #1437: Stage 3.2 Ch6 prove branch_classification (Dynkin D_n/E-type case).

Built the proof infrastructure for `branch_classification` — the last sorry in the Dynkin classification chain. The proof is now decomposed into well-defined sorry'd subgoals:

1. **`dynkin_unique_degree_three`** (~10 lines, sorry'd): At most one vertex has degree 3 in a Dynkin diagram. Proof strategy documented: define null vector x with weight 2 on the v-to-w path and weight 1 on extra neighbors, giving B(x,x) = 0.

2. **`branch_has_leaf_neighbor`** (~90 lines, partially proved): Branch vertex has a leaf neighbor. Extracted 3 neighbors, showed each has degree ≥ 2 if no leaf exists, extracted second neighbors, defined T_{2,2,2} null vector. Sorry'd: the B(x,x) ≤ 0 computation.

3. **`branch_classification` main body** (~270 lines, partially proved):
   - Leaf removal: removes the leaf neighbor u of branch vertex v (~150 lines, fully proved)
   - Reduced graph is Dynkin: connectivity, positive definiteness, all degrees ≤ 2 (fully proved, using unique_branch)
   - Path walk construction applied (fully proved)
   - v's position b determined in path ordering
   - Arm lengths (1, min(b, k-1-b), max(b, k-1-b)) set up
   - `arm_length_solutions` applied for case split into D_n/E6/E7/E8
   - Sorry'd subgoals:
     - `hv'_deg2`: v has degree 2 in reduced graph (clear strategy)
     - `hb_lt_k1`: v is not the last path vertex (needs endpoint degree argument)
     - `hrecip`: reciprocal-sum constraint from positive definiteness
     - 4 isomorphism constructions (D_n, E6, E7, E8)

## Current frontier

9 sorry locations in 3 declarations. The sorry's fall into categories:
- **Null vector computations** (2): B(x,x) ≤ 0 for specific vectors. Mechanical but tedious sum decomposition.
- **Degree counting** (2): v' has degree 2 in adj', and v' is not an endpoint. Both follow from v having degree 3 in adj and losing 1 edge.
- **Positive definiteness constraint** (1): deriving the reciprocal-sum inequality from the Cartan form. Requires embedding T_{p,q,r} as a subgraph.
- **Isomorphism constructions** (4): building σ : Fin t.rank ≃ Fin n for each type. The path ordering σ' provides most of the structure; need to extend with the removed leaf u and verify adjacency matches the standard type.

## Overall project progress

- `branch_classification` went from 1 monolithic sorry to 9 well-structured sorry's across 3 lemmas
- The proof strategy (unique branch → leaf removal → path walk → arm lengths → case split) is fully implemented
- ~270 lines of new proven Lean code for the reduced graph construction and type determination
- Once sorry's are filled: `dynkin_classification_forward` becomes sorry-free, completing the Dynkin classification theorem

## Next step

Fill in the remaining sorry's in priority order:
1. `hv'_deg2` and `hb_lt_k1` (degree counting — straightforward finset cardinality)
2. `branch_has_leaf_neighbor` B(x,x) computation (sum decomposition)
3. `dynkin_unique_degree_three` (similar null vector argument)
4. `hrecip` (T_{p,q,r} embedding + null vector)
5. D_n isomorphism construction (general n, extend σ' with leaf)
6. E6/E7/E8 isomorphism constructions (fixed n, more concrete)

## Blockers

None — all remaining sorry's have clear proof strategies documented in comments.
