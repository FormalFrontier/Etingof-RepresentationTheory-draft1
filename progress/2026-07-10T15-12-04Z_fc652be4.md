## Accomplished

Assembled both general-`L` main theorems of **Problem 3.8.4** sorry-free and axiom-clean,
completing issue #6193 (the last remaining piece of #6058's deliverable 3).

- The two descent lemmas named as deliverables 1 & 2 in #6193
  (`exists_fg_subalgebra_baseChange_iso`, `exists_fg_subalgebra_baseChange_directSummand`) were
  already proven sorry-free on `main` by earlier PRs (#6170, #6185). The issue body was stale on
  that point.
- The real remaining gap was **part (i)**: `iso_of_baseChange_iso` still carried a bare `sorry`
  in `Problem3_8_4.lean`, and the proven iso descent lemma was not consumed anywhere.
- Added `iso_of_baseChange_iso` to `Problem3_8_4_General.lean` (downstream of the
  descent/functoriality machinery, avoiding the import cycle with `Problem3_8_4.lean`), mirroring
  the existing part-(ii) assembly: descend to a finitely generated `K`-subalgebra `R ⊆ L`,
  specialize to a residue field `κ = R ⧸ m` finite over `K` (Zariski), push the isomorphism
  forward along `R →ₐ[K] κ` (`Functoriality.nonempty_baseChange_iso`), finish with
  `iso_of_baseChange_iso_finite`.
- Replaced the sorried theorem in `Problem3_8_4.lean` with a docstring pointer (matching how
  part (ii) is already handled there). No external code referenced the moved theorem by name.
- Updated the `General.lean` header docstring to describe both parts.
- Flipped `Chapter3/Problem3.8.4` to `status: "sorry_free"`, `sorry_free: true` in
  `progress/items.json` with a completion note.

## Current frontier

`Chapter3/Problem3.8.4` is complete. `#print axioms` on both
`Etingof.Problem3_8_4.iso_of_baseChange_iso` and
`Etingof.Problem3_8_4.directSummand_of_baseChange_directSummand` shows only
`propext, Classical.choice, Quot.sound`. No `sorry` remains in any `Problem3_8_4*.lean` file.
`lake build` of the whole 3.8.4 module chain succeeds.

## Overall project progress

Stage 3 formalization ongoing. One more exercise (`Chapter3/Problem3.8.4`) is now fully
sorry-free.

## Next step

Pick up another unclaimed feature issue — e.g. #6139 (strict Exercise_4_2_3) or #6162
(Problem_8_2_6_v_tor). Note #6058's assembly issue is now moot for part (i)/(ii): the theorems
it wanted are proven; if #6058 has a residual PR (#6194 was CONFLICTING), a repair/planner pass
should reconcile it against this completion.

## Blockers

None.
