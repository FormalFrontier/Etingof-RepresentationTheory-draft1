## Accomplished

- Claimed issue #1846 (polynomial ring global dimension upper bound)
- Proved `hasProjectiveDimensionLT_of_equivalence`: equivalences of abelian categories with enough projectives preserve projective dimension bounds (induction on n via kernel SES from projective presentations)
- Proved `hasHomologicalDimensionLE_of_ringEquiv`: ring isomorphisms preserve global homological dimension (via `restrictScalarsEquivalenceOfRingEquiv`)
- Closed the inductive step of `mvPolynomial_hasHomologicalDimensionLE` by combining the ring equiv transfer with `MvPolynomial.finSuccEquiv`, reducing it to one focused sorry
- Created PR #1861 with auto-merge enabled

## Current frontier

The remaining sorry for the upper bound is `hasHomologicalDimensionLE_polynomial`: if every R-module has projective dimension ≤ d, then every R[x]-module has projective dimension ≤ d+1. This requires constructing the standard short exact sequence for R[x]-modules and proving flat base change preserves projective dimension — substantial homological algebra infrastructure not yet in Mathlib.

## Overall project progress

27 sorries across 19 files (from sorry landscape wave 38). This PR does not change the sorry count but decomposes the inductive step sorry into a cleaner, more focused target. Two new general-purpose lemmas proved (equivalence preserves pd, ring equiv preserves homological dimension).

## Next step

1. The core remaining work for #1846 is `hasHomologicalDimensionLE_polynomial` — constructing the R[x]-module SES and the dimension shift argument
2. Issue #1847 (lower bound / Ext^n(k,k) ≠ 0) is independent and still unclaimed
3. Other unclaimed issues available: #1691, #1773, #1782, #1785, #1788, #1799

## Blockers

None — the infrastructure for the transfer lemmas is complete. The polynomial extension theorem is self-contained remaining work.
