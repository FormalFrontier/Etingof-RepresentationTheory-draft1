## Accomplished

Feature session for issue #7322: exposed the **grand-total count headline** for the complete
irreducible classification of the Heisenberg group, upgrading **Problem 4.12.2(d)** from
`covered_partial` to `covered_full`.

Added to `EtingofRepresentationTheory/Chapter4/Problem4_12_2.lean` (new section after
`irreducible_dim`, sorry-free, all four new theorems axiom-clean
`#print axioms = {propext, Classical.choice, Quot.sound}`):

- `IrrepIndex p` (abbrev): the index set `(Heisenberg p →* ℂˣ) ⊕ {z : ℂ // zᵖ = 1 ∧ z ≠ 1}`
  — `p²` characters plus the `p-1` nontrivial `p`-th roots.
- `card_nontrivial_pthRoots`: `Nat.card {z : ℂ // zᵖ = 1 ∧ z ≠ 1} = p - 1`, via
  `Polynomial.nthRootsFinset` + `IsPrimitiveRoot.card_nthRootsFinset` (`p` roots, minus `1`).
- `instance : Finite {z : ℂ // zᵖ = 1 ∧ z ≠ 1}` (injects into the finite root finset).
- `nonempty_irrepClasses_equiv`: the **enumeration bijection**
  `Nonempty (IrrepClasses ℂ (Heisenberg p) ≃ IrrepIndex p)`. Built with `Equiv.ofBijective` on
  `i ↦ ⟦⟨E i, simple⟩⟧` into the iso-class quotient `Etingof.IrrepClasses` (from Exercise4_2_3),
  reusing completeness (`simple_iso_charRep_or_rhoHom`) for surjectivity and the already-exposed
  `charRep_iso_iff` / `rhoHom_iso_iff` / `charRep_not_iso_rhoHom` for injectivity.
- `card_irreducibles`: **the headline** — `Nat.card (Etingof.IrrepClasses ℂ (Heisenberg p)) =
  p ^ 2 + (p - 1)`, via the bijection + `Nat.card_sum` + `one_dim_reps_card` +
  `card_nontrivial_pthRoots`.
- `sum_sq_dim_eq_card` (optional deliverable 2): `p²·1² + (p-1)·p² = |Heisenberg p|` (= `p³`).

Key decision: indexed the `p`-dimensional block by the subtype of **nontrivial `p`-th roots**
`{z // zᵖ = 1 ∧ z ≠ 1}` rather than `{k : ZMod p // k ≠ 0}` (as the issue suggested). This makes
`FDRep.of (rhoHom z hz)` match the completeness lemma's output *definitionally*, so surjectivity
and injectivity of the bijection need no discrete-log transport; the only extra cost is the
one-line cardinality `card_nontrivial_pthRoots`. Reused the top-level pairwise-non-iso lemmas
directly instead of lifting the proof-local `hEinj` out of `simple_iso_charRep_or_rhoHom`.

Updated `progress/items.json`: part (d) derived item → `covered_full` with the new `lean_decl`
pointers and `followup_issue_resolved: 7322`. Parent `Chapter4/Problem4.12.2` roll-up stays
`covered_partial` because part (c) remains partial (unrelated follow-up #7321). Updated the
module docstring's (d) bullet to cite the new headline.

## Current frontier

Issue #7322 fully complete. `lake build EtingofRepresentationTheory.Chapter4.Problem4_12_2`
succeeds; file sorry-free and axiom-clean.

## Overall project progress

Stage 3.7 coverage/fidelity audits and iso-/count-level upgrades of §4.12 in progress. This lands
the 4.12.2(d) grand-total count headline. Part (c) of 4.12.2 still `covered_partial` (parametrizing
bijection headline, follow-up #7321). Remaining unclaimed §4.12 coverage audits: #7306 (4.12.7),
#7320 (Exercise 5.27.2).

## Next step

Merge this PR. A future session can pick up #7321 (expose the (c) one-dim-rep parametrization
headline) to flip the 4.12.2 parent roll-up to `covered_full`, or the coverage audits #7306/#7320.

## Blockers

None.
