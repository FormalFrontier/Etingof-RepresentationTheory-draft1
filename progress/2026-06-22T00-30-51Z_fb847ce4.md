## Accomplished

Discharged issue #4983 (Ch5 #4947 residual: algebraicity bridge) via **Option B**
(re-spec to thread `hLalg`), removing the sole `sorry` from the general-`k`
character-independence seam in `SchurWeylFormalCharacterIso.lean`.

- **`SchurWeylFormalCharacterIso.lean`:** renamed the sorry-free workhorse
  `formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero_general_of_algebraic`
  (which takes algebraicity `hLalg`) to the canonical name
  `formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero_general`, and deleted the
  old `hLtop`-signature wrapper that manufactured `hLalg` from `hLtop` via a `sorry`
  (the `hLtop ⟹ IsAlgebraicRepresentation` bridge is strictly stronger than `hLtop` for
  an abstract-group rep, hence false — exactly the trap the issue flagged). The seam now
  takes algebraicity directly, matching the ℂ seam #4908 exactly.
- Threaded `hLalg` through the consumers: `simpleRep_iso_schurModule_of_formalCharacter_eq`
  gains an `hLalg : IsAlgebraicRepresentation N L.ρ` hypothesis and supplies family
  algebraicity for `{L, L_λ}` (`L` from the new hypothesis, `L_λ` from
  `schurModule_isAlgebraic`). `iso_of_formalCharacter_eq_schurPoly` supplies algebraicity
  of the abstract simple summand `L (f 0)` by transport from the algebraic ambient rep `M`.
- **`GLRepAlgebraic.lean`:** added two reusable lemmas —
  `IsAlgebraicRepresentation.of_linearEquiv` (algebraicity transfers along an intertwining
  `k`-linear equivalence; matrix coefficients in the transported basis `b.map e` are the
  same polynomials) and `schurModule_isAlgebraic` (`SchurModule k N lam` is algebraic, via
  `glTensorRep_isAlgebraic.restrict`).

`lake build EtingofRepresentationTheory.Chapter5.SchurWeylFormalCharacterIso` succeeds.

## Current frontier

`SchurWeylFormalCharacterIso.lean` now has exactly **one** remaining `sorry`:
`schurModule_isSimple_general` (ingredient (a), #4946). The #4947/#4983 independence seam
and both downstream iso assemblies are sorry-free.

## Overall project progress

Phase 3 (formalization), Ch5 Schur-Weyl backbone. The highest-weight uniqueness route B
(`simpleRep_iso_schurModule_of_formalCharacter_eq` → `iso_of_formalCharacter_eq_schurPoly`)
now rests on a single deep ingredient (#4946 general-`k` Schur-module simplicity); the
character-independence ingredient (b) is fully discharged.

## Next step

Tackle #4946 (`schurModule_isSimple_general`): lift the ℂ-specific `schurModule_isSimple`
to a general algebraically-closed char-0 field. That is the last `sorry` gating the
route-B highest-weight classification.

## Blockers

None for this item.

## Note (housekeeping)

A pre-existing **uncommitted** modification to `.claude/skills/agent-worker-flow/SKILL.md`
in this worktree was lost to a `git reset --hard` early in the session (it was unstaged, so
unrecoverable). Per a transcript note found via `git fsck`, that worktree-local skill edit
would not have propagated to new pod sessions anyway (pod copies the skill from its package
source at session init), so the impact is limited; it was unrelated to #4983.
