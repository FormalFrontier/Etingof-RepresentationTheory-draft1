## Accomplished

Feature session `4dc310e2`, issue #4555 (Ch6 #4548 sub-1): the
foundational **homogeneous-tube indecomposability mechanism** for
tree-shaped affine quivers. New file
`EtingofRepresentationTheory/Chapter6/FieldGenericTube.lean` (added to the
`Chapter6.lean` aggregator), two deliverables:

1. **Reusable §3 reduction core** —
   `eigenvalue_jordan_invariant_compl_trivial_gen`. Captures "the eigenvalue
   `λ` drops out": any complementary pair invariant under `λ • id + N`
   (`N` nilpotent, 1-dim kernel) has one component `⊥`. Proof: invariance
   under `λ • id + N` upgrades to invariance under `N`
   (`N x = (λ•id+N)x − λ•x`, both terms stay in the submodule), then the
   existing workhorse `nilpotent_invariant_compl_trivial_gen` finishes. This
   is the portable lemma the per-shape sporadic sub-issues (#4557/#4558/#4559)
   call at their eigenvalue site.

2. **D̃₄ validation** — `starTubeRepGen F lam m`: the 4-subspace star tube
   (`δ = (2;1,1,1,1)`, dim `(m+1)·δ`) whose fourth arm carries the square
   eigenvalue site `λI + J` (`jordanShiftLinGen`) instead of a rank-deficient
   nilpotent twist. `starTubeRepGen_isIndecomposable` proves it indecomposable
   **for every `λ`** through the reduction lemma, generalizing the existing
   `λ = 0` construction `starRepGen`. The proof mirrors
   `starRepGen_isIndecomposable`: the three iso/identity arms collapse
   `W(1)=W(2)=W(3)=W(4)`, arm 4 deposits a `(λ•id+N)`-invariant pair at the
   common space, the reduction core kills it.

Deliverable 1 of the issue ("identity/iso-arrow collapse helper") was already
satisfied by the shared `compl_le_forces_eq` (`FieldGenericInfiniteType.lean`);
the proof reuses its one-sided inline variant `compl_eq_of_le`. Documented this
in the module header rather than re-factoring.

Verification: `lake build EtingofRepresentationTheory.Chapter6` →
8087 jobs, success. `#print axioms` on both new theorems shows only
`[propext, Classical.choice, Quot.sound]` (no `sorryAx`). No `sorry` in the
new file; the `def` bodies are real objects.

## Current frontier

D̃₄ tree-tube example landed with the reusable eigenvalue/`J` mechanism. The
per-shape sporadic sub-issues (#4557 Ẽ₆, #4558 Ẽ₇, #4559 T(1,2,5)) can now
import `FieldGenericTube` and call
`eigenvalue_jordan_invariant_compl_trivial_gen` at their eigenvalue site once
they derive the regular-simple rectangular matrices (design doc §5, the honest
open part).

## Overall project progress

Etingof draft 1 — Stage 3 (formalization), Ch6 affine/infinite-type program.
Sporadic-tube redesign (#4548) foundation complete. Pre-existing sorries
remain in `InfiniteTypeConstructions.lean` (3249/3392/3496) and
`FieldGenericStar.lean:764` — unrelated to this issue.

## Next step

A worker claims #4557 (Ẽ₆ = T(2,2,2)) and builds `etilde6Tube_kQ` on the
`FieldGenericTube` mechanism: derive `R_λ` at `δ = (3;2,1;2,1;2,1)`, inject
`λI + J` at the eigenvalue site, collapse via the iso arms, finish with
`eigenvalue_jordan_invariant_compl_trivial_gen`. Then #4558, #4559.

## Blockers

None for this issue. PR created from branch `agent/4dc310e2`.
