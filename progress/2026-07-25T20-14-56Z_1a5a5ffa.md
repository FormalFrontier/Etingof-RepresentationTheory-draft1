# 2026-07-25T20:14:56Z — feature session (1a5a5ffa), issue #7820

## Accomplished

Removed the `(2 : k) ≠ 0` hypothesis from the exterior half of Problem 2.11.3(d)
(`EtingofRepresentationTheory/Chapter2/Problem2_11_3_SymExtPow.lean`). `exteriorPowerEquiv`,
`exteriorPowerEquiv_ιMulti`, `extPowBasis`, `finrank_extPow` and `extRelSubmodule_le_ker` now hold
over an arbitrary field, with the hypothesis gone from the signatures rather than a parallel API
appearing. Section `CharNeTwo` renamed to `ExteriorEquiv`.

Two new lemmas carry the work:

* `piTensorProduct_repr_permAct b σ T g` — permuting the tensor factors precomposes coordinates in
  a tensor-product basis: the `g`-coordinate of `permAct σ T` is the `g ∘ σ`-coordinate of `T`.
  Proved by `PiTensorProduct.induction_on` + `Basis.piTensorProduct_repr_tprod_apply` +
  `Fintype.prod_equiv σ`. Reusable for any `permAct`/coordinate argument.
* `tensorPowToExteriorPower_eq_zero_of_permAct_swap_eq b hij hT` — the injectivity input, proved on
  a basis so it covers characteristic 2.

The characteristic-2-safe argument (this is the whole point of the issue): expand
`T = ∑_g c_g e_g` in the tensor-product basis. `sT = T` gives `c_{g∘s} = c_g`, and `Φ e_{g∘s} =
-Φ e_g` because `ιMulti` is alternating, so `Φ T = ∑_g c_g Φ e_g` cancels **in pairs** under
`g ↦ g ∘ s`. A single `Finset.sum_involution` over the whole support handles it: the `g` fixed by
the involution are exactly those with `g i = g j`, whose term is already `0`, so the required
`F g + F (g ∘ s) = 0` and "fixed ⟹ zero" side conditions are uniform. No halving anywhere, so no
characteristic hypothesis. The old `2 • Φ T = 0` route is simply deleted.

Also restored 320 lines of `.claude/` workflow guidance (commands + `agent-worker-flow` SKILL.md)
that arrived in this worktree as uncommitted *deletions* from a crashed prior session — exactly the
accidental-revert failure mode the skill's own Step 2 documents. Third recorded occurrence.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter2.Problem2_11_3_SymExtPow` clean; full-project
`lake build` clean (0 errors). `#print axioms` on `exteriorPowerEquiv`, `finrank_extPow`,
`extPowBasis`, `extRelSubmodule_le_ker` and both new lemmas: only
`propext, Classical.choice, Quot.sound`. Signatures confirmed free of `(2 : k) ≠ 0`.

## Overall project progress

Stage 3 formalization, Chapter 2. Problem 2.11.3 part (d) is now complete and hypothesis-free on
the exterior side. Remaining parts of 2.11.3 are open as separate issues: (e) char-0 identification
with the (anti)symmetric subspaces (#7817), (f) trace formulas (#7819, blocked on #7816), (g)
`⋀^N A = det(A) • id` (#7818). The symmetric half of (d) — a multiset-indexed basis of `S^n V` —
is still open and has no Mathlib `SymmetricPower` universal property to lean on.

## Next step

#7818 (`⋀^N A = det(A) • id`) is the natural follow-up and just got easier: its route transports
along `exteriorPowerEquiv`, whose `(2 : k) ≠ 0` hypothesis this issue removed, so the endpoint no
longer needs a characteristic side condition either. The issue body still says "for a
finite-dimensional `V` ... and `(2 : k) ≠ 0`" — that clause is now stale and should be dropped.

## Blockers

None.

## Skill notes recorded

Added to `.claude/skills/lean-formalization/SKILL.md`: `Basis.piTensorProduct` lives at
`_root_.Basis.…` (not `Module.Basis.…`, the opposite of the neighbouring `repr_reindex_apply`
note); `Basis.sum_repr` needs `[Fintype ι]` so an arbitrary-index basis expansion has to go through
`linearCombination_repr`; and the pair-cancellation pattern for making an alternating-map argument
work in characteristic 2.
