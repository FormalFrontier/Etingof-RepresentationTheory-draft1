## Accomplished

Issue #4554 (shared field-generic N-invariance infrastructure), feature session.

**Deliverable 1 — relocation (as specified).** Moved the `(I - N)⁻¹`
infrastructure from `FieldGenericD5Tilde.lean` into the shared
`FieldGenericStar.lean`: `cumTailSumMatrix`, `cumTailSumLin`, the
closed-form application lemmas (`cumTailSumLin_apply`, `_apply_last`,
`_apply_succ`), and the telescoping inversion identity
`cumTailSumLin_oneSubNilp` (`M (v - N v) = v`). `D5Tilde` keeps compiling
unchanged via its existing `Star` import; no new sorries. Note:
`nilpotentShiftLinGen` / `nilpotentShiftMatrixGen` were *already* shared in
`FieldGenericInfiniteType.lean`, so contrary to the issue body only the
`cumTailSumLin` cluster needed moving.

**Deliverable 2 — reformulated (the literal spec was circular).** The
specified per-leaf N-invariance `y ∈ Wmain⟨leaf⟩ → N y ∈ Wmain⟨leaf⟩` is
**circular** as standalone infrastructure: the partial fact only gives
`y∈Wmain⟨1⟩ → Ny∈Wmain⟨5⟩`, and bridging leaf 5 back to leaf 1 is itself
part of the leaf-equality conclusion the mixed branches must derive.
Confirmed via second-opinion (Codex). Instead landed the correct
non-circular shared lemma in `FieldGenericStar.lean`:

`linearEquiv_invariant_isCompl_symm_mem` — a linear iso `g : V ≃ₗ[F] W`
carrying a complementary invariant pair `(A₁,A₂)` into `(B₁,B₂)` maps `A₁`
onto `B₁`, so `g.symm w ∈ A₁` whenever `w ∈ B₁`. Field-generic, abstract
(no d5 vertex literals), no finite-dimensionality, proven sorry-free. This
is the genuine untwisting tool: package γ as a `LinearEquiv` and pull
center-B membership back to center A, recovering the untwisted coordinates
`e₁(a - M(a-b)) + e₂(M(a-b))` via the relocated `cumTailSumLin = M`.

Posted a detailed reformulation comment on #4554.

## Current frontier

Shared untwisting infrastructure is in place. Downstream mixed-direction
branches (d5/d6/d7/d8) can now consume `linearEquiv_invariant_isCompl_symm_mem`,
but first need their γ packaged as a `LinearEquiv` (the
`d5tildeGamma_F ∘ d5tildeGammaInv_F = id` identities, currently unproven,
were explicitly scoped out of #4554).

## Overall project progress

Chapter 6 D̃-family orientation-generic indecomposability program ongoing.
This issue removes the shared cross-family blocker (the `(I-N)⁻¹` algebra
is now reusable, and the real untwisting lemma exists). `FieldGenericStar`
and `FieldGenericD5Tilde` build; `FieldGenericD6Tilde` also rebuilds clean.

## Next step

Planner should re-point #4551 (d6), #4533 (d7), #2850/#2851 (d5) to consume
`linearEquiv_invariant_isCompl_symm_mem` rather than "per-leaf
N-invariance". The first concrete consumer task is: prove the γ-equiv
packaging (`d5tildeGamma_F`/`d5tildeGammaInv_F` two-sided inverse →
`LinearEquiv`), then apply the untwisting lemma to close one mixed-direction
branch (e.g. d6 combo C) as a proof of concept.

## Blockers

None for this issue. Downstream consumption is gated on the γ-equiv
packaging (small, well-defined follow-up), not on anything here.
