## Accomplished

Issue #1298: Stage 3.2 — Ch5 assemble innerProduct_sum_eq_card for Lemma 5.25.3.

1. **Fixed build error** — Removed broken `upper_tri_conj_00_eq` lemma (wrong statement: claimed (x⁻¹gx)₀₀ = g₀₀, which is false in char 2 and for non-upper-triangular g).

2. **Added 3 correct helper lemmas** for parabolic character value infrastructure:
   - `upper_tri_conj_00_sq_eq_det`: For upper-triangular conjugate of disc=0 element, M₀₀² = det(g).
   - `upper_tri_conj_00_trace`: For same, 2·M₀₀ = tr(g).
   - `upper_tri_conj_00_unique`: In char ≠ 2, all upper-triangular conjugates of a disc=0 element have the same (0,0) entry (= tr(g)/2).

3. **Proved `complementaryChar_parabolic_val`** modulo one focused sorry (`charVα₁_parabolic`):
   - Shows χ(g) = -α(a) for parabolic g by proving χ(g) = -charVα₁(g) (from charW₁=0 and Ind=0)
   - Uses `parabolic_conj_not_in_ellipticSubgroup` for the induced character vanishing

4. **Proved `normSq_complementaryChar_parabolic`** from `complementaryChar_parabolic_val`.

5. **Eliminated forward-reference sorry** by moving parabolic lemma block after `parabolic_conj_not_in_ellipticSubgroup`.

6. **Added `charVα₁_parabolic` as a well-documented sorry** with detailed proof strategy:
   - Step 1: All nonzero Frobenius sum terms equal α(tr(g)/2) (proved via `upper_tri_conj_00_unique`)
   - Step 2: Count of upper-triangular conjugates = borelCard (orbit-stabilizer: 1 eigenvector direction × (q-1) scalings × q(q-1) second columns)
   - Step 3: charVα₁(g) = borelCard⁻¹ · borelCard · α(a) = α(a)

## Current frontier

Two sorry'd lemmas remain (same count as before, but better structured):
- `charVα₁_parabolic` (line ~1249): Counting argument — needs to show the number of GL₂ matrices x with x⁻¹gx upper-triangular equals borelCard for parabolic g
- `induced_normSq_sum_elliptic` (line ~1448): Elliptic sum — needs conjugacy class decomposition and character orthogonality

## Overall project progress

- ~193+/583 items sorry-free
- Lemma5_25_3.lean: 2 sorries remain, but now with complete proof framework
  - `complementaryChar_parabolic_val` and `normSq_complementaryChar_parabolic` are fully proved
  - `innerProduct_sum_eq_card` is fully proved assuming the two helper sorries
  - Helper infrastructure (trace/det invariance, uniqueness) is in place

## Next step

1. **`charVα₁_parabolic`**: The counting argument requires ~100-150 lines of new Lean code:
   - Formalize: x⁻¹gx upper-tri ↔ first column of x is eigenvector of g
   - Count matrices with a given first-column direction: (q-1) · q(q-1) = borelCard
   - Use charW₁(g) = 0 for parabolic g (1 eigenvector direction on P¹)

2. **`induced_normSq_sum_elliptic`**: More complex, requires:
   - Conjugacy class decomposition within elliptic elements (K \ F_q×)
   - Frobenius character formula on K: Ind(ζ) = ν(ζ) + ν^q(ζ)
   - Character orthogonality: ∑_K ν^{q-1} = 0

## Blockers

None — both sorries have clear proof strategies documented in the code.
