## Accomplished

Session type: **feature** (`/work` → `/feature 6961`).

Worked the octahedral realization (#6961, `{2,3,4} → S₄`, `|G|=24`) in
`EtingofRepresentationTheory/Chapter4/Problem4_12_8.lean`. After scoping, the octahedral case is
materially harder than the already-landed tetrahedral one: the pole-orbit sizes are `{12, 8, 6}`,
so **none** is `4`, and the full `S₄` comes only from the action on the four body diagonals =
the `8` order-`3` poles modulo antipode. That needs (a) an antipode-quotient `4`-element set with a
`MulAction`, and (b) a harder faithfulness argument routing through a coplanarity refutation.

Landed the clean **reduction** (all sorry-free except the isolated geometric core):
- Proved `hcard : Nat.card G = 24` from the `{2,3,4}` counting identity (`RHS = 23/12`).
- Proved the cardinality assembly: injective `φ : G →* Equiv.Perm (Fin 4)` plus
  `|G| = 24 = |S₄|` ⟹ bijection (`Fintype.bijective_iff_injective_and_card`) ⟹ `MulEquiv.ofBijective`.
- Isolated the geometry into a stated helper `exists_octahedral_faithful_hom`
  (`∃ φ : ↥G →* Equiv.Perm (Fin 4), Function.Injective φ`) with a full docstring proof sketch.
- `so3_octahedral_of_poleData` is now **sorry-free modulo** `exists_octahedral_faithful_hom`.

Filed sub-issue **#6972** for the helper with a fully-worked route (antipode quotient →
`4`-diagonal set → faithfulness via `g²=1`, `ord g = 2 ∤ 3`, coplanarity refuted by
`isCyclic_of_common_fixed_vector` + max-pole-order-4). Left the `Decomposed into #6972` breadcrumb
on #6961.

`lake build EtingofRepresentationTheory.Chapter4.Problem4_12_8` succeeds.

## Current frontier

`exists_octahedral_faithful_hom` (line ~1739) is the sole remaining sorry of the octahedral case.
The icosahedral `so3_icosahedral_of_poleData` (#6962) is still fully sorried (pre-existing, untouched).

## Overall project progress

Ch4 Problem 4.12.8 part (a) dispatch (`so3_classification_aux`) is assembled. Of the five family
disjuncts: cyclic, dihedral, tetrahedral are sorry-free; octahedral is sorry-free modulo #6972;
icosahedral (#6962) remains. Part (b) (`su2_finite_subgroup_double_cover`) still open.

## Next step

Claim **#6972** and prove `exists_octahedral_faithful_hom` following the route in its body
(mirror `so3_tetrahedral_of_poleData` Steps 2–4 for the orbit/transport; the new content is the
antipode quotient and the coplanarity refutation — consider splitting the latter into its own
sub-lemma). Then #6962 (icosahedral) is the last family disjunct.

## Blockers

None. #6961 is decomposed; its residual geometric core is tracked as #6972.
