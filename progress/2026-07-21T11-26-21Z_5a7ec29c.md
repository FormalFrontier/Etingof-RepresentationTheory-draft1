## Accomplished

Feature session for issue #7210: proved `finrank_schurModule_eq_eval_one`
(`EtingofRepresentationTheory/Chapter5/Theorem5_22_1.lean`), replacing its
`sorry` with the full weight-space decomposition `dim L_λ = ∑_μ dim (L_λ)_μ`.

New declarations (all sorry-free):
- `glTensorRep_diagUnit_isSemisimple`: the diagonal torus operator on the
  tensor power is semisimple — it is diagonal in `tensorStdBasis` with
  eigenvalues `u^m` (`m ≤ n`), so the squarefree polynomial `∏_{s∈S}(X−s)`
  annihilates it (`isSemisimple_of_squarefree_aeval_eq_zero`,
  `separable_prod_X_sub_C_iff'`, `aeval_apply_of_mem_apply_eq_smul`).
- `schurModule_rho_diagUnit_isSemisimple`: the restriction to the GL-stable
  Schur submodule stays semisimple (`IsSemisimple.restrict`).
- `finrank_schurModule_eq_eval_one`: reusing the existing
  `glWeightSpace_finite_support` machinery, each `glWeightSpace μ` equals the
  simultaneous max generalized eigenspace of the commuting semisimple family
  (`IsFinitelySemisimple.maxGenEigenspace_eq_eigenspace`); independence comes
  from `independent_iInf_maxGenEigenspace_of_forall_mapsTo`, spanning from
  `iSup_iInf_maxGenEigenspace_eq_top_of_...commute` plus a basis-coefficient
  argument (`repr_glTensorRep_diagUnit`) pinning every nonzero joint eigenspace
  to a genuine `tensorWeight`. `DirectSum.isInternal_...` + `Module.finrank_directSum`
  give the dimension identity.

`#print axioms finrank_schurModule_eq_eval_one`: `[propext, Classical.choice,
Quot.sound]` — no `sorryAx`.

## Current frontier

`Theorem5_22_1_dim` (Weyl dimension formula, part 3) now depends on only one
remaining sorry: the sibling `schurPoly_eval_one_eq_weylDimension`
(`S_λ(1,…,1) = weylDimension`), which is issue #7212 (claimed by another agent).

## Overall project progress

Stage 3 formalization. Theorem 5.22.1 part 3's dimension half is fully proved;
the character-side principal-specialization limit (#7212) is the last piece
before `Theorem5_22_1_dim` is sorry-free.

## Next step

Land #7212 (`schurPoly_eval_one_eq_weylDimension`). Once both siblings are in,
`#print axioms Etingof.Theorem5_22_1_dim` should be free of `sorryAx`.

## Blockers

None for this item.
