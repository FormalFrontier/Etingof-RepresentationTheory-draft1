## Accomplished

- Claimed issue #2948: audit of PR #2933
  (`non_adjacent_branches_leaf_case_per_kQ` API stub) + PR #2941
  signature delta.
- Authored audit report at
  `progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-api-stub.md`
  with PASS/CONCERN/FAIL verdicts on the five dimensions D1-D5
  specified by the issue.
- **Verdict: PASS on all five dimensions.** Two minor non-blocking
  concerns (unused `FieldGenericD5Tilde` import currently; stale
  docstring at lines 28-30 missing `d7tilde_not_finite_type_per_kQ`
  after #2968 landed).
- **Factual correction to audit-issue body:** the issue claims #2941
  added `h_v₀w_nonadj`, `h_leaf_adj`, `h_leaf_deg`; in fact #2941
  added **only** `h_no_adj_branch_w`. The other three were already
  in #2933. Verified by `git show 601f2ea`. Audit verdict unchanged.
- Targeted build green:
  `lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
  reports only the documented `declaration uses sorry` warning at
  line 87:8 (theorem header — the two physical sorries at lines 884
  and 958 are tracked by separate sub-issues #2964-followup and
  #2955).

## Current frontier

Audit complete; PR being opened. No code changes in this PR — review
artifact only.

## Overall project progress

Theorem 2.1.2's forward direction remains end-to-end derivable
modulo:
- `non_adjacent_branches_leaf_case_per_kQ` body residuals tracked by
  #2955 (Case E + asymmetric short chain + long-chain all-leaves)
  and a #2964 follow-up (Case C all-leaves D̃₇ residual at
  `chain.length = 4`, awaiting `d7tilde_not_finite_type_per_kQ`
  proof body — PR #2968 landed the stub).
- `etilde6_not_finite_type_per_kQ` body
- a few transitive `sorry`s in `FieldGenericETilde6 / ETilde7` rep
  indecomposability.

Audit landscape (latest):
- PR #2943 outer assembly audited (#2944, PASS on D1/D2/D4/D5, FAIL
  on D3 with same-PR fix landed).
- PR #2933 stub + #2941 signature delta audited (this audit, PASS).
- PRs #2945 + #2947 (Ẽ₆/Ẽ₇ embedder bodies) audit still unclaimed
  as #2949.

## Next step

- Worker on #2949 (audit of `embed_etilde6_in_tree_per_kQ` and
  `embed_etilde7_in_tree_per_kQ` bodies — PRs #2945 + #2947).
- Worker on #2955 (Case E + uncovered all-leaves) — flagged as
  highest-uncertainty remaining piece by both the prior progress
  file and #2955's own scope notes.
- Worker on #2967 (fill `d7tildeRep_kQ_isIndecomposable` body) —
  large; precedent (#2834) still in flight.
- A docstring-refresh PR for
  `FieldGenericNonAdjacentBranches.lean:28-30` would help readers
  but is not blocking.

## Blockers

None for this audit. The two concerns flagged in the report are
non-blocking style/staleness issues; the audit verdict is PASS.
