## Accomplished
- Proved `principalSeries_decomp`: V(μ,μ) ≅ ℂ_μ ⊞ W_μ in FDRep
- Defined `scaledAugMorphism` (scaled augmentation V → ℂ_μ as FDRep morphism)
- Defined `complementWProjection` (projection V → W_μ as FDRep morphism)
- Proved `emb_comp_scaledAug_eq_id` (augmentation retracts embedding)
- Proved `total_condition` (sAug ≫ emb + proj ≫ incl = 𝟙)
- Proved `emb_comp_proj_eq_zero` (projection kills embedding image)
- Proved `incl_comp_proj_eq_id` (projection retracts inclusion)
- Created PR #1674 with auto-merge enabled

## Current frontier
- `complementWProjection.comm` has one sorry (equivariance through opaque FGModuleCat/ModuleCat layers — the `show` tactic fails because Lean's normal form after ext differs from the expected mathematical form)
- `complementW_simple` (W_μ is irreducible) not yet proved

## Overall project progress
- Theorem 5.25.2 part 1 (V irreducible when χ₁≠χ₂): structure complete, helper lemmas sorry'd
- Theorem 5.25.2 part 2 (V = ℂ_μ ⊕ W_μ): decomposition proved, W_μ simplicity not proved
- Theorem 5.25.2 part 3 (isomorphism classification): sorry'd

## Next step
- Fix `complementWProjection.comm` sorry — the main blocker is that after `FGModuleCat.hom_ext; ext; Subtype.ext; funext`, the goal has opaque `ModuleCat.Hom.hom ((A ≫ B).hom)` wrappers that `show`/`change` can't match. Try `Action.comp_hom` + `simp` approach or restructure the definition.
- Prove `complementW_simple` — needs either character inner product ⟨χ_V, χ_V⟩ = 2 calculation or direct subrepresentation argument using `complementW_eval_injective` and `complementW_eval_surjective`.

## Blockers
- FGModuleCat/ModuleCat layer opacity: The `Action.Hom.ext → FGModuleCat.hom_ext → ext` chain produces goals with deep `ModuleCat.Hom.hom` wrapping that `show`/`change` cannot match. Solved for standalone lemmas using `Action.Hom.ext; simp [Action.comp_hom, Action.zero_hom]; FGModuleCat.hom_ext; ext; show`, but this doesn't work inside `where` clause `comm` proofs where we start at the FGModuleCat level.
