## Accomplished

Issue #4651 (D̃₈ corrected homogeneous-tube), **sub-A landed** — the corrected
construction. `EtingofRepresentationTheory/Chapter6/FieldGenericD8Tilde.lean`:

- Swapped the `{2, 3}` central edge of `d8tildeRepMap_kQ` from the refuted
  rank-deficient bridge `d5tildeGamma_F` / `d5tildeGammaInv_F` to the corrected
  eigenvalue-site tube `d5tildeGammaTube_F F lam m` / `d5tildeGammaTubeInv_F F lam m`
  (reusing the D̃₅ primitives, already imported). Threaded a `lam` parameter through
  `d8tildeRepMap_kQ`; the rep `d8tildeRep_kQ` instantiates it at the generic
  eigenvalue `d5tildeTubeLam F`.
- Propagated `[IsAlgClosed F]` (needed for `d5tildeTubeLam`) to `d8tildeRep_kQ` and
  all rep-mentioning helpers (`d8tildeRep_kQ_dimVec`, `d8tilde_core_F`,
  `d8tilde_core6_F`, the four `_proj` siblings, `d8tildeRep_kQ_chain_collapse`,
  `d8tilde_gamma_containment_F`). The dimension vector is unchanged, so
  `d8tildeRep_kQ_dimVec` keeps its statement.
- With the correction, `d8tildeRep_kQ_isIndecomposable` is now a `sorry` of a
  statement that is **true for every orientation** (the mixed/reversed-leaf
  under-coupling of #4566 / #4597 is gone), rather than a sorry of a false one.
  Docstrings updated to reflect the corrected route.
- `d8tilde_gamma_containment_F` is now disconnected from the rep's central map (it
  is hypothesised against the legacy `d5tildeGamma_F`); kept compiling as the worked
  legacy template, with a docstring note that sub-C must re-derive a
  `d5tildeGammaTube_F`-based containment.

Verification: `lake build` of `FieldGenericD8Tilde` is clean (only the one deferred
`isIndecomposable` sorry); downstream consumers `FieldGenericNonAdjacentBranches`
and `FieldGenericDTilde` both build.

## Current frontier

Sub-A (corrected construction + re-point, file builds) done. Remaining: the
`isIndecomposable` assembly for all orientations (deliverable 2) and the
re-derived `d5tildeGammaTube_F`-based containment.

## Overall project progress

Stage 3 (formalization), Chapter 6 infinite-type frontier. D̃ corrected-tube series:
D̃₄ (#4648/#4674), D̃₅ pattern-setter (#4647; assembly #4663 still open), D̃₆ (#4649),
D̃₇ (#4650) in flight. This lands D̃₈ sub-A.

## Next step

Sub-C follow-up issue (created this turn): close `d8tildeRep_kQ_isIndecomposable`
for all orientations by generalising the D̃₅ assembly `d5tildeRep_kQ_isIndecomposable`
(#4663) across the length-4 internal chain via `d8tildeRep_kQ_chain_collapse`. It
depends on the D̃₅ assembly #4663 landing first (the pattern to generalise).

## Blockers

The D̃₈ `isIndecomposable` assembly cannot be cleanly written until the D̃₅ assembly
(#4663, currently blocked on #4662) lands — it must generalise a worked D̃₅ proof
rather than invent the two-center pattern from scratch. Tracked by the sub-C issue.
