## Accomplished

Proved `IrrepDecomp.n_eq_card_conjClasses'` in `Corollary4_2_2.lean`, eliminating all sorry's from the file. The proof establishes that the number of Wedderburn-Artin blocks equals `Fintype.card (ConjClasses G)` via the center dimension argument:

1. `center_coeff_conj_invariant` / `mem_center_of_conj_invariant`: Characterize center of `MonoidAlgebra k G` as elements with conjugation-invariant coefficients
2. `finrank_center_monoidAlgebra`: dim center(k[G]) = |ConjClasses G| via linear equivalence with `ConjClasses G → k`
3. `finrank_center_pi_matrix`: dim center(∏ Mat(dᵢ,k)) = n via `Algebra.IsCentral` showing each block's center is scalar
4. `AlgEquiv.centerLinearEquiv`: The Wedderburn isomorphism preserves center finrank
5. Combined via `linarith` to get n = |ConjClasses G|

Key Mathlib APIs used: `MonoidAlgebra.single_mul_apply`/`mul_single_apply`, `Algebra.IsCentral.center_eq_bot`, `Algebra.mem_bot`, `ConjClasses.mk_eq_mk_iff_isConj`.

## Current frontier

`Corollary4_2_2.lean` is now sorry-free. The main theorem `Etingof.Corollary4_2_2` is fully proven.

## Overall project progress

Stage 3.2 proof work continues. Corollary 4.2.2 (number of irreducible representations = number of conjugacy classes) is now complete. The infrastructure file `IrreducibleEnumeration.lean` still has 2 sorry's (lines 426, 543) which are pre-existing.

## Next step

Look for other sorry'd theorems that depend on `n_eq_card_conjClasses` (e.g., `Theorem4_10_2.lean` line 162-165 has its own sorry'd copy). Consider proving those or finding other Stage 3.2 proof targets.

## Blockers

None.
