## Accomplished

Continued the Mathlib `v4.28.1 → v4.31.0` pin bump for #2841 (+ #2564), this time
on a base **rebased onto current `main`** rather than the stale `agent/7add64ef`
(which was 39 commits / 71 files behind and would have conflicted + missed the
API-drift in code merged since 2026-06-21).

New working branch: **`agent/64f2e40f-bump`** (pushed). It contains:

- The pin bump itself (cherry-picked from `agent/7add64ef`'s commit): `lakefile.toml`
  rev → v4.31.0, `lean-toolchain` → `leanprover/lean4:v4.31.0`, regenerated
  `lake-manifest.json`. `lake exe cache get` succeeds; all of Mathlib builds from cache.
- Local-copy cleanups, refreshed for `main`'s layout:
  - `Theorem5_22_1.lean`: removed `isIdempotentElem_eq_zero_of_trace_eq_zero`, call
    site forwards to upstream `IsIdempotentElem.eq_zero_of_trace_eq_zero` (PR #39523).
  - `EvalEqOnGL.lean` (the file `eq_of_eval_eq_on_gl` was *relocated* to on `main`):
    removed the local `eq_of_eval_eq_on_gl`, kept the newer density primitive
    `eq_zero_of_eval_eq_zero_off_zeroLocus`. Call sites in `PolynomialRepEmbedding`
    and `DetLocalization` resolve to upstream `MvPolynomial.eq_of_eval_eq_on_gl` (PR #38583).
- **Mechanical API-drift buckets fixed project-wide** (benefit all downstream modules):
  - Removed `pow_eq_zero` (gone upstream) → private `pow_eq_zero` helper via
    `sq_eq_zero_iff` in `DynkinTypes.lean` (all 21 uses are the `^2` case).
  - `Equiv.Perm.apply_inv_self` / `inv_apply_self` (removed) →
    `Equiv.Perm.coe_inv, Equiv.{apply_symm_apply,symm_apply_apply}` across 6 files.
  - Deprecated `{Finsupp,AddSubmonoidClass}.coe_finset_sum` → `coe_finsetSum`.
- **5 modules fixed to green** this session: `Chapter4/Corollary4_2_4`,
  `Chapter4/Proposition4_1_2`, `Chapter5/Lemma5_18_3`, `Chapter5/Definition5_4_1`,
  `Chapter5/PowerSumCauchyIdentity`.

Error count went from ~150 (29 modules) to **119 (26 modules)**. Note: fixing
frontier modules surfaced two *new* downstream failures (`Theorem5_18_2`,
`Theorem5_25_2`) that the original inventory could not see — the real total grows
as upper layers compile.

## Current frontier

Verified in an isolated `/tmp/etingof-bump` clone (the pod worktree shares
`.lake/packages/mathlib` by symlink — must not bump there). Branch
`agent/64f2e40f-bump` pushed at each green module.

## Overall project progress

Pin bump + cleanups are settled and rebased onto current `main`. The residual is
project-wide API-drift repair (atomic: cannot merge until every module is green,
so it cannot be split across auto-merging per-issue PRs). This is a multi-session
coordinated effort on the single branch `agent/64f2e40f-bump`.

## Next step

Continue on `agent/64f2e40f-bump` (NOT `agent/7add64ef`). Remaining failing modules
(error count, from `lake build` after this session's fixes):

```
22 Chapter6/DynkinTypes        (split_ifs no-ite, dsimp/linarith/omega drift — hardest)
10 Chapter6/Problem6_9_1
 9 Chapter5/Theorem5_25_2
 7 Chapter5/Lemma5_13_1        (3 perm-rename errors already fixed; 7 simp/group/rewrite remain)
 6 Chapter9/Theorem9_6_4       (unusedSimpArgs linter is now an error — drop unused simp args)
 6 Chapter5/GL2CharacterValues (incl. whnf heartbeat timeouts)
 4 Chapter2/Sl2Irrep, Chapter3/Theorem3_10_2, Chapter5/FRTHelpers,
   Chapter5/Proposition5_21_1, Chapter5/Theorem5_27_1, Chapter5/Theorem5_4_4,
   Chapter6/Example6_3_1, Chapter9/ShapiroLemma, Infrastructure/BasicAlgebraExistence
 3 Chapter5/Theorem5_18_2, Chapter7/Example7_6_3, Infrastructure/ColumnRepSimple,
   Infrastructure/RegularCharacter
 2 Chapter3/Theorem3_8_1, Chapter5/Corollary5_12_4, Chapter6/Definition6_6_3,
   Chapter9/KoszulHelpers
 1 Chapter4/Lemma4_10_3, Chapter5/Theorem5_10_1, Chapter9/Theorem9_2_1
```

Recurring root causes for the residual (mostly per-site simp-normal-form drift):
- `split_ifs` "no if-then-else conditions" — preceding `simp only` now fully resolves
  the ite; wrap as `(try split_ifs)` or drop it (DynkinTypes, RegularCharacter).
- `unusedSimpArgs` linter is now a hard error — remove the flagged unused simp lemmas.
- `dsimp`/`simp` "made no progress" — fold the lemma into the next combinator or drop.
- `Corollary5_12_4`: `MonoidAlgebra.mapRangeRingHom_single` simp form changed (the
  `φ a_int = RowSymmetrizer` step). `Theorem5_10_1`: `Action.res` API change.
- A useful pattern found for MonoidAlgebra coefficient drift (see `Proposition4_1_2`):
  cast the sum to `Finsupp.single` (defeq), then `Finsupp.finsetSum_apply` + `simp`.

## Blockers

- Atomic migration, multi-session by nature. Cannot land as a partial auto-merge PR
  (would be red until 100% green). Keep accumulating fixes on `agent/64f2e40f-bump`;
  open the single landing PR only once `lake build` is project-wide green.
- The issue body still points successors at the stale `agent/7add64ef`; it should be
  updated to `agent/64f2e40f-bump`.
</content>
