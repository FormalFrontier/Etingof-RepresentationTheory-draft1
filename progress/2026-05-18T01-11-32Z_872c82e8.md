## Accomplished

- Claimed #2839 (orientation-generic D̃₅ indecomposability — main proof body).
- Discovered main was broken: PR #2843 landed Section 5b/5d helper proofs in
  `Chapter6/FieldGenericD5Tilde.lean` that no longer compile after #2846's
  K_{1,4} projection dedup. The Section 5b `starProj1_F_starEmbed*_F` /
  `starProj2_F_starEmbed*_F` lemmas duplicated existing
  `FieldGenericStar.lean` theorems and assumed `starProj1_F = starFirst_F`
  (which post-#2846 is `starFirst_F - starSecond_F`). Section 5d's
  `gammaInv_embed*_F` proofs depended on Section 5b and broke similarly.
- Repaired main by:
  - De-privatizing the four `starFirst_F_starEmbed*_F` /
    `starSecond_F_starEmbed*_F` helpers in `FieldGenericStar.lean`.
  - Deleting the broken Section 5b duplicates in `FieldGenericD5Tilde.lean`.
  - Updating Section 5d `hP1` / `hP2` to use the correct
    `starFirst_F` / `starSecond_F` names directly.
  - Build now passes (`lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde`).
- Analyzed the proof complexity for `d5tildeRep_kQ_isIndecomposable`.
  Discovered that #2839's strategy ("per-leaf direction-aware `core_F`")
  does not work in mixed sub-cases: the canonical `core` argument requires
  BOTH leaf-center invariances (to push `embed_l1(a)` and `embed_l2(c)` into
  `W(center)` for the `embed_sum_zero`-based contradiction), and in mixed
  cases (one leaf canonical, the other reversed) only one push direction is
  available. The conclusion `x ∈ W₁(leaf1)` is NOT derivable from
  leaf-only invariances — the central γ edge's invariance is needed to
  bridge the gap.
- Decomposed #2839 into two sub-issues:
  - **#2850 (Sub A)**: derive leaf subspace equalities
    `W₁(0) = W₁(1) = W₁(4) = W₁(5)` (and same for `W₂`) for any
    orientation `Q`. ~600-900 lines, 32 sub-cases.
  - **#2851 (Sub B)**: assemble indecomposability via N-invariance +
    propagation, using the leaf equalities from #2850.
    Depends on #2850.
- Left machine-readable breadcrumb comment on #2839 documenting the
  decomposition and reasoning.

## Current frontier

- `Chapter6/FieldGenericD5Tilde.lean` builds. The Section 5b/5d helpers
  are now correct.
- `d5tildeRep_kQ_isIndecomposable` still has its sorry — the proof body
  is deferred to #2850 + #2851.
- Partial PR for the broken-main fix is the deliverable of this session.

## Overall project progress

Chapter 6 D̃₅ orientation-generic chain (#2804):
- Construction `d5tildeRep_kQ` + dimension vector: done (#2802).
- F-generic helper lemmas (`embed_sum_zero_F`, `center_decomp_F`,
  `gamma_from_embed1_F`, `gamma_from_embed2_F`): done.
- Closed-form γ⁻¹ + identity lemmas (`gammaInv_embed*_F`): done (#2843).
- Section 5b/5d broken-main fix: this PR.
- Indecomposability proof body: now decomposed into #2850, #2851.
- Per-(F, Q) infinite-type theorem `d5tilde_not_finite_type_per_kQ`:
  in place, depends transitively on the sorry.

## Next step

Two parallel critical-path workstreams:

1. **Sub-A worker (#2850)**: derive the leaf subspace equalities for
   arbitrary orientation `Q`. This is the larger of the two sub-issues
   (600-900 lines). The worker should use `compl_le_forces_eq` at each
   edge and chain through the central γ edge for the mixed cases.

2. **Sub-B worker (#2851)**: blocked on #2850. Once Sub-A merges, this
   worker derives N-invariance from the leaf equalities, applies
   `nilpotent_invariant_compl_trivial_gen`, and propagates `W(0) = ⊥`
   to all vertices via `center_decomp_F`. Medium-to-large
   (300-500 lines).

The broken-main fix PR (this session) is independent of both and
unblocks main now.

## Blockers

None. The fix PR is ready; sub-issues are filed with full strategy
notes.
