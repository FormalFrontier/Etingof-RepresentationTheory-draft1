## Accomplished

- Proved `coxeterAction_fixed_zero`: the core lemma that if the Coxeter element fixes a vector, then the vector is zero. This is the key mathematical insight (telescoping argument) of Lemma 6.7.2.
- Proved `intermediateState_eq_drop_foldr` and `intermediateState_full`: connecting the recursive `intermediateState` definition to the `List.foldr`-based `coxeterAction`.
- Proved `intermediateState_coord_stable`: coordinates not affected by intermediate reflections remain stable.
- Proved `intermediateState_coord_low`: coordinates below the current reflection index are untouched.
- Proved all B-preservation lemmas: `foldr_preserves_B`, `coxeterAction_preserves_B`, `coxeterActionIter_preserves_B`.
- Set up the full proof structure for `Lemma6_7_2` with the by-contradiction argument.

## Current frontier

Three sorry's remain in `Lemma6_7_2.lean`:
1. `nonneg_coord_sq_le_two_B` (line ~297): coordinate bound `vᵢ² ≤ 2·B(v,v)` for nonneg vectors. Needs either degree bound (max degree ≤ 3 for Dynkin) or eigenvalue analysis of the Cartan matrix.
2. `hperiod` (inside main theorem): pigeonhole argument showing the orbit of β under the Coxeter element is eventually periodic. Depends on #1 for finiteness of the level set.
3. `hS_fixed` (inside main theorem): linearity of `coxeterAction` over `Finset.sum` plus index shifting using `c^M β = β`.

## Overall project progress

Stage 3.2 Chapter 6 formalization. Lemma 6.7.2 is ~80% complete (core mathematical content proved, infrastructure sorry's remain).

## Next step

Fill the remaining 3 sorry's:
- For `nonneg_coord_sq_le_two_B`: consider proving `B(v,v) ≥ ½Σvᵢ²` using degree bound ≤ 3 for Dynkin diagrams, or strengthen `IsDynkinDiagram` to include this.
- For `hS_fixed`: prove `simpleReflection` is a linear map (it is: `v ↦ v - (v·w)·α`), then composition and foldr preserve linearity, giving `coxeterAction (Σ fₖ) = Σ coxeterAction(fₖ)`.
- For `hperiod`: once coordinate bound is established, use pigeonhole on the finite set `{v ∈ ℤⁿ : 0 ≤ vᵢ, vᵢ ≤ √(2K)}`.

## Blockers

None critical. The remaining sorry's are tractable but require significant formalization effort (linearity infrastructure, coordinate bounds).
