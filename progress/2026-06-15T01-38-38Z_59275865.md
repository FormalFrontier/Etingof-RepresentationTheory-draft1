## Accomplished

Claimed #4745 (Ch5 #2483 sub-B residual: prove
`iso_of_formalCharacter_eq_schurPoly` sorry-free) and, after investigating the
codebase, **decomposed it** into two well-scoped sub-issues. No code change —
this was a scope-assessment / decomposition turn (a sanctioned success path:
the issue body itself said "a worker either proves these here or files them as
explicit depends-on dependencies").

Findings driving the decomposition:
- `iso_of_formalCharacter_eq_schurPoly`
  (`Chapter5/SchurWeylFormalCharacterIso.lean:94`) is a single `sorry` at the
  top of the Schur-Weyl #6 stack, routing through `decompose_polynomial_gl_rep`.
- **Gap 1 (algebraicity):** audited the whole tree — **no
  `IsAlgebraicRepresentation` proof exists for any concrete rep**. The det-twist
  algebraicity the consumer must supply is genuinely new infrastructure.
- **Gap 3 (classification exposure):** `decompose_polynomial_gl_rep`
  (`PolynomialGLDecomposition.lean:267`) hides the `(e, he, hLsimp, hLdist)`
  data that `glTensorRep_schurWeyl_simples_formalCharacter_linearIndependent`
  consumes, and it rests on `polynomial_homog_rep_asModule_embeds_directSum_simple`
  (`:237`, still `sorry`, = #4716). Exposing the schurPoly-classification is
  wiring that depends on #4716 + #4731 (hLdist) + #4732 (classification crux).
- **Gap 2 (h_span) + final assembly:** mechanical once gaps 1/3 exist — the
  consumer (`Proposition5_22_2.lean:387`) already proves `h₁_top` = `h_span`.

Created:
- **#4756** sub-A: det-twist algebraicity lemma
  `IsAlgebraicRepresentation N (detTwistedSchurModuleRep …)` (independent, can
  start immediately).
- **#4758** sub-B: expose schurPoly-classification of abstract simples through
  `decompose_polynomial_gl_rep` (blocked — depends-on #4716, #4731, #4732).

Wired `#4745 depends-on #4756, #4758`, left a `Decomposed into …` breadcrumb,
and `coordination skip`'d #4745 (now `replan` for planner triage to narrow to
the final-assembly residual).

## Current frontier

#4745 reduced to its final-assembly residual (signature gains `halg` + `h_span`;
consumer supplies them; then the module-docstring route closes). It is blocked
on the two new sub-issues.

## Overall project progress

Stage 3 formalization ongoing. Schur-Weyl #6 (`iso_of_formalCharacter_eq_schurPoly`,
parent #2483) is the live Ch5 frontier; its remaining work is now an accurate
3-deep sub-tree (#4756 independent; #4758 → #4716/#4731/#4732). Ch6 affine/sporadic
indecomposability tubes remain the other active frontier.

## Next step

A worker should claim **#4756** (det-twist algebraicity) — it is independent and
unblocks the #4745 residual. #4758 stays blocked until #4716/#4731/#4732 land.

## Blockers

None for this turn (decomposition completed cleanly). #4758 is dependency-blocked
by design.
