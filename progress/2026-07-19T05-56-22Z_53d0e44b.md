## Accomplished
- Merged ready PR #6984 (icosahedral crux reframe) to unblock this work — it introduced
  `so3_icosahedral_G_simple`, `faithful_perm5_of_simple_index_five`,
  `simpleGroup_card60_exists_index_five` on `main`.
- **Discharged the geometric (SO(3)) content of `so3_icosahedral_G_simple`** (#6983) in
  `EtingofRepresentationTheory/Chapter4/Problem4_12_8.lean`: from an order-5 pole `b` (12-element
  orbit) produced an off-axis orbit pole `g·b`; the stabilizers of `b` and `g·b` are two distinct
  Sylow-5 subgroups (order 5, index 12 coprime to 5, built via `IsPGroup.toSylow`), giving
  `Nontrivial (Sylow 5 ↥G)`. `so3_icosahedral_G_simple` is now **sorry-free in its own body**.
- Isolated the SO(3)-free half as `isSimpleGroup_of_card_sixty_of_nontrivial_sylow5`
  (order 60 + n₅>1 ⇒ simple), sorried, with a full proof route in its docstring.
- Created sub-issue #6991 for that pure-group-theory lemma; left a `Decomposed into #6991`
  breadcrumb on #6983.
- File builds clean: `lake build EtingofRepresentationTheory.Chapter4.Problem4_12_8` succeeds.

## Current frontier
Icosahedral disjunct: `so3_icosahedral_of_poleData` still depends on two sorried lemmas —
`simpleGroup_card60_exists_index_five` (#6982, in flight) and
`isSimpleGroup_of_card_sixty_of_nontrivial_sylow5` (#6991, newly split out). Both are pure
finite group theory with no SO(3) content.

## Overall project progress
Stage 3 formalization ongoing. Ch4 SO(3) classification: cyclic/dihedral/tetrahedral done;
octahedral has one sorry (`exists_octahedral_faithful_hom`, #6972 sub-issues #6987/#6988);
icosahedral reduced to the two group-theory lemmas above. Sorry count in Problem4_12_8.lean
unchanged at 3, but one SO(3)-flavoured sorry converted to a clean, delegatable group-theory sorry
and the geometric reduction verified.

## Next step
Claim #6991 and prove `isSimpleGroup_of_card_sixty_of_nontrivial_sylow5` (route in the docstring:
n₅=6 ⇒ 24 order-5 elements; order-30 normal-Sylow lemma; divisor case analysis). Also #6982 for
the index-5 subgroup. Once both land, `so3_icosahedral_exists_faithful_perm5` /
`so3_icosahedral_of_poleData` become fully sorry-free.

## Blockers
None. #6991 is well-specified and self-contained (no SO(3) dependency).
