## Accomplished
- Bumped toolchain + Mathlib to **v4.30.0-rc2**. Full build green (0 errors, 8659 jobs).
- v4.30.0-rc2 Mathlib API/behavior changes migrated:
  - `pow_eq_zero` (general form) removed; added a local `pow_eq_zero` helper via `pow_eq_zero_iff` (DynkinTypes, 21 call sites unchanged).
  - `List.Sorted` / `Multiset.sort_sorted` removed; `Multiset.sort` now exposes `Pairwise`. `.Sorted (· ≥ ·)` → `.Pairwise (· ≥ ·)`, `Multiset.sort_sorted _ _` → `Multiset.pairwise_sort _ _` (Lemma5_13_2, Theorem5_15_1).
  - `DecidablePred` synthesis for `Finset.filter (· ^ p^n = ·)` loops in whnf; forced `Classical.decPred` explicitly (GL2CharacterValues).
  - `complementWProjection` (FDRep morphism) timed out: structurally decomposed into bare-ambient map + membership lemma + codRestrict + comm, eliminating the whnf/isDefEq blowup (Theorem5_25_2) — even removed the old 4M-heartbeat override.
  - `Module.Free` typeclass synthesis on Schur/weight-space submodules slowed past the 20000 default; raised `synthInstance.maxHeartbeats` to 80000 on three decls (Theorem5_22_1 ×2, Proposition5_22_2).

## Current frontier
v4.30.0-rc2 PR being opened with auto-merge. Next: v4.30.0 (final).

## Overall project progress
Sweep: v4.28.1 → v4.30.0-rc1 merged (11 versions). v4.30.0-rc2 green and merging. Remaining: v4.30.0, v4.31.0-rc1, v4.31.0-rc2, v4.31.0, v4.32.0-rc1.

## Next step
After v4.30.0-rc2 merges, run bumpstep for v4.30.0.

## Blockers
None.
