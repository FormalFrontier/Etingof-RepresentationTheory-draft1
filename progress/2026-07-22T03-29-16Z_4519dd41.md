## Accomplished

Resolved fidelity gap #7231 for `Chapter5/Problem5.16.3(b)` — exposed the scalar value.

- Added headline theorem `sumTranspositionsWith1_scalar_on_rectangular` to
  `EtingofRepresentationTheory/Chapter5/Problem5_16_3.lean`: for rectangular
  `λ` (`la.parts = Multiset.replicate r c`), `E = (12)+⋯+(1n)` acts on `V_λ` by
  the scalar `(c − r)` (= content of the unique removable corner `(r−1, c−1)`).
  This renders "compute this scalar" from the book's part (b) as a checkable result.
- Refactored `sumTranspositionsStab_acts_scalar_iff_content_const` (an
  `exists_congr`) by extracting the per-scalar iff as
  `sumTranspositionsStab_acts_scalar_iff_content_const_of`, then defining the
  original existential lemma as `exists_congr` of it. The `exists_congr`
  packaging severs the witness link under `obtain`, so the pointwise form is
  what lets the new theorem read off the concrete stab scalar. Original lemma
  keeps its exact signature (existing call site unchanged).
- Flipped `Chapter5/Problem5.16.3` `fidelity: gap → verified` in
  `progress/items.json` and dropped `fidelity_issue`.

Proof route: on `V_λ`, `C_{n-1} = sumTranspositionsStab` acts by
`content λ − (c − r)` (every `ν ∈ removeSquare λ` has that content, since the
only removable corner of a rectangle is `(r−1,c−1)`); `C_n` acts by `content λ`
(`sumTranspositions_mul_eq_content_smul`); `E = C_n − C_{n-1}` gives `c − r`.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5.Problem5_16_3` and
`...Chapter5` succeed. New theorem axiom-clean
(`[propext, Classical.choice, Quot.sound]`, no `sorryAx`).

## Overall project progress

Stage 3.7 fidelity audits ongoing. This closes one `gap` back to `verified`,
mirroring the #7204/#7211 "expose the proved-inside value" pattern.

## Next step

Remaining unclaimed queue: #7225 (dihedral classification feature), #7243/#7244
(fidelity audits), #7245 (items.json hygiene). Pick per `/work` judgment.

## Blockers

None.
