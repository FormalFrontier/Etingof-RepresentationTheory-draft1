## Accomplished
Statement pass for the six not-yet-formalized Chapter 6 exercises in issue #5900.
Wrote faithful Lean 4 statements (with `sorry` proofs, per the statement-pass
phase — no proving), one new file per exercise, all added to the `Chapter6.lean`
aggregator; the chapter builds cleanly with only the expected `sorry` warnings.

- **Problem 6.1.3** (`Problem6_1_3.lean`): the Cartan-type matrix `A = 2·Id - R`
  (`cartanMatrix`, matching `IsDynkinDiagram`'s form), its diagonal, and its
  symmetry.
- **Problem 6.1.3 continued (a)-(d)** (`Problem6_1_3_continued_E7_E8.lean`):
  `det A = n+1` (Aₙ), `4` (Dₙ), `3/2/1` (E₆/E₇/E₈); `IsDynkinDiagram` for each;
  the cycle Cartan matrix kills the all-ones vector so `det = 0` (a Dynkin
  diagram is a tree); degree bounds (≤ 3, at most one branch vertex). Reuses
  `Etingof.DynkinType.adj`.
- **Problem 6.1.3 continued (e)-(g)** (`Problem6_1_3_continued_tildeE.lean`):
  `IsAffineDynkinDiagram`, an `AffineType` enumeration (Ãₙ = cycle, D̃ₙ, Ẽ₆/Ẽ₇/Ẽ₈)
  with adjacency and `marks`, `marks`-in-kernel / `det A = 0` (e), the Dynkin
  classification pointer (f), and the affine Dynkin classification (g).
- **Problem 6.1.6** (`Problem6_1_6.lean`): the tautological 2-dim SU(2)
  representation `V` (**genuinely constructed** — `map_one'`/`map_mul'` proved
  via `toLin'`), multiplicity `rᵢⱼ = dim Hom(Wᵢ, V ⊗ Wⱼ)`, symmetry (a),
  McKay-graph connectivity (b), affine-Dynkin / PSD-not-definite (c), dims =
  marks (e), and the McKay correspondence (d) as a `Prop`-valued statement.
  Reuses the affine-Dynkin infrastructure from the tildeE file.
- **Problem 6.9.2** (`Problem6_9_2.lean`): the E₈ lattice in `½ℤ⁸`, the simple
  roots `αᵢ` as a `ℤ`-basis (a), the Gram matrix = E₈ Cartan / graph-iso to
  `DynkinType.E8.adj` (b), the E₆/E₇ sublattices (c), root counts 240/126/72 (d).
- **Problem 6.9.3** (`Problem6_9_3.lean`): `simpleRep` Sᵢ, source/sink, `Ext¹`
  via the two-term-complex differential (`extDiff`, `Ext1Vanishes = surjective`),
  Ext-vanishing at sources/sinks (a), Jordan-Hölder data as a `Prop`-def (b).

`progress/items.json` updated: all six ids set to `statement_formalized` with new
coverage notes.

## Current frontier
All six Chapter 6 exercises from #5900 are `statement_formalized`. The statements
are faithful and non-vacuous (definitions constructed, no `True` placeholders; the
two genuinely infrastructure-blocked parts — 6.9.3(b) Jordan-Hölder and 6.1.6(d)
McKay correspondence — are pinned as `Prop`-valued defs, not vacuous theorems).

## Overall project progress
Phase 3 statement pass. The Chapter 6 exercise backlog listed in #5900 is now
complete (6/6 `statement_formalized`). Proofs remain (`sorry`) for a later phase.

## Next step
A proof pass on the Chapter 6 exercise `sorry`s. The most tractable first targets
are the concrete determinant computations in `Problem6_1_3_continued_E7_E8.lean`
(`det_cartan_A/D/E6/E7/E8` — `decide`/row-reduction) and the marks-in-kernel
lemmas in the tildeE and McKay files (`cartan_mulVec_marks_eq_zero`,
`mckay_dims_are_marks` — direct `Finset.sum` computations). The affine and McKay
*classification* theorems are the deep, infrastructure-heavy items.

## Blockers
None for the statement pass. Two conjuncts are genuinely blocked on missing
project infrastructure and are recorded as `Prop`-valued statements rather than
vacuous theorems: 6.9.3(b) needs a composition-series notion for
`QuiverRepresentation` (Gabriel-theorem infrastructure), and 6.1.6(d) needs the
finite-subgroups-of-SU(2) classification from Problem 4.12.8.
