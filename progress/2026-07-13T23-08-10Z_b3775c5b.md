## Accomplished

Partial completion of issue #6526 (Ch9 cons-splitting for the standard length-`1` resolution).
Landed a new sorry-free file `EtingofRepresentationTheory/Chapter9/PathAlgebraConsSplittingIso.lean`
carrying the **analytic seed** of the cons-splitting (deliverable 3's core):

- `lengthProj_ofPath_mul_arrowElt_gen`, `lengthProj_mul_arrowInclusion`,
  `lengthProj_mul_arrowInclusion_zero` — right multiplication by an arrow (`arrowInclusion v`,
  degree 1) shifts the length grading by exactly one:
  `lengthProj (n+1) (a * arrowInclusion v) = lengthProj n a * arrowInclusion v` (and no length-`0`
  component). This is the algebraic crux both remaining deliverables rest on.
- `inducedCoordMapGen N` (+ `_tmul`, `_injective`) — the `N`-polymorphic length-graded coordinate
  map on `A ⊗_S N`, generalizing `inducedCoordMap` (the `N = restrictObj M` case) so it also applies
  to the **domain** `A ⊗_S (V ⊗_S M)` of `stdd` (whose `S`-factor `VtensObj M` is not a `restrictObj`).
- `inducedCoordMap_stdd_tmul_succ` / `inducedCoordMap_stdd_tmul_zero` — the degree-`(n+1)` / degree-`0`
  component of `d` on a generator `a ⊗ (v ⊗ m)`, split into the cons-splitting top term
  `(lengthProj n a · v) ⊗ m` and the lower term `−(lengthProj (n+1) a) ⊗ v·m`.

`lake build EtingofRepresentationTheory.Chapter9` succeeds (2692 jobs), no sorries in the new file.

## Current frontier

The two large, distinct pieces of #6526 remain, filed as #6535:
- **2b.** the genuine bundled `S`-bimodule iso `A_n ⊗_S V ≅ A_{n+1}` (needs new `S`-sub-bimodule
  `A_n` + the `ArrowTgt`/`VtensCarrier` diamond machinery + a real inverse from the combinatorial
  core `exists_ofPath_mul_arrowElt` / `ofPath_mul_arrowElt_inj`);
- **3-general.** the `hshift_gen`-style shift for all `ξ` (define the two half-maps `Φ, Ψ` with
  `stdd = Φ − Ψ`, then `TensorProduct.induction_on` onto the per-generator lemma already landed).

#6535 has the full roadmap. It was based on PR #6533 (combinatorial core, `PathAlgebraConsSplitting.lean`),
which was still in CI at branch time — this branch includes those commits and will collapse to just
the new file once #6533 merges.

## Overall project progress

Stage 3 formalization, Ch9 Problem 9.4.6 (i) chain (parent #6420). Length grading (#6510), coordinate
map (#6514), and combinatorial core (#6533) are landed. This turn adds the degree-shift/analytic seed.
Still open toward `standardResolution_shortExact` (#6512): the bundled iso + general shift (#6535).

## Next step

Claim #6535 and build 2b + 3-general per its roadmap, extending `PathAlgebraConsSplittingIso.lean`.
Then #6512 can assemble `Mono (stdd M)` (top-degree via the shift) and middle exactness (telescoping
via the iso inverse). Re-point #6512's `depends-on` from #6526 to #6535.

## Blockers

None. Both remaining pieces are proof/definition-level work with a clear roadmap in #6535.
