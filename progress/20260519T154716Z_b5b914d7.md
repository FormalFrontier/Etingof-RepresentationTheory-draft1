## Accomplished

Audited PR #2979 (Case E.aa + E.s1c4 partial coverage of #2955) for issue
#2980. Five-dimension audit report committed at
`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-cases-e-aa-and-e-s1c4.md`
against `main` commit `ec4696f` and PR #2979 merge commit `2a9398f`.

Verdicts:
- D1 (E.aa Ẽ₆ at w correctness) — **PASS**. All 19 positional arguments to
  `embed_etilde6_in_tree_per_kQ` match the embedder signature; the dispatch
  mirrors Case D exactly with `chain.length = 3` specialisation.
- D2 (E.s1c4 D̃₇ at (v₀, w) correctness) — **PASS**. All 23 positional
  arguments to `embed_d7tilde_in_tree_per_kQ` match; the dispatch is
  structurally identical to the audited Case C.short all-leaves residual.
- D3 (residual sorry documentation + sub-issue breadcrumbs) — **CONCERN**.
  The residual comment block has 3 bullets covering 4 sub-issues
  (chain.length=3 split between #2974 all-leaves and #2976 mixed is
  hidden in a parenthetical aside); sub-issue numbers
  #2974/#2976/#2977/#2978 are not cited in the code; one entry in the
  `let _ := ...` block (`arm₂_ne_chain`) is redundant.
- D4 (build sanity) — **PASS**. Exactly one expected `declaration uses
  sorry` warning at line 88; downstream `FieldGenericAssembly` still
  green.
- D5 (cross-file consistency) — **CONCERN**, driven by stale file
  docstring (lines 27-34, 36-38, 40-52) still claiming "API stub" and
  missing `d7tilde_not_finite_type_per_kQ` /
  `embed_d7tilde_in_tree_per_kQ` from the per-(F, Q) library inventory.
  Issue #2982 already tracks this docstring refresh.

No code changes pushed. Configuration-coverage walk-through of the
six landed cases + four sub-issues confirms the full input
configuration space is partitioned without overlap or orphans.

## Current frontier

Audit landed. The two new sub-cases (E.aa, E.s1c4) are mathematically
correct; the surrounding doc/breadcrumb work is the only quibble and
is already tracked by an open follow-up (#2982).

## Overall project progress

Chapter 6 Phase 2 dispatch for `non_adjacent_branches_leaf_case_per_kQ`
is now reviewed end-to-end up to PR #2979: Phase 1 setup + Cases
A/B/C-main/C.short/D audited by PR #2975, Cases E.aa/E.s1c4 audited
here. Residual is decomposed into four open sub-issues (#2974/#2976/
#2977/#2978), all blocked on #2955.

The Chapter 6 field-generic infinite-type chain continues to progress
on the leaf-case via small, well-scoped PRs each adding one or two
configurations. Each landed PR is followed by a review audit within
~24h.

## Next step

Next worker can pick from any of #2974/#2976/#2977/#2978 (all blocked
on #2955, all unblock once partial PR #2979 merges and triggers
`check-blocked`) or claim the docstring refresh issue #2982 directly
(no dependency, label `feature`). Among the residual sub-issues,
#2976 (Ẽ₇ extension splits at chain.length=3 mixed arms) is the
smallest scope; #2978 (parametric D̃_n at chain.length≥6 all-leaves)
is the largest and may need multi-session decomposition.

## Blockers

External blocker on #2564 (awaiting Mathlib PR merge) unchanged.
