## Accomplished

- Claimed #6398 (`compositionFactors_areLinked`, Problem 9.5.3(ii) Ext-splitting core).
- Found the target theorem does **not** exist on `main` yet — it is stated (with `sorry`)
  only in the unmerged PR #6399 (partial progress on #6378), which also edits
  `Chapter9/Problem9_5_3.lean`. #6399 was MERGEABLE with CI in progress.
- The full proof is a 5-stage homological-algebra dévissage — far beyond one session.
  Decomposed per the strategy in the issue body.
- Landed **step 1** as a genuine reusable lemma in `Chapter9/Definition9_5_1.lean`
  (no conflict with #6399, which only touches `Problem9_5_3.lean`):
  `Etingof.ext_subsingleton_of_not_areLinked` — for simple `S`, `T` with `¬ AreLinked`,
  `Subsingleton (Abelian.Ext S T 1)`. Builds sorry-free.

## Current frontier

Step 1 done. Remaining stages filed as sub-issues:
- #6403 — step 2: finite-length dévissage `ext_subsingleton_of_factors_unlinked`.
- #6404 — step 3: `Ext¹`-vanishing ⟹ SES splits (`B ≅ A ⊞ C`).
- #6405 — steps 4–5: assemble `compositionFactors_areLinked` (blocked on #6403, #6404, and
  #6399 merging to bring the stated theorem to `main`).

## Overall project progress

Phase 3 formalization ongoing across chapters. This turn advanced Ch9 Problem 9.5.3(ii)
block theory by one proof stage and produced a clean sub-issue chain for the rest.

## Next step

Once #6399 merges, workers pick up #6403 then #6404 (independent), then #6405 (capstone).
Steps 2 and 3 are independent of #6399 and can start immediately.

## Blockers

None for the sub-issues #6403/#6404 (both independent of #6399). #6405 is correctly marked
blocked on #6403/#6404; it additionally needs #6399 merged for the stated theorem.
