## Accomplished
Claimed #6406 (corrected replacement for the false Problem 9.4.5(i) statement). Rewrote
`EtingofRepresentationTheory/Chapter9/Problem9_4_5.lean`:
- Restated `cartan_det_eq_pm_one` with the correct §9.3/§9.4.5 hypotheses (deliverable 1,
  complete): `A` finite dimensional over `k`; `M` a complete (`hM_complete`) irredundant
  (`hM_distinct`) family of simple `A`-modules; each `P i` an indecomposable projective
  (`hP_indec`) covering `M i` via the essential-cover identity
  `dim_k Hom_A(Pᵢ,Mⱼ)=δᵢⱼ` (`hP_cover`); finite homological dimension (`hfin`). This mirrors
  the hypothesis pattern of `Etingof.projective_cover_hom_multiplicity` (Prop 9.2.3). The old
  statement was false (counterexample `A=k`, `P 0 = k²` gives `det = 4`).
- Proved the elementary half sorry-free: `det_eq_pm_one_of_mul_eq_one` (an integer matrix
  with an integer right inverse has `det = ±1`, via `Int.eq_one_or_neg_one_of_mul_eq_one`
  and `Matrix.det_mul`).
- Reduced the main proof to a single documented sorry: `∃ D : Matrix ι ι ℤ, C · D = 1`.
- Did NOT modify `algebraCartanMatrix` / `homologicalDimension` defs; did NOT touch the two
  `homologicalDimension … = ⊤` sorries (owned by #6381 / #6382).

`lake build EtingofRepresentationTheory.Chapter9.Problem9_4_5` succeeds (2505 jobs); file has
3 sorries (1 mine, 2 pre-existing).

## Current frontier
Restatement + reduction landed on `agent/336b3b27`. The K₀/Euler-characteristic core remains.

## Overall project progress
Stage 3 (formalization) ongoing across chapters. Problem 9.4.5(i) is now a true-but-partially-
proven statement instead of a false one; parts (ii) still sorry'd under #6381 / #6382.

## Next step
Work #6410: discharge `∃ D, C · D = 1` using composition-multiplicity class vectors (additive
on SES, `c(Mⱼ)=eⱼ`, columns of `C` = `c(Pⱼ)` via Prop 9.2.3) and the Euler characteristic of
finite projective resolutions of the simples (from `homologicalDimension ≠ ⊤` + Krull–Schmidt).
Likely needs further planner decomposition (see #6410 body).

## Blockers
None. #6410 is unblocked (depends only on already-landed infra).
