## Accomplished

Session type: feature (#4887, the #4875 sub-C assembly + seam + spec resolution).

- **Resolved the #4887 spec question (option C-a).** The original
  `schurWeyl_simples_formalCharacter_linearIndependent_complex` was *false* as
  stated (non-algebraic "ghost" simples ⇒ zero / linearly dependent formal
  characters). Added the minimal-friction span hypothesis
  `hLtop : ∀ i, ⨆ μ, glWeightSpace ℂ N (L i) (fun j => μ j) = ⊤` — exactly the
  form the torus-trace connection (B) consumes. Documented the choice in the
  lemma doc.
- **Proved the assembly sorry-free.** `..._linearIndependent_complex` now reduces
  a vanishing ℚ-combination of formal characters to torus-trace vanishing via (B)
  (`trace_combination_eq_zero_of_formalCharacter_combination_eq_zero`), then to
  coefficient vanishing via the isolated seam lemma. No `sorry` in this lemma.
- **Isolated the two genuine content pieces as precisely-typed `sorry`'d lemmas**
  (new in `SchurWeylSimplesClassificationComplex.lean`):
  - `formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero` (the seam) →
    sub-issue #4908: Dedekind/Artin (A) on `MonoidAlgebra ℂ (GL_N)` + torus →
    full-group density bridge.
  - `schurWeyl_simple_summand_glWeightSpace_top` (algebraicity) → sub-issue #4909:
    simple summands of `V^{⊗n}` have spanning weight spaces.
- Threaded `hLtop` at the `..._classification_core_complex` call site via the
  algebraicity helper.
- Added imports `FormalCharacterTorusTrace`, `CharacterIndependence`.

## Current frontier

`SchurWeylSimplesClassificationComplex.lean` builds green. The file's content
sorries went from 1 (the vague `..._linearIndependent_complex` body) to 2
(precisely-typed: the seam #4908 and algebraicity #4909). The assembly connecting
sub-issues (A) and (B) is sorry-free.

## Overall project progress

Stage 3 (formalization) ongoing on Chapter 5 Schur-Weyl. The #4875 decomposition
crux is now: (A) #4885 ✓ merged, (B) #4886 ✓ merged, (C) assembly ✓ this PR, with
the two hard residuals isolated as #4908 (seam / Zariski density) and #4909
(algebraic-summand span). Both need new infrastructure not currently in the
project.

## Next step

Pick up #4909 first (more self-contained: the only genuinely new piece is the
reusable `IsAlgebraicRepresentation ⟹ ⨆ glWeightSpace = ⊤` lemma; the embedding
uses existing `glTensorRep_isAlgebraic` + `IsAlgebraicRepresentation.restrict`).
Then #4908 (the seam), whose density sub-piece may warrant its own further split.

## Blockers

None hard. #4908 and #4909 each require new infrastructure (Zariski density of
diagonalizable matrices; algebraic ⇒ span) but those are normal formalization
work, not external blockers.
