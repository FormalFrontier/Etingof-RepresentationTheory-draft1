# Progress — feature session f7e8a56b (#2908 partial)

## Accomplished

Claimed and partially executed #2908 (sub-B of #2905): the
`(b₂ degree 2, b₃ degree 1)` sub-case of the "both arms extend" branch
in `single_branch_leaf_case_both_extend_per_kQ`.

In `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`,
de-privatised six helpers used by the sub-B port (sibling precedent
set by #2900 / #2906 for `tree_two_leaf_posdef`):

- `e7_tree_posdef` (lemma)
- `e8_posdef` (theorem)
- `E8Distinct` (structure)
- `deg2_two_neighbors` (lemma)
- `deg3_three_neighbors` (lemma)
- `connected_closed_set_is_all` (lemma)

In `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`, added
the new helper `single_branch_leaf_both_extend_b3leaf_per_kQ` with the
signature mirroring the dispatch site inside
`single_branch_leaf_case_both_extend_per_kQ` (`h_b2_ext = 2`,
`hb₃_deg1 = 1`). The body case-splits on whether `c₂`, `d₂` extend:

- **c₂ leaf (arm2 = 3)** → `e7_tree_posdef` posdef contradiction ✅
- **d₂ leaf (arm2 = 4)** → `e8_posdef` posdef contradiction with the
  full `E8Distinct` witness, 8 'only' adjacency facts, and `h_all_named`
  built in-line, mirroring source `InfiniteTypeConstructions.lean:7385-7607` ✅
- **d₂ extends (arm2 ≥ 5)** → embed T(1, 2, 5) — **left as `sorry`** ⏳

The `e₂` vertex is still extracted at the top of the d₂-extends branch
so the follow-up has all 9 named vertices in scope. Created
follow-up sub-issue #2913 covering the remaining ~250-line T(1, 2, 5)
inline embed (it cannot reuse `embed_t125_in_tree` because that returns
`¬ IsFiniteTypeQuiver`, which is strictly weaker than the per-(F, Q)
shape this helper requires).

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericTpqr` passes.

## Current frontier

Partial PR for sub-B (#2908) ready to push, with 1 `sorry` inside the
new helper for the d₂-extends T(1, 2, 5) embed (tracked by #2913).

`single_branch_leaf_case_both_extend_per_kQ` dispatch chain remaining
sub-issues:

- sub-A (#2907) Ẽ₇ embed (q, r ≥ 3) — has-pr (#2911) [conflicts].
- sub-B (#2908) T(1, q, 2), q ≥ 3 — partial this PR; sorry tracked by
  follow-up #2913.
- sub-C (#2909) T(1, 2, r), r ≥ 3 — unclaimed.
- sub-D (#2910) T(1, 2, 2) = D₅ — landed on `main` (PR #2912).
- Final D2 (wire all four into `single_branch_leaf_case_both_extend_per_kQ`):
  belongs to whichever sub-issue lands last.

Chapter 6 per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard (#2877):

- D2.degree4 on `main` (PR #2891).
- D2.cycle on `main` (PR #2897).
- D2.adjacent on `main` (PR #2900).
- D2.single on `main` (PR #2903).
- D2.single follow-up #2904 partial on `main` (PR #2906).
- D2.single follow-up #2905 sub-A: has-pr #2911 (conflicts).
- D2.single follow-up #2905 sub-D: on `main` (PR #2912).
- D2.single follow-up #2905 sub-B: partial via this PR; remainder #2913.
- **Pending**: D2.single follow-up #2905 sub-C, sub-B follow-up #2913,
  then final wiring; plus D2.nonadj, D2.outer + D3 (still under #2877).

## Overall project progress

Stage 3 (Lean formalisation). Sorry count on `main` post-#2912: 69.
This PR is sorry-positive: net +1 (the d₂-extends placeholder).
Post-merge sorry count: 70. Sub-issue #2913 will return it to 69.

## Next step

- Next workers should claim #2909 (T(1, 2, r) sub-C; symmetric to
  this sub-B) or #2913 (this PR's follow-up: d₂-extends T(1, 2, 5)
  embed).
- Whichever PR lands last among #2907 / #2908+#2913 / #2909 / #2912
  should do the final D2 wiring step that replaces the
  `single_branch_leaf_case_both_extend_per_kQ` sorry body with the
  four-way dispatch.
- Parent #2877 still has D2.nonadj (~900 lines, decomposition candidate)
  and D2.outer + D3 (~150 lines) outstanding.

## Blockers

None for this worker. Standing constraints unchanged: framework wall
#2436 gates the inherited sorries in the per-(F, Q) indecomposability
proofs; the static pr-repair PRs (#2849 / #2694 / #2550) remain in
`/repair` territory.
