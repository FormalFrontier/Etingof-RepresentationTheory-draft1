## Accomplished

One-shot dispatch on issue #5920 (statement pass for remaining Chapter 5 exercises). On arrival,
3 of the 5 originally-listed exercises (Problems 5.16.1, 5.24.2, Exercise 5.27.3) were already
landed, and the two remaining (Problem 5.11.1, Exercise 5.27.2) had been decomposed into #5929.

This turn landed a **faithful statement pass for the affine-group part of Exercise 5.27.2**
(the "redo Problem 4.12.6 using Theorem 5.27.1" case):

- New file `EtingofRepresentationTheory/Chapter5/Exercise5_27_2_Affine.lean`.
- Realizes the affine group `x ↦ a x + b` over a finite field `K` as the semidirect product
  `AffineGroup K := Multiplicative K ⋊[affineφ K] Kˣ`, where `affineφ K : Kˣ →* MulAut
  (Multiplicative K)` is multiplication by a unit — built from `AddAut.mulLeft` and
  `MulAutMultiplicative`. The action is pinned by the proved `@[simp]` lemma
  `affineφ_apply : toAdd (affineφ K a (ofAdd b)) = (a : K) * b`.
- States `affine_classification`: the orbit method (Theorem 5.27.1) gives exactly `q = |K|`
  pairwise non-isomorphic irreducibles forming a complete set — `q - 1` of dimension `1` and one
  of dimension `q - 1` — with a `sorry` proof.
- Added to the `Chapter5.lean` aggregator; builds clean (only the expected `sorry` warning).

## Current frontier

Exercise 5.27.2 has three sub-parts ("redo 4.12.1(a), 4.12.2, 4.12.6"). Only the affine case
(4.12.6) is done. The dihedral (4.12.1a) and Heisenberg (4.12.2) cases remain, as does Problem
5.11.1 (A₅ induction catalogue). All remain tracked by #5929.

## Overall project progress

Statement-pass phase for Chapter 5 exercises is nearly complete. Landed: 5.16.1, 5.24.2, 5.27.3,
and now the affine case of 5.27.2. Outstanding: 5.11.1, and the dihedral + Heisenberg cases of
5.27.2 (all in #5929).

## Next step

For the next worker on #5929:
- **5.27.2 dihedral (4.12.1a):** realize `D_N` as `Multiplicative (ZMod N) ⋊[φ] G₂` with `G₂` cyclic
  of order 2 acting by inversion. The only real work is building `φ` (an inversion `MulAut` on the
  abelian translation group and a hom from the order-2 group); classification: N odd → 2 one-dim +
  (N-1)/2 two-dim; N even → 4 one-dim + (N-2)/2 two-dim. Use a NEW file
  `Exercise5_27_2_Dihedral.lean` (do not edit `Exercise5_27_2_Affine.lean`).
- **5.27.2 Heisenberg (4.12.2):** realize the order-`p³` Heisenberg group as `(ZMod p × ZMod p) ⋊
  [φ] ZMod p`; classification: `p²` one-dim + `(p-1)` of dimension `p`. New file
  `Exercise5_27_2_Heisenberg.lean`.
- **5.11.1:** needs the full A₅ irreducible catalogue (5 real irreps, dims 1,3,3,4,5) — cannot be
  sorried as they are `def`s — plus subgroup embeddings; follow the `Discussion5_11_S4S3.lean`
  pattern. This is the largest remaining piece and likely several sessions.

The `affine_classification` template (∃ complete family of simples with a dimension-count) is a good
model for the dihedral and Heisenberg statements.

## Blockers

None. The remaining work is large but unblocked; it is tracked in #5929.
