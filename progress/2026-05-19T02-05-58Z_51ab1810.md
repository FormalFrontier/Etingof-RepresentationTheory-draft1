## Accomplished

Implemented Phase 2 **Case B** of `non_adjacent_branches_leaf_case_per_kQ`
(`Chapter6/FieldGenericNonAdjacentBranches.lean`), closing the
`6 ≤ chain.length ∧ (vertexDegree adj arm₁ = 2 ∨ vertexDegree adj arm₂ = 2)`
branch of the `by_cases` negation. Implementation mirrors Case A but
centres T(1, 2, 5) at `w` instead of `v₀`:

* Built four reversed chain edges `chain[d-2] → chain[d-3] → … → chain[d-6]`
  via `(adj_comm _ _).trans (hchain_edges …)` with `Fin.ext` recasting of
  `(d-k) + 1` indices.
* Derived four chain-index distinctness facts via `hchain_nodup.get_inj_iff`
  + `by simp; omega`.
* Inner `rcases` on the disjunction; each sub-branch extracts `y` (the
  unique non-`w` neighbour of the extending arm) via
  `Finset.card_eq_one` on `Sarm.erase w`, then dispatches to
  `embed_t125_in_tree_per_kQ` with vertex map
  `0→w, 1→arm_other, 2→arm_ext, 3→y, 4..8→chain[d-2..d-6]`.

Build of `EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
succeeds (8046 jobs). No new sorries: only the documented
remaining-cases (C, D, E) sorry persists.

## Current frontier

`non_adjacent_branches_leaf_case_per_kQ` now closes:

* **Case A** (T(1, 2, 5) at `v₀`) — landed by PR #2956.
* **Case B** (T(1, 2, 5) at `w`) — this PR.
* **Cases C, D, E** — still a single `sorry` in the negative branch of
  `by_cases hB`, tracked by #2954 (sub-CD: Ẽ₇ / Ẽ₆ for chain.length ∈
  {4, 5}) and #2955 (sub-ER: Case E + all-leaves residual).

## Overall project progress

Stage 3.x (Chapter 6 per-(F, Q) infinite-type assembly) — the leaf-case
helper is the last remaining proof body for `non_adjacent_branches_infinite_type_per_kQ`,
which is the largest leaf of `not_posdef_infinite_type_per_kQ`. Two of
five sub-cases now closed (A, B); three remain (C, D, E).

## Next step

Pick up #2954 (Cases C+D — Ẽ₇ / Ẽ₆ for `chain.length ∈ {4, 5}`) next.
#2955 (Case E + all-leaves residual) is unblocked by #2954.

## Blockers

None.
