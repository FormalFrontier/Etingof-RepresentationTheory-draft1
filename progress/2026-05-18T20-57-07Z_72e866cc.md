## Accomplished

Closed issue #2913 (`feat(Ch6 #2908 follow-up): d₂-extends T(1, 2, 5) embed
in single_branch_leaf_both_extend_b3leaf_per_kQ`).

- Added shared helper `embed_t125_in_tree_per_kQ` in
  `EtingofRepresentationTheory/Chapter6/FieldGenericT125.lean`: a per-(F, Q)
  port of `embed_t125_in_tree`
  (`InfiniteTypeConstructions.lean:4918-5279`) that takes the same nine
  vertices + adjacencies + structural distinctness facts plus `F`, `Q`,
  `hOrient`, and dispatches to `t125_not_finite_type_per_kQ` via
  `subgraph_infinite_type_transfer_per_kQ` +
  `restrictOrientationViaEmb_isOrientationOf`. Body mirrors the universal
  helper line-for-line; only the final dispatch differs.
- Wired the helper into the d₂-extends branch of
  `single_branch_leaf_both_extend_b3leaf_per_kQ` in
  `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean:596`,
  using the renaming
  `(v₀, u₁, p₁, p₂, q₁, q₂, q₃, q₄, q₅) ↦ (v₀, leaf, a₃, b₃, a₂, b₂, c₂, d₂, e₂)`.
  The branch now extracts `e₂` and calls the helper with named local
  hypotheses (`h_leaf_adj`, `ha₃_adj`, `hb₃_adj`, …, `he₂_ne_c₂`).

Net change: +386 lines in `FieldGenericT125.lean` (the new helper),
+10 / -21 lines in `FieldGenericTpqr.lean` (call site shrinks from a
17-line sorry stub to an 8-line helper invocation).

## Current frontier

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericTpqr` passes
(8045 jobs). The remaining `sorry` count in `FieldGenericTpqr.lean` is
unchanged in raw count because the new shared helper inherits two
existing chains:

* `t125_not_finite_type_per_kQ` itself is still a stub (issue #2793 / #2875
  D1), so the d₂-extends case now propagates that single sorry instead of
  the previous local sorry.
* The d₃-extends case in `single_branch_leaf_both_extend_b2leaf_per_kQ`
  (sibling issue #2915) is unchanged — it can be closed by calling the
  same new helper with the renaming
  `(v₀, u₁, p₁, p₂, q₁, q₂, q₃, q₄, q₅) ↦ (v₀, leaf, a₂, b₂, a₃, b₃, c₃, d₃, e₃)`.

## Overall project progress

Stage 3 (Lean formalization) ongoing. The Ch6 per-(F, Q) wave is
progressing on the leaf-leaf both-extend cases (sub-A done; sub-B and
sub-C had two of three case bodies; sub-B's d₂-extends case is now
closed by this PR; sub-C's d₃-extends case can land via #2915 using the
same helper).

## Next step

A worker should claim #2915 and call `embed_t125_in_tree_per_kQ` with
the symmetric renaming (a₂ ↔ a₃, b₂ ↔ b₃, c₂ → c₃, d₂ → d₃, e₂ → e₃).
That should be a ~15-line edit at
`FieldGenericTpqr.lean` line 977.

## Blockers

None.
