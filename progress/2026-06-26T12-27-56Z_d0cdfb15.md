## Accomplished

One-shot dispatch on **#5376** (fidelity gap: `Chapter4/Example4.8.1`). The issue was
reopened by the owner with a high required bar: each character-table row must be proven
equal to the **trace of an actual irreducible representation**, with `native_decide`
forbidden (it is an unverified trust hole). The existing file proves only orthonormality of
hand-typed rows plus conjugacy-class counts, all via `native_decide` — genuinely vacuous, as
the owner argues (a continuum of orthonormal bases satisfy that certificate).

I investigated the Mathlib representation-theory API and confirmed the genuine path:
- `FDRep.character V g` = trace of `ρ(g)` (`Character.lean`).
- `FDRep.simple_iff_char_is_norm_one` (`FinGroupCharZero.lean:121`): over ℂ, `Simple V ↔
  ∑ g, χ(g)·χ(g⁻¹) = Nat.card G` — the converse direction needed to *prove* irreducibility
  from a constructed rep.
- `FDRep.char_orthonormal` (`Character.lean:129`) — distinct simples ⟹ orthogonal characters.
- `Representation.ofMulAction` / `Representation.trivial` (`Basic.lean`).

The full fix requires constructing **15 honest `FDRep ℂ G`** across Q₈/S₄/A₅, proving each
`Simple` + pairwise non-iso + character = book row, and reproving all six conjugacy-count/
cardinality theorems without `native_decide`. The two A₅ icosahedral 3-dim reps (golden-ratio
traces `(1±√5)/2` over ℚ(√5)⊂ℂ) are research-grade. This is a multi-session program.

**Decomposed #5376 into four sub-issues**, each carrying the exact verified Mathlib lemmas:
- **#5428** — Q₈ (smallest, explicit matrices; the template). No deps.
- **#5429** — S₄ (trivial, sign, std perm ±trivial, S₄→S₃ pullback). depends-on #5428.
- **#5430** — A₅ permutation rows (trivial, 4-dim, 5-dim). depends-on #5428.
- **#5431** — A₅ golden-ratio 3-dim reps + final assembly + `IsCharacterTable` certificate;
  closes #5376. depends-on #5428, #5429, #5430.

Left the `Decomposed into …` breadcrumb on #5376 and skipped it (→ `replan`). No code
changes this session — the decomposition is the deliverable.

## Current frontier

#5428 (Q₈) is unblocked and ready to claim; the other three are `blocked` until their deps
close. The shared technical hurdle flagged in every sub-issue: evaluating
`∑ g : G, χ(g)·χ(g⁻¹)` over G **without** `native_decide` — best done via a class-function
sum helper `∑ g = ∑ class c, |c|·f(rep c)`, which is reusable across all four sub-issues and
for reproving the conjugacy-class counts.

## Overall project progress

Phase 3 formalization, fidelity-sweep follow-up (epic #5338). #5376 is now a 4-issue program
rather than a single stuck issue. No change to overall sorry/native_decide counts this turn.

## Next step

Claim **#5428** and build the Q₈ character table genuinely (4 one-dim homs + the 2-dim
quaternion rep over ℂ, `Simple` via norm-one character, characters matching `chiQ8`). It is
the template; once its class-function-sum helper exists, #5429/#5430 follow the same shape.

## Blockers

None for #5428. #5431 (A₅ icosahedral 3-dim reps) is the genuinely hard piece and may itself
need further decomposition by whoever claims it.
