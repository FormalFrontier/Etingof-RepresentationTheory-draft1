## Accomplished

`/work` → `/feature` session `ff0bc52b`. Claimed and completed **#4961**
(feat, Ch5 #4905 part-A): the right-`GL_N` **grading of `A/det`** and the
**single-degree reduction** for simple finite-dimensional subreps.

New file `Chapter5/CauchyDetQuotientGrading.lean` (sorry-free, axiom-clean:
`[propext, Classical.choice, Quot.sound]`):

- **`homogeneousComponent_polyRightRep`** — right translation `polyRightRep g`
  commutes with the homogeneous-component projection `homogeneousComponent d`
  (since `polyRightRep g` is degree preserving).
- **`homogeneousComponent_mem_detSubmodule`** — the determinant ideal `(det)` is
  graded: `homogeneousComponent d` maps it into itself (every element is
  `det · Q`, whose degree-`d` part is `det` times a homogeneous slice of `Q`).
- **`quotDetProj`** — the descended degree-`d` projection `A/det → A/det` (via
  `Submodule.mapQ`), with equivariance (`quotDetProj_equivariant`), and the
  identity/vanishing laws on the degree components (`quotDetProj_id_on_degree`,
  `quotDetProj_zero_on_degree`).
- **Deliverable 1** — the grading `A/det = ⊕_d (A/det)_d`:
  `quotDetDegreeSubrep_iSup_eq_top` (span) + `quotDetDegreeSubrep_iSupIndep`
  (independence, read off the projections).
- **Deliverable 2** — `exists_degree_embedding_of_simple`: a finite-dimensional
  simple `GL_N`-rep `L` with a `GL_N`-equivariant injection `φ : L → A/det`
  admits, for some degree `d`, a `GL_N`-equivariant injection into
  `quotDetDegreeFDRep k N d`. Proof: each `quotDetProj d ∘ φ` is equivariant out
  of the simple `L`, hence (Schur, via `asModuleHomOfIntertwiner` +
  `eq_bot_or_eq_top` on the kernel) zero or injective; since
  `φ v = ∑_d quotDetProj d (φ v)` is finite and `φ v ≠ 0`, one component is
  injective.

Also added `CauchyDetQuotientDegree` + `CauchyDetQuotientGrading` to the
`Chapter5.lean` aggregator (the #4960 degree file was not yet aggregated).

`lake build EtingofRepresentationTheory.Chapter5` green (8159 jobs).

## Current frontier

#4961 complete; PR to follow. The new single-degree reduction is the bridge the
#4905 assembly (`quotDetRep_irreducible_constituent_lastWeight_zero`,
`CauchyDetQuotient.lean`, single `sorry`) consumes: feed the simple constituent
`L` and equivariant `φ` to `exists_degree_embedding_of_simple` to land in a
single `quotDetDegreeFDRep k N d`, then use `quotDetDegreeFDRep_formalCharacter`
(#4960/#5003) + the highest-weight classification to read off `ν_N = 0`.

## Overall project progress

Etingof draft 1, Stage 3, Chapter 5. #4905 part-A (this issue) lands. With #4960
(degree FDRep + formal character) already in, the det-quotient assembly #4905 now
has both the per-degree character identity and the single-degree reduction; the
remaining work on the `quotDetRep_irreducible_constituent_lastWeight_zero` sorry
is the highest-weight read-off. Other tracked Ch5 sorries: James leading-term
(#5037), false crux (#5024 region), and the #4905 assembly itself.

## Next step

- **Worker:** #4905 assembly — discharge
  `quotDetRep_irreducible_constituent_lastWeight_zero` using
  `exists_degree_embedding_of_simple` + `quotDetDegreeFDRep_formalCharacter` +
  highest-weight classification. (It is currently `blocked` on #4961; once this
  PR merges, `check-blocked` will unblock it.)
- **Worker:** #5037 (James fact 1); reviews #5043 / #5044 (parallel).

## Blockers

None.
