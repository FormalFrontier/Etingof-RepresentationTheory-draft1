# Directive #4516 verification and closure

Session type: work (meta) → directive verification. UUID prefix `a9e3c86a`.

## Accomplished

Claimed and verified directive #4516 ("revert unsound affine-Dynkin
constructions; re-derive infinite type via the book's existence proof").
The directive's concrete deliverable — the revert landed by PR #4517
(merged, 75dacf74) — is genuinely complete in `main` (HEAD e253f907):

- **Unsound reps removed.** `etilde6v2Rep`, `etilde7Rep`, and `t125Rep`
  (the single-nilpotent-twist-at-`m·δ` constructions, provably
  decomposable for every `m ≥ 1`) no longer appear anywhere in
  `Chapter6/InfiniteTypeConstructions.lean`, nor do their false
  `_isIndecomposable` theorems or the private twist helpers.
- **Genuine theorems retained with honest sorries.** `etilde6_not_finite_type`
  (line 3249), `etilde7_not_finite_type` (3392), and `t125_not_finite_type`
  (3496) keep their true statements; each body is a documented `sorry`
  pointing at the book's existence proof (Tits form + orbit-counting,
  Etingof Problem 6.1.5 / 6.1.2).
- **Builds clean.** `lake build EtingofRepresentationTheory.Chapter6.InfiniteTypeConstructions`
  succeeds (8038 jobs); the only `sorry` warnings are the three genuine
  theorems above (3249/3392/3496), no errors. The 4th textual "sorry" at
  line 3521 is prose in a comment describing `not_posdef_infinite_type`.

## Current frontier

The directive is informational/strategic: it records the owner's
"Option B (FieldGeneric subgraph-embedding), not Option A (Tits-form
infra)" decision and delegates the remaining genuine work to live feature
issues. With its deliverable verified in `main`, the directive is closed
via the PR (worker flow: a directive whose deliverables are in is closed,
not left open). The strategy correction persists in the closed issue, in
PR #4517's message, and in the harness protocols added by that PR.

## Overall project progress

Stage 3 formalization ongoing. The open per-(F, Q) infinite-type frontier
continues via tracked feature issues — chiefly `t125_not_finite_type_per_kQ`
(#2793), the K_{1,4} chain (#2801/#2823), and the D̃ helper chain
(#2853/#2967/#2976/#2977/#2978, several `blocked` on #2955). No duplication
of the abandoned Tits-form path.

## Next step

A planner/worker should keep the per-(F, Q) feature issues flowing
(#2793, #2853, #2967 are unclaimed and unblocked). The ℂ-specific
`_not_finite_type` sorries in `InfiniteTypeConstructions.lean` stay low
priority: derive them from the per-(F, Q) results at the canonical
orientation once those land, or retire the file in favour of
`FieldGenericInfiniteType.lean`.

## Blockers

None.
