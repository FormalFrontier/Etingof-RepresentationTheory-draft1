## Accomplished

Resolved issue #2859: hoisted `embed_sum_zero_F` and `center_decomp_F`
from `FieldGenericD5Tilde.lean` into `FieldGenericStar.lean`,
dedup'ing the two helper-lemma duplications flagged by the wave-60
audit of #2835.

Concrete changes:

- Moved `starFirst_F` / `starSecond_F` from the "Direction-aware leaf
  maps" section to right after the embeddings, so they are available
  before `starRepGen_isIndecomposable`. Updated the section comment
  in the lower half of `FieldGenericStar.lean` to point readers to
  the new location.
- Added a new "Embedding sum lemmas" section in `FieldGenericStar.lean`
  with the public theorems `embed_sum_zero_F` and `center_decomp_F`
  (latter using `starFirst_F` / `starSecond_F`, matching the form of
  the original public theorem in `FieldGenericD5Tilde.lean`).
- Replaced the local `have embed_sum_zero` and `have center_decomp`
  inside `starRepGen_isIndecomposable` with direct calls to
  `embed_sum_zero_F F m …` and `center_decomp_F F m w` at the single
  use site of each.
- Deleted the redundant public copies in `FieldGenericD5Tilde.lean:215-249`
  and updated the Section 5 doc-comment plus the
  `d5tildeRep_kQ_isIndecomposable` docstring to point to the new home
  for the two lemmas.

Verification:

- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericStar` green.
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde` green.
- `lake build EtingofRepresentationTheory.Chapter6` green (8079 jobs, no errors).
- `FieldGenericD5Tilde.lean` sorry count: 6 → 6 (unchanged; was the verification gate).
- `FieldGenericStar.lean` sorry count: 0 → 0.
- No downstream caller change needed: `embed_sum_zero_F` continues to be
  reachable from `FieldGenericD5Tilde.lean:530, 588` (now resolves via
  the `FieldGenericStar` import); `center_decomp_F` has no callers yet.

Diff: 75 insertions, 92 deletions across the two Lean files (~17 line
net deletion).

## Current frontier

#2859 is complete. The wave-60 audit follow-ups remain:

- The two unrelated review observations from the audit (the
  `gamma_from_embed*_F` follow-ups, plus the inner `d5tildeRepMap_kQ`
  match-arm review).

## Overall project progress

Stage 3 (formalization) — Chapter 6 is in the middle of the D̃₅ / Ẽ₆ /
Ẽ₇ / Ẽ₈ cascade. The orientation-generic indecomposability proofs are
the major remaining sorries in `FieldGenericD5Tilde.lean`,
`FieldGenericETilde6.lean`, and `FieldGenericETilde7.lean`. This PR is
a small infrastructure cleanup that keeps the K_{1,4} / D̃₅ helper
chain tidy ahead of those proofs.

## Next step

A planner can pick up the wave-60 audit's remaining follow-ups (the
`d5tildeRepMap_kQ` inner-match-arm review and any further dedup of the
star-cascade helpers in `FieldGenericETilde6.lean` /
`FieldGenericETilde7.lean` — they likely have similar local copies of
star-graph helpers).

## Blockers

None.
