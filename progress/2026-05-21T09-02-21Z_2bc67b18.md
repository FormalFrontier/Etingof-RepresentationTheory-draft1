## Accomplished

Closed wave-63 chore chain on `FieldGenericTpqr.lean` by refreshing the
inner per-theorem docstring of `single_branch_leaf_case_both_extend_per_kQ`
(issue #3009).

* **Deliverable 1**: rewrote the "API stub" paragraph (originally lines
  1230-1235) to mirror the post-#3008 4-way TODO dispatch. The
  `t125_not_finite_type_per_kQ` clause now names both `(r = 2, q ≥ 3
  → T(1, q, 2))` and `(q = 2, r ≥ 3 → T(1, 2, r))` branches (was: only
  the `(q = 2, r ≥ 5 → T(1, 2, 5))` half). The ADE clause now restricts
  the explicit-shape enumeration to `T(1, 2, 2) = D₅` (the only shape
  landed via PR #2912) and notes that the asymmetric ADE shapes
  T(1, 3, 2)/T(1, 4, 2)/T(1, 2, 3)/T(1, 2, 4) are folded into the
  T(1, q, 2) / T(1, 2, r) sub-cases. Was a 3-way dispatch read; is now a
  4-way dispatch read matching the TODO block at lines (was 1284-1289)
  1288-1293.
* **Deliverable 2** (optional, taken): completed the sentence fragment
  "given the T(1, q, r) configuration where both of `v₀`'s non-leaf"
  by adding "neighbours have degree 2." on a fourth line.

No code edits; the body `sorry`, parameter-discards, file-level
docstring, and TODO block were left untouched per the issue's
"do not modify" list.

## Current frontier

Wave-63 chore-citation refresh chain on `FieldGenericTpqr.lean` is now
fully closed: PR #2989 (file docstring), PR #2994 (TODO block), PR
#3008 (line numbers + #2912 attribution), this PR (inner docstring
4-way dispatch + sentence fragment). The `sorry` at line 1303 (was
line 1298, +5 due to docstring expansion) and the parent-assembly
chain (#2905 → #2907/#2908/#2909/#2910) remain as the next bodies of
work, tracked separately.

## Overall project progress

Stage 3.x (proof construction) is the active stage. Chapter 6 field-
generic ladder is structurally intact with assembly building green.
This turn is a comment-only chore that improves accuracy of the
inner-docstring dispatch description; it does not advance any sorry
count.

## Next step

The wave-63 chore audit has no remaining loose ends. The next planner
should select from the open feature queue or pick up parent-assembly
sub-issues #2907/#2908/#2909/#2910 (the actual `_per_kQ` body work
under #2905).

## Blockers

None.
