## Accomplished
Issue #5063 (Ch5 #4905 glue-2): proved sorry-free in
`Chapter5/CauchyDetQuotientDegree.lean` the two structural facts about
`M = quotDetDegreeFDRep k N d` consumed by the #4905 final assembly:

- `quotDetDegree_iSup_glWeightSpace_eq_top` — the weight spaces of `(A/det)_d`
  span (`h_span`), via the surjective equivariant projection `π : A_d → (A/det)_d`
  applied to `polyRight_iSup_glWeightSpace_eq_top` (mirrors the inlined `hWtop`
  derivation at lines 106-114).
- `quotDetDegree_homog` — every nonzero `μ`-weight space of `(A/det)_d` forces
  `∑ μ = d` (`h_homog`).

Supporting declarations added:
- `polyRight_glWeightSpace_homog` — homogeneity for `A_d` itself, read off
  `map_polyOf_glWeightSpace` (a nonempty `Dset N d μ` means `∑ μ = d`).
- `quotDetDegreeProj` (+ `_surjective`, `_equivariant`) — the surjective
  `GL_N`-equivariant projection `A_d → (A/det)_d`, extracted from the inlined
  construction in `quotDetDegreeFDRep_formalCharacter`.

The `quotDetDegree_homog` proof avoids element-level weight-space decomposition:
`⊤ = range π = ⨆_ν (A_d)_ν.map π`; only degree-`d` weights contribute
(`polyRight_glWeightSpace_homog`), so when `∑ μ ≠ d` every contributing `ν ≠ μ₀`,
giving `⨆_{ν ≠ μ₀} (A/det)_ν = ⊤`; weight-space independence
(`glWeightSpace_iSupIndep`) then forces the `μ₀`-weight space to `⊥`.

## Current frontier
`lake build EtingofRepresentationTheory.Chapter5.CauchyDetQuotientDegree` green
(8109 jobs). `#print axioms` on both deliverables and the two helpers: only
`propext, Classical.choice, Quot.sound` — no `sorry`.

## Overall project progress
Stage 3 (formalization), Chapter 5. #4905 was blocked on #5063 (this) and #5064.
This lands the glue-2 inputs to
`simple_constituent_formalCharacter_eq_schurPoly_mem`; #5064 (glue-3, character
as antitone Schur sum) remains the other blocker.

## Next step
Pick up #5064 (Ch5 #4905 glue-3) to finish unblocking #4905.

## Blockers
None.
