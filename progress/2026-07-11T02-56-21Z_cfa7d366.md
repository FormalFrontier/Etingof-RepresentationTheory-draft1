## Accomplished

Feature session for issue #6419 (Ch9 Problem 9.4.6(i) packaging lemmas).

Added two sorry-free theorems to
`EtingofRepresentationTheory/Chapter9/HomologicalDimensionReduction.lean`
(in `namespace Etingof`):

1. `homologicalDimension_le {d : ℕ} (h : HasHomologicalDimensionLE R d) :
   homologicalDimension R ≤ (d : ℕ∞)` — proved via `unfold` + `iInf₂_le d h`.
2. `homologicalDimension_eq_one_of_not_le_zero (h1 : HasHomologicalDimensionLE R 1)
   (h0 : ¬ HasHomologicalDimensionLE R 0) : homologicalDimension R = 1` — `le_antisymm`;
   upper bound from (1), lower bound via `le_iInf₂` with the `d = 0` term ruled out by `h0`
   and every `d ≥ 1` giving `(d : ℕ∞) ≥ 1`.

No monotonicity lemma or general `= n` lemma was added (scope kept small per the issue).
Import surface unchanged (`Definition9_4_3` + `Mathlib.Data.ENat.Lattice`).

## Current frontier

Issue #6419 fully complete. Both theorems build and `#print axioms` shows only
`[propext, Classical.choice, Quot.sound]`. The existing `homologicalDimension_eq_top`
still builds unchanged.

## Overall project progress

Chapter 9 §9.4 packaging infrastructure advancing. This unblocks the downstream
`homologicalDimension_pathAlgebra_eq_one` / `homologicalDimension_freeAlgebra_eq_one`
computations in `Problem9_4_6.lean`, which can now combine #6412's `h1` upper bound with a
non-projective-module `h0` via `homologicalDimension_eq_one_of_not_le_zero`.

## Next step

Downstream consumers: Problem 9.4.6(i) still needs `h0` (a non-projective path-algebra module)
to invoke the new lemma; and #6420 (`hasHomologicalDimensionLE_pathAlgebra_one`) supplies the
`h1` upper bound via the standard length-1 projective resolution.

## Blockers

None.
