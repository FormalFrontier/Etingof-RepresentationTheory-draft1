## Accomplished

Feature session (`/work` → `/feature`), claimed issue #6070 (Problem 4.12.2 parts (c),(d)
for the Heisenberg group over 𝔽_p). Landed **part (c) fully, sorry-free**:

- `one_dim_reps_card : Nat.card (Heisenberg p →* ℂˣ) = p ^ 2`. New helpers `abHom` (the
  abelianization map `⟨a,b,c⟩ ↦ (a,b)` to `Multiplicative (ZMod p × ZMod p)`),
  `abHom_surjective`, `central_mem_commutator` (⟨0,0,1⟩ = ⁅xGen,yGen⁆ ∈ commutator), and
  `abHom_ker_le_ker` (ker abHom = center ⊆ commutator ⊆ ker of every character). Count via
  `MonoidHom.liftOfSurjective` + `CommGroup.card_monoidHom_of_hasEnoughRootsOfUnity`.
- `R1_decomposes`: R₁ (z=1) = internal direct sum of p one-dim G-invariant lines. The p DFT
  characters χ_j(t) = ζ^{(j·t).val} (ζ a primitive p-th root, `Complex.isPrimitiveRoot_exp`)
  are eigenvectors of the shift `ρ(xGen)` with distinct eigenvalues ζ^{(-j).val}; linear
  independence from `Module.End.eigenvectors_linearIndependent'`, IsInternal from
  `isInternal_submodule_of_iSupIndep_of_iSup_eq_top` (`iSupIndep_span_singleton` +
  `span_eq_top_of_card_eq_finrank`). G-invariance via the submonoid-closure pattern from
  `irreducible_iff`.

Both proofs `#print axioms` clean ([propext, Classical.choice, Quot.sound]).

## Current frontier

Part (d) `irreducible_dim` (every irreducible has dim 1 or p) is the sole remaining `sorry`
in `Problem4_12_2.lean`. Decomposed into follow-up issue **#6094** with a detailed plan:
exhibit the complete family (p² characters + (p-1) copies of R_z, z≠1) with Σd² = p³ = |G|,
and use `Infrastructure.exists_simples_sum_finrank_sq_eq_card` + a pigeonhole/positivity
argument to force every simple's dimension into {1, p}.

## Overall project progress

Phase 3 formalization ongoing. This turn removed 2 of the 3 `sorry`s in Problem 4.12.2
(item now `proof_partial`). Merged 4 ready PRs (#6077–6080) at cycle start.

## Next step

Claim #6094 and finish part (d) per its plan, turning Problem 4.12.2 fully `sorry_free`.

## Blockers

None. #6094 is unblocked (all its inputs are on main after this PR merges).
