## Accomplished

One-shot dispatch for #5901 (statement pass: Chapter 7 exercises). Landed faithful
Lean 4 statements with `sorry` proofs for **4 of the 6** listed exercises, each in its
own new file, all building with only the expected `sorry` warnings and added to the
`Chapter7.lean` aggregator:

- **Exercise 7.8.4** (`Chapter7/Exercise7_8_4.lean`): acyclic complex of vector spaces
  is contractible (`Homotopy (𝟙 K) 0`), plus `_split` (SES of vector spaces splits) and
  `_not_abelianGroups` (the answer to the final question — fails over ℤ).
- **Problem 7.7.3** (`Chapter7/Problem7_7_3.lean`): the full subcategory of `ModuleCat A`
  on finitely generated modules is abelian, for `A` a finite-type ℤ-algebra.
- **Problem 7.8.5** (`Chapter7/Problem7_8_5.lean`): connecting hom + long exact sequence
  of cohomology for a short exact sequence of complexes of abelian groups.
- **Exercise 7.9.7** (`Chapter7/Exercise7_9_7.lean`): adjoint additive functors — left
  adjoint right exact, right adjoint left exact (via the project's Definition 7.9.3
  aliases).

`progress/items.json` updated to `statement_formalized` for all four.

## Current frontier

The two remaining exercises (Problem 7.8.7, Exercise 7.9.8) were **decomposed into
follow-up sub-issue #5902** because they need infrastructure investigation:
- 7.8.7 (tensor complex + Künneth): Mathlib has `HomologicalComplex.Monoidal.tensorObj`
  but **no Künneth formula**; part (iv)'s direct sum over `{(j,m):j+m=i}` needs care.
- 7.9.8 (reflection functors as adjoint pair): Ch6 `reflectionFunctorPlus/Minus` are
  custom `QuiverRepresentation` structures, not categorical `Functor`s, so the
  adjunction cannot be stated as `CategoryTheory.Adjunction` directly.

## Overall project progress

Chapter 7 exercise statement pass: 4 of 6 now `statement_formalized`; remaining 2 tracked
in #5902. Statements only (all `sorry`), per the spec-first phase.

## Next step

A worker claims #5902 to state 7.8.7 and 7.9.8 (create `Problem7_8_7.lean`,
`Exercise7_9_8.lean`). Later a proof-phase pass discharges the eight new `sorry`s.

## Blockers

None for the landed work. #5902 is gated on infrastructure investigation (no Künneth in
Mathlib; reflection functors not bundled categorically).
