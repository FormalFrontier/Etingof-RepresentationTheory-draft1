# Progress — feature session f222547a (#2904 partial)

## Accomplished

Claimed and executed a coherent partial slice of #2904
(`single_branch_leaf_case_per_kQ` real body — T(1, q, r) leaf-case
dispatch). In
`EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`:

- **Top-level scaffold** for `single_branch_leaf_case_per_kQ`: replaces
  the previous sorry body with the prelude (extract `a₂`, `a₃`) plus
  the outer case-split on whether each of `a₂`, `a₃` has degree 2.
- **Both arms extend (q, r ≥ 2)**: dispatch by name to the new
  `single_branch_leaf_case_both_extend_per_kQ`. The helper signature
  exposes `a₂`, `a₃`, their adjacency facts, and the degree equalities,
  so the follow-up worker can skip the extraction prelude and start
  directly with extracting `b₂`, `b₃`.
- **One leaf branch (a₃ leaf, q ≥ 2, r = 1)**: ~12-line `tree_two_leaf_posdef`
  contradiction mirroring `InfiniteTypeConstructions.lean:8353-8373`.
- **Other leaf branch (a₂ leaf, T(1, ≥1, 1))**: ~12-line symmetric
  contradiction mirroring `InfiniteTypeConstructions.lean:8374-8390`.

In `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`:

- Dropped `private` on `tree_two_leaf_posdef` (line 5415) so that
  `FieldGenericTpqr.lean` can dispatch the leaf-leaf cases through it.
  Mirrors the visibility-cleanup precedent set by sibling PR #2900
  (#2877's audit recipe).

Filed follow-up issue #2905 for the residual ~1370-line
`single_branch_leaf_case_both_extend_per_kQ` body (Ẽ₇ / T(1, 2, 5) /
ADE dispatch), with optional decomposition along the
(b₂ extends?, b₃ extends?) case-split axis.

`lake build EtingofRepresentationTheory.Chapter6` passes; the only new
sorry warning is on the new
`single_branch_leaf_case_both_extend_per_kQ` stub.

## Current frontier

PR for the partial #2904 deliverable ready to push. The
`single_branch_leaf_case_per_kQ` real-body chain stands at:

- D1.scaffold-and-leaf-leaf: this PR (closes the
  `single_branch_leaf_case_per_kQ` sorry, introduces the
  `single_branch_leaf_case_both_extend_per_kQ` stub).
- D1.both-extend: tracked by #2905 (Ẽ₇ + T(1, 2, 5) + ADE).

Chapter 6 per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard, one tick from
cycle 5d60cec5:

- D2.degree4 on `main` (PR #2891).
- D2.cycle on `main` (PR #2897).
- D2.adjacent on `main` (PR #2900).
- D2.single on `main` (PR #2903).
- D2.single follow-up: partial here (this PR), full body via #2905.
- **Pending**: D2.nonadj, D2.outer + D3 (still under #2877).

## Overall project progress

Stage 3 (Lean formalisation). Sorry count on `main` post-#2903: 19.
This PR is sorry-neutral (closes 1, introduces 1). Expected post-merge
total: 19.

## Next step

- A pod planner should triage the `replan` label on #2904 (close it
  with a forward link to #2905, since #2905 fully covers the residual
  scope).
- The next worker should claim #2905 — or, given its ~1370-line size,
  the next planner cycle may prefer to pre-split it along the
  (b₂ extends?, b₃ extends?) axis (cf. #2904's optional-decomposition
  note).
- Parent #2877 still has D2.nonadj and D2.outer + D3 outstanding; both
  remain candidates for the next planner cycle (D2.outer + D3 is the
  smaller remaining unit at ~150 lines).

## Blockers

None for this worker. Standing constraints unchanged: framework wall
#2436 gates the inherited sorries in the per-(F, Q) indecomposability
proofs; the static pr-repair PRs (#2849 / #2694 / #2550) remain in
`/repair` territory.
