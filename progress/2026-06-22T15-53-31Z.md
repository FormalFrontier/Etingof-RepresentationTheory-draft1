## Accomplished

Migrated reflection-functor downstream consumers to Lean v4.29.0-rc1 (Chapter 6 only,
no sorry/axiom/statement changes).

**Fully fixed and building clean (zero sorries):**
- `ReflectionFunctorInfrastructure.lean`: `reflectionFunctorMinus_map_iso_naturality`
  (the `a≠i, b=i` case at the old L317 `match inst i i` desync). Introduced supporting
  private lemmas:
  - `reflFunctorMinus_equivAt_eq_mkQ` (Decidable-free restatement of `mkQ`).
  - `reflectionFunctorMinus_map_iso_equivAtAt_eq` (NEW noncomputable def: the vertex-i map
    iso with the `Decidable` discriminant exposed as an explicit argument — mirrors
    `reflFunctorMinus_equivAtAt_eq`). `reflectionFunctorMinus_map_iso_equivAt` at `v=i` now
    calls this instead of an inline `match`.
  - `heq_linearEquiv_apply` (HEq congruence for applying a `LinearEquiv`).
  - `reflectionFunctorMinus_map_iso_equivAt_factor` (the cokernel-coordinate factorisation,
    proven by the HEq technique: `congr 1` on the same-family explicit-discriminant term,
    `equivAt_eq` charts shown HEq-id via `show ... ; rw [h_di]`, instance HEqs via the
    `generalize/rw[h_di]/subst` trick, assembled with `heq_linearEquiv_apply`).
  - `reflectionFunctorMinus_map_iso_equivAt_mkQ` (the consumer-facing apply formula). The
    naturality `b=i` case now uses it + `directSumMapEquiv_lof` instead of the broken match.
- `Proposition6_6_8.lean`: all four `finrank`-rewrite failures (sink/source × v=i/v≠i).
  Replaced `match hd; rw [hd]; …` with `LinearEquiv.finrank_eq` of the `reflFunctorPlus/
  Minus_equivAt_eq`/`equivAt_ne` equivalences (no discriminant rewrite).

**Verified clean:** `lake build` of ReflectionFunctorInfrastructure, Proposition6_6_5,
Proposition6_6_8, Definition6_6_3, Definition6_6_4 — all succeed, zero sorries.
(Proposition6_6_5 needed no edits; already built.)

**Partial progress:**
- `Proposition6_6_7.lean`: fixed the `IsZero`/`Subsingleton` case (old L121-146) by
  transporting `Subsingleton` through `reflFunctorPlus_equivAt_eq/_ne` (`.symm.toEquiv.
  subsingleton_congr.mp`) instead of `match hd; rw [hd]; dsimp`. Net 2 fewer errors.

## Current frontier

`Proposition6_6_6.lean` and `Proposition6_6_7.lean` still fail to build.

- `Proposition6_6_6.lean` (1 logical site, L408 consumer naturality `a≠i,b=i`): evaluates
  `equivAt_eq_sink (reflFunctorMinus_mkQ … (lof …))`. Two `match inst_dec i i` (one inside
  `equivAt_eq_sink`, one inside `reflFunctorMinus_mkQ`'s `equivAt_eq.symm`) must resolve
  together; `refine match`, `rw/simp [h_di]`, `generalize`, `Subsingleton.elim` all fail
  the v4.29 motive/desync check. Clean fix needs `equivAt_eq_sink` itself refactored to
  precompose the `reflFunctorMinus_equivAt_eq` chart (build on the cokernel, no match) —
  attempted, but the body's quotient instances don't defeq-align with the chart's
  `addCommGroupOfRing` instances (`RingHomCompTriple`/`AddCommMonoid` synthesis stuck on
  the `.trans`). Reverted to baseline (unchanged) to avoid a worse state.

- `Proposition6_6_7.lean` (~11 remaining errors at L84, 215, 309, 348, 689, 709, 766, 849,
  880, 996, 1173): each is an independent `match hd; rw [hd]; dsimp`/`unfold
  reflectionFunctorPlus; rw [h_da,h_db]` discriminant pattern in a separate
  subrep/naturality helper. Each needs the HEq-identity rewrite of `equivAt_ne` (the
  `heqv` template pattern) rather than unfolding `reflectionFunctorPlus` and rewriting the
  discriminant.

## Overall project progress

Toolchain bump to v4.29.0-rc1. Hard core (Definition6_6_2/3/4) already migrated. This turn
completed the two explicitly-listed consumer files (ReflectionFunctorInfrastructure,
Proposition6_6_8) + confirmed Proposition6_6_5. Two consumers (6_6_6, 6_6_7) remain.

## Next step

For `Proposition6_6_7`: rework each remaining `match hd; rw [hd]; dsimp` site to transport
through `reflFunctorPlus_equivAt_ne` (HEq-identity) or `equivAt_eq` (`finrank_eq` /
`subsingleton_congr` / explicit value), exactly as done for the Subsingleton case and for
Proposition6_6_8 — no `unfold reflectionFunctorPlus` + discriminant `rw`.

For `Proposition6_6_6`: refactor `equivAt_eq_sink` to build on the cokernel and precompose
`reflFunctorMinus_equivAt_eq` (eliminating its internal `match inst_dec i i`), carefully
threading the quotient `AddCommGroup`/`Module` instances so they defeq-match the chart's
`addCommGroupOfRing` instances; then the consumer naturality reduces cleanly via the
chart-cancellation `equivAt_eq (equivAt_eq.symm _) = _`.

## Blockers

None at the definition level. Remaining work is proof-level v4.29 `rw`/`match` motive
adaptations of the same kind already solved for the listed files; just sizable
(per-lemma HEq/equiv rework in 6_6_7, one instance-alignment refactor in 6_6_6).
