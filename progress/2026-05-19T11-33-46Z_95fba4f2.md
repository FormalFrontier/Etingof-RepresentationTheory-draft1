## Accomplished

Audited PRs #2952 / #2956 / #2958 / #2961 (Phase 1 setup + Phase 2
Cases A/B/C-main/D of `non_adjacent_branches_leaf_case_per_kQ`) per
issue #2972. Five-dimension report at
`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-phase-1-and-cases-a-b-c-d.md`.

Verdict per dimension:
- **D1 Phase 1 lattice fidelity (PR #2952):** PASS — line-for-line
  port of `InfiniteTypeConstructions.lean:9772-10120`. No hypothesis
  is silently weakened relative to the universal `leaf_case`.
- **D2 vertex map + embedder args (Cases A/B/C-main/D):** PASS for all
  four cases. Vertex tuples, edge facts, distinctness facts, and the
  orientation witness `hOrient` thread through each embedder
  signature correctly (`embed_t125_in_tree_per_kQ`,
  `embed_etilde7_in_tree_per_kQ`, `embed_etilde6_in_tree_per_kQ`).
- **D3 case coverage exhaustiveness:** PASS — Lean's `by_cases` ladder
  structurally exhaustive; residual `sorry` at line 969 covers only
  the documented Case E sub-cases tracked by #2955.
- **D4 build sanity:** PASS — both target lake builds green; only
  expected `sorry` warnings (Case E at line 969; downstream sorries
  unrelated to the audited PRs).
- **D5 docstring / dead-code:** CONCERN (non-blocking) — file-level
  docstring at lines 13-53 and theorem docstring at lines 83-87 are
  stale (the "API stub" framing, the available-leaves list missing
  D̃₇, and the strategy summary missing D̃₇). Staleness was
  introduced by subsequent PRs (#2966, #2968, #2970), not by the four
  audited PRs themselves. Suggest a docstring-refresh follow-up
  issue.

No code changes pushed. No build-breaking issue discovered.

## Current frontier

Audit deliverable complete. Outstanding work on this theorem chain:
Case E and remaining all-leaves residuals tracked by #2955; sibling
audit issue #2973 covers PRs #2966 / #2968 / #2970 (Case C.short
residual + D̃₇ helper).

## Overall project progress

Chapter 6 infinite-type dispatch chain — `non_adjacent_branches_*`
family is the active frontier. `non_adjacent_branches_leaf_case_per_kQ`
body is now substantially proven: Phase 1 setup + Cases A, B,
C-main, C-short (tractable + chain.length=4 all-leaves), D landed;
only Case E plus the chain.length ∈ {4, 5} asymmetric residuals
remain (`sorry` on line 969). Audit cadence is keeping up with the
case-build cadence.

## Next step

Recommended priorities for the next planner cycle:
1. Open the docstring-refresh follow-up issue suggested by D5 (low
   priority).
2. Continue the audit cycle: #2973 (C.short residual + D̃₇ helper)
   if still open.
3. Pick up the Case E sub-issues (#2955) once a planner has
   decomposed the residual cases.

## Blockers

None.
