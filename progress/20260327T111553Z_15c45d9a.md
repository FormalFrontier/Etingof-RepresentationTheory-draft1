## Accomplished

- Defined `tabloidProjection : SymGroupAlgebra n →ₗ[ℂ] (Tabloid n la →₀ ℂ)` — the quotient map to the tabloid module M^λ = ℂ[S_n/P_λ]
- Proved `tabloidProjection_single`, `tabloidProjection_apply`
- Proved `toTabloid_rowPerm_mul` (left P_λ-multiplication preserves tabloids)
- Added `tabloidDominates_of_toTabloid_eq_{left,right}` (dominance well-definedness)
- Made `polytabloid_apply`, `polytabloid_self_coeff`, `youngSymmetrizer_rowPerm_coeff` public (needed downstream)

**Critical mathematical finding**: The proposed "right-multiplication dominance" approach and the "tabloid projection diagonal ≠ 0" claim in issue #1852 are **mathematically incorrect**.

### Counterexample (λ=(2,2), n=4)

For the SYT T' = [[0,2],[1,3]] with σ_{T'} = (12):
- σ_{T'} Q_λ σ_{T'}⁻¹ = P_λ (conjugation swaps row/column subgroups)
- The tabloid projection diagonal Σ_{q ∈ Q_λ : [σ_T q] = [σ_T]} sign(q) = 1 + (-1) + (-1) + 1 = 0
- So `tabloidProjection_polytabloid_self_ne_zero` is FALSE as stated
- The claim `relRowSubgroup_inter_ColumnSubgroup` (σ_T⁻¹ P_λ σ_T ∩ Q_λ = {1}) is also FALSE

### Correct approach for linear independence

The correct proof uses the **tabloid-module polytabloid** (distinct from the group-algebra tabloid projection):

`v_T = Σ_{q ∈ Q_λ} sign(q) · single(toTabloid(q⁻¹ * σ_T), 1)`

Key: `toTabloid(q⁻¹ σ_T) = toTabloid(σ_T)` iff `q⁻¹ ∈ P_λ` iff `q = 1` (by P_λ ∩ Q_λ = {1}). So the coefficient at tabloid {T} is exactly 1. Off-diagonal tabloids are dominated by `column_perm_dominance`. This gives unitriangularity and hence linear independence.

The map from polytabloids to these v_T vectors factors through the left-module map: `π(e_T) = |P_λ| · Σ sign(q) · [σ_T · q]`, but this uses RIGHT multiplication `σ_T · q`, not LEFT multiplication `q⁻¹ · σ_T`. These give DIFFERENT tabloid expansions. The LEFT-mult version `Σ sign(q) · [q⁻¹ σ_T]` has diagonal 1, while the RIGHT-mult version `Σ sign(q) · [σ_T q]` can have diagonal 0.

## Current frontier

- TabloidModule.lean: tabloid projection map defined, all proofs compile
- PolytabloidBasis.lean: 5 sorries remain (same as before)
- The correct linear independence approach needs: define `v_T` directly and prove `LinearIndependent.of_comp` using the LEFT-multiplication formulation

## Overall project progress

27 sorries across 19 files. 91.9% of files sorry-free. No new sorries introduced; 3 private lemmas made public.

## Next step

The correct approach for `polytabloid_linearIndependent` (#1853 blocker):
1. Define `tabloidPolytabloid T = Σ_{q ∈ Q_λ} sign(q) · single(toTabloid(q⁻¹ * σ_T), 1)` as a `Tabloid →₀ ℂ`
2. Show `tabloidPolytabloid T (sytToTabloid T) = 1` (trivially from P_λ ∩ Q_λ = {1})
3. Show off-diagonal has dominance (from `column_perm_dominance`)
4. Show `tabloidPolytabloid` values are linearly independent (unitriangularity)
5. Show the map from `SymGroupAlgebra` to `Tabloid →₀ ℂ` that sends `e_T ↦ tabloidPolytabloid T` is ℂ-linear
6. Apply `LinearIndependent.of_comp`

Step 5 is the subtle part — need to define the RIGHT linear map that sends `a ↦ Σ_g a(g) · (Σ_q sign(q) · single(toTabloid(q⁻¹ g), 1))`. This is the "column antisymmetrization" in the tabloid module.

**Issue #1852 should be replanned** — the deliverables as stated contain false mathematical claims. A new issue should be created with the correct approach.

## Blockers

The original issue's deliverables 2 (right-multiplication dominance) and 3 (diagonal computation) are based on incorrect mathematical claims. The issue needs replanning.
