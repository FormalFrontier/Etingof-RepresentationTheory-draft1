## Accomplished

Executed issue #7041 (Ch5 docstring-fidelity, `feature`). Corrected the stale
`## Remaining work (issue successor)` module-docstring block in
`EtingofRepresentationTheory/Chapter5/DetIrreducible.lean`, which falsely claimed
the determinant-specific induction was split into a successor issue and that
`detPoly_prime` / `detPoly_irreducible` each carried a `sorry`. Both are proved
sorry-free in the file.

- Replaced the stale block with a present-tense `## Main results (proven in this
  file)` inventory listing `detPoly_prime` and `detPoly_irreducible`, preserving
  the genuine induction-route exposition (cofactor repackaging
  `detPoly = X(0,0)·M₀₀ + R`, `(0,0)`-minor as a `rename` of `detPoly k (N-1)`,
  coprimality `M₀₀ ∤ R`).
- Comment-only change inside the `/-! ... -/` doc block; no signature, `def`,
  `theorem` body, or proof term touched.
- Verified `grep -nE '\bsorry\b|successor|split into|Remaining work'` now returns
  zero matches in the file.

Committed as a7c5fe90.

## Current frontier

Ch5 docstring-fidelity cleanup. One sibling item remains unclaimed: #7042
(PolynomialTensorBridge equivariance `## Status` block).

## Overall project progress

Chapters largely proved; ongoing wave of docstring-fidelity corrections
(stale statement-first-phase banners on now-proved, sorry-free files).

## Next step

Claim and execute #7042 (same shape: rewrite the `## Status` block in
`PolynomialTensorBridge.lean` to reflect that equivariance is proved sorry-free
in-file).

## Blockers

None.
