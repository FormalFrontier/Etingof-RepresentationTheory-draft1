# Progress — feature session f2dd58f4 (Issue #2895)

## Accomplished

Implemented `graph_with_list_cycle_infinite_type_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericCycle.lean` (D2.cycle
sub-deliverable of #2877). The new theorem is the per-(F, Q) version of
`graph_with_list_cycle_infinite_type`
(`InfiniteTypeConstructions.lean:3910-4057`). Structure follows the
recipe from `progress/reviews/2026-05-18-degree4-per-kQ-placement.md`,
D3 "Pattern recipe":

- **Signature**: append `(F : Type) [Field F]`,
  `(Q : @Quiver.{0,0} (Fin n))`,
  `[∀ a b, Subsingleton (@Quiver.Hom (Fin n) Q a b)]`,
  `(hOrient : @Etingof.IsOrientationOf n Q adj)`. `[IsAlgClosed F]`
  **omitted** per recipe — the only leaf used
  (`cycle_not_finite_type_per_kQ`) does not require it.
- **Conclusion**: canonical per-(F, Q) shape
  `¬ Set.Finite { d : Fin n → ℕ | ∃ V, V.IsIndecomposable ∧ ∀ v, ... }`.
- **Body**: structurally identical to the `_kQ`-free original. The
  strong-induction key lemma carries the new conclusion type; the
  chord-case dispatch (recursion on shorter sub-cycles) is unchanged.
  The chordless-case dispatch swaps
  `subgraph_infinite_type_transfer ... (cycle_not_finite_type m hm)`
  for
  `subgraph_infinite_type_transfer_per_kQ φ F Q
   (cycle_not_finite_type_per_kQ F m hm (restrictOrientationViaEmb φ Q)
     (restrictOrientationViaEmb_isOrientationOf φ hembed hOrient))` —
  exactly the same pattern already used by
  `chordless_cycle_infinite_type_per_kQ` (lines 388-390).
- **Placement**: directly after `triangle_infinite_type_per_kQ`
  (line 428) and before `end Etingof`. No new imports — the file
  already imports `FieldGenericInfiniteType` for
  `subgraph_infinite_type_transfer_per_kQ` and
  `restrictOrientationViaEmb_isOrientationOf`.
- **Attribute prefix**: `attribute [-instance] CategoryTheory.CategoryStruct.toQuiver CategoryTheory.ReflQuiver.toQuiver in` applied per recipe step 4.

Verification:

- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericCycle` —
  clean.
- `lake build EtingofRepresentationTheory.Chapter6` — clean (no new
  warnings/errors on the new theorem; only pre-existing warnings on
  unrelated declarations).
- No `sorry` in the new theorem body.

## Current frontier

D2.cycle landed (PR pending creation). Remaining D2 sub-deliverables
under #2877: D2.adjacent, D2.single, D2.nonadj, D2.outer, plus D3
(`not_posdef_infinite_type_per_kQ` outer assembly + Theorem 2.1.2
bridge).

## Overall project progress

Stage 3 (Lean formalisation). Wave-62 substantive PRs since wave-61
summarize: 2 (#2891 = D2.degree4, plus this D2.cycle landing). #2894 =
documentation-only audit verdict, does not count. Sorry-count delta
from this PR: **0**.

## Next step

Planner can pre-split the next D2 sub-deliverable. Per the audit
verdict (#2894) D3 recommendations table, the next-cheapest splits are
either D2.outer (new file `FieldGenericNotPosdef.lean`) or, if the D̃₅
chain unblocks, D2.adjacent. The remaining splits (D2.single,
D2.nonadj) carry non-trivial cross-imports and larger scope.

## Blockers

None for this work item. Standing constraints unchanged.
