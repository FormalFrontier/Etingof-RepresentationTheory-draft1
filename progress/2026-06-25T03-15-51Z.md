## Accomplished

Formalized the prose count in `blobs/Chapter5/Discussion_5.25.4.md` (issue #5169):
there are `½ q (q − 1)` complementary-series representations `Y_ν = Ind_K^G ℂ_ν`.

New file `EtingofRepresentationTheory/Chapter5/Discussion5_25_4.lean` (sorry-free, axiom-clean:
only `propext`/`Classical.choice`/`Quot.sound`). The content is a pure character-group orbit
count, independent of the GL₂ machinery, modelling `K^∨ ≅ ZMod (q²−1)` additively with
`ν ↦ ν^q` corresponding to `x ↦ q·x`:

- `cs_f_involutive` — `x ↦ q·x` is an involution on `ZMod (q²−1)` (since `q² ≡ 1`).
- `fixed_iff` — `q·x = x ⟺ (q+1) ∣ x.val` in the additive model.
- `fixedSet_card` — the fixed set has exactly `q−1` elements (explicit `card_nbij'` with the
  multiples-of-`(q+1)` transversal).
- `cs_moved_card` — exactly `q(q−1)` characters have `ν^q ≠ ν`.
- `cs_reps_transversal` — `cs_f` pairs the moved set into two-element orbits; `cs_reps`
  (the val-minimal representative of each pair) is a genuine transversal: moved set is the
  disjoint union of `cs_reps` and its `cs_f`-image, of equal cardinality.
- `cs_reps_card` — **the count**: the number of orbits (= complementary-series reps) is
  `q(q−1)/2`.

Added the import to `EtingofRepresentationTheory/Chapter5.lean`.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5.Discussion5_25_4` passes; full-chapter
aggregator build in progress to confirm no downstream breakage before opening the PR.

## Overall project progress

Endgame. This closes a claim-coverage gap from the #5137 vacuity audit — the count was
previously only stated in comments in the GL₂ character files. No new sorries introduced.

## Next step

Open the PR for #5169 and enable auto-merge once the chapter build confirms green.

## Blockers

None.
