## Accomplished
Worked #6691 (cyclic-vs-`-Id` dichotomy for finite `G ⊂ SU(2)`). Landed in
`Chapter6/Problem6_1_6.lean`, all sorry-free with clean axioms
(propext/Classical.choice/Quot.sound):
- `eq_negIdSU_of_sq_eq_one`: the unique order-2 element of `SU(2)` is `-Id`
  (Cayley–Hamilton on the 2×2 matrix: `(tr A)•A = 2•1`, `det` forces `(tr A)²=4`,
  so `A = ±1`).
- `even_card_contains_negId`: even `|G|` ⇒ `-Id ∈ G` (Cauchy gives order-2 elt).
- `su2_finite_cyclic_or_contains_negId` (the deliverable): proved **modulo** the
  single odd-order crux, by even/odd split.

## Current frontier
The odd-order half `isCyclic_of_odd_card` (odd `|G|` ⇒ cyclic) remains a `sorry`.
It needs "dim of a complex irrep divides `|G|`" (⇒ tautological 2-dim rep reducible
⇒ injective character `G ↪ ℂˣ` ⇒ cyclic via `isCyclic_of_injective_ringHom`). That
theorem is **not in Mathlib**, and the double-cover route is blocked by the sorried
`so3_finite_subgroup_classification`. Split out as **#6710**.

## Overall project progress
Phase 3 formalization ongoing. #6691 reduced to its one hard crux (#6710); the even
half and order-2 uniqueness are done. Filling #6710 completes both
`su2_finite_cyclic_or_contains_negId` and `mckayAdj_no_selfLoop` (#6674), making
`mckay_isAffineDynkin` sorry-free.

## Next step
Claim #6710: fill `isCyclic_of_odd_card` via the worked route (dim-of-irrep ∣ |G|,
or Frobenius–Schur self-duality). It is a substantial rep-theory piece; may decompose
further (e.g. land "dim of irrep divides order" separately).

## Blockers
None for #6691's landed subset. #6710 is blocked on the missing Mathlib result
"dim of complex irrep ∣ group order" (must be proven in-project).
