## Accomplished

Closed issue #7789 — the affine arm, sub-part (iii), of `Chapter5/Exercise5.27.2`.

`EtingofRepresentationTheory/Chapter5/Exercise5_27_2_Affine.lean`:

* `affineEquiv : Problem4_12_6.Affine K ≃* AffineGroup K` — a genuine `≃*` (real
  `toFun`/`invFun`/`left_inv`/`right_inv`/`map_mul'`), sending `⟨a, b⟩` to
  `⟨Multiplicative.ofAdd b, a⟩`. No correction term is needed: `Affine` multiplies as
  `⟨a₁a₂, a₁·b₂ + b₁⟩` and the semidirect product as `⟨b₁ + a₁·b₂, a₁a₂⟩`.
* `affine_classification'` (`q > 2`) and `affine_classification_two'` (`q = 2`) — the two
  orbit-method classifications restated over `FDRep ℂ (Problem4_12_6.Affine K)`, obtained by
  feeding the existing `affine_classification` / `affine_classification_two` through
  `transport_classification` from the shared transport file.
* `zeroSumFDRep`, `finrank_zeroSumFDRep`, `zeroSumFDRep_simple` — the hint's `(q-1)`-dimensional
  zero-sum representation `Vsub` packaged as an `FDRep`.
* `affine_classification_named` — the honest completion of "redo Problem 4.12.6": every
  one-dimensional member of the transported family is a `Problem4_12_6.charRep χ`, every
  character occurs, the unique `(q-1)`-dimensional member is `zeroSumFDRep`, and it occurs.

`EtingofRepresentationTheory/Chapter5/Exercise5_27_2_Transport.lean`:

* `exists_charRep_iso_of_finrank_eq_one` — every one-dimensional `FDRep ℂ G` for a finite group
  `G` is isomorphic to `charRep ξ` for its own character. This is the non-commutative counterpart
  of `AbelianFDRep.exists_charFDRep_iso`, whose argument only ever used `finrank = 1`; it lives in
  the shared file because the dihedral and Heisenberg arms want the same statement.

`progress/items.json`: sub-part (iii) and the `Chapter5/Exercise5.27.2` roll-up both promoted to
`covered_full`, `followup_issue` cleared.

## Current frontier

Exercise 5.27.2 is now fully covered on all three arms — each classification is stated over the
group its Chapter 4 problem actually studies, and each family is matched against the book's own
named representations.

## Overall project progress

Stage 3.7 coverage-arm auditing continues. This turn closed the last `covered_partial` arm of
`Chapter5/Exercise5.27.2` (the sibling Heisenberg arm landed as #7792 an hour earlier), so the
audit item #7320 has no remaining open follow-ups from this exercise.

## Next step

The largest remaining critical-path item is #7783 (the Gabber-Kac dimension bound for `𝔤₄`),
which blocks #7759 and then #7720. Its own issue body warns that a worker should expect to spend
a session choosing a route (Gröbner-Shirshov vs Verma/contravariant form) and decomposing rather
than closing it; that is still the right framing.

## Blockers

None.

## Notes for successors

* The three copies of `charRep` in the project (`Example4_3_S3.charRep`, `AbelianFDRep.charRep`,
  `Problem4_12_6.charRep`) are definitionally equal, so a lemma stated with one of them applies
  to the others by `rfl`/`exact`. `affine_classification_named` relies on this.
* `omit [Inst] in` must precede the docstring, not sit between the docstring and the declaration.
