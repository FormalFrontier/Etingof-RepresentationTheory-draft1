## Accomplished

Issue #6989 (Ch9 Problem 9.4.6 cleanup), both deliverables:

1. **Header docstring refreshed.** Replaced the stale `## Statement-pass note` prose (which
   falsely claimed proofs were deferred `sorry` and that the Hom-space identification was carried
   only as a hypothesis) with a `## Formalization notes` section describing the completed state:
   part (i) via the standard length-1 resolution; part (ii) available both conditionally
   (`cartanMatrix_pathAlgebra_eq_pathCount`) and unconditionally
   (`cartanMatrix_pathAlgebra_eq_pathCount'`). The note now states that acyclicity is load-bearing
   for path-finiteness and derived, not assumed.

2. **`hacyclic` made load-bearing (preferred route, not the fallback).** Added a self-contained
   `AcyclicFinite` section proving `finite_path`: in a finite quiver with finitely many arrows, if
   every closed path is trivial then each `Quiver.Path a b` is finite. Chain:
   - `pathSupport` — vertex list of a path (length `p.length + 1`);
   - `exists_comp_of_mem_pathSupport` — a path through vertex `c` splits as `a ⟶* c` then `c ⟶* b`;
   - `pathSupport_nodup` — acyclicity forbids revisiting a vertex (a repeat gives a nontrivial
     closed subpath, contradicting `hacyclic`);
   - `path_length_lt_card` — hence `p.length < Fintype.card V` (nodup list bound);
   - `pathSuccEquiv` + `finite_pathLen` — fixed-length paths are finite (finite arrows);
   - `finite_path` — inject `Path a b` into `Σ n : Fin (card V), {p // p.length = n}`, finite.

   Both `cartanMatrix_*` theorems now take `[∀ i j, Finite (i ⟶ j)]` (arrow-finiteness = "finite
   oriented graph") **instead of** the old `[∀ i j, Finite (Quiver.Path i j)]` instance, and derive
   path-finiteness internally via `finite_path hacyclic`. `hacyclic` is now genuinely used.

## Current frontier

Issue #6989 complete. `lake build EtingofRepresentationTheory.Chapter9.Problem9_4_6` succeeds;
comment-stripped `sorry` count = 0; `#print axioms` on `cartanMatrix_pathAlgebra_eq_pathCount`,
`cartanMatrix_pathAlgebra_eq_pathCount'`, `finite_path`, `path_length_lt_card` reports only
`[propext, Classical.choice, Quot.sound]`. `progress/items.json` for `Chapter9/Problem9.4.6`
updated to `sorry_free` with a current coverage note.

The two remaining lint warnings in the file (`LoopVertex` unused `n`, one long line in
`freeToPath_ι`) are pre-existing part-(i) code, not touched by this change.

## Overall project progress

Phase 3 formalization. Problem 9.4.6 fully proved and now documented/typed to match. The design
adds reusable acyclic-quiver path-finiteness infrastructure (`finite_path` and friends) local to
the file; could be promoted to a shared Quiver helper if other chapters need it.

## Next step

Other open feature issues: #6972 (octahedral assembly), #6985 (Ch4 complexified irreducibility),
#6982 (simple order-60 → index-5). #6982 and #6972 were claimed by other agents this cycle.

## Blockers

None.
