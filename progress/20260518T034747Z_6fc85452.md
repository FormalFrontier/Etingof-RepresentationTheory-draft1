## Accomplished

Wave-60 cleanup audit (issue #2864) — PRs #2862 / #2863 pre-flight
check ahead of #2853:

- Verified statement fidelity of PR #2862's three hoisted top-level
  theorems (`d5tilde_core_F` / `d5tilde_core3_F` /
  `d5tilde_gamma_containment_F`) against the local `have` blocks they
  replaced. Bodies are byte-equivalent; hypothesis lists are
  preserved character-for-character; `(Wmain, Wother)` parameterisation
  is symmetric (confirmed via `(W₁, W₂)` and `(W₂, W₁)` canonical-branch
  calls).
- Verified statement fidelity of PR #2863's two hoisted theorems
  (`embed_sum_zero_F` / `center_decomp_F`) in `FieldGenericStar.lean`.
  Confirmed the local `have embed_sum_zero` / `have center_decomp`
  blocks are gone (grep returned only ℂ-source matches). Cross-file
  consumers in PR #2862 (`d5tilde_core_F`/`_core3_F`) wired correctly.
- Built `EtingofRepresentationTheory.Chapter6` green. Sorry count
  unchanged at wave-60 baseline: 6 raw / 2 declarations in
  `FieldGenericD5Tilde.lean`; 0 in `FieldGenericStar.lean`. No new
  lint warnings.
- Quantified downstream readiness for #2853: helpers usable for 14 of
  31 reversed-orientation sub-cases (7 for `core_F`, 7 for `core3_F`,
  0 for `gamma_containment_F`). The remaining 24 need
  direction-reversed sibling lemmas using `starFirst_F` / `starSecond_F`
  projections — already part of #2853's scope per wave-60 audit §4.2.
- Verdict: **PASS** for both PRs. No follow-up sub-issues filed.
- Audit report committed at
  `progress/reviews/2026-05-18-d5tilde-subB-cleanup-hoist.md`.
- Verdict comment posted on #2804.

## Current frontier

D̃₅ Sub B cascade. Wave-60 helper infrastructure cleanup is complete
and audited. The next worker can claim #2853 (the 31 reversed-
orientation sub-cases of `d5tildeRep_kQ_leaf_equalities`).

## Overall project progress

Chapter 6 D̃₅ cascade: construction (#2803), helpers (#2835, #2843,
#2854), wave-60 audit (#2858 / PR #2861), wave-60 cleanup hoists
(#2862, #2863), wave-60 cleanup audit (this — #2864) all landed. The
canonical-orientation branch of leaf-equality is proven inline; the
31 reversed-orientation sub-cases (#2853) and the
`d5tildeRep_kQ_isIndecomposable` stub (`FieldGenericD5Tilde.lean:846`)
remain.

## Next step

Pick up #2853 (the 31 reversed-orientation sub-cases of
`d5tildeRep_kQ_leaf_equalities`). The helper infrastructure
(`d5tilde_core_F`, `d5tilde_core3_F`, `d5tilde_gamma_containment_F`,
`embed_sum_zero_F`, `center_decomp_F`, `gammaInv_embed1_plus_embed2_F`,
`gammaInv_embed1_plus_embedNshift_F`, plus the four
`starFirst_F_starEmbed*` / `starSecond_F_starEmbed*` projection
identities) is ready. The 31 sub-cases will need 24 calls to
direction-reversed sibling lemmas — those siblings can be filed as
part of #2853 or as a separate scoping sub-issue if #2853 is too
large for one session.

## Blockers

None.
