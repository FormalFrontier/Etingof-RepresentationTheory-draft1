## Accomplished

Worker session on issue #6436 (Ch9 Problem 9.4.6 (i), Deliverable 1 of parent #6420).

Built the **arrow `S`-bimodule `V`** of a path algebra in the new file
`EtingofRepresentationTheory/Chapter9/PathAlgebraArrowBimodule.lean` (sorry-free, builds clean):

- `ArrowIndex Q` (`Σ a b, (a ⟶ b)`) with `src`/`tgt`/`toPathIndex`.
- `arrowElt` (an arrow as the length-1 basis path in `A`) and `arrowInclusion : V →ₗ[k] A`.
- `ofPath_mul_ofPath`, `vertexIdem_mul_arrowElt`, `arrowElt_mul_vertexIdem`, and the
  sum-collapsed `vertexEmbedding_mul_arrowElt` (`f s * arrowElt x = s x.src • arrowElt x`) and
  `arrowElt_mul_vertexEmbedding` (`arrowElt x * f s = s x.tgt • arrowElt x`). These realize the
  source/target `S`-actions inside `A` and are what make the boundary map `d` well-defined later.
- The two commuting `S = (Q → k)`-module structures `arrowSourceModule` / `arrowTargetModule`
  (twisted per-index scaling `wSMul`/`wModule` via `Finsupp.ofSupportFinite`), packaged as explicit
  `Module` *values* to avoid a diamond on the single carrier, plus bimodule compatibility
  `wSMul_comm`.

Wired the file into `EtingofRepresentationTheory/Chapter9.lean`.

Decomposed the remaining deliverables into successor issues and left the `Decomposed into #6480,
#6481` breadcrumb on #6436.

## Current frontier

Deliverable 1 complete. The tensor `V ⊗_S M`, the short complex, and its exactness remain.

## Overall project progress

Phase 3 formalization ongoing. Problem 9.4.6 (i) is being assembled bottom-up:
`PathAlgebraVertexSubalgebra` (S, vertexEmbedding) → `PathAlgebraInduction` (`A ⊗_S -`, adjunction,
projectivity) → `PathAlgebraArrowBimodule` (V, this PR) → [#6480 complex] → [#6481 exactness] →
#6438 discharges the `hasHomologicalDimensionLE_pathAlgebra_one` sorry in `Problem9_4_6.lean`.

## Next step

Claim #6480 (blocked until this PR merges): build `V ⊗_S M` via a type synonym selecting
`arrowTargetModule` for the tensor and retaining `arrowSourceModule` as the descended left
`S`-module (using `wSMul_comm` for the `SMulCommClass`), then the short-complex terms as
`inducedModule.obj` of `S`-modules, `ε` = counit of `inducedRestrictAdj`, `d` via
`arrowInclusion` + the `A`-action, and prove `d ≫ ε = 0` and `epi ε`.

## Blockers

None. #6480 and #6481 are labelled `blocked` on their predecessors as intended; they unblock as
each PR merges.

Note: `Chapter9/PathAlgebraInduction.lean` is not imported by the `Chapter9.lean` aggregator (only
via my new file's transitive chain it still is not — my file imports `PathAlgebraVertexSubalgebra`,
not `PathAlgebraInduction`). #6480 will need to import `PathAlgebraInduction` and should add it to
the aggregator too.
