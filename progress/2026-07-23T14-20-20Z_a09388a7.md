## Accomplished

Resolved issue #7563 (feat(Ch5 §5.25): expose the principal-series dimension q + 1).

- Added the public endpoint `Etingof.GL2.principalSeries_finrank` in
  `Chapter5/Theorem5_25_2.lean`, stating
  `Module.finrank ℂ (principalSeries p n chi1 chi2).V = p ^ n + 1`
  (i.e. `dim V(χ₁, χ₂) = q + 1` for `q = |𝔽_q| = p^n`). It reuses the existing
  private `principalSeriesSubmodule_finrank` computation as an implementation input
  (via a definitional `change`), without weakening the conclusion.
- Added `Chapter5/Theorem5_25_2_Test.lean`, a downstream import/`#check` client that
  states and proves the dimension through the normal Chapter 5 API, demonstrating no
  private-namespace access is needed. Wired it into `Chapter5.lean`.
- Updated `progress/items.json` `Chapter5/Discussion_5.25.3` to
  `fidelity: full`, `coverage: covered_full`; removed the `fidelity_issue: 7563` marker.

Verification: `lake build EtingofRepresentationTheory.Chapter5.Theorem5_25_2_Test`
succeeds (8583 jobs). `#print axioms Etingof.GL2.principalSeries_finrank` reports only
`[propext, Classical.choice, Quot.sound]` — no `sorryAx`.

## Current frontier

Issue #7563 complete. PR to follow.

## Overall project progress

Stage 3 formalization ongoing. Chapter 5 §5.25 principal-series discussion dimension
endpoint now public and source-facing. Many sibling `fix(...): restore ...` issues
remain unclaimed (Chapters 2–9).

## Next step

Pick another unclaimed issue. The `fix(...): restore ...` cluster (e.g. #7555, #7559,
#7565) represents genuine regressions or missing source-faithful endpoints and is high
value.

## Blockers

None.
