## Accomplished

Resolved issue #7442 (formalize the characteristic-p nonfaithfulness of the polynomial
Weyl representation). Commit `3f461589`.

- `Chapter2/Proposition2_7_1.lean`:
  - `Etingof.iterate_derivative_eq_zero_of_charP` — over a commutative ring `k` of prime
    characteristic `p`, `(d/dt)^p Q = 0` for every polynomial `Q`. Proof: `coeff_iterate_derivative`
    writes the `m`-th coefficient as `(m+p).descFactorial p • Q.coeff (m+p)`, and
    `(m+p).descFactorial p` is divisible by `p!` (`Nat.factorial_dvd_descFactorial`) hence by `p`
    (`Nat.dvd_factorial`), so it is `0` in `k` (`CharP.cast_eq_zero_iff`).
  - `Etingof.WeylAlgebra.polyRep_y_pow_eq_zero` — `polyRep k (yᵖ) = (d/dt)^p = 0`.
- `Chapter2/FaithfulWeylModule.lean`:
  - `Etingof.WeylAlgebra.monomial_ne_zero` — a Weyl monomial `xⁱyʲ` is nonzero (from
    `monomialsLinearIndependent.ne_zero`).
  - `Etingof.WeylAlgebra.polyRep_not_injective` — `k[t]` is not faithful in characteristic `p`,
    witnessed by the nonzero `yᵖ` with zero action.
  - `Etingof.repE_injective_and_polyRep_not_injective` — faithful `E` vs non-faithful `k[t]`
    comparison in one statement.
- `progress/items.json`: `Chapter2/Discussion_faithful_example` `covered_partial → covered_full`,
  added `fidelity_decl`, removed the `followup_issue`/`fidelity_issue` for #7442.

No sorries introduced. Both files build clean:
`lake build EtingofRepresentationTheory.Chapter2.Proposition2_7_1` and
`...FaithfulWeylModule` succeed (only pre-existing deprecation warnings in `Proposition2_7_1.lean`).

## Current frontier

Issue #7442 complete. The three claims of the discussion after Proposition 2.7.1 (char-0 k[t]
faithful, char-p k[t] not faithful, E faithful in every characteristic) are all public theorems.

## Overall project progress

Stage 3 formalization ongoing. This was a completeness-audit gap fill (documented-only endpoint
now exposed as public theorems). Many `feature`/`completeness-audit` issues remain unclaimed.

## Next step

Create the PR for #7442 and exit. Other high-value definition-fidelity issues still open:
#7443 (progenerator finite vs arbitrary), #7437 (Young projectors), #7439 (algebraic closure of ℚ).

## Blockers

None.
