## Accomplished

Closed issue #4782 (directive #4777, S2a): **finitely many orbits ⟹ a
Zariski-dense orbit**. Added `EtingofRepresentationTheory/Chapter6/Problem6_1_5_DenseOrbit.lean`
with three sorry-free results:

- `exists_dense_of_iUnion_closure`: pure-topology core — an irreducible space
  covered by the closures of a finite family `A i` has one dense member. Proof
  via `isIrreducible_iff_sUnion_isClosed` (an irreducible space that is a finite
  union of closed sets equals one of them).
- `exists_dense_orbit`: specialisation to `MulAction G V` with
  `[Finite (orbitRel.Quotient G V)]` on an irreducible `V`, conclusion phrased on
  the orbit quotient.
- `exists_dense_orbit_point`: the point form `∃ x, Dense (orbit G x)` — exactly
  the hypothesis S2b (#4783) consumes (dense orbit of a point gives the dominant
  orbit map `G → W`, `g ↦ g • x`).

Stated abstractly (any `TopologicalSpace`, any `MulAction`) so it lands without S0
but applies to `W(m)` with the `G(m)`-action once S0 is in place. Wired into
`Chapter6.lean` (aggregation root; CI globs modules directly so this is for
convention only).

## Current frontier

S2a is done and sorry-free. The orbit chain still needs S2b (#4783, dense orbit ⟹
field embedding) and S2c (Problem 6.1.1 — `Problem6_1_1.lean` exists), then S1
(finite type ⟹ finitely many orbits, partly via S0 #4780 which is still claimed)
and S4 (#4786 wire + delete).

## Overall project progress

Chapter 6 infinite-type direction is mid-redirect to the orbit-counting argument
(directive #4777). Step 3 (`Problem6_1_5_PosDef.lean`) and now step 2(a)
(`Problem6_1_5_DenseOrbit.lean`) exist. Steps 2(b), 2(c)-wiring, 1, and 4 remain.

## Next step

Pick up S2b (#4783): from `exists_dense_orbit_point` build the injective comorphism
`k[W] ↪ k[G]` and lift to fraction fields, packaging the conclusion as the
hypothesis of Problem 6.1.1 (S2c). It depends on S0 (#4780) for the concrete
`W(m)`/`G(m)` setup, so confirm S0's status first.

## Blockers

None for S2a. Project-state note (not introduced by this work): the dead
construction-track file `Chapter6/FieldGenericD5Tilde.lean` is **broken on main**
(genuine `Unknown identifier heq0*` errors, not just sorries), so the per-module CI
sweep is red independent of this PR. That file is slated for deletion by S4 (#4786).
This PR's new file builds clean and standalone.
