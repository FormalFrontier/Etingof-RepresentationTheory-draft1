## Accomplished

Proved `alternatingKostka_norm_sq_eq_one` (∑ L²_{νλ} = 1) in Theorem5_15_1.lean, completing issue #1698. The proof assembles three ingredients:

1. **hY (Young's rule expansion)**: θ(σ) = ∑_ν L_ν · χ_ν(σ), via `coeff_eq_youngsRule_expansion` and `alternatingKostka_eq_cast`

2. **hP (Parseval identity)**: n!·∑L² = ∑_σ θ(σ)·θ(σ⁻¹), by expanding θ via hY, applying `specht_char_inner` (character orthonormality ∑_σ χ_ν(σ)·χ_μ(σ⁻¹) = n!·δ_{νμ}), and simplifying the Kronecker delta

3. **hC (Cauchy identity)**: ∑_σ θ(σ)² = n!, by expanding θ² as a double alternating sum, applying `powerSum_bilinear_coeff` to each inner sum ∑_σ c_π(σ)·c_τ(σ), factoring out n!, and applying `vandermonde_cauchy_diagonal`

4. **Assembly**: n!·∑L² = ∑θ·θ⁻¹ [hP] = ∑θ² [cycle type invariance hI] = n! [hC], cancel n!

Also fixed:
- `spechtModuleFDRep_iso_iff_eq` proof (line 467) broken by Mathlib API change (`LinearEquiv.conj_apply` behavior)
- `specht_char_inner` decidable instance mismatch (Classical vs Nat.instDecidableEqPartition)

## Current frontier

- `grep -c sorry Theorem5_15_1.lean` returns 0
- File builds cleanly: `lake build EtingofRepresentationTheory.Chapter5.Theorem5_15_1`
- 2 accepted sorrys remain in PowerSumCauchyBilinear.lean: `card_sigma_fiberPerm_eq_factorial_mul` and `vandermonde_cauchy_diagonal`

## Overall project progress

- Theorem5_15_1.lean is sorry-free (was 2 sorrys before this work)
- PowerSumCauchyBilinear.lean has 2 sorrys (bridge lemmas)
- ~41 sorries remain project-wide (down from 43)

## Next step

- The 2 sorrys in PowerSumCauchyBilinear.lean are independent proof obligations:
  - `card_sigma_fiberPerm_eq_factorial_mul`: combinatorial counting lemma
  - `vandermonde_cauchy_diagonal`: Vandermonde-Cauchy determinant identity
- These should be separate feature issues

## Blockers

None
