## Accomplished

Feature session on #4783 (Ch6 directive #4777, S2b: dense orbit ⟹ field embedding
`k(W) ↪ k(g_pq)`). Landed the **downstream half** of Etingof Problem 6.1.2(b) —
the field-embedding bridge — sorry-free in a new file
`EtingofRepresentationTheory/Chapter6/Problem6_1_5_FieldEmbedding.lean`:

* `exists_field_embedding_of_injective`: general bridge. An injective comorphism
  `φ : k[x₁..x_N] → B` into a domain `B` whose fraction field is `k`-isomorphic to
  `k(g₁..g_M)` gives an injective `k`-algebra hom `k[x₁..x_N] → k(g₁..g_M)`
  (`= FractionRing (MvPolynomial (Fin M) k)`), the exact hypothesis Problem 6.1.1
  (`n_le_m_of_injective_to_rationalFunctions`) consumes.
* `exists_field_embedding_of_injective_mvPolynomial`: polynomial-action case
  (`B = k[g₁..g_M]`), for a single `GLₘ` acting algebraically (Problem 6.1.2 proper).
* `exists_field_embedding_of_injective_isLocalization`: principal-open case
  (`B` a localization of `k[g₁..g_M]`, e.g. `k[g_pq, det⁻¹]`), the form `W(m)` with
  the base-change `g_j·M·g_i⁻¹` action needs. The principal-open function-field
  identification is `IsFractionRing.isFractionRing_of_isDomain_of_isLocalization`
  (no sorry — the issue had flagged this as the allowed one-sorry spot).
* `dim_le_of_injective_comorphism` / `..._isLocalization`: the payoff `N ≤ M`,
  composing the bridge with Problem 6.1.1.

Wired into `Chapter6.lean`. `lake build` of the new module is green, **zero
sorries, zero warnings**.

## Current frontier

The bridge takes an *injective comorphism* (= the book's Zariski-density of the
orbit, 6.1.2(a)) as input. The **upstream half** — constructing the concrete
orbit-map comorphism on `repSpace`/`repGroup` and proving it injective from S2a's
*topological* density — was not built: S0 only set up an abstract `MulAction` and
S2a only an abstract `TopologicalSpace`, so the concrete Zariski topology,
coordinate rings, generic-matrix comorphism (with `det⁻¹`), and the
topological→algebraic density conversion (Nullstellensatz flavour over alg-closed
fields) are all still missing. This is genuinely large and was decomposed.

## Overall project progress

Directive #4777 orbit chain: S0 (#4801) ✓ merged, S1b (#4798) claimed/in-progress,
S2a (#4782) ✓ merged, **S2b downstream (this PR)** ✓, S2b upstream (#4803, filed)
pending, S3 (`Problem6_1_5_PosDef`) present, S4 (#4786) final assembly pending.
Theorem_6_1_5 forward direction still has its single `sorry` awaiting S4.

## Next step

Pick up #4803: build the concrete orbit-map comorphism via `MvPolynomial.aeval`
into the `det⁻¹` localization and prove injectivity from the dense orbit, then feed
`dim_le_of_injective_comorphism_isLocalization` to get `dim W(m) ≤ dim G(m)`. The
`Problem6_1_5_FieldEmbedding` module docstring spells out the interface to hit.

## Blockers

None hard. The topological→algebraic density conversion in #4803 may need
Nullstellensatz-style polynomial-evaluation faithfulness that could be missing
from Mathlib in the required form; if so it gets isolated as its own lemma with a
filed follow-up.
