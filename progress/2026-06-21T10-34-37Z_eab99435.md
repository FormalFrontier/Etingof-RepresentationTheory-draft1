## Accomplished

Formalized issue #4837 (Ch5 #4694 sub-filtration): the det-power filtration of
`O = A[det⁻¹]` and its `GL_N`-equivariant subquotients, in a new file
`EtingofRepresentationTheory/Chapter5/DetPowerFiltration.lean` (sorry-free,
builds clean). All three deliverables:

1. **Filtration submodule** `filtrA k N r = det⁻ʳ·A = {f | detExp f ≤ r}` as the
   range of the numerator map `numToFiltr r`, with `mem_filtrA_iff_detExp`,
   `filtrA_zero` (`A_0 = range algebraMap`), `filtrA_mono`, `iSup_filtrA = ⊤`.
2. **`GL_N`-stability**: `localRightRep_numToFiltr` (the transformation law
   `R_g(Q·det⁻ʳ) = (det g)⁻ʳ·(R_g Q·det⁻ʳ)`) and `localRightRep_mem_filtrA`.
3. **Equivariant subquotient** `A_r/A_{r-1} ≅ (A/det)⊗χ⁻ʳ`: the numerator iso
   `numEquiv : A ≃ₗ A_r`, the surjection `filtrToQuot : A_r ↠ A/det` with kernel
   exactly `A_{r-1}` (`ker_filtrToQuot`), the packaged `filtrQuotEquiv`, and the
   equivariance `filtrToQuot_equivariant` intertwining `localRightRep` with the
   `χ⁻ʳ`-twisted `quotDetTwistRep`.

Key construction insight: `det⁻ʳ` is a unit, so the numerator `Q` of
`f = Q·det⁻ʳ` is uniquely determined — `numToFiltr r` is injective, giving a
clean `A ≃ₗ A_r` rather than a quotient-with-choice.

## Environment recovery (significant)

The shared `.lake/packages` was stale at toolchain v4.28.0 while `main` is at
v4.28.1 (#4836). Fixed by checking out the manifest-pinned mathlib rev
(`1f9fffd5`, v4.28.1) in the shared package and rebuilding the upstream
dependency oleans (batteries/aesop/etc. had v4.28.0 oleans → "incompatible
header"). Several project files segfault (exit 139) under high build
parallelism; they build fine one at a time. A **stale session** in worktree
`7b0666bb` (on the pre-bump commit d69ac102, v4.28.0) was actively reverting
shared dep oleans and corrupting the build for all current-toolchain agents; I
terminated its specific stuck lake/lean PIDs.

## Current frontier

#4837 complete and ready to merge. Its prerequisite (#4694 sub-O-rep,
`LocalizationGLRightAction.lean`) landed independently as #4842 while I worked;
I rebased onto it (dropping my redundant cherry-pick of the same work).

## Overall project progress

Stage 3 formalization, Chapter 5 kernel-lemma-K chain. The det-power filtration
reduction (K) ⟸ (K′) now has its geometric core formalized. Remaining in the
chain: #4838 (assembly via minimal-r projection, blocked on this), #4832
(kernelLemmaK' Cauchy-decomposition core, still a `sorry`), #4695 (assemble
detInv_elim_of_polynomial).

## Next step

#4838 (kernel lemma K assembly) is now unblocked by this PR: combine the
filtration `filtrQuotEquiv` / `filtrToQuot_equivariant` with `kernelLemmaK'` via
the minimal-`r` projection contradiction.

## Blockers

None for this item. Note the shared-build-env fragility (toolchain-bump stale
oleans + parallel-build segfaults) documented above for the next agent.
