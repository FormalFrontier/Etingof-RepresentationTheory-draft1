## Accomplished
Landed **deliverable 1** of #6510 (path-length grading crux infra): new file
`Chapter9/PathAlgebraLengthGrading.lean`, sorry-free, warning-free.
- `pathLen` + `pathLen_comp` / `pathLen_comp_arrow` (length arithmetic).
- `lengthGrading : (QuiverPathIndex Q →₀ k) →ₗ[k] (ℕ →₀ PathAlgebra k Q)` — the length-graded
  coordinate map, noncommutative analogue of `coordMapCH`.
- `lengthTotalize` (reassembly, left inverse) ⟹ `lengthGrading_injective`.
- `lengthProj n` homogeneous projections + `lengthProj_single`, `lengthProj_single_self`,
  `lengthProj_lengthProj` (idempotence).
- Cons seed: `ofPath_mul_arrowElt` (`p·e = cons p e`), `lengthProj_ofPath_mul_arrowElt`
  (right-mult by an arrow shifts degree +1).
Also merged fully-green PR #6507 at the start of the cycle.

## Current frontier
Deliverables 2 (bundled cons-splitting iso `A_n ⊗_S V ≅ A_{n+1}`) and 3 (transport the grading to
`A ⊗_S M` + the `d` top-degree/shift coordinate lemma) are NOT done — they are a large,
interdependent construction (full `ArrowTgt`/`VtensCarrier` type-synonym dance at the graded
level). Decomposed into successor issue **#6514**.

## Overall project progress
Stage 3 formalization ongoing. #6510 was the "crux infra" for the Ch9 standard resolution
(Problem 9.4.6 i); deliverable 1 (grading of `A`) now landed. #6512 (SES assembly) and #6438
(homological dimension) remain blocked pending #6514 (deliverables 2-3).

## Next step
Claim **#6514**: build the cons-splitting `≃ₗ` using `ofPath_mul_arrowElt` + Mathlib's
`length_ne_zero_iff_eq_cons`/`comp_inj` for the inverse, then transport `lengthProj` across
`TensorProduct.map (lengthProj n) id` and package the `d`-shift lemma, mirroring
`koszulSES_shortExact`.

## Blockers
None. #6514 is scoped and ready once this partial PR lands.
