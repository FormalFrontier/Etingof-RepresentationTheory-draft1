## Accomplished

Session type: feature worker (`/work` → `/feature`).

- Claimed #4974 (general-k Specht character bridge), assessed it as multi-session
  (a genuinely hard field-independence/injectivity core plus a voluminous mechanical
  ℂ→k generalization of ~15 private bridge lemmas, 2 public theorems, and a wrapper in a
  3600-line file), and **decomposed** it into:
  - **#4991** — general-k Specht character API + field-independence + injectivity (foundational);
  - **#4992** — generalize the Specht bridge + `trace_symGroupAction_eq_spechtModuleCharacter`
    + `simpleSubmodule_iso_of_spechtCharacter_eq` + wrapper over k (depends on #4991).
  Left the `Decomposed into #4991, #4992` breadcrumb on #4974 and `coordination skip`ped it.
- Claimed and **fully completed #4991** (no sorries). New file
  `Chapter5/SpechtCharacterGeneral.lean`:
  - `spechtModuleActionK` / `spechtModuleCharacterK k n la σ : k` — left-mult-by-`of σ`
    trace on `SpechtModuleK k n la = k[S_n]·c_λ`;
  - `spechtModuleCharacterK_eq_intCast` — field-independence: via the idempotent `α⁻¹·c_λ`
    and `LinearMap.trace_restrict_eq_of_forall_mem`, the character equals `(N₀:k)⁻¹·(M₀:k)`
    with `N₀ = (c_λ·c_λ)(1)` and `M₀ = ∑_τ c_λ(τ⁻¹σ⁻¹τ)` fixed integers from the
    ℤ-coefficients of `c_λ`;
  - `spechtModuleCharacterK_algebraMap_rat` — `= algebraMap ℚ k (spechtModuleCharacterK ℚ …)`;
  - `spechtModuleCharacterK_complex` — ℂ specialisation recovers `spechtModuleCharacter`
    (via `LinearEquiv.ofEq` + `trace_conj'` over the equal submodule `SpechtModuleK ℂ = SpechtModule`);
  - `spechtModuleCharacterK_injective` — equal `σ`-characters ⟹ equal labels, over any
    char-0 field, by transferring the ℂ injectivity through field-independence.
  - Wired into `Chapter5.lean` aggregator so CI builds it.

## Current frontier

#4991 complete and committed (`04676fea`). PR pending (this turn). #4992 is the next
piece: mechanical ℂ→k generalization of the Specht bridge in `Theorem5_22_1.lean` and the
`SchurModuleSpecialBlock.lean` wrapper, now consuming `spechtModuleCharacterK`.

## Overall project progress

Stage 3 (formalization), Chapter 5 general-k generalization track. The parent #4946
(`schurModule_isSimple_general`) chain: Sub-A (#4973) merged; Sub-B now split into #4991
(done) and #4992 (open, depends on #4991); Sub-C (#4975) and Sub-D (#4976) remain blocked
downstream.

## Next step

A worker should claim **#4992** once #4991's PR merges. Note the likely import-cycle:
`SpechtCharacterGeneral` imports `Infrastructure.SpechtModuleSimple` which imports
`Theorem5_22_1`, so the general bridge cannot live *inside* `Theorem5_22_1`; put the
general-k bridge results in a new file downstream of both `Theorem5_22_1` and
`SpechtCharacterGeneral` (or relocate `SpechtModuleK`). The character API to consume:
`spechtModuleCharacterK`, `spechtModuleCharacterK_injective`, `spechtModuleCharacterK_complex`.

## Blockers

None for #4991 (complete). #4992 is unblocked once #4991 merges.
