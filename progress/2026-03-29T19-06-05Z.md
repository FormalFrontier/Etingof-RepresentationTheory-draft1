## Accomplished

Structural proof for `inducedRepV_simple` (Theorem 5.27.1 part i-a): proving that the induced representation V_{(O,U)} is irreducible.

**Proof structure completed:**
1. Given a subrepresentation σ ≠ ⊥ of the induced representation, find a nonzero function f ∈ σ
2. Use the A-action (character eigenspace decomposition) to isolate a function g ∈ σ supported on a single coset q₀
3. Translate via ρ(1, q₀⁻¹) to get g₁ ∈ σ supported on q₁ = [1], so g₁ = Pi.single q₁ (g₁ q₁)
4. Show the set S = {v | Pi.single q₁ v ∈ σ} is a subrepresentation of U (invariant under ρ_U)
5. S ≠ ⊥ since g₁ q₁ ∈ S; by simplicity of U, S = ⊤
6. Transport via G-action: for any coset q and any u ∈ U, Pi.single q u ∈ σ
7. Every function is a sum of Pi.single terms → σ = ⊤

**Added lemma:** `isIrreducible_of_simple_fdRep` — bridge from `CategoryTheory.Simple U` to `Representation.IsIrreducible (FDRep.ρ U)` (sorry'd, needs categorical plumbing through FDRep → Rep → ModuleCat k[G] equivalence chain).

## Current frontier

File: `Theorem5_27_1.lean` — 4 sorrys remain:
1. **Line 683**: `isIrreducible_of_simple_fdRep` — bridge lemma (categorical plumbing needed)
2. **Line 847**: `h_conj_act` equality — `Pi.single` DecidableEq instance diamond between `Quotient.decidableEq` and `QuotientGroup.instDecidableEq`. Mathematically trivial (conjugation action preserves single-coset support), blocked by Lean type system issue.
3. **Line 958**: `inducedRepV_orbit_injectivity` (part ii, untouched)
4. **Line 969**: `inducedRepV_completeness` (part iii, untouched)

## Overall project progress

- Theorem5_27_1.lean: 4 sorrys (was 3 before; structural proof for part (i) added, which needs 2 helper sorrys)
- The irreducibility proof strategy is fully laid out and verified structurally
- Key blocker is the Pi.single DecidableEq diamond, which affects many proofs in this file

## Next step

1. **Fix the Pi.single DecidableEq diamond** (line 847): Either add a `Subsingleton.elim` based conversion lemma, or use `open scoped Classical` earlier in the proof to normalize instances, or refactor to avoid Pi.single entirely (use explicit if-then-else functions).
2. **Prove isIrreducible_of_simple_fdRep** (line 683): Construct FDRep monos from Subrepresentations and use Simple U. Key insight: submodules of finite-dim modules are finite-dim, so every Subrepresentation gives an FDRep mono.
3. Parts ii and iii (injectivity and completeness) are independent and can be tackled separately.

## Blockers

- Pi.single `DecidableEq` diamond between `Quotient.decidableEq` and `QuotientGroup.instDecidableEq` is a systematic issue affecting all proofs that manipulate `Pi.single` on quotient group cosets.
