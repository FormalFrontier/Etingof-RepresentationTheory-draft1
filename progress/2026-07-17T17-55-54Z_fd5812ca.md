## Accomplished

Partial completion of #6911 (`affine_tree_two_branch_iso`, two-branch ⟹ D̃ₙ). The **assembly
half** is done: `affine_tree_two_branch_iso` in
`Chapter6/Problem6_1_3_continued_tildeE.lean` (~line 2758) is now **sorry-free**. It wires:

- `affine_two_branch_has_leaf` (~2459) → a leaf `ℓ` adjacent to one of the two branch vertices
  `v, w`;
- a small degree-1-uniqueness fact (`huniqueN`) giving `ℓ`'s unique neighbour;
- the new helper `affine_two_branch_deleted_isD` (the classification crux, `sorry`) →
  `σ'` identifying the leaf-deleted survivor with the finite `Dₖ`, with the reattach point at
  `Dₖ`-index 1;
- the reindexing engine `affine_two_fork_reindex` (#6912) → the final `σ : Fin (Dtilde k).rank ≃
  Fin (k+1)`.

Both leaf-attachment cases (leaf at `v` vs at `w`) are handled by a `v/w` swap.

`lake build EtingofRepresentationTheory.Chapter6.Problem6_1_3_continued_tildeE` completes
(8586 jobs). No new lint warnings introduced.

## Current frontier

The single remaining `sorry` in this pipeline is isolated in
`affine_two_branch_deleted_isD` (~line 2734): from the affine two-branch data + a fork-leaf,
delete the leaf, classify the survivor via `branch_classification`, **rule out the E-types via
affine degeneracy**, and localize the reattach point at `Dₖ`-index 1. Filed as sub-issue #6922.

## Overall project progress

Stage 3, Chapter 6 Problem 6.1.3 (g). The affine Dynkin classification ⟹ direction is
structurally complete (`affine_dynkin_classification`, #6916). Remaining tree-case sorries feeding
it:
- `affine_two_branch_deleted_isD` — the E-ruling-out + reattach localization (#6922, new).
- `affine_one_branch_arm_layout` (~2937) and `affine_tree_one_arm_reciprocal` (~3165) — the
  one-branch Ẽ₆/Ẽ₇/Ẽ₈ residuals (#6913 thread).

## Next step

Claim #6922 and discharge `affine_two_branch_deleted_isD`. The hard step is ruling out E: it needs
the two-branch analogue of the one-branch arm-length machinery (`affine_arm_length_solutions`,
`affine_tree_one_arm_reciprocal`) — test the degenerate Cartan form against its strictly-positive
null vector, which is linear along each arm, to force `w`'s two fork-arms to length 1 (equivalently
`w'` has two leaf-neighbours, distinguishing `Dₖ` from every E branch vertex). Consider landing
`affine_tree_two_arm_reciprocal` as its own sub-issue first if step 3 is too large.

## Blockers

None. `affine_tree_two_branch_iso`'s `#print axioms` is transitively `sorryAx` until #6922 lands.
