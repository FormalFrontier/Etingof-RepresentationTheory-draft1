## Accomplished

Worked on #6681 (projectivity of the external tensor of projectives, Problem 8.2.8). Added
`EtingofRepresentationTheory/Chapter8/ExternalTensorProjective.lean` and wired it into
`Chapter8.lean`. Landed the **retract reduction** sorry-free:

- `Etingof.extTensorRetract`: assembles a pair of retracts (`X ◁ F₁`, `Y ◁ F₂`) into a retract
  `extTensorFunctorObj X Y ◁ extTensorFunctorObj F₁ F₂`, using the existing
  `extTensorFunctorMap_comp`/`extTensorFunctorMap_id` lemmas.
- `Etingof.extTensor_projective`: `X`, `Y` projective ⇒ `(extTensorFunctor).obj X |>.obj Y`
  projective, via the free/forget counit split by projectivity + `Retract.projective`.

The single remaining `sorry` is the **base case** `extTensorFunctorObj_projective_of_free` (free ⊗
free is free over `(A₁⊗A₂)ᵐᵒᵖ`), decomposed into new issue **#6717**.

## Current frontier

The base-case free-module lemma. Key finding (documented in #6717): `finsuppTensorFinsupp` does not
apply, because `extTensorFunctorObj` gives the free modules their `k`-structure by restriction of
scalars along `k → A₁ᵐᵒᵖ` (`Module.compHom`), which is **not definitionally** the standard `Finsupp`
`k`-module (verified: `rfl` fails to identify the two `⊗[k]` types). The `(A₁⊗A₂)ᵐᵒᵖ`-linear basis
equivalence must be built directly for the external action (route (a) in #6717).

## Overall project progress

Stage 3 (formalization) ongoing. Chapter 8 Problem 8.2.8 (Künneth for Tor) is being assembled from
pieces; this issue is the fourth piece of #6667. Retract reduction done; base case → #6717.

## Next step

Claim #6717 and fill `extTensorFunctorObj_projective_of_free` (route (a): `TensorProduct.lift`
forward map + `Finsupp.linearCombination` inverse on the `compHom`-tensor, generator-wise
`(A₁⊗A₂)ᵐᵒᵖ`-linearity via `extTensorModule_op_smul_tmul` + `opAlgEquiv` multiplicativity). Once
filled, `extTensor_projective` becomes fully sorry-free.

## Blockers

None. #6681 landed partial (retract scaffolding sorry-free); base case tracked in #6717.
