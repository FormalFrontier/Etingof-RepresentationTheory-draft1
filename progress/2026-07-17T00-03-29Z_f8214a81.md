## Accomplished

Resolved the residual scope of **#6793** (Ch6 #Problem6.1.3-g tree-case assembly, step 4
of #6785). Added the lemma `affine_tree_case` to
`EtingofRepresentationTheory/Chapter6/Problem6_1_3_continued_tildeE.lean` (immediately
before the final `affine_dynkin_classification` theorem):

```lean
lemma affine_tree_case {n : ℕ} (adj : Matrix (Fin n) (Fin n) ℤ)
    (hn : 1 ≤ n) (hD : IsAffineDynkinDiagram n adj)
    (hacyc : (∑ i, ∑ j, adj i j) < 2 * (n : ℤ)) :
    ∃ t : AffineType, ∃ σ : Fin t.rank ≃ Fin n,
      ∀ i j, adj (σ i) (σ j) = t.adj i j
```

The proof is the specified trivial case split on `affine_degree_four_dichotomy` (#6853):
the degree-4 branch hands back a `D̃₄` isomorphism directly (`⟨_, σ, hσ⟩`); the
all-degrees-`≤ 3` branch discharges via `affine_tree_degree_le_three_iso` (#6854).

Statement shape chosen to match what #6785 step 5 consumes: the acyclicity hypothesis
`∑ᵢ∑ⱼ adjᵢⱼ < 2n` is exactly the complement of `affine_cyclic_case`'s
`hcyc : 2n ≤ ∑ᵢ∑ⱼ adjᵢⱼ`, and the conclusion `∃ t : AffineType, ∃ σ, ...` matches
`affine_tree_degree_le_three_iso`, so #6785 can glue the cyclic (#6792) and acyclic
(#6793) cases with a single case split on that edge-sum.

## Current frontier

`affine_tree_case` is sorry-free; the assembly logic is complete. `lake build
EtingofRepresentationTheory.Chapter6.Problem6_1_3` exits 0. `#print axioms
affine_tree_case` reports `[propext, sorryAx, Classical.choice, Quot.sound]` — the
`sorryAx` is inherited only transitively from the two still-open sub-issues:
`affine_tree_two_branch_iso` (#6881) and `affine_tree_one_branch_iso` (#6882). It
disappears automatically once those land. No new sorry was introduced.

## Overall project progress

Stage 3 formalization ongoing. Ch6 affine Dynkin classification (⟹ direction, #6785)
architecture: cyclic case (#6792), tree case (#6793, this turn ✅ residual assembly).
Tree-case sub-lemmas #6881/#6882 still claimed/open; #6785 step-5 final glue still
`sorry`. Ch6 file `Problem6_1_3_continued_tildeE.lean` now has 3 sorries (down 0 net —
this issue added an assembly lemma, no sorry): #6881, #6882, #6785-step5.

## Next step

Land #6881/#6882 (the two-branch / one-branch arm-length isos), then #6785 step 5 can
glue `affine_cyclic_case` + `affine_tree_case` into the ⟹ direction of
`affine_dynkin_classification`.

## Blockers

None for this issue — completed. Downstream #6785 remains blocked on #6881/#6882 (both
claimed).
