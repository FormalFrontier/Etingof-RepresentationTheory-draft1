## Accomplished

Work session (`768482c8`). Claimed **#7892 — triage the two unlanded Specht decls on stranded
branch `agent/13603a0c`** and completed it as a decision, not a code landing.

**Verdict: not wanted. Both unlanded decls are fully superseded on `main`.** Recorded in full
at
https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/issues/7892#issuecomment-5083166063

The branch (2 commits, 2026-03-23) touches only `Chapter5/Theorem5_15_1.lean` and one March
progress file. Comparing decl by decl against `main` at `ad71908`, as the issue instructed,
rather than trusting the merge-base diffstat:

| branch decl | status on current `main` |
|---|---|
| `spechtModuleFDRep_simple` | landed, `Theorem5_15_1.lean:141` |
| `spechtModuleFDRep_noniso` | superseded by `spechtModuleFDRep_iso_iff_eq`, `Theorem5_15_1.lean:462` |
| `spechtCharacter_orthonormal` | superseded by `specht_char_inner`, `Theorem5_15_1.lean:2335` |

`spechtModuleFDRep_noniso` asserted `IsEmpty (spechtModuleFDRep n ν₁ ≅ spechtModuleFDRep n ν₂)`
for `ν₁ ≠ ν₂`. `main` proves the biconditional
`Nonempty (spechtModuleFDRep n ν₁ ≅ spechtModuleFDRep n ν₂) ↔ ν₁ = ν₂`, whose forward direction
is the branch lemma's contrapositive — strictly stronger, and by the identical proof
(`FDRep.isoToLinearEquiv` → `FDRep.Iso.conj_ρ` → `MonoidAlgebra.induction_on` →
`Theorem5_12_2_distinct`).

`spechtCharacter_orthonormal` asserted the `⅟|Sₙ|`-normalised character inner product
`= δ_{νμ}`. `main`'s `specht_char_inner` proves the same identity with the normalisation
cleared (`∑_σ χ_ν(σ)·χ_μ(σ⁻¹) = n! · δ_{νμ}`), which is interderivable and more usable — no
`Invertible` instance needed at the call site. It is public where the branch lemma was
`private`, and already has a downstream consumer (`spechtModuleCharacter_injective`,
`Theorem5_15_1.lean:2363`).

Two facts settle the "cherry-pick anyway?" question against landing: the branch's own stated
blocker `alternatingKostka_norm_sq_eq_one` is now proved on `main` (`Theorem5_15_1.lean:2381`)
and the whole file is sorry-free, so a cherry-pick would add nothing while conflicting with a
substantially rewritten file. The branch's `progress/20260323T024200Z.md` was likewise left
unlanded: it reports "1 sorry remaining" in a file that now has zero, so as a historical
record it would mislead.

No audited remote branch was deleted, per the issue's instruction. No Lean sources changed
this session, so no build was required.

## Current frontier

#7892 is closed by this PR. The other 13 branches from the #7891 stranded-branch audit were
already spot-checked as superseded by the issue author and need no action.

One loose end, deliberately not addressed here: the issue's goal that "a later audit does not
re-flag the branch" cannot be mechanised yet. `scripts/list_stranded_branches.py` has not
landed — it is still in
https://github.com/FormalFrontier/Etingof-RepresentationTheory-draft1/pull/7895
(`meditate: recover commits stranded on dead agents' local branches`), currently
`CONFLICTING`. There is no allowlist file to annotate today, and creating one here would
collide with that PR. `origin/agent/13603a0c` will keep appearing in the audit output until
#7895 lands; the issue comment is the verdict a future auditor should find.

## Overall project progress

Stage 3 (formalization) remains the active stage; this session touched no Lean. `Chapter5/
Theorem5_15_1.lean` is sorry-free, and the Frobenius character formula chain in it is complete
— worth noting because the stranded branch's March-era handoff still describes it as blocked.

Housekeeping at session start: ran the merge sweep over open PRs. Ten PRs (#7902, #7901,
#7900, #7899, #7898, #7897, #7890, #7875, #7856, #7762) report `MERGEABLE` with no failing
checks but are refused by the base-branch policy because required checks are still pending.
Auto-merge is already enabled on them by `coordination create-pr`, so they will land on their
own; no intervention needed.

## Next step

Unchanged from the previous handoff, and still the highest-leverage target: **#7882**
(exactness of the Koszul complex, Problem 8.2.10(i)) — four of the six Chapter 8 Koszul
children hang off it. It is `blocked` on #7881, whose data half landed in PR #7888.

Separately, #7895 needs a repair pass for its merge conflict before the stranded-branch audit
tooling is usable; that is a `repair` agent's job, not a worker's.

## Blockers

None. This session produced a decision and a progress file, with no failed verification.
