## Accomplished
Corrected and proved `pole_order_diophantine` (Ch4 #Problem4.12.8-a-iii, issue #6842).
The statement on `main` was mathematically false (dihedral disjunct listed as
`m = {2, 2, n}` instead of `m = {2, 2, n/2}` with `n` even). Replaced that disjunct
with `∃ k, n = 2 * k ∧ m = {2, 2, k}`, updated the surrounding docstring, and proved
the lemma sorry-free via the self-contained arithmetic route:
- Each summand `1 - 1/mᵢ ∈ [1/2, 1)`; the lower bound forces `card m ≤ 3`, and the
  `card 0/1` cases are excluded (sum would drop below 1), so `card m ∈ {2, 3}`.
- `card 2` (via `Multiset.card_eq_two`): `1/a + 1/b = 2/n` with `a, b ∣ n` forces
  `a = b = n`, giving `{n, n}`.
- `card 3` (via `Multiset.sort`/`pairwise_sort` for `a ≤ b ≤ c`): smallest order is 2,
  middle ∈ {2, 3}; `b = 2` gives the dihedral family `n = 2c`, `b = 3` gives
  `c ∈ {3, 4, 5}` → `{2,3,3}/{2,3,4}/{2,3,5}`.

`#print axioms` limited to `propext, Classical.choice, Quot.sound`.

## Current frontier
`pole_order_diophantine` is complete. Milestones (ii) `isCyclic_of_common_fixed_vector`
and the assembly `so3_classification_aux` remain `sorry` (tracked by their own issues).

## Overall project progress
Stage 3 formalization ongoing. Ch4 Problem 4.12.8 part (a) milestones (i) and (iii) are
now proved sorry-free; (ii) and the final assembly remain.

## Next step
Downstream assembly issue #6836 (`so3_classification_aux`) consumes this lemma; it should
now use the corrected dihedral clause `{2,2,k}` (`n = 2k`) → `G ≃* DihedralGroup k`.

## Blockers
None.
