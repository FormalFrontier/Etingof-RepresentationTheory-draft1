## Accomplished

Decomposed issue #2960 (Phase 2 Case C.short residual: `vertexDegree adj x = 1`
in `non_adjacent_branches_leaf_case_per_kQ`) into three sub-issues after
analysing that the single `sorry` cannot be closed with existing per-(F, Q)
forbidden-subgraph helpers:

- **#2963** (tractable cases): the Ẽ₇ dispatch for `x.deg = 3` and the
  T(1,2,5) dispatches for `x.deg = 1 ∧ chain.length = 5` and
  `x.deg = 1 ∧ chain.length = 4` with at least one of `arm₁.deg = 2` or
  `arm₂.deg = 2`. Three dispatches against existing embedders
  (`embed_etilde7_in_tree_per_kQ`, `embed_t125_in_tree_per_kQ`).

- **#2964** (new helper): `d7tilde_not_finite_type_per_kQ` — the
  per-(F, Q) infinite-type leaf for D̃₇ (8-vertex extended Dynkin
  diagram with two non-adjacent degree-3 vertices). No such leaf
  exists on `main`. Independent; mirrors the structure of
  `FieldGenericD5Tilde.lean`.

- **#2965** (residual fill): use the new helper from #2964 to close
  the all-leaves residual (chain.length = 4 ∧ x.deg = 1 ∧
  arm₁.deg = arm₂.deg = 1). Depends on #2963 (for the file structure)
  and #2964 (for the leaf helper).

Parent #2960 was `skip`ped via `coordination skip` with a breadcrumb
comment pointing at the three sub-issues; the parent now carries the
`replan` label for the next planner cycle to close it.

## Current frontier

`Chapter6/FieldGenericNonAdjacentBranches.lean:707` still carries the
original Case C.short sorry (no code changes this session); the file
also has the Case E sorry at line 781 (tracked by #2955). All Phase 2
case-split scaffolding (Cases A, B, C.main, D) is in place from
earlier sessions.

The next planner cycle should triage parent #2960 (close it, since
sub-issues fully cover the scope). Workers can then claim #2963 and
#2964 in parallel; #2965 is `blocked` on both.

## Overall project progress

* Stage 3 (Lean formalization, Chapter 6) ongoing.
* `non_adjacent_branches_leaf_case_per_kQ` Phase 2 case-split:
  Cases A, B, C.main, D landed; Case C.short and Case E still open.
* The post-#2963/#2964/#2965 landing closes Case C.short entirely;
  Case E remains open via #2955.

## Next step

Workers can claim:

- **#2963** — implement the three tractable dispatches (~250-350 lines
  of Lean). Self-contained; uses only existing per-(F, Q) embedders.
- **#2964** — port the D̃₇ infinite-type structure (~1000-1500 lines
  based on the D̃₅ precedent). Independent of #2960's other sub-issues.

#2965 is `blocked` on both above; the next planner cycle will unblock
it automatically once they close.

The next planner cycle should also close parent #2960 (the
`Decomposed into …` breadcrumb on the parent triggers the standard
replan-triage path).

## Blockers

None for the sub-issues individually (each has self-contained
deliverables). #2965 is `blocked` on #2963 and #2964 — this is normal
dependency tracking, not a true blocker.
