## Accomplished

Completed issue #6616 (Problem 9.4.6 (i), path algebra): the path algebra of a quiver with at
least one edge has homological dimension exactly `1`.

- New file `EtingofRepresentationTheory/Chapter9/PathAlgebraLowerBound.lean`:
  - `comp_eq_some_nil_iff`, `comp_eq_some_self_iff` — length/endpoint bookkeeping for path
    concatenation producing a trivial path.
  - `coeffAt` (= `Finsupp.lapply`) with `coeffAt_single` / `coeffAt_compSingle` — coefficient
    access on `PathAlgebra` products without unfolding the semireducible `def`.
  - `augHom b : PathAlgebra k Q →+* k` — the augmentation at a vertex `b` (coefficient at the
    trivial path `e_b`); multiplicativity via `comp_eq_some_nil_iff`.
  - `augModule` — the simple/augmentation module `S_b`, realised on `ULift.{u+1} k` (the path
    algebra lives in `Type (u+1)`, so its module category is at `Type (u+1)`).
  - `not_hasHomologicalDimensionLE_zero_pathAlgebra` — the lower bound (non-semisimplicity):
    if `S_b` were projective, the split surjection `A ↠ S_b` would give a section `s` with
    `x · s(1) = 0` for the edge `x`, yet the `x`-coefficient of `x · s(1)` equals `ε_b(s(1)) = 1`.
- `Problem9_4_6.lean`: filled the `sorry` in `homologicalDimension_pathAlgebra_eq_one` with
  `homologicalDimension_eq_one_of_not_le_zero` applied to the upper bound and the new lower bound.

Both `not_hasHomologicalDimensionLE_zero_pathAlgebra` and `homologicalDimension_pathAlgebra_eq_one`
are sorry-free (`#print axioms`: only `propext`, `Classical.choice`, `Quot.sound`).

## Current frontier

Chapter 9, Problem 9.4.6. Part (i) path-algebra statement is complete.

## Overall project progress

Stage 3 (formalization) ongoing. This turn closed the path-algebra half of Problem 9.4.6 (i).

## Next step

`homologicalDimension_freeAlgebra_eq_one` (Problem9_4_6.lean:117) remains `sorry` — explicitly
out of scope for #6616. It is the free-algebra corollary (`FreeAlgebra k (Fin n)` = the path
algebra of the one-vertex `n`-loop quiver); the natural follow-up is a ring iso
`FreeAlgebra k (Fin n) ≃ PathAlgebra k (one-vertex n-loop quiver)` transported through
`homologicalDimension_pathAlgebra_eq_one`. Also Problem 9.4.6 (ii) Cartan matrix `hcover`
hypothesis discharge remains.

## Blockers

None.

## Key patterns

- `PathAlgebra k Q : Type (u+1)` (the quiver path type bumps the universe), so modules for the
  homological-dimension predicate must be built at `Type (u+1)` — use `ULift.{u+1} k`.
- `PathAlgebra` is a semireducible `def` with no `FunLike`, so `a x` does not elaborate for
  `a : PathAlgebra`; route coefficient access through the `Finsupp.lapply`-backed `coeffAt`.
- `rw` on a `Prop` inside an `if` whose `Decidable` instance depends on the term fails with a
  motive error; use `simp only [the_iff_lemma]` instead.
