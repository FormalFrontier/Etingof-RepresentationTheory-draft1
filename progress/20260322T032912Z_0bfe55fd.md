## Accomplished

1. **Structured the proof of `alternating_kostka_eq_zero_of_strict_dom`** (the last sorry in Theorem 5.15.1):
   - Defined `alternatingKostkaInt` (ℤ-valued alternating Kostka sum)
   - Proved `alternatingKostka_eq_cast` (bridge between ℂ and ℤ versions)
   - Proved `alternatingKostka_diag` (diagonal case L_{λλ} = sign(rev))
   - Proved the integer arithmetic conclusion: if ∑_ν L²_{νλ} = 1 and L²_{λλ} = 1, then L_{νλ} = 0 for ν ≠ λ

2. **Reduced the problem to a single clean mathematical statement**: `alternatingKostka_norm_sq_eq_one` — the norm-squared identity ∑_ν L²_{νλ} = 1

## Current frontier

- **File:** `EtingofRepresentationTheory/Chapter5/Theorem5_15_1.lean`
- **Remaining sorry:** `alternatingKostka_norm_sq_eq_one` (line ~2141)
- The sorry count in the file is unchanged (1), but the sorry is now a clean, well-documented mathematical statement rather than an opaque placeholder

## Overall project progress

- Theorem 5.15.1 (Frobenius character formula) is blocked on one sorry: the norm-squared identity
- All other sorry chains in the theorem are resolved through the new structural proof
- The norm-squared identity requires three pieces of infrastructure:
  1. **FDRep bridge**: Construct `Representation ℂ (Equiv.Perm (Fin n))` from `spechtModuleAction`, wrap in `FDRep`, prove `Simple`, prove character matches `spechtModuleCharacter`
  2. **Character orthonormality for Specht modules**: Apply Mathlib's `FDRep.char_orthonormal` via the bridge to get (1/n!) ∑_σ χ_λ(σ) χ_μ(σ⁻¹) = δ_{λμ}
  3. **Vandermonde coefficient orthogonality**: Prove (1/n!) ∑_σ |[x^{λ+ρ}](Δ·P_σ)|² = 1

## Next step

- Create a sub-issue for the FDRep bridge (deliverable 1 above) — this is reusable infrastructure
- Create a sub-issue for Vandermonde coefficient orthogonality (deliverable 3) — this is a purely polynomial identity
- The norm-squared identity follows from combining these with the expansion θ_λ = ∑_ν L_{νλ} χ_ν (which is already proved via Young's Rule + Theorem 5.14.3, non-circular)

## Blockers

- FDRep bridge needs `Simple` instance, but `FDRep.simple_of_isSimpleModule_asModule` in IrreducibleEnumeration.lean is `private` — either make it public or duplicate the proof
- Vandermonde orthogonality requires non-trivial polynomial coefficient computation
