## Accomplished
- Bumped toolchain + Mathlib to **v4.31.0** (final). Full build green (0 errors, 8909 jobs).
- Migrated v4.31.0-final Mathlib API changes:
  - **`LieRing.ofAssociativeRing` demoted from a global instance to file-local in Mathlib** — `LieRing (Module.End k V)` / `End ℂ V` no longer synthesized. Re-enabled per file via `attribute [local instance] LieRing.ofAssociativeRing` (Example7_6_3, Theorem2_1_1, Theorem5_18_2). This also cleared a cascading `rcases`-not-inductive error.
  - **`SimpleGraph.symm` field is now `Std.Symm Adj`** (was `Symmetric Adj`; `IsSymm` is deprecated → `Std.Symm`, whose field is `symm : ∀ a b, r a b → r b a`, explicit binders). Wrapped the raw lambdas in the `Std.Symm` constructor with explicit `i j` (DynkinForward ×2, Theorem_Dynkin_classification ×2).
  - **`Finset.sum_eq_add_sum_diff_singleton_of_mem` renamed `diff` → `sdiff`** → `sum_eq_add_sum_sdiff_singleton_of_mem` (Theorem5_15_1).
  - **`AddCon.addConGen_le` is now an `Iff`** (was a function) → use `.mpr` (Example5_19_3).

## Current frontier
v4.31.0 PR being opened with auto-merge. Next (final version of the sweep): v4.32.0-rc1.

## Overall project progress
Sweep: v4.28.1 → v4.31.0-rc2 merged (15 versions). v4.31.0 green and merging. Remaining: v4.32.0-rc1.

## Next step
After v4.31.0 merges, run bumpstep for v4.32.0-rc1 (the last in the sweep).

## Blockers
None.
