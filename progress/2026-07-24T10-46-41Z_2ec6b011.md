## Accomplished
Resolved #7559 (fix Ch5 Theorem 5.9.1): exposed the coset-representative form of the
Frobenius formula and proved it equivalent to the existing averaged form.

New file `EtingofRepresentationTheory/Chapter5/Theorem5_9_1_Coset.lean`:
- `Etingof.frobeniusSummand_smul_left`: the Frobenius summand
  `x ↦ [x g x⁻¹ ∈ H] · χ_V(x g x⁻¹)` is invariant under left multiplication of `x` by `h₀ ∈ H`
  (membership preserved by conjugation, trace preserved since `χ_V` is a class function — proved
  via `LinearMap.trace_mul_cycle`).
- `Etingof.frobeniusSummand_congr`: the summand depends only on the right `H`-coset of `x`, so it
  is independent of the choice of coset representative (the issue's required independence result).
- `Etingof.frobenius_coset_bridge`: for any `F` constant on right `H`-cosets,
  `(1/|H|)·Σ_{x∈G} F x = Σ_{σ∈H\G} F σ.out`. Proved via the bijection
  `H × Quotient(rightRel H) ≃ G, (h,q) ↦ ↑h·q.out` (inverse `x ↦ (cosetTwist H x, mk'' x)`,
  reusing `cosetTwist` from Remark 5.8.3), then `Fintype.sum_prod_type` + constancy + counting.
- `Etingof.Theorem5_9_1_coset`: the Frobenius formula summed over `H\G` with representatives
  `x_σ = σ.out`, proved equal to `Etingof.Theorem5_9_1` (averaged form) by the bridge.

Right cosets modelled by `Quotient (QuotientGroup.rightRel H)`; representatives via `Quotient.out`,
matching Remark 5.8.3. Averaged form (`Theorem5_9_1`, `Remark5_9_2`) retained unchanged.

Updated `progress/items.json`: `Chapter5/Theorem5.9.1` now `proved`/`full`/`covered`.
Added import to `EtingofRepresentationTheory/Chapter5.lean`.

## Current frontier
Item complete. `lake build` of the new module succeeds; sorry-free;
axioms `[propext, Classical.choice, Quot.sound]`.

## Overall project progress
Stage 3 formalization ongoing. This closes a Chapter 5 completeness-audit coverage gap.

## Next step
Pick up another unclaimed feature/completeness-audit issue.

## Blockers
None.
