## Accomplished

Session type: `/work` → claimed feature #4994 (retire the upstream classification
crux sorry). Two concrete outcomes:

1. **Fixed a red `main` (regression).** While orienting on #4994's dependency
   (#4993/PR #5009, the general-k classification core), discovered `lake build` is
   red on `main`: `SchurWeylSimplesClassificationComplex.lean:378` calls
   `formalCharacter_simples_coeff_eq_zero_of_torus_trace_eq_zero_general_of_algebraic`,
   which PR #5007 (#4983, commit 02045bae) had renamed to `..._general` (now taking
   `hLalg` directly). #5009 was based on a main that already contained the rename but
   still referenced the old name, and auto-merged on top without re-checking (stale
   branch). Confirmed empirically: `Unknown identifier …_general_of_algebraic`.
   Fix = pure rename (line 378 call + line 341 doc); argument list unchanged. Leaf now
   builds green (8086 jobs). Shipped as **PR #5022** closing **issue #5021**
   (`agent/c4445299-fixmain`). Auto-merge unavailable (no branch protection) — will be
   merged by the next planning cycle's merge-ready-PRs step once CI passes.

2. **Decomposed #4994** into two precise, self-contained sub-issues, because
   investigation showed #4994's premise is inaccurate and the work is a large
   multi-file refactor:
   - **#4994's premise correction:** it assumes the general-k core "lives downstream,
     exactly where `iso_of_formalCharacter_eq_schurPoly` lives." In reality #4993
     landed `schurWeyl_simples_formalCharacter_classification_core_general` (+ ~10
     support lemmas) in the **leaf** `SchurWeylSimplesClassificationComplex.lean`,
     which `import`s `SchurWeylFormalCharacterIso` — strictly **downstream** of the
     crux's consumers. So BOTH routes first require relocating the core upstream of
     those consumers.
   - **#5023 (sub-A):** relocate the 11 leaf decls into `SchurWeylFormalCharacterIso`
     (between the seams and `iso_of_`), add `SchurModuleSimple`/`SemisimpleIsotypic`
     imports, make `glTensorRep_schurWeyl_decomposition_equivariant_simple` emit
     `hSne`, delete the now-empty leaf + drop it from `Chapter5.lean`. `depends-on: #5021`.
   - **#5024 (sub-B):** retarget `decompose_polynomial_gl_rep` /
     `polynomial_homog_rep_asModule_embeds_directSum_simple` to expose decomposition
     witnesses instead of `hclass`; have `iso_of_formalCharacter_eq_schurPoly` and
     `ConstituentCharacterExtraction` derive the classification via the relocated
     `…_core_general` (with `hSne`); delete the false crux + 2 corollaries.
     `depends-on: #5023`.
   - Parent #4994 `coordination skip`-ped (replan) with a `Decomposed into #5023, #5024`
     breadcrumb.

Key verified fact recorded in the sub-issues: `hSne : ∀ i, 0 < finrank k (S i)` is
derivable — each multiplicity space `S i = ↥(S' i)` is a simple `symGroupImage`-module
(`Theorem5_18_4_GL_rep_decomposition_explicit_simple`), hence `Nontrivial`, hence
`Module.finrank_pos_iff.mpr inferInstance`.

## Current frontier

`main` regression fix is in review as PR #5022. The #4994 work is fully planned but
not started (sub-issues #5023/#5024 in the blocked queue pending #5021 then #5023).

## Overall project progress

Stage 3 (Lean formalization) of Chapter 5's Schur-Weyl / Theorem 5.12.2 classification
cluster. The general-k classification core (#4993) exists and is sorry-free modulo the
tracked seams (#4946 Schur-module simplicity; #4947 character independence). The
remaining architectural task is wiring it in to replace the false upstream crux — now
scoped as #5023 → #5024.

## Next step

1. Merge PR #5022 (regression fix) — restores green `main`, unblocks everyone.
2. Then a worker picks up #5023 (relocation; unblocks once #5021 closes), then #5024.

## Blockers

- PR #5022 cannot auto-merge (no branch protection); relies on the planning-cycle
  merge-ready step. Until it merges, `main` stays red and all builds fail.
- #5023 blocked on #5021 (→ PR #5022); #5024 blocked on #5023.
