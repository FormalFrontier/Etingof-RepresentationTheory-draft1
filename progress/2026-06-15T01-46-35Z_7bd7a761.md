## Accomplished

Worked issue #4689 (feat(Ch6 #4650 sub-B): orientation-generic
`d7tildeRep_kQ_leaf_equalities` on the corrected D̃₇ tube). **Partial.**

- Added the eigenvalue-site tube containment helper
  `d7tilde_gammaTube_containment_F` in
  `EtingofRepresentationTheory/Chapter6/FieldGenericD7Tilde.lean`: the
  corrected-tube analogue of the now-historical `d7tilde_gamma_containment_F`,
  built on `d5tildeGammaTube_F` + `gammaTube_from_embed1_F`/`gammaTube_from_embed2_F`
  (depositing `Λy = jordanShiftLinGen F lam m y` on the embed-2 column), with the
  internal identity chain `3-4-5` threaded through via the collapse equality
  `Wmain⟨3⟩ = Wmain⟨5⟩`.
- Replaced the single deferred `sorry` in `d7tildeRep_kQ_leaf_equalities` with
  the full five-edge orientation case tree (`0-2, 1-2, 2-3, 6-5, 7-5`),
  mirroring the D̃₅ precedent `d5tildeRep_kQ_leaf_equalities` exactly:
  - **ALL-CANONICAL** branch proven via `d7tilde_gammaTube_containment_F`
    (both `W₁`-main and `W₂`-main) + `compl_le_forces_eq`.
  - **COMBO-D** branch (central `2→3` canonical, both v=5 leaf edges reversed
    `5→6`, `5→7`) proven via the projection siblings
    `d7tilde_core5_F_proj1`/`d7tilde_core5_F_proj2` after chain-collapse to v=5.
  - Orientation-independent chain collapse extracted once at the top via
    `d7tilde_chain_collapse` for both blocks (`hcol_main`, `hcol_other`).
- Marked the old `d7tilde_gamma_containment_F` (on the refuted nilpotent
  `d5tildeGamma_F`) as a historical pre-correction template in its docstring.

`lake build EtingofRepresentationTheory.Chapter6.FieldGenericD7Tilde` passes
(green, 8044 jobs).

## Current frontier

`d7tildeRep_kQ_leaf_equalities` still carries `sorry` in **5 of 7** leaf
orientation classes (file line ~718): combo C (e75 canonical / e65 reversed),
combo C′ (e75 reversed / e65 canonical), e23 reversed (central `3→2` carrying
`γ_λ⁻¹`), e12 reversed (`2→1`), e02 reversed (`2→0`). These are *exactly* the
same branches left open in the D̃₅ precedent `d5tildeRep_kQ_leaf_equalities`
(`FieldGenericD5Tilde.lean:1123`, still 5 sorries) — they are not closeable
with the current `core`/proj infrastructure because the mixed-direction v=5
leaves lack the symmetric leaf push that `d7tilde_core5_F` requires, and the
reversed central edge needs the `γ_λ⁻¹` (`d5tildeGammaTubeInv_F`) transport.

## Overall project progress

Stage 3 (Chapter 6, affine-tube indecomposability family). The D̃₇ leaf-equality
foundation now matches the D̃₅ achieved state: tube containment helper + 2 of 7
orientation classes proven. The full sorry-free leaf-equality (all orientations)
remains open across the *entire* family (star/d5/d6/d7/d8tilde/dTilde/etilde6/
etilde7/t125), which is why the downstream assembly issues (#4690 here, plus
#4577/#4613/...) stay blocked on it.

## Next step

The remaining 5 branches should follow the **re-scoping** decision already taken
for D̃₅ in #4647 → #4743: *restrict* `d7tildeRep_kQ_leaf_equalities` to
eigenvalue-free orientations and fold the mixed-v5 / central-reversed branches
directly into `d7tildeRep_kQ_isIndecomposable` (#4690) via the eigenvalue-site /
brick argument, rather than trying to prove the standalone leaf collapse in those
branches (which the family-wide diagnosis in #4613 argues may be impossible with
the forward `core` criterion alone). A follow-up issue should mirror #4743's
scope for D̃₇ once #4743 lands and establishes the pattern.

## Blockers

None for this partial PR. The residual 5 branches are blocked on the same
open mathematical content as the rest of the family (reversed-arm leaf-collapse
foundation, tracked for D̃₅ by #4743 and described in #4613); not a hard blocker,
a re-scoping decision.
