## Accomplished

Statement pass for Chapter 9 exercises (issue #5906), one-shot dispatch. Formalized faithful
Lean statements (with `sorry` proofs) for 4 of the 6 listed exercises, each in its own new
file, added to the `Chapter9.lean` aggregator:

- `Chapter9/Problem9_4_2.lean` — projective dimension: (i) Ext-vanishing characterization of
  `pd(M) ≤ d`, (ii) `pd(N) = pd(M) + 1` for a nonsplit SES `0 → M → P → N → 0` with `P`
  projective, (iii) dimension shifting for the syzygy.
- `Chapter9/Problem9_4_5.lean` — (i) `det(Cartan) = ±1` for finite homological dimension
  (using `Etingof.algebraCartanMatrix` and `Etingof.homologicalDimension`); (ii) the
  homological dimensions of `k[t]/tⁿ` (`n > 1`) and of the Problem 9.3.2 algebra are both `⊤`.
- `Chapter9/Problem9_5_3.lean` — (i) bijection between blocks and indecomposable central
  idempotents (predicate defined here); (ii) Hom-orthogonality across distinct blocks and
  existence of a block for every indecomposable finite-length object. Part (iii) (the concrete
  `k[S₃]`, char 2 computation) deferred.
- `Chapter9/Exercise9_6_3.lean` — `P` is a projective generator iff `Hom(P, L) ≠ 0` for every
  simple `L`, plus the deduction that every finite abelian category has a projective generator.

`lake build EtingofRepresentationTheory.Chapter9` succeeds (only expected `sorry` warnings and
pre-existing style warnings). `progress/items.json` updated: the 4 exercises set to
`statement_formalized`.

## Current frontier

Two of the six listed exercises were **not** formalized because they require substantial new
definitions that do not exist in Mathlib or the project, beyond the scope of a statement pass:

- `Chapter9/Problem9.4.6` — needs the **path algebra of a quiver** as a `k`-algebra (free
  `k`-module on `Quiver.Path`s with concatenation multiplication). Mathlib has `Quiver.Path`
  but no path-algebra `Ring`/`Algebra` instance; constructing it (and proving associativity)
  is a real definition task, not a statement.
- `Chapter9/Problem9.6.5` — needs the **tensor functor `G(X) = P ⊗_B X`** (a coequalizer in a
  finite abelian category) to state parts (i)-(iii). `Etingof.IsProgenerator.preadditiveCoyonedaObjFG`
  (the functor `F`) and `Etingof.Theorem_9_6_4` (F is an equivalence) already exist, but the
  explicit quasi-inverse `G` must be constructed to state 9.6.5 faithfully.

These were decomposed into a follow-up statement-pass sub-issue.

## Overall project progress

Chapter 9 statement pass advancing. This turn: +4 exercises `statement_formalized`
(9.4.2, 9.4.5, 9.5.3, 9.6.3).

## Next step

Pick up the follow-up sub-issue for `Problem9.4.6` and `Problem9.6.5`: first construct the
path-algebra definition and the tensor functor `G`, then state the exercises against them.

## Blockers

None for the landed work. The two deferred exercises are definition-blocked (path algebra,
tensor functor `G`), tracked in the follow-up sub-issue.
