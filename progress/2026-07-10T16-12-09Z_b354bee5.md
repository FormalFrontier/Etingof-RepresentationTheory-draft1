## Accomplished

Claimed and **fully closed #6197** — the horseshoe-lemma capstone. `theorem Etingof.horseshoe`
in `Chapter8/Horseshoe.lean` is now sorry-free with clean axioms
(`propext, Classical.choice, Quot.sound`).

- **Refactor**: extracted the degree-0 off-diagonal seed from the base case of
  `horseshoeTwistAux` into reusable `horseshoeSeed` / `horseshoeTwistZero` with the seed relations
  `horseshoeSeed_comp_f`, `horseshoeSeed_comp_d`, `horseshoeTwistZero_comp_π`,
  `horseshoeTwist_zero_comp_f` (behaviour-preserving).
- **Augmentation**: `horseshoeD_zero_comp_π` (degree-0 chain-map condition `d²₁₀ ≫ ε₂ = 0`) and
  `horseshoeπ : horseshoeComplex ⟶ single₀ S.X₂`, with `horseshoeπ_f_zero` / `horseshoeπ_f_succ`.
- **QuasiIso** `horseshoeπ`:
  - degree `n+1`: `quasiIsoAt_iff_exactAt'` (target `single₀` exact) + `exactAt_X₂` from the
    horseshoe SES and `P₁.complex_exactAt_succ`, `P₃.complex_exactAt_succ`.
  - degree `0`: `IsIso (homologyMap horseshoeπ 0)` via the **middle three-lemma**
    (`ShortComplex.mono_of_mono_of_mono_of_mono` / `epi_of_epi_of_epi_of_epi`) applied to
    `(homologyFunctor 0).mapShortComplex.map horseshoeMorphism`, where `horseshoeMorphism` is the
    map of short complexes of complexes into `S` placed in degree 0 (`horseshoeSingle`,
    short-exact via `single₀` exact). Outer verticals `homologyMap P₁.π 0`, `homologyMap P₃.π 0`
    are isos since `P₁`, `P₃` are resolutions; `Mono (homologyMap horseshoeSingle.f 0)` from
    `homology_exact₁` + `isZero_single_obj_homology`; `Epi (homologyMap β 0)` from
    `epi_homologyMap_of_epi_of_not_rel`.
- **Assembly**: `horseshoeResolution : ProjectiveResolution S.X₂`; the theorem packages it with
  `horseshoeα`, `horseshoeβ`, the split `ShortExact`, and the two augmentation squares
  (`horseshoeπZero_inl`, `horseshoeπZero_comp_g`).

Added imports: `HomologySequenceLemmas`, `SingleHomology`, `DiagramLemmas.Four`.

## Current frontier

`Etingof.horseshoe` sorry-free and axiom-clean. Chapter 8 horseshoe lemma complete
(statement + data + resolution + theorem across #6180/#6195/#6196/#6197).

## Overall project progress

Phase 3 formalization ongoing. Chapter 8 horseshoe lemma fully closed.

## Next step

Merge this PR. Downstream: the two augmentation squares feed
`ProjectiveResolution.isoLeftDerivedObj_hom_naturality`; the degreewise-split SES powers the
Problem 8.2.6 `Tor`/`Ext` long exact sequences. Other open work: #6206 (Ch9 projective generator),
#6207 (Ch3 ext1 vanishing).

## Blockers

None.
