## Accomplished

Assembled the First Fundamental Theorem for matrix invariants (Problem 5.24.2, issue #6501),
discharging the lone `sorry` in `Chapter5/Problem5_24_2.lean`
(`weightedHomogeneous_invariant_mem_adjoin`). `Problem5_24_2.lean` is now **sorry-free**, and with
it `invariantSubalgebra_le_adjoin_traceWord` and the problem-statement theorem
`invariantSubalgebra_eq_adjoin_traceWord` (the invariant ring is generated by trace-of-word
functions).

Key work:

1. **Import-architecture fix.** The assembly needs `endTensorEval` and the range-identification
   lemma from `Problem5_24_2_Bridge.lean`, but Bridge *imports* `Problem5_24_2.lean` — an import
   cycle that blocked making the target file sorry-free. Extracted all upstream definitions
   (`MatrixTupleRing`, `genericMatrix`, `conjAlgHom`, `invariantSubalgebra`, `traceWord`,
   `matrixWeight`, and the conjugation/homogeneity lemmas) into a new
   `Chapter5/Problem5_24_2_Core.lean`, repointed Bridge to import Core, and rebuilt
   `Problem5_24_2.lean` as the assembly importing Core + Bridge + `PermutationTraceWord`.

2. **Tensor-trace ↔ trace-word identity** (`endTensorEval_symGroupAction_mem_adjoin`).
   `toMatrix_symGroupAction` computes the permutation operator `symGroupAction σ` as the `{0,1}`
   permutation matrix `f = g ∘ σ⁻¹` in the standard tensor basis. Collapsing the contraction gives
   `endTensorEval slot (symGroupAction σ) = ∑_g ∏_i X(slot(σi), g(σi), g i)`, which matches
   `matrixSum_eq_prod_orbit` (over the coordinate ring, with `M_i = genericMatrix (slot (σ i))`).
   Each per-orbit factor `trace (matrixCycleProd σ M r)` is a `traceWord`
   (`trace_matrixCycleProd_mem_range_traceWord`), hence the whole product lies in
   `Algebra.adjoin ℂ (Set.range (traceWord k N))`.

3. **Multidegree slot construction** (`exists_slot_of_multidegree`): realizes
   `Fin (∑ᵢ dᵢ) ≃ Σ i, Fin (d i)` via `finSigmaFinEquiv` and reads the slot off the first
   coordinate; the fiber over `i` is `Fin (d i)` (`Equiv.sigmaSubtype`), so letter `i` is carried by
   exactly `d i` slots.

4. **Final assembly**: choose a slot of multidegree `d`; the bridge writes `p = endTensorEval slot M`
   for `M ∈ symGroupImage`; `symGroupAlgHom_range` expresses `M` as a finite `ℂ`-linear combination
   of permutation operators; push `endTensorEval`'s linearity through and use (2).

## Current frontier

`Problem5_24_2.lean` is sorry-free and the whole of Chapter 5 builds. The FFT proof rests on two
sorried upstream dependencies that are tracked by their own issues (acceptable per CLAUDE.md
"a sorry'd dependency is not a blocker"):

- `Problem5_24_2_Bridge.lean:130` `weightedHomogeneous_invariant_mem_range_endTensorEval` — the deep
  surjectivity content (range identification) left as a sorry by bridge issue #6499.
- `PermutationTraceWord.lean:132` `matrixSum_eq_prod_orbit` — the orbit-partition combinatorics
  being discharged in issues #6506 (step4b) / #6513 (step4c).

## Overall project progress

Stage 3 formalization ongoing. Problem 5.24.2's assembly layer is complete; the two remaining
sorries feeding it are already owned by in-flight issues. Once those land, the entire First
Fundamental Theorem for matrix invariants is sorry-free with no further work in this file.

## Next step

Land the two upstream sorries (#6499 range identification, #6506/#6513 `matrixSum_eq_prod_orbit`).
No further changes to `Problem5_24_2.lean` are needed for the FFT.

## Blockers

None. The assembly is complete and independent of the remaining sorries' proofs.
