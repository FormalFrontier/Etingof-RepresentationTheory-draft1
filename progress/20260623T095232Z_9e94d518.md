## Accomplished

One-shot dispatch on **#4881** (`garnir_twisted_in_lower_span` /
`twistedPolytabloid_residual_invariant`). The gating dependency **#5037
(fact 1) landed** this morning in commit `e4a65153`
(`twistedPolytabloid_maximal_support_colStandard`,
`SpechtModuleBasis.lean:2090`, sorry-free): the dominance-maximal support
tabloid of `twistedPolytabloid w σ` is column-standard (equals `[σ]`).

Fresh source read confirmed the live state: the **only** real `sorry` in
`Chapter5/SpechtModuleBasis.lean` is `twistedPolytabloid_residual_invariant`
(`:2516`); occurrences at :720/:2535/:2671 are docstring text. The leading-term
elimination engine (`maxSrRankSupp`/`cardAtMaxSrRank`/`resMeasure`/
`resMeasure_sub_lt` + strong-induction assembly), `twistedPolytabloid_residual_in_V`,
`garnir_twisted_in_lower_span`, and `garnir_straightening_step` are all
sorry-free and consume the invariant as stated.

With fact 1 in hand the invariant is now actionable as the two facts #5037
explicitly deferred ("do NOT create until this lands"). **Decomposed #4881 into:**

- **#5097 (fact 2)** — column-standardizability of the dominance-maximal
  *residual* tabloid `t₁` of `X = f_w(σ) − M` (M ∈ V, supp(X) ≼ [σ]). The
  genuine per-tableau Garnir involution; not a fact-1 corollary because
  `max(X) ≠ max(f_w)` most of the time. No deps (gating #5037 closed).
- **#5098 (fact 3)** — IH-availability + final assembly: easy strict-dominance
  arm via `srRank_lt_of_tabloidStrictDominates`, plus the equal-tabloid
  (`[β']=[σ]`) row-inversion descent, then close the invariant sorry and #4881.
  `depends-on: #5097` (auto-`blocked`).

Routed #4881 to `replan` with a `Decomposed into #5097, #5098` breadcrumb.

## Current frontier

`twistedPolytabloid_residual_invariant` (`SpechtModuleBasis.lean:2516`) — the
single remaining `sorry` in the Specht standard-basis development. Now split
into the actionable #5097 → #5098 chain.

## Overall project progress

Phase 3 (formalization). Ch5 standard-basis straightening is sorry-free modulo
this one isolated invariant. Garnir chain predecessors closed: #4995 → #4998 →
#5010 → #5037. Fact 1 done; facts 2 and 3 outstanding (#5097, #5098).

## Next step

Claim **#5097** (fact 2). Suggested de-risking first deliverables (reusable,
non-scaffold): a `tabloid t is column-standardizable ↔ <row-content dominance
condition>` characterization and `twistedPolytabloid w σ = w • generalizedPolytabloidTab σ`,
then the per-tableau Garnir sign-reversing involution keyed on the
column-standardness violation (NOT the refuted global `H`, #4604).

## Blockers

None new. #5098 is correctly blocked on #5097. No code change this session: a
sorry-relocating scaffold is forbidden and the full invariant is not a
single-session fill (consistent verdict of 5+ prior sessions, Codex, and this
session's independent source read). Build state inherited from main (#5096
landed green); nothing edited.
