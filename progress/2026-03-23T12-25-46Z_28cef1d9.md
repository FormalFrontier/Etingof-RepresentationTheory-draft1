## Accomplished

- Claimed issue #1646: prove `principalSeries_simple_of_ne` (V(χ₁,χ₂) irreducible when χ₁ ≠ χ₂)
- Proved `sum_monoidHom_units_eq_zero`: for a nontrivial MonoidHom φ : F →* ℂˣ on a finite commutative group, ∑ φ(x) = 0. This is a key ingredient for the character inner product computation (character orthogonality for multiplicative characters).
- Documented complete proof strategy for `principalSeries_simple_of_ne`:
  1. Reduce via `FDRep.simple_iff_char_is_norm_one` to ∑_g χ(g)·χ(g⁻¹) = |G|
  2. Use induced character formula on each conjugacy class type
  3. Apply character orthogonality ∑ (χ₁/χ₂)(z) = 0
  4. Arithmetic: (q-1)(q+1)² + (q-1)(q²-1) + q(q+1)(q-1)(q-3) = q(q+1)(q-1)² = |G|

## Current frontier

The sorry in `principalSeries_simple_of_ne` remains. The blocker is computing the character of the principal series (trace of right translation on the covariant function space). This requires:
- Constructing a basis for `principalSeriesSubmodule` indexed by coset representatives of B\G
- Computing the trace of right translation in this basis (induced character formula)
- Splitting the inner product sum by conjugacy class type via `GL2.sum_split`

## Overall project progress

- `Theorem5_25_2.lean`: 6 sorries (same count as before)
- New helper lemma `sum_monoidHom_units_eq_zero` proved (0 sorries)
- Clear proof strategy documented for `principalSeries_simple_of_ne`
- The sorry is NOT blocked on external dependencies - it needs internal infrastructure (basis + trace computation)

## Next step

The highest-priority sub-task is building the **induced character formula** for the principal series:
1. Define coset representatives for B\G (ℙ¹(𝔽_q), q+1 elements)
2. Show covariant functions biject with their values on coset reps (basis construction)
3. Prove tr(ρ(h)) = ∑_{x ∈ B\G, xhx⁻¹ ∈ B} λ(xhx⁻¹)

Once the character formula is established, the inner product computation follows from:
- GL2.sum_split (already proved in GL2ConjugacyClasses.lean)
- Conjugacy class cardinalities (already proved)
- sum_monoidHom_units_eq_zero (proved this session)
- Arithmetic (standard)

## Blockers

None (the work is self-contained, just requires substantial infrastructure building).
