## Accomplished

Closed the C.short all-leaves residual sorry in
`non_adjacent_branches_leaf_case_per_kQ`
(`Chapter6/FieldGenericNonAdjacentBranches.lean`). Issue #2965.

- Added `embed_d7tilde_in_tree_per_kQ` helper to
  `Chapter6/FieldGenericD7Tilde.lean` (~250 lines, Section 7).
  Mirrors the shape of `embed_etilde7_in_tree_per_kQ`
  (`FieldGenericETilde7.lean:356`) for the D̃₇ tree: two non-adjacent
  degree-3 branch points `p, s` each with two leaves (`a, b` for `p`;
  `u, v` for `s`) connected by an internal length-3 chain
  `p–q–r–s`. Takes 8 vertices, 7 edges, the `p-s` non-edge, 7
  distinctness hypotheses; derives the 21-pair adjacency lattice
  (8 triangle non-edges + 12 path-based non-edges) and dispatches via
  `subgraph_infinite_type_transfer_per_kQ` and
  `d7tilde_not_finite_type_per_kQ`. Uses `set_option maxHeartbeats
  800000 in` for the final `hembed` 64-case linarith.
- Wired the dispatch in the residual sub-case at
  `FieldGenericNonAdjacentBranches.lean:872-895` (formerly the sorry
  at line 884). Vertex map: `0→leaf, 1→side_arm, 2→v₀, 3→chain[1],
  4→chain[2], 5→w, 6→arm₁, 7→arm₂`. The 9th host vertex `x` is
  excluded from the embed. Distinctness drawn from
  `arm₁_ne_chain` / `arm₂_ne_chain` / `harm₁₂` / `hne`. The p-s
  non-edge is `(h01 v₀ w).resolve_right h_v₀w_nonadj`.
- Added `import EtingofRepresentationTheory.Chapter6.FieldGenericD7Tilde`
  to `FieldGenericNonAdjacentBranches.lean`.

Targeted `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD7Tilde
EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
passes. The only remaining sorry in `FieldGenericNonAdjacentBranches.lean`
is the documented Case E sorry at line 969, tracked by #2955.

## Current frontier

Issue #2965 deliverables landed. The C.short body in
`non_adjacent_branches_leaf_case_per_kQ` is now sorry-free; only the
Case E sub-issue #2955 remains in this file. The deferred
`d7tildeRep_kQ_isIndecomposable` sorry (tracked by #2967) is carried
transitively through `d7tilde_not_finite_type_per_kQ`.

## Overall project progress

Stage 3 / Phase 2: the forbidden-subgraph dispatch library
(d5tilde, t125, etilde6, etilde7, d7tilde) is now complete for the
non-adjacent-branches assembly. The case-split of
`non_adjacent_branches_leaf_case_per_kQ` has only the chain.length=3
Case E sorry remaining.

## Next step

Two parallel tracks:

1. Issue #2955 — fill the Case E (chain.length = 3) sorry and the
   uncovered all-leaves sub-cases. Flagged as the trickiest sibling.
2. Issue #2967 — fill the indecomposability proof body
   `d7tildeRep_kQ_isIndecomposable`. Sizeable proof; the d5tilde
   precedent is still in flight (#2834).

## Blockers

None. The PR for #2965 (`embed_d7tilde_in_tree_per_kQ` + C.short
all-leaves dispatch) inherits the documented `d7tildeRep_kQ_isIndecomposable`
sorry transitively, but that's a tracked stub, not a blocker.
