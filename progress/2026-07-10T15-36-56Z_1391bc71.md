## Accomplished

Claimed and delivered **#6162** (Ch8 Problem 8.2.6(v), `Tor` half): the left-derived-functor
six-term homology exact sequence.

New file `EtingofRepresentationTheory/Chapter8/LeftDerivedSequence.lean`:

- `Etingof.shortExact_map_of_degreewise_projective`: an additive functor `F` preserves a short
  exact sequence of chain complexes whose right-hand complex is degreewise projective. Route:
  `shortExact_iff_degreewise_shortExact` → each degree splits via
  `ShortComplex.ShortExact.splittingOfProjective` (right term `P₃ᵢ` projective) →
  `ShortComplex.Splitting.map F` preserves the splitting → `shortExact_of_degreewise_shortExact`.
- `Etingof.Functor.leftDerived_sixTerm_exact` (the reusable crux): for an additive `F : C ⥤ D`
  between abelian categories with enough projectives in `C`, and `hS : S.ShortExact`, produces the
  connecting map `δ` and exactness of the six-term window
  `Lₙ₁(X₁)→Lₙ₁(X₂)→Lₙ₁(X₃)→[δ]→Lₙ₀(X₁)→Lₙ₀(X₂)→Lₙ₀(X₃)`. Proof: horseshoe SES of complexes →
  `F`-image is short exact (above helper, using `P₃.projective`) → homology LES
  (`ShortComplex.ShortExact.δ` / `homology_exact₁/₂/₃`) assembled into a `mk₅` via
  `exact_of_δ₀` → transported to the left-derived window by an `isoMk₅` whose components are
  `ProjectiveResolution.isoLeftDerivedObj` and whose squares are exactly
  `isoLeftDerivedObj_hom_naturality` fed the horseshoe augmentation squares (the `δ` square is
  iso cancellation by the definition of `δ`). Finish with `ComposableArrows.exact_iff_of_iso`.

Wired `Problem_8_2_6_v_tor` to it (one line: apply the general lemma with
`F := tensorRightFunctor A N`; `TorFunctor` unfolds definitionally to `leftDerived`). Registered
the new module in `Chapter8.lean`.

## Current frontier

`Problem_8_2_6_v_tor` builds and is sorry-free in its own right. `#print axioms` shows
`[propext, sorryAx, Classical.choice, Quot.sound]`; the only `sorryAx` is transitive through
`Etingof.horseshoe` (still `sorry`, #6197). Both `leftDerived_sixTerm_exact` and `v_tor` become
axiom-clean automatically once the horseshoe closes — this issue's own contribution adds no new
sorry.

## Overall project progress

Chapter 8 homological algebra: the general left-derived LES crux now exists and is reused for the
`Tor` first-argument six-term sequence. Remaining Ch8 sorries near here: `horseshoe` (#6197), and
`Problem_8_2_6` parts ii / iii_ext / iii_tor / iv (sibling issues). `iii_tor` can now reuse
`leftDerived_sixTerm_exact` too.

## Next step

Land #6197 (close the `horseshoe` sorry) to make `v_tor` axiom-clean. A worker could also apply
`Etingof.Functor.leftDerived_sixTerm_exact` to discharge `Problem_8_2_6_iii_tor`.

## Blockers

None. (`horseshoe`'s sorry is a dependency, not a blocker, per the issue and project policy.)
