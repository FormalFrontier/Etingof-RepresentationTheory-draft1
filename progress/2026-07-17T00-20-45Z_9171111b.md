## Accomplished

- Claimed **#6818** (`Ext` half of Problem 8.2.8 — assemble `Problem_8_2_8_ext`). The blocker
  building blocks #6867/#6868 (`rearrangeHomComplex`) had landed on `main`, so the item was
  workable.
- **Landed the mathematical core** `Etingof.Problem_8_2_8_extₖ` in
  `EtingofRepresentationTheory/Chapter8/Problem8_2_8.lean` (commit `35cd9986`), sorry-free at its
  own level and building cleanly. It is the exact cohomological mirror of `Problem_8_2_8_tor`: a
  four-step `≪≫` composition of
  `extIsoCohomologyHomₖ` (#6817) → `homologyFunctor.mapIso (rearrangeHomComplex …)` (#6868) →
  `kunnethCochainComplexNat` (#6817) → `Sigma.mapIso (tensorIso extIsoCohomologyHomₖ.symm …)`,
  giving
  `Extₖ k (A₁⊗A₂) (M₁⊗M₂) (N₁⊗N₂) i ≅ ∐_{j+m=i} Extₖ k A₁ M₁ N₁ j ⊗ Extₖ k A₂ M₂ N₂ m`
  at the `ModuleCat k`-valued left-derived-functor (`Extₖ`) level. Takes the f.g. projective
  resolutions `P₁ P₂` and the componentwise `hN` as hypotheses; supplies the N-side towers.
- Documented the residual reduction of the final `Problem_8_2_8_ext` (still `sorry`) as a concrete
  4-item plan in a code comment.
- **Decomposed** the residual into #6897 (the reusable comparison iso `Etingof.Ext ≃ₗ[k] Extₖ`) and
  #6898 (final assembly; `depends-on: #6897`). Left a `Decomposed into #6897, #6898` breadcrumb on
  #6818.

## Current frontier

- `Problem_8_2_8_ext` (line ~231) still `sorry`. Its reduction to `Problem_8_2_8_extₖ` needs:
  (1) f.g. projective resolutions with `Module.Finite`/`Projective` term instances;
  (2) module-structure reconciliation (`instM`/`instN` via `hM`/`hN` ↔ `extTensorFunctorLeftObj`);
  (3) the `Abelian.Ext ≃ₗ[k] Extₖ` comparison bridge (#6897 — no such bridge in Mathlib);
  (4) `∐`/monoidal-`⊗` → `⨁`/`TensorProduct k` conversion.

## Overall project progress

- Stage 3 endgame. Ch8 Problem 8.2.8 `Ext` half: the Künneth core is now assembled; only the
  derived-category ↔ resolution-Ext bridge and the final signature-plumbing remain.
- Note: `rearrangeHomComplex` still transitively rests on one tracked proof-level `sorry`
  (`rearrangeHomComplexXIso_inv_comm`, `RearrangeHomComplex.lean:89` — the differential-commutation
  sign bookkeeping, follow-up per #6844). `Problem_8_2_8_extₖ` is sorry-free at its own level.

## Next step

- Pick up **#6897** (the `Etingof.Ext ≃ₗ[k] Extₖ` bridge): identify `CategoryTheory.Abelian.Ext`
  with the resolution-computed `Extₖ` (`= CategoryTheory.Ext k (ModuleCat A) n`) via a shared
  projective resolution — `extIsoCohomologyHomₖ` on one side, the derived-category
  cohomology-class machinery on the other. Then #6898 (final assembly) closes `Problem_8_2_8_ext`.

## Blockers

- None new. #6898 is correctly `blocked` on #6897. The `rearrangeHomComplex` differential-commutation
  `sorry` is a pre-existing tracked follow-up, not a blocker for building on it.
