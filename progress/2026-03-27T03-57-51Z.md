## Accomplished

- Proved `alternating_coeff_eq_cauchyRHS_coeff` in `PowerSumCauchyBilinear.lean` — the theorem that the double alternating sum of `fullCauchyProd` coefficients equals the corresponding `cauchyRHS` coefficient
- Added ~160 lines of infrastructure:
  - `permExponentX/Y`: exponent vectors encoding Vandermonde monomials
  - `vandermondeFPS_x/y`: Vandermonde alternants as MvPowerSeries
  - `coeff_vandermondeFPS_x_mul`: coefficient extraction for Vandermonde-sum times FPS
  - Various `_le_bilinExponent_iff`, `bilinExponent_sub_` lemmas for exponent arithmetic
- Cleaned up lint warnings (unused simp args, unused variable)
- Created PR #1801 (partial) and sub-issue #1802 for remaining sorry

## Current frontier

One sorry remains: `vandermonde_mul_fullCauchyProd_eq_cauchyRHS` at line 1086 of `PowerSumCauchyBilinear.lean`. This is the FPS Cauchy identity `Δ_x · Δ_y · ∏ 1/(1-xᵢyⱼ) = det(1/(1-xᵢyⱼ))`. Filed as #1802.

## Overall project progress

The `alternating_coeff_eq_cauchyRHS_coeff` theorem is the key step connecting the double alternating sum (from the Frobenius character formula) to the Cauchy determinant. It is now proved modulo one deep FPS identity. The downstream `vandermonde_cauchy_diagonal` already uses it.

## Next step

Prove `vandermonde_mul_fullCauchyProd_eq_cauchyRHS` (#1802). Suggested approach: clear denominators using `IsUnit.mul_left_cancel` to reduce to a polynomial identity, then derive from `Lemma5_15_3` via `MvPolynomial.funext_set`.

## Blockers

None — the sorry is self-contained and has a clear proof strategy outlined in #1802.
