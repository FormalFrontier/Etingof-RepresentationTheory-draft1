## Accomplished

Working issue #4960 (feat(Ch5 #4905 part-B): FDRep packaging + formal character of
the determinant-quotient degree component `(A/det)_d`).

New file `EtingofRepresentationTheory/Chapter5/CauchyDetQuotientDegree.lean`.

Landed (committed, compiles, no sorry):
- `formalCharacter_add_of_shortExact` — general additivity of `formalCharacter`
  over an equivariant short exact sequence `0 → U → V → W → 0` of FDReps with
  spanning weight spaces (`char V = char U + char W`). Reusable. Plus helpers
  `mem_glWeightSpace_iff`, `glWeightSpace_map_le`, `glWeightSpace_inf_range`.
- `polyRight_iSup_glWeightSpace_eq_top` — the weight spaces of `A_d` span `⊤`.
- `quotDetDegreeSubrep` / `quotDetDegreeFDRep` — Deliverable 1: the degree-`d`
  component `(A/det)_d` as an `FDRep` (image of `A_d` in `A/det`), no sorry in body.
- `detChar_diagUnit_val` — `det(diagUnit i t) = t`.

In progress (Step B, building): `twistFDRep` (the twist `A_e ⊗ χ`) and
`formalCharacter_twistFDRep : char(A_e ⊗ χ) = (∏ᵢ Xᵢ) · char A_e`, via
`glWeightSpace_twistFDRep_pos`/`_zero` (weight shift by `(1,…,1)`).

## Current frontier

Step B (twist character) building. Step C (the final assembly
`quotDetDegreeFDRep_formalCharacter`) not yet written: build the SES
`0 → twistFDRep(d-N) → polyRightDegreeFDRep(d) → quotDetDegreeFDRep(d) → 0`
(ι = mulDet restricted, π = mkQ restricted; exactness from
`detSubmodule_inf_homogeneous`), apply `formalCharacter_add_of_shortExact`, then
`polyRightDegreeFDRep_formalCharacter` (both degrees) + `formalCharacter_twistFDRep`
+ `cauchyMult_mul_prodX_eq_lastPart_pos` to land the `0 ∈ range ν.parts` identity.

## Overall project progress

Chapter 5 Cauchy / determinant-quotient strand. #4960 is part-B of #4905 (#4896
part (a)). Deliverable 1 (the FDRep) done; Deliverable 2 (the formal-character
identity) in progress.

## Next step

Finish Step B build, then write Step C assembly. If Step C plumbing overruns,
land the partial (additivity + spanning + def + twist character) and file a
focused successor for the final character identity (all infrastructure is in
place; only the SES-map construction + arithmetic remains).

## Blockers

None. `polyRightDegreeFDRep_formalCharacter` (a cited dependency) is now
sorry-free (landed in #4978).
