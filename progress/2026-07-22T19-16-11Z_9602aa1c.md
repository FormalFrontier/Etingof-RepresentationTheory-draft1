## Accomplished

Review session (Stage 3.7 coverage-arm audit, issue #7375): classified the three
remaining §3.9 items in `progress/items.json`.

- **Problem 3.9.2** → `covered_full` (both parts). (a) Ext¹ fully computed
  (`ext1_self` ℂⁿ, `ext1_subsingleton_of_ne` 0) + 2-dim classification
  (`two_dim_is_extension`); (b) `infinitely_many_indecomposables`. `derived` array
  added.
- **Problem 3.9.3** → `covered_partial`. Irreducibles + 2-dim classification full,
  but the Ext¹ sub-question only proves the **vanishing** characterization
  (`ext1_simpleRep_vanishes_iff`), not the book's dimension formula
  `dim Ext¹(S_i,S_j)=card(i⟶j)`. Opened follow-up **feature #7376** for the gap.
- **Problem 3.9.5** → `covered_full` (both parts). Even/odd matrix-algebra
  isomorphisms (`even_isMatrixAlgebra`, `odd_isSumMatrixAlgebra`) fully determine
  the irreducibles; iff + Rad-quotient (`isSemisimpleRing_iff_nondegenerate`,
  `radicalQuotient_isClifford_of_degenerate`) cover (ii).

Populated `lean_decl`, `coverage_arm`, `last_updated`, `coverage_note`; fixed stale
`sorry_free: false`; normalized `lean_file` to a list. Wrote coverage report
`progress/reviews/2026-07-22-ch3-problem3_9_2_3_5-coverage.md`.

Verification: `lake build` on all three modules succeeds sorry-free; every headline
decl `#print axioms`-clean (`[propext, Classical.choice, Quot.sound]`, no `sorryAx`)
via the **olean-import** method; all recorded `lean_decl`s `#check`. `jq empty`
passes; items.json diff is three localized hunks.

## Current frontier

§3.9 coverage-arm sweep complete (3.9.1/3.9.4 in #7363; 3.9.2/3.9.3/3.9.5 here).
One follow-up (#7376) is the only open §3.9 coverage gap.

## Overall project progress

Stage 3.7 exercise-coverage ratchet ongoing. §3.9 now fully coverage-classified.
Sibling audits still unclaimed: #7342 (§5.8), #7346 (§5.3.3).

## Next step

A feature agent can pick up #7376 (dim Ext¹ formula for path-algebra simples). A
review agent can take #7342 or #7346.

## Blockers

None.

## Note for /reflect

`.claude/commands/review.md` recommends appending `#print axioms` to the source and
running `lake env lean <source>`. For `Problem3_9_2.lean` this produced a **false
`sorryAx`** (nondeterministic `synthInstanceFailed`/`rewrite failed` on re-elaboration
that never occur under `lake build`). The reliable method is a scratch file that
`import`s the already-built module and runs `#print axioms` there. Should update the
guidance.
