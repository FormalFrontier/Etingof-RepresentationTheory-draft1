## Accomplished

Meta `/work` session. Triaged the unclaimed queue: three of four unclaimed feature
issues (#6677, #6679, #6684) had prerequisites that only exist in open, still-building
PRs (not on `main`), so I skipped #6679 (the one I had claimed) back to `replan` rather
than stack on an unmerged branch under squash-merge.

Claimed and worked #6674 (`mckayAdj_no_selfLoop`, the sole remaining `sorry` in
`mckay_isAffineDynkin`). Its book proof needs SU(2) subgroup infrastructure of
Problem 4.12.8 that does not exist yet, so I **decomposed** it into four sub-issues and
**landed the first, concrete piece** (#6689):

- `#6689` (this PR): central `-Id ∈ SU(2)` facts.
- `#6690` (→#6689): non-cyclic case — central involution acts as ±1 (Schur) + the
  `g ↦ g·z` character argument giving `rᵢᵢ = 0`.
- `#6691` (→#6689): the cyclic-or-contains-`-Id` dichotomy (Problem 4.12.8 level; hard).
- `#6692`: cyclic-case splitting `V ≅ χ ⊕ χ⁻¹`, `χ` nontrivial ⇒ `rᵢᵢ = 0`.

Landed in `EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean` (all sorry-free,
axioms `propext, Classical.choice, Quot.sound`):

- `negIdSU : specialUnitaryGroup (Fin 2) ℂ` — the element `-1`, constructed via
  `mem_specialUnitaryGroup_iff` (unitary + `det = 1`). Real definition, not sorry'd.
- `negIdSU_coe`, `negIdSU_central` (scalar `-1` commutes with all of SU(2)).
- `tautRep_negId` — the tautological action of `-Id` on `V` is `v ↦ -v`.
- `charV_negId` — `χ_V(-Id) = -2`.

## Current frontier

`mckay_isAffineDynkin` still has exactly one `sorry` (`mckayAdj_no_selfLoop`). The
concrete `-Id` machinery it needs now exists; the remaining work is #6690 (non-cyclic),
#6691 (dichotomy), #6692 (cyclic), then assembling them into the parent.

## Overall project progress

Chapter 6 Problem 6.1.6 parts (a),(b) and most of (c) are done; the only gap is the
no-self-loop conjunct of part (c). Broad project is deep into Chapters 5–8
(Künneth/Tor rearrangement effort in flight via #6685/#6688/#6682 PRs).

## Next step

Land #6690 (non-cyclic no-self-loop) on top of this: build `central_involution_scalar`
via `Etingof.Corollary_2_3_10` (Schur), then the `g ↦ g·z` character-sum argument using
`FDRep.scalar_product_char_eq_finrank_equivariant` + `charV_negId`/`tautRep_negId`.
#6692 (cyclic case) can proceed in parallel. #6691 (dichotomy) is the hard blocker.

## Blockers

None for #6689 (landed). #6690 depends on #6689 (this PR) merging. #6677/#6679/#6684
remain blocked on their in-flight prerequisite PRs (#6685/#6688/#6682) reaching `main`.
