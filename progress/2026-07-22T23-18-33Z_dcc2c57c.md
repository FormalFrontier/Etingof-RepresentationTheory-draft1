## Accomplished

Resolved issue #7422 (fix Ch9 Definition 9.4.3: add right homological dimension).

- `Chapter9/Definition9_4_3.lean`: kept the historical `Etingof.HasHomologicalDimensionLE` /
  `Etingof.homologicalDimension` as the **left** versions (all existing Chapter 9 consumers
  depend on these names). Added explicit aliases `HasLeftHomologicalDimensionLE` /
  `leftHomologicalDimension`, and new **right** versions `HasRightHomologicalDimensionLE R d :=
  HasHomologicalDimensionLE Rᵐᵒᵖ d` and `rightHomologicalDimension R := homologicalDimension Rᵐᵒᵖ`
  (right modules = left modules over the opposite ring). Added `@[simp]` transport lemmas
  `hasRightHomologicalDimensionLE_iff_op` and `rightHomologicalDimension_eq_left_op` (both `rfl`).
- `Chapter9/HomologicalDimensionRingEquiv.lean`: added the commutative-ring agreement lemmas
  `hasRightHomologicalDimensionLE_iff_left` and `rightHomologicalDimension_eq_left`, using
  `RingEquiv.toOpposite R : R ≃+* Rᵐᵒᵖ` together with the existing
  `hasHomologicalDimensionLE_congr` / `homologicalDimension_congr`.
- Updated `progress/items.json` coverage for `Chapter9/Definition9.4.3`
  `covered_partial → covered_full`, removed the `followup_issue`.

Full `lake build EtingofRepresentationTheory.Chapter9` succeeds (only pre-existing warnings in
`Problem9_4_6.lean` and the chapter header).

## Current frontier

Issue #7422 complete. No sorries introduced; all transport lemmas are `rfl` / one-line proofs.

## Overall project progress

Stage 3 formalization ongoing; this was a definition-level fidelity fix (highest-priority class).
Many `feature`/`completeness-audit` issues remain unclaimed (#7400–#7453 range).

## Next step

Create the PR for #7422 and exit. Other high-value definition-fidelity issues still open:
#7425 (Morita k-linearity), #7443 (progenerator finite vs arbitrary), #7437 (Young projectors).

## Blockers

None.
