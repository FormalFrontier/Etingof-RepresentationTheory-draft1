## Accomplished

Reviewed issue #6641 — the three recently-merged McKay Cartan results in
`EtingofRepresentationTheory/Chapter6/Problem6_1_6.lean`:
`mckayCartan_posSemidef`, `mckayCartan_not_posDef`, `mckay_dims_are_marks`.

Verdict: **all three are faithful, non-vacuous, and sound.**

- **Definitions are genuine.** `mult` = `finrank ℂ (W i ⟶ V G ⊗ W j)` (real Hom
  multiplicity, Schur), `mckayAdj` = `(mult ·)`, `mckayCartan` = `2δ − mult`,
  `V G` = `FDRep.of (tautRep G)` (genuine tautological `SU(2)` action with
  `map_one'`/`map_mul'` proved), `IsCompleteIrreps` = a real `Prop` structure
  (`simple`/`distinct`/`exhaustive`). No `True` placeholders, no sorry'd `def`
  bodies.
- **Axioms.** `#print axioms` on all three theorems (and on `mult`, `mckayAdj`,
  `mckayCartan`, `mult_symm`, `mckay_marks_aux`) shows only
  `[propext, Classical.choice, Quot.sound]` — no `sorryAx`. They do not route
  through the two sorry'd theorems `mckay_connected` (b) / `mckay_isAffineDynkin`
  (full c).
- **Faithfulness.** `posSemidef` = `∀ x : Fin m → ℤ, 0 ≤ xᵀ M x` (integer test
  vectors capture PSD of a symmetric integer form). `not_posDef` exhibits a
  genuinely nonzero null vector (the dimension vector; entry `i₀` for the trivial
  rep has dim 1). `dims_are_marks` = `M d = 0`. All match Etingof 6.1.6 (c)/(e).
- **Soundness.** The PSD proof genuinely uses the `2I − A` structure: it rewrites
  `xᵀMx` as `(1/|G|) ∑_g (2 − χ_V(g))|f(g)|²` and bounds each factor via the
  `SU(2)` trace bound `charV_re_le_two` — not a degenerate form.

Observation (not a defect): `hne : Nontrivial G` is **unused** in both
`mckayCartan_posSemidef` and `mckayCartan_not_posDef` (verified by removing it —
the file still builds). The two pieces hold for every finite `G ⊆ SU(2)`. Kept
`hne` as-is: it documents the book's stated context (`G ≠ {1}`) and parallels the
sorry'd `mckay_isAffineDynkin` that genuinely needs it. The linter does not flag
`hne` because `Nontrivial` is a class type.

Cleanup applied: renamed the two genuinely-unused `hW : IsCompleteIrreps W`
binders (in `mult_symm`, part (a) — symmetry follows from self-duality of `V`
alone; and in the `McKayCorrespondence` Prop def, part (d)) to `_hW`, silencing
two `unusedVariables` linter warnings. Build still exits 0; only the two expected
`sorry` warnings (parts b / full c) remain.

## Current frontier

Review complete. PR opened for the two-line linter cleanup and the verdict posted
as a comment on #6641.

## Overall project progress

Stage 3 formalization ongoing. Problem 6.1.6 parts (a), (c)-explicit-form, (e)
are sorry-free and now independently reviewed; parts (b) `mckay_connected` and
full (c) `mckay_isAffineDynkin` remain sorry'd.

## Next step

A feature session could tackle `mckay_isAffineDynkin` (assemble
`posSemidef` + `not_posDef` + no-self-loops via the central `−Id` + Problem
6.1.3) or `mckay_connected` (Problem 4.12.10).

## Blockers

None.
