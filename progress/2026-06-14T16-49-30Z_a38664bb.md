## Accomplished

Closed **Sub-A1 (#4669)** of the Frobenius route to `dim V_λ = #SYT` (#4595 → #4608).
Added two sorry-free `private` lemmas to
`EtingofRepresentationTheory/Chapter5/CharValueHookFormula.lean`:

- `coeff_sum_X_pow_eq_multinomial` (step 4): the coefficient of
  `monomial (symm γ) 1` in `(∑ᵢ Xᵢ)ⁿ` is the multinomial coefficient
  `Nat.multinomial univ γ`, for `γ` summing to `n`. Proved from
  `Finset.sum_pow_eq_sum_piAntidiag`.
- `charValue_trivialCycleType_eq_descPochhammer_det` (steps 1–6, the deliverable):
  `charValue = (n!/∏ βⱼ!) · det((descPochhammer ℤ (eⱼ)).eval βᵢ)`, where
  `β = shiftedExps`, `e = vandermondeExps`.

Proof route as designed in `progress/frobeniusDetForm-route-design.md`:
expand the alternant `det` as a signed monomial sum (`Matrix.det_apply`), shift each
coefficient through `(∑X)ⁿ` (`coeff_monomial_mul'`), read off the multinomial
coefficient, and recognise the signed sum of falling factorials as the
descending-Pochhammer determinant. The ℕ-level cancellation
`multinomial γ · ∏βⱼ! = n! · ∏ descFactorial(β(σi), eᵢ)` uses
`factorial_mul_descFactorial` + `Nat.multinomial_spec`; the support-gap branch
uses `descFactorial_eq_zero_iff_lt`. The ℚ-cast of the ℤ determinant goes through
`RingHom.map_det` + `descPochhammer_eval_eq_descFactorial`.

`lake build EtingofRepresentationTheory.Chapter5.CharValueHookFormula` succeeds;
the only remaining `sorry` in the file is the pre-existing capstone
`charValue_trivialCycleType_eq_frobeniusDetForm` (#4671's job).

## Current frontier

`charValue_trivialCycleType_eq_frobeniusDetForm` still `sorry` at
`CharValueHookFormula.lean:409`. It now has both halves available:
Sub-A1 (this PR, the descPochhammer determinant) and Sub-A2 (#4670, the Vandermonde
column reduction, still open). The capstone #4671 combines them.

## Overall project progress

Stage 3 formalization. Frobenius route Part A is now two-thirds done:
Sub-A1 landed, Sub-A2 (#4670) outstanding, capstone (#4671) blocked on both.

## Next step

Pick up **#4670** (Sub-A2, Vandermonde column reduction): prove
`det (of fun i j => (descPochhammer ℤ (N-1-j)).eval (β i)) = ∏_{i<j}(β i − β j)`
via `Matrix.det_eval_matrixOfPolynomials_eq_det_vandermonde` + `det_vandermonde`,
handling the `j ↦ N-1-j` column-reflection sign. Then #4671 capstone combines
Sub-A1 + Sub-A2 to close `frobeniusDetForm`. Coordinate the exact statement of
Sub-A1's determinant with the capstone (it consumes
`charValue_trivialCycleType_eq_descPochhammer_det`).

## Blockers

None.
