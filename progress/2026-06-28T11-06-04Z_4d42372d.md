## Accomplished
Worked issue #5550 (`peterWeylMap_surjective`, the Cauchy spanning half of Peter-Weyl for
`GL_n`). After confirming (via thorough exploration) that the matrix-coefficient-correspondence
bridge and the finite-dimensional right-translation-hull infrastructure do not exist and a full
proof is multi-session research work, I landed a **verified reduction** that isolates the crux:

- Added `range_toModule_eq_iSup_range` (sorry-free, reusable): `range (DirectSum.toModule R ι M f)
  = ⨆ i, range (f i)`. The surjectivity counterpart of `injective_toModule_of_iSupIndep_range`.
- Rewrote `peterWeylMap_surjective` to be **sorry-free in its body**, reducing it to the clean
  spanning statement `peterWeylSummandMap_iSup_range_eq_top : ⨆ lam, range (peterWeylSummandMap n
  lam k) = ⊤`.
- Isolated that spanning statement as the sole remaining sorry, with a detailed 4-step proof-route
  docstring (finite-dim right-translation hull → complete reducibility → φ is a matrix coefficient
  of its hull → matrix-coefficient correspondence).
- Created sub-issue #5572 for the crux.

`lake build EtingofRepresentationTheory.Chapter5.Theorem5_23_2_PeterWeyl` is green; the only sorry
is the isolated crux `peterWeylSummandMap_iSup_range_eq_top`.

## Current frontier
The Cauchy spanning crux `peterWeylSummandMap_iSup_range_eq_top` (#5572). The two genuinely
missing pieces: (a) the finite-dimensional right-translation-hull machinery (hull def, finite
dimensionality, `localRightRep`-invariance, det-twist to a polynomial rep so
`decompose_polynomial_gl_rep` applies); (b) the matrix-coefficient correspondence relating the
abstract `peterWeylSummandMap` to arbitrary `GL`-equivariant maps `L_λ → R`.

## Overall project progress
Peter-Weyl for `GL_n` (Theorem 5.23.2(ii)): injectivity half (`peterWeylMap_injective`) is
sorry-free; surjectivity half now reduced to a single clean spanning sorry (#5572). All the
equivariant-structure assembly (`peterWeylMap_equivariant`, `nonempty_equivariantEquiv_of_bijective`)
is sorry-free.

## Next step
Tackle #5572. Recommended decomposition: first land the finite-dimensional right-translation-hull
infrastructure (steps 1–3 of the route) as a building-block file, then the matrix-coefficient
correspondence (step 4), then assemble. Expect a multi-PR effort.

## Blockers
None at the definition level. #5572 is a research-level proof obligation, not a blocker — all
inputs (`Cauchy*`, `PolynomialGL*`, `localRightRep`, `evalGLAway`) are sorry-free; the gap is the
assembly bridge.
