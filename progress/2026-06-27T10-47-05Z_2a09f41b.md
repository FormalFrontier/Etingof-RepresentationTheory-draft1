## Accomplished

Discharged issue #5523 — assembled the typed contragredient pairing
`algIrrepDualPairing` and proved its perfectness, in the new file
`EtingofRepresentationTheory/Chapter5/AlgIrrepDualPairing.lean`.

Deliverables, all sorry-free in their bodies:

- `algIrrepDualPairing n lam k : AlgIrrepGLDual n lam k ⊗[k] AlgIrrepGL n lam k →ₗ[k] k`
  — `contractLeft k (AlgIrrepGL n lam k) ∘ₗ TensorProduct.map e id`, where
  `e := algIrrepGLDualIso` is a chosen representative of #5522's iso
  `algIrrepGLDual_iso_linearDual` (real data via `Classical.choice`, not sorried).
- `algIrrepDualPairing_invariant` — `GL_n`-invariance for the diagonal action
  `(algIrrepGLRepDualρ g, algIrrepGLRepρ g)`, by transporting
  `contractLeft_dual_invariant` (#5515) across the intertwining property of `e`.
- `algIrrepDualPairing_ne_zero` — nonvanishing (needs `[Nontrivial (AlgIrrepGL …)]`):
  `TensorProduct.map e id` is surjective (it is the underlying map of
  `TensorProduct.congr e (.refl)`), so the composite inherits nonvanishing from
  `contractLeft_ne_zero`.
- `algIrrepDualPairing_nondegenerate` — **perfectness** over `ℂ` in the Schur-Weyl
  range (`∑ lam.toNatWeight ≤ n` and the same for `lam.w0Twist`): both factors are
  simple (`algIrrepGLRep_isSimple`), so the curried bilinear form is nondegenerate
  on both sides.

Supporting reusable lemmas (general field/group):
- `ker_eq_bot_of_invariant_of_simple` — a nonzero invariant pairing has trivial
  left radical when the left factor is simple (radical is a `ρV`-invariant
  `k[G]`-submodule, hence `⊥`), mirroring
  `nondegenerate_of_invariant_of_simple` (FrobeniusSchurRealType.lean) for the
  two-module case.
- `nondegenerate_pairing_of_invariant_of_simple` — both-sided version, applying the
  above to `B` and to `B.flip`.

Registered the file in `Chapter5.lean`. `lake build
EtingofRepresentationTheory.Chapter5` is green (8742 jobs); the new file has no
`sorry` and no warnings.

## Current frontier

The typed pairing and its perfectness are landed. This is brick (1) of discharging
`Theorem5_23_2_ii_equivariant` (the matrix-coefficient map
`u ⊗ v ↦ (g ↦ ⟨u, g⁻¹ v⟩)` of Peter-Weyl).

## Overall project progress

Phase 3 formalization. Theorem 5.23.2(ii) statement is in place
(`Theorem5_23_2_PeterWeyl.lean`); its proof is being assembled from bricks. This
turn lands the typed contragredient pairing with perfectness. Remaining inputs to
the equivariant Peter-Weyl iso are the matrix-coefficient map and the Cauchy
decomposition (`PolynomialGLDecomposition`, `CauchyDetQuotient*`).

## Next step

Use `algIrrepDualPairing` + `algIrrepDualPairing_invariant` to build the
`GL_n × GL_n`-equivariant matrix-coefficient map `L*_λ ⊗ L_λ → R`,
`u ⊗ v ↦ (g ↦ ⟨u, g⁻¹ v⟩)`, the next brick toward
`Theorem5_23_2_ii_equivariant`. Note the underlying contragredient identity
`algIrrepGLDual_iso_linearDual` (#5522) still rests on the sorried character input
`exists_detTwist_equivariantEquiv_dual` (#5526).

## Blockers

None for this issue. Downstream perfectness is scoped to `ℂ` and the Schur-Weyl
range because `algIrrepGLRep_isSimple` is currently only available there;
generalizing simplicity (separate work) would lift the scope.
