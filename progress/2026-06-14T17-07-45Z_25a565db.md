## Accomplished

Work session `25a565db` (`/work` → `/feature`), issue **#4683**
(`detInv_elim_of_polynomial`, the det⁻¹-elimination core of #4654 / Theorem
5.23.2(i) polynomial case).

After deep analysis I established that #4683 is genuinely multi-session and
**decomposed it** into a concrete, correct route:

- Wrote `progress/detInv-elim-route-analysis.md` — pins down *why* the
  elementary routes fail and isolates the kernel lemma. Key new finding: even
  **two-sided** torus-weight nonnegativity does not force `det^r ∣ Q` (the
  `N=2` counterexample `α·g₁₁g₂₂ + β·g₁₂g₂₁` has *both* torus weights ≥ 0 yet
  is not `det`-divisible). The constraint must come from the full `GL` action —
  i.e. the `GL×GL` coordinate-ring decomposition (Peter–Weyl, Theorem
  5.23.2(ii)). This corrects/sharpens the two vague "honest routes" in the
  original #4683 body.
- Created **#4694** (sub-K, the kernel lemma: a nonneg-weight finite-dim
  right-`GL_N`-subrep of `k[Xᵢⱼ,det⁻¹]` lies in `k[Xᵢⱼ]` — the real math) and
  **#4695** (sub-A, assembly of `detInv_elim_of_polynomial` from the kernel,
  `depends-on #4694`).
- Landed the route doc via a partial PR; skipped/`replan`'d #4683 with a
  `Decomposed into #4694, #4695` breadcrumb.

## Current frontier

`detInv_elim_of_polynomial` (`Chapter5/PolynomialRepEmbedding.lean:1013`)
remains the lone `sorry` blocking the polynomial-rep → `V^⊗n` embedding chain.
The route to close it is now concrete: prove #4694 then assemble #4695.

## Overall project progress

Etingof draft 1, Stage 3. Ch5 Schur-Weyl: the det⁻¹-elimination piece (#4654)
now has a precise decomposition (#4694 kernel + #4695 assembly) instead of a
single deep `sorry`. Ch5 Frobenius route (#4595) progressing separately
(#4669 sub-A1 in flight, #4671 capstone pending). Existing coordinate-ring
infrastructure to build #4694 on: `Theorem5_23_2.lean` (`GLCoordinateRing`,
`DominantWeight`, `AlgIrrepGL`, `Theorem5_23_2_ii`), `PolynomialTensorBridge`,
`PolynomialGLDecomposition`, weight direct-sum infra in `FormalCharacterIso`.

## Next step

A worker claims **#4694** (the kernel lemma). It is genuinely deep and may need
further decomposition against the `Theorem5_23_2.lean` Peter–Weyl machinery —
that is an acceptable outcome. Once #4694 lands, #4695 assembly is mechanical.

## Blockers

#4695 blocked on #4694. #4654 / the polynomial-rep embedding chain transitively
blocked on #4694 + #4695. No environment blockers.
