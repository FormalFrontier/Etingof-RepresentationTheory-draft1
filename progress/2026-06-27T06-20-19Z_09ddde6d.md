## Accomplished

Finished `Theorem5_18_4_partition_decomposition` (issue #5405) — the partition-indexed
form of Schur-Weyl duality — sorry-free, and hence `Etingof.Corollary5_19_2`.

New / changed:
- `EtingofRepresentationTheory/Infrastructure/SimpleModuleCount.lean` (new). Reusable
  counting infrastructure:
  - `transportModule` / `transportLinearEquiv` — conjugation transport of a
    `k`-algebra module structure along a `k`-linear equiv.
  - `repOfModule` (`g ↦ single g 1 • ·`) and `repOfModuleAsModuleEquiv` bridging a
    `k[G]`-module to a `Representation` whose `asModule` recovers the action.
  - `card_le_card_conjClasses` (modules in `k`'s universe) and
    `card_le_card_conjClasses'` (any universe, via small models `Fin (finrank) → k`):
    a `Fintype` family of pairwise non-iso simple `k[G]`-modules, finite over `k`, has
    `card ≤ |ConjClasses G|`. Built on `Etingof.Corollary4_2_2`.
- `Infrastructure/IrreducibleEnumeration.lean`: de-privatised
  `FDRep.simple_of_isSimpleModule_asModule`.
- `Chapter5/SchurWeylBimoduleFull.lean`: `_full` now also exposes
  `IsScalarTower k (symGroupImage) (S i)` (needed by the count).
- `Chapter5/SchurWeylPartition.lean` (new). Holds the moved, now sorry-free
  `Theorem5_18_4_partition_decomposition`:
  - `card_le_card_partition`: `card ι ≤ p(n)` via restriction along the surjection
    `k[Sₙ] ↠ symGroupImage` (`symGroupAlgHomToImage`) + `card_le_card_conjClasses'` +
    `|ConjClasses(Sₙ)| ≤ p(n)`.
  - generic `DirectSum` plumbing: `directSumCongrRight`, `directSumSigmaLequiv`,
    `directSumUniqueEquiv`, `directSumTensorSubsingleton`.
  - the reindex: `S' p / L' p = ⨁_{i : fibre p} S i / L i` over the subsingleton
    fibre `{i // e i = p}`; simple when singleton, `Subsingleton` when empty; global
    iso regroups `⨁_i S i ⊗ L i` by fibres and collapses subsingleton-fibre tensors.
- `Chapter5/Theorem5_18_4.lean`: removed the old sorry stub (left a pointer comment).
- `Chapter5/Corollary5_19_2.lean`, `Chapter5.lean`: import the new file.
- `progress/items.json`: `Chapter5/Theorem5.18.4` set `fidelity: verified`.

## Current frontier

`Theorem5_18_4_partition_decomposition` and `Corollary5_19_2` build sorry-free.
A full `lake build` was running at handoff to confirm no other file regressed from
the `_full` signature change / theorem move.

## Overall project progress

Phase 3 (formalization). Schur-Weyl duality Theorem 5.18.4 part (iii) is now complete
and faithful; this closes the last fidelity gap for that theorem (history: #5383,
#5326). The new `SimpleModuleCount` infrastructure (count of simple `k[G]`-modules)
is reusable elsewhere.

## Next step

Confirm the full build is green and merge the PR for #5405.

## Blockers

None.
