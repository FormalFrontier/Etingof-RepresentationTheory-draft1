## Accomplished

Restored fresh-buildability of `Chapter5/Theorem5_27_1.lean` (issue #7507). The file
had nine source-check failures (lines 1116, 1194, 1856, 2023, 2029, 2051, 2056, 2090,
2096) that cached oleans concealed from the aggregate build. All were carrier/transport
mismatches between the opaque `inducedRepV` value and its raw function-space
implementation `(G ⧸ stabAux φ χ) → ↥U`: `rw [map_smul]` / `rw [Pi.smul_apply]` /
`simp [map_add]` could not match because the scalar-action term lived at the raw Pi
carrier while the enclosing linear map (`T`, `T.symm`, `U.ρ`) expected the `.V` carrier
(defeq but not syntactic).

Fix pattern (no statements changed):
- Replaced the fragile `rw [map_smul]`/`rw [map_add]` steps with term-mode
  `T.map_smul`/`T.symm.map_smul`/`(FDRep.ρ U _).map_smul`/`T.map_add`, closed by `exact`
  (defeq), staged behind a `have`/`rw` so the carrier is fixed at `.V`.
- Dropped the redundant `Pi.smul_apply` from the two `A_action_scalar` rewrites; the
  subsequent `exact hco` already closes by defeq.
- Rewrote `h_inj`'s `hsub` to `rw [sub_smul c₁ c₂ v, h, sub_self]` (explicit args avoid
  the higher-order-unification failure of bare `rw [sub_smul]`).

Added `Chapter5/Theorem5_27_1_Test.lean` (downstream import/`#check` test) and wired it
into `Chapter5.lean`, matching the `Chapter4/Theorem4_2_1_Test.lean` convention. It
`#check`s `Etingof.Theorem5_27_1` and `Etingof.inducedRepV_basepoint_independent` and
signature-locks the 15-component classification tuple (including base-point clause vii).

## Current frontier

Issue #7507 complete. Verified:
- `lake env lean EtingofRepresentationTheory/Chapter5/Theorem5_27_1.lean` — exit 0, 0 errors.
- `lake build EtingofRepresentationTheory.Chapter5.Theorem5_27_1` — exit 0.
- `lake env lean EtingofRepresentationTheory/Chapter5/Theorem5_27_1_Test.lean` — exit 0.
- `#print axioms` on both public endpoints — only `propext`, `Classical.choice`,
  `Quot.sound` (no `sorryAx`, no replacement axiom). No `sorry`/`admit` in the file.

The stable public API is `Etingof.Theorem5_27_1`, whose data (`dualSmul`, `stab`, `V`,
`transport`) and nine conjuncts (including base-point independence, clause vii) are
existentially packaged with no private-name leakage. `inducedRepV_basepoint_independent`
remains the named internal engine; its printed type still shows private `✝` names (as the
issue noted), but it is importable and `#check`s downstream.

## Overall project progress

Stage 3 formalization maintenance. Another "restore fresh-buildable" regression cleared;
~46 similar issues remained unclaimed at start of this turn.

## Next step

Pick another unclaimed `restore fresh-buildable` issue (e.g. #7534 once its dependency
#7554 lands, or a self-contained one like #7508/#7509). Same defeq-carrier fix pattern
recurs across the induced-representation files.

## Blockers

None.
