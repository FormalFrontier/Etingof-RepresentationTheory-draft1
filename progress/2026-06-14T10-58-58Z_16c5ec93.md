## Accomplished

Closed issue #4531 (Ch6 #2967 sub-A): D̃₇ F-generic indecomposability
infrastructure.

- Deliverable 1 (`d7tilde_chain_collapse`, identity-chain collapse
  W₁⟨3⟩=W₁⟨4⟩=W₁⟨5⟩) was already present and sorry-free at HEAD; left
  as-is.
- Deliverable 2 (`d7tildeRep_kQ_leaf_equalities`) implemented from
  scratch, mirroring the d5tilde/d6tilde precedents. Added a new
  "Section 4b/4c" to `Chapter6/FieldGenericD7Tilde.lean`:
  - `d7tilde_core_F` — v=2 left-branch core decomposition (verbatim
    port of `d5tilde_core_F`).
  - `d7tilde_core5_F` — v=5 right-branch core decomposition
    (index-shifted port of `d5tilde_core3_F`, `3↦5, 4↦6, 5↦7`).
  - `d7tilde_core_F_proj1/2`, `d7tilde_core5_F_proj1/2` — reversed-edge
    projection siblings (one leaf component each via the left-inverse
    identities).
  - `d7tilde_gamma_containment_F` — γ-coupled leaf containments,
    threading the two identity chain edges `3-4-5` through the collapse
    equality `Wmain⟨3⟩=Wmain⟨5⟩`.
  - `d7tildeRep_kQ_leaf_equalities` — concludes `W₁⟨0⟩=W₁⟨1⟩`,
    `W₁⟨6⟩=W₁⟨7⟩`, `W₁⟨0⟩=W₁⟨6⟩`. The all-canonical branch
    (`0→2,1→2,2→3,6→5,7→5`) and **combo D** (both v=5 leaf edges
    reversed `5→6,5→7`) are proven inline; the 31 remaining
    non-canonical branches are `sorry` (tracked by #4533, sub-B).

All new helper lemmas are sorry-free. `lake build
EtingofRepresentationTheory.Chapter6.FieldGenericD7Tilde` succeeds; the
only `sorry` warnings are `d7tildeRep_kQ_leaf_equalities` (non-canonical
branches, by design) and `d7tildeRep_kQ_isIndecomposable` (sub-C).

## Current frontier

D̃₇ indecomposability proof. With sub-A (this issue) done, the remaining
D̃₇ work is:
- #4533 (sub-B): non-canonical orientation case tree for
  `d7tildeRep_kQ_leaf_equalities` (the 5 coarse `sorry` branches — two
  mixed-direction v=5 cases need N-invariance infrastructure; three
  outer reversed branches nest those).
- #4534 (sub-C, blocked on #4531/#4533): final assembly of
  `d7tildeRep_kQ_isIndecomposable`.

## Overall project progress

Stage 3 (formalization) ongoing, Chapter 6 affine-Dynkin
infinite-type frontier. The D̃ₙ chain (D̃₄–D̃₈ + parametric D̃_{k+5})
indecomposability machinery is being built per-shape; D̃₅ and D̃₆
leaf-equality canonical branches were done previously, D̃₇ sub-A done
now.

## Next step

A worker can now claim #4533 (D̃₇ sub-B). Note that the two
mixed-direction v=5 branches (combo C / C′) are blocked on
nilpotent-shift (N) invariance infrastructure that is shared with the
d5tilde/d6tilde mixed cases — #4554 is building exactly that
(`FieldGenericStar` N-stability). Sub-B may want to depend on #4554.

## Blockers

None for this issue. Sub-B (#4533) mixed-direction branches are
effectively blocked on the shared N-invariance infrastructure (#4554,
currently claimed).
