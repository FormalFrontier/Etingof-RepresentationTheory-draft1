## Accomplished

Closed #4982 (`feat(Ch5 #4979 sub-B): general-k Wedderburn block machinery + counting +
classification assembly`), the final deliverable of the #4979 / #4946 classification half.

Created `EtingofRepresentationTheory/Chapter5/Theorem5_12_2_ClassificationGeneral.lean`,
porting the entire ℂ-hardcoded `Theorem5_12_2_Classification.lean` to an arbitrary field
`k` with `[Field k] [IsAlgClosed k] [CharZero k]`. New public results:

- `Theorem5_12_2_classification_general` — every simple `k[S_n]`-module is `≃ₗ` to some
  `SpechtModuleK k n λ`. Axiom-clean (`propext, Classical.choice, Quot.sound`; no `sorryAx`).
- `irrepDecomp_n_le_card_partition_general` — `D.n ≤ |Nat.Partition n|` via the already-generic
  `Corollary4_2_2` plus the field-free conjugacy-class→partition cycle-type injection.

The Wedderburn block machinery (`centralIdem'` … `same_block_iso`,
`blockOf_specht_injective_general`, `exists_young_symmetrizer_nontrivial_general`) ported
mechanically — it only ever touches `IrrepDecomp` blocks and abstract simple modules / left
ideals, so `ℂ → k` plus the `[IsAlgClosed k]` binder was sufficient. Specht-specific inputs
came from the existing general-`k` infrastructure: `SpechtModuleK_isSimpleModule_general`,
`Theorem5_12_2_distinct_general` (sub-A, #4981, merged), and `youngSymmetrizerK_sq_ne_zero`.

Key simplification vs. the issue plan: the `D.n ≤ |Partition n|` bound needs only
`Corollary4_2_2` + the field-free `card_conjClasses_le_card_partition`, so none of the
center-dimension machinery (`finrank_center_pi_matrix`, `wedderburn_blocks_le_card_partition`)
had to be ported. The required `NeZero (Nat.card (S_n) : k)` instance is supplied automatically
by `neZero_card_perm_general` under `CharZero`.

The existing ℂ theorems in `Theorem5_12_2_Classification.lean` are untouched.

## Current frontier

`lake build EtingofRepresentationTheory.Chapter5.Theorem5_12_2_ClassificationGeneral` is green
(only pre-existing style-linter warnings, matching the ℂ file's `open scoped Classical`).

## Overall project progress

Chapter 5 general-`k` classification half (#4979 / #4946) now has its terminal assembly. The
remaining open Ch5 work (per the queue) is the algebraicity-bridge residual #4983 and the
#4905 Cauchy/grading assembly (deps #4960/#4961 still in progress).

## Next step

A planner should verify #4974 (general-k Specht character bridge, previously blocked on #4982)
is now unblockable once #4982's PR merges; `coordination check-blocked` will clear it.

## Blockers

None.
