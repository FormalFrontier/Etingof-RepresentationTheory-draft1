## Accomplished

Feature session on issue #6596 (Ch9 Problem 9.5.3(i)-A: central character map + linkage
invariance). Both deliverables completed sorry-free in
`EtingofRepresentationTheory/Chapter9/Problem9_5_3.lean`:

1. **Central character.** Added `CentralIdempotent R` (abbrev for idempotent central elements),
   `centralCharacter` (a `Bool` recording whether a central idempotent acts as the identity on a
   simple module — a real `decide`-based def, not sorried), and the two characterizing lemmas
   `centralCharacter_eq_true_iff` / `centralCharacter_eq_false_iff`.

2. **Linkage invariance.** Proved `centralCharacter_eq_of_areLinked`: linked simple modules share
   their central character at every central idempotent. Supporting lemmas:
   - `mk₀_smul_id_comp` (the crux): for a central element `z`, `(mk₀ (z•𝟙 X)).comp α = α.comp (mk₀ (z•𝟙 Y))`, both `= z • α`. This works because `ModuleCat R` is `Linear` over `Subring.center R` (via the scoped `ModuleCat.Algebra.instLinear` + the `Algebra (Subring.center R) R` instance), so `Ext`'s `smul_comp`/`comp_smul`/`mk₀_smul` from `Ext.Linear` apply.
   - `actsAsId_iff_of_directlyExtLinked` (Ext-adjacency step): a character mismatch across an `Ext¹`-adjacency would force every class of `Ext¹(X,Y)` to equal both `α` and `0`, contradicting `Nontrivial (Ext X Y 1)`.
   - `actsAsId_iff_of_iso` (transport along an R-linear iso), `actsAsId_iff_of_extOrIsoSimple`, and `actsAsId_iff_of_areLinked` (EqvGen closure via reflexivity/symmetry/transitivity of `Iff`).

`#print axioms` on all new declarations shows only `propext, Classical.choice, Quot.sound` — no
`sorryAx`. `lake build EtingofRepresentationTheory.Chapter9.Problem9_5_3` succeeds with no new
warnings.

## Current frontier

Central-character well-definedness engine for Problem 9.5.3(i) is done. The remaining
`sorry` in the file is the top-level bijection
`blocks_equiv_indecomposableCentralIdempotents` (needs finiteness / `1 = Σ eₖ` decomposition),
which is the other two of the three planned pieces of parent #6590.

## Overall project progress

Phase 3 formalization ongoing. This lands the first of three sub-pieces of #6590 (block ↔
central-idempotent bijection). Two central-character lemmas + linkage invariance now sorry-free.

## Next step

Continue #6590: (2) the `Block R → central idempotents` map using `centralCharacter_eq_of_areLinked`
for well-definedness, and (3) surjectivity/injectivity via the `1 = Σ eₖ` primitive-central-idempotent
decomposition under `FiniteDimensional k R`.

## Blockers

None.
