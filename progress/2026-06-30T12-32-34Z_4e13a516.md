## Accomplished
Formalized **Problem 9.3.2** and surfaced **Example 9.5.2 (iii)** (issue #5722,
folding in the #5648 part-(iii) gap). New file
`EtingofRepresentationTheory/Chapter9/Problem9_3_2.lean`:

- Constructs the algebra `A = ℂ⟨g, x⟩ / (gx + xg, x², g² - 1)` as
  `RingQuot` of `FreeAlgebra ℂ (Fin 2)`, with the three relation lemmas.
- A reusable `repHom` (representation = pair of endomorphisms satisfying the
  relations, via the free-algebra + `RingQuot` universal property) and an
  `intertwine_all`/`mkAlgLinear` helper that promotes a `ℂ`-linear map
  intertwining `g`, `x` to an `A`-linear map (generators generate `A`,
  proved by `FreeAlgebra.induction`).
- The two one-dimensional sign modules `S₊` (`g = +1`), `S₋` (`g = -1`),
  both proved `IsSimpleModule A` (every `A`-submodule restricts to a
  `ℂ`-subspace of a one-dimensional space) and non-isomorphic
  (`splus_not_iso_sminus`).
- The 2-dimensional `P₊ = ℂ²` with `g = diag(1,-1)`, `x = [[0,0],[1,0]]`,
  and the short exact sequence `0 → S₋ → P₊ → S₊ → 0` (`ses`, `ses_shortExact`).
- `extClass_ne_zero`: the extension does not split. Proved homologically via
  the covariant `Ext` long exact sequence (`covariant_sequence_exact₃`): a
  vanishing class would lift `𝟙 S₊` through `g`, i.e. give a section, which
  `A`-linearity rules out. Hence `Ext¹(S₊, S₋) ≠ 0`, so `S₊` and `S₋` are
  `Etingof.AreLinked` (`areLinked`): the algebra has a single block.

`Example9_5_2.lean` now imports the new file, re-exports the result as
`Etingof.problem_9_3_2_single_block`, and its scope docstring records that
part (iii) is formalized. Added the module to `Chapter9.lean`.

## Current frontier
`lake build EtingofRepresentationTheory.Chapter9` succeeds; no `sorry`,
`native_decide`, or `admit`.

## Overall project progress
Stage 3 formalization; Wave-3 fidelity / completeness-audit (epic #5338)
ongoing. Example 9.5.2 now has parts (i), (ii), (iii) all formalized.

## Next step
Continue completeness-audit / Wave-3 fidelity items.

## Blockers
None.
