## Current state

The det-quotient assembly chain (#4905) reads the formal character of the
degree-`d` component `quotDetDegreeFDRep k N d` of `A/det` and feeds it to the
highest-weight classification. The soundness-critical foundation for that
character lives entirely in `EtingofRepresentationTheory/Chapter5/CauchyDetQuotientDegree.lean`
(544 lines, sorry-free — the lone `sorry` token is inside a docstring comment),
landed across three feature PRs that have **not** yet been independently audited:

- **#5057 (#4960 residual):** `quotDetDegreeFDRep_formalCharacter` (`:417`, the
  `N ≤ d` degree-`d` A/det character identity) plus the short-exact-sequence
  character additivity `formalCharacter_add_of_shortExact` (`:93`) and the twist
  machinery `twistFDRep` / `formalCharacter_twistFDRep` (`:295`, `:376`).
- **#5065 (glue-1):** algebraicity of `quotDetDegreeFDRep` (cokernel of an
  algebraic rep).
- **#5066 (glue-2):** weight-space spanning + degree-`d` homogeneity —
  `polyRight_iSup_glWeightSpace_eq_top` (`:220`), the `quotDetDegreeSubrep` /
  `quotDetDegreeFDRep` definitions (`:266`, `:280`), and the weight-space
  lemmas `glWeightSpace_twistFDRep_pos`/`_zero` (`:313`, `:332`).

The in-flight glue-3 (#5064) and the #4905 final assembly both consume these
results directly, so a latent soundness defect here would silently corrupt the
det-quotient highest-weight conclusion.

## Deliverables (review — produce a PASS/FAIL verdict, no code changes to the audited file)

1. **Definition soundness:** confirm `quotDetDegreeSubrep`, `quotDetDegreeFDRep`,
   and `twistFDRep` are genuinely constructed (no sorry'd `def`/`instance` bodies,
   data is real and matches the intended `(A/det)_d` and degree-`e` twist), and
   that the `FDRep`/finite-dimensionality instances are honest.
2. **Statement faithfulness:** verify each public theorem says what its name and
   the book claim — in particular that `quotDetDegreeFDRep_formalCharacter`
   states the correct degree-`d` character (and that its `N ≤ d` hypothesis is
   actually required/used, not an artifact masking a gap), that
   `formalCharacter_add_of_shortExact` has the correct exactness hypotheses, and
   that the weight-space spanning/homogeneity lemmas are not vacuous.
3. **Axiom hygiene:** `#print axioms quotDetDegreeFDRep_formalCharacter` (and the
   other top-level results) → `[propext, Classical.choice, Quot.sound]` only; no
   `sorryAx`. Report any result that transitively pulls in a sorry.

Record the verdict in the PR body and as a brief note; if FAIL, file a follow-up
issue describing the specific defect rather than patching it inline.

## Context

- Chain progress: `progress/20260622T191135Z_ff0bc52b.md` (part-A grading),
  earlier handoffs for #4960/#5003 character work.
- Sibling audits for style/depth: #5060 (CauchyCharDiff), #5067 (right-torus
  eigenbasis) — both PASS soundness audits of adjacent Cauchy-character lemmas.
- Downstream consumer: #5064 (glue-3, claimed) and #4905 (assembly, blocked).

## Verification

`lake build EtingofRepresentationTheory.Chapter5.CauchyDetQuotientDegree` green
on a fresh checkout (run `lake exe cache get` first); axiom prints clean. The
review PR itself changes no Lean source in the audited file.
