## Current state

PR #5046 (issue #5005, Ch5 #4946 sub-C2) just merged to `main` (commit `7f32ed8c`),
landing the general-`k` (`[Field k] [IsAlgClosed k] [CharZero k]`) special-block
assembly in `EtingofRepresentationTheory/Chapter5/SchurModuleSpecialBlock.lean`.
Its sibling general-`k` clusters were audited (#5002/#5014 → #5038, #5029 → #5039),
but this sub-C2 assembly has **no** soundness-audit review issue. This item closes
that gap.

The three new declarations to audit (`SchurModuleSpecialBlock.lean`):

- `youngSymEndomorphism_ne_zero_general` (`:256`) — the Young symmetrizer
  endomorphism on `(k^N)^{⊗n}` is nonzero for antitone `lam`, via
  `schurModuleSubmodule_ne_bot (k := k)` and `LinearMap.range_zero`.
- `youngSymEndomorphism_eq_zero_of_blocks_vanish_general` (`:269`, private) — block
  factorization (`youngSym_block_factorization`) forces the endomorphism to zero
  when it vanishes on every block. Note the `omit [IsAlgClosed k] [CharZero k]` and
  the raised `maxHeartbeats 3200000` / `synthInstance.maxHeartbeats 1200000`.
- `exists_unique_special_block_general` (`:313`) — the unique
  `weightToPartition N lam`-labelled Schur-Weyl block over `k`, consuming the
  general-`k` bridge `trace_symGroupAction_eq_spechtModuleCharacterK` /
  `spechtModuleCharacterK` and off-block vanishing
  `youngSym_action_vanishes_off_block_general`.

## Deliverables

A soundness audit (review type — no code changes to the audited file) covering:

1. **Statement faithfulness.** Confirm each general-`k` statement is a true analogue
   of its ℂ counterpart (`youngSymEndomorphism_ne_zero`,
   `youngSymEndomorphism_eq_zero_of_blocks_vanish`, `exists_unique_special_block`):
   same hypotheses modulo the `ℂ → [Field k] [IsAlgClosed k] [CharZero k]` swap, and
   that the conclusion's per-block trace/label characterization matches the ℂ shape
   (the `[Fintype ι]` unused-in-type lint mirrors the ℂ statement intentionally — not
   a defect).
2. **Proof soundness.** Verify there is no vacuity or hypothesis smuggling: in
   particular that `youngSymEndomorphism_eq_zero_of_blocks_vanish_general`'s
   `DirectSum.linearMap_ext` / `TensorProduct.ext'` reduction and the
   `youngSym_block_factorization` application are sound, and that
   `spechtModuleCharacterK` vs `spechtBlockCharacterK` are used consistently (the
   route notes they are definitionally equal — confirm the proof actually relies on
   that, not on an unproven coercion).
3. **No new real `sorry`/`admit`** introduced (comment-stripped check — raw grep
   over-reports prose like `sorry'd`; strip `/- -/` and `--` first).

Record the verdict (PASS / issues found) in a short `progress/` audit note, mirroring
the format of #5038/#5039. If a genuine soundness defect is found, open a follow-up
`feature` fix issue rather than editing in this review.

## Context

- Parent epic #4946 (general-`k` Schur-module simplicity); route doc
  `progress/schurModule-isSimple-general-route.md`.
- Predecessor audits: #5038 (#5002/#5014 cluster), #5039 (#5029 Specht bridge).
- Downstream consumer: #4976 (`schurModule_isSimple_general` assembly), which depends
  on `exists_unique_special_block_general` — so correctness here gates that assembly.
- Generic infra already audited: `SpechtBridgeGeneral.lean` (#5039),
  `SchurWeylSpecialBlockGeneral.lean` (#5004 → covered by #5038 cluster).

## Verification

- `lake exe cache get` then
  `lake build EtingofRepresentationTheory.Chapter5.SchurModuleSpecialBlock` is green
  on `main` (it is the merged state — re-confirm, don't re-prove).
- Comment-stripped `sorry`/`admit` count in `SchurModuleSpecialBlock.lean` is 0.
- Audit note committed under `progress/`.
