## Current state

PR #2943 (commit `f231879`, merged 2026-05-19) closed sub-A2 of #2919
by filling the body of `non_adjacent_branches_infinite_type_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericAssembly.lean:74-388`
(~314 lines, +288/-12 in that file). It mirrors the universal
`non_adjacent_branches_infinite_type`
(`Chapter6/InfiniteTypeConstructions.lean:9682-10597`) and is the
single largest leaf of `not_posdef_infinite_type_per_kQ`. It is now on
`main` and is part of the chain that makes Theorem 2.1.2's forward
direction sorry-free at the outer-assembly level.

Five structural pieces went in via this PR (per the commit message
and the diff against `de5a545..ac9a485`):

1. `set_option maxHeartbeats 6400000 in` added at the theorem header,
   matching the universal budget at
   `InfiniteTypeConstructions.lean:9678`.
2. Adjacent-pair fallback: when some `u₀ ∈ N(v₀)` is adjacent to some
   `u₁ ∈ N(w)`, dispatch to `adjacent_branches_infinite_type_per_kQ`
   (`Chapter6/FieldGenericD5Tilde.lean:1043`).
3. Neighbour extraction and `h_v₀w_nonadj` derivation: inline,
   mirroring the universal version.
4. Three leaf branches (one of `v₀`'s three neighbours is a leaf, one
   of `w`'s three neighbours is a leaf, or the symmetric case),
   each delegating to `non_adjacent_branches_leaf_case_per_kQ`
   (Sub-A1 helper landed by PR #2933 in
   `Chapter6/FieldGenericNonAdjacentBranches.lean`).
5. All-deg-2 case (universal lines 10324-10597): embed
   `Ẽ₆ = T(2, 2, 2)` via `subgraph_infinite_type_transfer_per_kQ`
   and `etilde6_not_finite_type_per_kQ`. Verifies the 49-pair
   `etilde6Adj`/host adjacency table by `fin_cases` + `linarith`.

The PR adds an import for `FieldGenericNonAdjacentBranches` to
`FieldGenericAssembly.lean`. No other files in `EtingofRepresentationTheory`
are changed by the PR.

The previous wave-62 audits used a five-dimension template (D1
signature, D2 conclusion / case-tree, D3 inner dispatch, D4 sorry
budget, D5 light pattern audit). Recent comparables for the template:
- #2924 / PR #2921 — `not_posdef_infinite_type_per_kQ` outer assembly
  + Theorem 2.1.2 bridge (audited PASS).
- #2934 / PR #2940 — D2 per-(F, Q) wrapper trilogy (audited PASS).
- #2928 / PR #2931 — `embed_t125_in_tree_per_kQ` shared helper
  (audited PASS).

## Deliverables

D1. **Signature fidelity.** Verify the parameter list and conclusion
of `non_adjacent_branches_infinite_type_per_kQ`
(`FieldGenericAssembly.lean:74`) matches the universal
`non_adjacent_branches_infinite_type`
(`InfiniteTypeConstructions.lean:9682`) for the shared parameters
(`n`, `adj`, `hn`, `hsymm`, `hdiag`, `h01`, `hconn`, `h_acyclic`,
`h_deg`, `v₀`, `w`, `hv₀`, `hw`, `hne`) and that the per-(F, Q)
carriage `(F, [Field F], [IsAlgClosed F], Q, [Subsingleton …],
hOrient)` is attached in the same shape used by sibling per-(F, Q)
leaves (`adjacent_branches_infinite_type_per_kQ`,
`non_adjacent_branches_leaf_case_per_kQ`,
`single_branch_not_posdef_infinite_type_per_kQ`). Conclusion shape
must match the canonical per-(F, Q) `infinite_type_per_kQ` predicate
used across the assembly.

D2. **Case-tree fidelity.** Confirm the top-level case analysis at
`FieldGenericAssembly.lean:74-388` mirrors the universal case tree
at `InfiniteTypeConstructions.lean:9682-10597` in order and branch
condition: (a) adjacent-pair fallback, (b) v₀-leaf, (c) w-leaf,
(d) symmetric leaf branch, (e) all-deg-2 / Ẽ₆ case. No reordering
that would affect overlap of hypotheses; no implicit case fusion or
splitting beyond what the universal version does.

D3. **Inner-dispatch correctness.** For each of the five branches in
D2, verify that the per-(F, Q) dispatch target is the structural mirror
of the universal version's target, with `F`, `Q`, `hOrient` threaded
from the outer binder. Specifically:
- (a) → `adjacent_branches_infinite_type_per_kQ`
  (`FieldGenericD5Tilde.lean:1043`), arguments line up with the
  universal `adjacent_branches_infinite_type` call site.
- (b)/(c)/(d) → `non_adjacent_branches_leaf_case_per_kQ`
  (`FieldGenericNonAdjacentBranches.lean`, helper from PR #2933),
  arguments mirror the three universal `leaf_case` invocations
  inside `non_adjacent_branches_infinite_type`.
- (e) → `subgraph_infinite_type_transfer_per_kQ` composed with
  `etilde6_not_finite_type_per_kQ`, with the embedding constructed
  via the `etilde6Adj` table verification at universal lines
  10324-10597. Confirm the `fin_cases` + `linarith` 49-pair
  adjacency-table proof matches the universal version's pair list
  (no missing pairs, no extras).

D4. **Sorry-propagation accounting.** Confirm
`FieldGenericAssembly.lean` has no new proof-body sorries introduced
by PR #2943 (count delta should be 0 on this file; the file already
had a sorry at line 96 in `non_adjacent_branches_infinite_type_per_kQ`
pre-PR which is now closed, so total sorries on this file should
decrease by 1). Confirm that the all-deg-2 case's reliance on
`etilde6_not_finite_type_per_kQ` is correctly tracked as a downstream
sorry (the `embed_etilde6_in_tree_per_kQ` helper body is being filled
by #2937 → presently still `sorry`), and that no other helper from a
non-tracked source is invoked.

D5. **Light pattern audit.** Spot-check for non-trivial divergence
from the universal proof: variable naming, hypothesis-extraction
order, intermediate `have`/`obtain` lemmas, placement of
`set_option maxHeartbeats` and `attribute [-instance]` lines.
Confirm consistency with the convention from #2894 (D2.degree4
audit catalog) and the per-(F, Q) wave's `[IsAlgClosed F]`
propagation pattern. Report any deviation; do not require code
changes for cosmetic differences unless they affect correctness.

## Context

- Universal source: `Chapter6/InfiniteTypeConstructions.lean:9682-10597`.
- Per-(F, Q) target: `Chapter6/FieldGenericAssembly.lean:74-388`.
- Per-(F, Q) leaf-case helper (Sub-A1, used by branches b/c/d):
  `Chapter6/FieldGenericNonAdjacentBranches.lean` (PR #2933).
- Per-(F, Q) Ẽ₆ leaf used in case (e):
  `etilde6_not_finite_type_per_kQ` (still a sorry stub; downstream
  helper `embed_etilde6_in_tree_per_kQ` is in flight via #2937 /
  #2938 / #2939 and not blocking this audit).
- Sibling per-(F, Q) dispatch (case a):
  `adjacent_branches_infinite_type_per_kQ`
  (`Chapter6/FieldGenericD5Tilde.lean:1043`).
- Subgraph transfer machinery used in case (e):
  `subgraph_infinite_type_transfer_per_kQ`
  (`Chapter6/FieldGenericInfiniteType.lean:374`).
- PR body: `Closes #2923` (the sub-A2 tracking issue is now closed,
  with `has-pr` applied automatically).
- Recent audit templates to follow:
  * `progress/reviews/2026-05-18-not-posdef-per-kQ-and-theorem2_1_2-bridge.md`
    (audit of PR #2921, the parent outer assembly).
  * `progress/reviews/2026-05-19-embed-t125-in-tree-per-kQ.md`
    (audit of PR #2917/#2918, the embed_t125 helper).
- Build: `lake build EtingofRepresentationTheory.Chapter6.FieldGenericAssembly`
  is expected to succeed; PR #2943 was merged with passing CI.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericAssembly`
  succeeds.
- Audit report written to
  `progress/reviews/2026-05-19-non-adjacent-branches-outer-assembly-per-kQ.md`
  with one section per deliverable, citing universal line ranges and
  per-(F, Q) line ranges side-by-side. Verdict
  (PASS / PASS-with-notes / FAIL) recorded at top.
- If non-trivial divergence is found in D5, file a follow-up issue
  with a `replan` or `feature` label as appropriate. Cosmetic
  differences may be noted without action.
- No code changes are expected from this audit unless D1-D4 reveal a
  correctness gap. Audit-only PRs do not modify
  `EtingofRepresentationTheory/`; they add the report file and a
  progress entry.
