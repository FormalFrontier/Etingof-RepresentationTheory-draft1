## Current state

PR #2933 (merge commit `601f2ea`, +231/-0 in
`Chapter6/FieldGenericNonAdjacentBranches.lean` + `Chapter6.lean`,
closing issue #2922) introduced a new file
`Chapter6/FieldGenericNonAdjacentBranches.lean` that adds the per-(F, Q)
**API stub** `non_adjacent_branches_leaf_case_per_kQ`
(`FieldGenericNonAdjacentBranches.lean:87`). The body is intentionally
`sorry` — the actual proof is tracked by #2932 and its sub-issues
(#2937 done, #2938 done, #2939 blocked-now-unblocked). The follow-up
PR #2941 (merge commit `6fc9d40`) strengthened the signature (added
`h_v₀w_nonadj`, `h_leaf_adj`, `h_leaf_deg` and introduced the Ẽ₆/Ẽ₇
embedder stubs that #2945/#2947 then filled).

This audit is the **first review of PR #2933**. It is not a
five-dimension proof audit (there is no proof) — it verifies that the
file scaffolding, the stub signature, and the strategy docstring are
sound enough that the dependent outer assembly
(`non_adjacent_branches_infinite_type_per_kQ`, PR #2943, audit
in flight as #2944) can dispatch through it safely, and that the
strengthened signature on `main` from #2941 is a compatible
refinement of #2933's original.

## Deliverables

Produce a five-dimension audit report at
`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-api-stub.md`
covering PR #2933 (and the signature delta from PR #2941). Each
dimension is PASS / CONCERN / FAIL with file-line evidence.

**D1 — File scaffolding fidelity.** Verify
`Chapter6/FieldGenericNonAdjacentBranches.lean:1-11` imports exactly
the modules that the strategy docstring (lines 12-52) and the
dispatch comment (lines 117-147) say it needs:
`Proposition6_6_5`, `OrientationDefs`, `FiniteTypeDefs`,
`InfiniteTypeConstructions`, `FieldGenericInfiniteType`,
`FieldGenericD5Tilde`, `FieldGenericETilde6`, `FieldGenericETilde7`,
`FieldGenericT125` — no missing modules whose names the docstring
references, no superfluous imports beyond what the planned proof
needs. Verify the file is wired into `EtingofRepresentationTheory/
Chapter6.lean` (single `import` line, alphabetised correctly relative
to siblings). Verify the `namespace Etingof` / `attribute [-instance]
CategoryTheory.CategoryStruct.toQuiver
CategoryTheory.ReflQuiver.toQuiver in` shape matches the precedent in
`FieldGenericTpqr.lean` and `FieldGenericT125.lean`.

**D2 — Signature compatibility (PR #2933 → PR #2941 delta).** The
strengthened signature now on `main`
(`FieldGenericNonAdjacentBranches.lean:87-116`) has parameters
`{n} (adj) (hn) (hsymm) (hdiag) (h01) (hconn) (h_acyclic) (h_deg) (v₀
w) (hv₀ hw hne) (h_no_adj_branch h_no_adj_branch_w) (h_v₀w_nonadj)
(leaf) (h_leaf_adj h_leaf_deg) (F) [Field F] [IsAlgClosed F] (Q)
[∀ a b, Subsingleton …] (hOrient)`. Verify each hypothesis is
necessary: in particular, that `h_v₀w_nonadj` (added by #2941) is not
redundant with the existing `hne : w ≠ v₀` + `h_no_adj_branch w` (it
isn't — non-adjacency is strictly stronger), and that
`h_leaf_adj` is logically independent from `h_leaf_deg` (they pin two
different facts about `leaf`). Compare against PR #2933's original
signature (use `git show 601f2ea:EtingofRepresentationTheory/
Chapter6/FieldGenericNonAdjacentBranches.lean` to inspect the
pre-strengthening shape). Document any hypothesis the strengthening
added or removed, and confirm the conclusion is unchanged.

**D3 — Conclusion fidelity to siblings.** The conclusion
(`FieldGenericNonAdjacentBranches.lean:113-116`) is `¬ Set.Finite {d :
Fin n → ℕ | ∃ V : @Etingof.QuiverRepresentation.{0,0,0,0} F (Fin n) _
Q, V.IsIndecomposable ∧ ∀ v, Nonempty (V.obj v ≃ₗ[F] (Fin (d v) →
F))}`. Verify it is **textually identical** to the conclusion shape
used by sibling per-(F, Q) infinite-type leaves:
`t125_not_finite_type_per_kQ` (`FieldGenericT125.lean:39`),
`etilde6_not_finite_type_per_kQ` (`FieldGenericETilde6.lean:319`),
`etilde7_not_finite_type_per_kQ` (`FieldGenericETilde7.lean:301`),
`single_branch_leaf_case_per_kQ`
(`FieldGenericTpqr.lean:1306`), and the outer assembly
`non_adjacent_branches_infinite_type_per_kQ`
(`FieldGenericAssembly.lean:74`). Confirm the universe annotations and
the `(Fin n) → ℕ` indexing match.

**D4 — Strategy docstring accuracy.** The docstring/comment block
(lines 12-52, 60-86, 117-147) claims:
(a) the universal `leaf_case` at `InfiniteTypeConstructions.lean:9770-
10316` embeds `D̃_{k+5}` parameterised in chain length;
(b) no `dTilde_not_finite_type_per_kQ` for general `n` exists on
`main`;
(c) the per-(F, Q) port must case-split on chain length and arm
extensions and embed one of `T(1,2,5)`, `Ẽ₆ = T(2,2,2)`, or
`Ẽ₇ = T(1,3,3)`.
Verify (a) by line-checking the universal `leaf_case` range
(`InfiniteTypeConstructions.lean:9770-10316`) — does it actually use
`dTilde_not_finite_type` with a chain-length-dependent `n`? Verify
(b) by grepping for `dTilde_not_finite_type_per_kQ` across
`EtingofRepresentationTheory/` and confirming no general-`n` variant
exists (only the fixed-`n` `d5tilde_not_finite_type_per_kQ` for the
specific D̃₅ case in `FieldGenericD5Tilde.lean:999`). Verify (c) by
confirming the three named per-(F, Q) embedders cited
(`embed_t125_in_tree_per_kQ`, `embed_etilde6_in_tree_per_kQ`,
`embed_etilde7_in_tree_per_kQ`) and their leaves
(`t125_not_finite_type_per_kQ`, `etilde6_not_finite_type_per_kQ`,
`etilde7_not_finite_type_per_kQ`) all exist on `main`.

**D5 — Build sanity + dispatch readiness.** From `main` =
`23e7a62`, run `lake exe cache get` then build
`EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
(targeted, not full mathlib) and confirm only the documented `sorry`
warning at line 153 (plus any pre-existing sorries documented elsewhere
in the file) is reported. Then verify the stub is actually dispatched
by the new outer assembly: grep
`Chapter6/FieldGenericAssembly.lean` (PR #2943, audited in flight as
#2944) for calls to `non_adjacent_branches_leaf_case_per_kQ` and
confirm at least one call exists and passes the strengthened
hypotheses (especially `h_v₀w_nonadj`, `h_leaf_adj`, `h_leaf_deg`) in
the correct argument positions.

## Context

PR #2933 was merged at `2026-05-18T23:19:21Z` and closes issue #2922
(the original API-stub request). It was followed by PR #2941
(signature strengthening + Ẽ₆/Ẽ₇ embedder stubs) at
`2026-05-19T~00:00` and by PRs #2945 / #2947 (the embedder
bodies). The outer assembly that dispatches to this helper is PR #2943
(`non_adjacent_branches_infinite_type_per_kQ`), already audited
in flight as #2944. The proof body of this leaf-case helper is tracked
by issue #2939 (blocked on #2937/#2938 — both now closed).

Comparable audits for the audit-report style:
- `progress/reviews/2026-05-19-embed-t125-in-tree-per-kQ.md` (audit
  of #2928 — fellow shared embedder helper, PASS)
- `progress/reviews/2026-05-19-not-posdef-per-kQ-and-theorem2_1_2-bridge.md`
  (audit of #2924 — outer assembly + bridge)
- `progress/reviews/2026-05-19-d2-wrapper-trilogy-per-kQ.md` (audit of
  #2934 — D2 wrapper trilogy, also a partial-content audit)

This audit is intentionally narrower than the five-dimension proof
audit template (since #2933 has no proof body); use D1-D5 as listed
above instead of the signature/conclusion/embedding/dispatch/call-site
template. If you find a CONCERN or FAIL, open a follow-up issue rather
than blocking the audit landing — the body-fill issue #2939 already
exists.

## Verification

Audit report committed at
`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-api-stub.md`
on a branch named after the session UUID, with explicit PASS / CONCERN
/ FAIL verdicts per dimension and file-line citations. Targeted build
of `EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
green (only the documented `sorry` warning at line 153). PR opened via
`coordination create-pr` with `Closes #<this-issue>` and auto-merge
enabled.
