## Current state

Two PRs feeding directly into **#2877**
(`not_posdef_infinite_type_per_kQ` assembly + Theorem 2.1.2 bridge)
landed today and have not been audited:

- **PR #2878** (closes #2875 partially, deliverable D1) — adds two
  per-(F, Q) API stubs:
  - `star_not_finite_type_per_kQ` (`Chapter6/FieldGenericStar.lean:529-554`),
    K_{1,4} / D̃₄ stub. Body `sorry`; proof tracked by #2789 / #2801.
  - `t125_not_finite_type_per_kQ` (new file
    `Chapter6/FieldGenericT125.lean:1-55`), T(1, 2, 5) stub. Body
    `sorry`; proof tracked by #2793.
  - Adds `import EtingofRepresentationTheory.Chapter6.FieldGenericT125`
    to `Chapter6.lean`.
  - Sorry-count delta: **+2 raw / +2 declarations** (both tracked).
- **PR #2882** (closes #2880) — adds three transfer-wrapped per-(F, Q)
  subgraph dispatch wrappers:
  - `chordless_cycle_infinite_type_per_kQ`
    (`Chapter6/FieldGenericCycle.lean:351-401`) — pure composition,
    no body proof obligation beyond plumbing.
  - `triangle_infinite_type_per_kQ`
    (`Chapter6/FieldGenericCycle.lean:404-426`) — `k = 3`
    specialisation of `chordless_cycle_*_per_kQ`.
  - `star_subgraph_not_finite_type_per_kQ`
    (`Chapter6/FieldGenericStar.lean:558-638`) — non-trivial
    embedding construction `Fin 5 ↪ Fin n` with case-split adjacency
    proof (center-center, center-leaf, leaf-center, leaf-leaf).
  - Sorry-count delta: **0** (the star wrapper transitively inherits
    the `star_not_finite_type_per_kQ` `sorry` from PR #2878).

**Documented deviation from spec.** Issue #2880 sketched placement of
all three wrappers in `FieldGenericInfiniteType.lean` next to
`subgraph_infinite_type_transfer_per_kQ`. The worker landed them in
`FieldGenericCycle.lean` (cycle/triangle) and `FieldGenericStar.lean`
(K_{1,4}) with the stated rationale: `FieldGenericInfiniteType` is
imported by the per-graph files, so the wrappers — which call
`cycle_not_finite_type_per_kQ` / `star_not_finite_type_per_kQ` —
must live downstream. Section headers in both files document the
rationale. (Quick check: `grep import` on the three files confirms
`FieldGenericCycle` and `FieldGenericStar` each import
`FieldGenericInfiniteType`, not the reverse.)

This audit is the pre-flight before #2877 is claimed and
self-decomposed by a worker. Pattern matches #2868 → #2873 audit →
#2853 claim chain.

## Deliverables

Light audit. No code changes expected unless follow-up issues are
filed.

1. **Statement fidelity audit (PR #2878 stubs + PR #2882 wrappers).**
   For each of the five new top-level theorems, verify:
   - Signature is byte-equivalent to its `_kQ`-free counterpart modulo
     the `(F, Q, hOrient)` triple + the restricted-orientation proof:
     - `star_not_finite_type_per_kQ` vs. `star_not_finite_type`
       (`InfiniteTypeConstructions.lean`, search by name).
     - `t125_not_finite_type_per_kQ` vs. `t125_not_finite_type` (ditto).
     - `chordless_cycle_infinite_type_per_kQ` vs.
       `chordless_cycle_infinite_type`
       (`InfiniteTypeConstructions.lean:4112-4120`).
     - `triangle_infinite_type_per_kQ` vs. `triangle_infinite_type`
       (`InfiniteTypeConstructions.lean:3880-3902`).
     - `star_subgraph_not_finite_type_per_kQ` vs.
       `star_subgraph_not_finite_type`
       (`InfiniteTypeConstructions.lean:1133-1183`).
   - The conclusion shape
     `¬ Set.Finite {d : Fin n → ℕ | ∃ V : QuiverRepresentation F (Fin n) Q,
     V.IsIndecomposable ∧ ∀ v, Nonempty (V.obj v ≃ₗ[F] (Fin (d v) → F))}`
     matches the canonical per-(F, Q) shape used elsewhere
     (`subgraph_infinite_type_transfer_per_kQ`,
     `cycle_not_finite_type_per_kQ`,
     `etilde6_not_finite_type_per_kQ`).
   - Sorry-count delta matches commit messages: PR #2878 adds exactly
     2 raw `sorry`s (both with TODO comments naming the tracking
     issues #2789/#2801 and #2793 respectively); PR #2882 adds 0 raw
     `sorry`s. Confirm by `grep -n sorry` on the three touched files
     before/after.

2. **`star_subgraph_*_per_kQ` embedding-construction correctness.**
   Unlike the other two PR #2882 wrappers (which are pure
   compositions), `star_subgraph_not_finite_type_per_kQ` builds an
   explicit injection `φ : Fin 5 ↪ Fin n` mapping `0 ↦ center` and
   `i+1 ↦ leaves i`, and proves `starAdj i j = adj (φ i) (φ j)` by
   a four-way case-split. Verify:
   - Injectivity proof (`hφ_inj`) handles all four `(i.val = 0, j.val = 0)`
     case combinations correctly. In particular, the
     `dif_pos / dif_neg` rewrites match the actual `φ_fun` body.
   - The adjacency-preservation proof (`hembed`) closes all four cases
     (center-center, center-leaf, leaf-center, leaf-leaf) using the
     correct hypotheses (`hadj_diag center`, `hadj_edge`,
     `hadj_symm.apply` + `hadj_edge`, `hadj_indep`).
   - The `starAdj` unfold-to-if-then-else step (`change`) matches the
     actual definition of `starAdj` in
     `Chapter6/FiniteTypeDefs.lean` (or wherever it lives).
   - Final invocation `subgraph_infinite_type_transfer_per_kQ φ F Q
     (star_not_finite_type_per_kQ F ...)` uses the correct
     restricted-orientation form via `restrictOrientationViaEmb`
     + `restrictOrientationViaEmb_isOrientationOf`.

3. **Downstream readiness for #2877.D2.degree4 + D2.cycle.** Per
   #2877's decomposition table:
   - **D2.degree4** (~50 lines, mirrors
     `InfiniteTypeConstructions.lean:4064-4101`) consumes
     `star_subgraph_not_finite_type_per_kQ` (K_{1,4} branch) and
     `triangle_infinite_type_per_kQ` (3-cycle branch).
   - **D2.cycle** (~150 lines, mirrors
     `InfiniteTypeConstructions.lean:3910-4057`) consumes
     `chordless_cycle_infinite_type_per_kQ`.

   For each of these two sub-deliverables, locate the call site(s)
   in the `_kQ`-free helper and confirm the per-(F, Q) wrapper's
   signature matches what the assembly will need (correct arity,
   correct hypothesis ordering, correct universe parameters).
   Identify any other per-(F, Q) primitives the assembly will need
   that **do not yet exist** on `main` (e.g., per-(F, Q) versions of
   `cycleAdj`-related machinery, `restrictOrientationViaEmb` helpers
   specific to specific subgraph shapes), and flag them for a
   follow-up planner cycle. The audit's verdict should answer: "are
   D2.degree4 and D2.cycle worker-ready, or are there still missing
   primitives?"

## Context

- **Parent (unclaimed feature):** #2877 — `not_posdef_infinite_type_per_kQ`
  assembly + Theorem 2.1.2 bridge. Body recommends worker-led
  decomposition; this audit pre-flights the two smallest
  sub-deliverables (D2.cycle, D2.degree4).
- **Grand-parent (replan):** #2875 — D2 + D3 of the bridge to
  Theorem 2.1.2; decomposed to #2877.
- **Sibling primitives already on `main`:**
  - `subgraph_infinite_type_transfer_per_kQ`
    (`FieldGenericInfiniteType.lean:374`).
  - `cycle_not_finite_type_per_kQ`
    (`FieldGenericCycle.lean:326`) — fully proven.
  - `etilde6_not_finite_type_per_kQ`, `d5tilde_not_finite_type_per_kQ`,
    `etilde7_not_finite_type_per_kQ` — the existing per-(F, Q) leaf
    helpers that establish the naming convention.
- **Closed companions (recent audit pattern):** #2858 (audit of D̃₅
  Sub B cascade helpers), #2864 (audit of cleanup hoists), #2873
  (audit of proj-sibling lemmas — most recent).

Reference files:
- `EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean` —
  PR #2878 stub at lines 529-554; PR #2882 wrapper at lines 558-638.
- `EtingofRepresentationTheory/Chapter6/FieldGenericT125.lean` (new
  file, PR #2878) — T(1,2,5) stub.
- `EtingofRepresentationTheory/Chapter6/FieldGenericCycle.lean` —
  PR #2882 wrappers at lines 351-426.
- `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean` —
  the `_kQ`-free counterparts at lines 1133-1183 (`star_subgraph_*`),
  3880-3902 (`triangle_*`), 4112-4120 (`chordless_cycle_*`); and the
  outer case helpers D2.cycle and D2.degree4 at lines 3910-4057
  and 4064-4101 respectively.
- Worker progress notes:
  `progress/2026-05-18T09-52-11Z_1a918f61.md` (PR #2878),
  `progress/2026-05-18T10-37-56Z_37f21857.md` (PR #2882).

## Verification

`lake build EtingofRepresentationTheory.Chapter6` should succeed
unchanged (no code changes expected). If the audit finds defects, file
follow-up sub-issues; do not alter the audited PRs themselves.
