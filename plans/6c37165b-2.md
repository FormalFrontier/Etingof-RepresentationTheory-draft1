## Current state

`Chapter6/FieldGenericTpqr.lean:1267-1279` contains the TODO
comment block inside the body of
`single_branch_leaf_case_both_extend_per_kQ` (the residual
`sorry` at line 1286). The block describes the planned four-way
dispatch correctly but does **not** cite the sub-issue numbers
that decompose the work:

```
-- TODO (follow-up issue): replace this `sorry` with the per-(F, Q)
-- "both arms extend" body mirroring `single_branch_leaf_case`
-- (`InfiniteTypeConstructions.lean:6981-8352`, ~1370 lines).
-- Further case-splits on whether `b₂`, `b₃` and deeper vertices
-- extend, dispatching to:
--   * both arms ≥ 3 → embed Ẽ₇ and call `etilde7_not_finite_type_per_kQ`
--   * one arm length 2, other ≥ 5 → embed T(1, 2, 5) and call
--     `t125_not_finite_type_per_kQ`
--   * ADE shapes T(1, 2, 2/3/4) → contradict `h_not_posdef` via the
--     `e7_tree_posdef` / `e8_posdef`-style posdef facts in
--     `InfiniteTypeConstructions.lean`.
-- The real body will need `set_option maxHeartbeats 6400000 in`
-- (mirroring the `_kQ`-free original at
-- `InfiniteTypeConstructions.lean:6896`); the stub elaborates fine
-- without it.
```

The wave-62 / wave-63 cycle has fully decomposed the four-way
dispatch into sub-issues, three of which are still open and the
fourth landed via PR #2912:

| Sub-issue | Configuration                                  | Helper / contradiction       | Status                          |
|-----------|------------------------------------------------|------------------------------|---------------------------------|
| #2907     | both arms ≥ 3 (Ẽ₇ embed)                       | `etilde7_not_finite_type_per_kQ` | `has-pr` (PR #2911, in /repair) |
| #2908     | T(1, q, 2) (`b₃` leaf, q ≥ 3)                  | `t125_not_finite_type_per_kQ` | `replan`, claimed               |
| #2909     | T(1, 2, r) (`b₂` leaf, r ≥ 3)                  | `t125_not_finite_type_per_kQ` | `replan`, claimed               |
| #2910     | T(1, 2, 2) = D₅ posdef contradiction           | `e7_tree_posdef` / `e8_posdef` | landed via PR #2912            |

The parent assembly issue is #2905 (currently `replan`).

The TODO block's three bullets correspond exactly to the four
sub-issues only after merging the "one arm length 2, other ≥ 5"
bullet (#2908 + #2909) and splitting the "ADE shapes T(1, 2,
2/3/4)" bullet (#2910 covers T(1, 2, 2); T(1, 2, 3) and T(1, 2,
4) are also addressed in the same posdef-contradiction branch
under #2910's scope per its closed PR #2912). A reader who only
sees this comment cannot navigate from the residual `sorry` to
the four open / closed sub-issues without searching by hand.

This is the direct analogue of issue #2986 (the wave-63 chore
that refreshed the file-level docstring of the same file): same
pattern, different comment block.

## Deliverables

Edit only `Chapter6/FieldGenericTpqr.lean`. One surgical change
inside the existing TODO comment block at lines 1267-1279:

1. Cite the four sub-issues (`#2907`, `#2908`, `#2909`, `#2910`)
   inline next to the appropriate bullet, splitting the "one arm
   length 2, other ≥ 5" bullet into the two symmetric
   sub-cases (`b₃` leaf q ≥ 3 ↔ #2908; `b₂` leaf r ≥ 3 ↔ #2909)
   so the four-way structure is visible.

2. For #2910, note that the T(1, 2, 2) sub-case landed via
   PR #2912 and that the ADE bullet T(1, 2, 3) / T(1, 2, 4) cases
   are part of the same posdef-contradiction branch (i.e.,
   indicate which configurations are still open vs landed).

3. Cite the parent assembly issue #2905 once at the head of the
   block so a reader can find the four sub-issues from one
   pointer.

Do **not** modify the per-theorem docstring at lines 1219-1232
(that block is reserved for issue #2986 if it touches anything
neighbouring; this issue is solely for the inner TODO block at
lines 1267-1279).

Do **not** modify the `let _ := …` discard block (lines
1280-1285), the `sorry` (line 1286), or anything outside the TODO
block.

## Context

* Precedent for this kind of residual-sorry citation refresh:
  the parallel chore issue (filed in the same planner cycle as
  this one) for the two residual-sorry comment blocks in
  `Chapter6/FieldGenericNonAdjacentBranches.lean`.
* Issue #2986 (the wave-63 docstring refresh for this same file)
  explicitly scoped itself to the file-level docstring (lines
  11-44) and the per-theorem docstring of
  `single_branch_leaf_case_per_kQ` (lines 1300-1305), and called
  the inner TODO block at lines 1267-1279 out as "do not modify
  outside that range". So this issue does not overlap with
  #2986's deliverables.
* The four sub-issues are all `replan` / `has-pr` / `blocked`,
  i.e. the assembly cannot be done now. This chore does not
  unblock anything — it makes the residual `sorry` navigable to
  the worker that will pick up #2905 once sub-A/B/C land.

## Verification

* `git diff main -- Chapter6/FieldGenericTpqr.lean` shows changes
  only inside lines 1267-1279 (the TODO `--` comment block). No
  changes to `theorem`/`def`/`sorry` lines or to any other
  comment block.
* `lake build EtingofRepresentationTheory.Chapter6.FieldGenericTpqr`
  passes (comment-only edits should compile unchanged, but
  verify).
* No new `sorry`s introduced. Existing `sorry` at line 1286 left
  intact.
* Sorry count for the file unchanged at 1.

## Notes

* Strictly chore-level. Mark the PR with `chore(Ch6):` title
  prefix to match the convention of PRs #2983, #2986.
* Issue #2910's PR (#2912) is closed and merged; cite it as
  "landed via PR #2912" rather than as an open sub-issue, to
  signal that the bullet is informational rather than actionable.
* If #2986 is still claimed at the time you start this issue,
  rebase on top of its PR once it lands to avoid spurious
  conflicts in the file-level docstring.
