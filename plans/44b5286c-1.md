## Current state

The wave-63 chore-citation audit
(`progress/reviews/2026-05-20-wave-63-chore-citation-refresh.md`,
issue #2995) flagged in its D6 auxiliary observation that the **inner
per-theorem docstring** of `single_branch_leaf_case_both_extend_per_kQ`
in `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean`
(lines 1230-1235, which the wave-63 audit cites as 1230-1235 against
the pre-PR-#3008 source) describes the dispatch asymmetrically and
does not align with the now-corrected TODO block.

PR #3002 (closed by PR #3008) listed this refresh as deliverable 3
("Optional, deferrable") and the PR #3008 worker handoff
(`progress/2026-05-21T08-32-39Z_2727a2a8.md`) explicitly left it for
a future cleanup, noting: "If the planner decides to extend the
citation-refresh chore one further, the deferrable item from #3002
(asymmetric q-r split in the inner docstring at lines 1230-1235) is
the natural next pickup."

Current inner docstring text (lines 1226-1239 in
post-#3008 `main`, sha `44faac8`):

```
/-- Per-(F, Q) version of the "both arms extend" branch of
`single_branch_leaf_case` (`InfiniteTypeConstructions.lean:6981-8352`):
given the T(1, q, r) configuration where both of `v₀`'s non-leaf

API stub: the body is `sorry`, tracked by a follow-up sub-issue. The real
proof mirrors the `_kQ`-free original — further case-splits on whether
`b₂`, `b₃` and deeper vertices extend, dispatching to
`etilde7_not_finite_type_per_kQ` (q, r ≥ 3 → Ẽ₇),
`t125_not_finite_type_per_kQ` (q = 2, r ≥ 5 → T(1, 2, 5)), or contradicting
`h_not_posdef` for the ADE shapes T(1, 2, 2), T(1, 2, 3), T(1, 2, 4).

The "one or both arms are leaves" branches are handled inline in
`single_branch_leaf_case_per_kQ` via `tree_two_leaf_posdef` and do not flow
through this helper. -/
```

Two asymmetries with the corrected TODO block at lines 1284-1289
(post-PR #3008):

* (a) The `t125_not_finite_type_per_kQ` clause lists **only** `(q = 2,
  r ≥ 5 → T(1, 2, 5))` — the symmetric `(r = 2, q ≥ 5 → T(1, q, 2))`
  branch (which the TODO block splits explicitly into `b₃` leaf
  sub-issue #2908 vs `b₂` leaf sub-issue #2909) is missing.
* (b) The ADE-shape clause lists `T(1, 2, 2), T(1, 2, 3), T(1, 2, 4)`
  but is missing `T(1, 3, 2)` and `T(1, 4, 2)`. (PR #3008's TODO-block
  fix made this explicit: those four asymmetric ADE shapes are
  q,r ∈ {3, 4} sub-rows of #2908/#2909, not separately tracked; only
  T(1, 2, 2) = D₅ is landed via PR #2912.)

A reader of the inner docstring is led to believe the dispatch is a
3-way (Ẽ₇ / T(1, 2, 5) / 3-case ADE) when it is in fact a 4-way
(Ẽ₇ / T(1, q, 2) / T(1, 2, r) / T(1, 2, 2) = D₅, with the other
non-D₅ ADE shapes folded into the T(1, q, 2) / T(1, 2, r) sub-issues).

## Deliverables

1. **Refresh lines 1230-1235** (the "API stub:" paragraph of the inner
   docstring, the `/-- … -/` block immediately preceding `theorem
   single_branch_leaf_case_both_extend_per_kQ`) of
   `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean` to
   mirror the 4-way decomposition of the post-#3008 TODO block at lines
   1284-1289. Suggested replacement for the "dispatching to … shapes
   T(1, 2, 2), T(1, 2, 3), T(1, 2, 4)." span:

   ```
   API stub: the body is `sorry`, tracked by a follow-up sub-issue. The real
   proof mirrors the `_kQ`-free original — further case-splits on whether
   `b₂`, `b₃` and deeper vertices extend, dispatching to
   `etilde7_not_finite_type_per_kQ` (q, r ≥ 3 → Ẽ₇),
   `t125_not_finite_type_per_kQ` (r = 2, q ≥ 3 → T(1, q, 2), or q = 2,
   r ≥ 3 → T(1, 2, r)), or contradicting `h_not_posdef` for the
   T(1, 2, 2) = D₅ shape (with the asymmetric ADE configurations
   T(1, 3, 2)/T(1, 4, 2)/T(1, 2, 3)/T(1, 2, 4) folded into the
   T(1, q, 2)/T(1, 2, r) sub-cases above).
   ```

   The exact prose is suggested, not mandatory. The non-negotiable
   semantic content is:

   * the t125 clause must mention **both** `(r = 2, q ≥ 3)` and
     `(q = 2, r ≥ 3)` branches (not just one);
   * the ADE clause must restrict the explicit-shape enumeration to
     T(1, 2, 2) = D₅ (the only shape landed via PR #2912), with the
     other four asymmetric ADE shapes either omitted or noted as
     sub-cases of the t125 branches.

2. (Optional, encouraged) The opening sentence "given the T(1, q, r)
   configuration where both of `v₀`'s non-leaf" reads as a sentence
   fragment (missing the verb / object). Either complete it (e.g.
   "given the T(1, q, r) configuration where both of `v₀`'s non-leaf
   neighbours have degree 2") or restructure the paragraph to flow
   into the "API stub" paragraph. This is a longstanding stylistic
   wart unrelated to the wave-63 chore audit; touch only if natural.

3. Do **not** modify:
   * the file-level docstring at lines 26-43 (PR #3008 already fixed
     the `(line 1240)` reference);
   * the TODO block at lines 1284-1289 (PR #3008 already fixed the
     PR #2912 attribution);
   * the `let _ := …` parameter-discard block at lines 1292-1298
     (planner cycle `4afbf7c8` verified each `let _` discards a
     top-level parameter, no slop to drop);
   * the `sorry` itself (line 1298) — body work is tracked by the
     #2905 sub-chain (#2907/#2908/#2909/#2910).

## Context

Relevant files:
* `EtingofRepresentationTheory/Chapter6/FieldGenericTpqr.lean` — the
  only file to edit.
* `progress/reviews/2026-05-20-wave-63-chore-citation-refresh.md` —
  the audit report; D6 verdict's "Auxiliary observation" paragraph
  is the source of this chore.
* `progress/2026-05-21T08-32-39Z_2727a2a8.md` — PR #3008's worker
  handoff naming this as the natural next pickup.

Predecessor chores in the FieldGenericTpqr citation-refresh chain
(close, do not duplicate):
* PR #2989 (closed #2986) — file docstring refresh.
* PR #2994 (closed #2991) — TODO block refresh.
* PR #3008 (closed #3002) — line number + PR #2912 attribution fix.

This issue closes the chain by refreshing the only docstring text
the audit identified as still inaccurate.

## Verification

* `lake build EtingofRepresentationTheory.Chapter6.FieldGenericTpqr`
  passes with exactly the same warning set as before — one residual
  `sorry` at line 1298 (the line may shift by 0-2 depending on
  whether deliverable 2 is also taken). No new warnings.
* `lake build EtingofRepresentationTheory.Chapter6.FieldGenericAssembly`
  passes with no new warnings (the inner docstring is comment-only,
  no transitive impact possible).
* `git diff` shows changes confined to lines ~1230-1235 (and
  optionally 1228-1230 if deliverable 2 is taken). Total diff: ≤ 10
  lines.

## Sizing

Tiny. Single file, comment-only edit, ≤ 10 lines. Atomic chore in
the same vein as #3004 (1-line `let _` drop) and PR #3008
(2-edit citation fix).
