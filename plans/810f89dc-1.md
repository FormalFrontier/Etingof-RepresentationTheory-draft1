## Current state

Wall 3 closure is gated on a single sorry —
`garnir_twisted_in_lower_span` at
`EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean` (line
~1681 / 1726 depending on snapshot — search the symbol). The
predecessor strategy A from `progress/algorithm-A-redesign.md`
escalated through meditate **#2676**, which produced a refined
**Strategy A\*** documented in `progress/q-high-involution.md`. That
note re-decomposes the proof into three follow-ups: **R2.a**, **R2.b**,
**R2.c**.

This issue is **R2.a — Per-q expansion + IH extraction**, the first
of the three. It is the algebraic sum-rearrangement step that isolates
the IH-killable part of `f_w(σ) := twistedPolytabloid w σ` from a
single residual `Δ_w_σ`.

The R1 bridge (`in_L_of_in_V_of_supp_bounded`,
`SpechtModuleBasis.lean` line ~1438) is already merged via
**#2669** and provides the consumer for the eventual final assembly.

## Deliverables

Add to `EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean`,
in a new section immediately after `in_L_of_in_V_of_supp_bounded`
(line ~1438) and before `twistedPolytabloid_pigeonhole_pair`
(line ~1475), the following infrastructure and lemma. Read
`progress/q-high-involution.md` §3 (Strategy A\*) and §4.1 in full
before starting — those sections fix the partition definitions and the
exact statement.

**1. The Q-partition definitions**:

```lean
/-- The reindexer γ_q := garnirColReindex σ w q ∈ Q_λ from
the redesign §3.1. -/
private noncomputable def reindexer
    (σ : Equiv.Perm (Fin n)) (w q : Equiv.Perm (Fin n)) :
    Equiv.Perm (Fin n) := garnirColReindex (la := la) σ w q

/-- τ_q := γ_q · w · q⁻¹ · σ, col-std by construction. -/
private noncomputable def tauQ
    (σ : Equiv.Perm (Fin n)) (w q : Equiv.Perm (Fin n)) :
    Equiv.Perm (Fin n) := reindexer (la := la) σ w q * (w * q⁻¹ * σ)

/-- Q_low := { q ∈ Q_λ | [τ_q] strictly below [σ] in dominance }. -/
private noncomputable def Q_low (σ w : Equiv.Perm (Fin n)) :
    Finset (Equiv.Perm (Fin n)) := ...

/-- Q_eq, Q_eq', Q_high: defined analogously per §3.1 of the redesign,
keyed on (tabloid equality + rowInv comparison). -/
```

**2. The per-q decomposition lemma**:

```lean
/-- Per-q expansion of f_w(σ) extracting the Q_low ∪ Q_eq IH-killable
part and isolating the Q_eq' ∪ Q_high residual. The residual Δ_w_σ
has support contained in tabloids ≼ [σ]. -/
private theorem twistedPolytabloid_per_q_decomp
    (σ : Equiv.Perm (Fin n)) (hcs : isColumnStandard' n la σ)
    (hrp : 0 < rowInvCount' (la := la) σ)
    (w : Equiv.Perm (Fin n))
    (ih : ∀ τ', isColumnStandard' n la τ' →
      (srRank la τ' < srRank la σ ∨
       (srRank la τ' = srRank la σ ∧
        rowInvCount' (la := la) τ' < rowInvCount' (la := la) σ)) →
      generalizedPolytabloidTab (n := n) (la := la) τ' ∈ V) :
    ∃ Δ : TabloidRep,
      twistedPolytabloid (la := la) w σ
        = (∑ q ∈ Q_low (la := la) σ w ∪ Q_eq (la := la) σ w,
            (sign q : ℂ) * (sign (reindexer (la := la) σ w q) : ℂ)
              • generalizedPolytabloidTab
                  (n := n) (la := la) (tauQ (la := la) σ w q))
          + Δ
      ∧ (∑ q ∈ Q_low (la := la) σ w ∪ Q_eq (la := la) σ w, ...) ∈ V
      ∧ (∀ α : Tabloid, Δ α ≠ 0 → tabloidLE la α (toTabloid n la σ))
```

The exact shape may need adjustment — e.g. returning a structured
record with named fields rather than a 4-tuple — but the three
deliverables are: the decomposition identity, the IH-part-in-V
witness, and the support bound on Δ.

**3. Supporting lemmas** as needed:
- The `Q_λ = Q_low ⊔ Q_eq ⊔ Q_eq' ⊔ Q_high` disjoint partition
- The `τ_q` col-std witness (reindexer is in Q_λ; γ ∈ Q_λ acting on
  any τ produces col-std rep when applied as `γ · w · q⁻¹ · σ`)

## Context

- **Parent meditate**: #2676 (CLOSED).
- **Predecessor R-issues**: #2667 (CLOSED, escalated to #2676),
  #2669 (MERGED, R1 = `in_L_of_in_V_of_supp_bounded`).
- **Sibling siblings**:
  - **R2.b** (Δ ∈ V via inner induction) — to be filed in this same
    cycle, blocked on R2.a.
  - **R2.c** (final assembly) — to be filed in this same cycle,
    blocked on R2.a + R2.b.
- **Superseded predecessors** (closed in this cycle):
  - **#2500** (Wall 3 part D, A+B+C assembly) — superseded by R2.c.
  - **#2520** (Wall 3 #2499 C.2, classify τ's) — superseded; the
    classification in Strategy A\* is implicit in the support bound on
    Δ rather than an explicit τ-classification.
- **Files**:
  - `EtingofRepresentationTheory/Chapter5/SpechtModuleBasis.lean`
    (the only file touched).
- **Strategic note**: `progress/q-high-involution.md` §3 (A\*) and §4.1.
  Read both. The note documents *why* Sub-claim 3.1.3 (`Q_eq' = ∅`)
  was refuted on the running λ=(2,2) example; this issue's design must
  not assume `Q_eq' = ∅`.

## Verification

- `lake build EtingofRepresentationTheory.Chapter5.SpechtModuleBasis`
  succeeds. **Zero new sorries** in this issue's deliverables — the
  lemma is a pure sum-rearrangement, no involution required at this
  layer.
- `lake build` full project succeeds.
- The lemma's `ih` hypothesis is consumed by R2.b (which uses the
  same outer IH).

## Sizing

Medium; ~80–120 lines. The proof is mechanical sum-rearrangement
once the regions Q_low / Q_eq / Q_eq' / Q_high are defined and the
γ_q properties (already in `garnirColReindex` API merged via #2503)
are invoked. Worker may decide to batch all four Q definitions into
one shared section block or to split definitions and partition lemma
across separate `theorem`s.

## Depends on

- [x] R1: `in_L_of_in_V_of_supp_bounded` (#2669, MERGED)
- [x] `garnirColReindex` API (#2497, MERGED)
- [x] `garnir_pigeonhole_collapse` (#2498, MERGED) — used by R2.b, not
  by this issue, but co-located in the file
