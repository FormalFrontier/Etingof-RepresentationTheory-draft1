## Current state

The general-`k` **Specht-side** bridge of the Chapter 5 Schur-Weyl push landed
sorry-free across two files and has **not** had a soundness audit. The two
in-flight/recent reviews cover different clusters:

- #5019 / PR #5028 — Cauchy-identity / formal-character cluster (#4957/#4958/#4934),
  the GL side.
- #5025 (merged) — general-`k` Schur-Weyl Theorem 5.12.2 *classification* cluster
  (#4985/#4989/#5009), the GL classification core.

Neither touches the general-`k` Specht character or the young-symmetrizer
special-block lemmas below. Both target files are confirmed completely sorry-free
(comment-stripped), so this is a pure soundness/faithfulness audit of landed `main`
code, not a proof-completion task.

Files and declarations to audit:

`EtingofRepresentationTheory/Chapter5/SpechtCharacterGeneral.lean` (195 lines, from PR #5002):
- `spechtModuleActionK` (:38, noncomputable def) — the general-`k` Specht-module action.
- `spechtModuleCharacterK` (:48, noncomputable def) — the general-`k` Specht character.
- `spechtModuleCharacterK_algebraMap_rat` (:153) — field-independence via `ℚ`-algebra map.
- `spechtModuleCharacterK_complex` (:160) — agreement with the existing `ℂ` character.
- `spechtModuleCharacterK_injective` (:180) — injectivity of `λ ↦ spechtModuleCharacterK`.

`EtingofRepresentationTheory/Chapter5/SchurWeylSpecialBlockGeneral.lean` (374 lines, from PR #5004/#5014):
- `spechtBlockActionK` (:50, def), `spechtBlockCharacterK` (:61, def).
- `youngSym_trace_kronecker_K` (:205).
- `youngSym_action_vanishes_off_block_general` (:246).
- `youngSym_action_on_special_block_rank_one_scaled_proj_general` (:312).

## Deliverables

A soundness audit recorded as a progress note (and, if issues are found, GitHub
issues — one per defect, with the `replan` label only on the originating issue if a
landed result is actually wrong). Specifically:

1. **Definitions are real, not vacuous.** Confirm the four `def`/`noncomputable def`
   bodies (`spechtModuleActionK`, `spechtModuleCharacterK`, `spechtBlockActionK`,
   `spechtBlockCharacterK`) construct genuine objects matching the intended math
   over a general `[Field k] [IsAlgClosed k] [CharZero k]` (or whatever weaker
   class is actually used) — no `True`-style placeholders, no defaulting that makes
   the action trivial. Check the action is the honest permutation/young-symmetrizer
   action and the character is the honest trace.

2. **`ℂ`-specialization agreement.** Verify `spechtModuleCharacterK_complex` really
   states and proves that the general-`k` character specializes to the pre-existing
   `ℂ` character (no defeq cheat, correct instance arguments), and that
   `spechtModuleCharacterK_algebraMap_rat` gives genuine field-independence rather
   than a tautology.

3. **Hypotheses are not too strong / conclusions not too weak.** Scrutinize
   `spechtModuleCharacterK_injective`, `youngSym_action_vanishes_off_block_general`,
   and `youngSym_action_on_special_block_rank_one_scaled_proj_general` for
   statement faithfulness against the book: in particular that the "off-block"
   vanishing and the "rank-one scaled projection" on the special block match the
   ℂ versions they generalize (`youngSym_action_vanishes_off_block`,
   `…_rank_one_scaled_proj` in `SchurWeylSpecialBlockGeneral`/`SchurModuleSimple`),
   and that `youngSym_trace_kronecker_K` has the right Kronecker-delta normalization.

## Context

- Parent push: general-`k` Schur-Weyl / Specht (Chapter 5, Stage 3). Route doc:
  `progress/schurModule-isSimple-general-route.md`. Sorry landscape:
  `progress/sorry-landscape.md`.
- These lemmas are consumed by the (currently blocked) general-`k` special-block
  assembly #5005 and the bridge generalization #4992, so a soundness defect here
  would silently poison that downstream chain — hence the audit now, before those
  land.
- Read the relevant blob(s) under `blobs/Chapter5/` for the book's statements of
  the Specht character and young-symmetrizer special-block facts before judging
  faithfulness.

## Verification

`lake build` of both files stays green (no edits expected unless a defect is
fixed in place). Audit conclusion (PASS, or per-defect issues filed) recorded in a
`progress/<UTC-timestamp>.md` handoff. If any landed statement is found false or
vacuous, file a focused issue rather than fixing silently.
