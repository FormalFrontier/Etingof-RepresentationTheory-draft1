## Current state

Sub of #2877 (`not_posdef_infinite_type_per_kQ` assembly + Theorem
2.1.2 bridge), pre-split by planner cycle `8c042f69` as the third
parallel-worker pre-split, following D2.degree4 (#2889 / PR #2891 /
audit #2894) and D2.cycle (#2895 / PR #2897).

The audit doc `progress/reviews/2026-05-18-degree4-per-kQ-placement.md`
(D3, "Recommended placement for each remaining D2 sub-deliverable")
explicitly recommended this issue's placement: "D2.adjacent →
`FieldGenericD5Tilde.lean` (already imports Star; needs nothing more).
Carries `[IsAlgClosed F]`." Both prerequisite leaves
(`d5tilde_not_finite_type_per_kQ`) and the per-(F, Q) transfer wrapper
(`subgraph_infinite_type_transfer_per_kQ`) are already on `main`.

The `_kQ`-free original `adjacent_branches_infinite_type` lives at
`Chapter6/InfiniteTypeConstructions.lean:4764-4917` (~154 lines: D̃₅
embedding by extracting 4 non-center neighbors of two adjacent degree-3
branch points, verifying 15 distinctness facts and the 9 non-edges via
`acyclic_no_triangle` / `acyclic_path_nonadj`, then transferring
through `subgraph_infinite_type_transfer` to `d5tilde_not_finite_type`).
The umbrella #2877 sizes this sub-deliverable at ~150 lines.

## Deliverables

1. **`adjacent_branches_infinite_type_per_kQ`** in
   `Chapter6/FieldGenericD5Tilde.lean`. Mirror
   `adjacent_branches_infinite_type`
   (`Chapter6/InfiniteTypeConstructions.lean:4764-4917`) with the
   per-(F, Q) signature pattern: append
   `(F : Type) [Field F] [IsAlgClosed F]`,
   `(Q : @Quiver.{0,0} (Fin n))`,
   `[∀ a b, Subsingleton (@Quiver.Hom (Fin n) Q a b)]`,
   `(hOrient : @Etingof.IsOrientationOf n Q adj)` at the end of the
   argument list. Carry `[IsAlgClosed F]` because the dispatch leaf
   `d5tilde_not_finite_type_per_kQ` requires it (per the rule of thumb
   "a wrapper carries `[IsAlgClosed F]` iff any of its dispatch leaves
   does", recorded in the D2.degree4 audit).

   Conclusion uses the canonical per-(F, Q) shape
   `¬ Set.Finite {d : Fin n → ℕ | ∃ V : QuiverRepresentation F (Fin n) Q,
       V.IsIndecomposable ∧ ∀ v, Nonempty (V.obj v ≃ₗ[F] (Fin (d v) → F))}`.

   The body is structurally identical to the `_kQ`-free original
   through the 15 distinctness facts and the 9 non-edges (no field /
   quiver use). The final `subgraph_infinite_type_transfer` call
   swaps to its per-(F, Q) form:
   - Original (line 4915-4917):
     ```
     exact subgraph_infinite_type_transfer φ adj d5tildeAdj hsymm
       (fun v h => by linarith [hdiag v]) hembed d5tilde_not_finite_type
     ```
   - Per-(F, Q):
     ```
     exact subgraph_infinite_type_transfer_per_kQ φ F Q
       (d5tilde_not_finite_type_per_kQ F (restrictOrientationViaEmb φ Q)
          (restrictOrientationViaEmb_isOrientationOf φ hembed hOrient))
     ```
   exactly the dispatch shape used by `chordless_cycle_infinite_type_per_kQ`
   (`Chapter6/FieldGenericCycle.lean:373-390`) and the chordless branch
   of `graph_with_list_cycle_infinite_type_per_kQ`
   (`Chapter6/FieldGenericCycle.lean:440+`). Note: `subgraph_infinite_type_transfer_per_kQ`
   does not take the `hsymm` / diagonal arguments (its signature is
   `(φ : Fin m ↪ Fin n) (F : Type) [Field F] (Q : @Quiver ...) [...]
   (h_inf : ¬ Set.Finite ...) : ¬ Set.Finite ...` — see
   `Chapter6/FieldGenericInfiniteType.lean:374-389`), so the embedding
   adjacency obligation is discharged via
   `restrictOrientationViaEmb_isOrientationOf` from the existing
   `hembed`.

## Context

- **Parent**: #2877 (covers D2.single, D2.nonadj, D2.outer + D3 after
  this issue lands).
- **Sibling pre-splits** already landed:
  - D2.degree4: #2889 → PR #2891 → audit #2894
    (`degree_ge_4_infinite_type_per_kQ` in `FieldGenericStar.lean:649`).
  - D2.cycle: #2895 → PR #2897
    (`graph_with_list_cycle_infinite_type_per_kQ` in
    `FieldGenericCycle.lean:440`).
- **Established per-(F, Q) recipe**: see
  `progress/reviews/2026-05-18-degree4-per-kQ-placement.md`, D3
  "Pattern recipe (for citation by future D2 sub-PRs)". Apply steps
  1–4 verbatim (with `[IsAlgClosed F]` **included** for this wrapper).
- **Per-(F, Q) leaf used** (already on `main`):
  - `d5tilde_not_finite_type_per_kQ`
    (`Chapter6/FieldGenericD5Tilde.lean:999`) — carries
    `[IsAlgClosed F]`; transitively depends on
    `d5tildeRep_kQ_isIndecomposable`
    (`Chapter6/FieldGenericD5Tilde.lean:974`) whose body is sorry'd
    (tracked by #2853 / #2851). This is a **proof-level sorry chain**,
    not a definition-level blocker — per project CLAUDE.md ("a sorry'd
    dependency is not a blocker"), proceed.
- **Per-(F, Q) transfer wrapper**:
  `subgraph_infinite_type_transfer_per_kQ`
  (`Chapter6/FieldGenericInfiniteType.lean:374`).
- **Per-(F, Q) embedding-adjacency lemma**:
  `restrictOrientationViaEmb_isOrientationOf`
  (`Chapter6/FieldGenericInfiniteType.lean:345`).
- **Pattern example** (same dispatch shape, larger scale):
  `graph_with_list_cycle_infinite_type_per_kQ`
  (`Chapter6/FieldGenericCycle.lean:440+`); the **closest** structural
  analog (single-leaf dispatch through `subgraph_infinite_type_transfer_per_kQ`)
  is the chordless branch of that theorem.

### Placement

`FieldGenericD5Tilde.lean` is the natural host: it already contains
`d5tilde_not_finite_type_per_kQ` (line 999), and already imports
`FieldGenericInfiniteType` (for `subgraph_infinite_type_transfer_per_kQ`
and `restrictOrientationViaEmb_isOrientationOf`) and
`InfiniteTypeConstructions` (for `acyclic_no_triangle`,
`acyclic_path_nonadj`, and `d5tildeAdj`). No new imports are required.

Insert the new theorem after `d5tilde_not_finite_type_per_kQ` (ends
line 1027) and before the final `end Etingof` (line 1029). Apply the
`attribute [-instance] CategoryTheory.CategoryStruct.toQuiver
CategoryTheory.ReflQuiver.toQuiver in` prefix per recipe step 4 — see
existing uses in `FieldGenericCycle.lean:367, 392` and
`FieldGenericStar.lean:649`.

The `set_option maxHeartbeats 3200000 in` annotation on the `_kQ`-free
original (`InfiniteTypeConstructions.lean:4762`) is likely needed for
the per-(F, Q) wrapper too — the body has 15 distinctness facts and a
large `fin_cases` adjacency proof. Keep it if elaboration times out
without it.

### Out of scope

- D2.single (`single_branch_not_posdef_infinite_type_per_kQ`, ~300
  lines, needs `etilde6_*`/`etilde7_*`/`t125_*_per_kQ`,
  `[IsAlgClosed F]`) — remains in #2877.
- D2.nonadj (`non_adjacent_branches_infinite_type_per_kQ`, ~900
  lines, audit recommends dedicated file
  `FieldGenericNonAdjacent.lean`) — remains in #2877.
- D2.outer + D3 — remain in #2877.
- Closing #2853 / #2851 to discharge the inherited sorry chain — out
  of scope; per spec-driven development this issue's wrapper is sound
  with a sorry'd dispatch leaf.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6` passes.
- New theorem builds with no `sorry` in its body — it threads through
  `d5tilde_not_finite_type_per_kQ` (whose own body is sorry-free; the
  inherited sorry chain runs through `d5tildeRep_kQ_isIndecomposable`,
  pre-existing and tracked by #2853 / #2851).
- Net sorry-count delta on `main`: **0** (no new sorries introduced;
  no existing sorries closed).
- Run `lake exe cache get` before the first build to avoid a Mathlib
  rebuild (per project CLAUDE.md).
