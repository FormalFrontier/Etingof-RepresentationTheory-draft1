## Current state

Wave-60 audit #2858 of the D̃₅ Sub B cascade flagged two non-blocking
cleanup follow-ups, both of which landed today:

- **PR #2862** (closes #2860) — hoisted the three local `have` lemmas
  inside the canonical-orientation branch of
  `d5tildeRep_kQ_leaf_equalities` to top-level theorems:
  - `d5tilde_core_F` (`FieldGenericD5Tilde.lean:478`)
  - `d5tilde_core3_F` (`FieldGenericD5Tilde.lean:540`)
  - `d5tilde_gamma_containment_F` (`FieldGenericD5Tilde.lean:598`)
  Each is parameterised over `(F, Q, hOrient, m, Wmain, Wother)`.
  Diff: +185/-157 in `FieldGenericD5Tilde.lean`. No sorry-count change.

- **PR #2863** (closes #2859) — hoisted two helpers cross-file from
  `FieldGenericD5Tilde.lean` to `FieldGenericStar.lean`:
  - `embed_sum_zero_F` (`FieldGenericStar.lean:86`)
  - `center_decomp_F` (`FieldGenericStar.lean:111`)
  Also moved `starFirst_F` / `starSecond_F` upstream so
  `center_decomp_F` can be stated in projection form, replaced the
  local `have embed_sum_zero` (was at `FieldGenericStar.lean:114-135`)
  and `have center_decomp` (was at `FieldGenericStar.lean:270-278`)
  inside `starRepGen_isIndecomposable` with calls to the new public
  theorems, and deleted the redundant copies in
  `FieldGenericD5Tilde.lean:215-249`. Diff: +144/-92 across two files.
  `embed_sum_zero_F` now also feeds the canonical-branch proof of
  `d5tildeRep_kQ_leaf_equalities` (via `FieldGenericD5Tilde.lean:530,
  588`). No sorry-count change.

Both PRs claim "mechanical hoist, no proof changes". The audit
recommended them ahead of #2853 (the 31 non-canonical orientation
sub-cases of `d5tildeRep_kQ_leaf_equalities`), which will consume the
top-level theorems directly. If the hoists silently changed a
hypothesis or lost a side-condition, #2853 will fail in confusing
ways once it tries to apply `d5tilde_core_F` / `d5tilde_core3_F` /
`d5tilde_gamma_containment_F` to its reversed-orientation case-splits.

This is a pre-flight check on the helper infrastructure #2853 depends
on. No code change expected from this issue.

## Deliverables

1. **Statement fidelity (PR #2862).** Read
   `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean:476-680`
   and confirm that the hoisted statements of `d5tilde_core_F`,
   `d5tilde_core3_F`, and `d5tilde_gamma_containment_F` are
   logically equivalent to the local `have` blocks they replaced. Use
   `git show 2332f15 -- EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean`
   to inspect the diff. Confirm in particular:
   - All hypotheses on the local `have`s are present on the top-level
     theorem statement (no implicit assumption from the enclosing
     scope was silently dropped).
   - The `(Wmain, Wother)` parameterisation is genuinely symmetric —
     applying the lemma to `(W₁, W₂)` and to `(W₂, W₁)` both work and
     give the expected leaf containments.
   - The canonical-branch proof body (the part of
     `d5tildeRep_kQ_leaf_equalities` that previously held the local
     `have`s) now invokes the three new top-level theorems with
     correctly threaded hypotheses, with the same overall proof
     structure preserved.

2. **Statement fidelity (PR #2863).** Read
   `EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean:78-120`
   and confirm that the hoisted `embed_sum_zero_F` and
   `center_decomp_F` are logically equivalent to the local `have`
   blocks they replaced inside `starRepGen_isIndecomposable`. Use
   `git show dc3a601 -- EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean`
   to inspect the diff. Confirm:
   - The hoisted `embed_sum_zero_F` proves `x = 0 ∧ y = 0` given
     `starEmbed1_F F m x + starEmbed2_F F m y = 0` — i.e. the
     disjointness-at-the-center fact #2853 needs.
   - The hoisted `center_decomp_F` decomposes any
     `w : Fin (2 * (m + 1)) → F` as
     `starEmbed1_F F m (starFirst_F F m w) +
      starEmbed2_F F m (starSecond_F F m w)`.
   - The local `have embed_sum_zero` and `have center_decomp`
     inside `starRepGen_isIndecomposable` are gone (verify with
     `grep -n "have embed_sum_zero\|have center_decomp"
     EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean`)
     and the two former use sites now call the public theorems.

3. **Mathematical preservation of `starRepGen_isIndecomposable`.**
   Build `lake build EtingofRepresentationTheory.Chapter6.FieldGenericStar`
   on the current `main` and confirm:
   - The build is green (sorry count = 0 in this file).
   - The proof of `starRepGen_isIndecomposable` still goes through
     with the existing `set_option maxHeartbeats 1600000` bump (the
     hoist should not have changed the heartbeat profile materially).
   - Document the build-time and the sorry summary in the audit
     report. The K_{1,4} canonical proof is the foundation the D̃₅
     cascade reuses; any regression here cascades downstream.

4. **Downstream-readiness for #2853.** Read
   `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean`
   around the case-split sorries at lines 802 / 804 / 806 / 808 / 810
   (the 31 reversed-orientation sub-cases #2853 will fill). For each
   sorry position, briefly note which of the three hoisted helpers
   (`d5tilde_core_F` / `d5tilde_core3_F` / `d5tilde_gamma_containment_F`)
   the sub-case will need — i.e. confirm that the hoisted helpers
   are usable for at least some of the 31 sub-cases (the audit
   estimated ~26% direct reuse, with the other ~74% needing
   projection-direction siblings). This is a sanity check that the
   parameterisation hasn't accidentally made the helpers
   canonical-only.

5. **Build green:** `lake build EtingofRepresentationTheory.Chapter6`
   passes; sorry count in `FieldGenericD5Tilde.lean` is unchanged at
   6 raw sorries / 2 declarations using sorry; sorry count in
   `FieldGenericStar.lean` is 0; no new heartbeat bumps introduced.

6. **Verdict** (PASS / PASS-with-followups / FAIL per PR) in a
   single audit report at `progress/reviews/2026-05-18-d5tilde-subB-cleanup-hoist.md`,
   with one section per PR. PASS-with-followups should file a sub-issue
   for each follow-up. The audit verdict should be posted as a comment
   on issue #2804 (the D̃₅ Sub B parent), following the wave-60
   pattern established by #2858 / #2861.

## Context

- Parent context: D̃₅ Sub B cascade (#2804 → #2834 → #2839 → #2850 →
  #2853 / #2851). Wave-60 audit was #2858 (closed by PR #2861).
- The two PRs under audit landed in dispatch cycle 12f509fe (~2 hours
  ago); the audit closes the wave-60 cleanup loop before #2853 starts.
- Audit report: `progress/reviews/2026-05-18-d5tilde-subB-cascade-helpers.md`
  (the wave-60 audit that originally recommended these two refactors).
- Latest progress files:
  `progress/20260518T031934Z_e3af775b.md` (worker on PR #2863) and
  `progress/20260518T030155Z_4e34128e.md` (worker on PR #2862).
- Sorry landscape (wave-60 snapshot): `progress/sorry-landscape.md`.
- Files in scope:
  `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean`
  (478 / 540 / 598 / 802-810 line ranges) and
  `EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean`
  (78-120 / 160-340 line ranges).
- This is a **light cleanup audit** — both PRs are mechanical hoists.
  Do not re-audit the mathematical content of the original lemmas
  (already covered by #2858); focus narrowly on whether the hoist
  preserved the statements and proofs, and on whether the new
  parameterisation is ready for #2853's use.

## Verification

- Audit report committed at the path above.
- Verdict comment posted on #2804.
- `lake build EtingofRepresentationTheory.Chapter6` exits green;
  sorry counts in `FieldGenericD5Tilde.lean` and `FieldGenericStar.lean`
  match the wave-60 baseline.
- Any PASS-with-followups identifies the follow-up sub-issues by
  number in the verdict.
