## Current state

Sub of #2877 (`not_posdef_infinite_type_per_kQ` assembly + Theorem
2.1.2 bridge), pre-split by planner cycle `68d03f5f` to feed a
parallel worker. This issue covers **D2.cycle**, the second pre-split
following D2.degree4 (#2889 / PR #2891, audit verdict PR #2894).

The audit verdict for PR #2891 (review file
`progress/reviews/2026-05-18-degree4-per-kQ-placement.md`) closed
with PASS and explicitly recommended D2.cycle as the natural next
pre-split: "natural host; no cross-import needed; no
`[IsAlgClosed F]`. Asymmetric to D2.degree4 because the only leaf
needed lives in Cycle itself" (D3, "Recommended placement", first
bullet).

The `_kQ`-free original `graph_with_list_cycle_infinite_type` lives
at `Chapter6/InfiniteTypeConstructions.lean:3910-4057` (~148 lines
of strong induction with chord detection). The umbrella #2877 sizes
this sub-deliverable at ~150 lines.

## Deliverables

1. **`graph_with_list_cycle_infinite_type_per_kQ`** in
   `Chapter6/FieldGenericCycle.lean`. Mirror
   `graph_with_list_cycle_infinite_type`
   (`Chapter6/InfiniteTypeConstructions.lean:3910-4057`) with the
   per-(F, Q) signature pattern (append `(F : Type) [Field F]`,
   `(Q : @Quiver.{0,0} (Fin n))`,
   `[∀ a b, Subsingleton (@Quiver.Hom (Fin n) Q a b)]`,
   `(hOrient : @Etingof.IsOrientationOf n Q adj)` at the end of the
   argument list; do **not** add `[IsAlgClosed F]` — the only leaf
   used, `cycle_not_finite_type_per_kQ`, does not require it).
   Conclusion uses the canonical per-(F, Q) shape
   `¬ Set.Finite {d : Fin n → ℕ | ∃ V : QuiverRepresentation F (Fin n) Q,
       V.IsIndecomposable ∧ ∀ v, Nonempty (V.obj v ≃ₗ[F] (Fin (d v) → F))}`.
   The body is structurally identical to the `_kQ`-free original;
   the chordless-branch dispatch swaps
   `subgraph_infinite_type_transfer φ adj (cycleAdj m hm) hsymm ...
   (cycle_not_finite_type m hm)` for the per-(F, Q) equivalent
   `subgraph_infinite_type_transfer_per_kQ φ F Q
   (cycle_not_finite_type_per_kQ F m hm (restrictOrientationViaEmb φ Q)
   (restrictOrientationViaEmb_isOrientationOf φ hembed hOrient))` —
   exactly the pattern already used by
   `chordless_cycle_infinite_type_per_kQ` at
   `Chapter6/FieldGenericCycle.lean:388-390`.

## Context

- **Parent**: #2877 (covers D2.adjacent, D2.single, D2.nonadj,
  D2.outer + D3 after this issue lands).
- **Established per-(F, Q) recipe**: see
  `progress/reviews/2026-05-18-degree4-per-kQ-placement.md`, D3
  "Pattern recipe (for citation by future D2 sub-PRs)". Apply steps
  1–4 verbatim (with `[IsAlgClosed F]` **omitted** for this wrapper).
- **Per-(F, Q) leaf used** (already on `main`):
  - `cycle_not_finite_type_per_kQ`
    (`Chapter6/FieldGenericCycle.lean:326`) — no `[IsAlgClosed F]`.
- **Per-(F, Q) transfer wrapper**:
  `subgraph_infinite_type_transfer_per_kQ`
  (`Chapter6/FieldGenericInfiniteType.lean:374`).
- **Pattern example** (same dispatch shape, smaller scale):
  `chordless_cycle_infinite_type_per_kQ`
  (`Chapter6/FieldGenericCycle.lean:373-390`).

### Placement

`FieldGenericCycle.lean` is the natural host: it already contains
`cycle_not_finite_type_per_kQ` (line 326),
`chordless_cycle_infinite_type_per_kQ` (line 373), and
`triangle_infinite_type_per_kQ` (line 398). No new imports are
required — the file already imports `FieldGenericInfiniteType` for
`subgraph_infinite_type_transfer_per_kQ` and
`restrictOrientationViaEmb_isOrientationOf`. Place the new theorem
after `triangle_infinite_type_per_kQ` (line 428) and before the
final `end Etingof` (line 430).

Apply the `attribute [-instance] CategoryTheory.CategoryStruct.toQuiver
CategoryTheory.ReflQuiver.toQuiver in` prefix per recipe step 4 — see
existing uses at lines 367 and 392 in the same file.

### Out of scope

- D2.adjacent (`adjacent_branches_infinite_type_per_kQ`, ~150 lines,
  needs `d5tilde_*_per_kQ`, `[IsAlgClosed F]`) — remains in #2877.
- D2.single (`single_branch_not_posdef_infinite_type_per_kQ`, ~300
  lines, needs `etilde6_*`/`etilde7_*`/`t125_*_per_kQ`,
  `[IsAlgClosed F]`) — remains in #2877.
- D2.nonadj (`non_adjacent_branches_infinite_type_per_kQ`, ~900
  lines, audit recommends dedicated file
  `FieldGenericNonAdjacent.lean`) — remains in #2877.
- D2.outer + D3 — remain in #2877.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6` passes.
- New theorem builds with no `sorry` in its body — it threads
  through `cycle_not_finite_type_per_kQ` (proven on `main`) via
  `subgraph_infinite_type_transfer_per_kQ`. The `_kQ`-free original
  is sorry-free; the per-(F, Q) version inherits that property.
- Net sorry-count delta on `main`: **0** (no new sorries
  introduced; no existing sorries closed).
- Run `lake exe cache get` before the first build to avoid a
  Mathlib rebuild (per project CLAUDE.md).
