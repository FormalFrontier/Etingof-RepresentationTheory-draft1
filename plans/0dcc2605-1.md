Stage 3.7 statement-fidelity & non-vacuity audit of **Problem 4.12.11** (Chapter 4 —
application of representation theory to elasticity / Hooke's law). Report-only: record
the verdict as durable state; no Lean proof edits unless a genuine `gap` requires a
repair-issue follow-up.

## Current state

- Book statement (`blobs/Chapter4/Problem4.12.11.md`): the elasticity problem. The two
  mathematical parts to be audited are:
  - **(a)** `End(V)` decomposes as `ℝ ⊕ V ⊕ W` (trivial ⊕ standard 3-dim ⊕ 5-dim), and
    `S²V = ℝ ⊕ W`, as representations of `SO(3)` acting by conjugation.
  - **(b)** `V` and `W` are irreducible, even after complexification; deduce via Schur
    that a Galileo-invariant Hooke's law `f : S²V → End(V)` satisfies `f(x + y) = Kx + μy`
    for `x ∈ ℝ`, `y ∈ W`, with `S_P` always symmetric.

- Lean: `EtingofRepresentationTheory/Chapter4/Problem4_12_11.lean` (1504 lines), status
  `sorry_free`, `fidelity: (none)` (un-audited), `coverage: (none)`. `SO3` is the special
  orthogonal group, `EndV = Matrix (Fin 3) (Fin 3) ℝ`, `conjRep` the conjugation
  representation. Headline declarations to audit:
  - **(a) decomposition.** `endV_isInternal` (line 187):
    `DirectSum.IsInternal ![scalarSub, skewSub, tracelessSymSub]` (the internal direct
    sum `ℝ ⊕ V ⊕ W`), with `scalarSub_finrank = 1`, `skewSub_finrank = 3`,
    `tracelessSymSub_finrank = 5`, and `symSub_eq_scalar_sup_tracelessSym` for
    `S²V = ℝ ⊕ W`. Note the identification of the standard rep `V` with the skew-symmetric
    matrices `skewSub` (3-dim) and `W` with the traceless-symmetric matrices
    `tracelessSymSub` (5-dim) — confirm this is a faithful rendering of the book's
    `End(V) = ℝ ⊕ V ⊕ W`.
  - **(b) irreducibility.** `skewSub_irreducible` (647), `tracelessSymSub_irreducible`
    (720) over ℝ; `skewSub_irreducible_complexified` (1041),
    `tracelessSymSub_irreducible_complexified` (1110) over ℂ (on
    `EndVc = Matrix (Fin 3) (Fin 3) ℂ` via the ring hom `cx` and `cx_conjRep`).
  - **(b) Hooke's law.** `hooke_law` (1407): for `f : EndV →ₗ[ℝ] EndV` intertwining
    `conjRep`, there exist `K μ : ℝ` with `f x = K • x` on `scalarSub`, `f y = μ • y` on
    `tracelessSymSub`, and `f` preserving `symSub` (symmetry of `S_P`).

## Deliverables

1. A **statement-fidelity & non-vacuity verdict** covering all headline theorems of both
   parts, written to
   `progress/reviews/<UTC>-ch4-problem4_12_11-elasticity-hooke-fidelity.md`. The audit
   must explicitly address:
   - **Hypothesis faithfulness.** Confirm `conjRep` is the genuine `SO(3)`-conjugation
     action on `End(ℝ³)` (a real `Representation`, not a placeholder), that `SO3` is the
     actual special orthogonal group, and that the intertwiner hypothesis on `f` in
     `hooke_law` (`∀ A, f ∘ conjRep A = conjRep A ∘ f`) faithfully encodes "`f` is a
     homomorphism of `SO(3)`-representations" (Galileo invariance). Confirm the
     `skewSub`/`tracelessSymSub`/`scalarSub` submodules are the intended `V`/`W`/`ℝ`.
   - **Conclusion faithfulness — the key check.** Confirm `endV_isInternal` +
     the three `finrank` lemmas faithfully deliver `End(V) = ℝ ⊕ V ⊕ W` with dims
     `1, 3, 5`, and `symSub_eq_scalar_sup_tracelessSym` delivers `S²V = ℝ ⊕ W`. Confirm
     the four irreducibility theorems genuinely prove irreducibility of `V` and `W` over
     both ℝ and ℂ (a submodule `U ≤ skewSub` invariant under `conjRep` is `⊥` or the
     whole space — check the exact form of the conclusion). Confirm `hooke_law` faithfully
     yields the book's `f(x + y) = Kx + μy` and the symmetry of `S_P`. State whether the
     complexified irreducibility is phrased over the correct object (`EndVc` via `cx`).
   - **Non-vacuity.** The intertwiner hypothesis in `hooke_law` is satisfiable by genuine
     invariant `f` (e.g. scalar multiples / projections), so the conclusion is not
     vacuous; `SO3` is nontrivial; the submodules `skewSub`, `tracelessSymSub` are nonzero
     (dims 3, 5); no `True`-typed or trivially-dischargeable hypothesis; the irreducibility
     theorems are non-vacuous (the ambient submodules are nonzero). Record whether the book
     question is `covered_full` (both parts, real + complex, Hooke's law) or
     `covered_partial` with a precise note.
2. `#print axioms` on each headline theorem — confirm `[propext, Classical.choice,
   Quot.sound]` only, no `sorryAx`. `lake build` the file (run `lake exe cache get` first).
3. Record the verdict durably in `progress/items.json` under `Chapter4/Problem4.12.11`:
   set `fidelity` (`verified` / `gap`), add `fidelity_decl` (list the headline theorems),
   `lean_file`, `coverage` (`covered_full` / `covered_partial`), and a short
   `fidelity_note` — following the field conventions of the #7276 / #7280 audits.
4. If (and only if) a genuine fidelity gap is found, file a follow-up issue describing the
   exposure and reference it via `fidelity_issue`; do **not** silently edit the Lean
   statement in this review issue.

## Context

Problem 4.12.11 is the last major un-audited Chapter 4 problem (all its feature work
landed in #6353, #6539, #6985). This mirrors the report-only Stage 3.7 audit pattern of
#7276 (Problem 4.12.5) and #7280 (Problem 4.12.8).

## Verification

`lake build` exits 0; the file remains `sorry`-free; the review report and
`items.json` entry are committed. No change to the Lean statements unless a genuine gap
is found and split into a separate repair issue.
