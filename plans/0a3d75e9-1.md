## Current state

PR #5029 (#4992 / #4974 sub-B2, merged `2a990bc4`→`main`) landed a new,
self-contained, **sorry-free** file
`EtingofRepresentationTheory/Chapter5/SpechtBridgeGeneral.lean` (+402 lines, 13
declarations). It lifts the ℂ Specht bridge (β.2) of `Theorem5_22_1.lean` to a
general characteristic-zero algebraically closed field `k`: it identifies a simple
`symGroupImage`-submodule `S ≤ V^⊗n` (`V = Fin N → k`) with a Specht module and
reads off its permutation-trace character as the general-`k` Specht character
`spechtModuleCharacterK k n la` (#4991).

This file has **not** had a soundness audit. The adjacent reviews cover different
clusters and do not touch it:

- #5017 (merged) — general-`k` Theorem 5.12.2 *classification* cluster (#4985/#4989/#5009)
- #5019 / PR #5028 (merged) — Cauchy-identity / formal-character cluster (#4957/#4958/#4934)
- #5030 (claimed, in flight) — general-`k` Specht *character* + young-symmetrizer
  *special-block* cluster (`SpechtCharacterGeneral.lean` + `SchurWeylSpecialBlockGeneral.lean`)

#5005 (now unblocked) will *consume* the public lemmas of this file
(`trace_symGroupAction_eq_spechtModuleCharacterK`,
`simpleSubmodule_iso_of_spechtCharacterK_eq`,
`simpleSymGroupImageSubmodule_iso_of_spechtCharacterK_eq`), so auditing the bridge
now de-risks that downstream assembly.

## Deliverables

1. Soundness audit of `EtingofRepresentationTheory/Chapter5/SpechtBridgeGeneral.lean`.
   Confirm the file is genuinely sorry-free / axiom-free / admit-free
   (comment-stripped, not raw `grep`), and that every `def`/`noncomputable def`/
   `instance` has a real (non-sorry) body.
2. Verify the three **public** lemmas state what their names and docstrings claim,
   that hypotheses are not vacuous or accidentally too strong, and that the
   general-`k` generalization faithfully mirrors the ℂ original in
   `Theorem5_22_1.lean` (no silent weakening of the simplicity / character
   conclusions, no `[Field k]`-vs-`ℂ` instance gaps that trivialize a statement).
3. Spot-check the private support lemmas feeding the public results
   (`submoduleAsSymGroupAlgebra_isSimpleModuleK`, `submoduleSemilinearIdK_bijective`,
   `trace_restrictedSymGroupAction_eq_of_spechtIsoK`, the `symGroupAlgHomToImageK_*`
   family) for the usual hazards: trivial/degenerate instances, hypothesis-name
   mismatches, and reliance on an unstated nonzero-dimension assumption.

## Context

This is a read-only soundness review. Follow the `review` work-type flow in the
`agent-worker-flow` skill. The bridge's mathematical content is Etingof Theorem
5.22.1 (β.2); the book proof strategy is in the relevant `blobs/` file for Theorem
5.22.1 and the ℂ original is `Theorem5_22_1.lean` (the lemmas
`trace_symGroupAction_eq_spechtModuleCharacter` and
`simpleSubmodule_iso_of_spechtCharacter...`). Compare the general-`k` versions
against those originals rather than re-deriving from scratch.

If the audit finds a genuine soundness defect (vacuous statement, sorry'd
definition body, false-as-stated lemma), open a `feature` fix issue with details
rather than fixing in the review PR.

## Verification

- `lake build EtingofRepresentationTheory.Chapter5.SpechtBridgeGeneral` succeeds on
  a fresh `main`-based branch (run `lake exe cache get` first).
- Comment-stripped sorry/axiom/admit count for the file is **0** (use the awk
  block-comment-depth method from `progress/sorry-landscape.md`, not raw `grep -rc`).
- Audit findings recorded in the review PR body (PASS, or PASS-with-notes, or a
  pointer to the fix issue opened for any defect).
