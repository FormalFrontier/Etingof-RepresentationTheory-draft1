## Current state

PR #2979 (merge commit `2a9398f`, +146 in
`Chapter6/FieldGenericNonAdjacentBranches.lean` plus the
companion progress entry, closing none — partial progress on #2955)
landed two sub-cases of the negative `by_cases hA … hD` residual in
`non_adjacent_branches_leaf_case_per_kQ`
(`Chapter6/FieldGenericNonAdjacentBranches.lean:87`):

- **Sub-case E.aa** — `chain.length = 3 ∧ vertexDegree adj arm₁ = 2 ∧
  vertexDegree adj arm₂ = 2`: dispatches to
  `embed_etilde6_in_tree_per_kQ` (Ẽ₆ at `w`) using
  `chain[chain.length − 3] = v₀` and `chain[chain.length − 2] = c` as
  the third length-2 leg. Mirrors Case D's dispatch with the
  arithmetic `chain.length − 3 = 0`, `chain.length − 2 = 1` falling
  out of `omega` from `hchain_len`.
- **Sub-case E.s1c4** — `chain.length = 4 ∧
  vertexDegree adj side_arm = 1`: dispatches to
  `embed_d7tilde_in_tree_per_kQ` (D̃₇ at `(v₀, w)`) on the 8-vertex
  induced subgraph `{leaf, side_arm, v₀, chain[1], chain[2], w, arm₁,
  arm₂}`. The branch-point degrees `chain[1].deg = chain[2].deg = 2`
  fall out of `h_no_adj_branch` / `h_no_adj_branch_w`.

The residual `sorry` (still at the bottom of the negative branch) is
re-documented to cover only:
- `chain.length = 3` with mixed arm degrees (E.ab, E.bb).
- `chain.length = 5` (any sub-case): needs unavailable D̃₈ helper.
- `chain.length ≥ 6` all-leaves: needs general parametric D̃_n helper.

Each is tracked by a follow-up sub-issue (`#2974` D̃₆ helper for
chain.length=3 all-leaves, `#2976` Ẽ₇ extension splits for
chain.length=3 mixed, `#2977` D̃₈ for chain.length=5, `#2978` general
D̃_n for chain.length≥6 all-leaves). The same-PR sorry count on
`non_adjacent_branches_leaf_case_per_kQ` is unchanged (the top-level
header still warns `declaration uses sorry`), but the physical
sorry-protected configuration space genuinely shrinks.

This audit is the **first review of PR #2979**.

## Deliverables

Produce a five-dimension audit report at
`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-cases-e-aa-and-e-s1c4.md`
covering PR #2979. Each dimension is PASS / CONCERN / FAIL with
file:line evidence against **current `main`** (record the audited
commit in the header).

**D1 — Sub-case E.aa correctness (Ẽ₆ at w via reversed chain edge).**
The block lives around
`FieldGenericNonAdjacentBranches.lean:984-1037` (verify against
landed commit). Walk the dispatch end-to-end:
   (a) Hypothesis destructuring matches the `by_cases hE_aa` pattern
       (`chain.length = 3 ∧ harm₁.deg = 2 ∧ harm₂.deg = 2`).
   (b) `y₁` extraction: confirm the `Sarm1 := filter (adj arm₁ · = 1)`
       / `card_eq_one` / `Finset.erase` pattern mirrors Case D's
       extraction at `FieldGenericNonAdjacentBranches.lean:~870-890`
       (line numbers approximate; verify against landed text). The
       `hy₁_adj : adj arm₁ y₁ = 1` and `hy₁_ne_w` conclusions must
       match exactly what `embed_etilde6_in_tree_per_kQ` consumes.
   (c) `y₂` extraction: same shape, with `arm₂`.
   (d) Reversed chain edge `hcL_2_3` /
       `hcR_2_3 : adj chain[len-2] chain[len-3] = 1`: confirm the
       `Fin.ext`-based index rewrite works for `chain.length = 3`
       (i.e., `chain[1] → chain[0]` is what falls out — these are
       `c → v₀` in the comment, so check the orientation matches
       Case D's "mirror" pattern, not Case A's "forward" pattern).
   (e) `hc_3_ne_w`: distinctness of `chain[len-3]` from `w` derived
       via `hw_get` + `hchain_nodup.get_inj_iff`.
   (f) Final `embed_etilde6_in_tree_per_kQ` call: verify the 7
       vertex arguments map onto `Ẽ₆ = T(2,2,2)` at `w` as claimed
       (`0 → w, 1 → chain[len-2], 2 → chain[len-3], 3 → arm₁,
       4 → y₁, 5 → arm₂, 6 → y₂`) by reading the
       `embed_etilde6_in_tree_per_kQ` signature at
       `FieldGenericETilde6.lean:372` and matching positions. Match
       the 13 fact arguments (6 adjacency + 6 distinctness +
       `harm₁₂`) against the signature.
   Spot-check ≥3 of the harder distinctness/adjacency arguments by
   resolving each `let _` and `:= h_*` to its definition; do not
   exhaustively re-derive.

**D2 — Sub-case E.s1c4 correctness (D̃₇ at (v₀, w)).** The block
lives around `FieldGenericNonAdjacentBranches.lean:1038-1080`
(verify). Walk it end-to-end:
   (a) Hypothesis destructuring: `chain.length = 4 ∧ side.deg = 1`
       (note `_hside_deg1` is bound but unused — confirm it is the
       harmless `let _ := …` or eliminated).
   (b) `hc12 : adj chain[1] chain[2] = 1` from `hchain_edges 1`
       (literal index, not arithmetic): confirm `chain.length = 4`
       makes this in-range.
   (c) `hc2_eq_pre : chain[2] = chain[len-2]` via `Fin.ext` —
       verify the `omega` discharge.
   (d) `hc2_w : adj chain[2] w = 1` via `hc2_eq_pre` + `adj_comm` +
       `hw_chain_adj`: verify the rewrite direction.
   (e) `hps_eq : adj v₀ w = 0` via `(h01 v₀ w).resolve_right
       h_v₀w_nonadj`: confirm `h_v₀w_nonadj` is the strengthened
       hypothesis introduced by PR #2941 (the audit
       `progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-api-stub.md`
       confirmed this is on `main`).
   (f) Distinctness facts `harm₁_ne_c2`, `harm₂_ne_c2` from
       `arm{1,2}_ne_chain 2`: verify these are the existing
       Finset-derived distinctness in scope.
   (g) Final `embed_d7tilde_in_tree_per_kQ` call: verify the 8
       vertex arguments map onto `D̃₇ at (v₀, w)` as claimed
       (`0 → leaf, 1 → side_arm, 2 → v₀, 3 → chain[1],
       4 → chain[2], 5 → w, 6 → arm₁, 7 → arm₂`) by reading the
       `embed_d7tilde_in_tree_per_kQ` signature at
       `FieldGenericD7Tilde.lean` (line tbd — locate it; this
       embedder was introduced by PR #2970, audit pending under
       #2973). Match all adjacency / non-adjacency / distinctness
       facts position-by-position.

**D3 — Residual sorry documentation + sub-issue breadcrumbs.** The
residual `sorry` block at the end of the negative branch
(`FieldGenericNonAdjacentBranches.lean:~1080-1090`, the
`hE_s1c4 → False` arm of the inner `by_cases`) is now guarded by:
   (a) A comment block enumerating *exactly* which residual
       configurations remain: chain.length=3 mixed (E.ab/E.bb),
       chain.length=5 (any), chain.length≥6 all-leaves.
   (b) `let _ := …` bindings for unused-but-referenced hypotheses
       (Lean lint quiet); verify these are *all* documented
       hypotheses that the residual *would* use, not just a random
       subset.
   (c) Sub-issue references in the comments. Confirm by reading
       the issue bodies of #2974 / #2976 / #2977 / #2978: each
       sub-issue's "Current state" must claim coverage of exactly
       one of the documented residual configurations, and the
       union of the four must cover the comment-block enumeration
       exactly (no orphan configuration; no double-coverage). If
       a residual configuration is documented in the comment but
       has no sub-issue (or vice versa), CONCERN.

**D4 — Build sanity at current `main`.** Run
`lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
on current `main`. Record the warning list verbatim. Expected:
   - exactly one `declaration uses sorry` warning at the top of
     `non_adjacent_branches_leaf_case_per_kQ` (line 87), from the
     residual at the negative branch.
   - no new warnings introduced by the E.aa / E.s1c4 blocks
     themselves (no unused-variable warnings beyond the documented
     `let _ := …` bindings in the residual).
Then build
`EtingofRepresentationTheory.Chapter6.FieldGenericAssembly` to
confirm downstream still green.

**D5 — Cross-file consistency / progress alignment.** Spot-check:
   - Comment-block enumeration in the residual matches the
     sub-issue decomposition exactly (cross-checked under D3).
   - The progress entry
     `progress/20260519T113642Z_5cda04fc.md` (landed in the same
     PR) accurately summarises which sub-cases closed and matches
     the actual landed sub-issue numbers.
   - The "Case D's dispatch pattern" claim in the E.aa comment
     (`chain.length - 3 = 0` etc.) is exactly the dispatch pattern
     used by Case D at `FieldGenericNonAdjacentBranches.lean:~900`
     (no signature divergence — the same `embed_etilde6_in_tree_per_kQ`
     positional argument shape).
   - The "T(1, 2, r)" / "T(1, q, 2)" / etc. forbidden-subgraph
     library exhibited by this PR's two dispatches is consistent
     with the documented per-(F, Q) library inventory in the
     parent file's docstring (`FieldGenericNonAdjacentBranches.lean:
     ~28-50`). Note if the docstring is stale (e.g., still claims
     "no D̃₇ helper" after PR #2968 + the dispatch added here);
     the prior audit
     `progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-api-stub.md`
     flagged this exact staleness — confirm it is still present
     and flag CONCERN.

## Context

- Parent: #2955 (still open, `replan`-labelled — replan triage
  will decide further decomposition). PR #2979 was *partial*
  progress on #2955 and decomposed the residual into #2974 / #2976
  / #2977 / #2978.
- PR #2979 reuses two embedders:
   - `embed_etilde6_in_tree_per_kQ` (audited PASS by review #2949 →
     `progress/reviews/2026-05-19-embed-etilde6-and-etilde7-in-tree-per-kQ.md`)
   - `embed_d7tilde_in_tree_per_kQ` (audit pending under #2973 —
     PR #2970). If #2973 audit lands FAIL on the D̃₇ embedder
     signature/correctness, this audit's D2 verdict should reflect
     the dependency.
- Sibling Phase 2 cases A/B/C-main/D were audited by PR #2975
  (`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-phase-1-and-cases-a-b-c-d.md`).
  Use that as the audit-style precedent for the per-case
  five-dimension format.
- The progress entry `progress/20260519T113642Z_5cda04fc.md`
  documents the planner-side decomposition and is itself part of
  what D5 verifies.

## Verification

- Audit report committed at the named path with PASS / CONCERN /
  FAIL per dimension and file-line citations.
- Report header records the audited `main` commit
  (`git rev-parse HEAD`) and the PR #2979 merge commit
  (`2a9398f`).
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
  and `…FieldGenericAssembly` are green from the audited commit;
  warning lists captured verbatim.
- No code changes in this PR unless a build-breaking issue is
  discovered (in which case fix in-PR per audit #2944 precedent).
- PR opened via `coordination create-pr` with `Closes #<this-issue>`
  and auto-merge enabled.
