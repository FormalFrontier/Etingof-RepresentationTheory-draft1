## Current state

The wave-63 chore-citation audit
(`progress/reviews/2026-05-20-wave-63-chore-citation-refresh.md`,
issue #2995) flagged three small concerns in the residual-`sorry`
block at `Chapter6/FieldGenericNonAdjacentBranches.lean:1100-1119`:

* (i) the comment-block enumeration listed 3 bullets while the
  sub-issue decomposition is 4 items wide;
* (ii) the block's `let _ := arm₂_ne_chain` at line 1118 is **redundant**
  — `arm₂_ne_chain` is already syntactically used elsewhere in the
  proof (lines 862, 887, 1071), so the `let _` is not needed to
  silence an unused-variable warning, and the asymmetric inclusion of
  `arm₂_ne_chain` but not `arm₁_ne_chain` (which is also used:
  lines 782, 834, 885, 1069) is a minor inconsistency;
* (iii) the comment block did not cite the four sub-issue numbers.

Concerns (i) and (iii) were addressed by PR #2993 (issue #2990).
Concern (ii) remains open: lines 1114-1118 still read

```lean
let _ := hn; let _ := h_deg; let _ := h_no_adj_branch
let _ := h_no_adj_branch_w
let _ := hleaf_ne_arm₁; let _ := hleaf_ne_arm₂
let _ := hside_ne_arm₁; let _ := hside_ne_arm₂
let _ := leaf_ne_chain; let _ := arm₂_ne_chain
sorry
```

## Deliverables

1. Drop the redundant `let _ := arm₂_ne_chain` from line 1118 in
   `Chapter6/FieldGenericNonAdjacentBranches.lean`. The resulting line
   should read just `let _ := leaf_ne_chain` (no trailing `; let _ := …`).
2. Verify the other entries in the `let _` block are still needed (i.e.
   that removing each would re-introduce an unused-variable warning).
   Specifically: `hn`, `h_deg`, `h_no_adj_branch`, `h_no_adj_branch_w`,
   `hleaf_ne_arm₁`, `hleaf_ne_arm₂`, `hside_ne_arm₁`, `hside_ne_arm₂`,
   `leaf_ne_chain`. The audit report verified each of these is used
   only at its Phase 1 binding site and in this `let _` block, so they
   should be kept. **Do not modify them.**
3. Do **not** touch the surrounding comment block (lines 1100-1113),
   the `sorry` (line 1119), or the upstream enumeration block (lines
   994-1003). Comment changes were handled by PR #2993.

## Context

Relevant files:
* `Chapter6/FieldGenericNonAdjacentBranches.lean` — the only file to
  edit. Only line 1118 changes.
* `progress/reviews/2026-05-20-wave-63-chore-citation-refresh.md` —
  the audit report (D5(a) sub-concern carried over from
  PR #2984's D3).

The residual `sorry` at line 1119 is decomposed into sub-issues
#2974/#2976/#2977/#2978, all currently `blocked` on #2955 — those
sub-issues are out of scope; this chore only removes the redundant
`let _`. Total diff: 1 line.

## Verification

* `lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
  passes with exactly the same warning set as before — one residual
  `sorry` at line 1118 (now shifted by 0 since only the trailing part
  of one line was removed). No new unused-variable warnings.
* `git diff` shows exactly one line changed (line 1118), removing the
  ` ; let _ := arm₂_ne_chain` suffix.
