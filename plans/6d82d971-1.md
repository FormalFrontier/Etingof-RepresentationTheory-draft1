Stage 3.7 **coverage-arm audit** of the two multi-part §3.9 problems on
extensions and deformations: **Problem 3.9.1** (extensions of representations
classified by `Ext¹`, parts (a)-(d)) and **Problem 3.9.4** (formal deformations,
parts (a)-(b)). Both are already sorry-free with a prior fidelity review; this
issue adds the missing `coverage` field at sub-part granularity and reconciles
any stale notes. Continues the exercise-coverage ratchet sweep (siblings:
merged §5.11 #7351, §5.12.5 #7349, §5.16 #7347, §5.1 #7345; in-flight §5.8 #7342,
§5.3.3 #7346).

## Current state

- Blobs (both multi-part):
  - `blobs/Chapter3/Problem3.9.1.md` (p.56-57): (a) 1-cocycle condition on `f`,
    `Z¹(W,V)`; (b) coboundary `dX`, `B¹ ≅ Hom_k/Hom_A`, defines `Ext¹`;
    (c) `f-f' ∈ B¹` ⇒ extensions isomorphic, and converse; (d) for f.d.
    irreducibles, `U_f ≅ U_{f'}` iff `f,f'` proportional, so nontrivial
    extensions ↔ `ℙ Ext¹`, trivial iff `Ext¹ = 0`.
  - `blobs/Chapter3/Problem3.9.4.md` (p.58): (a) `Ext¹(V,V)=0` ⇒ every formal
    deformation is trivial; (b) open question — is the converse true (dual
    numbers `k[x]/x²`)?
- Lean (both **sorry-free**, `sorry count: 0`, `fidelity: verified`):
  - `EtingofRepresentationTheory/Chapter3/Problem3_9_1.lean` (458 lines).
    Part map: (a) `IsCocycle` def (line 50) + `blockOp_mul_iff_isCocycle`
    (line 67); (b) `coboundaries_eq_range`/`mem_coboundaries_iff` (lines 162,
    172) + `coboundaries_le_cocycles` (178); (c) `iso_of_sub_mem_coboundaries`
    (203) + `ext_iso_of_sub_smul_mem_coboundaries` (248); (d)
    `irreducible_ext_iso_iff_proportional` (307, `[IsAlgClosed k]`).
  - `EtingofRepresentationTheory/Chapter3/Problem3_9_4.lean` (347 lines).
    Part map: (a) `isTrivial_of_ext1_subsingleton` (line 309); (b) the open
    converse recorded as the proposition `ConverseHolds` (335) /
    `Problem3_9_4b` (342) specialised to the dual numbers, matching the book's
    open-ended question.
- `progress/items.json`: items `Chapter3/Problem3.9.1` and
  `Chapter3/Problem3.9.4` each have `status: sorry_free`, `sorry_free: true`,
  `fidelity: verified`, `lean_file` — but **no `coverage` field** and no
  `derived` sub-part entries.

## Deliverables

1. Assign a `coverage` field to each of the two items in `progress/items.json`
   (`covered_full` / `covered_partial` / `not_started` / `non_formalizable`),
   with `lean_decl`, `lean_file`, `last_updated`, and a one-line rationale —
   mirror the item shape used by the §5.11.1 (#7351) and §5.16 (#7347) audits.
   Because both problems are multi-part, record **per-sub-part** coverage via a
   `derived` array (one entry per part, with `derived_from`, `source_span`,
   `claim`, and per-part coverage) exactly as Problem 5.11.1 did for its five
   parts. For 3.9.4(b), if the open-ended converse is only recorded as an
   uninhabited/open proposition, classify it honestly (`non_formalizable` with
   `reason`, or `covered_partial`) rather than `covered_full`.
2. Re-confirm the recorded `lean_decl` for each part actually resolves in the
   sorry-free file and that the file still builds sorry-free (`lake build` on
   the two modules; `#print axioms` on the headline decls should show no
   `sorryAx`). The prior `fidelity: verified` reviews stand — reuse them; do
   **not** redo a full fidelity sweep. Only apply the Stage 3.2 step 6-7 spot
   check to any part whose Lean decl looks weaker than the book claim (e.g.
   (c)'s converse direction, or (d)'s `[IsAlgClosed k]` hypothesis vs the
   book's plain "irreducible").
3. Reconcile any stale `coverage_note`/status so the note and the `coverage`
   field agree; re-validate `progress/items.json` parses. Write a short
   coverage report under `progress/reviews/`. If (and only if) a sub-part's
   Lean statement is strictly weaker than the book's claim, mark that sub-part
   `covered_partial` and open a follow-up `feature` issue describing the gap —
   otherwise no follow-up issue is needed.

## Context

- Stage 3.7 exercise-coverage ratchet (PLAN.md §"Exercise-coverage ratchet"):
  every exercise/problem item must carry an honest `coverage` field instead of
  a blanket `sorry_free`; multi-part problems are tracked at sub-part
  granularity via `derived` items so partial credit and the live frontier are
  visible. The metric may only ratchet upward and is reported separately from
  release criteria.
- Pattern to copy: `progress/2026-07-22T17-26-21Z_*.md` (Problem 5.11.1 5-part
  coverage-arm audit) and the merged #7351 diff.
- `Ext¹` here is the book's elementary `Z¹/B¹` construction from this very
  problem, not Mathlib's derived-functor `Ext` — verify the Lean uses the
  in-book definitions, not an unrelated Mathlib `Ext`.

## Verification

- `progress/items.json` parses and both items carry a `coverage` field with a
  `derived` array covering every book sub-part ((a)-(d) for 3.9.1, (a)-(b) for
  3.9.4).
- Every recorded `lean_decl` resolves; both modules build sorry-free with clean
  `#print axioms` on the headline decls.
- Status/`coverage_note` are internally consistent; a coverage report exists
  under `progress/reviews/`.
- A follow-up `feature` issue exists **iff** a genuine sub-part gap was found.
