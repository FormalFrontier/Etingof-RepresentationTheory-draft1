## Current state

Between 2026-05-17 and 2026-05-18 a six-PR wave landed the bulk of the per-(field, orientation) refactor of Chapter 6. This issue audits the **per-graph construction** half of that wave — four PRs adding the K_{1,4}, Ẽ₆, and Ẽ₇ base cases.

The infrastructure half (cycle case + shared lemmas + subgraph transfer) is reviewed in a sibling issue.

## Deliverables

Audit the following four PRs together:

- **#2798** `feat(Ch6 #2796): F-generic K_{1,4} (D̃₄) — starRepGen, indecomposability, star_not_finite_type_F` — 384 adds. F-generic only (not yet (F, Q)-generic — that's PR #2802, still in pr-repair queue). Lives in `FieldGenericInfiniteType.lean`. Introduces `starEmbed1_F`, `starEmbed2_F`, `starEmbedDiag_F`, `starEmbedNilp_F`, `starRepGen`, `starRepGen_isIndecomposable`, `star_not_finite_type_F`.

- **#2808** `feat(Ch6 #2806): etilde6Rep_kQ — orientation-generic Ẽ₆ construction` — 363 adds. New file `FieldGenericETilde6.lean`. Introduces `embed2to3_AB_F`, `embed2to3_CA_F`, `etilde6Gamma_F`, their reverse maps, `etilde6LeafProj_F`, the match-based `etilde6RepMap_kQ` and `etilde6Rep_kQ` + dim vec lemma. **Zero indecomposability content here** — Sub A is construction-only.

- **#2809** `feat(Ch6 #2807): etilde6Rep_kQ_isIndecomposable + etilde6_not_finite_type_per_kQ` — 148 adds. Adds `etilde6Rep_kQ_isIndecomposable` (carrying a single `sorry` inherited from the wave-54 framework wall — see `progress/indecomposability-framework-investigation.md`) and the final per-(F, Q) theorem `etilde6_not_finite_type_per_kQ`.

- **#2810** `feat(Ch6 #2792): etilde7Rep_kQ + etilde7_not_finite_type_per_kQ — orientation-generic Ẽ₇` — 416 adds. New file `FieldGenericETilde7.lean`. Single-PR (combining Sub A + Sub B). Introduces `embed3to4_ABC_F`, `embed3to4_ACD_F`, `etilde7Arm1Embed_F`, their reverse maps, `etilde7RepMap_kQ`, `etilde7Rep_kQ`, dim vec, `etilde7Rep_kQ_isIndecomposable` (carrying a framework-wall sorry), `etilde7_not_finite_type_per_kQ`.

Audit with these specific questions:

1. **Framework-wall sorry classification** — each of `etilde6Rep_kQ_isIndecomposable` (in #2809) and `etilde7Rep_kQ_isIndecomposable` (in #2810) carries one `sorry` claimed to be inherited from the wave-54 wall. Verify by reading the docstring of each, then cross-referencing against `progress/indecomposability-framework-investigation.md` Sections 1 and 5. Confirm no *new* sorries snuck in beyond the framework wall.

2. **Dedup candidates between FieldGenericETilde6 and FieldGenericETilde7** — both files define block-inclusion embeddings and their reverse projections. Concrete candidates:
   - `embed2to3_AB_F` (`(a, b) ↦ (a, b, 0)`) and `embed3to4_ABC_F` (`(x, y, z) ↦ (x, y, z, 0)`) — both are zero-extensions on a prefix block. A general `prefixBlockEmbed_F : (Fin (a*(m+1)) → F) →ₗ (Fin (b*(m+1)) → F)` (when `a ≤ b`) would subsume both. Worth lifting, or premature?
   - `embed2to3_AB_reverse_F` and `embed3to4_ABC_reverse_F` — analogous projections. Same question.
   - `etilde6LeafProj_F` (defined in FieldGenericETilde6, reused in FieldGenericETilde7) and the impending `starFirst_F` in PR #2802 (flagged in `progress/2026-05-17T13-41-48Z_f2bc2de3.md` as identical-semantics duplicates). When PR #2802 lands, one of them should go.

3. **`starRepGen` (#2798) vs the per-(F, Q) shape** — #2798 produces an `F`-generic construction but uses the canonical K_{1,4} orientation. The follow-up PR #2802 generalises to (F, Q). The two should compose cleanly: `starRepGen` over the canonical Q should be definitionally / theorem-equal to `starRep_kQ` instantiated at the canonical Q. Is that bridge already in #2798 or planned for #2802?

4. **Naming consistency across per-graph files** — `etilde6RepMap_kQ` (FieldGenericETilde6) vs `etilde7RepMap_kQ` (FieldGenericETilde7) vs `cycleRepGen_kQ` (FieldGenericInfiniteType, from #2799). Are private match-based map functions named consistently? Are public APIs (`Rep_kQ`, `_isIndecomposable`, `_dimVec`, `_not_finite_type_per_kQ`) using the same convention?

5. **File-structure forward planning** — D̃₅ Sub A (#2803) is in flight (PR #2813), Sub B (#2804) is blocked on it. T(1,2,5) (#2793) is `replan`-labelled. Once those land, there'll be 5 per-graph files (the impending D̃₅ + T(1,2,5) follow the established pattern). Is FieldGenericInfiniteType.lean accumulating too much shared infrastructure (1147 lines already), or is the split between shared / per-graph clean? Flag if a Section-2 split (`FieldGenericShared.lean` / `FieldGenericCycle.lean`) would help.

For each cleanup target, post a follow-up issue (one per concrete change, kept small). Cross-link from the review summary on this issue. If no follow-ups, post a clean-bill comment and close.

## Context

- Parent wave: PR #2794 records the worker-led decomposition of #2773 into 6 per-(F, Q) sub-issues.
- Sibling review issue covers the infrastructure half: #2799 (cycle + `compl_le_forces_eq`) and #2805 (subgraph transfer + `restrictOrientationViaEmb`).
- Open follow-on PRs (already in pr-repair queue, will likely land before this review claims): #2802 (K_{1,4} per-(F, Q)), #2813 (D̃₅ per-(F, Q) Sub A).
- Files: `EtingofRepresentationTheory/Chapter6/FieldGenericInfiniteType.lean` (lines 333-660 cover #2798), `FieldGenericETilde6.lean` (329 lines, all from #2808 + #2809), `FieldGenericETilde7.lean` (338 lines, all from #2810).
- Framework wall investigation: `progress/indecomposability-framework-investigation.md` (Sections 1 and 5 are the canonical references).

## Verification

- Each follow-up issue has a clear, atomic deliverable.
- The review summary links to each follow-up (or explicitly notes "no follow-ups: clean bill of health").
- This issue is closed after the summary is posted.
