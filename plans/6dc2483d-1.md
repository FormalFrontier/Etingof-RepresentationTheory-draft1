## Current state

Between 2026-05-17 and 2026-05-18 a six-PR wave (#2798, #2799, #2805, #2808, #2809, #2810) landed the bulk of the per-(field, orientation) refactor of Chapter 6's forbidden-subgraph infinite-type theorems. Total: ~1996 lines added, spanning three files (`FieldGenericInfiniteType.lean`, `FieldGenericETilde6.lean`, `FieldGenericETilde7.lean`).

This issue audits the **shared infrastructure** half of that wave: `compl_le_forces_eq`, the cycle base case, `restrictOrientationViaEmb`, and `subgraph_infinite_type_transfer_per_kQ` — the pieces every later per-graph construction (#2798, #2808, #2809, #2810, plus impending D̃₅ / T(1,2,5)) builds on.

The per-graph constructions themselves are reviewed separately (see sibling review issue).

## Deliverables

Audit the following two PRs together:

- **#2799** (PR title: `feat(Ch6 #2787): cycle_not_finite_type_per_kQ + compl_le_forces_eq`) — 331 adds. Introduces:
  - `compl_le_forces_eq` in `FieldGenericInfiniteType.lean` (the cross-cutting submodule-equality lemma used by every per-(F, Q) indecomposability argument).
  - `cycleRepGen_kQ`, `cycleRepGen_kQ_isIndecomposable`, `cycleRepGen_kQ_dimVec`, `cycle_not_finite_type_per_kQ` — the cycle (Ã_n) base case.

- **#2805** (PR title: `feat(Ch6 #2774): per-(F, Q) subgraph transfer (deliverable 1 of 3)`) — 354 adds. Introduces:
  - `restrictOrientationViaEmb : (Fin m ↪ Fin n) → Quiver (Fin n) → Quiver (Fin m)`.
  - `restrictOrientationViaEmb_isOrientationOf`.
  - `subgraph_infinite_type_transfer_per_kQ` — the per-(F, Q) form of subgraph transfer that the per-(k, Q) assembly (deliverable 2 of #2774) will dispatch through.

Audit each PR with these specific questions:

1. **`compl_le_forces_eq` API surface** — the lemma takes explicit submodule arguments (per the docstring, implicit inference fails when the carrier is `(cycleRepGen_kQ …).obj a`). Are downstream callers (in #2808/#2809/#2810) using it correctly? Are there hidden alternative spellings (e.g., the reverse direction `compl_le_forces_eq'`) that should be added now, before more callers ossify the API?

2. **`subgraph_infinite_type_transfer_per_kQ` API readiness for deliverable 2 of #2774** — read the sketch in #2774's body (the case-analysis structure of `not_posdef_infinite_type` at `InfiniteTypeConstructions.lean:10661`: degree-4 vertex, list-cycle, adjacent branches, single branch (T(p,q,r)), non-adjacent branches). For each case, is the existing transfer API expressive enough, or are extra helpers needed now? In particular, check whether `restrictOrientationViaEmb` composition / identity / range lemmas are missing.

3. **`cycleRepGen_kQ` reuse strategy** — the per-(F, Q) cycle case is the first concrete consumer of `compl_le_forces_eq`. Is the proof structured so that the three-step pattern (adjacent-vertex equality / constancy across vertices / nilpotent invariance at the closing edge) is reusable, or is the closing-edge analysis inlined? The Ẽ₆ / Ẽ₇ / D̃₅ proofs may want a packaged version.

4. **Naming consistency in the new shared file** — `cycleRepGen_kQ` vs `starRepGen` (no `_kQ` suffix because #2798 is per-F only) vs `subgraph_infinite_type_transfer_per_kQ`. Document the convention (`_F` = field-generic, `_kQ` = field-and-orientation-generic, no suffix = original ℂ-only) somewhere visible, or call out PRs that diverge.

5. **Sorry classification** — confirm zero new sorries in these two PRs. The progress notes (da0a10c6 for #2799, 489a142e implicitly) claim zero; verify against current `main`.

For each cleanup target identified, post a follow-up issue using `coordination plan` (one issue per concrete change, keep them small — naming docs, missing API, dedup proposals each get their own issue). Cross-link from the review summary comment on this issue. If the review finds no issues worth filing, post a clean-bill-of-health comment and close.

## Context

- Parent wave: PR #2794 records the worker-led decomposition of #2773 into 6 per-(F, Q) sub-issues; these two PRs (#2799 cycle, #2805 transfer) are the infrastructure half.
- Sibling review issue covers the per-graph half: #2798, #2808, #2809, #2810.
- Downstream consumer: #2774 deliverables 2 (per-(k, Q) assembly) and 3 (wire into Theorem 2.1.2), which together close #2401.
- Files: `EtingofRepresentationTheory/Chapter6/FieldGenericInfiniteType.lean` (1147 lines, the shared file). Lines roughly: 27-160 (nilpotent shift), 660-687 (`compl_le_forces_eq`), 688-900 (cycle case), 901-1147 (transfer infrastructure).

## Verification

- Each follow-up issue has a clear, atomic deliverable.
- The review summary comment links to each follow-up (or explicitly notes "no follow-ups: clean bill of health").
- This issue is closed after the summary is posted.
