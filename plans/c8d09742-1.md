## Current state

The most recent sorry-landscape snapshot is
`progress/sorry-landscape.md` (wave 60, generated via #2855 on
2026-05-18T02:25Z). Since wave 60 closed, **10 substantive PRs
have merged on `main`** (counting feature, refactor, doc, and
review PRs; excluding progress notes and planner-cycle no-ops).
This is exactly the 10+ substantive-PR threshold for wave-61
summarize.

The wave-61 story is dominated by the **per-(F, Q) ↔ Theorem 2.1.2
bridge** infrastructure layer landing in earnest:

- The two missing per-(F, Q) API stubs for the forbidden-subgraph
  family (K_{1,4} and T(1,2,5)) shipped (PR #2878), giving Chapter 6
  the complete six-leaf set required by the bridge.
- The three per-(F, Q) subgraph dispatch wrappers (chordless cycle
  / triangle / star_subgraph) shipped (PR #2882), giving the outer
  case-analysis assembly clean leaf functions to call.
- Both PRs cleared light-audit review (#2879 for the proj-sibling
  pre-step in PR #2871; #2885 for the dispatch wrappers + stubs in
  PR #2878 / #2882) — all three audits returned PASS verdicts.

Wave 61 was the **first wave since wave-60 with no broken-main
events** and the first wave in which the per-(F, Q) infrastructure
work moved at a faster pace than the D̃₅ Sub B cascade. The D̃₅
Sub B chain still has all its open work (`/replan` on #2839, #2850,
#2853, #2851), but two cleanup hoists (#2862, #2863) landed to
prepare the file layout for the eventual 31-case fill in #2853.

Substantive PRs since wave 60 (chronological by merge time):

1. **#2861** — `review(Ch6 #2858)`: audit D̃₅ Sub B cascade
   helpers (#2835 / #2843 / #2854). PASS verdict.
2. **#2862** — `refactor(Ch6 #2860)`: hoist `core_F` / `core3_F` /
   `gamma_containment_F` to top-level (pre-step for #2853).
3. **#2863** — `refactor(Ch6 #2859)`: hoist `embed_sum_zero_F` /
   `center_decomp_F` to `FieldGenericStar` (pre-step for #2853).
4. **#2866** — `review(Ch6 #2864)`: audit #2862 / #2863 cleanup
   hoists. PASS verdict.
5. **#2867** — `doc(Ch5 #2841)`: forward
   `isIdempotentElem_eq_zero_of_trace_eq_zero` to Mathlib
   PR 39523.
6. **#2871** — `feat(Ch6 #2868)`: projection-based reversed-leaf-
   edge sibling lemmas (`d5tilde_core_F_proj1/2`,
   `d5tilde_core3_F_proj1/2`) for D̃₅ Sub B's #2853 31-case fill.
7. **#2878** — `feat(Ch6 #2875 D1)`: per-(F, Q) API stubs for
   K_{1,4} and T(1,2,5) (**+2 sorries**, both tracked).
8. **#2879** — `review(Ch6 #2868)`: audit proj-sibling lemmas in
   PR #2871. PASS verdict.
9. **#2882** — `feat(Ch6 #2877 pre-step)`: per-(F, Q) subgraph
   dispatch wrappers (star_subgraph / triangle / chordless_cycle)
   for the Theorem 2.1.2 bridge.
10. **#2885** — `review(Ch6 #2877 pre-step)`: audit per-(F, Q)
    dispatch wrappers + K_{1,4}/T(1,2,5) API stubs. PASS verdict.

Sorry-count motion expected since wave 60:

- **+2**: `star_not_finite_type_per_kQ` body
  (`Chapter6/FieldGenericStar.lean:542`, tracked by #2789/#2801)
  and `t125_not_finite_type_per_kQ` body
  (`Chapter6/FieldGenericT125.lean:39`, tracked by #2793).
  Both API stubs introduced by PR #2878.
- Total: **18 sorries on `main`** as of cycle c8d09742 (verified
  by `grep -c '^\s*sorry\s*$'`). Wave 60 closed at 16.

Per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard at end of wave 61:

- All six per-(F, Q) forbidden-subgraph leaves exist (4 proven, 2
  stubbed pending framework-wall resolution at #2436).
- All three per-(F, Q) subgraph dispatch wrappers exist (PR #2882).
- The outer case-analysis assembly
  `not_posdef_infinite_type_per_kQ` is the only remaining gap on
  the bridge — tracked by #2877 (worker-self-decomposes into 6
  sub-issues per its body; the audit (#2885) confirmed two of
  those sub-deliverables — D2.cycle ~150 lines and D2.degree4
  ~50 lines — are worker-ready).

Other chains, status motion since wave 60:

- **D̃₅ Sub B cascade**: file layout cleaned up (#2862, #2863) and
  projection-based sibling lemmas added (#2871) ahead of #2853's
  31-case fill. #2869 (γ⁻¹-coupled containment lemma) closed
  without code as a structural finding. Remaining work
  (#2839/#2850/#2853/#2851) all in `/replan`.
- **K_{1,4} per-(F, Q) indecomposability** (#2789/#2801/#2823):
  still `replan`. PR #2878's K_{1,4} stub uses this when filled.
- **T(1,2,5)** (#2793): still `replan`. PR #2878's T(1,2,5) stub
  uses this.
- **Mathlib upstream trackers**: #2841 forwarded to Mathlib PR
  39523 in PR #2867 (deliverable complete on our side); #2564
  still external-blocked.
- **Schur-Weyl L_i C-tier**: no movement. γ.A #2694 still
  CONFLICTING. All downstream blocked.
- **Wall 3 (Ch5 SpechtModuleBasis)**: no movement.

Notable structural events of wave 61:

- **First wave since wave-60 with no broken-main events**. The
  refactor hoists (#2862, #2863) landed cleanly without
  squash-merge collisions.
- **Per-(F, Q) bridge tempo accelerated**. Six substantive PRs in
  this wave touched the per-(F, Q) layer (#2862, #2863, #2871,
  #2878, #2882, plus the three review PRs), vs. only two
  cleanup-touching PRs in wave 60's tail.
- **Review-PR ratio at all-time high**. Four of the ten
  substantive PRs are review audits (#2861, #2866, #2879, #2885)
  — a 4:6 review:feature ratio. All four returned PASS verdicts
  with no follow-up issues filed. The audit cadence is paying off
  by catching potential issues at the pre-step layer.

A fresh wave-61 landscape is warranted to take stock: the sorry
count has shifted (+2), the per-(F, Q) Theorem 2.1.2 bridge is now
within one outer-assembly issue of closing, and the design-walls
doc should reflect Wall 1's unchanged status alongside the
infrastructure progress.

## Deliverables

Produce `progress/sorry-landscape.md` (overwrite the existing
wave-60 snapshot) covering:

1. **Header**: wave number (61), generation timestamp, source
   issue number.
2. **Executive summary**: total sorry count and per-chapter /
   per-file distribution. Verify via:
   ```bash
   grep -rn '^\s*sorry\s*$' EtingofRepresentationTheory/
   ```
   Compare against wave 60's 16. Expected net delta: **+2 sorries
   in Chapter 6** (`FieldGenericStar.lean` +1,
   `FieldGenericT125.lean` +1).
3. **Merges since wave 60** (~10 substantive PRs). Tabulate in
   chronological order with a sorry-impact column. Distinguish
   substantive merges from progress / no-op PRs. Verify the list
   via:
   ```bash
   gh pr list --state merged --limit 50 --json number,title,mergedAt \
     --jq '.[] | select(.mergedAt > "2026-05-18T02:25:17Z")'
   ```
4. **Per-(F, Q) ↔ Theorem 2.1.2 bridge scoreboard**: visualise
   the current state of the bridge layer — six leaves (4 proven,
   2 stubbed), three dispatch wrappers, one remaining outer
   assembly. Cross-reference each component to its merge PR or
   open issue.
5. **Per-chapter breakdown**: Ch2, Ch5, Ch6 (and any others with
   active sorries). Cross-reference each sorry to its blocking
   issue.
6. **Per-file detail** for each active sorry: file path, line
   number, theorem name, current status, blocking issue (if any),
   short description.
7. **Updated design walls section** (write
   `progress/design-walls-wave61.md`): refresh from wave 60 with
   per-(F, Q) bridge progress; confirm Wall 1 status (still 6+
   waves stale, no movement); note that wave 61 was the first
   broken-main-free wave since wave 60; note the high review-PR
   ratio (4 of 10) and that all four audits passed.
8. **Open critical paths**: identify the top 3 unblocked items a
   new worker should pick up. **#2877** (Theorem 2.1.2 bridge
   outer assembly — worker-ready per #2885's audit) is the
   headline pick. The remainder is shaped by `/replan` triage on
   the D̃₅ Sub B chain (#2839 / #2850 / #2853 / #2851), the
   K_{1,4} per-(F, Q) chain (#2789 / #2801 / #2823), and T(1,2,5)
   (#2793).

## Context

- Files to write/overwrite: `progress/sorry-landscape.md` and
  `progress/design-walls-wave61.md`.
- Previous summarize: #2855 (closed 2026-05-18T02:25Z) → wave-60
  landscape file `progress/sorry-landscape.md` and
  `progress/design-walls-wave60.md`.
- Substantive PR list for the table: use the list in "Current
  state" above; verify via the `gh pr list` command in
  Deliverable 3. Filter out progress notes
  (`progress: planner cycle …`) and any pure-progress PRs.
- Sorry-count verification: the bare `^\s*sorry\s*$` regex from
  the wave-60 template (string `sorry` in comments doesn't count).
- Threshold sanity check: at exactly 10 substantive PRs, this is
  the canonical trigger — don't second-guess by re-counting.

## Verification

- `progress/sorry-landscape.md` exists with the wave-61 header
  and an accurate sorry count matching the `grep -c '^\s*sorry\s*$'`
  numbers (expected: 18 total).
- Every active sorry is cross-referenced to a tracking issue (or
  flagged as untracked if none exists).
- The PR table covers all substantive merges in
  (2026-05-18T02:25Z, now].
- `progress/design-walls-wave61.md` exists with the per-wall
  status updates.
- This issue closes itself on PR merge via `Closes #<this issue>`.
