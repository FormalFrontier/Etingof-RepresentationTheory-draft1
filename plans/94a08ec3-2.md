## Current state

Planner-led decomposition pre-step for #2853 — companion of the
projection-based sibling-lemmas pre-step. Originally noted in
`progress/20260518T034747Z_6fc85452.md` and
`progress/reviews/2026-05-18-d5tilde-subB-cleanup-hoist.md` §3: of the
31 non-canonical orientation sub-cases #2853 must close, the
**4 sub-cases at line 802** of `d5tildeRep_kQ_leaf_equalities`
(`e23 = 3→2` reversed central edge, both leaf pairs canonical) need a
γ⁻¹-coupled containment lemma that mirrors `d5tilde_gamma_containment_F`
but uses the γ⁻¹ direction.

The `gammaInv_embed*_F` closed-form identities (PR #2843) supply the
algebraic backbone:
- `gammaInv_embed1_plus_embed2_F` collapses
  `d5tildeGammaInv_F (starEmbed1_F x + starEmbed2_F y) = ...` into a
  closed form usable for the pull case.
- `gammaInv_embed1_plus_embedNshift_F` is the partner for the
  `nilpotentShiftLinGen`-routed component.

This sub-issue pre-stages the γ-reversed containment lemma so #2853's
e23-reversed branch (line 802 of the case-split, 4 sub-cases) can call
it directly.

## Deliverables

Add one top-level theorem to
`EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean`
(immediately after `d5tilde_gamma_containment_F` at line 598):

`d5tilde_gamma_containment_F_inv` — γ⁻¹-coupled analogue of
`d5tilde_gamma_containment_F` for the `e23 = 3→2` reversed orientation.

Replace the canonical γ-push hypothesis

```lean
(hMain_23 : ∀ (x : Fin (2 * (m + 1)) → F), x ∈ Wmain ⟨2, by omega⟩ →
    d5tildeGamma_F F m x ∈ Wmain ⟨3, by omega⟩)
```

with the reversed γ⁻¹-pull hypothesis

```lean
(hMain_32 : ∀ (w : Fin (2 * (m + 1)) → F), w ∈ Wmain ⟨3, by omega⟩ →
    d5tildeGammaInv_F F m w ∈ Wmain ⟨2, by omega⟩)
```

(or whatever the equivalent γ⁻¹ form is in `FieldGenericStar.lean` /
`FieldGenericD5Tilde.lean` — `gammaInv` is implicit in
`gammaInv_embed*_F`; the worker should pick the concrete pull
formulation that matches #2843's lemmas).

Conclusion mirrors `d5tilde_gamma_containment_F` (the four containment
facts `Wmain ⟨0⟩ → Wmain ⟨4⟩`, etc.) — possibly with the
`nilpotentShiftLinGen` routing adjusted to match the closed form of
`gammaInv_embed1_plus_embedNshift_F`.

Proof skeleton:
- Take `x ∈ Wmain ⟨0⟩` and push via canonical `hMain_02` to get
  `starEmbed1_F x ∈ Wmain ⟨2⟩` — same as the canonical version's
  first move.
- Then **either** chain through `gammaInv_embed1_plus_embed2_F` (with
  `y = 0`) to land in `Wmain ⟨3⟩` via the inverse direction, **or**
  use the γ⁻¹-pull from `Wmain ⟨3⟩` to derive containment at v=2 and
  apply `d5tilde_core3_F` directly. The worker should pick whichever
  threading matches the actual e23-reversed sub-case call signature.

Expected proof body length: ~25–35 lines, comparable to
`d5tilde_gamma_containment_F` (lines 628–648).

## Context

- Parent (decomposing): #2853 (blocked on #2850 `replan`).
- Companion sub-issue: projection-based reversed-leaf-edge siblings
  (`d5tilde_core_F_proj*` / `d5tilde_core3_F_proj*`) — filed
  separately.
- γ⁻¹ closed-form lemmas (the key infrastructure):
  - `gammaInv_embed1_plus_embed2_F`
    (`FieldGenericD5Tilde.lean:426`).
  - `gammaInv_embed1_plus_embedNshift_F`
    (`FieldGenericD5Tilde.lean:445`).
  - Both landed in PR #2843.
- Canonical-direction spec reference:
  `d5tilde_gamma_containment_F` (`FieldGenericD5Tilde.lean:598`)
  (landed in PR #2862).
- Mixed-case analysis (why this sub-case is structurally different
  from the leaf-edge-reversed siblings): #2850 issue body.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericD5Tilde`
  passes.
- The new theorem is universally quantified over `(F, Q, hOrient, m,
  Wmain, Wother)` matching the canonical-direction signature shape.
- No new `sorry`s introduced; total `FieldGenericD5Tilde.lean` sorry
  count stays at 6 raw / 2 declarations.
- No `set_option maxHeartbeats` bumps required.

## Notes for the worker

- This issue is **not blocked** on #2850. Land it whenever.
- The exact form of the γ⁻¹ pull hypothesis and the call-site
  threading depends on how `gammaInv_embed*_F` is consumed in
  practice. The worker should look at how `gamma_from_embed1_F` /
  `gamma_from_embed2_F` are used in `d5tilde_gamma_containment_F`'s
  body (lines 628–648) and write the γ⁻¹ analogue with the
  symmetric structure.
- If the worker discovers the γ⁻¹ form does not need to be a
  separate top-level theorem (e.g., if it inlines cleanly in the
  e23-reversed sub-case of #2853 in only a few lines), close this
  issue with a comment to that effect and let #2853 absorb the
  scope back. The split is a pre-staging convenience, not a hard
  requirement.
