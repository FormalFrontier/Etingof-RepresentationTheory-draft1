## Current state

PR #2871 (closes #2868) landed at 2026-05-18T07:03Z, adding four
projection-based reversed-leaf-edge sibling lemmas to
`EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean`
(Section 5e′, lines 590-714):

- `d5tilde_core_F_proj1` — `e02 = 2→0` reversed (`starFirst_F` pull
  at v=2 → v=0).
- `d5tilde_core_F_proj2` — `e12 = 2→1` reversed (`starSecond_F` pull
  at v=2 → v=1).
- `d5tilde_core3_F_proj1` — `e43 = 3→4` reversed (`starFirst_F` pull
  at v=3 → v=4).
- `d5tilde_core3_F_proj2` — `e53 = 3→5` reversed (`starSecond_F`
  pull at v=3 → v=5).

Each is a 3-line proof: apply the projection hypothesis, rewrite
using the left-inverse identities `starFirst_F_starEmbed1_F = id`
/ `starFirst_F_starEmbed2_F = 0` (or `starSecond_F` analogues),
conclude.

**Documented deviation from spec.** The issue body sketched a joint
conclusion `x ∈ Wmain ⟨0⟩ ∧ z ∈ Wmain ⟨1⟩` mirroring the canonical
`d5tilde_core_F` conjunction. The worker landed half-conclusion
lemmas (`_proj1` returns only `x ∈ W ⟨0⟩`; `_proj2` returns only
`z ∈ W ⟨1⟩`) and argued in the progress note that the joint
conclusion is not derivable from a single-pull + single-push
hypothesis set. Planner's "Notes for the worker" explicitly
allowed minimisation; the worker's framing is "strictly more
flexible than a joint-conclusion lemma."

**Companion closure.** Sub-issue #2869 (γ⁻¹-coupled containment
lemma for `e23 = 3→2` reversed central edge) was closed at
2026-05-18T09:04Z without code — the inv-direction containment
cannot factor as a sibling lemma of the canonical mirror shape;
#2853 will inline `gammaInv_embed*_F` calls directly per #2850's
strategy notes. This audit must therefore verify that the
proj-sibling design and the inline γ⁻¹ strategy are jointly
sufficient for the 31 non-canonical orientation sub-cases of
`d5tildeRep_kQ_leaf_equalities`.

## Deliverables

Light audit. No code changes expected unless follow-up issues
are filed.

1. **Statement fidelity + projection identity correctness.** For
   each of the four `_proj*` lemmas, verify:
   - Hypothesis signature `hW_xy : ∀ w, w ∈ W ⟨source⟩ →
     projection w ∈ W ⟨target⟩` correctly reflects its target
     sub-case's local context (e.g., for e02 reversed, the pull
     direction is 2→0, not 0→2).
   - The `(F, Q, hOrient, m, W, hW_*, x, z, hmem)`
     parameterisation matches the canonical `d5tilde_core_F` /
     `d5tilde_core3_F` signatures in the same file
     (`FieldGenericD5Tilde.lean:399-589`).
   - The four left-inverse identities consumed
     (`starFirst_F_starEmbed1_F = id`,
      `starFirst_F_starEmbed2_F = 0`,
      `starSecond_F_starEmbed1_F = 0`,
      `starSecond_F_starEmbed2_F = id`) exist in
     `FieldGenericStar.lean` (around lines 420-450) with the
     consumed signatures and have closed proofs (no `sorry`).

2. **Half-conclusion design audit.** Re-verify the worker's
   structural argument (progress/20260518T070149Z_4141c541.md,
   "Deviation from spec" section) that the joint conclusion is
   not derivable from a single pull + single push hypothesis set.
   Confirm whether the half-conclusion form is the right
   primitive for #2853, or whether a refactor (e.g., adding a
   "both leaves reversed" wrapper that composes `_proj1` +
   `_proj2` to recover the full joint conjunction) would be
   useful pre-staging.

3. **Downstream readiness assessment for #2853.** For each of the
   five `sorry` positions in `d5tildeRep_kQ_leaf_equalities`
   (`FieldGenericD5Tilde.lean:802` / `804` / `806` / `808` /
   `810`, covering 1/2/4/8/16 sub-cases respectively, totalling 31),
   identify which proj-sibling(s) apply and what additional inline
   reasoning each sub-case needs. Specifically:
   - Confirm the e23-reversed branch (line 806, 4 sub-cases) is the
     only one needing inline `gammaInv_embed*_F` rather than a
     proj-sibling call.
   - Note any sub-cases where neither proj-sibling nor inline γ⁻¹
     appears sufficient (would indicate a gap to file separately).
   - Quantify the worker's claim from the wave-60 audit §3 that
     "24 of 31 sub-cases need direction-reversed sibling lemmas"
     against the actually-landed proj-sibling shapes.

## Context

- Parent (blocked): #2853 (blocked on #2850 `replan`).
- Closed companion: #2869 (structural finding — no code).
- Wave-60 cleanup audit (analogous pattern): #2864 (verdict on
  #2858, audit report at
  `progress/reviews/2026-05-18-d5tilde-subB-cleanup-hoist.md`).
- Worker progress note documenting the deviation:
  `progress/20260518T070149Z_4141c541.md`.
- Closure progress note for #2869:
  `progress/2026-05-18T09-04-43Z_bcc5a2ad.md`.

Reference files:
- `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean:590-714`
  — the four new proj-sibling theorems (Section 5e′).
- `EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean:420-450`
  (approximate) — the left-inverse projection identities consumed.
- `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean:399-589`
  — the canonical `d5tilde_core_F` / `d5tilde_core3_F` /
  `d5tilde_gamma_containment_F` (template for proj-sibling
  parameterisation).
- `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean:559-800`
  — the canonical-orientation branch of
  `d5tildeRep_kQ_leaf_equalities` (template for #2853 sub-cases).
- `EtingofRepresentationTheory/Chapter6/FieldGenericD5Tilde.lean:802-810`
  — the five `sorry` positions covering the 31 non-canonical
  sub-cases.

## Verification

- `lake build EtingofRepresentationTheory.Chapter6` builds clean
  on `main` at the audit point (current `main` is `a7d3430`).
- Sorry count in `FieldGenericD5Tilde.lean` unchanged at the
  PR #2871 landing: **6 raw / 2 declarations**.
- Audit report committed at
  `progress/reviews/2026-05-18-d5tilde-2871-proj-siblings.md`.
- Verdict comment (PASS / PASS-with-follow-up / FAIL) posted on
  this issue.

## Notes for the worker

- This is a **light review**, not a re-proof. No code changes
  expected on this branch unless follow-up issues are filed (in
  which case file the issues separately and post the audit's
  verdict comment).
- The worker progress note 4141c541 makes a structural argument
  for the half-conclusion form; the audit should agree or
  disagree explicitly in writing. If disagreeing, file a
  follow-up issue suggesting the refactor — do not silently
  modify PR #2871's lemmas.
- Deliverable 3 (downstream readiness for #2853) is the highest
  value output of this audit. It feeds directly into the eventual
  /replan resolution of #2850 / #2853.
