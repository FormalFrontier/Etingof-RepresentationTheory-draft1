## Current state

Two sister PRs landed back-to-back filling the bodies of the Ẽ₆/Ẽ₇
embedder helpers introduced as stubs by PR #2941:

- **PR #2945** (merge commit `aaaaaad`, +279/-15, closing #2937):
  `embed_etilde6_in_tree_per_kQ` body — T(2, 2, 2) embedding in
  `Chapter6/FieldGenericETilde6.lean:372-559`.
- **PR #2947** (merge commit `23e7a62`, +347/-19, closing #2938):
  `embed_etilde7_in_tree_per_kQ` body — T(1, 3, 3) embedding in
  `Chapter6/FieldGenericETilde7.lean:356-640`.

Both are structural mirrors of the already-audited
`embed_t125_in_tree_per_kQ` (`FieldGenericT125.lean:71-436`, audited
PASS by #2928 → `progress/reviews/2026-05-19-embed-t125-in-tree-per-kQ.md`)
but for different shapes: the T(1,2,5) audit verified bit-identity to a
universal counterpart (`embed_t125_in_tree`); these two have no
universal counterpart (no `embed_etilde6_in_tree` /
`embed_etilde7_in_tree` exists in
`Chapter6/InfiniteTypeConstructions.lean` — verified by grep). The
construction here is genuinely new per-(F, Q) code, written to the
**pattern** of `embed_t125_in_tree_per_kQ` rather than line-cloned
from a universal.

This audit covers both PRs in one report — they share an author
(claude-code), a parent issue (#2932), a structural template, and a
dispatch shape (`subgraph_infinite_type_transfer_per_kQ` →
`etildeN_not_finite_type_per_kQ` →
`restrictOrientationViaEmb_isOrientationOf`). Combining the audit
saves duplicated context-loading; the report should still verify each
PR independently against the dimensions below.

## Deliverables

Produce a combined audit report at
`progress/reviews/2026-05-19-embed-etilde6-and-etilde7-in-tree-per-kQ.md`
covering both PRs, with each dimension marked PASS / CONCERN / FAIL
**per-PR** (two columns or two sub-sections) and file-line citations.

**D1 — Signature fidelity to the PR #2941 stub.** For each helper,
verify the on-`main` signature
(`FieldGenericETilde6.lean:372-395` for Ẽ₆;
`FieldGenericETilde7.lean:356-380` for Ẽ₇) matches the stub introduced
by PR #2941 parameter-for-parameter. The Ẽ₆ stub takes seven vertices
`(v₀, c₁, d₁, c₂, d₂, c₃, d₃)` with six edge hypotheses and three
same-level distinctness hypotheses plus three off-center distinctness
hypotheses; the Ẽ₇ stub takes eight vertices `(v₀, u₁, c₂, d₂, e₂,
c₃, d₃, e₃)` with seven edge hypotheses and corresponding distinctness
hypotheses. Compare against `git show 6fc9d40:EtingofRepresentationTheory/
Chapter6/FieldGenericETilde6.lean` / `…ETilde7.lean` to confirm the
parameter list is unchanged from the stub.

**D2 — Distinctness lattice exhaustiveness.** Both helpers must
derive every vertex-pair distinctness fact needed by the
`hembed : ∀ i j, etildeNAdj i j = adj (φ i) (φ j)` step, and every
non-edge fact between distinct vertices that should not be adjacent
in the host graph. For Ẽ₆ (T(2,2,2), 7 vertices, C(7,2)=21 pairs):
verify the helper derives all 6 same-arm adjacencies + 6 triangle
non-edges (v₀-d_i via apex c_i, c_i-c_j via apex v₀) + 6 distance-3
non-edges (c_i-d_j for i ≠ j via 4-vertex paths) + 3 distance-4
non-edges (d_i-d_j for i ≠ j via 5-vertex paths) = the full 21
relations. For Ẽ₇ (T(1,3,3), 8 vertices, C(8,2)=28 pairs): verify
the larger lattice (7 triangle + 6 distance-3 + 5 distance-4 + 2
distance-5 + 1 distance-6, per the docstring at lines 350-352).
Spot-check that each `acyclic_no_triangle` / `acyclic_path_nonadj`
invocation passes the correct apex / path argument and the correct
Nodup / edges witnesses (no transposed arguments). It is acceptable to
spot-check 3-4 entries per dimension rather than re-derive every
linarith.

**D3 — Embedding map + injectivity + `hembed`.** For each helper,
verify the `φ_fun : Fin N → Fin n` match-on-`i` definition
(`FieldGenericETilde6.lean:527-531` for Ẽ₆ with N=7;
`FieldGenericETilde7.lean:604-608` for Ẽ₇ with N=8) assigns vertices
to the indices stated in the docstring:

- Ẽ₆: `0→v₀, 1→c₁, 2→d₁, 3→c₂, 4→d₂, 5→c₃, 6→d₃` — confirms a T(2,2,2)
  shape (center `0` connected to `1,3,5`; arms `1-2`, `3-4`, `5-6`).
- Ẽ₇: `0→v₀, 1→u₁, 2→c₂, 3→d₂, 4→e₂, 5→c₃, 6→d₃, 7→e₃` — confirms a
  T(1,3,3) shape (center `0` connected to `1,2,5`; arms `1` alone,
  `2-3-4`, `5-6-7`).

Cross-check the adjacency map against `etilde6Adj` / `etilde7Adj`
definitions (find them in
`Chapter6/InfiniteTypeConstructions.lean`) — for every pair `(i, j)`
with `etildeNAdj i j = 1`, the embedding image pair must satisfy
`adj (φ i) (φ j) = 1`; for every pair with `etildeNAdj i j = 0`, the
image pair must satisfy `adj (φ i) (φ j) = 0`. The `hembed` proof uses
`fin_cases i <;> fin_cases j <;> simp <;> norm_num <;> linarith` with
a curated fact list — verify the fact list contains every fact used
(spot-check 3-4 cases; if any case fails the linarith locally without
a missing fact, that is a FAIL).

**D4 — Dispatch chain shape.** Both helpers close with
```
exact subgraph_infinite_type_transfer_per_kQ φ F Q
  (etildeN_not_finite_type_per_kQ F (restrictOrientationViaEmb φ Q)
    (restrictOrientationViaEmb_isOrientationOf φ hembed hOrient))
```
(`FieldGenericETilde6.lean:557-559`; `FieldGenericETilde7.lean:638-
640`). Verify the argument order matches the universal pattern in
`embed_t125_in_tree_per_kQ` (`FieldGenericT125.lean:434-436`); verify
`hembed` is the same `etildeNAdj i j = adj (φ i) (φ j)` predicate that
`restrictOrientationViaEmb_isOrientationOf` expects; verify the leaf
`etildeN_not_finite_type_per_kQ` is the per-(F, Q) variant (not the
universal). Confirm the `hOrient` argument threading is correct.

**D5 — Build + isolated stub-fill consistency.** From `main` =
`23e7a62`, run `lake exe cache get` then build
`EtingofRepresentationTheory.Chapter6.FieldGenericETilde6` and
`EtingofRepresentationTheory.Chapter6.FieldGenericETilde7` (targeted,
not full mathlib). Both should compile with no errors and only the
expected per-file warnings (the unrelated documented `sorry` in
`etilde6Rep_isIndecomposable` / `etilde7Rep_isIndecomposable`
transitively gated by these helpers — see file docstrings around
`FieldGenericETilde6.lean:266-289` and `FieldGenericETilde7.lean:247-
271`). Confirm no new sorries were introduced by these two PRs in
either file.

## Context

Parent issue: #2932 (proof body for
`non_adjacent_branches_leaf_case_per_kQ` via case-split). This is
sub-1 + sub-2 done; sub-3 is #2939 (Phase 1/2 of the case-split body,
now unblocked since both its dependencies closed). The Ẽ₆ helper
addresses the "three length-2 arms" case (one arm is the chain, one
from `v₀`'s side, one from `w`'s side); the Ẽ₇ helper addresses
"length-1 + length-3 + length-3" arms.

Comparable audit templates:
- `progress/reviews/2026-05-19-embed-t125-in-tree-per-kQ.md` (audit
  of the sibling T(1,2,5) embedder, PASS via bit-identity to universal)
- `progress/reviews/2026-05-19-d2-wrapper-trilogy-per-kQ.md` (audit of
  three sibling D2 wrappers, multi-PR combined audit precedent)

Both helpers are now reachable from `non_adjacent_branches_leaf_case_per_kQ`'s
docstring-documented dispatch table; once issue #2939's body is filled
they become live dependencies of PR #2943's outer assembly.

## Verification

Combined audit report committed at
`progress/reviews/2026-05-19-embed-etilde6-and-etilde7-in-tree-per-kQ.md`
on a branch named after the session UUID. Explicit per-PR PASS /
CONCERN / FAIL verdicts on each of D1-D5. Targeted builds of
`Chapter6.FieldGenericETilde6` and `Chapter6.FieldGenericETilde7`
green. PR opened via `coordination create-pr` with `Closes
#<this-issue>` and auto-merge enabled.
