## Current state

The most recent sorry-landscape summary is `progress/sorry-landscape.md`
(wave 59, generated via #2811 on 2026-05-17T17:58Z). Since wave 59
closed, **10 substantive PRs have merged on `main`** (excluding
progress notes and planner-cycle no-ops). This crosses the 10+
substantive-PR threshold for wave-60 summarize.

The wave-60 story is dominated by the D̃₅ Sub B decomposition chain
plus an unusual structural event: **main was broken twice in one
day** and required two repair PRs to fix.

Substantive PRs since wave 59 (chronological by merge time):

1. **#2831** — `doc(Ch6)`: document _kQ / _per_kQ / _F naming
   convention in `FieldGenericInfiniteType` module docstring.
2. **#2842** — `refactor(Ch5 Theorem5_22_1)`: reduce
   `youngSym_action_vanishes_off_block` heartbeat bumps from
   800000/800000 to 400000/200000 (resolves a deliverable from
   review #2832).
3. **#2802** — `feat(Ch6 #2800)`: direction-aware projections +
   `starRep_kQ` + dim vec. Was CONFLICTING for one cycle; pr-repair
   resolved; landed in planner cycle 79ff34ce.
4. **#2813** — `feat(Ch6)`: `d5tildeRep_kQ` construction + dim vec
   (sub of #2790).
5. **#2835** — `feat(Ch6 #2804)`: D̃₅ per-(F, Q) helpers + API
   stubs (partial); decomposes #2804 into helpers landing + #2834
   for proof body.
6. **#2843** — `feat(Ch6 #2834)`: closed-form γ⁻¹ identity helpers
   (`gammaInv_embed*_F`) for D̃₅ Sub B.
7. **#2844** — `refactor(Ch6)`: split `FieldGenericInfiniteType.lean`
   into shared / cycle / star modules (+793/-644, 8 files).
8. **#2848** — `fix(Ch6 #2846)`: repair main — move `starRep_kQ` to
   `FieldGenericStar`, dedupe D̃₅ projections. (Broken-main repair
   triggered by the squash-merge interaction between #2802 and the
   file split #2844.)
9. **#2852** — `fix(Ch6 #2839)`: repair broken main — fix Section
   5b/5d in `FieldGenericD5Tilde`. (Second broken-main repair,
   different root cause.)
10. **#2854** — `feat(Ch6 #2850)`: canonical-orientation case of
    `d5tildeRep_kQ_leaf_equalities` + API stub; decomposes #2850
    into canonical case (done) + 31 non-canonical cases in #2853.

Sorry-count motion expected since wave 59:
- **+1**: `d5tildeRep_kQ_isIndecomposable` stub in
  `FieldGenericD5Tilde.lean` (added by #2835).
- **+5**: 5 case-split sorries in `d5tildeRep_kQ_leaf_equalities`
  (one per "reversed at level k" branch — added by #2854, tracked
  by #2853).
- Net delta over wave 59: roughly **+6 sorries** in
  `FieldGenericD5Tilde.lean`, all tracked by issues
  (#2839 / #2853 / #2851), all on the D̃₅ Sub B path.

D̃₅ Sub B decomposition tree (the headline cascade of wave 60):

```
#2804 (parent) — D̃₅ per-(F, Q) indecomposability
  ├── PR #2835: helpers + API stubs (DONE)
  └── #2834 (proof body)
       ├── PR #2843: γ⁻¹ identity helpers (DONE)
       └── #2839 (main proof body) — replan
            ├── #2850 (sub-A: leaf-equality derivation) — replan
            │    ├── PR #2854: canonical case + API stub (DONE)
            │    └── #2853 (sub-A2: 31 non-canonical cases) — blocked on #2850
            └── #2851 (sub-B: assembly via N-invariance + propagation) — blocked on #2850
```

Other chains, status motion since wave 59:

- **K_{1,4} per-(F, Q) indecomposability** (#2801, #2823, #2789):
  partially unblocked by #2802 landing (K_{1,4} construction); the
  bridge `starRep_kQ ↔ starRepGen` (#2823) and the
  indecomposability proof (#2801) are both `replan` and pending
  triage. Recent repair PR #2848 resolved a collision between
  #2802 and #2813 (`starProj1_F` / `starProj2_F` redefinitions);
  this also partially addressed the dedupe #2821 (which has been
  in PR #2849 with failing CI).
- **Subgraph transfer** (#2774): unchanged (deliverable 1 landed in
  wave 59; 2 + 3 still `replan`).
- **T(1,2,5)** (#2793): unchanged (`replan`).
- **Schur-Weyl L_i C-tier**:
  - γ.A (#2694): still CONFLICTING, pr-repair queue.
  - γ.B (#2693), C-4a aggregation (#2708), C-4c (#2612), and
    Schur-Weyl #5 / #6 (#2482 / #2483): unchanged, all blocked
    further upstream.
  - β.3 review #2832 closed in wave 59 → produced lint cleanup
    #2842.
- **Wall 3 (Ch5 SpechtModuleBasis)**: unchanged. R2.b.i (#2769)
  still in `replan`; R2.b.ii (#2770), R2.c (#2703) still blocked.

Notable structural events of wave 60:

- **Two broken-main events in one day** (#2846, #2839): the squash-
  merge of #2802 against the rebased file split #2844 created the
  first breakage; a stale rebase in #2839's branch against the
  Section-5 refactor created the second. Worth a brief note in the
  design walls doc — this is a coordination signal, not a math
  signal.
- **#2844 file split**: substantial refactor (+793/-644 across 8
  files) splitting `FieldGenericInfiniteType.lean` into
  shared / cycle / star modules. New file boundaries land in the
  per-(F, Q) chain; future per-graph work should target the new
  files.
- **#2854 canonical-case proof template**: the core_F / core3_F /
  gamma_containment_F helpers proven in #2854 are the template
  #2853 will reuse for the 31 non-canonical cases.

A fresh wave-60 landscape is warranted to take stock — the sorry
count has shifted (the D̃₅ Sub B sub-chain isolates several
sorries), the file layout changed under the per-(F, Q) refactor,
and the design-walls doc should reflect the second consecutive wave
in which Wall 1 has been "structurally dissolved but not yet
formally closed".

## Deliverables

Produce `progress/sorry-landscape.md` (overwrite the existing
wave-59 snapshot) covering:

1. **Header**: wave number (60), generation timestamp, source issue
   number.
2. **Executive summary**: total sorry count and per-chapter /
   per-file distribution. Verify via:
   ```bash
   grep -rn '^\s*sorry\s*$' EtingofRepresentationTheory/
   ```
   Compare against wave 59's 10. Net delta and an explanation of
   any change (D̃₅ Sub B chain adds stubs in
   `Chapter6/FieldGenericD5Tilde.lean`, all tracked).
3. **Merges since wave 59** (~10 substantive PRs). Tabulate in
   chronological order with a sorry-impact column. Distinguish
   substantive merges from progress / no-op PRs.
4. **D̃₅ Sub B decomposition tree**: visualise the cascade in the
   "Current state" section above (parent → grandchild). Cross-
   reference each node to its merge PR or open issue.
5. **Per-chapter breakdown**: Ch2, Ch5, Ch6 (and any others with
   active sorries). Cross-reference each sorry to its blocking
   issue.
6. **Per-file detail** for each active sorry: file path, line
   number, theorem name, current status, blocking issue (if any),
   short description.
7. **Updated design walls section** (write
   `progress/design-walls-wave60.md`): refresh from wave 59 with
   D̃₅ Sub B cascade narrative; note the two-broken-mains-in-a-day
   structural signal (without overclaiming it as a wall); confirm
   Wall 1 status unchanged from wave 59.
8. **Open critical paths**: identify the top 3 unblocked items a
   new worker should pick up. The K_{1,4} bridge #2823 and the
   D̃₅ leaf-equality work in #2850 / #2853 / #2851 are gated by
   `/replan` triage at the moment, so the immediate critical path
   is shaped by which of those `/replan` triage produces.

## Context

- Files to write/overwrite: `progress/sorry-landscape.md` and
  `progress/design-walls-wave60.md`.
- Previous summarize: #2811 (closed 2026-05-17T17:58Z) → wave-59
  landscape file `progress/sorry-landscape.md` and
  `progress/design-walls-wave59.md`.
- Substantive PR list for the table (use the list in "Current
  state" above; verify via `gh pr list --state merged --limit 50
  --json number,title,mergedAt --jq '.[] | select(.mergedAt >
  "2026-05-17T17:58:11Z")'`).
- Sorry-count verification: the bare `^\s*sorry\s*$` regex from
  the wave-59 template (string `sorry` in comments doesn't count).

## Verification

- `progress/sorry-landscape.md` exists with the wave-60 header
  and an accurate sorry count matching the `grep -c '^\s*sorry\s*$'`
  numbers.
- Every active sorry is cross-referenced to a tracking issue (or
  flagged as untracked if none exists).
- The PR table covers all substantive merges in (2026-05-17T17:58Z,
  now].
- `progress/design-walls-wave60.md` exists with the per-wall
  status updates.
- This issue closes itself on PR merge via `Closes #<this issue>`.
