## Current state

`Chapter6/FieldGenericNonAdjacentBranches.lean` has two
residual-sorry comment blocks that document remaining work in
the `non_adjacent_branches_leaf_case_per_kQ` body but neither
cites the four sub-issue numbers that already track the work,
and the bullet enumeration in both blocks is 3 items wide while
the sub-issue decomposition is 4 items wide.

The wave-63 audit
`progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-cases-e-aa-and-e-s1c4.md`
flagged this as **D3 CONCERN** (lower comment block) and
**D5(a) CONCERN** (cross-file alignment): the audit passed
overall but documented this mismatch as something a downstream
reader has to reconcile by hand.

### Affected comment blocks

**Block 1 — upstream enumeration (lines 994-1003):**
```
-- Remaining cases (still `sorry`):
--
-- * E.aa siblings at `chain.length = 3` with mixed arm degrees
--   (requires Ẽ₇ at `v₀` or `w` plus extension-degree splits, or
--   the unavailable D̃₆ helper for all-leaves at chain.length=3).
-- * `chain.length = 5` (any sub-case): needs D̃₈ helper, not in
--   the per-(F, Q) library.
-- * `chain.length ≥ 6` all-leaves: needs general D̃_n helper.
--
-- Tracked by follow-up sub-issues spawned from #2955.
```

**Block 2 — residual-sorry site (lines 1098-1102):**
```
-- ===== Residual: tracked by follow-up sub-issues =====
-- Remaining: chain.length = 3 mixed arm cases (E.ab, E.bb),
-- chain.length = 5 (any), chain.length ≥ 6 all-leaves.
-- Requires Ẽ₇ extension splits, D̃₆, or D̃₈+/parametric D̃_n
-- helpers — see follow-up sub-issues.
```

### Sub-issue decomposition (currently `blocked` on #2955)

| Issue   | Configuration                                  | Helper needed              |
|---------|------------------------------------------------|----------------------------|
| #2974   | `chain.length = 3`, all-leaves                 | `D̃₆` per-(F, Q) helper    |
| #2976   | `chain.length = 3`, mixed arm degrees (E.ab/E.bb) | Ẽ₇ extension splits      |
| #2977   | `chain.length = 5` (any)                       | `D̃₈` per-(F, Q) helper    |
| #2978   | `chain.length ≥ 6`, all-leaves                 | general `D̃_n` per-(F, Q) helper |

The 3-bullet vs 4-issue mismatch comes from the `chain.length = 3`
case splitting along the all-leaves / mixed-arm axis: Block 2
lumps everything `chain.length = 3` under "mixed arm cases (E.ab,
E.bb)" but the all-leaves variant (#2974) is *not* mixed and is
*not* E.ab/E.bb. Block 1's bullet 1 mentions the all-leaves
variant only parenthetically.

## Deliverables

Edit only `Chapter6/FieldGenericNonAdjacentBranches.lean`. Two
surgical changes inside existing `--`-style comment blocks:

1. **Block 1 (lines 994-1003)**: split bullet 1 into two bullets
   (one for `chain.length = 3` all-leaves → `#2974`, one for
   `chain.length = 3` mixed arm degrees → `#2976`). Cite the issue
   numbers inline next to each bullet. Cite `#2977` and `#2978`
   next to the existing `chain.length = 5` and `chain.length ≥ 6
   all-leaves` bullets. Keep the final "Tracked by follow-up
   sub-issues spawned from #2955" line.

2. **Block 2 (lines 1098-1102)**: rewrite the bullet enumeration
   to match Block 1's 4-bullet shape (so the two blocks stay in
   sync). Cite `#2974`, `#2976`, `#2977`, `#2978` inline. Keep the
   block's header line `===== Residual: tracked by follow-up
   sub-issues =====` unchanged.

Do **not** modify the `let _ := …` discard block immediately below
(lines 1103-1107), the `sorry` (line 1108), or anything else
outside the two comment blocks.

## Context

* Precedent for this kind of comment-block refresh: PR #2983 /
  issue #2982 (the wave-63 docstring refresh for the same file).
  PR #2983 fixed the file-level docstring but not these two inner
  comment blocks.
* The sub-issues #2974 / #2976 / #2977 / #2978 are all currently
  `blocked` on #2955 (which itself is `replan`-labelled). This
  chore does not unblock anything — it only makes the comment
  blocks navigable for the downstream agent/reader who will pick
  up the sub-issues once #2955 resolves.
* The audit report at
  `progress/reviews/2026-05-19-non-adjacent-branches-leaf-case-cases-e-aa-and-e-s1c4.md`
  (sections D3 and D5(a)) has the exact wording of the existing
  blocks and the rationale for the 4-way split.

## Verification

* `git diff main -- Chapter6/FieldGenericNonAdjacentBranches.lean`
  shows changes only inside lines 994-1003 and 1098-1102 (both
  `--` comment blocks). No changes to `theorem`/`def`/`sorry`
  lines.
* `lake build EtingofRepresentationTheory.Chapter6.FieldGenericNonAdjacentBranches`
  passes (comment-only edits should compile unchanged, but
  verify).
* No new `sorry`s introduced. Existing `sorry` at line 1108 left
  intact.
* Sorry count for the file unchanged at 1.

## Notes

* Strictly chore-level. Mark the PR with `chore(Ch6):` title
  prefix to match the convention of PRs #2983, #2986.
* Do not expand scope. Other docstrings / comments in this file
  were refreshed by PR #2983; this issue is solely for the two
  residual-sorry comment blocks the previous refresh did not
  touch.
