## Current state

PR #2891 closed #2889 (D2.degree4 of #2877), landing
`degree_ge_4_infinite_type_per_kQ` in
`EtingofRepresentationTheory/Chapter6/FieldGenericStar.lean` at
the end of the file (~60 lines of proof body, no `sorry`). This
is the first concrete per-(F, Q) outer-assembly piece on the
Theorem 2.1.2 bridge and establishes the structural recipe that
the remaining D2 sub-deliverables (D2.cycle, D2.adjacent,
D2.single, D2.nonadj, D2.outer) and D3 will mirror.

Two structural decisions in PR #2891 deserve a light audit
before downstream D2 sub-PRs commit to the same recipe:

1. **Placement = cross-import** (option 1 of #2889's three
   options). PR #2891 added
   `import EtingofRepresentationTheory.Chapter6.FieldGenericCycle`
   to `FieldGenericStar.lean` and placed the new wrapper there
   (rather than option 2 — add `FieldGenericStar` import to
   `FieldGenericCycle.lean` and place there; or option 3 — new
   file `FieldGenericDegree4.lean`).
2. **`[IsAlgClosed F]` propagation** is required because
   `star_subgraph_not_finite_type_per_kQ`
   (`FieldGenericStar.lean:586`) carries `[IsAlgClosed F]` and is
   one of the two dispatch targets. PR #2891 carries it on the
   wrapper signature; the `triangle_*_per_kQ` branch does not
   need it but inherits it via the unified signature.

Per the wave-61 audit pattern (#2861, #2866, #2879, #2885), the
first per-(F, Q) outer-assembly PR gets a PASS-style audit that
both confirms correctness and surfaces any pattern decisions that
the downstream worker(s) on #2877 (the remaining five D2 sub-cases
+ D3) need to mirror.

## Deliverables

1. **Statement fidelity audit.** Confirm
   `degree_ge_4_infinite_type_per_kQ`
   (`Chapter6/FieldGenericStar.lean:644-697`) mirrors
   `degree_ge_4_infinite_type`
   (`Chapter6/InfiniteTypeConstructions.lean:4064-4101`) at the
   statement level: argument order, hypotheses (`hsymm`, `hdiag`,
   `h01`, `v`, `hv`), per-(F, Q) signature tail (`(F : Type)
   [Field F] [IsAlgClosed F] (Q : @Quiver (Fin n)) [∀ a b, Subsingleton …]
   (hOrient : @Etingof.IsOrientationOf n Q adj)`), and canonical
   per-(F, Q) conclusion shape (compare against
   `etilde6_not_finite_type_per_kQ` at
   `FieldGenericETilde6.lean:319`). Note any deviation.

2. **Proof body correctness.** Walk the two-case proof against
   the `_kQ`-free original:
   - **Setup (neighbor extraction)**: `set S := …`, `Finset.exists_subset_card_eq`,
     `equivFinOfCardEq`, `neighbors`, `h_adj`, `h_ne`, `h_inj` —
     verify these match the `_kQ`-free proof and that
     `h_inj : Function.Injective neighbors` is derived correctly.
   - **Pairwise-non-adjacent branch** (`by_cases h_indep`,
     positive case): dispatches to
     `star_subgraph_not_finite_type_per_kQ` with `(F, Q, hOrient)`
     appended. Verify the argument order against
     `FieldGenericStar.lean:586`.
   - **Triangle branch** (`push_neg`, negative case): extracts
     `(i, j, h_adj_ij)`, derives `adj (neighbors i) (neighbors j) = 1`
     via the `h01` case-split, then dispatches to
     `triangle_infinite_type_per_kQ` with `(F, Q, hOrient)` appended.
     Verify the argument order against `FieldGenericCycle.lean:398`
     and that `hij : neighbors i ≠ neighbors j` is derived
     correctly from `adj (neighbors i) (neighbors j) = 1` plus
     `hdiag`.

3. **Pattern documentation for downstream D2 sub-PRs.** Record:
   - **Placement decision rationale**: was option 1 (cross-import
     in `FieldGenericStar.lean`) the right choice for D2.degree4
     vs. option 2 (cross-import in `FieldGenericCycle.lean`) or
     option 3 (new file)? Note whether the file growth in
     `FieldGenericStar.lean` is acceptable or whether option 3
     should be preferred for the next sub-deliverable (D2.cycle
     uses `chordless_cycle_*_per_kQ` only, so no cross-import
     needed — placement in `FieldGenericCycle.lean` is natural;
     audit should record this asymmetry).
   - **`[IsAlgClosed F]` propagation rule of thumb**: which
     downstream D2 sub-deliverables need it (those that dispatch
     to a leaf carrying `[IsAlgClosed F]` — currently `star_*` and
     `t125_*`) vs. those that do not (pure cycle/triangle paths).
     This recipe matters for D2.adjacent and D2.single which
     dispatch to D̃₅ leaves (also `[IsAlgClosed F]`).
   - **Build verification**: confirm `lake build EtingofRepresentationTheory.Chapter6`
     is green on the post-#2891 `main` (commit `733bd0d`). No new
     warnings; no new `sorry`s.

## Verification

- Verdict file in `progress/<timestamp>_<session>.md` with:
  - PASS/FAIL determination on deliverables 1 + 2.
  - Pattern-decision notes (deliverable 3) suitable for citation
    by future D2 sub-PR authors.
  - Build-green confirmation with job count.
- No code changes expected. If the audit finds a real defect,
  file a follow-up sub-issue with `agent-plan` label — do **not**
  push changes to `main`.
- Sorry-count delta: **0** (review session, no code changes).

## Context

- **PR under review**: #2891 (commit `354a3c3`, merged into
  `733bd0d` on `main`).
- **Parent feature issue**: #2889 (closed by #2891). Body cites
  audit #2885 deliverable 3 as the worker-readiness gate.
- **Umbrella**: #2877 — five remaining D2 sub-deliverables
  (D2.cycle, D2.adjacent, D2.single, D2.nonadj, D2.outer) +
  D3 still open. The next planner cycle is likely to pre-split
  D2.cycle (~150 lines per audit #2885) if this review's
  pattern-decision notes land cleanly.
- **Canonical per-(F, Q) shape reference**:
  `etilde6_not_finite_type_per_kQ` at `FieldGenericETilde6.lean:319`.
- **Pattern precedent for light audits in this wave**: #2861,
  #2866, #2879, #2885 (all PASS).
