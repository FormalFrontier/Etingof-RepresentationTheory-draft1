## Current state

PR #2921 (merged 2026-05-18) closed the critical-path slice of
**#2877** ("`not_posdef_infinite_type_per_kQ` assembly + Theorem
2.1.2 bridge"). The PR is ~350 lines across two files and is the
single largest piece of the Theorem 2.1.2 forward bridge to land
in waves 60-62. It has **not been independently audited**;
all earlier #2877 audits (#2885, #2892/#2894) covered upstream
infrastructure (D2.degree4, pre-step wrappers, API stubs), and
the D2.single follow-up cascade audits (D2.single sub-A/B/C/D)
have all targeted helper-level slices in `FieldGenericTpqr.lean`,
not the outer assembly.

What landed in PR #2921:

- **`acyclic_branch_not_posdef_infinite_type_per_kQ`**
  (`Chapter6/FieldGenericAssembly.lean:113-161`) — branch-point
  geometry dispatch (adjacent / unique / ≥2 non-adjacent),
  mirroring `acyclic_branch_not_posdef_infinite_type`
  (`Chapter6/InfiniteTypeConstructions.lean:10609`). Dispatches by
  name to:
  - `adjacent_branches_infinite_type_per_kQ` (already merged via
    #2900),
  - `single_branch_not_posdef_infinite_type_per_kQ` (merged via
    #2903, still carries the `single_branch_leaf_case_both_extend_per_kQ`
    sorry from #2906 → #2905),
  - `non_adjacent_branches_infinite_type_per_kQ` (sorry-bodied;
    tracked by #2919 → #2922 + #2923).
- **`not_posdef_infinite_type_per_kQ`**
  (`Chapter6/FieldGenericAssembly.lean:177-237`) — outer
  case-analysis over degree-4 / cycle / branch / `acyclic_deg_le_2`,
  mirroring `not_posdef_infinite_type`
  (`Chapter6/InfiniteTypeConstructions.lean:10661`). The
  `h_no_adj_branch` derivation inside the acyclic-branch case
  and the cycle-closure index normalisation
  (`getLast_eq_getElem` rewrite at line 213-214) are the only
  non-mechanical edits versus the universal original.
- **Theorem 2.1.2 forward bridge**
  (`Chapter2/Theorem2_1_2.lean:153-179`) — closes the
  `private lemma not_posdef_not_HasFiniteRepresentationType`
  body that was `sorry` at the start of the PR. Combines
  `not_posdef_infinite_type_per_kQ` with the contrapositive of
  `HasFiniteRepresentationType.finite_dimVectors`
  (`Theorem2_1_2.lean:111`). The `hn : 1 ≤ n` derivation from
  `x ≠ 0` is implicit in the universal flow but explicit here
  (lines 165-168).

Sorry-count delta on PR #2921: **closes 1 sorry** (the
Theorem 2.1.2 D3 bridge); introduces 0. Sorry-count on `main`
post-merge: 19 (down from 20). The bridge is now sorry-free
modulo the per-(F, Q) leaf bodies (Wall 1 + #2919 + #2905-chain).

The audit cadence pattern from waves 60-61 (#2861, #2866, #2879,
#2885, #2892/#2894) calls for a PASS-style audit of the first
end-to-end assembly PR. PR #2921 is that PR for the outer
assembly + bridge layer — it is the highest-leverage audit
target currently outstanding because every Chapter 2 statement
of the form `¬ posdef → ¬ HasFiniteRepresentationType k Q`
will dispatch through this path.

## Deliverables

Light audit. **No code changes expected** unless follow-up
issues are filed.

1. **Statement fidelity, outer assembly.** Confirm
   `not_posdef_infinite_type_per_kQ`
   (`Chapter6/FieldGenericAssembly.lean:177-237`) mirrors
   `not_posdef_infinite_type`
   (`Chapter6/InfiniteTypeConstructions.lean:10661-`) at the
   case-analysis level:
   - the 4 cases match (deg ≥ 4 / cycle / branch / `acyclic_deg_le_2`),
   - argument order and hypotheses propagated correctly into each
     dispatch leaf (`F`, `Q`, `hOrient` tail consistent with each
     `_per_kQ` leaf's expected signature),
   - the cycle-case `getLast_eq_getElem` rewrite (line 213-214)
     is a genuine index-form conversion (universal version's
     `hclose` uses `getLast` whereas
     `graph_with_list_cycle_infinite_type_per_kQ` expects
     `get ⟨length - 1, _⟩`), not a smuggled strengthening.
2. **Statement fidelity, acyclic-branch dispatch.** Confirm
   `acyclic_branch_not_posdef_infinite_type_per_kQ`
   (`Chapter6/FieldGenericAssembly.lean:113-161`) mirrors
   `acyclic_branch_not_posdef_infinite_type`
   (`Chapter6/InfiniteTypeConstructions.lean:10609-`):
   - 3-case dispatch (adjacent / unique / ≥2 non-adjacent) is
     correct,
   - `h_no_adj_branch` derivation (lines 148-152) from
     `h_adj_branch` (negated) + `h_deg` is sound,
   - the `non_adjacent_branches_infinite_type_per_kQ` call
     (line 160) passes `h_no_adj` as the
     `h_no_adj_branch` argument and not some other constraint.
3. **Bridge correctness.** Confirm
   `not_posdef_not_HasFiniteRepresentationType`
   (`Chapter2/Theorem2_1_2.lean:153-179`) correctly composes:
   - `hn : 1 ≤ n` derivation from `x ≠ 0` (lines 165-168) is
     not a vacuous case-split (Fin 0 → ℤ has only the zero
     function, so `x ≠ 0` indeed forces `n ≥ 1`),
   - the contrapositive direction is wired correctly: `h_inf`
     gives `¬ Set.Finite { dim vectors }`, and the conclusion
     applies `(HasFiniteRepresentationType.finite_dimVectors ...).subset`
     in the right direction,
   - the `subset` argument's witness construction (lines 178-179)
     correctly produces a `FinQuiverRep`-style triple from the
     `QuiverRepresentation`-style one — verify the
     `Module.Finite.equiv (hV_dim v).some.symm` step is sound
     (the universe-0 `Fin (d v) → F` target gives finiteness in
     the source, not the other way).
4. **Sorry-propagation accounting.** Confirm:
   - `not_posdef_infinite_type_per_kQ` carries no direct sorry,
   - its only transitively-sorry'd dispatch leaf is
     `acyclic_branch_not_posdef_infinite_type_per_kQ` (transitively
     via `non_adjacent_branches_infinite_type_per_kQ` and
     `single_branch_not_posdef_infinite_type_per_kQ`'s
     `..._both_extend_per_kQ` sorry),
   - the Theorem 2.1.2 D3 bridge introduces no new sorry of its
     own,
   - `Chapter2/Theorem2_1_2.lean:173` (the line cited in #2877's
     deliverables list) is no longer `sorry` post-merge.
5. **Light pattern audit (optional).** Compare the outer
   case-analysis prologue (lines 197-228 of
   `FieldGenericAssembly.lean`) with the universal original's
   prologue — flag any non-trivial divergences for posterity.
   The expected pattern is a 1:1 case-tree mirror with the only
   substantive edits being the per-(F, Q) tail (`F Q hOrient`
   propagation) and the index-form conversion noted in
   deliverable 1.

## Context

- Reviewed PR: PR #2921 — `feat(Ch2 #2877 partial):
  not_posdef_infinite_type_per_kQ outer assembly + Theorem 2.1.2
  forward bridge` (merged 2026-05-18, ~350 additions).
- Files of interest:
  - `EtingofRepresentationTheory/Chapter6/FieldGenericAssembly.lean`
    (full file ≈ 240 lines post-PR; primary audit surface).
  - `EtingofRepresentationTheory/Chapter2/Theorem2_1_2.lean`
    (`not_posdef_not_HasFiniteRepresentationType` lines 153-179
    and `HasFiniteRepresentationType.finite_dimVectors` lines
    109-131 for the contrapositive partner).
- Reference originals in
  `EtingofRepresentationTheory/Chapter6/InfiniteTypeConstructions.lean`:
  - `not_posdef_infinite_type` (`:10661`)
  - `acyclic_branch_not_posdef_infinite_type` (`:10609`).
- Parent tracker: #2877 (residual scope is the sorry-bodied leaves
  #2919 → #2922 + #2923 and the #2905 chain — both outside the
  scope of this audit).
- Wave-61 audit precedents: #2885 (per-(F, Q) subgraph dispatch
  wrappers + K_{1,4}/T(1,2,5) API stubs), #2894 (D2.degree4
  audit). The placement and `[IsAlgClosed F]` propagation
  recipes settled by those audits should now be visible in PR
  #2921's outer assembly — confirm consistency, flag any drift.

## Verification

- `lake build EtingofRepresentationTheory.Chapter2.Theorem2_1_2`
  passes on `main`.
- `lake build EtingofRepresentationTheory.Chapter6.FieldGenericAssembly`
  passes on `main`.
- Audit output is a PR comment-style summary in the review
  issue's closing PR (use the standard
  PASS/PASS-with-notes/FAIL verdict format from #2885 / #2894).
- If any deliverable surfaces a real concern (signature drift,
  wrong dispatch leaf, sorry-bookkeeping mismatch), file a
  follow-up `feature` or `agent-fix` issue rather than holding
  this review open.

## Notes for the reviewer

- This is a **light audit**. The PR is mechanical at the outer
  level (case-tree mirror of a universal-version theorem) and
  the per-(F, Q) leaves it dispatches to have all been audited
  individually upstream (#2885, #2894, plus the D2.adjacent /
  D2.cycle / D2.single direct merges). The expected verdict is
  PASS or PASS-with-notes; FAIL would indicate a structural
  issue worth a `replan` on #2877.
- Do **not** audit the sorry'd leaves themselves
  (`non_adjacent_branches_infinite_type_per_kQ` body or the
  `..._both_extend_per_kQ` body). Those are tracked by #2919 +
  #2905 respectively and will receive their own audits when
  their PRs land. The job here is the outer-assembly + bridge
  layer only.
- The Theorem 2.1.2 bridge (`Chapter2/Theorem2_1_2.lean:153-179`)
  is the user-facing closure: any audit finding here is higher
  severity than findings in `FieldGenericAssembly.lean`, since
  the bridge is the API consumers (and the next pipeline stage)
  will see.
