# References: Unit in an associative algebra

## Mathlib Coverage (exact)

- `Algebra`
- `One`

Mathlib algebras are unital by default (`Ring A` includes `One A`). The unit is built into the `Ring` structure.
