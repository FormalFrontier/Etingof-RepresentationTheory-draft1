# References: Universal enveloping algebra

## Mathlib Coverage (exact)

- `UniversalEnvelopingAlgebra`

`UniversalEnvelopingAlgebra R L` is the universal enveloping algebra of a Lie algebra L over R.

## External Dependencies

- **Universal enveloping algebra U(g) of a Lie algebra: construction and basic properties** (undergraduate_prerequisite)
  Mathlib (exact): `UniversalEnvelopingAlgebra`
  `UniversalEnvelopingAlgebra R L` constructs U(g). Basic algebraic properties available.
- **PBW (Poincaré-Birkhoff-Witt) theorem: the universal enveloping algebra U(g) has an ordered monomial basis** (external_result)
  Mathlib (partial): `UniversalEnvelopingAlgebra`
  `UniversalEnvelopingAlgebra R L` exists with its universal property. The PBW theorem (that U(g) has a basis of ordered monomials, equivalently that the canonical map g → U(g) is injective) is NOT proved in Mathlib.
